services:
  api:
    volumes:
      - ./backend/app:/app/app:ro
    command:
      - python
      - -m
      - uvicorn
      - app.main:create_app
      - --factory
      - --reload
      - --host
      - 0.0.0.0
      - --port
      - "8000"

