# 面向用户、管理员的企业级在线商城以及AI智能客服

## 0 目录

- 1 角色
  - 1.1 角色分类
  - 1.2 角色权限矩阵
  - 1.3 角色详细定义
  - 1.4 AI 客服会话架构
- 2 前端界面
  - 2.1 首页界面
  - 2.2 整体布局与全局展示契约
  - 2.3 商品详情界面
  - 2.4 消息与客服界面
  - 2.5 购物车界面
  - 2.6 订单结算界面
  - 2.7 店铺界面
  - 2.8 我的界面
  - 2.9 我的订单界面
  - 2.10 单个订单详情界面
  - 2.11 售后退款申请页面
  - 2.12 用户端完整页面、路由与通用交互契约
    - 2.12.3.1 用户登录页面
    - 2.12.3.2 用户注册页面
    - 2.12.3.3 认证页面共用体验、安全与可访问性
  - 2.13 管理后台界面设计
- 3 后端系统设计
  - 3.1 后端设计目标与范围
  - 3.2 后端总体架构
  - 3.3 技术栈与基础组件
  - 3.4 后端工程目录设计
  - 3.5 数据架构总览
  - 3.6 MySQL 数据库设计规范
  - 3.7 MySQL 业务数据表设计
  - 3.8 PostgreSQL 与 pgvector 设计
  - 3.9 Redis 设计
  - 3.10 对象存储设计
  - 3.11 API 设计规范
  - 3.12 API 接口清单
  - 3.13 身份认证、权限与安全
  - 3.14 核心业务流程与状态机
  - 3.15 消息与客服系统设计
  - 3.16 AI 智能客服总体设计
  - 3.17 店铺客服 Agent 设计
  - 3.18 专属客服 Agent 设计
  - 3.19 Agent 记忆系统设计
  - 3.20 Agent 上下文窗口管理
  - 3.21 RAG 知识检索设计
  - 3.22 MCP 设计
  - 3.23 Skill 系统设计
  - 3.24 Multi-Agent 设计
  - 3.25 Agent 权限与安全网关
  - 3.26 异步任务与事件驱动
  - 3.27 可观测性设计
  - 3.28 AI 评估体系
  - 3.29 业务监控与告警
  - 3.30 测试体系
  - 3.31 Docker 与部署设计
  - 3.32 稳定性与容灾
  - 3.33 性能与容量规划
  - 3.34 开发阶段与验收标准

## 1 角色

### 1.1 角色分类

用户角色分为两类：**用户（普通消费者）** 与 **管理员（运营/管理人员）**。

> **说明**：所有访问者必须先完成注册并登录才能使用商城核心功能（商品浏览为公开访问，但加入购物车、下单、使用AI客服等交互功能均需登录）。

此外，系统内设 **一类店铺作用域 AI 客服角色：店铺客服 Agent**；商品详情、店铺页和订单页统一使用“店铺客服/联系商家”入口，底层进入当前用户与当前店铺的唯一会话。它用于本店商品咨询、参数对比、店铺政策、受限订单解释和店内推荐。平台级全场景 AI 服务不作为独立用户角色，也不设置首页悬浮入口，其能力由消息中心内固定置顶的 **“专属客服”会话**承载，详见 2.4。

| AI 角色                        | 服务入口           | 核心能力                         | 权限边界             |
| :----------------------------- | :----------------- | :------------------------------- | :------------------- |
| **店铺客服 Agent** | 商品页、店铺页、订单“联系商家”入口 | 本店商品/政策咨询、当前用户本店订单解释、店内推荐 | 店铺作用域；交易/经营/资金零写入，仅可幂等创建本店人工工单并提出待确认记忆候选 |

---
### 1.2 角色权限矩阵

> **身份语义**：“管理员”列只表示 Admin Audience 在独立管理端的治理权限，不继承消费者购物权限。表中商品浏览/搜索是管理工作台内的目录读取，不表示 Admin Session 可进入用户商城。同一自然人如同时是消费者、店铺员工、人工客服或平台管理员，必须通过对应入口获取相互隔离的 `client_type/aud`、Cookie 路径和会话；不允许在同一 Token 上叠加用户与管理能力。

| 功能模块                  | 用户（已登录/普通）        | 管理员                       | 店铺客服 Agent          |
| :------------------------ | :------------------------- | :--------------------------- | :---------------------- |
| **商品浏览（列表/详情）** | ✅ 完全访问                 | ✅（管理工作台目录读取/治理投影） | ✅（只读当前店铺公开商品；当前商品优先） |
| **商品搜索**              | ✅ 完全访问                 | ✅（管理工作台受权目录检索）    | ✅（仅当前店铺在售商品） |
| **商品收藏**              | ✅ 添加/移除收藏列表        | ❌                            | ❌                       |
| **查看商品评价**          | ✅ 只读                     | ✅ 只读                       | ✅ 只读                  |
| **发表商品评价**          | ✅ 订单完成后可评价1次+追评 | ❌                            | ❌                       |
| **回复用户评价**          | ❌                          | ✅（商家回复）                | ❌                       |
| **屏蔽/恢复违规评价**       | ❌                          | ✅                            | ❌                       |
| **加入购物车**            | ✅                          | ❌（生产管理端身份；测试需使用独立 Sandbox 用户） | ❌                       |
| **下单/支付**             | ✅                          | ❌（生产管理端身份；测试需使用独立 Sandbox 用户） | ❌                       |
| **查看个人订单**          | ✅（仅自己的）              | ✅（按 RBAC 与平台/店铺数据范围） | ❌                    |
| **申请售后**              | ✅（订单内发起退款申请）     | ❌                            | ❌                       |
| **处理售后**              | ❌                          | ✅（按权限审批通过/拒绝）      | ❌                       |
| **订单发货操作**          | ❌                          | ✅                            | ❌                       |
| **修改订单信息**          | ❌                          | ✅（仅受权命令；不可任意改状态/金额） | ❌                |
| **查看个人信息**          | ✅（仅自己的）              | ✅（按 RBAC、数据范围和业务理由脱敏查看） | ❌             |
| **修改个人信息**          | ✅（限昵称/头像及手机号/邮箱安全换绑） | ✅（仅治理状态/受权字段，不得代填密码或任意改绑定标识） | ❌ |
| **修改密码**              | ✅（通过旧密码或验证码）    | ✅（强制重置）                | ❌                       |
| **收货地址管理**          | ✅（增删改查，最多20条）    | ❌                            | ❌                       |
| **用户列表查询**          | ❌                          | ✅                            | ❌                       |
| **用户封禁/解封**         | ❌                          | ✅（默认仅安全超级管理员角色持有 `users:manage`；运行时仍按 Permission、Scope、近期认证和审计校验） | ❌ |
| **用户角色分配**          | ❌                          | ✅（默认仅安全超级管理员角色持有 `rbac:manage`；运行时仍按 Permission、Scope、可委派性和职责分离校验） | ❌ |
| **专属/店铺客服会话**     | ✅                          | ❌（管理端只提供治理与人工工作台） | —                    |
| **商品信息获取**          | -                           | -                            | ✅（详情页全部信息）     |
| **订单信息获取**          | -                           | -                            | ✅（仅当前用户当前店铺的必要摘要） |
| **用户偏好记忆**          | -                           | -                            | ✅（有效 Store Scope Consent 下只读取已确认偏好，或提出待用户确认的候选；不得自行激活） |
| **售后自主处理**          | -                           | -                            | ❌                       |
| **商品智能推荐**          | -                           | -                            | ✅（仅当前店铺，禁止跨店） |
| **客服会话分配/工作台**   | ❌                          | ✅（客服管理员）              | ❌                       |
| **AI 知识库管理**         | ❌                          | ✅（FAQ条目/向量库/相似问法） | ❌                       |
| **AI 对话记录查看**       | ✅（仅自己的）              | ✅（按客服工单、RBAC、数据范围和业务理由审计访问） | ✅（仅当前会话） |

> **专属客服权限说明**：专属客服是“消息”中的平台级固定会话，不作为独立用户角色列入本矩阵。它调用订单查询、售后申请等工具时，必须基于当前登录用户的身份、逐项授权和服务端业务规则执行；不能因此绕过用户或管理员权限。

### 1.3 角色详细定义

#### 1.3.1 用户 (User)

- **定义**：已完成注册并通过手机号/邮箱验证的普通消费者。
- **核心诉求**：流畅购物、订单追踪、收藏管理、售后保障、智能咨询。
- **准入条件**：必须登录方可使用除商品浏览外的所有功能。
- **核心权限**：
  - **账户管理**：修改密码、绑定/更换手机号及邮箱、头像与昵称设置、账号注销（需二次确认）。
  - **收货地址管理**：增删改查地址簿（最多20条），支持设置默认地址。
  - **商品收藏**：收藏/取消收藏商品，查看"我的收藏"列表。
  - **购物车**：跨设备持久化（存于数据库），支持数量修改、选中结算、删除、清空。
  - **订单中心**：查看全部订单（按状态筛选：待支付、待发货、运输中、已完成、售后中、已取消），支持订单详情查看。
  - **评价系统**：对已完成的订单商品进行文字+图片评价（1次/商品），支持追评1次。
  - **售后**：在订单内发起仅退款或退货退款申请，查看售后进度。
  - **AI 智能客服**：
    - **店铺客服入口**：以当前商品为初始上下文提供参数解答、规格对比和政策咨询，也可在本店范围内搜索/推荐；读取当前用户本店订单摘要或使用已确认的本店偏好时仍须满足授权规则。店铺客服最多提出待确认的本店偏好候选，不得自行激活长期记忆。
    - **消息入口（专属客服）**：固定置顶于“消息”左栏，提供全场景咨询；读取订单或个人信息、发起售后等敏感操作前必须获得用户授权和确认。
  - **消息通知**：接收订单状态变更、物流信息、客服回复等系统通知（站内信+可选短信/邮件）。

#### 1.3.2 管理员 (Admin)

- **定义**：拥有后台管理权限的运营或技术人员，采用 RBAC 细粒度权限控制，支持角色细分。
- **身份边界**：管理员通过独立管理端、Admin Audience Session 和 MFA 登录；生产管理身份不用于用户商城购物，测试业务使用隔离的 Sandbox 用户账号。
- **角色细分**：
  - **超级管理员**：受控的平台级安全治理权限，包括角色权限恢复、紧急干预和审计查看；不代表可跳过数据范围、职责分离或业务状态机。
  - **商品管理员**：商品CRUD、上下架、库存调整、分类品牌管理。
  - **订单管理员**：订单查看、发货、受控状态命令、未支付订单金额调整、退款审批；不得直接写状态字符串或绕过支付/售后状态机。
  - **客服管理员**：AI知识库管理、客服会话分配与工作台、对话记录查看。
  - **运营管理员**：Banner/公告内容管理、数据看板查看。
  - **审计管理员**：仅操作日志查看与数据导出权限（只读）。
- **核心权限总览**：
  - **商品管理**：批量导入/导出Excel、调整SKU属性、上下架控制。
  - **订单管理**：按数据范围查看订单；通过显式命令标记发货、取消符合条件的风险订单、调整未支付订单金额或触发退款审批，不提供任意状态/金额 PATCH。
  - **用户管理**：按 RBAC 和业务理由查看脱敏用户资料、强制下线、发起密码重置流程、封禁/解封账号；管理员不得读取或设置用户明文密码，超级管理员也不默认绕过数据范围与审计。
  - **评价管理**：回复本店用户商品评价；有权平台审核员可按规则条款屏蔽/恢复违规评价并保留不可变治理记录，不物理删除交易评价。
  - **AI 客服管理**：
    - 管理知识库（FAQ条目、相似问法、关联商品、向量库更新）。
    - 按客服工单、数据范围和业务理由查看必要的脱敏对话及满意度评分，访问行为写审计。
    - 分配人工客服会话、查看客服工作台状态。
    - 查看经离线治理流程生成的脱敏评估报告；首版管理后台不提供评估数据集的创建、编辑、版本发布或原始样本浏览能力。如未来引入模型微调，必须另行完成用户授权、数据治理、安全评估与发布审批，首版不直接使用原始客服对话训练。
    - **监控 AI 售后资格预检、退款草稿和经用户确认的申请提交**，支持阻断、转人工与审计；AI 不审批退款、不直接执行资金退款或退货结论。
  - **内容管理**：发布首页Banner轮播图、系统公告、帮助中心文章。
  - **数据看板**：展示GMV、支付订单数、支付成功率、客单价、退款、库存、客服和 AI 运行指标，并标明统计口径和数据更新时间。
  - **系统运维**：查看操作审计日志、受控数据导出、异步任务、死信事件和接口调用监控；首版不提供通用系统参数在线修改能力。

#### 1.3.3 AI 客服角色：店铺客服 Agent

- **定义**：绑定单一店铺作用域的受限咨询 Agent；商品详情只是携带 Product Context 的入口，不另建商品专属会话。
- **服务入口**：商品详情、店铺页或订单详情的“客服/联系商家”按钮；点击后进入“消息”并定位当前用户与店铺的唯一会话。
- **核心能力**：
  - **商品信息咨询**：基于当前商品的结构化数据（标题、规格参数、价格、库存、SKU、详情描述）和知识库中的关联 FAQ，解答用户关于该商品的问题（如"这个手机支持快充吗？""内存有几种选择？"）。
  - **参数对比**：可比较同一商品的 SKU；不同商品仅允许在当前店铺候选范围内受控比较，跨店比较引导至专属客服。
  - **政策解答**：基于知识库中的售后政策、退换货规则、物流时效等信息，回答用户相关咨询（如"这个支持七天无理由吗？""多久能到货？"）。
- **能力边界**：
  - **无交易、经营和资金写权限**（不可下单、不可退款、不可修改订单）；唯一直接开放的低风险运营写是幂等创建当前会话的本店人工客服工单。店铺偏好只能经统一 Memory Pipeline 形成候选，并在有效 Consent 和用户结构化确认后由 Memory Service 激活。
  - 默认不读取个人信息；仅在用户从本店订单入口进入或明确授权时读取“当前用户 ∩ 当前店铺”的必要订单摘要，不读取其他店铺订单或完整敏感地址。
  - 可在有效 Consent 下读取 `namespace=store + 当前 store_no` 中已确认的 `data_classification=L2、memory_risk_level=low` 偏好；只能提出同 Scope 候选，不得自行激活；不得读取或泄露专属客服的全局偏好，撤销授权后停止使用并按生命周期清理。
  - 可推荐当前店铺 3～5 个在售候选并说明依据；不得跨店搜索、把推广伪装成最优推荐或承诺结算时价格/库存。
- **人工接管机制**：
  - 用户输入"转人工"、"人工客服"等关键词时，当前会话切换至人工客服工作台。
  - 人工客服进入后，AI 暂停面向用户回复。人工消息正常持久化，但后续 Agent Run 只接收经授权、脱敏的处理摘要和必要消息，不读取客服内部备注或无条件注入完整人工对话。
  - 人工客服明确结束服务后，系统只恢复 AI 可用状态；不主动生成回复，用户下一条消息才创建新的 Agent Run。人工超时先转派/释放客服席位，不直接判定问题结束或自动接管。
  - 人工客服与 AI 共享同一会话ID，对话记录完整保留。

### 1.4 AI 客服会话架构

系统不设置首页/全局 AI 悬浮入口。全场景服务能力由消息中心内置的“专属客服”固定会话承载；商品页客服仍服务于具体店铺和商品场景。

#### 1.4.1 入口分流

```text
用户进入商城
    ├── 在商品详情页点击客服 → 进入「消息」并定位对应店铺会话
    │   └── 复用“用户 + 店铺”唯一会话，把商品 ID 写入可替换、版本化的 Active Product Context；商品 ID 不参与会话唯一键，检索优先命中该商品相关文档
    │
    └── 点击顶部导航「消息」 → 左栏固定置顶「专属客服」
        └── 用户点击专属客服 → 会话绑定当前用户ID，按授权读取上下文并调用有限业务工具
```

#### 1.4.2 知识库差异

| 知识库/数据类型          | 店铺客服 Agent | 消息固定会话（专属客服） |
| :----------------------- | :-------------------- | :----------------------- |
| **商品知识库**           | ✅ 当前店铺；优先当前商品 | ✅ 可检索全部商品      |
| **FAQ 知识库**           | ✅ 售后/物流/政策      | ✅ 售后/物流/平台政策     |
| **用户订单数据**         | ✅（当前用户当前店铺的必要摘要） | ✅（逐次授权后）  |
| **用户偏好向量**         | ✅（同意后的店铺命名空间低敏偏好） | ✅（用户同意后长期记忆） |
| **交易写工具**           | ❌（仅可创建本店人工工单；记忆只提候选） | ✅（首版仅可经二次确认提交退款申请；不执行渠道退款、取消订单或支付） |

#### 1.4.3 会话上下文共享（人工接管场景）

```text
┌──────────────────────────────────────────────────────────┐
│                      统一会话存储                         │
│  conversation_id: conv_xxx                              │
│  messages: [                                            │
│    { role: "user", content: "这个手机支持快充吗？" },    │
│    { role: "assistant", content: "支持，最高30W..." },   │
│    { role: "user", content: "转人工" },                 │
│    { role: "human_agent", content: "您好，请问..." }    │
│  ]                                                      │
│  handoff_summary: 授权 + 脱敏 + 来源消息引用             │
│  internal_notes: 独立存储，用户/Agent 不可见             │
└──────────────────────────────────────────────────────────┘
```

- 人工发送的用户可见消息正常持久化到 `messages`；客服内部备注写入独立资源，不属于用户会话消息。
- 人工结束服务后不立即调用模型；用户下一条消息触发新 Agent Run 时，Context Builder 只装配当前目的已授权、脱敏人工处理摘要和必要的用户可见消息引用，不无条件读取全部人工历史。
- Agent 永不读取内部备注、客服绩效/排队字段、风控备注、Secret、原始 Trace 或隐藏推理；摘要不得扩大原消息权限。
- 人工客服/管理员只能在存在当前工单关系或明确受权调查任务时查看必要会话投影，并同时通过 RBAC、平台/店铺/Queue 数据范围、业务理由、近期认证与访问审计；不存在可任意浏览的“完整对话树”权限。

## 2 前端界面

商城界面设有顶部导航栏，包含顶部搜索框以及四个核心栏目：**首页**、**购物车**、**消息**、**我的**。

进入商城后默认显示首页界面，用户可通过点击切换各栏目。

> **说明**：所有界面均需登录后访问完整功能，未登录用户仅可浏览商品列表与详情页，点击交互操作（加入购物车、立即购买、客服、收藏、消息、我的、购物车等）时弹出登录引导弹窗。

### 2.1 首页界面

首页为商品展示与流量分发核心入口，采用宽屏布局，充分利用屏幕空间，自适应调整各个模块大小。

#### 2.1.1 首页内容结构（Text UI）

```text
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│ 全局顶部导航（参见 2.2）                                                                    │
├──────────────────────────────────────────────────────────────────────────────────────────────┤
│ [平台公告：配送服务调整说明 →]                                                              │
│ ┌──────────────────────────── Banner 轮播 ────────────────────────────┐  ┌── 分类导航 ──┐ │
│ │ [活动图/品牌内容]                  [立即查看]    ● ○ ○            │  │ 手机 / 家电 │ │
│ └─────────────────────────────────────────────────────────────────────┘  │ 食品 / 日用 │ │
│                                                                          └──────────────┘ │
│ 为你推荐（未授权个性化时显示通用推荐）                                                    │
│ [商品卡] [商品卡] [商品卡] [商品卡] [商品卡] [商品卡]                                   │
│ 热销商品                                                                                │
│ [商品卡] [商品卡] [商品卡] [商品卡] [商品卡] [商品卡]                                   │
│ 新品上架                                                                                │
│ [商品卡] [商品卡] [商品卡] [商品卡] [商品卡] [商品卡]                                   │
│                              [加载更多 / 重试]                                             │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
```

#### 2.1.2 数据来源与降级规则

首页首屏使用 `GET /api/v1/homepage` 聚合读取，返回当前有效 Banner、平台公告、平台分类摘要，以及 `recommended/hot/new_arrival` 三类商品区块；它是专用 Read Model，不在服务端串行调用多个公开 API。商品卡继续使用与搜索页相同的 `ProductCard` DTO，点击进入商品详情。

- Banner/公告只展示当前已发布且处于生效时间窗的版本；没有 Banner 时删除轮播占位并让分类/商品区上移，没有公告时不显示公告条。
- 未登录、未同意个性化或推荐服务降级时，`recommended` 使用平台通用推荐，并显示“热门推荐”，不得伪装成个性化结果。
- 热销来自稳定统计窗口，新品来自商品发布时间；区块数据为空时隐藏该区块，三个区块都为空时显示“暂时没有可展示商品”及 [重新加载]，不得展示管理端草稿或下架商品。
- 首页聚合失败但商品搜索可用时，降级为“全部商品”Cursor 流；Banner、推荐或单一区块失败时只显示该模块局部错误和 [重试]，不阻断其他区块。

#### 2.1.3 首页加载、分页与返回恢复

首屏显示与最终卡片等高的骨架屏；后续商品使用服务端 opaque Cursor 分区加载，前端只原样透传 `next_cursor`。一次只允许一个同区块请求，失败保留已加载卡片并显示行内 [重新加载]，不得无限自动重试。到达末页显示“已经到底了”，重复卡片按 `product_id` 去重但不能据此修补服务端排序错误。

从商品详情返回首页时，恢复 `home_feed_version、各区块 cursor、已加载商品公开 ID、筛选状态和 scroll_y`；恢复信息只保存在当前 Session 的内存/`sessionStorage`，设置短 TTL，不保存价格之外的敏感信息。若首页版本、登录身份或推荐 Consent 已变化，则丢弃旧 Cursor、重新加载并尽量滚动到原 `anchor_product_id`，不能继续请求已失效 Cursor。

#### 2.1.4 搜索与分类导航

顶部搜索提交始终导航至 `/search?q=<规范化关键词>`，分类点击导航至 `/search?category_id=<公开分类ID>`；首页不维护另一套隐藏搜索结果状态。空白关键词不提交；联想词只用于填充/提交，不能直接信任为商品 ID。搜索页筛选、排序、分页和无结果状态由 2.12.3 定义。

#### 2.1.5 首页响应式行为

桌面端 Banner 与分类并排、商品最多六列；平板端分类改为横向可滚动 Chip、商品三至四列；手机端 Banner 单列、分类为两行横向滚动、商品两列。任何断点下 DOM 阅读顺序均为公告→Banner→分类→商品区块，视觉重排不能改变键盘/屏幕阅读器顺序。

### 2.2 整体布局与全局展示契约

用户商城普通业务页默认使用以下 `StorefrontLayout` 框架。认证页使用独立 `AuthLayout`，公开协议/隐私政策正文使用不读取登录态角标的 `LegalLayout`，管理端使用独立 `AdminLayout`；支付渠道跳转承接页、系统维护页等特殊页面按 2.12.2/2.13.16 的 Route Meta 使用专用 Layout。任何 Route 必须且只能声明一个 Layout，例外页面不得加载购物车、消息角标或错误 Audience 的用户数据。

```text
┌────────────────────────────────────────────────────────────────────────────────┐
│  首页 │                                    你好，请登录/注册 | 购物车 | 消息 | 我的  │  ← 顶部导航栏（固定）
├─────────────────────────────────────────────────────────────────────────────────┐
|  logo │                         搜索框[  ]搜索（关键词搜索）                         │← 搜索栏
│       │                      【候选词（10个左右）】                                 │
├──────────────────────────────────────────────────────────────────────────────────┐
│                                                                                  │
│                                  页面推荐商品主体内容区域                            │  ← 主体滚动区域 
│                                                                                  │
│                                                                                  │
├───────────────────────────────────────────────────────────────────────────────────┐
│  底部信息：关于我们 | 帮助中心  | 友情链接                （不固定，滚动至底部可见）        │  ← 页脚
└───────────────────────────────────────────────────────────────────────────────────┘
```

如果已经登录则把“你好，请登录/注册 ”改为 “上午/下午/晚上好！用户名 ”。

全局层级使用 Design Token，不允许页面自行填写任意 `z-index`：顶部导航 `--z-global-nav=300`，搜索栏 `--z-global-search=290`，页面 Sticky Tab `--z-page-sticky=200`，移动端底部操作栏 `--z-page-action=250`，抽屉/遮罩/弹窗为 `400/500/600`。主体设置 `scroll-padding-top`，页面锚点设置 `scroll-margin-top`，其值等于当前断点下“固定导航 + 搜索栏 + 页面 Sticky 区”的实际高度；底部固定栏出现时主体增加等高 `padding-bottom + safe-area-inset-bottom`，避免首尾内容和焦点被遮挡。

#### 2.2.1 顶部导航栏

顶部导航栏固定，随上下滑动页面不会消失

| 导航项            | 说明                                                         |
| :---------------- | :----------------------------------------------------------- |
| 首页              | 点击返回首页                                                 |
| 你好，请登录/注册 | 未登录时显示。点击登录则跳转到登录界面，点击注册则跳转到注册页面<br />如果已经登录，则把“你好，请登录/注册 ”改为 “上午/下午/晚上好！用户头像和用户名 ”。 |
| 购物车            | 若未登录点击则跳转到登录界面。<br />已登录显示购物车角标（商品总件数），点击则跳转到购物车界面 |
| 消息              | 若未登录点击则跳转到登录界面。<br />已登录显示未读消息角标，点击则跳转到消息界面 |
| 我的              | 若未登录点击则跳转到登录界面。<br />已登录点击则跳转到我的界面 |

#### 2.2.2 搜索框

位于顶部导航栏下方：

| 导航项                | 说明                                                         |
| :-------------------- | :----------------------------------------------------------- |
| **搜索框 + 搜索按钮** | 输入关键词搜索商品，支持回车提交                             |
| **候选词**            | 灰色显示，默认为：打印机 洗衣液 电风扇 电动车 洗发水 纯牛奶 充电宝 电饭煲 手机  电脑 |

#### 2.2.3 主体滚动区域

| 区域           | 内容             | 说明                      |
| :------------- | :--------------- | :------------------------ |
| **商品展示区** | 多行多列商品网格 | 每行最多展示 6 个商品卡片 |

每个商品卡片包含：


```text
┌─────────────────────┐
│    [商品主图]        │  ← 图片占卡片主要面积
│                     │
│  商品名称（1~2行）     │  ← 超出不显示，点开商品详情才显示
│  ¥xx.xx            │  ← 价格突出显示
└─────────────────────┘
```

- 卡片采用网格布局，**每行最多展示 6个商品**（根据屏幕宽度自适应减少）。
- 鼠标悬停时卡片上红框包裹效果。
- 点击卡片任意区域跳转至商品详情界面。

#### 2.2.4 页脚

位于页面底部，不固定，滑动到底部可见。

内容包含：关于我们、帮助中心、友情链接。

#### 2.2.5 界面术语契约

| 标准术语 | 使用范围 | 禁止的同义漂移 |
| :--- | :--- | :--- |
| **店铺** | 商品归属的商家经营主体和其页面 | 不再交替使用“商铺” |
| **店铺客服** | 本店 AI Agent 及其会话名称 | 不再称“商品助手”；按钮可按语境显示 [店铺客服] 或 [联系商家] |
| **专属客服** | 消息左栏固定置顶的平台 AI 会话 | 不简写为无法辨别作用域的“AI 客服” |
| **人工客服** | 经工单接管的真实客服人员 | 不与店铺客服 Agent 混称 |
| **售后** | 退款、退货退款、换货/维修（如后续支持）和申诉的上位业务 | 列表 Tab 和总入口统一显示“售后”，不显示“退款/售后” |
| **退款申请** | 用户选择“仅退款/退货退款”后创建的 `refund_application` | 不用“售后”代替具体资源名称 |

页面标题、按钮、空状态、通知、OpenAPI Description 和 Agent 展示文案共享该术语表。数据库表名、稳定错误码和已发布 API 字段保留技术名称，但必须通过展示映射转换，不直接暴露给用户。

#### 2.2.6 金额、日期与时间展示契约

- 人民币统一显示为 `¥8,999.00`：币种符号与数字之间不加空格，使用千分位和两位小数。前端只使用公共 `MoneyText` 组件格式化 API Money，不用浮点重算、手工拼接符号或省略小数。
- 纯日期统一为 `YYYY-MM-DD`，当地日期时间统一为 `YYYY-MM-DD HH:mm`，时间范围两端均写完整日期，如 `2026-08-22～2026-08-23`。仅在同日聊天气泡中可单独显示 `HH:mm`。
- API 仍按 3.11.5 传输 RFC 3339 UTC/Offset 时间；组件使用用户已选时区显示，需避免歧义时补充时区缩写。“刚刚/昨天”只是辅助文案，可访问名称中仍提供绝对时间。

#### 2.2.7 履约预计时间与服务承诺契约

商品预计发货、结算预计送达、订单/物流预计送达和 AI 回复必须使用后端同一受控 `ServiceEstimate` 结构，禁止从商品富文本、FAQ/RAG 文本、历史轨迹或模型常识推断：

| 字段 | 含义 |
| :--- | :--- |
| `estimate_type` | `dispatch/delivery` |
| `status` | `available/unavailable`；只有 `available` 可展示日期 |
| `min_at/max_at` | 可空时间范围；可用时必须成对且 `min_at <= max_at` |
| `source` | `store_fulfillment_profile/shipping_template/carrier`，前端映射为可理解来源 |
| `source_updated_at/calculated_at` | 上游数据时间与本次计算时间 |
| `timezone/disclaimer_code` | 展示时区与“预计时间，仅供参考”等稳定说明码 |

界面使用范围文案，例如“配送模板预计 2026-08-22～2026-08-23 送达 · 更新于 2026-08-20 09:15 · 仅供参考”。即使上游只给出一个时点，也必须经确定性业务规则扩展为合理范围或置为 `unavailable`，不向用户展示不可靠的单日承诺。数据缺失、过期或来源不可靠时统一显示“暂无可靠预计时间”，不保留上一个过期日期冒充当前预计。

#### 2.2.8 规范唯一来源与引用规则

为避免同一规则在不同章节被复制后发生漂移，开发与评审按下表查找唯一权威来源；其他章节只描述页面如何消费结果，并使用章节号引用，不得另写一套枚举、权限条件或状态判断。

| 规范主题 | 唯一权威来源 | 其他位置允许保留的内容 |
| :--- | :--- | :--- |
| 页面结构、界面术语、金额/时间展示 | 2.1～2.13；其中全局展示契约为 2.2.5～2.2.7 | Text UI 布局示例和“参见”引用 |
| Vue 路由与 Route Meta | `docs/traceability.yaml`；2.12.2、2.13.16 为生成/校验后的展示 | 页面章节中的单一路由入口说明 |
| API 路径、请求/响应 Schema、`operationId` | 3.11～3.12 及据此生成并经追踪矩阵校验的 OpenAPI | 第二章只写业务用途，不复制完整 DTO |
| 业务状态、合法转换与资格规则 | `docs/domain_registry.yaml`；3.14 为生成/校验后的 Domain 状态机展示 | 第二章展示服务端状态标签和 `available_actions` |
| Permission、Audience、数据范围与 Agent 工具权限 | `docs/permission_registry.yaml`；3.7.2.2、3.13、3.25 为规范解释与消费规则 | 第一、二章写用户可见能力和禁止项 |
| 数据字段、唯一约束与生命周期 | 3.5～3.10 | API/页面使用公开投影，不复制表结构 |
| 页面—API—权限—状态机—测试映射 | 3.34.19 与 `docs/traceability.yaml` | 其他验收段落引用 Requirement ID |

发生冲突时，不通过“选择更详细的一段”临时解决：应修正非权威副本为引用；若业务规则本身需要变更，先修改权威章节、Schema/Registry 与追踪矩阵，再更新消费说明。目录只列实际存在的章节标题，不使用无法定位到单一标题的范围别名。

### 2.3 商品详情界面

用户从首页商品卡片点击进入，展示单件商品的完整信息。采用左右两栏布局。

#### 2.3.1 页面布局（左右两栏）

```text
┌─────────────────────────────────────────────────────────────────────────────────────────────────────┐
│          左侧列栏上下滑动显示更多                        │               右侧列栏-固定                    │
├───────────────────────────────────────────────────────┬─────────────────────────────────────────────┤
│                                                       │  ┌─────────────────────────────────────┐   │
│                                                       │  │  📦 XX官方旗舰店   ← 店铺             │   │  
│                                                       │  │  [🚪 进店逛逛 →]                     │   │
│                                                       │  └─────────────────────────────────────┘   │
│                                                       │                                             │
│                                                       │                                             │
│                                                       │  商品名称:                        [❤️收藏]   │
│                                                       │  描述:                                      │
│                                                       │                                             │
│                                                       │  价格:¥xxx.xx     库存状态：有货/紧张          │
│                [商品图片]                              │  已售 xxx 件                                 │
│                （大图展示，）                           │                                             │
│                                                       │                                              │
│                                                       │  ─── 规格选择 ───                             │
│                                                       │  [缩略图] 款式一(对应商品图片轮播为大图在左栏)     │
│                                                       │  [缩略图] 款式二(对应商品图片轮播为大图在左栏)     │
│                                                       │  [缩略图] 款式三(对应商品图片轮播为大图在左栏)     │
│                                                       │  [缩略图] 款式四(对应商品图片轮播为大图在左栏)     │
│                                                       │  [缩略图] 款式五(对应商品图片轮播为大图在左栏)     │
│                                                       │     ···（款式多少以各自商品为准）                │ 
│                                                       │                                              │
│                                                       │   ─── 数量选择 ───                             │
│                                                       │   数量:  [−] 1 [+]                            │
│                                                       │                                              │
│                                                       │                                              │
├───────────────────────────────────────────────────────┤                                              │
│ [店铺logo][店铺名称]                                    │                                              │
├───────────────────────────────────────────────────────┤                                              │
│  [评价]  |  [商品详情] |  [常见问题]  |  [客服]    ←（固定）│                                              │
├───────────────────────────────────────────────────────│                                              │
│ [评价]                                                 │                                              │
│ .....展示最新两条评价...                                 │                                              │
│             【全部评价→】                               │                                              │
│                                                       │                                              │
│ [商品详情]                                             │       [🛒 加入购物车]         [立即购买]        │
│ .....展示全部内容...                                    │                                              │
│                                                       │                                              │
│ [常见问题]                                             │                                              │
│....展示全部内容...                                     │                                              │
└─────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

#### 2.3.2 左侧栏 - 商品展示区

左侧栏为商品视觉展示与详情内容浏览区域，**支持上下滑动**，用户可滚动浏览完整商品信息。

##### 2.3.2.1 区域构成

| 序号 | 模块                          | 内容                                                         | 交互说明                                              |
| :--- | :---------------------------- | :----------------------------------------------------------- | :---------------------------------------------------- |
| 1    | **商品图片**                  | 大图展示当前选中的商品图片                                   | 用户点击右侧栏规格缩略图时，此处同步切换对应大图      |
| 2    | **[店铺 Logo] 和 [店铺名称]** | 店铺 Logo 缩略图和店铺名称                                  | 用户点击后跳转到商品所属的店铺页面                    |
| 3    | **Tab 导航栏**                | 评价 / 商品详情 / 常见问题 / 客服（固定于图片下方，滚动时悬浮置顶） | 点击 Tab 定位至对应内容区域                           |
| 4    | **评价区域**                  | 展示最新 2 条用户评价（含评分、文字、图片缩略图、评价时间）  | 底部显示"全部评价 →"链接，点击跳转至独立评价列表页    |
| 5    | **商品详情区域**              | 展示完整商品图文详情（富文本，含图片、文字、参数列表）       | 包含：商品卖点、材质说明、使用场景、发货地、注意事项  |
| 6    | **常见问题区域**              | 展示该商品预置 FAQ 列表                                      | 每个问题以折叠面板（Accordion）展示，点击问题展开答案 |

##### 2.3.2.2 商品图片

| 功能点       | 说明                                                         |
| :----------- | :----------------------------------------------------------- |
| **主图显示** | 展示当前选中款式的大图，占据左侧栏顶部主要区域               |
| **图片切换** | 主图随右侧栏规格缩略图点击而切换；也支持主图区域左右箭头切换 |
| **放大预览** | 鼠标悬停主图时出现放大镜效果，可查看局部细节；点击主图弹出全屏模态框浏览大图 |
| **图片数量** | 每个 SKU 可关联有序图片集；切换 SKU 后从该图片集第一张开始，SKU 无专属图片时回退商品公共图库 |

##### 2.3.2.3 Tab 导航栏

| Tab 名称     | 跳转目标             | 说明                             |
| :----------- | :------------------- | :------------------------------- |
| **评价**     | 定位至评价区域       | 首次进入停留在商品顶部；滚动到评价区后才高亮 |
| **商品详情** | 定位至商品详情区域   | 点击后滚动至详情内容区           |
| **常见问题** | 定位至常见问题区域   | 点击后滚动至 FAQ 区              |
| **客服**     | 进入该店铺唯一客服会话 | 导航至消息界面并更新当前商品上下文；桌面端如采用浮层，也必须复用同一会话 ID、消息记录和接口，不创建第二套临时会话 |

> **交互细节**：Tab 导航栏在用户滚动离开图片区域时使用 `position: sticky` 固定于全局导航/搜索栏下方。Tab 高亮由各内容区的 Intersection Observer/Scroll Spy 驱动；点击 Tab 的程序滚动使用 2.2 的统一顶部偏移，滚动结束后把焦点移到目标区标题但不产生二次滚动。浏览器返回可恢复原锚点，首次进入不得自动定位到评价区。

##### 2.3.2.4 评价区域

本小节只定义左栏中的位置与入口；评价内容、空状态和“全部评价”行为以 2.3.4.1 为唯一权威定义，独立评价列表页以 2.12.3 为准。

##### 2.3.2.5 商品详情区域

本小节只定义左栏中的位置；商品内容结构以 2.3.4.2、富文本安全以 2.3.4.4 为唯一权威定义。发货时效使用结构化范围、来源及更新时间，不从详情富文本提取。

##### 2.3.2.6 常见问题区域

本小节只定义左栏中的位置；FAQ 数量、Accordion、数据来源和交互以 2.3.4.3 为唯一权威定义，安全渲染以 2.3.4.4 为准。

#### 2.3.3 右侧栏 - 商品信息与操作区

右侧栏为商品核心信息展示与购买操作区域。桌面端使用商品主容器内的 `position: sticky`，从全局固定区下方开始吸附并在主容器结束时停止；禁止使用相对视口永久固定的 `position: fixed`，也不建立左右栏各自独立的整页滚动。平板和手机端回到正常文档流。

##### 2.3.3.1 区域构成（从上至下）

| 序号 | 模块               | 内容                                        | 说明                                                   |
| :--- | :----------------- | :------------------------------------------ | :----------------------------------------------------- |
| 1    | **店铺信息**       | 店铺名称 + 进店入口                         | 展示该商品所属的店铺，点击进入店铺可以查看该店铺的商品 |
| 2    | **商品名称**       | 商品名称（大号加粗）                        | 黑色加粗显示，可多行显示                               |
| 3    | **商品描述**       | 副标题/卖点描述                             | 小字灰色展示，可多行显示                               |
| 4    | **价格与销售数据** | 销售价（突出大号显示）+ 库存展示状态 + 已售件数 | 价格醒目；只有服务端返回低库存可展示数量时才显示“仅剩 x 件” |
| 5    | **规格选择区**     | SKU（界面可称“规格/款式”）缩略图列表       | 点击后选中对应 `sku_id`，左侧切换该 SKU 图片集；无专属图时回退商品公共图库 |
| 6    | **数量选择**       | 加减按钮（− / +）+ 数字显示                 | 最少 1 件，最大为服务端返回的本次可购买上限           |
| 7    | **操作按钮区**     | 加入购物车（主按钮）+ 立即购买（强调按钮）  | 水平并排显示                                           |

##### 2.3.3.2 店铺信息

店铺信息模块位于右侧栏最顶部，用于展示商品所属店铺并引导用户浏览店铺更多商品。

| 元素                  | 说明                                   |
| :-------------------- | :------------------------------------- |
| **店铺名称**          | 店铺名称，可点击跳转至店铺首页         |
| **进店逛逛入口（→）** | 点击跳转至该店铺主页，查看店铺全部商品 |

**展示示例：**


```text
┌─────────────────────────────────────┐
│  📦 XX官方旗舰店                      │
│  [🚪 进店逛逛 →]     │  ← 操作按钮     │
└─────────────────────────────────────┘
```

##### 2.3.3.3 价格与销售数据

| 数据项     | 展示样式                       | 说明                                          |
| :--------- | :----------------------------- | :-------------------------------------------- |
| **销售价** | 大号红色/橙色字体，如"¥99.00" | 当前款式实际售价                              |
| **库存**   | 按服务端库存展示策略显示         | 默认只显示“有货/库存紧张/暂时无货”；仅当服务端返回 `low_stock_remaining` 时显示“库存紧张！仅剩 x 件”，不得展示仓库真实库存或安全库存 |
| **已售**   | "已售 xxx 件"                  | 该款式累计销量                                |

##### 2.3.3.4 规格选择区

规格选择区用于展示当前商品的全部可选 SKU。界面文案可以使用用户更易懂的“规格”或“款式”，但 Vue 状态、API JSON、购物车、结算、订单快照和埋点统一使用公开 `sku_id`，不得再定义 `款式ID/style_id`。

| 功能点       | 说明                                                         |
| :----------- | :----------------------------------------------------------- |
| **展示方式** | SKU 缩略图及规格值；无 SKU 专属图时展示商品公共主图          |
| **选中状态** | 当前 `sku_id` 对应项带高亮边框和可访问选中状态               |
| **切换交互** | 点击后选中 SKU → 左侧切换该 SKU 图片集（无则回退公共图库）→ 价格/库存/已售同步更新 |

> **规格数据联动链路**：
>
> text
>
> ```
> 用户点击缩略图 → 记录 `sku_id` → 左侧切换该 SKU 图片集 → 右侧价格/库存/已售更新 → 数量重置为 1 → 加入购物车/立即购买提交当前 `sku_id`
> ```

##### 2.3.3.5 数量选择

| 元素              | 说明                                                |
| :---------------- | :-------------------------------------------------- |
| **减号按钮（−）** | 点击数量减 1，最小为 1                              |
| **数字显示**      | 显示当前数量，用户也可直接输入数字                  |
| **加号按钮（+）** | 点击数量加 1，最大值使用服务端返回的 `max_purchase_quantity`；该值综合可售数量、限购和风控规则，不等于真实仓库库存 |
| **购买上限提示**  | 达到 `max_purchase_quantity` 时按钮置灰并提示“已达本次可购买上限”；提交结算时仍由服务端重新校验 |

##### 2.3.3.6 操作按钮区

| 按钮           | 样式                                  | 交互行为                                                     |
| :------------- | :------------------------------------ | :----------------------------------------------------------- |
| **加入购物车** | 主按钮（如橙色/品牌色），宽占约 45%   | 将当前选中 `sku_id + quantity` 增量加入购物车；同一 SKU 已存在时由服务端累加，使用响应的 `cart_total_quantity` 更新顶部角标，不在本地推算 |
| **立即购买**   | 强调按钮（如红色/高亮色），宽占约 45% | 使用当前 `sku_id + quantity` 创建 `buy_now` 结算会话，成功后导航到 `/checkout/:checkoutId`，跳过购物车但不直接创建订单或扣款 |
| **按钮间距**   | 两按钮之间留出合适间距                | 水平并排，整体居中或靠右对齐                                 |

##### 2.3.3.7 右侧栏固定说明

| 特性         | 说明                                                         |
| :----------- | :----------------------------------------------------------- |
| **Sticky 定位** | 桌面端在商品主容器内吸附，顶部偏移读取全局布局 Token，容器结束时停止 |
| **高度适配** | 页面统一滚动；内容超高时折叠次要说明或自然延长页面，不建立右栏嵌套整页滚动 |
| **屏幕适配** | ≥1200px 左右并排；768～1199px 购买区进入详情前部正常流；<768px 单列并显示不遮挡内容的底部“客服/加入购物车/立即购买”栏 |

> **左侧栏与右侧栏联动总览**：
>
> | 用户操作                        | 左侧栏响应             | 右侧栏响应                       |
> | :------------------------------ | :--------------------- | :------------------------------- |
> | 点击右侧规格缩略图              | 主图切换为对应款式大图 | 价格/库存/已售更新，数量重置为 1 |
> | 点击店铺名称 / 进店逛逛         | —                      | 跳转至店铺主页                   |
> | 点击 「评价」                   | 滚动定位至评价区域     | —                                |
> | 点击「商品详情」                | 滚动定位至详情区域     | —                                |
> | 点击 「常见问题」               | 滚动定位至 FAQ 区域    | —                                |
> | 点击 「客服」                   | 获取店铺唯一会话并更新已验证商品上下文；默认导航到 `/messages/:conversationId`，桌面端可打开同一会话浮层 | — |
> | 修改数量                        | —                      | 数量显示更新                     |
> | 点击「加入购物车」/「立即购买」 | —                      | 执行对应购买流程                 |

客服入口只有一个领域会话：商品页、店铺页和订单页先调用 `PUT /stores/{store_id}/customer-service-conversation` 获取同一 `conversation_id`，再设置经过服务端验证的当前上下文。默认导航到 `/messages/:conversationId`。若桌面端为保持浏览位置使用客服浮层，它只是该路由会话的展示容器，必须复用同一 Conversation、REST/WebSocket、Read Cursor、草稿、AI Run 和人工接管状态；关闭浮层不关闭或新建会话。禁止创建“商品专属临时会话”“订单专属会话”或浮层私有消息历史。

#### 2.3.4 评价、商品详情、常见问题区域（左侧栏下方内容区）

本区域位于左侧栏 Tab 导航栏下方，用户点击对应 Tab 时滚动定位至对应内容区块。

##### 2.3.4.1 评价区域

评价区域展示已购买用户对该商品的真实反馈，帮助潜在买家决策。

| 功能点           | 说明                                     |
| :--------------- | :--------------------------------------- |
| **展示数量**     | 默认展示最新 2 条评价                    |
| **数据来源**     | 已完成订单且用户已发表评价的商品评价数据 |
| **排序规则**     | 按评价时间倒序排列（最新优先）           |
| **全部评价入口** | 区域底部右侧显示"全部评价 →"文字链接     |

评价条目结构

每条评价包含以下信息：

| 字段         | 说明                                                         |
| :----------- | :----------------------------------------------------------- |
| **用户昵称** | 展示脱敏后的昵称（如"张**"），保护用户隐私                   |
| **评价时间** | 评价发表时间，展示格式如"2026-01-15"                         |
| **评分星级** | 五星评分系统，以实心★和空心☆展示（如"★★★★☆"），分数值范围 1~5 |
| **评价内容** | 用户填写的文字评价，最多 500 字，超出部分折叠并显示"展开全文" |
| **评价图片** | 用户上传的图片缩略图（最多 6 张），点击可放大预览            |
| **追评**     | 如有追评，在评价内容下方以灰色背景展示追评文字 + 追评时间    |
| **商家回复** | 如有管理员回复，在评价/追评下方以浅蓝色背景展示"商家回复：xxx" |

评价条目结构示例

```text
┌─────────────────────────────────────────────────────────┐
│  张**  │  2026-01-15  │  ★★★★★                        │
│  手机很好用，系统流畅，拍照效果超出预期，电池续航也   │
│  很给力，非常满意这次购物。                           │
│  [图1] [图2] [图3]                                    │
│  ┌─────────────────────────────────────────────────┐   │
│  │  追评（2026-01-20）：用了几天，确实不错，推荐！ │   │
│  └─────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────┐   │
│  │  商家回复：感谢您的认可，祝您生活愉快！        │   │
│  └─────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│  李**  │  2026-01-14  │  ★★★★☆                        │
│  整体不错，就是发货速度稍微慢了一点。                   │
│  [图1]                                                 │
└─────────────────────────────────────────────────────────┘
                             ↓
               【 全部评价 → 】
```

交互行为说明

| 用户操作               | 系统响应                                                     |
| :--------------------- | :----------------------------------------------------------- |
| **点击"全部评价 →"**   | 跳转至该商品独立评价列表页，分页展示全部评价，支持按评分（全部/5星/4星/3星/2星/1星）和按时间/最新排序 |
| **点击评价图片缩略图** | 弹出图片预览模态框，支持左右切换浏览所有评价图片             |
| **点击"展开全文"**     | 评价内容完整展开，同时按钮变为"收起全文"                     |
| **空状态**             | 如该商品暂无评价，显示空状态插画 + "暂无评价，快来购买后分享您的使用感受吧~" |

##### 2.3.4.2 商品详情区域

商品详情区域展示该商品的完整信息，涵盖图文描述、规格参数、发货信息、注意事项等。

| 功能点       | 说明                                           |
| :----------- | :--------------------------------------------- |
| **数据来源** | 管理员在商品管理后台录入的完整商品信息         |
| **展示方式** | 富文本渲染，支持图文混排、表格、列表等多种格式 |

内容模块

| 模块             | 内容说明                                                     | 展示形式                                  |
| :--------------- | :----------------------------------------------------------- | :---------------------------------------- |
| **商品图文描述** | 商品核心卖点、设计理念、材质工艺、使用场景等详细描述         | 富文本（图片 + 文字混排）                 |
| **规格参数**     | 结构化参数列表，如品牌、型号、尺寸、重量、颜色、处理器、电池容量、接口类型等 | 表格（2 列：参数名 \| 参数值）            |
| **发货地与预计发货** | 城市级发货地 + `ServiceEstimate(type=dispatch)` | 按 2.2.7 显示时间范围、来源、更新时间与“仅供参考”；不从富文本读取“48小时内”承诺 |
| **注意事项**     | 该商品特殊说明，如退换货限制、使用禁忌、保养建议等           | 列表展示（项目符号）                      |

规格参数示例

| 参数名   | 参数值                |
| :------- | :-------------------- |
| 品牌     | XX 科技               |
| 型号     | X20 Pro               |
| 颜色     | 黑色、白色、金色      |
| 尺寸     | 156.8 × 72.3 × 8.2 mm |
| 重量     | 185g                  |
| 处理器   | 八核 3.2GHz           |
| 电池容量 | 4500mAh               |
| 屏幕尺寸 | 6.5 英寸              |
| 接口类型 | USB-C                 |

交互行为说明

| 用户操作                 | 系统响应                                                     |
| :----------------------- | :----------------------------------------------------------- |
| **点击 Tab「商品详情」** | 页面平滑滚动定位至该区域起始位置                             |
| **图文混排渲染**         | 富文本内容完整渲染，图片自适应宽度                           |
| **参数表格**             | 结构清晰展示                                                 |
| **长内容折叠**           | 如商品描述内容过长（超过 2000px 高度），默认展示部分内容 + "展开更多"按钮，点击后完整展开 |

数据同步说明

| 同步项                  | 说明                                                         |
| :---------------------- | :----------------------------------------------------------- |
| **右侧栏商品名称/描述** | 右侧栏展示的商品名称、副标题与此区域数据同源                 |
| **右侧栏价格/库存**     | 价格与库存展示联动规格选择，与此区域静态信息独立（价格来自 SKU，库存展示来自库存服务的安全 DTO，图文详情来自商品主表/履约资料） |
| **AI 客服数据来源**     | 店铺客服 Agent 通过受限工具实时获取本区域面向用户公开的字段（商品描述、规格参数、城市级发货地、注意事项、常见问题），确保回答与页面一致，不读取仓库地址或后台经营字段 |

##### 2.3.4.3 常见问题区域

常见问题区域展示该商品的高频预置问答，帮助用户快速了解商品的关键信息，减少重复咨询。

| 功能点       | 说明                                                         |
| :----------- | :----------------------------------------------------------- |
| **展示数量** | 默认展示该商品全部预设 FAQ（通常 3~8 条）                    |
| **数据来源** | 由管理员在商品编辑后台针对该商品单独预设，与 AI 知识库共享同一数据源 |
| **展示方式** | 折叠面板（Accordion），默认全部折叠                          |

问题条目结构

每个常见问题条目包含：

| 字段         | 说明                                                     |
| :----------- | :------------------------------------------------------- |
| **问题标题** | 用户常见疑问，以问句形式呈现（如"这款手机支持快充吗？"） |
| **答案内容** | 对应问题的详细解答，支持纯文本或富文本                   |
| **展开状态** | 默认收起，点击后展开显示答案                             |



常见问题展示示例

```text
┌─────────────────────────────────────────────────────────┐
│  ▼ 常见问题                                            │
├─────────────────────────────────────────────────────────┤
│  ▶ 这款手机支持快充吗？                                 │  ← 未展开状态
├─────────────────────────────────────────────────────────┤
│  ▶ 保修政策是怎样的？                                   │  ← 未展开状态
├─────────────────────────────────────────────────────────┤
│  ▶ 赠送哪些配件？                                       │  ← 未展开状态
├─────────────────────────────────────────────────────────┤
│  ▼ 支持7天无理由退货吗？                 ← 已展开状态  │
│  支持，但需满足以下条件：                                │
│  ① 商品包装完好，配件齐全，不影响二次销售；             │
│  ② 退货运费由买家承担（非质量问题）；                   │
│  ③ 需在签收后7天内发起申请。                            │
│  详细规则请参考[售后政策]。                             │
├─────────────────────────────────────────────────────────┤
│  ▶ 发货后多久能到？                                     │  ← 未展开状态
└─────────────────────────────────────────────────────────┘
```

交互行为说明

| 用户操作                 | 系统响应                                                     |
| :----------------------- | :----------------------------------------------------------- |
| **点击折叠的问题行**     | 该问题展开显示答案内容，同时图标由 ▶ 变为 ▼；其他已展开问题保持展开状态（不自动折叠） |
| **再次点击已展开问题行** | 该问题折叠收起，图标由 ▼ 变为 ▶                              |
| **点击全部展开/收起**    | 可选功能，一键展开或收起全部 FAQ                             |

##### 2.3.4.4 商品与 FAQ 富文本安全渲染契约

商品详情和 FAQ 不渲染商家提交的原始 HTML。公开 DTO 只允许二选一：版本化结构化内容块 `content_format=structured_v1`，或服务端按已发布策略生成的 `content_format=safe_html_v1`；同时返回 `content_version、content_hash、safe_text_fallback`。前端通过统一 `SafeRichContent` 组件按 `content_format` 渲染，禁止在业务组件对原始字段直接使用 `v-html/innerHTML`。

允许的 Safe HTML 子集仅包含语义展示元素：`p、br、h2～h4、strong、em、ul、ol、li、blockquote、table、thead、tbody、tr、th、td、a、img`。属性采用逐元素白名单：链接只允许标准化 HTTPS/站内相对地址及 `href/title`；图片只允许平台图片 CDN/对象存储转换域的 HTTPS URL、`src/width/height/alt`；表格只允许无脚本的语义属性。禁止以下内容：

- `script、style、iframe、object、embed、form、input、button、video、audio、canvas` 和可执行 SVG/MathML；
- 任意 `on*` 事件属性、`srcdoc`、内联样式、任意 `class/id`、CSS URL、模板表达式和自定义元素；
- `javascript:、data:、vbscript:、file:` 等危险 Scheme、协议相对外链、开放重定向和未经允许的第三方图片；
- 自动下载、自动跳转、表单提交、弹窗或任何可执行行为。

外部链接由组件强制增加 `target="_blank" rel="noopener noreferrer nofollow ugc"`，站内链接走 Router 且仍校验资源 ID。图片必须有服务端提供的宽高和有意义的 `alt_text`，使用懒加载、解码提示、最大尺寸和失败占位，不能因图片尺寸未知造成大幅布局抖动；装饰图使用空 Alt。浏览器 CSP 至少包含 `script-src` 不允许 `unsafe-inline/unsafe-eval`、`object-src 'none'`、`frame-src 'none'`、`base-uri 'none'`、受控 `img-src` 和 `form-action 'self'`，可配合 Trusted Types 限制 DOM 注入 Sink。

若 `content_format/content_version` 不受支持、内容 Hash 不匹配、渲染组件失败或图片不可用，页面降级显示 `safe_text_fallback` 与“部分图文暂不可用”，不得回退插入原始 Source。富文本中的价格、库存、发货/送达日期和售后资格不是权威实时事实；页面使用结构化业务字段，AI 使用受控 Tool。

HTML 消毒只解决浏览器执行风险，不能消除自然语言中的间接 Prompt Injection。商品/FAQ 内容传给 Agent、RAG 或摘要时只使用服务端生成的 `safe_text_fallback/structured_text`，标记为 `untrusted_product_content` 并绑定 Product/Store/Version；其中“忽略规则、调用工具、泄露数据”等文字永远是商品数据而非指令，不能改变 System、Skill、Tool、Scope 或 Approval。

##### 2.3.4.5 AI 数据联动关系图

```text
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           商品详情页数据源                                       │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐     │
│  │   商品主表     │  │  款式SKU表    │  │   FAQ 表      │  │   店铺表         │    │     
│  │  · 商品名称    │  │  · SKU价格    │  │  · 问题标题   │  │  · 店铺名称       │    │
│  │  · 商品描述    │  │  · SKU库存    │  │  · 答案内容   │  │  · 商品列表       │    │
│  │  · 规格参数    │  │  · 规格属性   │  │  · 关联商品ID │  │                  │    │
│  │  · 发货地      │  │  · 销量       │  │               │  │                │   │
│  │  · 注意事项    │  │  · 图片       │  │               │  │                │    │
│  │  · 店铺ID      │  │               │  │               │  │               │    │
│  └───────┬───────┘  └───────┬───────┘  └───────┬───────┘  └───────┬───────┘    │
│          │                  │                  │                  │            │
│          ▼                  ▼                  ▼                  ▼            │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │                            前端展示层                                     │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌────────────┐   │   │
│  │  │ 商品详情区域    │  │ 规格选择区    │  │ 常见问题区域   │  │ 店铺信息    │   │   │     
│  │  │ (图文/参数)    │   │ (缩略图/价)  │  │ (FAQ折叠)     │  │ (名称)     │   │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘  └────────────┘   │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                      │                                          │
│                                      ▼                                          │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │                    AI 工具调用层（Function Calling）                     │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌────────────┐ │   │
│  │  │ getProduct   │  │ getSKUInfo   │  │ getFAQ       │  │ getStore   │ │   │    
│  │  │ Detail()     │  │ ()           │  │ ()           │  │ Info()     │ │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘  └────────────┘ │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                      │                                          │
│                                      ▼                                          │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │                         店铺客服回答                                  │   │
│  │  基于上述工具获取的信息，生成准确一致的回答给用户                         │   │
│  │  （店铺信息也可作为回答依据，如"该店铺支持7天无理由退货"）              │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 2.4 消息与客服界面

用户点击顶部导航 **“消息”** 进入消息中心，也可以从商品详情、店铺、订单等页面的客服按钮进入。界面采用类似电脑版微信的双栏布局：左侧为会话列表，右侧为聊天窗口。

左栏第一项始终为平台级 **“专属客服”**，固定置顶且登录后自动存在；其下才按最近消息时间排列各店铺会话。首页和全局区域不再显示平台级 AI 悬浮入口。

商品页、店铺页和订单页的店铺客服按钮都解析到“当前用户 + 当前店铺”的唯一会话并选中它，默认进入 `/messages/:conversationId`。桌面客服浮层仅为同一会话的可选 UI 容器；它与消息页共享 URL 可恢复的会话 ID、消息、草稿、已读位点、Agent Run 和人工接管状态，不具有独立会话或独立授权。

#### 2.4.1 页面整体布局

```text
┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│  首页  │                                你好，[用户昵称]  |  购物车(2) |  消息(5)  |  我的        │  ← 顶部导航    
├─────────────────────────────────────────────────────────────────────────────────────────────┤
│                       左栏（聊天会话列表）                │           右栏（聊天历史记录）         │
│                                                       │                                      │
│┌───────────────────────────────────────────────────┐  │         （当前选中的会话）              │
││ ┌───────────┐   专属客服             [固定置顶]    │  │                                       │
││ │ [平台头像] │   [最新消息摘要]           [未读数]  │  │                  [询问内容] :[用户头像]  │
││ └───────────┘                                    │  │                                      │
│└───────────────────────────────────────────────────┘  │  [客服/AI头像]：[回答内容]              │
│─────────────── 店铺会话 ───────────────────────────│  │                                       │
│┌───────────────────────────────────────────────────┐ │                                       │
││ ┌───────────┐   [商家名称]         [最后一条消息日期] │ │                  [询问内容] :[用户头像] │
││ │ [商家1头像] │                                   	│  │                                      │
││ └───────────┘   [显示最新一条消息]      [未读消息数量] │ │   [客服/AI头像]：[回答内容]             │
│└───────────────────────────────────────────────────┘ │                                      │
│┌───────────────────────────────────────────────────┐  │                 [询问内容] :[用户头像]│
││ ┌───────────┐   [商家名称]         [最后一条消息日期] │  │                                     │
││ │ [商家1头像] │                                   	│  │   [客服/AI头像]：[回答内容]           │
││ └───────────┘   [显示最新一条消息]      [未读消息数量] │ │                                     │
│└───────────────────────────────────────────────────┘ │                [询问内容] :[用户头像]  │
│┌───────────────────────────────────────────────────┐ │                                     │
││ ┌───────────┐   [商家名称]         [最后一条消息日期] │  │   [客服/AI头像]：[回答内容]           │
││ │ [商家1头像] │                                   	│  │                                     │
││ └───────────┘   [显示最新一条消息]      [未读消息数量] │ │                                      │
│└───────────────────────────────────────────────────┘ │            （上下滚动）                │
│                                                      │                                      │
│                                                      │                                      │
│                                                      │ [预设快捷问题1]                        │
│                                                      │ [预设快捷问题2]                        │
│                   （上下滚动）                         │ [预设快捷问题3]                        │
│                                                      ───────────────────────────────────────
│                                                      │                                 [+]  │
│                                                      ───────────────────────────────────────│
│                                                      │                                      │
│                                                      │             输入框                    │
│                                                      │                                      │
└───────────────────────────────────────────────────────────────────────────────────[发送]────┘

[+]：
│────────────────────────────────────────
│  [订单]   [商品]                        │
│                                        │
────────────────────────────────────────┘

点击订单卡片就会出现
[订单]：
│────────────────────────────────────────
│  [购买过的订单1]              [发送]      │
│  [购买过的订单2]              [发送]      │
│  [购买过的订单3]              [发送]      │
│                                        │
────────────────────────────────────────┘

点击商品卡片就会出现
[商品]：
│────────────────────────────────────────────│
│       ┌─────────────────────┐              │
│       │    [商品1主图]       │              │  
│       │                     │ ←[点击即发送] │
│       │  商品名称（1~2行）    │              │ 
│       │  ¥xx.xx            │             │ 
│       └─────────────────────┘             │
│       ┌─────────────────────┐              │
│       │    [商品2主图]       │              │  
│       │                     │ ←[点击即发送] │
│       │  商品名称（1~2行）    │              │ 
│       │  ¥xx.xx            │             │ 
│       └─────────────────────┘             │
│       ┌─────────────────────┐              │
│       │    [商品3主图]       │              │  
│       │                     │ ←[点击即发送] │
│       │  商品名称（1~2行）    │              │ 
│       │  ¥xx.xx            │             │ 
│       └─────────────────────┘             │
│                ···                        │
│                                           │
│          （滑动显示更多）                    │
│                                           │
│───────────────────────────────────────────│
```

#### 2.4.2 进入方式与行为差异

| 进入方式                       | 左栏行为                          | 右栏行为               | AI 欢迎语              |
| :----------------------------- | :-------------------------------- | :--------------------- | :--------------------- |
| **点击商品详情页「客服」按钮** | 保留专属客服置顶，新增/定位该商品对应店铺会话 | 自动加载店铺聊天窗口 | 无历史消息时展示商品导向非持久欢迎态 |
| **点击店铺/订单「联系商家」**  | 保留专属客服置顶，新增/定位对应店铺会话 | 自动加载店铺聊天窗口并绑定来源上下文 | 无历史消息时按来源展示非持久欢迎态 |
| **点击顶部导航「消息」**       | 专属客服固定显示在第一项，店铺历史会话显示在其下 | 默认选中并加载专属客服 | 无历史消息时展示非持久欢迎态，否则恢复历史记录 |

> **会话规则**：
> - 专属客服属于平台级系统会话，每位已登录用户固定一个，不需要用户主动创建
> - 专属客服始终位于左栏第一项，不参与按时间排序，不支持删除、取消置顶或移入店铺会话区
> - 同一店铺同一用户只保留一个会话（不区分商品）
> - 如该店铺已有历史会话，点击「客服」不会新建，而是定位到已有会话并加载历史记录
> - 如该店铺无历史会话，点击「客服」则新建会话

#### 2.4.3 左栏 - 会话列表

左侧由两个固定层级组成：顶部是专属客服固定区，底部是店铺历史会话滚动区。即使用户从未咨询过任何店铺，专属客服仍必须显示。

##### 2.4.3.1 会话条目结构

```text
┌───────────────────────────────────────────────────┐
│ ┌───────────┐   专属客服          固定置顶        │
│ │ [平台头像] │   有什么可以帮您？       [2]        │
│ └───────────┘                                    │
├──────────────── 店铺会话 ─────────────────────────┤
├───────────────────────────────────────────────────┤
│ ┌───────────┐   店铺名称          昨天       […] │
│ │ [店铺头像] │   您好，请问商品有什么问题？         │
│ └───────────┘                                    │
├───────────────────────────────────────────────────┤
│ ┌───────────┐   店铺名称          2天前      […] │
│ │ [店铺头像] │   已转人工                         │
│ └───────────┘                                    │
└───────────────────────────────────────────────────┘
```

| 字段                 | 说明                                           |
| :------------------- | :--------------------------------------------- |
| **平台头像**         | 专属客服统一品牌头像，与任何店铺 Logo 区分      |
| **专属客服名称**     | 固定显示“专属客服”，不得由接口返回其他旧名称    |
| **固定置顶标识**     | 使用置顶图标或“置顶”小标签，不使用动态日期占位  |
| **店铺头像**         | 店铺 Logo / 默认头像，仅用于店铺会话            |
| **店铺名称**         | 店铺名称（如“XX数码旗舰店”）                    |
| **最后一条消息摘要** | 最新一条消息内容，最多 1 行，超出显示“...”      |
| **时间**             | 店铺会话今天显示 HH:MM，昨天显示“昨天”，更早显示 MM-DD |
| **未读角标**         | 有未读时显示红色数字角标（>99 显示“99+”）       |
| **更多菜单**         | 每个店铺会话右侧始终提供可聚焦的 `…` 按钮，具备可访问名称“更多会话操作”；专属客服不显示该按钮 |

##### 2.4.3.2 交互行为

| 用户操作             | 系统响应                                                     |
| :------------------- | :----------------------------------------------------------- |
| **点击会话条目**     | 右侧聊天窗口切换至该会话并加载消息；点击本身不清除未读，只有满足下述可见条件后才提交最高已读位点 |
| **点击专属客服**     | 右侧加载专属客服历史消息及快捷服务；首次读取敏感数据时请求授权 |
| **专属客服菜单**     | 不提供删除、隐藏、归档、取消置顶或改变名称；固定项不显示 `…` |
| **点击店铺会话 `…`** | 打开菜单，包含「查看店铺」和「从列表隐藏会话」；按钮支持 Enter/Space，菜单支持方向键、Esc 和焦点返回 |
| **右击店铺会话**     | 仅作为桌面快捷方式，打开与 `…` 完全相同的菜单；任何功能不得只能靠右键访问 |
| **点击「查看店铺」** | 跳转至该店铺的店铺页（店铺主页）                             |
| **点击「删除会话」** | 弹出确认弹窗“确认从列表隐藏该会话吗？重新咨询或收到新消息时，该会话会恢复显示。”；确认后立即从用户会话列表隐藏；服务端按 3.5.10 保留唯一会话及必要记录，不级联删除支付、售后、人工客服和安全审计数据 |
| **店铺会话空状态**   | 专属客服下方显示“暂无店铺会话，去商品页咨询吧~”；专属客服不受空状态影响 |

左栏发生滚动时，只滚动“店铺会话”区域，专属客服卡片和“店铺会话”分隔标题保持可见。顶部导航“消息”角标等于专属客服未读数与全部店铺会话未读数之和；进入消息页不直接清零，只有打开对应会话并使消息进入已读状态后才扣减。

已读可见条件统一为：当前会话处于激活态、`document.visibilityState=visible`、消息数据加载成功且目标消息气泡完成 DOM 渲染，并在会话滚动容器中至少 50% 可见持续 500 ms。前端只提交满足条件的最高 `message_id + sequence_no`；若用户仍停留在旧消息区，只推进到实际看到的位置，不把尚未滚入视口的新消息标记已读。浏览器切到后台、路由正在切换、消息骨架/加载失败、窗口被遮挡到不可见或目标消息尚未渲染时均不得推进。

`PUT read-cursor` 失败时保留服务端未读角标并用同一目标位点有界重试；Read Cursor 只能前进，旧页签提交较小位点视为幂等无变化。多标签页/设备收到 `unread.updated` 后直接采用服务端返回的“该会话绝对未读数 + 账号绝对总未读数 + cursor_version”，禁止在本地按事件做减法，避免乱序后出现负数。

**右键菜单示例**：


```text
┌───────────────────────────────────────────────────┐
│ ┌───────────┐   专属客服          固定置顶        │
│ │ [平台头像] │   有什么可以帮您？       [2]        │
│ └───────────┘                                    │
├──────────────── 店铺会话 ─────────────────────────┤
│ ┌───────────┐   店铺名称          10:32      […] │
│ │ [店铺头像] │   好的，我帮您查一下                │
│ └───────────┘                                    │
│                   ┌─────────────┐                │  ← 点击 […] 或右键后弹出
│                   │ 查看店铺     │                │
│                   │ 删除会话     │                │
│                   └─────────────┘                │
└───────────────────────────────────────────────────┘
```

#### 2.4.4 右栏 - 聊天窗口

右侧为聊天窗口，展示当前选中会话的完整对话内容。

##### 2.4.4.1 聊天窗口布局

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │  [← 返回会话]  XX数码旗舰店                         [查看店铺]     │ │  ← 页面标题栏
│  ├───────────────────────────────────────────────────────────────────────┤ │
│  │                                                                      │ │
│  │  ──────── 2026-01-18 ────────                                       │ │  ← 时间分隔线
│  │                                                                      │ │
│  │  🤖  您好，我是店铺助手！发现你正在看这款 iPhone 15 Pro，            │ │  ← AI 消息（左）
│  │      有什么关于它的疑问我可以帮您解答吗？😊                         │ │
│  │                                                       10:32         │ │
│  │                                                                      │ │
│  │                                                                      │ │
│  │                                           支持快充吗？         👤    │ │  ← 用户消息（右）
│  │                                                       10:33         │ │
│  │                                                                      │ │
│  │  🤖  支持的！iPhone 15 Pro 最高支持 30W 有线快充和                  │ │  ← AI 消息（左）
│  │      15W MagSafe 无线充电。                                         │ │
│  │                                                       10:33         │ │
│  │                                                                      │ │
│  │  ─── 用户发送了订单卡片 ───                                        │ │  ← 卡片消息
│  │  ┌───────────────────────────────────────────────────────────────┐   │ │
│  │  │  📦 订单号：ord_01K...               状态：✅ 已完成         │   │ │
│  │  │  [缩略图] iPhone 15 Pro × 1   ¥8,999.00                    │   │ │
│  │  │  共 1 件商品  实付：¥8,999.00  2026-01-18                  │   │ │
│  │  └───────────────────────────────────────────────────────────────┘   │ │
│  │                                                       10:35         │ │
│  │                                                                      │ │
│  │  🤖  我看到了您的订单，需要我帮您查询物流信息吗？😊                  │ │  ← AI 消息（左）
│  │                                                       10:35         │ │
│  │                                                                      │ │
│  ├───────────────────────────────────────────────────────────────────────┤ │
│  │  📌 这款支持分期吗？  📌 保修多久？  📌 有现货吗？                   │ │  ← 快捷提问区
│  │  📌 支持双卡吗？    📌 什么处理器？                                 │ │
│  ├───────────────────────────────────────────────────────────────────────┤ │
│  │  [输入框，占满剩余宽度...]                                   [+]    │ │  ← 输入区
│  ├───────────────────────────────────────────────────────────────────────┤ │
│  │                                                          [发送]     │ │  ← 发送按钮（右下角）
│  └───────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
```

消息路由是普通页面时，标题栏不显示无语义的最小化 `[−]` 或关闭 `[✕]`；桌面端“返回会话”可隐藏但保留语义标题，“查看店铺”只在店铺会话显示。仅当商品页采用 2.3.3.7 的同会话浮层容器时，浮层标题栏才显示“最小化/关闭”：最小化收起容器但保留草稿和连接状态，关闭只关闭容器并把焦点还给触发按钮，二者都不归档会话、不结束人工工单、不创建新 Conversation。

##### 2.4.4.2 桌面、移动端与消息状态

桌面端采用“固定专属客服 + 可滚动店铺会话列表 + 当前会话”双栏；宽度小于 768px 时使用两级路由视图：`/messages` 只显示会话列表，`/messages/:conversationId` 只显示单会话并提供 [← 返回会话]。刷新或分享单会话 URL 可直接恢复目标会话；无权/不存在时回到列表并显示稳定错误，不短暂渲染其他会话。每个 `(user_id, conversation_id)` 的纯文本草稿保存在 Session 级存储并设 TTL，附件只保存已上传 `file_id`，不得持久化 Token、地址或未上传文件二进制；发送成功后清除对应草稿。

消息滚动与发送状态遵循以下规则：

- 首次打开且用户位于底部时滚到最新已加载消息；用户上滑离开底部阈值后，新消息不得强制滚动，显示“有 N 条新消息”按钮和可访问提示，点击后滚到第一条未读消息；
- 用户消息先以 `sending` 显示并绑定稳定 `client_message_id`；服务端确认后变为 `sent`，超时/失败显示 `failed + [重试]`，重试复用同一 ID 和幂等语义，禁止复制出第二个气泡；
- WebSocket 断开显示“正在重新连接”，期间允许编辑草稿；发送进入受控 Outbox 或明确禁用。重连后先按最后确认 Sequence 补拉，再恢复实时订阅，重复消息按服务端 `message_id/sequence_no` 去重；
- AI 流式 Delta 可视觉更新同一气泡，但 `aria-live` 不逐 Token 播报；屏幕阅读器只在完整消息、发送失败、人工接入、授权请求和“有新消息”计数变化时以简短文本播报；
- 图片/文件发送显示上传、扫描、发送三个独立状态；扫描失败不得进入消息，离开页面时只提示仍在本地上传或尚未发送的内容。

##### 2.4.4.3 专属客服右栏状态（Text UI）

用户选中左栏第一项“专属客服”时，右栏标题、快捷入口和上下文均切换为平台服务模式，不显示任何店铺名称：

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│  [平台头像] 专属客服                                      [服务说明]        │
├─────────────────────────────────────────────────────────────────────────────┤
│  🤖 您好，我是您的专属客服。订单、物流、售后和选购问题都可以问我。          │
│                                                                             │
│  快捷服务                                                                  │
│  [查订单]  [查物流]  [申请售后]  [商品推荐]                                │
│                                                                             │
│                                           我的快递到哪里了？          👤    │
│  🤖 查询订单需要读取您的订单与物流信息。                                   │
│     [暂不允许] [允许本次查询]                                               │
├─────────────────────────────────────────────────────────────────────────────┤
│  [输入消息……]                                                       [+]    │
│                                                                  [发送]    │
└─────────────────────────────────────────────────────────────────────────────┘
```

专属客服支持以下能力：

| 能力 | 行为与边界 |
| :--- | :--------- |
| 全场景咨询 | 回答平台规则、购物流程、物流、售后与商品选择问题 |
| 订单/物流查询 | 用户授权后读取当前账号的订单与物流信息，并提供订单详情快捷链接 |
| 售后处理 | 预检售后资格；提交申请前必须展示订单、商品、原因、类型和金额并二次确认 |
| 商品推荐 | 可根据用户当前需求搜索全站商品；使用历史偏好前需取得个性化服务授权 |
| 长期偏好 | 用户同意后保存品类、价格带、品牌、风格等偏好；支持在设置中查看、清除或关闭 |
| 人工接管 | 复杂纠纷转平台人工客服，人工可读取本会话必要上下文，结束后由专属客服继续服务 |

专属客服不可修改密码、修改收货地址、代替用户下单、审批售后或执行渠道退款。它只能完成资格预检、构建草稿，并在用户通过结构化确认卡片明确确认后提交退款申请。申请提交后，由售后 Domain 规则引擎依据已发布规则决定自动批准或进入人工审核；Agent 不参与该决定，也不得向用户宣称退款已经批准或到账。

专属客服处理退款/退货时遵循以下流程：确认用户身份和本次授权 → 调取订单 → 调用确定性资格服务校验售后时效、商品政策与最大可退金额 → 构建退款申请草稿 → 向用户展示商品、数量、原因、类型、凭证和金额 → 用户点击确认按钮 → 提交 `refund_applications`。提交后的自动批准门槛、人工审核条件和规则版本全部由后端返回，前端不得写死金额、天数或品类规则。命中确定性低风险规则时由 Domain 推进为 `approved`；否则进入 `merchant_review` 审核队列。审核队列直接由 `refund_applications` 的状态、创建时间、审核截止时间和 Store Scope 投影，不另建客服工单；只有用户发起争议、咨询或要求平台介入时，才创建 `human_service_tickets`。

#### 2.4.5 欢迎消息与进入行为

##### 2.4.5.1 从商品详情页「客服」按钮进入

用户点击商品详情页的「客服」按钮后，进入消息界面：

| 场景                   | 左栏行为                       | 右栏行为             | AI 欢迎语                      |
| :--------------------- | :----------------------------- | :------------------- | :----------------------------- |
| **该店铺无历史会话**   | 幂等获取或创建店铺会话，并高亮选中 | 自动加载聊天窗口     | 展示商品导向非持久欢迎态       |
| **该店铺已有历史会话** | 定位到已有会话并高亮选中       | 加载完整历史聊天记录 | 不重复发送欢迎语，恢复上次对话 |

欢迎内容来自版本化 `welcome_state/welcome_version` 配置，只作为空时间线上的页面状态，不创建 `messages`、不改变 `last_message_at`、不增加未读数，也不触发 Agent Run。用户发送首条真实消息后才进入正常消息时间线。欢迎文案示例：

> "您好，我是店铺助手！发现你正在看这款【商品名称】，有什么关于它的疑问我可以帮您解答吗？比如参数、价格、发货政策都可以问我哦~ 😊"

##### 2.4.5.2 从顶部导航「消息」进入

用户点击顶部导航「消息」后，进入消息界面：

| 场景 | 左栏行为 | 右栏行为 |
| :--- | :------- | :------- |
| **首次进入消息** | 专属客服固定置顶，店铺会话区按实际数据展示 | 默认选中专属客服并展示非持久欢迎态 |
| **再次进入消息** | 专属客服仍在第一项，店铺会话按最新消息排序 | 默认恢复专属客服最近聊天记录 |
| **无店铺历史会话** | 仍显示专属客服，店铺会话区显示空状态 | 正常使用专属客服，不显示全页空白状态 |
| **通过站内通知进入** | 专属客服保持置顶 | 根据通知携带的 `conversation_id` 加载目标会话 |

专属客服首次欢迎语示例：

> “您好，我是您的专属客服。您可以问我订单、物流、售后或商品推荐等问题。需要读取订单或个人信息时，我会先征得您的授权。”

再次进入时仅恢复历史消息；没有历史消息时继续渲染同一版本欢迎态，不写入重复消息。专属客服基于购买或浏览历史主动推荐前，必须满足用户已同意个性化服务的条件。

##### 2.4.5.3 专属客服首次授权

用户第一次要求查询订单、物流、个人偏好或代办售后时，在右栏消息流内插入授权卡片：

```text
┌────────────────────── 授权专属客服 ──────────────────────┐
│ 为完成“查询我的订单”，需要读取：                         │
│ · 当前账号下的订单号、订单状态和物流信息                 │
│ 不会读取密码，也不会修改收货地址或代替您下单。           │
│                              [暂不允许] [允许本次查询]    │
└───────────────────────────────────────────────────────────┘
```

- 授权必须说明用途、数据范围和有效期，默认采用“仅本次”；长期授权必须由用户主动选择。
- 用户拒绝后，专属客服改为提供手动路径，例如“我的 → 我的订单”，不得继续调用敏感数据工具。
- 提交退款申请、修改或删除长期记忆等首版已开放写操作，即使已有读取授权，也必须针对本次操作再次展示对象、金额（适用时）和后果，并取得明确确认。取消订单、确认收货、修改地址、下单和支付首版只提供权威页面入口，不生成 Agent Approval，也不调用对应写 Tool。

#### 2.4.6 消息区

##### 2.4.6.1 消息类型支持

| 消息类型     | 发送方       | 说明                                                         |
| :----------- | :----------- | :----------------------------------------------------------- |
| **纯文本**   | 用户/AI/人工 | 常规对话内容                                                 |
| **商品卡片** | 用户/AI      | 用户通过 + 号发送商品时，以卡片形式发送到聊天窗口；AI 推荐/提及商品时，以卡片形式展示缩略图+名称+价格，点击可跳转 |
| **订单卡片** | 用户/AI      | 用户通过 + 号发送订单时，以卡片形式发送到聊天窗口；AI 查询订单时展示订单号+状态+金额 |
| **操作按钮** | AI           | 如"查看物流""申请售后""去支付"，点击跳转对应页面             |
| **系统提示** | 系统         | 灰色文字，如"人工客服已接入""会话已结束"                     |

##### 2.4.6.2 消息气泡样式

| 发送方   | 对齐   | 气泡颜色            | 头像         |
| :------- | :----- | :------------------ | :----------- |
| **用户** | 右对齐 | 品牌色（蓝色/橙色） | 用户头像     |
| **AI**   | 左对齐 | 浅灰色              | 🤖 / 店铺头像 |
| **人工** | 左对齐 | 浅绿色              | 👤 / 人工头像 |
| **系统** | 居中   | 灰色小字            | 无           |

#### 2.4.7 预设快捷问题

预设快捷问题位于消息区底部、输入区上方，并根据当前会话类型动态生成。

| 属性 | 店铺会话 | 专属客服会话 |
| :--- | :------- | :----------- |
| **数据来源** | 当前店铺通用 FAQ、当前商品关联 FAQ | 平台 FAQ、用户已授权的订单状态、当前对话意图 |
| **问题示例** | “这款支持分期吗？”“保修多久？”“有现货吗？” | “查询我的订单”“物流到哪里了？”“如何申请售后？”“帮我推荐商品” |
| **展示数量** | 动态展示 3～6 个 | 动态展示 4～6 个 |
| **展示方式** | 2～3 行，水平排列 | 服务快捷入口优先，随后展示推荐问题 |
| **交互方式** | 点击后自动填入并发送 | 点击敏感能力时先触发对应授权卡片，再继续执行 |

> **示例**："这款支持分期吗？""保修多久？""有现货吗？""支持7天无理由吗？"

#### 2.4.8 [+] 功能 - 订单与商品发送

点击输入区右侧的 **[+]** 按钮，弹出功能菜单，包含 **[订单]** 和 **[商品]** 两个选项。

##### 2.4.8.1 弹出菜单

```text
┌────────────────────────────────────────
│  [📋 订单]   [🛒 商品]               │
│                                        │
└────────────────────────────────────────┘
```

##### 2.4.8.2 订单功能

点击 **[订单]** 后，弹出订单选择面板：

```text
┌──────────────────────────────────────────────────────────────┐
│  📋 选择要发送的订单                              [✕]       │
├──────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────────┐  │
│  │  ord_01K...    ✅ 已完成  2026-01-18   [发送]      │  │
│  │  [缩略图] iPhone 15 Pro × 1  ¥8,999.00            │  │
│  └────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  ord_01M...    📦 运输中  2026-01-15   [发送]      │  │
│  │  [缩略图] 小米14 Ultra × 1  ¥4,999.00              │  │
│  └────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  ord_01N...    💰 待支付  2026-01-12   [发送]      │  │
│  │  [缩略图] 华为Mate 60 × 1  ¥6,999.00               │  │
│  └────────────────────────────────────────────────────────┘  │
│  ...（滚动加载更多）                                       │
└──────────────────────────────────────────────────────────────┘
```

| 操作                     | 行为                             |
| :----------------------- | :------------------------------- |
| **点击 [发送]**          | 该订单以卡片形式发送到聊天窗口中 |
| **点击订单条目其他区域** | 跳转至该订单详情页（新标签页）   |

订单选择范围依据当前会话变化：专属客服会话可展示当前用户的全部订单；店铺会话只展示该店铺的订单。订单列表和订单详情均只允许当前登录用户访问。

**订单发送后的行为**：

- 订单卡片出现在聊天窗口中（用户右侧气泡）
- 专属客服在用户授权后可调用订单查询工具获取订单状态、物流和商品明细
- 订单卡片是可选的用户可见消息，不是读取授权凭据；店铺会话可依据服务端验证的当前订单上下文读取本次咨询所需的本店订单最小摘要，不得读取其他店铺订单或用户完整地址
- 发送卡片后，服务端验证订单属于当前用户和当前会话店铺，再把该订单设为当前 Active Context；切换咨询对象时原子停用旧同类上下文并同步更新页面提示
- AI 根据订单信息主动提供协助，示例：
  > “我看到您发送了订单 ord_01K...。包裹 1 已发货，运单号 SF****7890；承运商预计 2026-08-22～2026-08-23 送达，更新于 2026-08-20 09:15，仅供参考。包裹 2 暂无可靠预计时间。需要我继续查看物流轨迹吗？”

AI、消息卡片和聊天记录只展示 `tracking_no_masked`。预计时间必须来自物流 Tool 的逐包裹 `delivery_estimate: ServiceEstimate`，并按 `source` 映射“承运商预计/配送模板预计”、展示 `source_updated_at` 与说明码；不得把模型推断的“后天”或单个包裹预计套用到整单。

##### 2.4.8.3 商品功能

点击 **[商品]** 后，弹出商品选择面板：

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│  🛒 选择要分享的商品                                  [✕]                  │
├─────────────────────────────────────────────────────────────────────────────┤
│       ┌─────────────────────┐       ┌─────────────────────┐                │
│       │    [商品1主图]       │       │    [商品2主图]       │                │
│       │                     │       │                     │                │
│       │  iPhone 15 Pro      │       │  小米14 Ultra       │                │
│       │  ¥8,999.00         │       │  ¥4,999.00         │                │
│       │     ←[点击即发送]   │       │     ←[点击即发送]   │                │
│       └─────────────────────┘       └─────────────────────┘                │
│       ┌─────────────────────┐       ┌─────────────────────┐                │
│       │    [商品3主图]       │       │    [商品4主图]       │                │
│       │                     │       │                     │                │
│       │  华为Mate 60        │       │  AirPods Pro        │                │
│       │  ¥6,999.00         │       │  ¥1,899.00         │                │
│       │     ←[点击即发送]   │       │     ←[点击即发送]   │                │
│       └─────────────────────┘       └─────────────────────┘                │
│                ···                                                         │
│          （滑动显示更多）                                                   │
└─────────────────────────────────────────────────────────────────────────────┘
```

| 操作                     | 行为                             |
| :----------------------- | :------------------------------- |
| **点击商品卡片任意区域** | 该商品以卡片形式发送到聊天窗口中 |

> **说明**：店铺会话中的商品面板只展示当前店铺的已上架商品；专属客服中的商品面板提供“最近浏览、我的收藏、搜索全站商品”三个来源，不绑定单一店铺。

**商品发送后的行为**：

- 商品卡片出现在聊天窗口中（用户右侧气泡）
- AI 通过工具调用自动识别商品信息，可查询该商品的详细数据
- AI 的会话上下文**自动绑定当前商品ID**，所有回答优先检索该商品的知识库（商品描述、规格参数、发货地、常见问题、注意事项）
- AI 根据商品信息主动提供协助，示例：
  > "您分享了 iPhone 15 Pro，这款手机搭载 A17 Pro 芯片，支持 30W 快充，目前有黑色、白色、金色三种颜色可选。有什么具体想了解的吗？😊"

#### 2.4.9 AI 与人工客服切换机制

店铺会话和专属客服会话均支持 AI 与人工接管，但人工服务队列不同：店铺会话转交对应店铺人工客服，专属客服会话转交平台人工客服。

##### 2.4.9.1 人工介入方式

| 触发方式           | 说明                                                         |
| :----------------- | :----------------------------------------------------------- |
| **用户主动转人工** | 用户输入包含"转人工" / "人工客服" / "人工" 等关键词意图时，触发转人工 |
| **AI 自动转人工**  | AI 识别到问题超出能力范围（如复杂售后纠纷、特殊诉求），主动提示转人工 |
| **人工主动介入**   | 人工客服通过工作台主动进入会话，此时 AI 暂停，人工接管       |

| 会话类型 | 转接目标 |
| :------- | :------- |
| 店铺会话 | 当前店铺人工客服队列 |
| 专属客服 | 平台人工客服/平台售后队列，不转给任意单一店铺 |

##### 2.4.9.2 切换流程

| 阶段           | AI 行为         | 系统表现                                                     |
| :------------- | :-------------- | :----------------------------------------------------------- |
| **触发转人工** | AI 暂停回答     | AI 发送“正在为您转接人工客服，请稍候…”，按会话类型推送到店铺或平台队列 |
| **人工接入**   | AI 暂停回复     | 系统提示"人工客服已接入"，消息气泡切换为人工样式（浅绿色）   |
| **人工回复**   | AI 暂停面向用户回复 | 人工消息正常持久化；AI 不读取人工内部备注，后续新 Run 只接收经授权的必要消息与脱敏处理摘要 |
| **人工离开**   | AI 恢复可用     | 系统提示“人工客服已结束服务，继续发送消息将由 AI 为您服务”；不在没有新用户消息时主动生成 AI 回复，用户下一条消息默认开启新的 Agent Run |

> **上下文共享**：人工回复作为受权限和保留策略约束的会话历史存在。用户继续提问时，AI 只接收经授权、脱敏的人工处理摘要和必要消息，不自动读取客服内部备注，也不恢复人工接管前已经过时的工具执行计划。

#### 2.4.10 会话生命周期

| 场景             | 行为                                                 |
| :--------------- | :--------------------------------------------------- |
| **离开界面**     | 会话状态保存，不丢失                                 |
| **关闭聊天窗口** | 会话状态保存，不丢失                                 |
| **再次进入**     | 恢复最近 10 条历史消息（后续消息在向上滚动时懒加载） |
| **店铺视角**     | 同一店铺同一用户只保留一个会话ID，消息全量同步       |
| **专属客服视角** | 每位用户固定一个平台会话ID，永久置顶展示；用户不可删除或关闭 |

专属客服会话的数据标识统一为 `conversation_type = exclusive`、`is_fixed = true`、`store_id = null`。用户登录后由服务端执行“获取或创建”，同一用户只能存在一个该类型会话；前端应根据会话类型渲染固定区，不能仅依赖接口列表顺序或普通 `pinned` 字段，以免刷新、分页或收到新店铺消息后改变位置。

### 2.5 购物车界面

顶部导航栏中购物车显示商品数量角标，点击后跳转至购物车界面。

#### 2.5.1 页面整体布局

```text
┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│  首页  │                                你好，[用户昵称]  |  购物车(2) |  消息(5)  |  我的       │  ← 顶部导航    
├────────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                             │
│  购物车                                                                          [ 管理 ]     │  ← 页面标题
│                                                                                             │
│  ┌────────────────────────────────────────────────────────────────────────────────────────┐ │
│  │  ┌──────────────────────────────────────────────────────────────────────────────────┐  │ │
│  │  │  ┌───────────────────────────────────────────────────────────────────────────┐   │  │ │
│  │  │  │  ☑  店铺名称：XX数码旗舰店                                                    │   │  │ │← 店铺分组标题
│  │  │  └───────────────────────────────────────────────────────────────────────────┘   │  │ │
│  │  │   ┌──────────────────────────────────────────────────────────────────────────┐   │  │ │ 
│  │  │   │    ┌──────┐  商品名称：iPhone 15 Pro                      单价：¥8,999.00  │   │  │ │← 商品条目1
│  │  │   │    │ 主图  │  规格：黑色 256G                              数量：  [−] 1 [+] │   │  │ │
│  │  │ ☑ │    └──────┘  库存充足                                     小计：¥8,999.00  │   │  │ │
│  │  │   │                                                               [删除 X ]│  │   │  │ │
│  │  │   └──────────────────────────────────────────────────────────────────────────┘   │  │ │
│  │  │   ┌──────────────────────────────────────────────────────────────────────────┐   │  │ │
│  │  │   │    ┌──────┐  商品名称：iPhone 11 Pro                      单价：¥3,789.00  │   │  │ │← 商品条目2
│  │  │   │    │ 主图  │  规格：白色                                  数量：  [−] 1 [+]  │   │  │ │
│  │  │ ☑ │    └──────┘  库存充足                                     小计：¥3,789.00  │   │  │ │
│  │  │   │                                                               [删除 X ]│  │   │  │ │
│  │  │   └──────────────────────────────────────────────────────────────────────────┘   │  │ │
│  │  └──────────────────────────────────────────────────────────────────────────────────┘  │ │
│  │  ┌──────────────────────────────────────────────────────────────────────────────────┐  │ │
│  │  │  ┌───────────────────────────────────────────────────────────────────────────┐   │  │ │
│  │  │  │  ☑  店铺名称：小米之家                                                       │   │  │ │← 店铺分组标题
│  │  │  └───────────────────────────────────────────────────────────────────────────┘   │  │ │
│  │  │   ┌──────────────────────────────────────────────────────────────────────────┐   │  │ │ 
│  │  │   │    ┌──────┐  商品名称：小米14 Ultra                      单价：¥4,999.00    │  │  │ │ ← 商品条目1
│  │  │   │    │ 主图  │  规格：白色 512G                            数量：  [−] 1 [+]   │  │  │ │
│  │  │ ☑ │    └──────┘  库存仅剩 3 件                              小计：¥4,999.00    │  │  │ │
│  │  │   │                                                              [删除 X ]│   │  │  │ │
│  │  │   └──────────────────────────────────────────────────────────────────────────┘  │  │ │
│  │  └─────────────────────────────────────────────────────────────────────────────────┘  │ │    
│  └───────────────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                            │
│  ┌──────────────────────────────────────────────────────────────────────────────────────┐  │
│  │  ☑ 全选                     合计（不含运费）：¥17,787.00           ([3]件)              │  │
│  │                                                                             [去结算]  │  │← 底部结算栏      
│  └──────────────────────────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────────────────────────────┘
```

示例中三个已勾选条目的金额校验为 `¥8,999.00 + ¥3,789.00 + ¥4,999.00 = ¥17,787.00`。Text UI 只用于布局验收，实际小计和合计仍使用购物车 API 返回的 Money，不由前端浮点计算。

#### 2.5.2 页面说明

##### 2.5.2.1 页面头部

| 元素         | 说明                                                         |
| :----------- | :----------------------------------------------------------- |
| **标题**     | 显示"购物车"                                                 |
| **管理按钮** | 标题右侧显示"管理"文字按钮，点击后进入管理模式（可批量删除/清空） |

##### 2.5.2.2 购物车列表

购物车商品按**店铺**分组展示，同一店铺的商品归为一组。

| 元素             | 说明                                                         |
| :--------------- | :----------------------------------------------------------- |
| **店铺分组标题** | 显示店铺名称，左侧带复选框（全选该店铺所有商品）             |
| **商品条目**     | 展示单个商品信息                                             |
| **空状态**       | 购物车为空时，居中显示空状态插画 + "购物车是空的，快去逛逛吧~" + [去首页逛逛] 按钮 |

##### 2.5.2.3 商品条目说明

每个商品条目包含以下信息：

| 元素           | 说明                                                         |
| :------------- | :----------------------------------------------------------- |
| **复选框**     | 勾选/取消勾选该商品，选中后计入结算                          |
| **商品主图**   | 商品缩略图，点击可跳转至商品详情页                           |
| **商品名称**   | 完整商品名称                                                 |
| **规格**       | 显示选中的 SKU 规格（如"黑色 256G"）                         |
| **单价**       | 该商品单价                                                   |
| **数量选择器** | [−] 按钮 / 数量数字 / [+] 按钮（最少 1 件，最大为服务端返回的 `max_purchase_quantity`） |
| **小计**       | 单价 × 数量                                                  |
| **库存状态**   | 根据 `stock_status` 显示“有货/库存紧张/暂时无货”；只有服务端返回 `low_stock_remaining` 时才显示具体剩余数量 |
| **操作按钮**   | [删除]（将该商品从购物车移除）                               |

##### 2.5.2.4 底部结算栏

底部结算栏固定于页面底部：

| 元素           | 说明                                                         |
| :------------- | :----------------------------------------------------------- |
| **全选复选框** | 一键选中/取消当前页面所有商品（所有店铺）                    |
| **合计金额**   | 显示"合计（不含运费）：¥xxx.xx"，已勾选商品的金额总和       |
| **去结算按钮** | 显示“去结算 (x件)”，点击后以已选购物车条目创建 `cart` 结算会话，成功后导航到 `/checkout/:checkoutId`；未选中任何商品时置灰不可点击 |

#### 2.5.3 交互行为

| 用户操作                   | 系统响应                                                     |
| :------------------------- | :----------------------------------------------------------- |
| **勾选/取消商品**          | 底部合计金额实时更新，去结算按钮商品数量同步变化             |
| **勾选/取消店铺全选**      | 该店铺下所有商品全部选中/取消，底部合计金额实时更新          |
| **点击商品**               | 会跳转到该商品页面                                           |
| **点击店铺**               | 会跳转到店铺界面                                             |
| **点击全选**               | 所有店铺所有商品全部选中/取消                                |
| **点击 [+] 增加数量**      | 提交新的最终数量并使用服务端响应更新购物车；达到 `max_purchase_quantity` 时按钮置灰，提示“已达本次可购买上限” |
| **点击 [−] 减少数量**      | 数量 -1，最小为 1；小计金额更新                              |
| **直接输入数量**           | 支持手动输入整数，校验 `1 ≤ quantity ≤ max_purchase_quantity`，服务端仍需再次验证 |
| **点击 [删除]**            | 弹窗确认"确认删除该商品吗？"，确认后从购物车移除             |
| **点击 [管理]**            | 切换管理模式，商品条目显示批量删除复选框，底部变为 [删除选中] 按钮 |
| **点击 [去结算]**          | 使用所有已勾选商品创建结算会话；创建成功后只携 `checkoutId` 进入结算页，失败时留在购物车并标出失效项 |
| **商品库存不足（结算时）** | 弹出提示"xxx 商品库存不足，请调整数量"，阻止结算             |

#### 2.5.4 购物车角标

| 场景         | 说明                                                         |
| :----------- | :----------------------------------------------------------- |
| **角标显示** | 顶部导航「购物车」右侧显示红色数字角标，数值 = 购物车中所有商品数量之和 |
| **角标更新** | 加入购物车、修改数量、删除商品时使用接口返回的 `cart_total_quantity` 更新，不根据点击次数自行累加 |
| **角标隐藏** | 购物车为空时，角标隐藏                                       |



### 2.6 订单结算界面

订单结算界面用于用户在付款前确认收货地址、商品规格、配送方式和应付金额。该界面有两个入口，但使用同一套页面和结算逻辑：

1. 商品详情页点击 **[立即购买]**，以 API JSON 字段 `sku_id + quantity` 创建单个商品结算；
2. 购物车点击 **[去结算]**，携带已勾选的购物车条目 ID 集合进入多商品结算。

> **按钮语义说明**：商品详情页的 [立即购买] 只创建结算快照并进入本界面，**不能创建订单或扣款**。结算页的 [提交订单] 只调用订单创建接口；订单成功后才进入独立支付收银台，由用户选择渠道并调用支付创建接口。两个步骤使用各自稳定的 Idempotency Key，任何一步都不得被前端合并成一个结果未知的请求。

#### 2.6.1 页面路由与进入参数

| 场景 | 结算会话创建参数 | 页面路由 | 返回行为 |
| :--- | :------- | :------- | :------- |
| 单个商品立即购买 | `source_type=buy_now`、`items[{sku_id, quantity}]` | `/checkout/:checkoutId` | 返回原商品详情页，保留已选规格和数量 |
| 购物车选中结算 | `source_type=cart`、`cart_item_ids[]` | `/checkout/:checkoutId` | 返回购物车，保留勾选状态 |

入口页先调用 `POST /api/v1/checkout-sessions` 创建短期结算会话，成功后使用响应中的 `checkout_id` 导航到 `/checkout/:checkoutId`。结算页刷新、浏览器返回和重新计价都根据 `checkoutId` 读取服务端快照；原始 SKU、购物车条目和客户端金额不能作为刷新后的权威状态。结算参数必须由服务端重新读取商品名称、价格、库存和运费，前端传入的金额只用于入口过渡展示，不能作为实际扣款依据。

#### 2.6.2 页面整体布局（Text UI）

```text
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│  首页  │                           你好，[用户昵称] | 购物车(2) | 消息(5) | 我的              │
├──────────────────────────────────────────────────────────────────────────────────────────────┤
│  确认订单                                                        [< 返回商品详情/购物车]     │
│                                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │  收货地址                                                           [管理地址]          │  │
│  │  ● 张三  138****8888（默认）                                                           │  │
│  │    上海市浦东新区 XX 路 XX 号 XX 室                                  [更换 >]          │  │
│  └────────────────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │  [店铺Logo] XX数码旗舰店                                           [联系客服]          │  │
│  │  ┌────────┐  iPhone 15 Pro                                                          │  │
│  │  │ 商品主图 │  规格：黑色 / 256G             ¥8,999.00 × 1        小计 ¥8,999.00       │  │
│  │  └────────┘                                                                          │  │
│  │  配送模板预计 2026-08-22～2026-08-23 送达 · 更新于 2026-08-20 09:15 · 仅供参考  │  │
│  │  配送方式：快递配送                                             运费 ¥0.00  │  │
│  │  订单备注：[选填，请先与商家协商，最多200字____________________]                        │  │
│  └────────────────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │  支付提示                          提交订单后前往收银台选择支付方式              │  │
│  │  商品金额                                                               ¥8,999.00      │  │
│  │  运费                                                                      ¥0.00      │  │
│  │  应付总额                                                         ¥8,999.00             │  │
│  └────────────────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                              │
│                                  配送至：上海市浦东新区……  收货人：张三 138****8888          │
│                                                                  [提交订单 ¥8,999.00]         │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
```

#### 2.6.3 收货地址区域

| 元素 | 展示与交互 |
| :--- | :--------- |
| 默认地址 | 优先选中用户默认收货地址，展示收货人、脱敏手机号和完整地址 |
| 更换地址 | 点击后弹出地址选择抽屉，单选其他地址并点击 [使用该地址] |
| 新增地址 | 地址抽屉内提供 [新增收货地址]，复用“我的界面”的地址表单 |
| 管理地址 | 跳转至“我的 → 我的收货地址”，保存后返回结算页并刷新地址列表 |
| 无地址状态 | 显示“还没有收货地址”，[新增收货地址] 为必选主操作；未添加前提交订单按钮禁用 |

#### 2.6.4 商品、店铺与配送区域

- 商品按店铺分组；单个商品立即购买时只展示一个店铺下的当前商品。
- 店铺 Logo、店铺名称可点击进入店铺页；商品图片、商品名称可点击进入商品详情页。
- 每个商品必须展示商品主图、名称、SKU 规格、单价、数量和小计；结算页不允许直接修改规格和数量，用户需返回来源页修改。
- 每个店铺拥有独立配送方式、运费和订单备注；一次结算生成一个合并交易单 `trade_order`，每个店铺生成一个店铺子订单 `order`，支付时可针对合并交易单统一支付。
- 点击 [联系客服] 进入与商品详情页相同的店铺唯一会话，并建立短期 `checkout_store_group` Active Context。该上下文由服务端绑定当前用户、会话店铺、`checkout_id + checkout_version` 和 Checkout 过期时间；只包含本店组的 `product_id、sku_id、商品名、规格、数量` 及必要的本店配送问题，不包含其他店铺分组、完整收货地址、手机号、优惠明细、支付偏好或跨店总额。
- Checkout 重新计价产生新 Version 后，旧 Context 立即因版本不匹配而不可调用 Tool；用户再次点击 [联系客服] 才以最新版本原子替换。Checkout 过期、成功创建订单或被显式放弃时 Context 进入 `expired`；仅浏览器离开页面不立即删除，在 TTL 内返回仍可恢复。

#### 2.6.5 金额与提交订单区域

金额统一保留两位小数，计算关系为：

```text
店铺应付小计 = Σ（SKU 实时单价 × 购买数量）+ 店铺运费
应付总额 = Σ 店铺应付小计
```

上述公式用于页面解释；实际商品金额、运费和应付金额全部使用 Checkout API 返回的 Money，前端不得自行重算最终应付。

| 操作/状态 | 系统响应 |
| :-------- | :------- |
| 点击 [提交订单] | 使用订单 Idempotency Key 调用 `POST /orders`；按钮 loading，服务端再次校验地址、上下架、价格、库存和配送；本步骤不创建 Payment |
| 订单创建成功 | 消耗 Checkout、锁定库存，立即保存 `trade_order_id/order_ids` 并导航 `/pay/:tradeOrderId`；刷新不得回到已消费 Checkout |
| 订单响应未知 | 使用同一 Idempotency Key 查询/重试订单创建结果，禁止换 Key 再消费 Checkout |
| 收银台点击 [确认支付] | 使用独立支付 Idempotency Key 调用 `POST /payments`；成功后按渠道跳转或展示二维码 |
| Payment 创建失败 | 跳转订单详情并提示“订单已创建，可继续支付”，保留待支付订单且不得重新创建订单 |
| 渠道返回/支付结果未知 | 导航 `/payments/:paymentId/result`，查询 Payment/Trade Order 或等待可信事件，显示“支付结果确认中”；不得根据 URL 参数判成功，也不得创建相同交易的新 Payment |
| 支付成功 | 以服务端 Payment/Trade Order 终态跳转支付成功页，提供 [查看订单] 和 [返回首页] |
| 用户取消支付/关闭收银台 | 仅关闭当前未完成支付尝试并保留待支付订单，跳转订单详情；不得恢复或重复消费 Checkout |
| 支付明确失败 | 订单详情显示安全失败原因和服务端 `available_actions.pay`；允许时可发起新的支付尝试，但不重复创建订单 |
| 价格变化 | 弹窗列出变化商品及新旧价格，用户确认后刷新金额，原提交动作取消 |
| 库存不足/下架 | 标红对应商品，禁用提交订单按钮，提示用户返回修改 |
| 重复提交 | 订单创建和支付创建分别复用各自 Idempotency Key；订单只创建一组，每个逻辑支付尝试只创建一个 Payment |

待支付订单默认支付时限为 30 分钟，倒计时结束后自动取消并释放库存。具体时限由服务端配置，前端读取服务端截止时间，不能仅依赖本地计时。

#### 2.6.6 页面状态与响应式要求

| 状态 | 页面表现 |
| :--- | :------- |
| 加载中 | 地址、商品和金额区域显示骨架屏；提交订单按钮禁用 |
| 加载失败 | 显示“结算信息加载失败”及 [重新加载]，保留来源参数 |
| 结算参数失效 | 显示“结算商品已失效”，提供 [返回商品详情] 或 [返回购物车] |
| 桌面端 | 内容最大宽度 1200px，底部应付金额与提交订单按钮固定可见 |
| 小屏端 | 模块单列；底部固定栏仅展示“应付金额 + 提交订单”，按 2.2 预留安全区和底部空间，不能遮挡备注、错误或焦点 |

### 2.7 店铺界面

无论用户在商品详情、购物车、结算、消息会话、订单列表或订单详情中点击店铺 Logo/名称，均进入同一个店铺页面，并在 Vue 路由参数中携带公开 `storeId`；调用 API 时映射为 `store_id`，不得传数据库自增主键。

#### 2.7.1 页面路由与整体布局（Text UI）

路由：`/stores/:storeId`；URL Query 与 API Query 统一使用 `?q=、?group_id=、?sort=、?cursor=`。Vue Router 读取后按原名传给 `GET /stores/{store_id}/products`，不再支持 `keyword/groupId` 第二套别名。Cursor 只能原样透传，前端不得解析或自行计算。

```text
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│  首页  │                           你好，[用户昵称] | 购物车(2) | 消息(5) | 我的              │
├──────────────────────────────────────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │ [店铺Logo]  XX数码旗舰店     企业认证✓     商品 128     店铺评分 4.9                  │  │
│  │             店铺简介：官方正品，品质保障                         [联系客服] [收藏店铺] │  │
│  └────────────────────────────────────────────────────────────────────────────────────────┘  │
│  [店铺首页] [全部商品] [手机] [电脑] [配件] [店铺介绍]                                    │
│                                                                                              │
│  店内搜索：[输入商品关键词________________] [搜索]    排序：[综合▼] [销量] [价格↕] [新品]    │
│                                                                                              │
│  ┌─────────────────────┐ ┌─────────────────────┐ ┌─────────────────────┐                    │
│  │      [商品主图]      │ │      [商品主图]      │ │      [商品主图]      │                    │
│  │ 商品名称（最多2行）   │ │ 商品名称（最多2行）   │ │ 商品名称（最多2行）   │                    │
│  │ ¥8,999.00  已售 2.3万 │ │ ¥4,999.00  已售 8千  │ │ ¥299.00  已售 1.2万  │                    │
│  └─────────────────────┘ └─────────────────────┘ └─────────────────────┘                    │
│                              [上一页]  已显示本批 24 件  [下一页]                           │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
```

#### 2.7.2 店铺头部

| 元素 | 说明与交互 |
| :--- | :--------- |
| 店铺 Logo/名称 | 展示当前店铺识别信息；当前页不可重复跳转 |
| 认证标识 | 根据店铺认证类型展示“企业认证/官方旗舰店”等，鼠标悬停显示认证说明 |
| 经营数据 | 展示在售商品数、综合评分；不展示后台敏感经营数据 |
| 店铺简介 | 最多两行，超出显示省略号，可在店铺介绍中查看完整内容 |
| 联系客服 | 进入该店铺唯一会话；无历史会话则创建，有历史会话则恢复，与商品页客服同入口、同会话 ID |
| 收藏店铺 | 登录后切换收藏状态并显示反馈；未登录则弹出登录引导 |

#### 2.7.3 店铺导航与商品列表

- **店铺首页**：展示店铺公告、推荐商品和热销商品；无运营内容时直接展示全部商品。
- **全部商品/店铺分组**：按店铺自定义商品分组筛选，只展示当前店铺已上架且可售商品；店铺分组与平台经营分类是两个不同概念。
- **店铺介绍**：展示主体资质摘要、开店时间、经营范围、发货地、服务政策和联系方式；敏感证照号码需脱敏。
- 店内搜索只检索路由 `storeId` 映射出的 API `store_id` 范围，不得跳转为全站搜索。
- 排序支持综合、销量、价格升序/降序、新品；改变搜索、分组或排序条件后清空旧 Cursor，从首批结果重新加载。
- 商品卡片沿用首页样式，点击进入商品详情；卡片悬停显示边框和轻微上浮效果。
- 每批默认 24 件，使用接口返回的 `previous_cursor/next_cursor` 导航；切换后滚动到商品列表顶部，仅在确有恢复浏览位置需求时把 opaque Cursor 原样写入 URL，不展示无法由 Cursor 准确支持的总页数和任意页跳转。

#### 2.7.4 空状态与异常状态

| 场景 | 页面反馈 |
| :--- | :------- |
| 店铺分组/搜索无结果 | 显示“没有找到相关商品”，提供 [清空筛选] |
| 店铺暂无商品 | 显示“店铺正在上新，先去首页逛逛吧”，提供 [返回首页] |
| 店铺休息/关闭 | 对拥有该店历史订单的用户返回受限店铺资料，保留店铺基本信息和历史订单客服入口，隐藏收藏与商品购买入口；其他公开访问是否返回受限资料或 404 由 `visibility_mode` 决定 |
| 店铺不存在 | 展示 404 状态，提供 [返回首页]，不得显示其他店铺数据 |

### 2.8 我的界面

顶部导航栏第四项“我的”进入用户中心。该页必须包含个人信息、收货地址、订单和评价，不把这些核心能力仅放在“设置”深层入口中。

#### 2.8.1 页面整体布局（Text UI）

路由：`/me`。未登录访问时保存目标路由并跳转登录，登录成功后返回本页。

```text
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│  首页  │                           你好，[用户昵称] | 购物车(2) | 消息(5) | 我的              │
├──────────────────────────────────────────────────────────────────────────────────────────────┤
│  我的                                                                                         │
│  ┌────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │ [头像]  张三（普通会员）                                              [编辑个人资料]     │  │
│  │ 账号：zhangsan / 138****8888       密码：••••••••                    [修改密码]         │  │
│  └────────────────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │ 我的订单                                                         [查看全部订单 >]       │  │
│  │ [待支付 1]   [待发货 2]   [运输中 1]   [待评价 3]   [售后 1]                         │  │
│  └────────────────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                              │
│  ┌─────────────────────────────────────────┐  ┌──────────────────────────────────────────┐   │
│  │ 我的收货地址                    [管理 >] │  │ 我的评价                       [查看 >] │   │
│  │ 张三 138****8888（默认）                │  │ 待评价 3 条 / 已评价 12 条              │   │
│  │ 上海市浦东新区……                        │  │ 最近评价：iPhone 15 Pro ★★★★★          │   │
│  │ [+ 新增收货地址]                        │  │                                          │   │
│  └─────────────────────────────────────────┘  └──────────────────────────────────────────┘   │
│                                                                                              │
│  [我的收藏]  [收藏店铺]  [帮助中心]  [关于我们]  [设置]                    [退出登录]       │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
```

#### 2.8.2 个人信息与账号安全

| 字段/操作 | 规则 |
| :-------- | :--- |
| 头像 | 支持 JPG/PNG/WebP，当前单文件最大 5 MiB；上传前裁剪为 1:1，失败时保留原头像，最终以上传策略接口为准 |
| 昵称 | 2～20 个字符，过滤纯空格和违规内容 |
| 账号 | 展示账号名或脱敏手机号/邮箱；唯一账号标识不可直接修改 |
| 手机/邮箱 | 默认脱敏展示；修改前必须进行验证码或原密码校验 |
| 密码 | 页面只显示 `••••••••`，任何接口不得返回明文或可逆密码 |
| 修改密码 | 点击打开修改密码弹窗，填写原密码、新密码、确认新密码 |

修改密码弹窗：

```text
┌──────────────────────────── 修改密码 ────────────────────────────┐
│ 原密码：     [••••••••________________] [显示/隐藏]              │
│ 新密码：     [••••••••________________] [显示/隐藏]              │
│ 确认新密码： [••••••••________________] [显示/隐藏]              │
│ 密码强度：   弱 / 中 / 强                                        │
│                          [取消]  [确认修改]                        │
└──────────────────────────────────────────────────────────────────┘
```

单因素密码最少 15 个字符，前端至少允许输入 64 个字符，并允许空格和 Unicode；不强制字母、数字、特殊字符组合，但应提示并拒绝已泄露、常用或与账号上下文高度相关的弱密码。密码不得与原密码相同，不截断粘贴内容，不妨碍密码管理器和浏览器自动填充。修改成功后关闭弹窗、显示成功提示，并使其他设备登录令牌失效；当前设备可根据安全策略保留登录或要求重新登录。若未来启用 MFA，最低长度仍不得少于服务端当时返回的密码策略。

#### 2.8.3 我的收货地址

“我的”首页显示默认地址摘要，点击 [管理] 进入地址管理页 `/me/addresses`；点击 [新增收货地址] 直接打开新增弹窗。

地址表单字段包含：收货人、手机号、所在地区（省/市/区三级联动）、详细地址、邮政编码（选填）、地址标签（家/公司/学校/自定义）、设为默认地址。

| 操作 | 交互规则 |
| :--- | :------- |
| 新增 | 校验必填项后保存；用户第一条地址自动设为默认地址 |
| 编辑 | 表单回填原数据，保存成功后刷新使用该地址的展示，但不修改历史订单快照 |
| 设为默认 | 新默认地址生效后自动取消旧默认地址，同一用户只能有一个默认地址 |
| 删除 | 二次确认；删除默认地址后，如仍有地址则由服务端按稳定规则自动选择新的默认地址，前端刷新列表并明确提示新默认地址；若没有剩余地址则进入无默认地址状态 |
| 地址上限 | 默认最多 20 条，达到上限时禁用新增并提示先删除不用的地址 |

手机号必须符合当前支持地区的格式；错误信息显示在对应字段下方。保存期间按钮进入 loading，避免重复新增。

#### 2.8.4 我的订单与我的评价概览

| 操作 | 行为 |
| :--- | :--- |
| 点击订单状态卡片 | 跳转 `/me/orders?view=对应视图`，并自动选中订单列表筛选项 |
| 点击 [查看全部订单] | 跳转 `/me/orders?view=all` |
| 点击 [待评价] | 跳转订单列表并筛选可评价订单 |
| 点击 [查看评价] | 进入 `/me/reviews`，默认展示已评价，可切换“待评价/已评价” |
| 点击最近评价商品 | 进入对应商品详情页并定位评价区域 |

评价规则沿用 1.3.1：订单完成后每件商品可评价 1 次并追评 1 次。待评价条目提供 [去评价]；已评价条目展示评分、正文、图片、商家回复及 [追评]（满足条件时）。

#### 2.8.5 其他入口与退出登录

保留我的收藏、收藏店铺、帮助中心、关于我们、设置等入口。点击 [退出登录] 必须二次确认；确认后清除本地登录凭证和用户敏感缓存，跳转首页，但不清空服务端购物车、订单或收藏数据。

### 2.9 我的订单界面

“我的订单”以从上到下的列表展示当前用户的全部订单。每个订单底部保留统一操作区，但实际按钮完全由订单列表/详情 DTO 的 `available_actions` 驱动；前端不根据 `order_status`、日期或售后状态自行推导可用操作。

#### 2.9.1 页面整体布局（Text UI）

路由：`/me/orders?view=all`。`view` 只能使用后端定义的 `all/pending_payment/pending_shipment/in_transit/completed/pending_review/after_sale/cancelled`，前端不得把中文标签或数据库状态自由拼入参数。桌面端上一批/下一批使用接口返回的 opaque `previous_cursor/next_cursor`，小屏滚动加载只使用 `next_cursor`；仅在确有恢复浏览位置需求时把 `cursor` 原样写入 URL，不使用页码推导消息或订单位置。

```text
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│  首页  │                           你好，[用户昵称] | 购物车(2) | 消息(5) | 我的              │
├──────────────────────────────────────────────────────────────────────────────────────────────┤
│  我的订单                                                                                     │
│  [全部] [待支付] [待发货] [运输中] [已完成] [待评价] [售后] [已取消]                         │
│  搜索订单：[商品名称 / 店铺名称 / 订单号____________] [搜索]    时间：[近三个月 ▼]            │
│                                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │ 2026-08-18  订单号：ord_01K...    [复制]   [店铺Logo] XX数码旗舰店      运输中          │  │
│  │ ┌────────┐ iPhone 15 Pro           规格：黑色/256G       ¥8,999.00 × 1                 │  │
│  │ │ 商品图  │ 共1件   实付：¥8,999.00（含运费 ¥0.00）                                    │  │
│  │ └────────┘                                                                            │  │
│  │ 操作：                       [申请售后] [查看物流] [评价] [删除订单] [确认收货]          │  │
│  └────────────────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │ 2026-08-10  订单号：ord_01M...    [复制]   [店铺Logo] 米家旗舰店        已完成          │  │
│  │ ┌────────┐ 小米14 Ultra            规格：白色/512G       ¥4,999.00 × 1                 │  │
│  │ │ 商品图  │ 共1件   实付：¥4,999.00（含运费 ¥0.00）                                    │  │
│  │ └────────┘                                                                            │  │
│  │ 操作：                       [申请售后] [查看物流] [去评价] [删除订单]                   │  │
│  └────────────────────────────────────────────────────────────────────────────────────────┘  │
│                                         [加载更多 / 分页]                                     │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
```

> **布局示例声明**：Text UI 刻意列出多种按钮，只用于验收操作区排列，不表示同一订单同时拥有这些能力。实际渲染以 `available_actions` 为唯一事实源；响应未出现的 Action 隐藏，出现但 `enabled=false` 的 Action 禁用并显示它的稳定 `reason_code` 对应说明。

#### 2.9.2 订单卡片内容与跳转

| 区域 | 说明 |
| :--- | :--- |
| 订单头 | 下单时间、订单号、复制按钮、店铺 Logo/名称、订单状态 |
| 商品区 | 商品主图、名称、SKU 规格、单价、数量；多商品默认最多展示 3 条，可展开全部 |
| 金额区 | 商品总件数、实付/应付金额、运费、退款金额（如有） |
| 操作区 | 所有可用操作在卡片底部右对齐横向排列，主操作使用强调色 |

点击订单卡片非按钮区域或订单号进入 `/me/orders/:orderId`。点击商品进入商品详情页；点击店铺 Logo/名称进入店铺页。按钮点击必须阻止卡片跳转事件冒泡。

#### 2.9.3 订单状态筛选

| 筛选项 | 包含的订单状态 |
| :----- | :------------- |
| 全部 | 当前用户尚未执行用户侧隐藏的所有订单 |
| 待支付 | 已创建、尚未支付且未超时 |
| 待发货 | 已支付、商家尚未发货 |
| 运输中 | 已发货、尚未确认收货 |
| 已完成 | 已确认收货或系统自动完成 |
| 待评价 | 已完成且至少有一个商品未评价 |
| 售后 | 售后申请中、退款中、退款完成或售后关闭 |
| 已取消 | 用户取消、支付超时或管理员取消 |

订单状态以服务端返回为准；前端只做展示映射，不自行推导或推进状态。

#### 2.9.4 服务端能力驱动的操作区

订单列表和详情共享同一 `OrderAction` Schema：

```json
{
  "code": "confirm_receipt",
  "enabled": false,
  "reason_code": "SHIPMENT_NOT_DELIVERED",
  "reason_message": "包裹尚未到达可确认收货阶段",
  "requires_confirmation": true,
  "target": {"type": "route", "name": "order_detail"}
}
```

- `code` 使用 OpenAPI 固定枚举，如 `pay/cancel_order/apply_after_sale/view_after_sale/view_logistics/review/delete_order/confirm_receipt/contact_store/repurchase`；未知 Code 不渲染并上报契约告警。
- `enabled=true` 才允许发起操作；`enabled=false` 时必须有 `reason_code`，界面通过服务端安全文案或版本化 i18n 映射显示原因，不只变灰。
- 点击时服务端仍重新校验归属、当前状态、时限和售后占用；旧 `available_actions` 不是授权票据。

下表仅是产品人员理解常见服务端返回结果的**说明性示例**，不得翻译为前端状态判断代码：

| 订单状态 | 售后 | 查看物流 | 评价 | 删除订单 | 确认收货 | 其他主操作 |
| :------- | :-------- | :------- | :--- | :------- | :------- | :----------- |
| 待支付 | 隐藏 | 隐藏 | 隐藏 | 隐藏 | 隐藏 | [立即支付]、[取消订单] |
| 待发货 | [申请售后] | 有物流单后可用，否则隐藏 | 隐藏 | 隐藏 | 隐藏 | [联系商家] |
| 运输中 | [申请售后] | 显示 | 隐藏 | 隐藏 | **显示（主按钮）** | [联系商家] |
| 已完成、待评价 | 在售后时效内显示 | 显示 | **[去评价]** | 隐藏 | 隐藏 | [再次购买] |
| 已完成、已评价 | 在售后时效内显示 | 显示 | [查看评价]/[追评] | **显示** | 隐藏 | [再次购买] |
| 售后处理中 | [查看售后] | 已发货则显示 | 依据原订单完成状态 | 隐藏 | 通常隐藏 | [联系商家] |
| 已取消/退款完成 | [查看退款详情]（如有） | 隐藏或保留历史物流 | 隐藏 | **显示** | 隐藏 | [再次购买] |

“删除订单”是用户侧隐藏，不是物理删除。是否可用只看 `available_actions.delete_order`，服务端内部的 `OrderHidePolicy` 条件以 3.12.9 为权威定义；后台审计、财务和售后记录始终保留。

#### 2.9.5 关键按钮交互

| 操作 | 系统响应 |
| :--- | :------- |
| 立即支付 | 打开该待支付订单收银台；超时则提示订单已取消并刷新状态 |
| 取消订单 | 弹窗选择原因并二次确认，成功后释放库存 |
| 查看物流 | 桌面端打开右侧物流抽屉，小屏端进入物流详情页 |
| 确认收货 | 弹窗提示“请确认已收到且商品无误”，确认后更新为已完成并出现评价入口 |
| 去评价 | 进入评价编辑页，按订单商品分别填写评分、文字和图片 |
| 删除订单 | `delete_order.enabled=true` 时二次确认，调用隐藏接口；成功响应的 `undo_until、restore_url、ETag` 驱动可撤销提示 |
| 再次购买 | 将仍在售的相同 SKU 和数量加入购物车；失效 SKU 提示重新选择规格 |
| 申请售后 | `apply_after_sale.enabled=true` 时进入对应售后申请页；页面再根据资格响应提供仅退款/退货退款类型 |

隐藏成功后，Toast 倒计时以服务端 `undo_until` 为准。用户点击 [撤销] 必须使用响应中的 `restore_url` 调用 `POST /users/me/orders/{order_id}/restorations`，携 `If-Match` 和幂等键；不能只把本地卡片插回列表。超过 `undo_until`、版本冲突或结果未知时按接口结果刷新，不声称已恢复。

#### 2.9.6 分页、空状态与加载状态

- 桌面端每批默认 10 条并使用 `previous_cursor/next_cursor` 的上一页、下一页，小屏端使用滚动加载；切换筛选时取消旧请求、清空 Cursor 并从首批结果加载，不显示任意页跳转和推算总页数。
- 无订单时显示对应文案，例如“暂无待支付订单”，提供 [去首页逛逛]。
- 搜索无结果时保留搜索条件，显示 [清空搜索]。
- 首次加载使用卡片骨架屏；翻页失败在列表底部提供 [重新加载]，不清空已展示订单。
- 所有订单查询必须按当前登录用户隔离，前端传入用户 ID 不能作为权限依据。

### 2.10 单个订单详情界面

用户点击“我的订单”中的订单卡片进入单个订单详情页。此页集中展示订单状态、物流、地址、商品、金额与售后操作。

#### 2.10.1 页面整体布局（Text UI）

路由：`/me/orders/:orderId`。

```text
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│  首页  │                           你好，[用户昵称] | 购物车(2) | 消息(5) | 我的              │
├──────────────────────────────────────────────────────────────────────────────────────────────┤
│  [< 返回我的订单]   订单详情                                                                  │
│                                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │ 当前状态：运输中                                                                        │  │
│  │ 商家已发货；2 个包裹，请分别查看承运商预计范围                                           │  │
│  │ 已下单 ───────── 已支付 ───────── 已发货 ───────── ○ 已完成                           │  │
│  │                                           履约状态：待确认收货                         │  │
│  │ 订单号：ord_01K... [复制]      下单时间：2026-08-18 10:32                              │  │
│  └────────────────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │ 物流信息                                                        [查看全部物流 >]        │  │
│  │ 包裹1 · 顺丰速运  SF****7890                                      [查看物流]          │  │
│  │ 承运商预计 2026-08-22～2026-08-23 送达 · 更新于 2026-08-20 09:15 · 仅供参考 │  │
│  │ 包裹2 · 圆通速递  YT****3256 · 暂无可靠预计时间                     [查看物流]          │  │
│  └────────────────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │ 收货信息：张三 138****8888  上海市浦东新区……                                         │  │
│  └────────────────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │ [店铺Logo] XX数码旗舰店                                           [联系商家]           │  │
│  │ [商品图] iPhone 15 Pro  黑色/256G                  ¥8,999.00 × 1   [申请售后]          │  │
│  │ 商品金额 ¥8,999.00   运费 ¥0.00                               实付 ¥8,999.00          │  │
│  └────────────────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                              │
│  页面操作：                         [申请售后] [查看物流] [联系商家] [确认收货]               │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
```

#### 2.10.2 订单状态与进度

- 顶部突出当前状态和下一步说明；待支付时同时展示服务端支付截止时间倒计时。
- 订单主状态进度为：已下单 → 已支付 → 已发货 → 已完成。用户确认或系统自动确认时，履约状态同时进入“已收货”，并在时间线中追加“用户确认收货/系统自动完成”事件；不把“已收货”设计成 `orders.order_status` 中独立于“已完成”的主状态。
- 已取消订单显示取消时间和原因，不继续显示正常进度线。
- 有售后申请时在正常进度下方增加售后状态条：已申请 → 审核中 → 待退货（如需要）→ 退款中 → 已完成/已拒绝。
- 页面收到订单状态推送或重新获得焦点时刷新详情；用户发起关键操作前必须再次获取最新状态。

#### 2.10.3 信息区域

| 区域 | 必须展示的字段 |
| :--- | :------------- |
| 订单信息 | 订单号、创建时间、支付时间、支付方式、订单来源；取消时展示取消原因 |
| 收货信息 | 收货人、脱敏手机号、下单时的地址快照；不能随地址簿后续修改而变化 |
| 店铺与商品 | 店铺 Logo/名称、商品主图、商品名称、SKU、单价、数量、商品售后状态 |
| 费用明细 | 商品金额、运费、改价、实付金额、已退款金额 |
| 买家备注 | 展示结算时填写的备注；无备注时显示“无” |

若一个订单包含多个商品，售后入口放在每个商品行末，以便按公开 `order_item_id` 发起部分退款；订单级顶部操作用于全部商品退款。

#### 2.10.4 查看物流

点击 [查看物流] 打开鉴权后的包裹抽屉/页面。产品政策确定为：订单本人可在该详情中查看并复制完整运单号；订单卡片、订单概览、消息卡片和 AI 回复默认只显示掩码。进入详情后按包裹展示承运商、完整运单号、复制按钮、预计送达范围、预计来源、数据更新时间和按实际发生时间排列的轨迹节点。完整单号只能来自订单本人的物流详情 DTO，不能从掩码反推。

预计送达以 2.2.7 的 `ServiceEstimate(type=delivery)` 为唯一来源：`status=available` 时显示完整范围、来源、`source_updated_at` 和“仅供参考”；`status=unavailable`、范围缺失或过期时显示“暂无可靠预计时间”。`source=carrier` 显示“承运商预计”，`shipping_template` 显示“配送模板预计”；两者都不构成送达承诺。任何单点、富文本日期或 AI 猜测都不得显示。

| 异常场景 | 页面反馈 |
| :------- | :------- |
| 商家尚未发货 | 显示“商家正在备货，暂无物流信息” |
| 已发货但轨迹未同步 | 本人详情显示承运商和完整单号，并提示“物流信息同步中”；预计字段为空时不猜日期 |
| 多包裹 | 顶部显示包裹 Tab（包裹1/包裹2…），每个包裹分别绑定商品、单号、状态、预计范围/来源/更新时间和轨迹；订单标题仅显示“共 N 个包裹” |
| 物流异常 | 红/橙色提示异常原因，并提供 [联系商家] |

完整运单号不得写入浏览器持久缓存、URL、埋点、错误上报或复制成功提示；离开详情页后不保留在 Pinia 持久化状态。复制动作仅复制当前订单本人 DTO 的 `tracking_no`，失败时不回退复制掩码后声称成功。

#### 2.10.5 联系商家

点击 [联系商家] 后进入 2.4 商品客服界面，规则如下：

1. 复用当前用户与该店铺唯一会话 ID，不另建订单专属会话；
2. 自动把当前 `orderId` 绑定为本次会话上下文，并在输入区上方显示“正在咨询订单 O… ”；
3. 可由用户确认后发送订单卡片，订单卡片只展示必要字段，收货地址和手机号不得直接暴露；
4. 经服务端验证并处于 Active 的订单上下文可以支持本次咨询所需的最小读取，发送订单卡片只是可选展示行为而非唯一授权来源；每次读取仍必须满足“当前会话用户 ∩ 当前会话店铺 ∩ 订单归属该用户和店铺 ∩ Active Context/本次明确授权”；需平台代办退款时，引导用户进入售后退款申请页或切换到专属客服按授权处理；
5. 商品页客服与订单页“联系商家”是**同一入口、同一历史记录、同一人工客服工作台**。

用户从同一会话切换到另一订单或商品时，服务端原子停用旧的同类型 Context、激活新 Context 并递增 `context_version`；输入区提示和 Agent Run 必须绑定新版本。旧 Run、旧卡片、浏览器返回缓存或模型历史中引用的旧上下文不得继续驱动 Tool 调用，若版本不匹配则要求重新选择/确认。

#### 2.10.6 申请售后与其他操作

| Action Code | 页面行为 |
| :--- | :--- |
| `apply_after_sale` | `enabled=true` 时显示 [申请售后]，进入 `/me/orders/:orderId/refund`；可选类型由售后资格接口返回 |
| `confirm_receipt` | 显示服务端确认摘要，二次确认后调用确认收货命令 |
| `pay` | 打开该 Trade Order 收银台；不根据本地倒计时自行允许 |
| `cancel_order` | 显示服务端原因选项和影响摘要，确认后调用取消命令 |
| `delete_order` | 按 2.9.5 使用服务端 `undo_until/restore_url/ETag` 完成隐藏与撤销 |

未返回的 Action 不显示；`enabled=false` 的 Action 显示 `reason_code` 对应原因。上表不包含订单状态条件，因为该条件只在 Domain/Policy 层计算。

#### 2.10.7 权限与异常处理

- 只允许订单所属用户访问。不存在或无权限时统一展示“订单不存在或无权查看”，避免泄露订单是否存在。
- 订单加载失败展示 [重新加载] 和 [返回我的订单]；不得用旧缓存执行支付、确认收货或退款。
- 所有关键操作按钮点击后进入 loading；接口返回前禁止重复提交，成功后以服务端最新订单数据和 `available_actions` 刷新页面。

### 2.11 售后退款申请页面

售后退款申请页同时承载“仅退款”和“退货退款”。入口来自订单列表、订单详情商品行或专属客服提供的安全跳转链接。

#### 2.11.1 页面路由与类型判定

路由：`/me/orders/:orderId/refund?order_item_id=<公开订单项 ID>`。路径占位符保留 Vue camelCase，URL Query 名与 API 字段统一 snake_case；Query 值是实际 `oit_` 前缀公开 ID，不是第二个 Vue 占位符。

- 传入 `order_item_id` 时默认为单个订单商品申请；未传入时可选择订单内一个或多个可售后商品。
- 未发货订单默认提供“仅退款”；已发货或已收货订单根据商品政策提供“仅退款”和“退货退款”。
- 页面可选类型、最大可退金额、售后期限必须由服务端资格校验接口返回，前端不能自行放开。
- 重复申请按订单项的数量和金额占用判断，不按整张订单做布尔拦截：同一订单项中已被活动售后占用的数量/金额不能再次申请；同订单其他订单项及该订单项尚未占用的剩余数量仍可申请。
- 页面为每个订单项使用服务端资格投影显示“购买数量、已成功退款数量、活动申请占用数量、当前可申请数量”；当前可申请数量为 0 时，该行显示 [查看售后进度] 并禁止勾选，不影响其他仍可申请行。

#### 2.11.2 页面整体布局（Text UI）

```text
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│  首页  │                           你好，[用户昵称] | 购物车(2) | 消息(5) | 我的              │
├──────────────────────────────────────────────────────────────────────────────────────────────┤
│  [< 返回订单详情]   申请售后（退款）                                                       │
│                                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │ 订单号：ord_01K...     [店铺Logo] XX数码旗舰店                                         │  │
│  │ ☑ [商品图] iPhone 15 Pro  黑色/256G      ¥8,999.00 × 3                              │  │
│  │    购买 3｜已成功退款 1｜活动申请占用 1｜当前可申请 1｜本次最多可退 ¥8,999.00       │  │
│  └────────────────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                              │
│  售后类型 *      ● 仅退款      ○ 退货退款                                                   │
│  退款原因 *      [请选择退款原因                                      ▼]                    │
│  申请数量 *      [−] 1 [+]                                                                  │
│  退款金额 *      [¥8,999.00_______]      （是否可编辑及范围以资格接口返回为准）             │
│  问题描述         [请补充说明，最多500字__________________________________________]          │
│                   [______________________________________________________________]          │
│  上传凭证         [+ 上传图片/视频]  0/6                                                      │
│                   当前策略：JPG/PNG/WebP≤10 MiB、MP4≤100 MiB；最终以上传策略接口为准          │
│                                                                                              │
│  退款方式         原支付方式退回（预计 1～7 个工作日，以支付渠道为准）                       │
│  联系方式         138****8888                                         [修改]                │
│                                                                                              │
│  □ 我已确认所填信息真实有效，并同意《售后规则》                                             │
│                                                                    [取消] [提交申请]         │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
```

#### 2.11.3 表单字段与校验

| 字段 | 必填 | 规则 |
| :--- | :---: | :--- |
| 售后商品 | 是 | 只能选择服务端返回的可售后订单项；逐行展示 `purchased_quantity、succeeded_refund_quantity、active_reserved_quantity、available_quantity`，本次数量不得超过 `available_quantity` |
| 售后类型 | 是 | 仅展示当前订单阶段与商品政策允许的类型 |
| 退款原因 | 是 | 使用服务端原因字典；选择“其他”后问题描述变为必填 |
| 退款金额 | 是 | 资格接口返回 `amount_editable、min_refundable_amount、max_refundable_amount`；默认使用服务端建议金额。`amount_editable=false` 时只读展示，允许编辑时必须处于服务端范围内，最终金额仍由服务端重算 |
| 问题描述 | 条件必填 | 最多 500 字，实时显示剩余字数，纯空格按未填写处理 |
| 凭证 | 条件必填 | 质量问题、少件/错件等原因至少上传 1 个凭证；当前最多 6 个文件，类型、单文件大小、总容量和视频时长以服务端上传策略为准 |
| 联系方式 | 是 | 默认使用账号手机号或订单联系方式并脱敏展示；修改需校验格式，提交时保存为该售后申请的加密联系方式快照，不随账号或地址簿后续修改而变化 |
| 售后规则确认 | 是 | 未勾选时提交按钮禁用 |

上传文件需先校验类型和大小，显示上传进度，支持预览和删除；上传失败的文件不得随表单提交。前端展示的可退金额仅供确认，最终金额由服务端再次计算。

#### 2.11.4 退款金额计算与说明

```text
当前可申请数量 = 购买数量 - 已成功退款数量 - 活动申请占用数量
最大可退金额 = 本次订单项可退实付金额（含订单级改价分摊）- 已成功退款 - 进行中售后占用金额 + 本次按政策可退运费
```

- 活动售后是 `submitted、merchant_review、approved、waiting_return、returning、received、refunding`；`rejected、cancelled、closed` 不继续占用，`succeeded` 计入已成功退款。资金结果未知不得释放占用，必须先完成对账后进入确定状态。
- 数量和金额均以 `order_item_id` 汇总；一个订单中 A 商品售后中，不得阻止 B 商品或 A 商品尚未占用的剩余数量申请。
- 订单级改价按改价事务最终保存到订单项的分摊结果计算，不能简单使用商品原价或订单级总调整额。
- 部分数量退款按服务端金额分摊结果计算，处理小数尾差时以最后一件/服务端结果为准；上述公式用于解释，页面不得据此自行生成权威金额。
- 运费是否可退取决于退款原因、是否整单退和平台政策，页面需明确展示“可退运费”。
- 退款预计到账时间应在金额下方以说明文字展示。

#### 2.11.5 提交交互

1. 用户点击 [提交申请] 后执行前端校验，首个错误字段滚动到可视区域并显示原因；
2. 校验通过后弹出确认框，汇总售后类型、商品、数量和退款金额；
3. 用户确认后按钮进入 loading，携带幂等键提交，禁止重复申请；
4. 服务端锁定所涉订单项，重新校验订单状态、逐项可申请数量/金额和活动占用；仅冲突订单项返回稳定原因与最新占用投影，不用“订单已有售后”笼统拒绝；
5. 成功后跳转售后详情页或订单详情售后区域，显示售后单号、当前进度和预计处理时限；
6. 失败时保留已填表单和已上传凭证，展示可理解的错误原因及 [重新提交]。

用户在有未保存内容时点击返回、关闭页面或跳转其他导航，弹窗询问“退款申请尚未提交，确认离开吗？”。

#### 2.11.6 提交后的状态与用户操作

| 售后状态 | 页面说明 | 可用操作 |
| :------- | :------- | :------- |
| 审核中 | 已提交，展示商家/平台预计响应时间 | 撤销申请、补充凭证、联系商家 |
| 待退货 | 审核通过，展示退货地址和寄回时限 | 填写退货物流、联系商家 |
| 商家收货/退款中 | 展示商家签收和支付渠道处理进度 | 查看物流、查看退款进度 |
| 退款成功 | 展示退款金额、到账方式和完成时间 | 返回订单、联系平台人工客服 |
| 已拒绝 | 展示拒绝原因和凭证，原申请/事件只读保留 | [创建新的退款申请]（仍有可申请数量/金额时）、[创建平台申诉]；两个入口由服务端分别返回 |
| 已撤销/已关闭 | 展示关闭原因和时间，原申请不可改写 | [创建新的退款申请]（仍在售后期限且有剩余资格时） |

“重新申请”永远创建新的 `refund_application/refund_no`，不得把 `rejected/cancelled/closed` 原记录改回 `submitted`，也不得覆盖原凭证、决定和事件。“平台申诉”创建独立 `refund_appeal`，用于复核原拒绝决定，不等于再次申请。页面只按服务端 `available_actions` 分别展示 `create_new_refund_application` 与 `create_refund_appeal`；二者的资格、路由和结果页面不可合并为一个按钮。

#### 2.11.7 与 AI 客服及管理员流程的联动

- 店铺客服 Agent 对交易、经营和资金资源保持零写入，不可直接提交退款；唯一直接写动作是幂等创建当前店铺人工工单，记忆只能提出待确认候选。从商品页进入时解释当前商品售后政策并引导到订单页，从订单入口进入时也只能解释当前用户本店订单的服务端资格结果或转人工。
- 专属客服经用户授权后可读取订单、进行资格预检并构建申请草稿；只有用户在结构化确认卡片中明确确认后，才可代当前用户提交 `refund_applications`，但 Agent 不决定自动批准或人工审核。
- 申请提交后，确定性售后 Domain 规则引擎根据已发布规则版本自动推进为 `approved`，或推进为 `merchant_review` 并进入由退款申请直接投影的售后审核队列。普通审核不使用客服 `human_service_tickets`；仅争议、咨询和平台介入另建客服工单。用户端必须区分“申请已提交”“审核通过”“退款处理中”“退款到账”。
- 管理员通过、拒绝、要求补充材料或触发退款后，订单页、售后页、消息中心和 AI 会话状态必须使用同一售后数据源同步更新。

### 2.12 用户端完整页面、路由与通用交互契约

#### 2.12.1 路由、参数与公开 ID 约定

Vue Router 路径参数使用前端惯用 camelCase（如 `:productId、:storeId、:orderId、:orderItemId、:checkoutId、:tradeOrderId、:paymentId、:shipmentId、:refundId、:appealId、:reviewId`）；API Path、Query 和 JSON 一律转换为 snake_case（如 `product_id、store_id、order_id`）。`q、group_id、sort、cursor` 等 URL Query 与 API Query 保持同名。组件只能通过生成的 TypeScript Client 完成映射，禁止把 camelCase JSON 发给后端或同时兼容两套字段。

上述 ID 均是带资源前缀的公开 ID，不是 MySQL/PostgreSQL 自增 `id`。前端不得接收、缓存、展示或拼接内部主键；Cursor、上传 Ticket、Eligibility Token、Approval Token 等不透明值只能原样传递，不得解析。路由标题在数据未加载前使用通用标题，成功后只用安全公开名称更新；错误页不得把输入 ID 或后端内部原因写入标题。

#### 2.12.2 完整用户端路由表

状态缩写：`L/E/403/404/410/N` 分别表示 Loading、Empty、无权限、资源不存在、资源过期和网络失败；所有数据页都必须实现这些适用状态。`Guard` 表示离开保护：`form` 为未保存表单确认，`upload` 为未完成上传确认，`payment` 为支付结果未知提示，`none` 为无需阻断；路由切换仍保存 2.12.5 允许的非敏感恢复状态。“主要 API”每项必须是唯一 `operationId` 或完整 `METHOD /api/v1/path`，禁止 `/events、/tracks、/products` 等依赖上下文的相对缩写。本表与 OpenAPI 映射的机器可读事实源是 `docs/traceability.yaml`。

补充解释：为了表格可读性，本表中未显式写出 `/api/v1` 的完整 Path 仍统一相对该 Base Path；“完整”指必须从资源根开始，绝不允许仅写子资源缩写。如果一个单元格只写 `operationId`，Method/Path 由 `docs/traceability.yaml → OpenAPI` 唯一解析。

| 页面 | Vue Route / Component | 登录 | 参数与 Query | 主要 API | 页面标题 / 状态 / Guard |
| :--- | :--- | :---: | :--- | :--- | :--- |
| 首页 | `/` / `HomePage.vue` | 否 | 区块 Cursor 仅保存在恢复状态 | `GET /homepage` | 在线商城；L/E/N；none |
| 登录 | `/login` / `LoginPage.vue` | 否 | `redirect` 仅允许 User Audience 站内白名单路径 | `POST /auth/login` | 登录；认证失败/挑战/限流/网络未知；form |
| 注册 | `/register` / `RegisterPage.vue` | 否 | 可选安全 `redirect`，规则同登录 | `GET /auth/registration-config`、`POST /auth/verification-codes`、`POST /auth/registrations` | 注册；配置/验证码/协议变化/冲突/限流；form |
| 验证码登录 | `/login/code` / `CodeLoginPage.vue` | 否 | `redirect` 规则同密码登录 | `POST /auth/verification-codes`、`POST /auth/login` | 验证码登录；倒计时/挑战/限流；form |
| 找回密码 | `/forgot-password` / `ForgotPasswordPage.vue` | 否 | 无 | `AuthVerificationCode_Create / POST /api/v1/auth/verification-codes`、`PasswordResetTicket_Create / POST /api/v1/auth/password-reset-tickets` | 找回密码；统一模糊结果；form |
| 重置密码 | `/reset-password` / `ResetPasswordPage.vue` | 否 | 一次性 `ticket` 仅由上一步写入内存或受限 `history.state`，不进 URL/日志/持久存储 | `POST /auth/password-resets` | 重置密码；410/已使用；form |
| 法务文档 | `/legal/:documentType` / `LegalDocumentPage.vue` | 否 | `documentType` 仅允许注册表值；`version、locale、region_code` | `GET /content/legal-documents/{document_type}` | 用户协议/隐私政策；L/404/410/N；none |
| 商品搜索 | `/search` / `ProductSearchPage.vue` | 否 | `q、category_id、brand_id、price_min、price_max、sort、cursor` | `Product_Search / GET /api/v1/products`、`SearchSuggestion_List / GET /api/v1/search/suggestions` | 搜索商品；L/E/410/N；none |
| 商品详情 | `/products/:productId` / `ProductDetailPage.vue` | 否 | `productId`，可选 `sku_id` | `Product_Get / GET /api/v1/products/{product_id}`、`ProductSku_List / GET /api/v1/products/{product_id}/skus`、`ProductFaq_List / GET /api/v1/products/{product_id}/faqs` | 商品名称；L/404/N；none |
| 商品全部评价 | `/products/:productId/reviews` / `ProductReviewsPage.vue` | 否 | `rating、has_image、sku_id、sort、cursor` | `GET /products/{product_id}/reviews` | 商品评价；L/E/410/N；none |
| 店铺 | `/stores/:storeId` / `StorePage.vue` | 否 | `q、group_id、sort、cursor` | `Store_Get / GET /api/v1/stores/{store_id}`、`StoreProduct_List / GET /api/v1/stores/{store_id}/products`、`StoreHomeContent_Get / GET /api/v1/stores/{store_id}/home-content`、`StorePolicy_ListPublic / GET /api/v1/stores/{store_id}/service-policies` | 店铺名称；L/E/404/N；none |
| 购物车 | `/cart` / `CartPage.vue` | 是 | 无 | `Cart_GetMine`、`CartItem_Create`、`CartItem_Patch`、`CartItem_Delete`、`CartSelection_Replace`、`CartInvalidItem_Clear` | 购物车；L/E/412/N；none |
| 结算 | `/checkout/:checkoutId` / `CheckoutPage.vue` | 是 | `checkoutId` | `CheckoutSession_Get`、`CheckoutSession_Patch`、`CheckoutRepricing_Create`、`Order_Create` | 确认订单；L/410/412/N；form |
| 支付收银台 | `/pay/:tradeOrderId` / `PaymentCashierPage.vue` | 是 | `tradeOrderId` | `GET /trade-orders/{trade_order_id}`、`GET /trade-orders/{trade_order_id}/payments`、`POST /payments` | 支付订单；L/410/409/N；payment |
| 支付结果 | `/payments/:paymentId/result` / `PaymentResultPage.vue` | 是 | `paymentId` | `GET /payments/{payment_id}`、`GET /trade-orders/{trade_order_id}` | 支付结果；确认中/成功/失败/超时；payment |
| 消息列表 | `/messages` / `ConversationListPage.vue` | 是 | 无 | `GET /conversations`、`PUT /users/me/exclusive-conversation` | 消息；L/E/N；none |
| 单会话 | `/messages/:conversationId` / `ConversationPage.vue` | 是 | `conversationId` | `GET /conversations/{conversation_id}`、`GET/POST /conversations/{conversation_id}/messages`、`PUT /conversations/{conversation_id}/read-cursor` | 会话名称；L/403/404/N；upload |
| 我的 | `/me` / `MyDashboardPage.vue` | 是 | 无 | `GET /users/me/dashboard` | 我的；L/N；none |
| 个人资料 | `/me/profile` / `ProfilePage.vue` | 是 | 无 | `GET/PATCH /users/me` | 个人资料；L/412/N；form |
| 账号安全/设置 | `/me/settings/security` / `SecuritySettingsPage.vue` | 是 | 无 | `UserSecurity_Get`、`UserPassword_Replace`、`AuthSession_ListMine/Revoke` | 账号与安全；L/近期认证/N；form |
| AI 个性化与记忆 | `/me/settings/ai-personalization` / `AiPersonalizationPage.vue` | 是 | `scope_type、store_id、status、cursor` | `AiConsent_ListMine/Grant/Revoke`、`AiPersonalization_DisableAll`、`AiMemory_ListMine/Revise/Delete`、`CleanupTask_GetMine` | AI 个性化与记忆；L/E/409/412/N；form |
| 注销账号 | `/me/settings/account-closure` / `AccountClosurePage.vue` | 是 | 无 | `POST /users/me/account-closure-requests` | 注销账号；阻断事项/冷静期；form |
| 收货地址 | `/me/addresses` / `AddressListPage.vue` | 是 | 可选 `return_to=checkout` | `Address_ListMine/Create/Get/Patch/Delete`、`Address_SetDefault` | 收货地址；L/E/412/N；form |
| 商品收藏 | `/me/favorites/products` / `FavoriteProductsPage.vue` | 是 | `cursor` | `GET /users/me/favorite-products` | 商品收藏；L/E/410/N；none |
| 店铺收藏 | `/me/favorites/stores` / `FollowedStoresPage.vue` | 是 | `cursor` | `GET /users/me/followed-stores` | 店铺收藏；L/E/410/N；none |
| 我的订单 | `/me/orders` / `OrderListPage.vue` | 是 | `view、q、created_from、created_to、cursor` | `GET /users/me/orders` | 我的订单；L/E/410/N；none |
| 订单详情 | `/me/orders/:orderId` / `OrderDetailPage.vue` | 是 | `orderId` | `Order_GetMine / GET /api/v1/orders/{order_id}`、`OrderEvent_ListMine / GET /api/v1/orders/{order_id}/events` | 订单详情；L/404/412/N；none |
| 多包裹物流 | `/me/orders/:orderId/logistics` / `OrderShipmentsPage.vue` | 是 | `orderId` | `GET /orders/{order_id}/shipments` | 订单物流；L/E/404/N；none |
| 单包裹详情 | `/me/shipments/:shipmentId` / `ShipmentDetailPage.vue` | 是 | `shipmentId` | `Shipment_GetMine / GET /api/v1/shipments/{shipment_id}`、`ShipmentTrack_ListMine / GET /api/v1/shipments/{shipment_id}/tracks`、`ShipmentRefresh_Create / POST /api/v1/shipments/{shipment_id}/refreshes` | 物流详情；L/404/429/N；none |
| 我的评价 | `/me/reviews` / `MyReviewsPage.vue` | 是 | `view=pending/published、cursor` | `GET /users/me/reviews` | 我的评价；L/E/410/N；none |
| 发表评价 | `/me/order-items/:orderItemId/review` / `ReviewCreatePage.vue` | 是 | `orderItemId` | `GET /review-eligibilities/{order_item_id}`、`POST /reviews` | 评价商品；L/404/409/N；form+upload |
| 编辑评价 | `/me/reviews/:reviewId/edit` / `ReviewEditPage.vue` | 是 | `reviewId` | `GET/PATCH /reviews/{review_id}` | 编辑评价；L/404/409/412/N；form+upload |
| 追评 | `/me/reviews/:reviewId/append` / `ReviewAppendPage.vue` | 是 | `reviewId` | `POST /reviews/{review_id}/append-records` | 追加评价；L/409/N；form+upload |
| 我的售后 | `/me/after-sales` / `AfterSaleListPage.vue` | 是 | `status、cursor` | `GET /users/me/refund-applications` | 我的售后；L/E/410/N；none |
| 申请退款 | `/me/orders/:orderId/refund` / `RefundApplicationPage.vue` | 是 | `orderId、order_item_id` | `RefundEligibility_Check / POST /api/v1/refund-eligibility-checks`、`RefundApplication_Create / POST /api/v1/refund-applications` | 申请退款；L/409/410/N；form+upload |
| 售后详情 | `/me/after-sales/:refundId` / `AfterSaleDetailPage.vue` | 是 | `refundId` | `GET /refund-applications/{refund_id}`、`GET /refund-applications/{refund_id}/events`、`POST /refund-applications/{refund_id}/cancellations` | 售后详情；L/404/412/N；none |
| 填写退货物流 | `/me/after-sales/:refundId/return-shipment` / `ReturnShipmentPage.vue` | 是 | `refundId` | `PUT /refund-applications/{refund_id}/return-shipment` | 填写退货物流；L/409/412/N；form |
| 申诉详情 | `/me/appeals/:appealId` / `RefundAppealPage.vue` | 是 | `appealId` | `GET /refund-appeals/{appeal_id}`、`GET /refund-appeals/{appeal_id}/events`、`POST /refund-appeals/{appeal_id}/cancellations` | 平台申诉；L/404/412/N；form+upload |
| 帮助中心 | `/help` / `HelpCenterPage.vue` | 否 | `q、category、cursor` | `HelpArticle_ListPublished / GET /api/v1/content/help-articles` | 帮助中心；L/E/410/N；none |
| 帮助文章 | `/help/:contentKey` / `HelpArticlePage.vue` | 否 | `contentKey` 来自内容 Key 注册表 | `HelpArticle_GetPublished / GET /api/v1/content/help-articles/{content_key}` | 文章标题；L/404/410/N；none |
| 关于我们 | `/about` / `AboutPage.vue` | 否 | 无 | `AboutContent_GetPublished / GET /api/v1/content/about` | 关于我们；L/404/410/N；none |
| 权限不足 | `/403` / `ForbiddenPage.vue` | 否 | 安全 `from` | 无或原资源安全摘要 | 无权访问；none |
| 页面不存在 | `/:pathMatch(.*)*` / `NotFoundPage.vue` | 否 | 不回显危险路径 | 无 | 页面不存在；none |
| 资源已失效 | `/gone` / `GonePage.vue` | 否 | `reason_code、recover_to` 来自白名单 | 按资源重新创建/返回 API | 内容已失效；none |
| 系统异常 | `/error` / `SystemErrorPage.vue` | 否 | `request_id` 可复制 | 健康/重试原 GET | 系统开小差；none |
| 系统维护 | `/maintenance` / `MaintenancePage.vue` | 否 | 可选安全预计恢复时间 | 无业务 API；由网关/CDN 维护响应与 `Retry-After` 驱动 | 系统维护中；none |
| 网络离线 | 全局 Overlay / `OfflineState.vue` | 否 | 保留当前 URL | 浏览器网络状态 + 原请求重试 | 网络已断开；none |
| Session 过期 | 全局 Dialog / `SessionExpiredDialog.vue` | 是→否 | 安全 `redirect` | Token Refresh 或重新登录 | 登录已过期；form |

#### 2.12.3 登录、注册及其他补充页面 UI 与交互规格

认证页面使用独立的 `AuthLayout.vue`，不显示购物车数量、消息未读数、用户菜单或任何登录后数据。桌面端为“品牌说明区 + 认证卡片”双栏，平板和手机折叠为单栏认证卡片；品牌说明只展示商城服务价值和安全提示，不自动播放轮播图。认证卡片最大宽度建议 440px，页面主体在常见屏幕高度下垂直居中，内容超过高度时允许整页自然滚动，不能让软键盘或浏览器缩放遮挡提交按钮和错误摘要。

##### 2.12.3.1 用户登录页面

页面覆盖账号密码登录与验证码登录两种方式。`/login` 默认使用账号密码，`/login/code` 使用验证码；二者复用同一页面外壳和字段组件，但保持独立路由，便于浏览器返回、埋点、限流与错误恢复。用户已登录时访问这两个路由，不重复创建 Session，直接跳转到通过安全校验的 `redirect`，没有有效目标则返回首页。

账号密码登录 Text UI：

```text
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│  [商城 Logo] 企业级在线商城                                                   [返回首页]     │
├──────────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                              │
│  ┌────────────────────────────────────┐     ┌────────────────────────────────────────────┐  │
│  │ 安心购物，智能服务                 │     │ 登录商城                                   │  │
│  │ · 订单、支付和售后全程可追踪       │     │ [账号密码登录]  [验证码登录]               │  │
│  │ · 专属客服与店铺客服边界清晰       │     │                                            │  │
│  │ · 账号凭证和隐私数据安全保护       │     │ 账号                                       │  │
│  │                                    │     │ [用户名 / 手机号 / 邮箱________________]    │  │
│  │ 新用户？[免费注册]                 │     │                                            │  │
│  └────────────────────────────────────┘     │ 密码                         [显示密码]    │  │
│                                             │ [____________________________________]    │  │
│                                             │                        [忘记密码？]      │  │
│                                             │                                            │  │
│                                             │ [登录__________________________________]    │  │
│                                             │                                            │  │
│                                             │ 还没有账号？[立即注册]                     │  │
│                                             └────────────────────────────────────────────┘  │
│                                                                                              │
│  登录即表示本次操作受《用户协议》和《隐私政策》约束；链接可在不清空表单的情况下查看。       │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
```

验证码登录 Text UI：

```text
┌────────────────────────────────────────────────────────────┐
│ 登录商城                                                   │
│ [账号密码登录]  [验证码登录]                               │
│                                                            │
│ 手机号或邮箱                                               │
│ [________________________________________]                  │
│                                                            │
│ 验证码                                                     │
│ [________________________] [获取验证码]                    │
│                                                            │
│ [登录__________________________________________________]    │
│                                                            │
│ [使用密码登录]                    没有账号？[立即注册]      │
└────────────────────────────────────────────────────────────┘
```

登录字段和交互契约：

| 项目 | 账号密码登录 | 验证码登录 |
| :--- | :--- | :--- |
| 登录标识 | 一个字段接受用户名、规范化手机号或邮箱；最大长度与字符范围由 OpenAPI 返回，不在前端猜测账号类型 | 接受手机号或邮箱；先在本地做基础格式提示，服务端仍执行规范化和风险校验 |
| 凭证 | 密码允许粘贴、密码管理器和浏览器自动填充；默认隐藏，显示按钮具有 `aria-pressed` 和明确可访问名称 | 验证码支持自动填充 `autocomplete=one-time-code`，按服务端长度限制，只接受允许字符 |
| 提交条件 | 账号和密码非空；本地校验只改善体验，不代替服务端认证 | 目标、`verification_id` 和验证码齐全；更换目标后旧验证码立即在前端失配，提交仍由服务端拒绝旧绑定 |
| 成功结果 | Access Token 只写内存，Refresh Cookie 由服务端设置；加载当前用户安全摘要后跳转 | 与密码登录创建相同 User Audience Session，不建立“低权限验证码会话”第二套模型 |
| 失败结果 | 账号不存在、密码错误或凭证禁用统一显示“账号或密码不正确”；不指出账号是否存在 | 目标不存在、验证码错误/过期/已使用统一显示安全文案；可重新发送但不能绕过倒计时 |

页面行为：

1. 用户点击 [登录] 后先锁定当前提交按钮并显示按钮内 Loading，但账号字段仍可复制；请求返回前禁止重复提交。密码不得写入 Pinia 持久化、`localStorage/sessionStorage`、URL、日志、埋点、错误上报或恢复草稿。
2. 密码输入框开启 Caps Lock 时仅显示辅助提示，不阻止提交；浏览器和密码管理器生成/填充的长密码不得被截断。按 Enter 提交当前有效表单，不触发另一个 Tab 的隐藏表单。
3. 验证码 [获取验证码] 调用 `POST /auth/verification-codes`，`purpose=login`。成功与“目标不存在”使用相同外观；倒计时以响应 `retry_after_seconds` 和服务端时间为准，刷新页面后可从短期非敏感发送状态恢复，但验证码原文不保存。
4. 返回 `AUTH_CHALLENGE_REQUIRED` 时，在原卡片内展示服务端批准的可访问挑战组件；挑战通过后以短时 `challenge_token` 重试原登录。不得让第三方挑战脚本读取密码或 Access Token。
5. 返回 429 时显示“尝试过于频繁，请在 HH:mm 后重试”，按钮倒计时遵循 `Retry-After`；不得换接口、变更标识格式或自动循环请求规避限流。
6. 凭证验证成功后若账号为业务冻结/关闭状态，展示经认证后才允许返回的安全状态说明和申诉/人工客服公开入口，不签发普通业务 Session。`must_change_password` 命中时导航到受限重置流程，不进入商城。
7. 网络超时不能宣称登录成功或失败，也不自动重复提交密码。页面显示“暂未确认登录结果”，先检查是否已收到有效 Session；没有时允许用户明确重试。若服务端已创建多个同设备 Session，由安全会话页可见并可撤销。
8. 登录成功后只接受相对站内白名单 `redirect`；拒绝 `//evil.example`、绝对 URL、编码绕过、管理端路径及不属于 User Audience 的目标。无效目标统一回首页。
9. 从结算、订单、消息等受保护页面跳转登录时，可展示“登录后继续访问：确认订单”等安全、固定文案，不回显含敏感 Query 的完整 URL。成功返回后重新获取目标数据，不复用登录前的敏感缓存或过期资格。

登录状态与反馈：

| 状态 | 页面表现 | 恢复动作 |
| :--- | :--- | :--- |
| 初始 | 第一个字段获得焦点；不自动弹出错误 | 输入账号 |
| 校验错误 | 字段下方说明并在顶部生成错误摘要；不清空用户已输入账号 | 修正后重新提交 |
| 认证失败 | 使用统一错误文案；密码字段清空，账号保留 | 重新输入密码或切换验证码登录 |
| 验证码已发送 | 显示掩码目标、有效期和重发倒计时 | 输入验证码或修改目标 |
| 风险挑战 | 保留账号，密码只留在当前受控表单内且挑战组件不得读取 | 完成挑战/取消返回 |
| 限流 | 提交或发送按钮禁用至服务端时间 | 等待 `Retry-After` |
| 网络异常 | 显示重试和返回首页；不显示“账号不存在” | 用户主动重试 |
| 登录成功 | 按钮显示“登录成功，正在跳转”，忽略迟到重复响应 | 安全跳转目标页 |

##### 2.12.3.2 用户注册页面

路由为 `/register`，首版支持手机号或邮箱二选一完成所有权验证，同时必须设置唯一用户名和密码。注册成功后创建普通用户、User Audience Session 和固定专属客服会话，并自动登录；若原请求来自受保护页面，可按登录页相同的安全 `redirect` 规则返回，否则进入首页。注册不是创建店铺或管理员身份的入口。

注册页面 Text UI：

```text
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│  [商城 Logo] 企业级在线商城                                                   [返回首页]     │
├──────────────────────────────────────────────────────────────────────────────────────────────┤
│                              ┌────────────────────────────────────────────┐                  │
│                              │ 创建商城账号                               │                  │
│                              │ 已有账号？[直接登录]                       │                  │
│                              │                                            │                  │
│                              │ 用户名                                     │                  │
│                              │ [____________________________________]    │                  │
│                              │ 用于登录和公开展示；注册后按规则修改       │                  │
│                              │                                            │                  │
│                              │ 验证方式  ● 手机号   ○ 邮箱               │                  │
│                              │ [手机号或邮箱________________________]    │                  │
│                              │                                            │                  │
│                              │ 验证码                                     │                  │
│                              │ [____________________] [获取验证码]        │                  │
│                              │                                            │                  │
│                              │ 设置密码                     [显示密码]    │                  │
│                              │ [____________________________________]    │                  │
│                              │ 密码要求：至少 15 个字符，支持密码管理器   │                  │
│                              │                                            │                  │
│                              │ 确认密码                                   │                  │
│                              │ [____________________________________]    │                  │
│                              │                                            │                  │
│                              │ □ 我已阅读并同意《用户协议》《隐私政策》   │                  │
│                              │   当前版本：以服务端返回的版本和生效时间为准│                  │
│                              │                                            │                  │
│                              │ [注册并登录____________________________]    │                  │
│                              └────────────────────────────────────────────┘                  │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
```

注册字段契约：

| 字段 | 必填 | 前端规则 | 服务端权威规则 |
| :--- | :---: | :--- | :--- |
| 用户名 | 是 | 去除首尾空白，显示允许字符、长度与示例；输入法组合期间不误报 | NFKC/大小写等规范化、保留词/冒充/内容安全、唯一约束；不把数据库 Collation 当唯一规则 |
| 验证方式 | 是 | `phone/email` 二选一，切换前提示旧验证码将失效 | `target_type` 与 `purpose=register`、验证码记录和规范化 Target Hash 完全匹配 |
| 手机号/邮箱 | 是 | 手机号使用国家/地区码组件，邮箱只做基础格式校验 | 规范化、长度、渠道策略、风险和唯一性；原文按 3.6.13 加密，查找使用 HMAC |
| 验证码 | 是 | 绑定本次 `verification_id`；支持系统一次性验证码自动填充 | 未过期、未失效、未消费、尝试数未超限，并原子消费一次 |
| 密码 | 是 | 至少允许输入 64 个字符，允许粘贴、空格、Unicode 和密码管理器；强度提示仅作建议 | 采用 3.13.11 当前密码策略、泄露/常见密码检查与 Argon2id；不得截断后再哈希 |
| 确认密码 | 是 | 与密码逐字符一致；只在前端使用 | 不传第二份确认密码，API 只接收一次 `password` |
| 协议同意 | 是 | 用户必须主动勾选；协议/隐私链接打开后不清空表单 | 提交精确 `document_type/version`，服务端校验仍可接受且在注册事务内写不可变接受记录 |
| Locale/Timezone | 否 | 从应用和浏览器给出候选，可由用户后续修改 | 只接受白名单值；缺失使用平台默认值，不信任它做安全决策 |

注册交互和事务反馈：

1. 页面初始化调用 `GET /auth/registration-config`，读取用户名策略、密码策略、可用验证渠道、验证码长度/冷却提示以及当前必须接受的协议版本；配置失败时禁止提交，不使用前端内置旧版本协议冒充当前版本。
2. [获取验证码] 只发送 `target_type、target、purpose=register` 和风险上下文。响应为 202，并返回公开 `verification_id、expires_at、retry_after_seconds`；无论目标是否已注册都保持相同响应外观和近似时序。
3. 用户修改手机号/邮箱的规范化值或切换验证方式后，前端立即废弃当前 `verification_id`、清空验证码并停止旧倒计时；后端即使收到旧组合也必须因 Target/Purpose 不匹配拒绝。
4. 用户名不提供无频率限制的逐键实时查重。失焦时只执行本地格式提示；最终提交后，用户名冲突返回 `REGISTRATION_USERNAME_UNAVAILABLE`。验证目标冲突只有在验证码所有权已验证后才返回可操作提示“该联系方式已绑定账号，可直接登录或找回密码”。
5. 点击 [注册并登录] 后先聚焦错误摘要或进入 Loading。请求携 `Idempotency-Key`；网络超时使用同一 Key 查询/重放，不生成第二个用户、第二份协议接受记录、第二个默认角色或第二个专属客服会话。
6. 成功响应为 201；前端清除密码、验证码、Verification ID 和注册草稿，Access Token 仅驻留内存，Refresh Cookie 由服务端设置。随后获取当前用户摘要并安全跳转，不在成功页显示密码或完整联系方式。
7. 验证码错误/过期时保留用户名和协议勾选，清空验证码；密码策略失败时保留非敏感字段但清空两次密码。协议版本在填写期间更新时返回 409 `AGREEMENT_VERSION_CHANGED`，页面展示新版摘要并要求用户重新查看、主动勾选。
8. 注册成功但欢迎通知、画像初始化或其他异步任务失败不回滚账号；页面仍按成功处理。只有用户、凭证、协议接受、默认角色、唯一专属会话、Session 与 Outbox 的核心事务失败才返回注册失败。
9. 用户离开存在未提交内容的注册页时触发离开确认；允许短期恢复用户名、验证方式和协议版本，但不得恢复密码、确认密码、验证码、完整手机号/邮箱或挑战 Token。

注册状态与错误映射：

| 场景 | HTTP / 错误码 | 页面处理 |
| :--- | :--- | :--- |
| 配置加载失败 | 503 `REGISTRATION_CONFIG_UNAVAILABLE` | 禁用发送/注册，提供 [重新加载] 和 Request ID |
| 验证码发送受理 | 202 | 展示统一发送结果、掩码目标和服务端倒计时 |
| 验证码错误/过期/已消费 | 422/410 `VERIFICATION_CODE_INVALID/EXPIRED/CONSUMED` | 关联验证码字段；允许重新发送 |
| 用户名不可用 | 409 `REGISTRATION_USERNAME_UNAVAILABLE` | 聚焦用户名，保留其他非敏感字段 |
| 已验证联系方式已绑定 | 409 `REGISTRATION_TARGET_ALREADY_BOUND` | 提供 [去登录]/[找回密码]，不回显完整目标 |
| 协议版本变化 | 409 `AGREEMENT_VERSION_CHANGED` | 加载新版本并清除旧勾选状态 |
| 幂等请求处理中 | 409 `IDEMPOTENCY_REQUEST_IN_PROGRESS` | 按 `Retry-After` 使用同一 Key 查询/重放 |
| 注册成功 | 201 | 建立登录态并跳转；重复响应不重复初始化 |

##### 2.12.3.3 认证页面共用体验、安全与可访问性

- 页面使用 `autocomplete=username/current-password/new-password/email/tel/one-time-code` 等正确语义，不通过 `autocomplete=off` 阻止密码管理器；密码字段支持粘贴，不监听或上传剪贴板内容。
- 所有 Label 永久可见，不只依赖 Placeholder。必填、格式、服务端错误分别通过 `aria-describedby` 关联；提交失败先聚焦页面错误摘要，摘要中的链接可跳到对应字段。
- “显示密码”是 `button`，不改变输入值和焦点；按下状态可被屏幕阅读器读取。错误、倒计时和成功提示使用节制的 Live Region，验证码倒计时不每秒播报。
- 验证挑战必须有键盘、屏幕阅读器或等效替代方案，不把图片验证码作为唯一通道。连续失败时可提高挑战强度，但提供可联系人工客服的公开安全入口。
- 认证页加载的第三方资源最小化并受 CSP、SRI/版本锁定和 Origin Allowlist 约束。禁止在认证页加载广告、会话回放、热力图或采集输入值的第三方脚本。
- 前端错误上报只记录 `error_code、request_id、route、client_version` 等安全字段；账号、手机号、邮箱、密码、验证码、Token、Cookie 和协议接受明细不进入埋点。
- 用户协议/隐私政策链接使用普通可访问链接打开 `/legal/:documentType?version=...`；认证表单中明确提示“在新窗口打开”，当前页不因此卸载或持久化密码。法务页显示标题、精确版本、生效时间、适用地区、正文和内容 Hash 摘要，404/410 时不拿当前新版冒充用户点击的旧版本。
- 手机端表单使用合适键盘类型但不能依赖键盘限制作为校验；输入框缩放后字号不小于 16px，提交按钮和协议链接满足 44×44 CSS px 触控目标。
- 登录/注册页的页脚显示协议、隐私、安全中心和帮助入口；这些公开页面失败不能阻断输入，但注册提交所需的权威协议配置不可用时必须 Fail Closed。

找回密码、重置密码等其他认证页面复用本节 `AuthLayout`、验证码组件、密码组件、错误摘要和安全规则。验证码倒计时以服务端 `retry_after_seconds` 为准；重置密码 Ticket 过期显示 [重新找回密码]，成功后清理表单和受限 Ticket，再跳转登录。

搜索、收藏与列表类页面使用统一结构：

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ 标题 / 搜索框                                                               │
│ [分类] [品牌] [价格] [有货]                         排序 [综合 ▼]          │
│ 已选条件：[手机 ×] [品牌A ×]                              [清除筛选]       │
│ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐                              │
│ │商品卡片│ │商品卡片│ │商品卡片│ │商品卡片│   → Cursor 加载             │
│ └────────┘ └────────┘ └────────┘ └────────┘                              │
│                         [加载更多 / 重新加载]                               │
└──────────────────────────────────────────────────────────────────────────────┘
```

- 搜索提交更新 URL 并重置 Cursor；浏览器前进/后退按 URL 恢复筛选和锚点商品。无结果展示当前条件、[清除筛选] 和推荐分类，不用首页结果冒充搜索结果。
- 商品全部评价页展示评分分布、`全部/有图/规格/评分`筛选、Cursor 和安全图片预览；筛选变化回到列表顶部，详情页只展示两条摘要。
- 我的评价分“待评价/已评价”；发表、编辑与追评使用同一编辑器外壳，但根据服务端 `available_actions` 决定字段可编辑性。离开未提交文本或上传任务必须确认。
- 商品/店铺收藏空状态分别提供 [去逛商品]/[去发现店铺]；失效资源保留可删除的受限摘要，不跳转到不存在详情。

支付页面分为“收银台”和“结果页”：收银台先展示 Trade Order、店铺订单摘要、金额、支付截止时间与渠道，再由 [确认支付] 创建 Payment；结果页只根据 `GET /payments/{payment_id}`、Trade Order 查询或可信实时事件进入 `confirming/succeeded/failed/closed`。`confirming` 显示“支付结果确认中”和 [刷新结果]，禁止再次支付；超过前端等待上限也只跳转订单详情继续查询，不能自行判失败。明确失败或关闭后，只有 `available_actions.pay=true` 才能返回收银台创建新支付尝试。

物流采用“订单包裹列表 → 单包裹详情”两级页面。列表逐包裹展示运单掩码、商品、状态、预计范围/来源/更新时间；详情按 2.10.4 仅向订单本人展示可复制完整单号和轨迹。多包裹不得合并状态，刷新按钮遵循 429 `Retry-After`。

售后采用“我的售后 → 售后详情 → 填写退货物流/申诉详情”路径。详情展示申请商品、逐项数量/金额、不可变状态时间线、凭证、退款资金进度和服务端 `available_actions`；填写退货物流是独立表单，成功返回详情。被拒绝后的“创建新申请”和“平台申诉”进入不同页面/资源，原申请始终只读。

设置页包含资料、安全会话、修改密码、联系方式与账号注销入口。账号注销必须列出未完成订单、退款、余额/争议等服务端阻断事项、冷静期和后果，要求近期认证和明确确认；提交后展示申请状态，不立即在前端删除数据。

错误页面必须提供可执行恢复入口：401/Session 过期 [重新登录并返回]，403 [返回上一页/首页]，404 [返回首页]，410 Checkout [重新结算]、Cursor [从第一页加载]、重置 Ticket [重新申请]，500 [重试并显示 Request ID]，维护页 [稍后重试]，离线状态 [网络恢复后重试]。错误页不显示堆栈、内部表名、权限码或其他用户资源是否存在。

##### 2.12.3.4 AI 个性化与记忆页面

`/me/settings/ai-personalization` 是 Consent 和长期记忆的用户权威控制面，不要求用户必须通过聊天管理隐私。页面按“专属客服”和“已授权店铺”分组，每个 Scope 展示授权类型、用途、告知版本、到期时间、暂停状态、Active/Candidate 数和最近清理任务。

```text
┌─ AI 个性化与记忆 ────────────────────────────┐
│ 总开关：[已开启]  用途：商品推荐与连续服务  [关闭全部] │
│ 专属客服  Consent:con_...  到期:2027-02-21 [暂停] [续期]   │
│   · 预算偏好：¥200–¥500  来源:你在 08-21 明确设置 [修改] [删除] │
│   · 颜色偏好：蓝色  [待确认] [确认保存] [不保存]       │
│ 店铺：示例旗舰店  Consent:con_...  [已暂停] [恢复]        │
│ 清理任务 job_... [处理中 3/5] [刷新]  失败项:[查看与重试]       │
└─ 记忆不包含密码、完整地址、支付凭证或实时订单状态 ────────┘
```

交互契约：

1. [关闭全部] 先展示影响的 Scope/记忆数和“立即停止召回、派生数据异步清理”说明；确认后提交一个幂等命令，不在前端逐项删除。
2. [暂停]/[恢复]/[续期] 都以服务端返回的 `consent_id + ETag` 定位；店铺 Scope 必须显示店铺名称并再校验 `store_id`，禁止仅用 `consent_type` 覆盖另一店铺。
3. [修改] 打开结构化编辑对话框，明确显示旧值/新值、Scope、用途和到期时间；确认后追加新 Memory Version，412 时展示差异并要求重新确认。
4. [删除] 成功后立即不再召回，页面显示 202 返回的 Cleanup Task；任务失败可查看最小原因并幂等重试，不把“已停止使用”误写成“备份已物理删除”。
5. Empty 状态说明基础 AI 问答仍可用；403/404 不泄漏其他用户/店铺记忆；409/412/网络未知时查询最新资源，不本地猜测。

##### 2.12.3.5 帮助、关于与友情链接

`/help`、`/help/:contentKey`和 `/about` 只渲染 Published 版本化内容。Loading 保留标题骨架，404 表示 Key 不存在，410 表示已撤回/过期并提供返回帮助中心，Network 仅重试原 GET。安全富文本由服务端清洗并带 Version/CSP，前端不执行脚本、内联事件或未登记 Scheme。页脚友情链接只来自已发布 URL Allowlist，外链显示外部站点标识，使用安全跳转且固定 `target="_blank" rel="noopener noreferrer"`。

#### 2.12.4 全局响应式矩阵

断点以内容可用性而非设备型号确定，建议 `<768px` 手机、`768～1199px` 平板、`≥1200px` 桌面；200% 缩放后仍按可用宽度触发重排。

| 页面 | 桌面 | 平板 | 手机 |
| :--- | :--- | :--- | :--- |
| 登录/注册 | 品牌说明 + 440px 认证卡片双栏；错误摘要不改变卡片宽度 | 隐藏非必要品牌说明，认证卡片居中 | 单列全宽、安全边距；软键盘下主按钮可滚动到达，不做遮挡式底栏 |
| 首页 | Banner+分类并排，商品 4～6 列 | 分类横向，商品 3～4 列 | 单列模块，商品 2 列 |
| 商品详情 | 图片/内容 + Sticky 购买区 | 购买区进入正常流，图库双列 | 全部单列 + 安全底部购买栏 |
| 购物车 | 店铺分组表格 + 底部汇总 | 商品行压缩、操作换行 | 卡片列表 + 安全底部结算栏 |
| 消息 | 会话列表/聊天双栏 | 可折叠双栏 | `/messages` 列表 → 单会话路由 |
| 结算 | 地址、店铺组、金额分区 | 单列内容、汇总 Sticky | 单列 + “金额/提交订单”底栏 |
| 店铺 | 店铺头部、侧/顶分类、商品网格 | 分类横向、3～4 列 | 头部压缩、分类横向、2 列 |
| 我的 | 左导航 + 右内容 | 顶部 Tab + 内容 | 卡片入口单列，子页面独立路由 |
| 订单/物流 | 筛选栏 + 宽订单卡片/时间轴 | 操作按钮可换行 | 单列卡片，操作区横向滚动或两列 |
| 售后/评价 | 左摘要 + 右表单/时间轴 | 单列分区 | 单列，主操作底栏且不遮挡错误 |

固定顶部栏、Sticky Tab 和底部操作栏必须遵循 2.2 层级、`scroll-padding`、Safe Area 与主体占位规则。横向滚动区域必须有可见提示和键盘操作；不得通过缩小字体、隐藏关键金额/状态或改变 DOM 顺序来“适配”。

#### 2.12.5 全局状态、冲突与恢复

- 每个页面分别实现首屏 Loading、局部 Refresh、Empty、403、404、410、Network Error；局部失败不得抹掉已成功区域。Skeleton 不参与 Read Cursor、埋点曝光或可用按钮判断。
- Access Token 失效先执行单次受控 Refresh；Refresh 失败弹出 Session 过期 Dialog，保存当前 URL、公开筛选、表单中非敏感草稿和滚动锚点，登录后回到原目标。密码、验证码、完整地址、支付参数、上传二进制和 Approval Token 不保存。
- 登录/注册页面只可短期恢复用户名、验证方式、协议版本和安全 `redirect`；不得恢复密码、确认密码、验证码、完整手机/邮箱、Challenge/CSRF/Access/Refresh Token。认证响应为 `no-store`，Service Worker 不缓存 `/auth/*`。
- 写操作返回 412 时停止提交、获取服务端最新资源/ETag、展示“内容已被更新”及字段级差异；用户确认后基于新版本重新编辑，不静默覆盖，也不自动复用旧确认。
- 429 必须读取 `Retry-After`，在倒计时结束前禁用对应动作；仅 GET 可按策略有界自动重试，写操作不得无限重试或换 Idempotency Key。
- 410 `CHECKOUT_SESSION_EXPIRED` 提供 [按原来源重新结算]，但重新读取当前商品/购物车；`CURSOR_EXPIRED` 保留筛选并从第一页重新加载，再尽量恢复锚点；过期重置/上传/授权 Ticket 必须重新发起对应流程。
- Network Timeout 后若写结果未知，页面进入“正在确认结果”并使用原 Idempotency Key 查询；禁止先回滚 UI 再提交第二次。离线期间可保留草稿，恢复网络后由用户确认重试。

#### 2.12.6 可访问性规范

- 优先使用原生 `button、a、input、dialog`；确需自定义可操作元素时必须具有正确 Role、名称、状态，并同时支持 Enter 与 Space。所有功能均可用键盘完成，不依赖 Hover、右键、拖拽或仅触屏手势。
- 页面首个可聚焦元素提供“跳到主要内容”；使用 `header/nav/main/aside/footer` Landmark 和连续语义标题层级。路由切换后把焦点置于页面 `h1`，但浏览器返回恢复原焦点/锚点。
- `:focus-visible` 使用不低于 2px、与背景至少 3:1 的焦点环，不能被 Sticky 区裁切。正文/关键文字对比度至少 4.5:1，大号文字和图形控件至少 3:1；状态不得只靠颜色、位置或图标表达。
- 表单错误通过 `aria-describedby/aria-errormessage` 关联字段，提交失败后聚焦错误摘要并可跳到首个错误。Loading、保存成功、冲突、断线、新消息和异步结果使用节制的 Live Region；AI 流式 Token 不逐字播报。
- 指针触控目标建议至少 44×44 CSS px，相邻目标保留间距。支持 200% 缩放、文本放大、横竖屏和 `prefers-reduced-motion`；减少动画模式关闭自动轮播、平滑滚动和非必要位移动画。
- 模态框/抽屉打开后焦点进入并圈定在容器内；非强制弹窗支持 Esc，危险确认可要求显式按钮但仍提供可访问取消；关闭后焦点返回触发元素。背景使用 `inert/aria-hidden`，不得让屏幕阅读器访问遮罩后内容。
- 商品图、凭证和头像具有与语境匹配的 Alt；装饰图 Alt 为空。图表同时提供文本摘要/数据表，验证码不得只提供无法替代的图形挑战。

#### 2.12.7 用户端开发验收要点

1. 完整路由表中的每一页面均能映射到 Vue Component、OpenAPI Operation、鉴权条件、状态集合和至少一个 E2E Case；不存在只有入口没有目标页或只有 API 没有恢复 UI 的孤儿项。
2. `/login`、`/login/code` 与 `/register` 的 AuthLayout、Text UI、字段、校验、错误摘要、挑战、倒计时和安全 `redirect` 均按 2.12.3 实现；注册配置、验证码 Purpose/Target、协议版本、幂等结果和 User Audience Session 与 3.12.1 契约一致，不保存或回显密码、确认密码、验证码和 Token。
3. 首页模块可独立降级，搜索进入独立 `/search`；商品详情首次停留顶部，SKU 图片集回退、Sticky 与移动购买栏正确且不产生嵌套整页滚动。
4. 订单创建与 Payment 创建使用两个独立幂等步骤；Payment 创建失败、渠道返回、结果未知、取消和明确失败均不重复创建订单或误判成功。
5. 结算联系客服只建立本店 `checkout_store_group` 上下文；跨店、旧 Checkout Version、已过期/已消费 Checkout 和完整地址/优惠信息均不能进入店铺客服。
6. 消息页在桌面/手机可恢复同一 URL/草稿，`…` 菜单可键盘使用，右键不是唯一入口；历史滚动不强制到底，发送失败可用同一 `client_message_id` 重试，流式内容不逐 Token 播报。
7. 全站 `storeId/orderId/orderItemId/checkoutId` 仅作为 Vue 路由参数命名，调用 API 时统一转换为 snake_case 公开 ID；店铺查询统一 `q/group_id/sort/cursor`。
8. 首页、认证、商品、购物车、消息、结算、店铺、我的、订单、物流、评价和售后通过桌面/平板/手机及 200% 缩放测试，所有固定栏不遮挡内容或焦点。
9. 401/403/404/410/412/429、离线、维护和 Session 过期均有明确恢复路径；写结果未知不自动重复副作用。
10. 键盘、焦点、Landmark、标题、Live Region、对比度、触控目标、Reduced Motion、弹窗焦点圈定和焦点恢复通过自动扫描与人工验收。
11. 进入“消息”后专属客服始终位于左栏第一项，店铺会话滚动、排序、隐藏或收到新消息均不能改变其位置，且页面中不存在首页/全局 AI 悬浮入口。

### 2.13 管理后台界面设计

管理后台属于首版交付范围，与用户商城使用同一业务主库和领域服务，但采用独立入口、独立前端路由树、独立管理端 Session Audience 和更严格的身份认证。后台不是能够任意修改数据库的“万能界面”；页面上的每个按钮都必须映射到 3.12.22 的明确查询或命令接口，并继续经过 RBAC、数据范围、业务状态、乐观锁、近期认证、职责分离和审计校验。

#### 2.13.1 管理端角色、入口与边界

管理端入口为 `https://admin.example.com`，前端基础路由为 `/admin`。生产管理端 Session 只能访问管理 API，不能用于用户商城购物、加购或下单；测试购物使用独立 Sandbox 环境和普通测试用户账号。一个自然人即使同时具备消费者、店铺员工、人工客服或平台管理员身份，也必须通过对应入口获得不同 `client_type/aud` 的会话，禁止前端复用 Token。

| 管理身份 | 默认数据范围 | 主要页面 | 禁止行为 |
| :--- | :--- | :--- | :--- |
| 超级管理员 | 平台，但仍受关键操作职责分离约束 | RBAC、系统治理、审计、紧急干预 | 日常使用超级权限、读取明文密码、绕过资金状态机 |
| 平台运营/审核员 | 获授平台业务范围 | 用户治理、店铺审核、商品治理、售后申诉、内容运营 | 访问未授权敏感字段、代替店铺处理无权限订单 |
| 店铺负责人/运营 | 被授予的一个或多个店铺 | 本店商品、SKU、库存、订单、发货、退款、评价、客服 | 切换或伪造未授权 `store_id`、读取其他店铺数据 |
| 平台/店铺客服 | 工单所属平台或店铺队列 | 人工客服工作台、必要业务摘要 | 无业务理由浏览全量会话、查看内部风控或资金密钥 |
| AI 运营/评估人员 | 获授 Agent/知识/评估范围 | Agent、Skill、Tool、知识库、评估与观测 | 查看隐藏推理、Secret、未经授权的原始用户敏感内容 |
| 审计/运维人员 | 只读审计或受控运维范围 | 审计、任务、死信、部署状态 | 修改审计历史、编辑死信 Payload 后冒充原事件 |

菜单由服务端返回的权限与数据范围投影生成；隐藏菜单只改善体验，不构成授权。直接输入 URL、调用 API 或恢复旧浏览器页面时仍必须重新鉴权。无权限资源通常展示统一 404/不可见页；允许暴露功能但权限不足时展示 403、所需权限码和申请权限入口，不展示其他租户数据。

#### 2.13.2 管理后台总体布局（Text UI）

```text
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│ [平台Logo] 企业商城管理后台     当前范围：[XX店铺/全平台 ▼]    通知(3)  帮助  [头像 管理员]   │
├──────────────────┬───────────────────────────────────────────────────────────────────────────┤
│ 仪表盘           │ 首页 / 业务模块 / 当前页面              数据截至 2026-08-22 10:30 [刷新] │
│ 用户与权限       ├───────────────────────────────────────────────────────────────────────────┤
│ 店铺管理         │                                                                           │
│ 商品与库存       │                         当前页面内容区                                    │
│ 订单与履约       │                                                                           │
│ 退款与评价       │       查询条件、数据表格、详情抽屉、批量任务与操作结果                    │
│ 客服工作台       │                                                                           │
│ AI 与知识库      │                                                                           │
│ 评估与可观测性   │                                                                           │
│ 内容运营         │                                                                           │
│ 审批中心         │                                                                           │
│ 审计与系统任务   │                                                                           │
├──────────────────┴───────────────────────────────────────────────────────────────────────────┤
│ 环境：Production  版本：Web/API/Schema/Agent Release                 Request ID 可复制       │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
```

- 左侧菜单支持折叠，但权限变化后必须立即重新拉取菜单；失去权限的已打开页面立即停止后续读取和写操作。
- 顶部“当前范围”只显示当前账号被授予的 Scope。平台管理员切换店铺仍需具备跨店权限；店铺员工不能通过下拉框扩大范围。
- 内容区默认采用“筛选区 + 表格/卡片 + 详情抽屉/独立详情页”。涉及长表单、版本 Diff、Trace 或多步骤发布时使用独立页面。
- 列表默认 Cursor 分页；只对稳定小字典使用页码。批量导入、导出、索引和评估统一进入异步任务中心。
- 桌面端最小支持宽度为 1280px；小屏仅提供审批、告警确认和客服等明确支持的响应式页面，不承诺在手机上完成复杂商品编辑、批量导入或 Trace 分析。

#### 2.13.3 管理员登录与安全验证

路由：`/admin/login`、`/admin/mfa-challenge`、`/admin/reauth`。

1. 管理员输入账号和密码，通过账号/IP/设备风险检查后进入 MFA；首版至少支持 TOTP，优先支持 WebAuthn/Passkey。
2. 登录成功后签发短时 `client_type=admin` Session；Access Token 只驻留内存，Refresh Cookie 限制在管理 API 路径。
3. 登录、换设备、权限提升、查看高敏字段、导出、资金调整、发布、重放等动作可要求近期认证；近期认证超时后使用 `/admin/reauth`，成功后返回原操作。
4. MFA 恢复码只在生成时展示一次；后台不得查看管理员的 MFA Secret。连续失败触发限流、风险告警和必要的账号保护。
5. 会话到期、被强制下线或权限版本变化时，前端清除内存 Token 和敏感缓存，保存不含敏感值的目标路由，重新登录后再做权限判断。

#### 2.13.4 管理仪表盘

路由：`/admin/dashboard`。仪表盘只展示当前 `data_scope` 范围内的聚合指标，并在卡片上标明指标口径、统计窗口、数据更新时间和是否为实时/最终一致数据。

| 区域 | 指标/内容 | 交互 |
| :--- | :--- | :--- |
| 交易概览 | GMV、支付订单数、支付成功率、客单价、退款金额 | 时间范围和店铺范围筛选；跳转到带同一筛选的列表 |
| 商品库存 | 在售商品、待审核商品、低库存 SKU、库存冲突率 | 进入商品/库存待处理队列 |
| 履约售后 | 待发货、物流异常、待审核退款、超时退款 | 进入相应操作队列，不在仪表盘直接跳过详情审批 |
| 客服 | 排队工单、平均首响、人工转接率、AI 自助解决率 | 按平台/店铺队列查看 |
| AI 质量与成本 | Agent 成功率、负反馈、越权阻断、Token/成本、依赖故障 | 进入脱敏 Run/评估/告警详情 |
| 系统健康 | API/Worker/Stream、支付/物流、索引任务和死信摘要 | 只有运维权限可执行重放或干预 |

接口失败时单卡片独立降级，不能因 AI 指标不可用而使订单指标页面整体失败。指标卡不得使用前端累加替代服务端聚合；金额使用统一 Money，时间范围使用服务端解释的半开区间。

#### 2.13.5 用户治理与 RBAC

路由：`/admin/users`、`/admin/users/:userId`、`/admin/roles`、`/admin/roles/:roleId`。

- 用户列表展示公开用户 ID、脱敏账号、状态、注册时间、最近登录时间和安全摘要；默认不返回完整手机、邮箱、地址或订单内容。
- 用户详情的“冻结/解冻”“强制下线”“要求重置密码”使用显式命令、原因码和说明。管理员不能录入、生成或查看用户新密码。
- 高敏字段临时查看必须具有独立权限、业务理由、近期认证和审计，并设置自动遮罩恢复；普通列表、导出和截图不得包含明文。
- 角色页面展示 Permission、Scope 和风险等级；角色变更使用 ETag。禁止操作者授予自己未拥有/不可委派的权限，禁止删除最后一个可恢复系统的安全管理员。
- 用户角色分配必须明确平台或具体店铺 Scope、有效期和理由；影响当前会话时实时撤销或递增 `permission_version`。

```text
┌──────────────────────────── 角色权限变更确认 ────────────────────────────┐
│ 对象：管理员 usr_...                                                     │
│ 变更：新增 refunds:review，范围：店铺 sto_...，有效期：2026-12-31        │
│ 风险：可查看并审核该店售后申请                                           │
│ 原因：[________________________________________]                         │
│ 近期认证：已通过（剩余 3 分钟）                         [取消] [确认]    │
└──────────────────────────────────────────────────────────────────────────┘
```

#### 2.13.6 店铺审核与店铺运营

路由：`/admin/store-certifications`、`/admin/stores/:storeId` 及其运营子页面。

- 店铺认证队列支持按状态、类型、申请时间和风险标记筛选；材料通过鉴权下载，页面水印显示管理员和访问时间。
- 审核决定为“通过、拒绝、要求补充材料”，原因必填且历史不可覆盖。暂停/关闭店铺是独立状态命令，并展示对在售商品、历史订单、客服和售后的影响预览。
- 店铺员工只能管理本店分组、公告、推荐商品、服务政策和配送模板；所有关联商品必须属于本店。
- 配送模板以版本发布，编辑草稿不影响现有结算；发布前展示影响地区、预计运费样例和引用商品数，发布后触发 Checkout/商品缓存失效。
- 店铺页面顶部始终展示当前 Scope；从平台视角切换店铺时清空旧列表 Cursor、选择项和批量操作状态。

#### 2.13.7 商品、SKU、分类、品牌与库存

路由：`/admin/products`、`/admin/products/new`、`/admin/products/import`、`/admin/products/:productId`、`/admin/inventories`、`/admin/categories`、`/admin/brands`；导入预检与执行进度统一进入 `/admin/system/jobs`、`/admin/system/jobs/:jobId`。

商品编辑使用分区表单：基础信息、分类品牌、规格与 SKU、图片、属性、履约资料、FAQ、富文本详情和发布检查。自动保存只保存草稿；上架必须点击明确发布命令并通过完整校验。

| 功能 | 关键规则 |
| :--- | :--- |
| 商品创建/编辑 | 店铺 Scope 从管理身份派生；If-Match；未知字段拒绝；服务端执行富文本安全过滤 |
| SKU 管理 | 规格组合唯一；已被订单引用的 SKU 只能禁用，不能物理删除；价格使用 Money |
| 图片 | 先上传、扫描和激活，再绑定商品；支持排序、主图和 SKU 图；失败文件不可发布 |
| 提交审核/发布 | 展示缺失字段和影响预览；使用显式 publication/moderation 命令；结果写状态流水 |
| 分类品牌 | 平台权限维护；移动分类防环；已被引用的品牌/分类停用而非级联删除 |
| 库存调整 | 录入有符号调整量、原因码、业务单号和备注；显示调整前后值；禁止直接设置无来源的最终库存 |
| 批量导入 | 上传模板后异步预检，展示逐行错误；用户确认有效批次后再执行；支持下载错误报告 |

库存页不得展示安全库存算法、风控阈值等内部字段给无权限店铺员工。并发调整使用当前库存版本；发生冲突时保留用户输入，刷新最新值并要求重新确认，不自动覆盖。

#### 2.13.8 订单、支付与履约管理

路由：`/admin/orders`、`/admin/orders/:orderId`、`/admin/payments`、`/admin/payments/:paymentId`。

- 订单列表支持订单号、店铺、用户掩码、状态、支付、履约、售后和时间筛选。店铺员工始终附加本店 Scope；平台跨店查询需要独立权限。
- 详情按“订单事实、支付、包裹、售后、用户可见时间线、受控操作”分区。收货信息只在履约必要期和权限范围内解密，访问行为写审计。
- 未支付改价使用专用命令，展示原金额、调整额、分摊结果和影响；存在进行中或结果未知的支付尝试时按钮不可用。支付后资金修正只能进入退款/补偿流程。
- 发货按包裹提交 `order_item_id + quantity`、承运商和运单号；多包裹数量合计不得超过可发数量。提交前展示完整影响，成功后返回包裹和订单最新状态。
- 支付页只读展示支付尝试、渠道状态和对账结果；管理员不能把 Payment 直接改为成功。结果未知时仅允许发起受控查询/对账任务。
- 所有订单按钮依据后端 `available_actions`；状态变化、ETag 冲突或其他管理员已处理时刷新页面并说明原因。

#### 2.13.9 退款、申诉与评价治理

路由：`/admin/refund-applications`、`/admin/refund-applications/:refundId`、`/admin/refund-appeals`、`/admin/refund-appeals/:appealId`、`/admin/reviews`。

- 退款队列按 Store Scope、状态、申请时间、审核截止时间、金额和风险标记筛选；领取/决定使用 If-Match，防止多人同时处理。
- 详情展示订单项、服务端资格结果、申请金额、凭证、协商事件和用户可见政策快照。决定可为同意、拒绝或要求补充材料，原因和用户可见说明必填。
- 高金额、平台申诉、超规则补偿和职责冲突进入双人复核；Agent 不出现在审核人选择中，也不能触发审批结果。
- 申诉是独立资源，平台决定不得覆盖原退款历史；支持申诉后的补偿通过新命令和事件执行。
- 评价治理支持状态筛选、内容查看、屏蔽/恢复及商家回复；违规处置必须选择规则条款和原因，不能物理删除交易评价。匿名评价对公开用户匿名，不对有权审计人员伪装为无来源。

#### 2.13.10 人工客服工作台

路由：`/admin/support/tickets`、`/admin/support/tickets/:ticketId`。

```text
┌──────────────────┬──────────────────────────────────────┬──────────────────────────┐
│ 队列/工单列表     │ 当前会话（用户可见消息）             │ 业务辅助区               │
│ [待领取 12]      │ 用户/AI/人工时间线                   │ 用户脱敏摘要             │
│ [我的处理中 4]   │                                      │ 商品/订单/物流/售后卡片  │
│ [等待用户 3]     │ [输入回复……]              [发送]    │ 内部备注（用户不可见）   │
│ [已解决]         │ [转派] [等待用户] [结束服务]         │ 权限与访问理由           │
└──────────────────┴──────────────────────────────────────┴──────────────────────────┘
```

- 平台工单与店铺工单进入不同队列；店铺客服不能读取平台内部工单，跨队列升级需显式转派和审计。
- 领取必须原子成功；失败时提示已被其他客服领取。转派、结束服务和调整优先级均要求原因。
- 用户可见消息与内部备注在数据模型、API 和视觉上分离；内部备注不得进入用户消息、Agent 上下文或普通导出。
- 打开订单、退款或敏感资料前记录业务理由；页面只请求当前工单所需的最小字段。
- 人工接管期间 AI 面向用户回复处于禁用状态；人工结束后只恢复 AI 可用，不自动生成消息。

#### 2.13.11 AI、Skill、MCP Tool 与权限策略管理

路由：`/admin/ai/agents`、`/admin/ai/skills`、`/admin/ai/tools`、`/admin/ai/policies`。

- Agent、Skill 和 Tool 均采用 Definition 与 Version 分离；已发布版本不可原地修改。编辑产生草稿版本，页面提供 Prompt/Graph/Schema/Tool Version Binding/权限/预算的结构化 Diff。
- 发布前必须运行依赖检查、安全测试、Golden Dataset 和成本评估；未达门槛时发布按钮禁用并链接失败报告。
- Tool 页面展示 MCP Server、输入输出 Schema、风险级别、网络范围、Secret 引用名称和健康状态，但不展示 Secret 值。
- 权限策略采用默认拒绝，明确 Agent Version、Skill Version、Tool、角色、资源 Scope、Consent、Approval 和额度。
- 发布、回滚、Disable 和 Kill Switch 均需独立权限、近期认证和原因；高风险发布实行双人复核。紧急 Kill Switch 可先阻断后补充复核，但必须自动告警和生成事件。
- 管理页面和 Trace 不展示 Chain-of-Thought；只展示输入类别/Hash、版本、节点、工具调用摘要、结果状态和经授权的脱敏业务字段。

#### 2.13.12 知识库、索引、评估与可观测性

路由：`/admin/knowledge/documents`、`/admin/knowledge/indexing-jobs`、`/admin/ai/evaluations`、`/admin/ai/runs`、`/admin/observability`。

- 知识文档创建时选择平台/店铺 Scope、知识类型和有效期；店铺员工只能选择本店。原文件扫描通过后才允许解析和索引。
- 文档页展示版本、内容 Hash、切片/索引状态、引用范围和当前 Active 版本；重新索引使用异步 Job，旧索引在新版本 Ready 前继续服务。
- 评估页支持选择候选 Model/Prompt/Agent/Skill/Tool Version，并从部署时注册的只读评估基线中发起运行，随后展示质量、安全、延迟和成本报告。首版管理后台不提供 Dataset 创建、编辑、样本浏览、版本发布或删除能力；Release Holdout 默认不展示答案全文给开发者。
- Run/Trace 页默认只显示脱敏时间线，不展示原始 Prompt、Secret、完整 PII 或隐藏推理。申请受控诊断样本需专项权限、理由、短时访问和审计。
- 可观测性页面显示 API、Worker、Redis Streams、模型、MCP、RAG 和业务 SLO；高基数用户/订单值不作为监控标签。

#### 2.13.13 内容运营、审计与系统任务

路由：`/admin/content`、`/admin/audit-logs`、`/admin/system/jobs`、`/admin/system/dead-letter-events`。

- 内容运营管理首页 Banner、平台公告、帮助中心文章、页脚链接，以及注册所需用户协议/隐私政策等法务文档。内容使用草稿—审核—发布—撤回版本流程，支持生效时间、失效时间、地区、语言、受众和排序；富文本按安全白名单发布。法务文档发布后版本正文与 Hash 不可原地修改，被协议接受记录引用的版本不得物理删除。
- 审计列表只读，支持按操作者、权限、资源、结果、时间和 Request/Trace ID 筛选。查看和导出审计本身继续写审计；任何页面都不提供修改/删除审计记录的按钮。
- 异步任务中心统一展示导入、导出、索引、评估和运维 Job 的进度、结果、失败摘要和产物过期时间。大文件通过短时鉴权 URL 下载。
- 死信重放先展示事件类型、目标资源、原 Trace、当前资源版本、影响预览和幂等能力；禁止编辑原 Payload 或“一键重放全部”。支付、库存和退款死信需要更高权限或双人复核。

#### 2.13.14 管理员审批中心

路由：`/admin/approval-requests`、`/admin/approval-requests/:approvalRequestId`。审批中心处理权限提升、高金额/例外退款、AI/策略发布、内容发布、敏感导出和高风险死信重放等需要职责分离的操作；它不是用户确认，也不复用 Agent 的 `ai_tool_approvals`。

```text
┌──────────────────────────────── 管理员审批申请 ────────────────────────────────┐
│ 申请：aar_...       类型：AI Tool Version 发布       状态：待 2 人审批（1/2） │
│ 发起人：管理员 A    Scope：全平台                    过期：30 分钟后          │
│ 目标：tool_x / Version 7                                                     │
│ 影响：切换当前 Tool Version；新 Run 使用新版，进行中 Run 继续使用原绑定版本   │
│ 参数摘要 Hash：a9c...       资源版本：v12       测试/评估：通过               │
│ [查看结构化 Diff] [查看审计]                                                  │
│ 决定：[拒绝原因/审批意见____________________________________]                 │
│ 近期认证：已通过                [拒绝] [批准]                                 │
└───────────────────────────────────────────────────────────────────────────────┘
```

- 申请只能由具体领域命令在命中审批策略时创建，前端不提供“自定义动作/参数”的通用审批创建表单。
- 列表和详情只展示当前管理员有权审批或查看的 Scope；发起人不能批准自己的申请，同一有效管理员账号不能占用两个审批席位。管理账号必须一人一号且禁止共享；如接入企业 IdP，还必须按不可变 Workforce Subject 去重。
- 批准/拒绝前必须显示规范化对象、金额/数量、影响、参数 Hash、资源版本、发起理由、审批门槛和过期时间，并要求 MFA/近期认证。
- 任一拒绝使申请终止；达到不同审批人数量后进入 Approved，再由专用 Executor 重新校验 Permission、Scope、资源状态和参数 Hash。Approved 不等于业务命令已经成功。
- 页面分别展示 `approved、executing、succeeded、failed、outcome_unknown`；结果未知时只能进入对账，不提供“再次执行”按钮。
- 目标资源版本或命令参数变化时旧申请失效，发起人必须从原业务页面重新提交；审批页面不能编辑原命令 Payload。

#### 2.13.15 管理端通用交互、安全与异常状态

1. 所有可变资源 GET 返回 ETag；编辑、发布、审核、删除和高风险命令按契约携带 If-Match。412 时展示服务端新版本与本地草稿 Diff，不静默覆盖。
2. 所有创建和命令操作按钮在请求期间禁用并携带幂等键；请求超时但结果未知时进入状态查询，不直接重复执行。
3. 批量操作先展示匹配范围和预计影响，再由服务端逐项授权；禁止只校验第一条。超过同步上限时创建异步 Job。
4. 危险操作不能只靠红色表达，确认框展示资源、动作、Scope、金额/数量、不可逆影响、原因和近期认证状态。
5. 列表筛选、排序、Cursor 和选中的 Tab 写入 URL；敏感条件、Token、手机号、地址和原始 Prompt 不写入 URL。
6. 无权限、资源不可见、版本冲突、限流、依赖故障和结果未知分别展示稳定状态；错误页显示可复制 Request ID，不展示堆栈、SQL、Secret 或其他租户数据。
7. 管理端路由切换时取消旧请求并校验响应所属 Scope，防止快速切店时把上一店铺数据渲染到新页面。
8. 表格、弹窗和表单支持键盘操作、可见焦点、语义标题、错误关联和屏幕阅读器；图表同时提供文字/表格摘要。
9. 浏览器本地只缓存非敏感 UI 偏好；用户、订单、支付、退款、会话和审计数据不进入持久化前端缓存。

#### 2.13.16 管理端完整路由、权限与 API 表

本节与 2.12.2 同为 Vue Route 的文档展示表，唯一机器可读来源是 `docs/traceability.yaml`。Vue 路由参数统一 camelCase，生成的 OpenAPI Path/JSON 参数统一 snake_case。每条管理路由固定 `audience=admin、layout=AdminLayout`；仅登录/MFA 为 `layout=AdminAuthLayout`。数据页通用实现 `L/E/403/404/410/412/N`，写操作页还必须实现幂等、近期认证、结果未知和审计状态。

| 页面 | Vue Route / Component | 主要 Operation（全部 `/api/v1`） | Permission / Scope | Guard / 领域规则 |
| :--- | :--- | :--- | :--- | :--- |
| 管理登录 | `/admin/login` / `AdminLoginPage.vue` | `AdminAuth_Login`、`POST /admin/auth/login` | 管理身份准入 | 已登录安全跳转；不加载 User Audience |
| MFA/近期认证 | `/admin/login/mfa` / `AdminMfaPage.vue` | `AdminAuth_MfaVerify`、`AdminAuth_Reauthenticate` | 待验证 Admin Session | AAL/Challenge 绑定，禁止开放跳转 |
| 管理身份安全 | `/admin/security` / `AdminSecurityPage.vue` | `AdminAuthSession_ListMine/Revoke`、`AdminAuth_Reauthenticate` | 当前 Admin Audience 身份 | 仅管理会话；近期认证；不得读取 User Audience 会话 |
| 仪表盘 | `/admin/dashboard` / `AdminDashboardPage.vue` | `AdminDashboard_Get`、`GET /admin/dashboard` | `dashboard:read` + Platform/Store | 指标口径、新鲜度和 Scope 一起返回 |
| 用户列表 | `/admin/users` / `AdminUserListPage.vue` | `AdminUser_List`、`GET /admin/users` | `users:read` + Platform | Cursor；默认脱敏 |
| 用户详情/安全 | `/admin/users/:userId` / `AdminUserDetailPage.vue` | `AdminUser_Get/ChangeStatus`、`AdminSensitiveGrant_Create`、`AdminRoleGrant_List/Create/Revoke` | `users:*`、`rbac:*` 对应原子权限 + Platform | ETag；敏感访问 Ticket；MFA/SoD/审计 |
| 角色列表/详情 | `/admin/roles`、`/admin/roles/:roleId` / `AdminRoleListPage.vue`、`AdminRoleDetailPage.vue` | `AdminRole_List/Get/Create/Update`、`AdminRolePermission_Replace` | `rbac:read/manage` + Platform/Store | 不可委派 Permission 不可进入自定义 Role |
| 店铺认证列表/详情 | `/admin/store-certifications`、`/admin/store-certifications/:certificationId` / `AdminStoreCertificationListPage.vue`、`AdminStoreCertificationDetailPage.vue` | `AdminStoreCertification_List/Get/Decide/AddMaterialVersion` | `stores:read/review` + Scope | 材料 Version+ETag；通过/拒绝/补件单一 Decision Enum |
| 店铺列表/详情 | `/admin/stores`、`/admin/stores/:storeId` / `AdminStoreListPage.vue`、`AdminStoreDetailPage.vue` | `AdminStore_List/Get/ChangeStatus` | `stores:read/manage` + Scope | 状态机、ETag、近期认证 |
| 店铺政策 | `/admin/stores/:storeId/policies` / `AdminStorePolicyPage.vue` | `AdminStorePolicy_List/Create/Update/Publish/Withdraw` | `store_policies:*` + Store | 不可变发布版；RAG 重建事件 |
| 商品列表/新建/详情 | `/admin/products`、`/admin/products/new`、`/admin/products/:productId` / `AdminProductListPage.vue`、`AdminProductEditPage.vue` | `AdminProduct_List/Create/Get/Update/Submit/Publish` | `products:*` + Store | 新建路由无 `productId`；详情 ETag/完整性/审核状态 |
| 分类/品牌 | `/admin/categories`、`/admin/brands` / `AdminCategoryPage.vue`、`AdminBrandPage.vue` | `AdminCategory_List/Upsert`、`AdminBrand_List/Upsert` | `catalog_taxonomy:manage` + Platform | 循环分类阻断；被引用项禁止物理删除 |
| 库存 | `/admin/inventories` / `AdminInventoryPage.vue` | `AdminInventory_List/Adjust` | `inventories:read/adjust` + Store | Delta+原因+业务号+ETag；并发不超售 |
| 订单列表/详情 | `/admin/orders`、`/admin/orders/:orderId` / `AdminOrderListPage.vue`、`AdminOrderDetailPage.vue` | `AdminOrder_List/Get/AdjustAmount/Cancel`、`AdminShipment_Create` | `orders:read/adjust/cancel`、`shipments:create` + Scope | 正交状态；金额、版本、SoD |
| 支付列表/详情 | `/admin/payments`、`/admin/payments/:paymentId` / `AdminPaymentListPage.vue`、`AdminPaymentDetailPage.vue` | `AdminPayment_List/Get/Reconcile` | `payments:read/reconcile` + Platform/Store | 对账不改写渠道原始事件 |
| 包裹详情 | `/admin/shipments/:shipmentId` / `AdminShipmentDetailPage.vue` | `AdminShipment_Get/CorrectTracking/Void/Refresh` | `shipments:read/correct/void/refresh` + Store | 揽收后不可 Void；Unknown 进对账 |
| 退款列表/详情 | `/admin/refund-applications`、`/admin/refund-applications/:refundId` / `AdminRefundListPage.vue`、`AdminRefundDetailPage.vue` | `AdminRefund_List/Get/Claim/Decide` | `refunds:read/review` + Scope | 资格/金额/政策快照/复核；批准/拒绝/补件为 Decision Enum |
| 退款申诉列表/详情 | `/admin/refund-appeals`、`/admin/refund-appeals/:appealId` / `AdminRefundAppealListPage.vue`、`AdminRefundAppealDetailPage.vue` | `AdminRefundAppeal_List/Get/Decide` | `refund_appeals:read/review` + Platform | 平台裁决与店铺退款审核职责隔离 |
| 评价列表/详情 | `/admin/reviews`、`/admin/reviews/:reviewId` / `AdminReviewListPage.vue`、`AdminReviewDetailPage.vue` | `AdminReview_List/Get/Reply/Moderate` | `reviews:read/reply/moderate` + Scope | 规则码；屏蔽/恢复留历史 |
| 客服工单列表/工作台 | `/admin/support/tickets`、`/admin/support/tickets/:ticketId` / `AdminSupportTicketListPage.vue`、`AdminSupportWorkspacePage.vue` | `SupportTicket_List/Get/Claim/Wait/Resume/Transfer/Resolve`、`SupportMessage_Send` | `support:*` + Queue/Store | Assignment；用户可见消息与内部备注隔离 |
| Agent/Skill/Tool/策略 | `/admin/ai/agents`、`/admin/ai/skills`、`/admin/ai/tools`、`/admin/ai/tools/:toolId`、`/admin/ai/policies` / 对应 `AdminAi*Page.vue` | `AdminAgent_*`、`AdminSkill_*`、`AdminTool_Get/Version_Create/Publish/Rollback`、`AdminAiPolicy_*` | 对应 `ai_*:read/manage/publish` + Platform | Definition/Version 分离；发布需审批/回滚点 |
| 知识文档/索引任务 | `/admin/knowledge/documents`、`/admin/knowledge/documents/:documentId`、`/admin/knowledge/indexing-jobs` / 对应 `AdminKnowledge*Page.vue` | `AdminKnowledgeDocument_List/Get/Create/Publish`、`AdminKnowledgeIndex_Create`、`AdminBatchJob_Cancel` | `knowledge:read/manage/publish` + Scope | MySQL Parent+PG Child+Version 对账 |
| AI 评估/Run | `/admin/ai/evaluations`、`/admin/ai/runs/:runId` / `AdminAiEvaluationPage.vue`、`AdminAiRunDetailPage.vue` | `AdminAiEvaluation_List/Run`、`AdminAgentRun_Get/Kill` | `ai_evaluations:read/run`、`ai_observability:read`、`ai_runtime:kill` | 只读基线；不提供 Dataset CRUD；不展示隐藏推理 |
| 可观测性 | `/admin/observability` / `AdminObservabilityPage.vue` | `AdminObservability_Query` | `observability:read` + Platform/Store | 指标下钻到 Trace/Audit Ref，全程脱敏 |
| 内容运营列表/详情 | `/admin/content`、`/admin/content/:contentId` / `AdminContentListPage.vue`、`AdminContentEditPage.vue` | `AdminContent_List/Get/Create/Update/Publish/Withdraw` | `content:read/manage/publish` + Platform | Schema/安全富文本/外链白名单/发布窗口 |
| 审批列表/详情 | `/admin/approval-requests`、`/admin/approval-requests/:approvalRequestId` / `AdminApprovalListPage.vue`、`AdminApprovalDetailPage.vue` | `AdminApproval_List/Get/Decide` | `admin_approvals:read/decide` + 原领域 Scope | 发起人不可自批；参数 Hash/资源 Version/MFA |
| 审计 | `/admin/audit-logs` / `AdminAuditLogPage.vue` | `AdminAudit_List/Export` | `audit:read` + Scope | 查看/导出自身亦审计；脱敏 |
| 异步任务列表/详情 | `/admin/system/jobs`、`/admin/system/jobs/:jobId` / `AdminJobListPage.vue`、`AdminJobDetailPage.vue` | `AdminBatchJob_List/Get/Confirm/Cancel` | `jobs:read` + 原命令 Permission/Scope | 预览后确认；协作取消；结果未知对账 |
| 死信列表/详情 | `/admin/system/dead-letter-events`、`/admin/system/dead-letter-events/:deadLetterId` / `AdminDeadLetterListPage.vue`、`AdminDeadLetterDetailPage.vue` | `AdminDeadLetter_List/Get/Preview/Replay` | `events:read/operate` + Scope | Schema/Version/幂等/审批；禁止任意改 Payload |

管理端开发验收必须建立 `页面 → Vue Route → API Operation → Permission → Data Scope → Domain Rule → Audit → Test` 追踪矩阵。任何菜单或按钮无法映射到明确权限和接口时，不得由前端临时调用通用更新接口实现。上表与 3.34.19 都由 `docs/traceability.yaml` 生成/校验；任一 Route、Operation、Permission、Scope 或 Requirement 孤儿都使 CI 失败。

#### 2.13.17 管理端首版页面 Text UI 全量规格

以下是 2.13.4～2.13.14 的强制页面蓝图。按钮只在后端 `available_actions` 和当前 Permission/Scope 同时允许时显示；禁用按钮必须显示服务端原因，不由前端复制状态机。

##### 2.13.17.1 仪表盘

```text
┌─ 交易与运行概览 ─ Scope:[全平台▼] 时间:[近7天▼] 口径:[?] 更新:2026-08-22 10:30 ─┐
│ GMV ¥1,280,000.00 | 支付订单 3,420 | 支付成功率 98.7% | 客单价 ¥374.27 │
│ 待发货 86[查看] 待退款 12[查看] 低库存 24[查看] 排队工单 9[查看] │
│ Agent成功率 93% | 越权阻断 7 | RAG无结果 3.1% | 死信 2[详情] │
└─ 单卡失败独立降级：[支付指标暂不可用 Request ID:...] [重试] ────┘
```

##### 2.13.17.2 用户详情与 RBAC

```text
┌─ 用户 usr_... ─ [已冻结至 2026-09-01] ETag:v12 ────────────┐
│ [账号摘要] [安全会话] [状态时间线] [Role Grants] [审计]         │
│ 手机:138****8888 [申请一次性敏感访问] [强制下线] [提前解冻] │
│ Grant:store_operator/sto_.../active/2026-12-31 [撤销] [查看事件] │
└─ [授予新角色] 只新建 Grant；不恢复旧行；权限提升可进审批中心 ─┘
```

##### 2.13.17.3 店铺认证与运营

```text
┌─ 认证 cert_... [待补件] 材料v3 ETag:v8 ──────────────────┐
│ 时间线:提交v1→要求补件→重提v2→要求补件→重提v3             │
│ (○通过 ○拒绝 ○要求补件) 规则码:[__] 说明:[____] [提交]         │
│ Tabs:[分组] [服务政策] [配送模板] [公告] [推荐]                     │
└─ 退换货政策 v4 Published [历史] [新建草稿] [发布] [撤回] ──┘
```

##### 2.13.17.4 商品与库存

```text
┌─ 商品 prd_... [草稿] ETag:v21 完整度 7/9 ──────────────────┐
│ [基础✓] [SKU✓] [图片✓] [属性✓] [履约✗] [FAQ✓] [详情✗]          │
│ 字段编辑区… [保存草稿] [提交审核(禁用:缺履约资料)]             │
│ SKU1 ¥129.00 在手/预占/可售=100/8/87 v9 [调整库存] [流水]    │
└─ 调整=有符号增量+原因+业务号+调整前后；412 保留输入要求重确认 ─┘
```

##### 2.13.17.5 订单、支付与包裹

```text
┌─ 订单 ord_... [待发货] 支付:[已支付] 履约:[部分] ETag:v15 ─────┐
│ [订单事实] [支付] [地址受控查看] [包裹] [售后] [时间线]       │
│ shp_1 created 商品A×1 SF****88 [纠错] [作废] [刷新]             │
│ 未履约:商品B×1 [创建并确认发货]                                │
└─ 无草稿；揽收后[作废]隐藏并显示不可作废原因 ──────────┘
```

##### 2.13.17.6 退款、申诉与评价

```text
┌─ 退款 rf_... [商家审核] 倒计时:03:12:20 ETag:v6 ───────────┐
│ 订单项/数量 | 金额 | 资格 | 政策快照 | 凭证 | 风险标记          │
│ (○同意 ○拒绝 ○补材料) 原因码:[__] 用户说明:[____] [提交]      │
│ rev_... [可见] [商家回复] [屏蔽] [恢复] [升级]                 │
└─ 评价处置必须选规则条款并保留历史；高风险退款进审批中心 ─┘
```

##### 2.13.17.7 AI、知识库与可观测性

```text
┌─ Tool order.lookup Definition:active Current:v7 ── [新建Version] [Disable] ─┐
│ v8[draft] v7[published] | Diff:Schema/MCP/Risk/Timeout/Idempotency | [发布v8] │
│ Document doc_... Source:v5 Index:64% | job_...→kjob_...v18 [取消] [错误] │
│ Eval:Agent v9 / 只读基线release-08 [运行] | Trace:Run→RAG→Skill→Tool   │
└─ 不展示 Secret/隐藏推理，不提供 Dataset CRUD；Job不一致显示“对账中” ─┘
```

##### 2.13.17.8 内容、批处理、审计与死信

```text
┌─ Content home.hero Current:v4 Published | v5[draft] [Diff] [提交审核] [发布] ─┐
│ Job job_... product_import awaiting_confirmation valid:980 invalid:20 [报告] [确认] │
│ Audit aud_... denied users:read_sensitive user:usr_... Request:... [详情]      │
│ DLQ dlq_... open OriginalTrace:... [影响预览] [重放(需审批)] [解决] │
└─ 禁止编辑 Payload/一键重放；过期产物显示“已过期”而非空下载 ─┘
```

客服工作台使用 2.13.10 的 Text UI，并在 `waiting_user` 时将 [等待用户] 替换为 [恢复处理]，显示暂停时间、原因和剩余 SLA。审批中心使用 2.13.14 的 Text UI。

##### 2.13.17.9 全页面状态与无障碍契约

| 状态 | 页面表现 | 交互要求 |
| :--- | :--- | :--- |
| Loading | 保留标题/筛选框架，表格 Skeleton 使用 `aria-busy=true` | 取消旧 Scope 请求，不允许对 Skeleton 执行命令 |
| Empty | 显示当前筛选摘要、清空筛选和有权创建入口 | 不用“加载失败”冒充空结果 |
| 403/404 | 403 显示 Permission/Scope；404 显示统一不可见页 | 不泄露其他店铺/用户资源存在性 |
| 412/428 | 展示 Current Version 和脱敏 Diff，保留本地输入 | 不自动以新版本重放旧意图 |
| 429 | 显示 `Retry-After`、原因和安全重试时间 | 按钮倒计时，不并发轮询 |
| 依赖故障 | 保留最后已知数据、标明新鲜度和 Request ID | 只读可降级；金额/权限/发布写操作 Fail Closed |
| 结果未知 | 显示 Job/Command ID 和“对账中” | 禁用再执行，只允许刷新/对账 |

所有表格、Tab、弹窗和抽屉必须有语义标题、键盘导航、可见焦点和关闭后焦点恢复。图表同时提供文字/表格摘要；颜色不是状态的唯一传达方式。高风险确认默认焦点放在取消，审批、原因和影响摘要可被辅助技术完整读取。

---

## 3 后端系统设计

### 3.1 后端设计目标与范围

后端系统负责承载商城全部可信业务状态、对外 API、异步任务、消息服务、AI 智能客服运行时及其权限控制。前端只能通过后端提供的 API、SSE 和 WebSocket 使用系统能力，不得直接连接 MySQL、PostgreSQL、Redis、对象存储内部端点或 MCP Server。

后端第一阶段采用 **模块化单体（Modular Monolith）+ 独立 Worker + 独立 Agent Runtime** 的形态：代码放在同一个仓库、共享统一的领域模型与工程规范，但 API、异步任务、定时任务和 AI Agent 以不同进程/容器运行，可分别扩容和故障隔离。

#### 3.1.1 建设目标

| 目标 | 说明 |
| :--- | :--- |
| **业务完整性** | 完整支持用户、店铺、商品、SKU、库存、购物车、结算、订单、支付、物流、评价、退款、消息和后台管理 |
| **交易正确性** | 金额、库存、支付和退款以服务端计算与数据库状态为准，任何前端参数和 AI 输出均不能直接成为交易事实 |
| **统一权限** | 普通用户、管理员、店铺客服、专属客服 Agent 和人工客服统一经过身份认证、数据范围与操作权限校验 |
| **AI 可治理** | Agent 的模型调用、RAG、记忆、Skill、MCP 工具、多 Agent 委派、用户确认和人工接管均可配置、追踪、审计和评估 |
| **可恢复性** | 支付回调、超时关单、退款、向量索引等流程能够幂等重试，进程重启后可以继续处理 |
| **可观测性** | 每个请求、异步任务和 Agent Run 都具有统一 `request_id`/`trace_id`，能够关联日志、指标、Trace、模型调用和工具调用 |
| **可测试性** | 业务状态机、权限策略、MCP 工具、Skill、RAG 和 Agent 流程均可在不依赖前端的情况下独立测试 |
| **可演进性** | 初期避免微服务复杂度，后续可按明确的领域边界拆分高负载模块，不推翻已有数据和 API 契约 |

#### 3.1.2 系统边界

后端系统范围划分如下：

| 类型 | 包含内容 |
| :--- | :------- |
| **系统内部** | FastAPI API、领域服务、数据库访问、Worker、Scheduler、Agent Runtime、权限网关、MCP Host/内部 MCP Server、RAG、记忆系统、审计和可观测埋点 |
| **自有基础设施** | MySQL、PostgreSQL + pgvector、Redis、MinIO/S3 兼容对象存储、Nginx、监控与日志组件 |
| **外部依赖** | 大语言模型/Embedding/Rerank 服务、支付渠道、短信/邮件、物流查询、内容安全服务（如接入） |
| **客户端** | Vue 3 用户端、Vue 3 管理端；只通过公开协议访问后端 |

后端负责保证以下事实：

- 当前用户是谁、拥有哪些角色和数据权限；
- 商品和 SKU 是否可售、实时价格和可售库存是多少；
- 订单是否创建、支付是否成功、物流和售后处于什么状态；
- 某次写操作是否已经执行，是否可以安全重试；
- 店铺客服和专属客服能看到什么、能调用什么工具、是否已经得到用户授权；
- AI 回复引用了哪些知识、调用了哪些工具、产生了多少成本以及结果是否可靠。

以下内容不属于后端可信事实：

- 前端计算的价格、折扣、运费、退款金额和库存；
- 前端传入的 `user_id`、角色或店铺归属；
- AI 自然语言中声称的“已支付”“退款已通过”等结果；
- Redis 缓存中的临时副本；
- PostgreSQL 向量库中由 MySQL 同步而来的商品或政策副本。

#### 3.1.3 核心设计原则

1. **MySQL 业务事实唯一**：用户、权限、商品、库存、订单、支付、退款和完整消息记录以 MySQL 为准。
2. **业务规则确定性执行**：价格计算、库存扣减、状态转换、售后资格和权限判断必须由后端代码执行，不能交给前端或模型自由判断。
3. **最小权限**：API、Worker、Agent、MCP Server 和管理员均只获得完成职责所需的最小数据与工具权限。
4. **先鉴权再查数**：任何资源查询必须先确定当前身份和数据范围，不允许查询后再由前端过滤。
5. **写操作默认不乐观**：支付、取消订单、确认收货、退款、删除等关键写操作等待服务端成功响应后再更新前端状态。
6. **幂等优先**：订单创建、支付回调、退款提交、库存释放和异步消费必须支持重复请求而不产生重复结果。
7. **事务内写业务与事件**：关键业务状态和待发布事件写入同一个 MySQL 事务，再由 Outbox Dispatcher 异步分发。
8. **缓存可丢弃**：Redis 只保存可以失效或重建的数据；缓存异常不得改变业务结果。
9. **向量库可重建**：PostgreSQL 中由业务内容生成的知识向量必须能够从权威来源重新切片和索引。
10. **AI 工具不直连数据表**：Agent 只调用受控 Service/API/MCP 工具，禁止生成 SQL 直接操作数据库。
11. **被授权的写操作仍需确认**：Agent 只有在 3.17/3.18 首版 Allowlist 已明确开放某项写能力时，才可提出结构化操作；执行前必须展示对象、金额（适用时）、原因和后果并取得本次用户确认。确认只是必要条件，不会授予取消订单、确认收货、修改地址、下单或支付等未开放 Tool。
12. **默认可观测**：新增接口、任务、工具和 Agent 节点必须同时设计日志、指标、Trace、错误码和审计字段。
13. **契约优先**：接口、事件、MCP Tool、Skill 输入输出均使用结构化 Schema，并进行版本管理。
14. **安全失败**：权限服务、资格校验或关键依赖不确定时拒绝写操作，而不是猜测或默认放行。

#### 3.1.4 模块化单体与微服务边界

第一阶段不拆分大量微服务。后端按领域划分模块，但在同一代码仓库维护：

```text
auth / users / stores / catalog / inventory
cart / checkout / orders / payments / logistics
refunds / reviews / messaging / admin
ai_customer_service / rag / memory / skills / mcp
```

运行时按职责拆为以下进程：

| 进程/容器 | 职责 | 是否直接访问业务主库 |
| :-------- | :--- | :------------------- |
| **api** | REST API、鉴权、业务查询与事务写入、Webhook 接收 | 是 |
| **worker** | Outbox 分发、通知、物流同步、向量化、失败重试 | 是（按任务所需） |
| **scheduler** | 产生到期任务，如超时关单、库存释放、记忆清理 | 只读取到期记录并投递任务，不直接完成复杂业务 |
| **agent-runtime** | 店铺客服、专属客服、LangGraph、多 Agent、上下文组装、流式响应 | 否；通过受控 API/MCP 工具访问业务 |
| **mcp-server** | 把目录、订单、物流、售后、客服等能力暴露为受控工具 | 不直接执行任意 SQL；调用对应领域 Service/API |

只有满足以下条件之一时才考虑拆为独立微服务：

- 模块存在独立且显著不同的扩容需求；
- 模块故障需要与交易主链路强隔离；
- 模块由独立团队负责并拥有清晰的数据所有权；
- 单体部署已经出现经过监控证实的性能或发布瓶颈；
- 外部合规要求强制物理隔离。

不得仅因为“企业级”或“以后可能扩容”提前拆分微服务。拆分前必须明确 API 契约、事件契约、数据迁移和故障补偿方案。

#### 3.1.5 同步请求与异步任务边界

用户需要立即知道是否成功的操作使用同步请求；耗时、可重试、可延后或需要调用外部服务的工作使用异步任务。

| 场景 | 处理方式 | 同步响应含义 |
| :--- | :------- | :----------- |
| 登录、读取商品、读取订单 | 同步 | 返回最终查询结果 |
| 创建订单 | 同步事务 | 返回订单已创建，不代表支付成功 |
| 支付回调验签与入账 | 同步完成关键落库，后续异步 | 向支付渠道返回已接收/已处理 |
| 提交退款申请 | 同步创建申请 | 返回申请已提交，不代表审批或到账完成 |
| 发送普通消息 | 同步持久化，异步推送 | 返回消息已写入数据库 |
| AI 回复 | 异步 Agent Run + SSE | 先返回 Run/流式事件，结束事件代表本次生成完成 |
| 物流轨迹同步 | 异步 | API 返回当前已持久化的最新轨迹 |
| 订单超时关闭 | 定时扫描 + 异步任务 | 无用户同步请求 |
| 发送短信/邮件/站内通知 | 异步 | 主业务不等待第三方发送完成 |
| 文档切片与向量化 | 异步 | 内容保存成功与向量索引完成分开表示 |
| 长期记忆提取 | 异步候选生成 | 候选生成不等于记忆立即生效 |

同步请求不得无限等待异步任务。耗时任务统一返回 `task_id`/`run_id` 和明确状态，前端通过轮询、SSE 或 WebSocket 获取后续进度。

#### 3.1.6 数据一致性原则

系统按数据重要性选择不同一致性级别：

| 一致性等级 | 数据 | 实现方式 |
| :--------- | :--- | :------- |
| **强一致** | 账户状态、权限、库存预占、订单、支付、退款金额 | 单个 MySQL 事务、唯一约束、行锁/乐观锁、状态机 |
| **事务后最终一致** | 通知、物流拉取、搜索索引、向量索引、统计事件 | MySQL Outbox + 幂等 Worker |
| **缓存一致性** | 商品详情、店铺信息、未读数量等缓存 | Cache Aside、短 TTL、事件失效、回源 MySQL |
| **AI 最终一致** | RAG 文档、长期记忆向量、对话摘要 | 带版本和 `content_hash` 的异步重建 |

禁止在一个业务请求中尝试同时提交 MySQL 和 PostgreSQL 的跨库分布式事务。正确流程为：

```text
MySQL 业务事务
    ├── 写入业务数据
    └── 写入 outbox_events
            │
            ▼
      Worker 幂等消费
            ├── 更新 PostgreSQL/pgvector
            ├── 删除或刷新 Redis 缓存
            └── 发送通知/触发后续任务
```

当异步同步失败时，MySQL 业务结果仍然有效；失败事件进入重试或死信队列，并触发告警，不允许通过回滚已完成交易来迁就非核心索引。

#### 3.1.7 AI 与业务系统隔离原则

AI Agent 被视为“不可信的决策建议与编排组件”，而不是拥有数据库权限的业务管理员。

```text
用户消息
  → Agent 理解意图
  → 选择 Skill/子 Agent
  → 申请调用 MCP Tool
  → 权限网关注入真实用户身份
  → 校验会话类型、数据范围和用户授权
  → 只读工具直接执行 / 写工具生成确认卡片
  → 用户明确确认
  → 领域 Service 再次校验并执行
  → 结果和审计记录返回 Agent
  → Agent 只能依据结构化结果向用户说明
```

必须遵守以下隔离要求：

- Agent、Prompt、RAG 文档和用户消息都不能直接构造数据库连接；
- MCP 工具中的 `user_id`、`store_id` 和权限范围由后端注入，不接受模型伪造；
- 店铺客服只能读取当前店铺的公开商品目录、有效政策和 FAQ，并优先使用当前 Active 商品上下文；订单读取必须满足“当前用户 ∩ 当前会话店铺 ∩ 订单归属 ∩ Active Context/本次明确授权”。经服务端验证的订单入口可直接建立本次 Context，订单卡片只是可选展示，不是唯一授权来源；
- 专属客服读取个人订单、物流和长期偏好前必须符合授权策略；
- 模型不能审批退款、伪造支付结果、修改密码/收货地址或绕过业务状态机；
- RAG 内容按不可信输入处理，其中的指令不能覆盖系统权限和工具策略；
- 所有模型调用、检索、工具调用、用户确认、多 Agent 委派和人工接管都写入可追踪记录；
- AI 服务不可用时，商城浏览、购物车、订单、支付、物流和人工客服等核心业务必须继续可用。

#### 3.1.8 非功能性目标

以下为首个可上线版本的设计与验收基线，后续根据压测和真实流量调整：

| 类别 | 初始目标 |
| :--- | :------- |
| **可用性** | 核心业务 API 月度可用性目标不低于 99.9%（计划维护窗口另计） |
| **普通查询延迟** | 不含外部依赖的 API，P95 ≤ 300ms，P99 ≤ 800ms |
| **关键写入延迟** | 创建购物车项、地址、订单等业务写入，P95 ≤ 800ms |
| **AI 首 Token** | 在模型服务正常时，P95 目标 ≤ 5 秒；超过阈值先推送“正在处理”状态 |
| **消息送达** | 已持久化消息在连接正常时 P95 ≤ 1 秒推送至在线客户端 |
| **交易正确性** | 不允许重复订单、重复扣款、超额退款和库存为负；通过约束与并发测试验证 |
| **可恢复性** | 支付回调、Outbox 和 Worker 任务至少一次投递，消费者必须幂等 |
| **备份目标** | 生产初期 RPO ≤ 15 分钟、RTO ≤ 60 分钟，最终以部署环境能力为准 |
| **审计** | 支付、退款、权限变更、Agent 写工具操作 100% 留痕 |
| **可观测性** | 错误请求和关键交易全量 Trace；普通读请求可采样，指标不采样 |
| **安全性** | 敏感字段加密/脱敏，不在日志、Trace、Prompt 或工具结果中暴露明文凭证 |
| **兼容性** | 公开 API 在同一大版本内保持向后兼容，破坏性变更通过新版本发布 |

性能目标统计时必须标记是否包含支付、物流、模型等第三方耗时，不能把外部耗时与本地业务 SQL 耗时混为一个指标。

#### 3.1.9 首期不实现的能力

为了控制第一阶段复杂度，以下内容不作为首期目标：

- 按每个领域拆分独立微服务和独立数据库；
- 跨地域双活、多活写入或全球分布式事务；
- 引入 Kafka 等独立大型消息平台（在 Redis/Outbox 无法满足真实需求后再评估）；
- 自研通用大模型、Embedding 模型或向量数据库；
- 允许 Agent 自由创建任意工具、执行任意 SQL/HTTP/脚本；
- 允许多个 Agent 无限制互相对话、递归委派或自动扩大权限；
- 由模型单独决定退款审批、封禁用户、改价、库存调整等高风险业务；
- 建设独立数据仓库、复杂推荐训练平台和实时流计算平台；
- 未经评估和审批自动更新线上 Prompt、Skill、模型或知识库策略。

上述能力若后续确有需求，必须依据监控数据、业务规模、合规要求和成本收益单独立项。

### 3.2 后端总体架构

#### 3.2.1 总体架构图

```text
┌────────────────────────────────────────────────────────────────────────────────────┐
│                                  客户端层                                          │
│        Vue 3 用户端                Vue 3 管理端              第三方回调方          │
└───────────────┬──────────────────────────┬─────────────────────────┬───────────────┘
                │ HTTPS REST/SSE/WS         │ HTTPS REST              │ Webhook
                └───────────────────────────┼─────────────────────────┘
                                            ▼
┌────────────────────────────────────────────────────────────────────────────────────┐
│                         Nginx / 入口网关 / TLS                                      │
│       静态资源、反向代理、请求大小限制、基础限流、安全响应头、连接升级              │
└───────────────────────────────┬─────────────────────────────┬──────────────────────┘
                                │                             │
                                ▼                             ▼
┌──────────────────────────────────────────┐   ┌─────────────────────────────────────┐
│            FastAPI API Service           │   │          Agent Runtime              │
│ auth/users/stores/catalog/inventory      │   │ LangGraph / RAG / Memory            │
│ cart/checkout/orders/payments/logistics  │   │ Skills / Multi-Agent / Context       │
│ refunds/reviews/messages/admin/webhooks  │   │ SSE 流式生成 / 人工审批中断恢复       │
└───────┬──────────────────┬───────────────┘   └───────────────┬─────────────────────┘
        │                  │                                   │ 受控工具调用
        │                  │                                   ▼
        │                  │                   ┌─────────────────────────────────────┐
        │                  │                   │ MCP Host + Permission Gateway       │
        │                  │                   │ 身份注入/范围检查/授权/确认/审计      │
        │                  │                   └───────────────┬─────────────────────┘
        │                  │                                   ▼
        │                  │                   ┌─────────────────────────────────────┐
        │                  └──────────────────►│ 领域 MCP Server / 领域 Service API  │
        │                                      │ catalog/order/logistics/refund/...   │
        │                                      └─────────────────────────────────────┘
        │
        ├──────────────► MySQL（业务事实、消息、配置、审计、Outbox）
        ├──────────────► Redis（缓存、Session、限流、在线状态、任务 Broker）
        ├──────────────► PostgreSQL + pgvector（知识、记忆、Checkpoint）
        └──────────────► MinIO/S3（图片、凭证、知识库原始文件）

┌────────────────────────────────────────────────────────────────────────────────────┐
│                        Worker / Scheduler                                           │
│ Outbox 分发、超时关单、库存释放、通知、物流同步、向量化、摘要、记忆候选、清理任务   │
└────────────────────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────────────────────┐
│                        可观测性与评估                                                │
│ OpenTelemetry → Metrics / Logs / Traces；Agent Trace / Dataset / Evaluation / Cost  │
└────────────────────────────────────────────────────────────────────────────────────┘
```

该架构在工程上是一个仓库，在运行上是多个无状态或有明确状态依赖的进程。API 与 Agent Runtime 可以分别扩容，AI 高延迟或模型异常不会占满普通商城 API 的工作进程。

#### 3.2.2 Vue 3 与 FastAPI 通信关系

用户商城与管理后台均使用 Vue 3，但构建为两个独立应用/入口：用户端只装配公开和用户 API，管理端只装配 `/admin`、`/support` 等 Admin Audience API。两者可以复用设计系统、Money/Time 组件和由 OpenAPI 生成的基础类型，但不能共享登录状态、Pinia 持久化凭证或把管理路由打进用户端菜单。

| 通信方式 | 使用场景 | 约束 |
| :------- | :------- | :--- |
| **REST/JSON** | 登录、商品、购物车、订单、退款、管理后台等请求 | 统一 `/api/v1`，使用 OpenAPI 描述，字段和错误码稳定 |
| **SSE** | AI 回复 Token/事件流、长任务单向进度 | 支持断线重连、`run_id`、事件序号和最终完成/失败事件 |
| **WebSocket** | 新消息、未读数、人工客服接入、会话状态变化 | 建立连接时鉴权；只推送通知，关键消息仍先持久化 MySQL |
| **预签名上传** | 商品图、头像、评价图、退款凭证、知识文件、管理导入和平台内容 | 后端按 Audience/Purpose/Scope 签发短期上传凭证，客户端不持有对象存储永久密钥 |
| **Webhook** | 支付、物流或外部平台回调 | 独立路由、读取原始请求体、验签、幂等、快速响应 |

前端不得依赖数据库字段或内部 ORM 模型。公开响应使用独立的 Pydantic Schema，通过 OpenAPI 生成 TypeScript 类型/客户端，减少前后端字段不一致。

#### 3.2.3 API 服务职责

FastAPI API Service 是商城业务的主要入口，负责：

- 认证登录、Session/Token 校验、RBAC 和数据范围校验；
- 请求参数验证、错误转换、请求 ID 和 Trace 上下文建立；
- 用户、地址、店铺、商品、SKU 和库存的业务接口；
- 购物车、结算预览、订单创建和订单状态操作；
- 支付单创建、支付回调验签和关键入账事务；
- 物流查询、评价、退款和售后申请；
- 会话及消息持久化、未读状态、人工客服工作台接口；
- 管理后台接口和管理员操作审计；
- 在业务事务中写入 Outbox Event；
- 对 Agent/MCP 提供受控的内部领域接口。

API Service 不负责：

- 在请求线程中执行耗时的向量化、批量通知或全量物流同步；
- 在业务接口中直接等待完整 AI 多步骤运行；
- 直接信任第三方回调状态、前端金额或模型输出；
- 把数据库 Session、内部异常堆栈或敏感字段返回前端。

API 中的事务由 Service 层控制，Router 只负责协议适配，Repository 只负责数据访问，不能把业务状态机散落在路由或 ORM Hook 中。

#### 3.2.4 Worker 异步任务职责

Worker 消费 Outbox/任务队列，处理可重试的后台工作：

| 任务类别 | 典型任务 | 关键要求 |
| :------- | :------- | :------- |
| **订单任务** | 支付超时关单、库存释放、自动确认/完成 | 重新读取最新状态，幂等，不能依赖任务首次投递时间推断状态 |
| **支付任务** | 回调后通知、对账、退款渠道查询 | 支付事实先落 MySQL，外部调用可重试 |
| **物流任务** | 定时拉取轨迹、更新包裹状态 | 按物流单去重，保存原始响应摘要 |
| **通知任务** | 站内信、短信、邮件 | 业务完成与通知发送解耦，记录渠道结果 |
| **知识任务** | 文档解析、切片、Embedding、建立索引 | 使用 `content_hash` 和版本防止重复索引 |
| **记忆任务** | 对话摘要、候选长期记忆提取、衰减和过期 | 候选记忆需经过规则和授权才能生效 |
| **维护任务** | 缓存预热、失效数据清理、分区归档 | 限制批次大小，避免影响在线请求 |

每个任务必须包含 `task_id`、业务唯一键、重试次数、下次执行时间和 Trace 上下文。消费者成功执行后才确认；达到最大重试次数后进入死信记录并告警。

#### 3.2.5 AI Agent 服务职责

Agent Runtime 独立承载店铺客服和专属客服，负责：

- 根据 `conversation_type` 加载正确的 Agent、系统规则和工具集合；
- 组装短期记忆、滚动摘要、长期记忆、RAG 结果和业务上下文；
- 识别意图并选择 Skill，必要时委派给受限的专业子 Agent；
- 通过 MCP Host 请求工具，而不是直接读取业务表；
- 在工具调用前后执行输入输出 Schema 校验；
- 对需要确认的写操作暂停执行，持久化 Checkpoint，等待用户确认后恢复；
- 流式输出文本、工具状态、授权卡片、确认卡片和最终结果；
- 记录模型、Prompt、Skill、检索文档、工具、Token、延迟和成本；
- AI 无法可靠处理时转人工客服或返回安全的手动操作路径。

Agent Runtime 不拥有退款审批、支付确认、库存修改和管理员权限。即使模型正确选择了工具，最终业务 Service 仍需重新执行身份、权限、状态和金额校验。

#### 3.2.6 MySQL、PostgreSQL、Redis 职责边界

| 组件 | 权威数据 | 典型内容 | 不应保存的内容 |
| :--- | :------- | :------- | :------------- |
| **MySQL** | 商城业务事实 | 用户、权限、店铺、商品、库存、订单、支付、退款、完整消息、AI 配置和业务审计 | 大规模向量索引、只为加速而存在的临时缓存 |
| **PostgreSQL + pgvector** | AI 子系统持久状态及向量 | 知识文档/切片、Embedding、长期记忆、对话摘要、LangGraph Checkpoint、检索记录 | 订单金额、支付结果、实时库存等交易事实 |
| **Redis** | 非权威临时状态 | Session 热数据、缓存、限流、在线状态、任务 Broker、Agent 热点上下文 | 唯一订单、唯一支付结果、唯一退款记录、唯一完整聊天记录 |

关键规则：

- MySQL 与 PostgreSQL 不建立跨库外键；使用稳定业务 ID 关联；
- Redis 命中只能加速，未命中或不可用时回源权威存储；
- Agent Checkpoint 用于恢复执行，不代替 MySQL 中已经产生的业务结果；
- 商品/政策向量包含 `source_id`、`source_version` 和 `content_hash`，可检测过期；
- 长期记忆与用户授权、来源消息和生命周期关联，不能变成无来源的隐藏用户画像。

#### 3.2.7 对象存储职责

MinIO/S3 兼容对象存储保存二进制文件，数据库只保存对象键、URL、MIME、大小、哈希和业务归属：

- 商品主图、详情图和 SKU 图片；
- 店铺 Logo、用户头像；
- 评价图片；
- 退款图片/视频凭证；
- 知识库上传的 PDF、Word、Markdown 等原始文件；
- 可观测性或评估流程生成的大型离线产物（如需要）。

文件上传流程：后端校验身份和业务范围 → 签发短期预签名 URL → 客户端上传 → 后端确认文件元数据 → 异步安全扫描/压缩 → 文件状态变为可用。

禁止将大文件二进制直接写入 MySQL，也禁止客户端自行决定最终公开 URL。私有凭证必须使用授权下载或短期签名 URL。

#### 3.2.8 第三方支付与物流服务

所有第三方服务都通过 `integrations/` 适配层接入，领域模块不依赖某一家供应商的私有字段。

```text
PaymentProvider
├── create_payment()
├── verify_callback()
├── query_payment()
└── create_refund()

LogisticsProvider
├── query_track()
├── subscribe_track()
└── normalize_events()
```

接入要求：

- 密钥只存 Secret，不写代码、日志或镜像；
- 所有外部请求设置连接/读取超时、有限重试和 Trace；
- 回调必须验签，使用第三方事件 ID/交易号做唯一幂等；
- 保存必要的原始事件用于审计，但对敏感内容加密或脱敏；
- 第三方响应转换为内部统一状态，业务层不直接使用供应商状态字符串；
- 第三方超时不等于支付失败，状态不确定时进入查询/对账流程；
- 本地开发和自动化测试使用 Mock/Sandbox Provider，不连接真实扣款环境。

#### 3.2.9 服务调用链路

##### 3.2.9.1 普通查询链路

```text
Vue 3 → Nginx → FastAPI Router → 鉴权/限流
      → Service → Redis 查询
          ├── 命中：返回缓存
          └── 未命中：Repository → MySQL → 回填缓存
      → Pydantic Response → Vue 3
```

##### 3.2.9.2 创建订单与支付链路

```text
Vue 3 提交 Idempotency-Key
  → API 鉴权
  → 服务端重新计算商品、库存和运费
  → MySQL 事务：幂等记录 + 库存预占 + 交易单 + 店铺订单 + 支付单 + Outbox
  → 提交事务并返回待支付订单
  → 支付渠道
  → 支付 Webhook：验签 + 去重 + MySQL 状态机更新 + Outbox
  → Worker 发送通知、刷新缓存和执行后续任务
```

前端支付成功跳转不改变订单状态，最终支付状态以服务端验签后的支付回调或主动查询结果为准。

##### 3.2.9.3 AI 客服链路

```text
Vue 3 发送消息
  → API 把用户消息写入 MySQL
  → 创建 Agent Run
  → Agent Runtime 加载 Agent 版本、Skill、Checkpoint、短期摘要和授权
  → 按会话范围执行 RAG
  → 模型决定回答或申请工具
  → MCP Permission Gateway 校验身份、会话范围、权限和确认策略
      ├── 只读：执行领域工具并返回结构化结果
      ├── 需授权：返回授权卡片并暂停
      ├── 需确认：返回操作确认卡片并暂停
      └── 禁止：拒绝并记录原因
  → Agent 根据真实工具结果生成回复
  → SSE 流式推送，同时持久化消息、运行轨迹与评估字段
```

##### 3.2.9.4 知识更新链路

```text
管理员更新商品 FAQ/平台政策/店铺政策
  → MySQL 保存新版本 + Outbox
  → Worker 获取权威内容
  → 清洗与切片
  → 生成 Embedding
  → PostgreSQL 写入新版本向量
  → 原子切换当前索引版本
  → 清理旧版本或保留回滚窗口
```

向量更新期间旧版本仍可查询；新版本全部写入并通过基本校验后再切换，避免知识库出现半更新状态。

#### 3.2.10 故障降级关系

| 故障组件 | 系统影响 | 降级行为 |
| :------- | :------- | :------- |
| **MySQL** | 核心业务不可安全读写 | 停止交易写操作并返回服务不可用；不得使用缓存伪造可交易状态 |
| **Redis** | 缓存、限流、在线状态或任务投递受影响 | 普通查询回源 MySQL；采用本地保护限流；关键任务保留在 Outbox 等待恢复 |
| **PostgreSQL/pgvector** | RAG、长期记忆和 Checkpoint 受影响 | 商城业务继续；AI 使用有限 FAQ/无个性化模式，需恢复的写操作暂停而非重做 |
| **对象存储** | 图片或凭证上传/读取失败 | 业务文本可继续浏览；需要凭证的退款暂不允许提交并保留表单 |
| **模型服务** | AI 无法生成回复 | 店铺/专属客服提示暂不可用并提供人工客服和手动页面入口 |
| **Embedding/Rerank** | 新文档暂不能索引或排序质量下降 | 使用旧索引/关键词检索；任务进入重试，不影响 MySQL 内容发布结果 |
| **MCP Server** | 对应工具不可用 | Agent 不猜测结果；说明工具暂不可用并给出页面入口或转人工 |
| **支付渠道** | 无法创建/确认支付 | 保留待支付订单；不标记支付成功，后续主动查询/对账 |
| **物流服务** | 轨迹无法刷新 | 展示最后更新时间和已保存轨迹，提示同步延迟 |
| **Worker** | 异步任务积压 | API 继续写 Outbox；告警任务延迟，恢复后幂等补处理 |
| **可观测平台** | 日志/Trace 上报失败 | 不能阻断业务请求；本地缓冲或丢弃低优先级遥测，保留关键审计入库 |

降级信息必须向用户表达真实状态，例如“物流信息暂未更新”，不能把依赖故障描述为“订单不存在”或“退款失败”。

### 3.3 技术栈与基础组件

本节技术版本为 **截至 2026-08-21 的建议开发基线**。所有生产依赖必须锁定具体版本和镜像摘要，禁止在构建中使用无版本的 `latest` 标签。实际首次生成项目骨架时，还需运行完整兼容性测试后把最终版本写入 `environment.yml`、`pyproject.toml`、pip-tools 生成的 `requirements/*.txt` 和 Docker Compose。

#### 3.3.1 Python 与 FastAPI

| 项目 | 选择 |
| :--- | :--- |
| Python | 3.13.x（统一开发、测试和生产小版本） |
| Web 框架 | FastAPI 0.136.x 基线 |
| ASGI Server | Uvicorn；生产由容器编排多副本，不在一个容器中盲目堆叠大量 Worker |
| HTTP Client | HTTPX AsyncClient，统一连接池、超时、重试和 Trace 注入 |

选择 FastAPI 的原因：

- 与 Python AI/Agent 生态一致；
- 原生支持异步请求、依赖注入、SSE/WebSocket 和 OpenAPI；
- 可直接使用 Pydantic Schema 约束请求、响应和工具输入输出；
- 普通业务 API 与 Agent 内部接口可以共享认证、错误处理和可观测基础设施。

实现约束：

- Router 不直接执行 SQL；
- 每个请求创建独立的数据库 Session，禁止跨并发任务共享；
- `async` 接口内不得调用无隔离的阻塞 SDK；阻塞调用放线程池或 Worker；
- 应用启动只做轻量初始化，不在每个 API 容器启动时执行不可控迁移或大规模索引；
- 健康检查分为存活检查与就绪检查，避免依赖故障时反复重启仍可恢复的进程。

#### 3.3.2 Pydantic 数据校验

采用 Pydantic 2.x，用于：

- API 请求与响应 Schema；
- 配置与环境变量读取；
- 领域命令和事件 Schema；
- MCP Tool 输入输出；
- Skill 输入输出；
- Agent State 中可序列化的数据；
- 第三方支付、物流响应的规范化结构。

数据库 ORM Model 与公开 API Schema 必须分离。禁止直接把 SQLAlchemy 对象作为外部响应，以免泄露内部主键、密码字段、成本价、删除标记或审计字段。

建议 Schema 分层：

```text
XxxCreateRequest     创建请求
XxxUpdateRequest     更新请求
XxxQuery             查询参数
XxxResponse          单个公开响应
XxxListResponse      列表响应
XxxCommand           领域写命令
XxxEvent             领域/Outbox 事件
XxxToolInput         MCP 工具输入
XxxToolResult        MCP 工具结果
```

#### 3.3.3 SQLAlchemy 异步 ORM

采用 SQLAlchemy 2.0.x 的 AsyncIO API，分别创建 MySQL 和 PostgreSQL Engine/Session Factory：

```text
mysql_engine / mysql_session_factory
postgres_engine / postgres_session_factory
```

建议驱动：

| 数据库 | 异步驱动 | 用途 |
| :----- | :------- | :--- |
| MySQL | `asyncmy`（兼容性验证后锁定） | 在线业务读写 |
| PostgreSQL | `asyncpg` | 知识、记忆、Checkpoint 和向量读写 |

使用规则：

- Service 层使用显式事务 `begin/commit/rollback`；
- Repository 不自行提交事务；
- 禁止在 ORM 属性访问时隐式触发不可预期的异步查询，关联数据使用明确加载策略；
- 列表接口必须防止 N+1 查询；
- 所有外部输入使用绑定参数，禁止字符串拼接 SQL；
- 库存、订单、支付和退款的关键查询明确使用行锁或 `version` 乐观锁；
- MySQL 和 PostgreSQL Session 不放入同一个事务；跨库同步通过 Outbox；
- 连接池大小按 API 副本数、Worker 并发和数据库上限整体计算，不能每个容器使用默认大池。

#### 3.3.4 Alembic 数据库迁移

采用 Alembic 1.x，并为两个数据库建立独立迁移链：

```text
migrations/
├── mysql/
│   ├── env.py
│   └── versions/
└── postgres/
    ├── env.py
    └── versions/
```

迁移规范：

- 每次 Schema 变化必须提交迁移文件，禁止仅修改 ORM Model；
- 迁移文件需要人工审查，不能无条件接受自动生成结果；
- 生产迁移由单独一次性 Job 执行，不由所有 API 副本同时执行；
- 大表变更采用“扩展字段 → 双写/回填 → 切换读取 → 清理旧字段”的兼容迁移；
- PostgreSQL 初始化迁移负责 `CREATE EXTENSION IF NOT EXISTS vector`；
- 数据迁移和结构迁移尽量分开；
- 每次发布前在生产规模近似数据上验证锁表时间、回滚或前向修复方案。

#### 3.3.5 MySQL 业务主库

采用 MySQL 8.4.x LTS、InnoDB、`utf8mb4`，作为商城唯一业务主库。

选择 LTS 分支而不是自动跟随 Innovation 分支，降低数据库行为变化风险。MySQL 负责用户、权限、地址、店铺、商品、SKU、库存、购物车、订单、支付、物流、退款、评价、消息、AI 配置、业务审计和 Outbox。

核心约束：

- 金额使用 `DECIMAL`，禁止 `FLOAT/DOUBLE`；
- 时间统一写 UTC，精度使用 `DATETIME(6)`；
- 业务状态使用 `VARCHAR` + 应用层枚举/状态机，不使用难迁移的数据库 `ENUM`；
- 同一事务内完成关键状态、库存和 Outbox 写入；
- 历史订单保存商品、SKU、价格和地址快照；
- 支付、退款、库存流水和审计数据禁止物理删除；
- 生产启用备份、慢查询日志和必要的高可用方案。

#### 3.3.6 PostgreSQL 与 pgvector

采用 PostgreSQL 18.x + pgvector 0.8.6 基线，服务于 AI 子系统：

- `knowledge` Schema：文档、切片、关键词索引、Embedding；
- `memory` Schema：长期记忆、滚动摘要、记忆生命周期；
- `agent_runtime` Schema：LangGraph Checkpoint 和中间写入。

向量索引首期采用 HNSW，并保留精确检索用于小数据集和质量基线。向量列维度由实际 Embedding 模型决定，不在业务代码中假定固定维度；模型升级采用新列/新表或新版本索引并行构建，再切换当前版本。

PostgreSQL 中的商品/政策内容是检索副本，不是交易事实。任何商品价格、库存、订单或退款状态都必须通过业务工具实时读取 MySQL。

#### 3.3.7 Redis 缓存、会话与限流

采用 Redis Open Source 8.x，主要用途：

| 用途 | 数据结构建议 | 是否允许丢失 |
| :--- | :----------- | :----------- |
| Session 热数据/Refresh Session 缓存 | String/Hash + TTL | 可通过持久记录或重新登录恢复 |
| 商品、店铺缓存 | String/Hash + TTL | 可以，回源 MySQL |
| API/Agent 限流 | String/Sorted Set + 原子脚本 | 可以短时降级为本地限流 |
| 在线连接与心跳 | Set/Hash + TTL | 可以，客户端重连恢复 |
| 未读计数缓存 | Hash | 可以，从 MySQL 重建 |
| Agent 热点上下文 | String/Hash + TTL | 可以，从消息和 Checkpoint 恢复 |
| Celery Broker/结果短存 | List/Stream/内部结构 | 关键任务必须有 MySQL Outbox 兜底 |

缓存采用 Cache Aside；更新权威数据后通过事件删除缓存，读取未命中时回源。不得仅依靠 Redis 分布式锁保证库存或支付正确性，最终约束必须在 MySQL。

#### 3.3.8 LangGraph Agent 编排

Agent Runtime 选择 LangGraph 1.2.x 作为唯一主要编排框架，不在第一阶段同时混用另一套 Agent Runtime。

LangGraph 负责：

- 店铺客服与专属客服状态图；
- 多步骤工具调用；
- Checkpoint、进程重启恢复；
- 用户授权/二次确认的 `interrupt/resume`；
- Human-in-the-loop；
- 专业子 Agent Subgraph；
- 并行只读任务和串行写任务；
- 状态事件流和调试追踪。

自研部分限于商城领域能力：

- Agent/Skill/Tool Registry；
- 权限网关与数据范围；
- 记忆写入、召回和生命周期策略；
- RAG Pipeline；
- 模型路由与成本策略；
- 评估数据集、指标和发布门禁。

不自研模型 Tool Calling 协议、通用 Agent 循环、图 Checkpoint 格式或 MCP 协议实现。所有 LangGraph State 字段应尽量使用可序列化的基础类型/Pydantic Model，避免把数据库 Session、网络 Client 或不可恢复对象写入 Checkpoint。

#### 3.3.9 MCP SDK 与 MCP Server

采用官方 MCP Python SDK/兼容的 FastMCP 实现，Agent Runtime 作为 MCP Host/Client，内部领域能力按职责暴露为 MCP Server：

```text
catalog-mcp       商品、SKU、店铺政策
order-mcp         当前用户订单查询
logistics-mcp     当前用户物流查询
after-sale-mcp    售后资格、退款草稿与确认提交
support-mcp       人工客服工单
memory-mcp        用户记忆查看、候选、修改和删除
```

容器间使用 Streamable HTTP；stdio 仅用于本地开发和受控测试。MCP Tool 必须定义稳定名称、说明、输入 Schema、结构化结果、权限、超时、幂等性和是否需要用户确认。

禁止提供 `execute_sql`、通用 `http_request`、`run_shell`、直接改订单状态、直接标记支付成功或直接审批退款等高风险工具。

#### 3.3.10 异步任务与定时任务组件

首期采用 Celery 5.x + Redis Broker，Celery Worker 执行异步任务，Celery Beat/独立 Scheduler 产生定时任务。关键业务事件使用 MySQL Outbox 作为可靠源：

```text
业务事务写 outbox_events
  → Outbox Dispatcher 读取未发布事件
  → 投递 Celery 任务
  → Worker 幂等执行
  → 标记事件/任务完成
  → 超过重试进入 dead_letter_events
```

Celery/Redis 的投递成功不能代替业务事务成功；即使 Broker 短时不可用，Outbox Event 仍保留在 MySQL 并可恢复投递。

Redis Streams 预留给需要多个独立消费者组读取同一事件流的场景。第一阶段不得让同一业务事件同时由 Celery 和 Redis Streams 重复承载，以免形成两套难以判断的重试语义。

#### 3.3.11 MinIO/S3 对象存储

本地开发和私有部署使用 MinIO，生产可切换为任意 S3 兼容对象存储。业务代码通过统一 `ObjectStorage` 接口调用，不依赖具体供应商 SDK 返回结构。

必须支持：

- 预签名上传和下载；
- 文件大小、类型、数量和业务归属校验；
- 私有 Bucket 与公开资源分离；
- 内容哈希、重复文件检测和对象元数据；
- 图片压缩、缩略图和可选内容安全扫描；
- 临时上传对象清理；
- 退款凭证和知识文件的授权访问与审计。

#### 3.3.12 日志、监控与链路追踪组件

采用 OpenTelemetry 作为统一遥测协议，避免业务代码绑定单一观测厂商：

| 能力 | 组件 | 主要用途 |
| :--- | :--- | :------- |
| Metrics | Prometheus | API、数据库连接池、队列、业务和 Agent 数值指标 |
| Dashboard/Alert | Grafana + Alertmanager | 看板、阈值和告警通知 |
| Logs | 结构化 JSON 日志 + Loki | 按 `trace_id`、用户脱敏标识、订单号和 Agent Run 查询 |
| Traces | OpenTelemetry + Tempo | API、SQL、Redis、HTTP、Worker、MCP、Agent 全链路 |
| LLM Observability/Evaluation | Langfuse v4（OpenTelemetry 接入） | Prompt/模型/Token/成本、检索、工具、数据集、评分与实验 |

Langfuse 用于 AI 专项可观测和评估，不替代业务审计；Prometheus 指标也不能作为支付计费或订单事实。遥测组件故障不得阻断业务请求，SDK 上报使用异步、批量和失败隔离策略。

所有日志和 Trace 在写出前必须脱敏，禁止记录密码、验证码、Token、支付密钥、完整手机号/地址、退款凭证原文以及不必要的完整 Prompt 用户隐私。

#### 3.3.13 Docker 与反向代理

采用 Docker Engine 29.x + Docker Compose v2 作为开发和首期部署基线，Nginx 作为统一入口。

开发环境主要服务：

```text
frontend
nginx
api
worker
scheduler
agent-runtime
mcp-catalog / mcp-order / mcp-after-sale（可按阶段合并进程）
mysql
postgres-pgvector
redis
minio
prometheus / grafana / loki / tempo
langfuse（可通过 profile 按需启动）
```

镜像规范：

- 使用多阶段构建；
- 运行阶段使用非 root 用户；
- API、Worker、Scheduler 和 Agent Runtime 尽可能复用同一后端基础镜像，通过不同 Command 启动；
- 镜像中不包含生产 Secret、测试数据和本地配置；
- 基础镜像、系统包和 Python 依赖固定版本/摘要；
- 容器写入数据必须放入显式数据卷或外部持久化服务；
- 健康检查区分 `liveness` 与 `readiness`；
- Nginx 配置 SSE 禁止代理缓冲、WebSocket 连接升级、上传大小和超时策略；
- 生产数据库和 Redis 优先使用独立高可用实例/托管服务，不与应用容器共享生命周期。

#### 3.3.14 技术版本锁定策略

建议基线版本表：

| 组件 | 建议基线 | 锁定位置 |
| :--- | :------- | :------- |
| Python | 3.13.x | `.python-version`、Dockerfile |
| FastAPI | 0.136.x | `pyproject.toml`、`requirements/base.txt` |
| Pydantic | 2.x 兼容版本 | `requirements/base.txt` |
| SQLAlchemy | 2.0.x | `requirements/base.txt` |
| Alembic | 1.x | `requirements/base.txt` |
| LangGraph | 1.2.x | AI Extra 对应的锁定 Requirements |
| Celery | 5.x | Worker Extra 对应的锁定 Requirements |
| MySQL | 8.4.x LTS | Compose 镜像版本 + digest |
| PostgreSQL | 18.x | pgvector 镜像版本 + digest |
| pgvector | 0.8.6 基线 | 镜像 tag + digest、迁移检查 |
| Redis | 8.x | Compose 镜像版本 + digest |
| Docker Engine | 29.x | 部署环境清单 |
| Langfuse | v4 兼容版本 | 独立 Compose/部署清单 |

版本管理规则：

1. macOS 本地解释器使用项目独立 Conda 环境并由 `environment.yml` 锁定 Python；PyPI 业务依赖由 `pyproject.toml` 声明、pip-tools 生成带 Hash 的 `requirements/base.txt` 与 `requirements/dev.txt`，生产只使用 `pip --require-hashes` 安装 Base Lock，不混用 `conda install` 修改业务依赖。
2. Docker 镜像禁止 `latest`，生产进一步锁定镜像 digest。
3. Patch 升级先进入依赖更新分支，执行单元、集成、数据库、Agent 回归和安全测试。
4. MySQL/PostgreSQL/Redis 小版本升级先验证备份恢复、驱动兼容和迁移脚本。
5. 模型、Embedding、Rerank、Prompt、Skill 和 Agent Graph 都视为可改变行为的版本化依赖，不能只记录代码版本。
6. LangGraph 正在运行的 Checkpoint 恢复可能受到 Graph 代码变化影响；发布新图前必须设计运行版本、兼容策略或让旧 Run 在旧版本完成。
7. 每次生产发布生成 SBOM、依赖漏洞扫描结果、数据库迁移清单和回滚/前向修复说明。
8. 文档中的大版本基线不等于允许范围安装；最终部署以 Lockfile 和镜像摘要为准。

---

### 3.4 后端工程目录设计

后端工程采用 **单仓库、模块化单体、按领域垂直组织** 的结构。目录划分的目的不是单纯把文件分类，而是通过依赖方向约束业务代码：协议层负责接收请求，应用层编排用例，领域层决定规则，基础设施层实现数据库与外部系统访问。

同一业务能力的 Router、Schema、Service、Domain 和 Repository 实现应尽量放在同一领域模块内，避免形成一个包含全部路由的 `routers/`、一个包含全部模型的 `models/` 和一个无限膨胀的 `services/`。

#### 3.4.1 项目目录结构

建议后端根目录如下：

仓库根目录另外保留 `environment.yml`、`.python-version`、`.env.example` 与 `compose.yaml`，分别作为 Conda/Python 基线、本地配置示例和服务编排入口；以下为 `backend/` 内部结构。

```text
backend/
├── pyproject.toml                    # Python 项目、依赖、工具配置
├── requirements/                     # pip-tools 输入与带 Hash 的 Base/Dev Lock
├── Dockerfile
├── README.md
│
├── app/
│   ├── __init__.py
│   ├── main.py                       # FastAPI 应用工厂与生命周期
│   │
│   ├── core/                         # 与具体业务无关的公共基础能力
│   │   ├── config.py
│   │   ├── constants.py
│   │   ├── exceptions.py
│   │   ├── error_codes.py
│   │   ├── logging.py
│   │   ├── security.py
│   │   ├── id_generator.py
│   │   ├── clock.py
│   │   ├── pagination.py
│   │   ├── context.py
│   │   └── lifespan.py
│   │
│   ├── database/                     # 数据库连接与通用持久化基础设施
│   │   ├── mysql.py
│   │   ├── postgres.py
│   │   ├── redis.py
│   │   ├── session.py
│   │   ├── unit_of_work.py
│   │   ├── health.py
│   │   ├── base.py
│   │   └── types.py
│   │
│   ├── api/                          # 全局 API 装配，不承载具体业务规则
│   │   ├── dependencies.py
│   │   ├── middleware.py
│   │   ├── exception_handlers.py
│   │   ├── response.py
│   │   ├── openapi.py
│   │   └── v1/
│   │       └── router.py             # 聚合各领域模块 Router
│   │
│   ├── modules/                      # 业务领域模块
│   │   ├── auth/
│   │   ├── users/
│   │   ├── stores/
│   │   ├── catalog/
│   │   ├── inventory/
│   │   ├── cart/
│   │   ├── checkout/
│   │   ├── orders/
│   │   ├── payments/
│   │   ├── logistics/
│   │   ├── refunds/
│   │   ├── reviews/
│   │   ├── messaging/
│   │   ├── files/                    # 上传会话、文件元数据、绑定与生命周期
│   │   ├── notifications/
│   │   ├── content/                  # 平台 Banner/公告/帮助/页脚的版本化发布
│   │   ├── admin/                    # 后台查询/命令编排，不绕过领域服务
│   │   ├── admin_approvals/          # 管理员职责分离、审批决定与受控执行
│   │   ├── admin_jobs/               # 管理导入导出/长任务及逐项结果
│   │   └── audit/
│   │
│   ├── ai/                           # Agent、RAG、记忆、Skill 与模型适配
│   │   ├── runtime/
│   │   ├── agents/
│   │   ├── graphs/
│   │   ├── skills/
│   │   ├── tools/
│   │   ├── memory/
│   │   ├── context/
│   │   ├── rag/
│   │   ├── guardrails/
│   │   ├── evaluation/
│   │   └── model_providers/
│   │
│   ├── mcp/                          # MCP Host、Client、Server 与权限拦截
│   │   ├── host/
│   │   ├── client/
│   │   ├── servers/
│   │   │   ├── catalog/
│   │   │   ├── order/
│   │   │   ├── logistics/
│   │   │   ├── after_sale/
│   │   │   ├── support/
│   │   │   └── memory/
│   │   ├── permissions/
│   │   └── schemas/
│   │
│   ├── integrations/                 # 第三方服务适配层
│   │   ├── payments/
│   │   ├── logistics/
│   │   ├── object_storage/
│   │   ├── sms/
│   │   ├── email/
│   │   ├── content_safety/
│   │   └── model_gateway/
│   │
│   ├── workers/                      # Celery Worker 任务
│   │   ├── app.py
│   │   ├── outbox.py
│   │   ├── orders.py
│   │   ├── admin_approvals.py
│   │   ├── payments.py
│   │   ├── logistics.py
│   │   ├── notifications.py
│   │   ├── indexing.py
│   │   └── memory.py
│   │
│   ├── scheduler/                    # 定时任务定义
│   │   ├── app.py
│   │   └── schedules.py
│   │
│   └── observability/
│       ├── telemetry.py
│       ├── metrics.py
│       ├── tracing.py
│       ├── audit.py
│       └── redaction.py
│
├── migrations/
│   ├── mysql/
│   │   ├── env.py
│   │   └── versions/
│   └── postgres/
│       ├── env.py
│       └── versions/
│
├── tests/
│   ├── unit/
│   ├── integration/
│   ├── contract/
│   ├── e2e/
│   ├── concurrency/
│   ├── security/
│   ├── agent/
│   ├── evals/
│   ├── factories/
│   ├── fixtures/
│   └── conftest.py
│
├── scripts/
│   ├── bootstrap_dev.py
│   ├── seed_dev_data.py
│   ├── reindex_knowledge.py
│   ├── backfill_data.py
│   └── check_migrations.py
│
└── deploy/
    ├── nginx/
    ├── docker/
    ├── observability/
    └── environments/
```

每个领域模块采用一致的内部结构：

```text
modules/orders/
├── api/
│   ├── router.py
│   └── schemas.py
├── application/
│   ├── commands.py
│   ├── queries.py
│   ├── dto.py
│   └── service.py
├── domain/
│   ├── entities.py
│   ├── value_objects.py
│   ├── enums.py
│   ├── state_machine.py
│   ├── policies.py
│   ├── events.py
│   └── repository.py                # Repository 抽象接口
├── infrastructure/
│   ├── models.py                    # SQLAlchemy ORM Model
│   ├── repository_sqlalchemy.py
│   └── mappers.py
└── exceptions.py
```

较简单的模块可以合并部分文件，但不能改变依赖方向。不得因为文件数量少就让 Router 直接访问 ORM，也不得让 Domain 引用 FastAPI 或 SQLAlchemy。

#### 3.4.2 Core 公共基础模块

`app/core/` 只存放与商城具体业务无关、可被多个模块稳定复用的能力。

| 文件/能力 | 职责 |
| :-------- | :--- |
| `config.py` | 使用 Pydantic Settings 读取、校验和分组环境变量 |
| `constants.py` | 全局协议常量，不放商品、订单等领域枚举 |
| `exceptions.py` | 基础异常类，如 `ApplicationError`、`NotFoundError`、`ConflictError` |
| `error_codes.py` | 通用错误码；具体业务错误码仍归对应模块 |
| `logging.py` | JSON 日志格式、级别、上下文字段和 Handler 初始化 |
| `security.py` | 密码哈希、Token/Cookie、签名、随机数和常量时间比较 |
| `id_generator.py` | 单调 ULID、公开 ID 前缀注册与格式校验；订单/支付等业务号策略由所属 Domain 提供 |
| `clock.py` | 可注入时钟，业务代码不直接散落调用当前时间，便于测试超时状态 |
| `pagination.py` | Page/Cursor 通用输入输出类型 |
| `context.py` | `request_id`、`trace_id`、当前身份等 ContextVar |
| `lifespan.py` | 应用启动/关闭时的连接初始化和资源释放 |

Core 模块禁止包含：

- 商品、订单、退款等业务状态机；
- 针对某张业务表的 SQL；
- 第三方支付或物流供应商实现；
- Agent Prompt 和 Skill；
- 为了“复用”而把多个领域的 Service 拼成通用 Service。

当一个所谓通用工具只有一个领域使用时，应先保留在该领域模块，出现真实稳定的复用需求后再提升到 Core。

#### 3.4.3 Database 数据库连接模块

`app/database/` 负责连接、Session、事务基础设施和数据库健康检查，不负责具体业务查询。

##### MySQL

`mysql.py` 负责：

- 创建 MySQL `AsyncEngine`；
- 配置连接池、连接回收、连接超时和 SQL Echo；
- 注册必要的连接初始化选项；
- 暴露 `mysql_session_factory`；
- 应用关闭时释放连接池。

##### PostgreSQL

`postgres.py` 负责：

- 创建 PostgreSQL `AsyncEngine`；
- 暴露知识库、记忆和 Agent Checkpoint 所需 Session Factory；
- 检查 `vector` 扩展是否已由迁移安装；
- 配置向量查询需要的事务级参数接口；
- 不在连接初始化阶段自动创建表或重建索引。

##### Redis

`redis.py` 负责：

- 创建异步 Redis 连接池；
- 按用途暴露 Cache、Session、Rate Limit、Broker 等逻辑客户端；
- 统一 Key 前缀、序列化和超时；
- 提供健康检查与降级状态；
- 禁止业务模块自行创建未受管理的新连接。

##### Unit of Work

`unit_of_work.py` 为需要多个 Repository 协同的 MySQL 用例提供事务边界：

```text
进入 UnitOfWork
  → 创建当前请求独享的 AsyncSession
  → 执行多个 Repository 操作
  → 写领域状态与 Outbox Event
  → 全部成功则 commit
  → 任一异常则 rollback
  → 关闭 Session
```

一个 Unit of Work 只管理一个数据库事务。禁止把 MySQL Session 与 PostgreSQL Session 包装成“统一提交”，跨库操作使用 3.5.8 的同步机制。

#### 3.4.4 API 路由模块

`app/api/` 负责全局协议装配，各业务 Router 放在领域模块的 `api/router.py`。

```text
app/api/v1/router.py
  ├── include auth.router
  ├── include users.router
  ├── include stores.router
  ├── include catalog.router
  ├── include cart.router
  ├── include orders.router
  ├── include refunds.router
  ├── include messaging.router
  └── include admin.router
```

Router 的职责仅限：

1. 声明 HTTP 方法、路径、状态码和 OpenAPI 元数据；
2. 解析 Path、Query、Header 和 Body；
3. 通过依赖注入取得当前用户、权限和应用 Service；
4. 调用一个明确的应用用例；
5. 把 DTO 转换为公开 Response Schema；
6. 不捕获无法处理的异常，由统一 Exception Handler 转换。

Router 禁止：

- 直接导入 SQLAlchemy ORM Model 执行查询；
- 在路由函数内提交/回滚事务；
- 在路由函数内计算价格、库存、退款资格或推进状态机；
- 返回任意字典而没有 Response Schema；
- 把内部异常堆栈、数据库错误或第三方原始错误直接返回前端；
- 使用前端传入的 `user_id` 代替认证上下文。

全局 Middleware 只处理 Request ID、Trace、日志、CORS、安全响应头和必要的请求大小限制。鉴权与细粒度权限使用显式依赖/Service 校验，避免隐藏在难测试的全局 Middleware 中。

#### 3.4.5 业务领域模块

业务按领域而非数据库表划分。推荐模块及数据所有权：

| 模块 | 主要职责 | 拥有的核心数据 |
| :--- | :------- | :------------- |
| `auth` | 注册、登录、刷新、退出、验证码、锁定 | 用户凭证、认证 Session |
| `users` | 个人资料、地址、用户状态 | 用户、地址 |
| `stores` | 店铺资料、认证、经营分类、店铺商品分组、公告/推荐、服务政策、配送运费模板、收藏店铺 | 店铺相关数据与配送规则 |
| `catalog` | 分类、品牌、商品、SKU、图片、FAQ | 商品目录 |
| `inventory` | 实时库存、预占、释放、库存流水 | 库存聚合 |
| `cart` | 购物车条目、勾选、数量 | 用户购物车 |
| `checkout` | 结算预览、价格快照、地址/配送校验 | 短期结算会话；只保存其他领域结果快照 |
| `orders` | 交易单、店铺订单、订单项、状态机 | 订单聚合 |
| `payments` | 支付单、回调、对账、渠道退款 | 支付聚合 |
| `logistics` | 发货包裹、包裹商品、轨迹 | 履约物流 |
| `refunds` | 资格检查、退款/退货申请、平台申诉、售后状态机 | 售后与申诉聚合 |
| `reviews` | 评价、图片、追评、商家回复 | 评价聚合 |
| `messaging` | 会话、消息、未读、人工接管 | 消息与客服会话 |
| `files` | 上传策略、直传会话、扫描状态、业务绑定、访问授权与清理 | 文件对象元数据与上传会话；二进制仍在对象存储 |
| `notifications` | 站内信、短信、邮件任务状态 | 通知记录 |
| `admin` | 后台用例编排、管理端查询 | 不复制其他领域事实 |
| `admin_approvals` | 高风险管理命令审批、职责分离、执行恢复与对账 | 管理员审批申请、决定和事件 |
| `admin_jobs` | 管理导入/导出、预检、确认、进度和产物授权 | 管理批处理任务与逐项结果；业务写仍调用所属领域命令 |
| `content` | 平台 Banner、公告、帮助文章和页脚链接的审核/发布 | 内容条目与不可变版本 |
| `audit` | 业务审计查询和归档 | 操作审计 |

一个模块不能直接修改另一个模块拥有的表。例如退款模块不能直接更新支付表；它应调用支付模块的退款命令或产生明确事件。订单模块也不能直接修改商品基础信息。

跨模块读取优先通过应用 Query Service 或稳定 DTO；跨模块写入通过应用 Command/Service 或领域事件。禁止通过导入另一个模块的 ORM Model 绕过其规则。

#### 3.4.6 Repository 数据访问层

Repository 隔离领域/应用逻辑与具体存储技术。

```text
domain/repository.py                 定义抽象接口
infrastructure/repository_sqlalchemy.py  SQLAlchemy 实现
```

示例接口语义：

```text
OrderRepository
├── get_by_id(order_id, user_scope)
├── get_for_update(order_id)
├── list_by_user(query)
├── add(order)
└── save(order)
```

Repository 规则：

- 方法按领域语义命名，不暴露“任意 where 条件”给上层；
- 返回领域实体或应用 DTO，不把 ORM Model 泄露到 Router；
- 不包含权限之外的数据过滤决定，必要的 `user_id/store_id` 范围必须成为显式参数；
- 不自行 `commit`，事务由 Unit of Work/Application Service 控制；
- 可执行必要的 `flush` 获取数据库生成值，但不能把 `flush` 当提交；
- 关键并发读取提供明确的 `for_update` 方法；
- 列表查询显式分页、排序和字段投影，禁止无界 `all()`；
- 避免 N+1，关联加载策略写入查询实现；
- 原生 SQL 仅在 ORM 无法合理表达且有性能证据时使用，并提供测试和参数绑定。

缓存不是 Repository 的默认职责。商品详情等读缓存由专门的 Query Service/Cache Decorator 控制，避免所有 Repository 隐式返回不确定的新旧数据。

#### 3.4.7 Service 业务服务层

Application Service 代表一个可被 API、Worker、MCP 或管理员后台调用的业务用例，例如：

```text
CreateOrderService
CancelOrderService
HandlePaymentCallbackService
ConfirmReceiptService
SubmitRefundApplicationService
SendMessageService
```

Service 负责：

- 接收已经通过 Schema 校验的 Command；
- 依据认证上下文执行资源归属和权限检查；
- 打开 Unit of Work；
- 调用 Repository 加载所需聚合；
- 调用 Domain 规则/状态机作出确定性判断；
- 调用同事务内所需 Repository；
- 生成领域事件并写入 Outbox；
- 提交后返回稳定 DTO；
- 把可预期业务异常转换为明确业务错误码。

Service 不负责：

- 解析 HTTP Request、Cookie 或 WebSocket；
- 拼装前端展示文案；
- 在事务中等待耗时模型调用、短信或全量物流查询；
- 根据 AI 自然语言直接执行写操作；
- 绕过领域规则直接赋值关键状态字段；
- 吞掉异常后仍返回成功。

查询与写入可以分别组织为 Query Service 和 Command Service。复杂报表查询允许使用专门只读 DTO/SQL，不必强行重建完整领域实体，但仍需遵守数据范围和分页。

#### 3.4.8 Domain 状态机与规则层

Domain 层保存不依赖 FastAPI、SQLAlchemy、Redis、Celery 和具体模型供应商的核心规则：

- 实体（Entity）和聚合（Aggregate）；
- 值对象（Value Object），如 Money、Quantity、OrderNo；
- 订单、支付、履约、退款状态机；
- 价格、库存、售后资格等确定性 Policy；
- 领域事件；
- Repository 抽象接口；
- 领域异常。

状态变更必须通过行为方法：

```text
order.mark_paid(...)
order.cancel(...)
order.mark_shipped(...)
order.confirm_received(...)
refund.submit(...)
refund.approve_by_rule(...)
```

禁止在 Service 或 Repository 中随意执行：

```python
order.status = "completed"
```

Domain 方法必须校验旧状态、操作身份、必要参数和不变量，并产生明确领域事件。状态机测试应覆盖合法转换、非法转换、重复调用和边界时间。

涉及当前时间的规则使用注入的 `Clock`；涉及金额使用 Money/Decimal；涉及外部查询的规则由 Service 先取得结构化事实，再传入纯 Domain Policy，确保单元测试不需要网络和数据库。

#### 3.4.9 Integration 外部服务适配层

`app/integrations/` 通过内部统一接口隔离第三方供应商：

```text
PaymentProvider
LogisticsProvider
ObjectStorage
SmsProvider
EmailProvider
ContentSafetyProvider
ModelGateway
```

每类集成至少包含：

```text
protocol.py / interface.py     内部抽象接口
schemas.py                     统一输入输出
provider_x.py                  供应商实现
mock.py                        本地和测试实现
exceptions.py                  标准化异常
```

适配层负责供应商鉴权、请求构造、超时、重试、验签、原始状态映射和错误标准化，不负责订单/退款状态机。第三方返回“成功”后，仍由对应 Application Service 根据本地状态和幂等规则更新业务。

外部调用必须：

- 显式设置连接、读取和总超时；
- 只对安全/幂等请求执行有限重试；
- 传播 `trace_id`，记录供应商、接口、耗时和标准错误类型；
- 脱敏请求/响应日志；
- 通过熔断/降级防止外部故障耗尽 API 连接；
- 提供 Mock/Sandbox 实现供自动化测试使用。

#### 3.4.10 Agent 与 AI 模块

`app/ai/` 只负责 AI Runtime，不拥有商城交易数据。内部建议：

| 子目录 | 职责 |
| :----- | :--- |
| `runtime/` | Agent Run 创建、取消、恢复、流式事件、使用量限制 |
| `agents/` | 店铺客服、专属客服及专业子 Agent 定义 |
| `graphs/` | LangGraph State、节点、边、Interrupt 和 Subgraph |
| `skills/` | Skill Registry、版本加载、输入输出和发布状态 |
| `tools/` | 本地只读工具包装、MCP 工具适配，不实现任意业务写入 |
| `memory/` | 短期/长期记忆提取、校验、召回、衰减和删除 |
| `context/` | Token 预算、消息裁剪、滚动摘要和上下文组装 |
| `rag/` | 文档切片、混合检索、Rerank、引用和无结果策略 |
| `guardrails/` | 输入输出安全、Prompt Injection、防越权和工具拦截 |
| `evaluation/` | 数据集、评估器、回归任务和发布门禁 |
| `model_providers/` | 模型、Embedding、Rerank 的统一 Gateway |

关键依赖方向：

```text
Agent Graph
  → Skill Registry
  → MCP Client / Permission Gateway
  → 领域 API/MCP Server

Agent Graph
  → Context Builder
      ├── Messages（受控读取）
      ├── Checkpoint
      ├── Memory Store
      └── RAG Retriever
```

Agent 不得导入业务 ORM Model/Repository。即使 Agent Runtime 与业务 API 位于同一仓库，也必须经受控 Tool/Service Port 调用，以保证权限、审计和未来拆分边界。

Prompt、Agent Graph、Skill、Tool Schema、Embedding 模型和评估集均需版本化。不得在 Python 代码中散落无法追踪的长 Prompt 字符串。

#### 3.4.11 Worker 与 Scheduler 模块

Worker 与 Scheduler 分工：

| 模块 | 职责 |
| :--- | :--- |
| **Scheduler** | 定期查找“应该发生的任务”，只投递轻量任务 |
| **Worker** | 领取任务、重新校验最新业务状态、执行、重试和记录结果 |
| **Outbox Dispatcher** | 领取 MySQL Outbox Event 并可靠投递到任务系统 |

Scheduler 不应直接循环更新大量业务表。例如超时订单处理应为：

```text
Scheduler 分页查找到期订单 ID
  → 投递 expire_order(order_id)
  → Worker 调用 ExpireOrderService
  → Service 锁定订单并检查仍为待支付且确已过期
  → 取消订单、释放库存、写 Outbox
```

任务规范：

- 参数只传业务 ID、事件 ID 和必要版本，不传不可序列化 ORM 对象；
- 每个任务必须幂等；
- 重试前重新读取状态，不能盲目重复副作用；
- 设置软/硬超时和最大重试；
- 指数退避并加入随机抖动；
- 记录 `task_id`、`event_id`、`trace_id`、尝试次数和标准错误码；
- 永久失败进入死信，提供后台查看和人工重放；
- 任务重放仍走 Application Service，不允许后台脚本直接改状态。

#### 3.4.12 测试目录

测试按验证目标划分：

| 目录 | 内容 |
| :--- | :--- |
| `unit/` | Domain、Policy、状态机、纯函数、Schema |
| `integration/` | Repository、MySQL/PostgreSQL/Redis、对象存储和迁移 |
| `contract/` | REST/OpenAPI、Outbox Event、MCP Tool、第三方 Provider 契约 |
| `e2e/` | 从 API 到数据库/Worker 的完整用户流程 |
| `concurrency/` | 库存争抢、重复下单、支付回调、退款并发 |
| `security/` | 越权、IDOR、Token、签名、Prompt Injection、MCP 权限 |
| `agent/` | Graph 路由、Interrupt/Resume、工具选择、记忆和上下文 |
| `evals/` | Golden Dataset、RAG、回答质量、工具正确率和回归评估 |
| `factories/` | 类型安全的测试数据工厂 |
| `fixtures/` | 可复用但作用域明确的测试夹具 |

测试原则：

- 单元测试不访问网络和真实数据库；
- Repository 集成测试使用真实 MySQL/PostgreSQL/Redis 测试容器，不用 SQLite 假装 MySQL；
- 每个测试使用独立数据库/事务/命名空间，不能依赖执行顺序；
- 第三方支付、物流、模型使用可编程 Fake/Mock Provider；
- 核心状态机、金额和权限测试必须是确定性的；
- AI 评估与普通单元测试分开，避免模型随机性阻塞所有代码测试，但发布门禁必须执行指定评估集；
- 数据库迁移需要测试“全新安装”和“上一版本升级”两条路径。

#### 3.4.13 配置文件与环境变量

配置使用 Pydantic Settings，按领域分组：

```text
AppSettings
├── EnvironmentSettings
├── SecuritySettings
├── MySQLSettings
├── PostgreSQLSettings
├── RedisSettings
├── ObjectStorageSettings
├── WorkerSettings
├── AgentSettings
├── ModelProviderSettings
├── ObservabilitySettings
└── ExternalProviderSettings
```

环境划分：

```text
local       本地开发
test        自动化测试
staging     预发布/验收
production  生产
```

`.env.example` 只记录变量名、用途和安全示例：

```text
APP_ENV=
APP_PUBLIC_URL=
MYSQL_DSN=
POSTGRES_DSN=
REDIS_URL=
OBJECT_STORAGE_ENDPOINT=
OBJECT_STORAGE_BUCKET=
JWT_SIGNING_KEY=
PAYMENT_PROVIDER=
MODEL_PROVIDER=
OTEL_EXPORTER_OTLP_ENDPOINT=
```

配置规则：

- 真实 Secret 不提交 Git，不写入镜像；
- 生产使用 Docker/Kubernetes Secret 或云 Secret Manager；
- DSN、密钥和 Token 不输出到日志；
- 必填配置缺失或格式错误时启动失败，不以不安全默认值继续；
- 开发默认值不能自动流入生产；
- 配置项区分启动时固定与运行时可动态变更；
- 动态业务配置进入数据库并版本化、审计，例如自动退款阈值、Agent/Skill 发布版本；
- 配置变更记录操作者、旧值摘要、新值摘要和生效时间；
- 对影响交易或 Agent 权限的配置使用审批/灰度机制。

#### 3.4.14 模块依赖规则

总体依赖方向：

```text
API / Worker / MCP Adapter
          │
          ▼
Application Service
          │
          ▼
Domain
          ▲
          │ implements ports
Infrastructure / Integration
```

强制规则：

1. Domain 不依赖 FastAPI、SQLAlchemy、Redis、Celery、LangGraph 或第三方 SDK。
2. Router 只能调用 Application Service/Query Service，不能调用 Repository/ORM。
3. Repository 实现只能被本领域 Application Service 或明确的只读 Query Service 使用。
4. Worker、Scheduler 和 MCP Tool 必须调用 Application Service，不能直接修改表。
5. Agent Runtime 不导入业务 Repository/ORM，只能调用受控 MCP/API Port。
6. 一个领域不能直接修改另一个领域拥有的表。
7. 跨领域写操作使用公开 Command/Service 或领域事件；跨领域读取使用 Query Port/DTO。
8. Integration 负责供应商协议，Domain 不出现供应商专有状态码。
9. Core 不反向依赖任何业务模块。
10. `admin` 模块只能编排其他模块公开用例，不能成为绕过领域规则的“超级 Repository”。
11. 共享 DTO 必须有明确所有者和版本，禁止多个模块共同修改无边界的 `common_models.py`。
12. 通过静态检查、导入边界测试和 Code Review 阻止违规依赖。

允许依赖矩阵：

| 调用方 | Domain | Application | Repository 实现 | Integration | Agent/MCP |
| :----- | :----: | :---------: | :-------------: | :---------: | :-------: |
| API Router | ❌ | ✅ | ❌ | ❌ | 仅 Agent API |
| Application | ✅ | 同模块可用 | 通过接口注入 | 通过接口注入 | 受控调用 |
| Domain | — | ❌ | ❌ | ❌ | ❌ |
| Repository 实现 | ✅ | ❌ | — | ❌ | ❌ |
| Worker | ❌ | ✅ | ❌ | ❌ | 仅 AI 后台任务 |
| MCP Tool | ❌ | ✅ | ❌ | ❌ | — |
| Agent Graph | AI State | AI Runtime | ❌ | Model Gateway | ✅ |

### 3.5 数据架构总览

数据架构采用“按权威来源分类、按生命周期治理、按事件异步同步”的原则。系统不追求把所有数据放在同一个数据库，也不允许同一个事实在多个数据库中同时作为权威版本。

```text
MySQL
  ├── 商城业务事实
  ├── 会话消息与操作审计的权威记录（读取仍按 Purpose 最小投影）
  ├── AI/Skill/Tool 配置与用户授权
  └── Outbox Event（跨存储同步起点）

PostgreSQL + pgvector
  ├── AI 知识文档与切片
  ├── 长期记忆与对话摘要
  ├── Agent Checkpoint
  └── 向量/关键词检索索引

Redis
  ├── 缓存与热点上下文
  ├── Session/限流/在线状态
  └── Broker/临时协调数据

MinIO/S3
  └── 图片、视频、退款凭证、知识库原始文件
```

#### 3.5.1 数据分类与归属

| 数据类别 | 权威存储 | 示例 | 是否可由其他来源重建 |
| :------- | :------- | :--- | :------------------- |
| **身份与权限** | MySQL | 用户、凭证、角色、授权、Session 撤销记录 | 否 |
| **商品交易数据** | MySQL | 店铺、商品、SKU、库存、购物车、订单 | 否 |
| **财务与售后数据** | MySQL | 支付、退款、金额流水、状态事件 | 否 |
| **完整消息数据** | MySQL | 用户/AI/人工消息、卡片、已读、转人工 | 否（除备份外） |
| **AI 配置数据** | MySQL | Agent/Skill/Tool 版本、权限、用户确认 | 否 |
| **业务派生知识** | PostgreSQL，来源仍为 MySQL/文件 | 商品 FAQ/政策切片、Embedding | 是 |
| **AI 原生持久数据** | PostgreSQL | 长期记忆、摘要、Checkpoint | 部分可从消息恢复，不能假设全部可重建 |
| **缓存与会话热点** | Redis | 商品缓存、限流、在线状态、热点上下文 | 是 |
| **二进制文件** | 对象存储 | 图片、视频、凭证、知识库文件 | 原文件通常不可从数据库重建，需备份 |
| **可观测数据** | Loki/Tempo/Prometheus/Langfuse | 日志、Trace、指标、模型运行与评估 | 不作为业务事实；按独立保留策略管理 |

每个数据实体必须指定：

- Data Owner（负责领域）；
- 权威存储；
- 数据敏感级别；
- 创建和更新入口；
- 生命周期和删除方式；
- 是否允许缓存/复制；
- 是否进入 Prompt、RAG 或长期记忆；
- 审计和备份要求。

#### 3.5.2 MySQL 业务数据

MySQL 数据按领域分组：

| 领域 | 核心数据 |
| :--- | :------- |
| 用户与权限 | 用户、凭证、地址、角色、权限、认证 Session、管理员 MFA 认证器、管理员审批申请/决定/事件、封禁/注销状态 |
| 店铺与目录 | 店铺、平台经营分类、店铺商品分组、品牌、商品、SKU、图片元数据、FAQ、公告、推荐位、服务政策、配送/运费模板 |
| 库存 | 实时库存、预占库存、销售库存、库存流水 |
| 购物与交易 | 购物车、结算会话、交易单、店铺订单、订单项、地址/价格/运费快照 |
| 支付与履约 | 支付单、支付事件、回调、包裹、物流轨迹 |
| 评价与售后 | 评价、追评、商家回复、评价治理记录、退款申请、凭证元数据、售后事件、退货物流、退款申诉与复核事件 |
| 消息与客服 | 专属客服/店铺会话、消息、已读、上下文引用、人工服务工单、分配和内部备注 |
| 文件与上传 | 文件对象元数据、上传会话、扫描/激活/删除状态、对象存储归属与引用计数 |
| AI 治理 | Agent/Skill/Tool 版本、绑定、权限策略、用户授权、写操作确认、业务审计 |
| 平台内容运营 | Banner、平台公告、帮助文章、页脚链接及其不可变审核/发布版本 |
| 系统可靠性 | 幂等记录、Outbox、通知任务、死信、管理员操作日志、管理批处理任务与逐项结果 |

MySQL 数据分为三种可变性：

1. **可更新主数据**：用户资料、地址、店铺资料、商品和 SKU；使用版本字段和审计日志。
2. **状态机数据**：订单、支付、物流和售后；只能通过合法状态转换更新。
3. **追加型数据**：库存流水、支付事件、售后事件、Outbox 和审计；原则上只追加不修改，错误通过补偿记录修正。

业务查询不得依赖向量库或可观测平台判断交易状态。即使 PostgreSQL/Redis 中存在相同 `order_id` 或商品价格快照，也必须回到 MySQL 读取权威结果。

#### 3.5.3 PostgreSQL AI 数据

PostgreSQL 按 Schema 隔离：

| Schema | 权威范围 | 主要表/内容 |
| :----- | :------- | :---------- |
| `knowledge` | AI 知识索引状态 | documents、chunks、embeddings、indexing_jobs、retrieval_logs |
| `memory` | AI 长期记忆与摘要 | memory_items、memory_events、conversation_summaries |
| `agent_runtime` | Agent 执行恢复状态 | checkpoints、checkpoint_writes、run_state_refs |

##### 业务派生知识

商品、FAQ、店铺政策和平台政策的原始业务内容来自 MySQL；PostgreSQL 保存经过清洗、切片和向量化的检索副本。每条记录至少包含：

```text
source_type
source_id
source_version
content_hash
store_id/product_id（如适用）
embedding_model
index_version
status
```

检索结果只能辅助生成回答。价格、库存、订单、物流和退款状态必须调用实时业务工具。

##### AI 原生数据

长期记忆、摘要和 Checkpoint 属于 AI 子系统数据：

- 长期记忆必须关联用户、命名空间、来源消息、置信度、敏感级别、授权范围和过期时间；
- 对话摘要不能替代 MySQL 原始消息；
- Checkpoint 只用于恢复 Agent Graph，不是业务写操作的事务日志；
- 已在 MySQL 成功执行的工具操作，Agent 恢复时通过工具审计/幂等键读取结果，不能重复执行；
- PostgreSQL 不保存业务数据库密码或第三方永久密钥。

#### 3.5.4 Redis 临时数据

Redis Key 统一包含环境和系统前缀；尖括号表示文档占位符，不是 Key 字面量：

```text
ecom:<env>:<namespace>:<identifier>
```

示例：

```text
ecom:prod:cache:product:<product_no>
ecom:prod:session:<session_no>
ecom:prod:rate:user:<user_no>:<route>
ecom:prod:chat:online:<conversation_no>
ecom:prod:chat:unread:<user_no>
ecom:prod:agent:context:<conversation_no>
ecom:prod:agent:lock:<conversation_no>
```

完整版本字段、Redis Cluster Hash Tag 和 Key Registry 规则以 3.9.1 为准。

Redis 数据要求：

- 除少量协调 Key 外全部设置明确 TTL；
- Key 设计文档必须说明数据类型、TTL、创建者、读取者和删除方式；
- Value 使用版本化 JSON/MsgPack 等安全格式，禁止 Pickle 反序列化不可信数据；
- 不保存密码、验证码明文、完整地址、支付密钥或完整退款凭证；
- 高基数 Key 需要容量估算与扫描/清理策略；
- 禁止在线接口使用 `KEYS` 等阻塞命令；
- 缓存删除使用明确 Key 或版本前缀，不依赖全库 Flush；
- Redis 故障时业务根据 3.2.10 降级，不能把缓存值提升为交易事实。

购物车最终持久化在 MySQL，Redis 仅可缓存。认证 Session 是否可完全依赖 Redis由安全设计决定；至少应保证 Redis 丢失只导致重新认证，不导致已撤销 Session 被错误恢复。

#### 3.5.5 对象存储文件数据

Bucket 建议按公开级别和业务用途拆分：

```text
public-assets          商品图、店铺 Logo 等公开文件
private-user-assets    用户头像原图等受控文件
review-assets          评价图片
after-sale-evidence    售后凭证，严格私有
knowledge-source       AI 知识库原始文件，严格私有
temporary-uploads      尚未确认归属的临时上传
```

数据库保存文件元数据：

```text
object_key
bucket
owner_type / owner_id
mime_type
size_bytes
sha256
visibility
status
created_by
created_at
deleted_at
```

文件状态：

```text
pending_upload → uploaded → scanning → active
                                └── rejected
active → pending_delete → deleted
```

客户端不得把临时对象 Key 直接当成业务文件。只有后端确认上传完成、类型/大小/哈希正确且安全扫描通过后，才能绑定商品、评价或退款申请。

公开文件可通过 CDN；私有文件通过后端鉴权后返回短期签名 URL。退款凭证和知识文件的下载必须记录访问审计。

#### 3.5.6 数据生命周期

数据生命周期统一分为：

```text
创建 → 活跃使用 → 冷数据 → 归档 → 匿名化/删除
```

建议基线（具体期限需结合实际法律、支付渠道、财务和运营要求确认）：

| 数据 | 活跃阶段 | 冷数据/归档 | 删除/匿名化原则 |
| :--- | :------- | :---------- | :--------------- |
| 验证码、临时 Token | 分钟级 | 不归档 | 到期立即不可用，短期保留哈希审计后清理 |
| 登录 Session | 登录有效期 | 撤销记录短期保留 | 到期/退出后清理热点数据，保留必要安全事件 |
| 购物车 | 持续使用 | 长期未活动可转冷 | 按产品策略清理，用户重新登录不应无故丢失有效购物车 |
| 结算预览 | 约 30 分钟或配置值 | 不归档 | 到期删除/失效，不作为订单事实 |
| 商品和店铺 | 在售/经营期 | 下架、关闭后归档 | 有历史订单引用时保留必要快照 |
| 订单、支付、退款 | 完整业务周期 | 按财务/合规期限归档 | 不直接物理删除；可对个人字段加密、脱敏或匿名化 |
| 消息 | 会话活跃期 | 按客服与审计策略归档 | 店铺会话可由用户隐藏，并在重新咨询或收到新消息时恢复显示；正文按政策隔离、匿名化和最终清理 |
| Agent Checkpoint | Run 活跃/可恢复期 | Run 结束后短期保留 | 到期删除，业务审计不依赖 Checkpoint |
| 短期上下文缓存 | 小时级 | 不归档 | TTL 自动删除，可从消息/Checkpoint 重建 |
| 长期记忆 | 授权有效且仍相关 | 衰减后归档/失效 | 到期、撤销授权或用户删除时移除正文和向量 |
| 知识向量 | 来源版本有效期 | 旧版本保留短期回滚窗口 | 来源删除/新版切换后清理旧向量 |
| 日志/Trace | 在线排障窗口 | 低成本存储可归档 | 到期删除，关键业务审计另存 MySQL |

生命周期期限必须配置化，并记录策略版本。清理任务分批、限速、可重试，不能在一个事务中删除海量数据。

#### 3.5.7 数据敏感等级

定义四级数据分类：

| 等级 | 名称 | 示例 | 基本控制 |
| :--- | :--- | :--- | :------- |
| **L0** | 公开数据 | 已上架商品名称、公开价格、店铺 Logo、公开评价 | 可公开读取，仍需防篡改和来源校验 |
| **L1** | 内部数据 | 内部配置、普通运行指标、未公开商品草稿 | 登录/服务身份访问，不进入公开响应 |
| **L2** | 个人/业务敏感 | 手机号、邮箱、地址、订单、聊天记录、用户偏好 | 行级权限、传输加密、展示脱敏、访问审计 |
| **L3** | 高敏感/凭证 | 密码哈希、验证码、Token、支付密钥、身份证明、退款私密凭证 | 强加密/哈希、最小人员访问、禁止日志/Prompt、严格审计 |

各组件处理规则：

| 场景 | L0 | L1 | L2 | L3 |
| :--- | :--: | :--: | :--: | :--: |
| 公共 API 返回 | ✅ | ❌ | 仅本人/授权且脱敏 | ❌ |
| 普通日志 | 摘要可用 | 摘要可用 | 仅脱敏 ID | ❌ |
| Trace 属性 | 可用 | 有限 | 仅引用/脱敏 | ❌ |
| RAG 知识库 | ✅ | 按 Agent 范围 | 原则上不直接进入共享知识库 | ❌ |
| Agent Prompt | ✅ | 必要最小范围 | 本次授权且最小化 | ❌ |
| 长期记忆 | 可用 | 不适用 | 经授权且允许用户管理 | ❌ |

完整地址、手机号、订单详情等即使属于当前用户，也不应无条件放入模型上下文；必须按当前意图最小化，例如查询物流通常只需订单号、状态和轨迹，不需要完整收货地址。

#### 3.5.8 跨数据库同步策略

跨数据库同步只允许从明确权威源向派生存储同步：

```text
MySQL 商品/FAQ/政策
    → Outbox
    → Indexing Worker
    → PostgreSQL knowledge

MySQL 用户/会话授权与消息引用
    → Memory/Summary Worker
    → PostgreSQL memory（经规则和授权）

MySQL 业务更新
    → Outbox
    → Cache Invalidation Worker
    → Redis 删除/刷新
```

每个同步事件包含：

```text
event_id
event_type
aggregate_type
aggregate_id
aggregate_version
schema_version
occurred_at
trace_id
payload（最小必要字段）
```

同步规则：

- 事件和业务数据在同一个 MySQL 事务提交；
- 消费者以 `event_id` 幂等；
- 派生记录保存 `source_version`，旧版本事件晚到时不得覆盖新版本；
- 事件 Payload 不携带不必要的 L2/L3 数据；
- 大正文/文件只携带对象引用和哈希，消费者再以服务身份读取；
- 删除事件使用 Tombstone，消费者删除向量、缓存和受影响记忆引用；
- 同步失败不回滚已完成的 MySQL 业务；
- 失败进入重试，超过上限进入死信并告警；
- 提供按 `source_id` 全量重建和对账工具；
- 定期比较 MySQL 当前版本与 PostgreSQL `source_version`，检测漏同步。

PostgreSQL 不反向修改 MySQL 业务状态。AI 长期记忆是例外的 AI 原生数据，但其授权和高风险业务动作仍由 MySQL 记录，记忆内容不能成为订单/退款决策的权威事实。

#### 3.5.9 Outbox 最终一致性机制

Outbox 表与业务变更处于同一 MySQL 事务，用于解决“业务已提交但消息未投递”或“消息已投递但业务回滚”的双写问题。

建议字段：

```text
outbox_events
├── id                    BIGINT UNSIGNED
├── event_id              VARCHAR(40) UNIQUE
├── aggregate_type        VARCHAR(64)
├── aggregate_id          VARCHAR(64)
├── aggregate_version     BIGINT UNSIGNED
├── event_type            VARCHAR(128)
├── schema_version        SMALLINT UNSIGNED
├── payload               JSON
├── status                VARCHAR(32)
├── attempts              INT UNSIGNED
├── available_at          DATETIME(6)
├── lease_owner           VARCHAR(128) NULL
├── lease_expires_at      DATETIME(6) NULL
├── trace_id              VARCHAR(64) NULL
├── created_at            DATETIME(6)
├── published_at          DATETIME(6) NULL
└── last_error            VARCHAR(1000) NULL
```

状态：

```text
pending → processing → published
              ├── pending（可重试失败）
              └── dead（达到最大重试）
```

完整流程：

```text
1. Application Service 开启 MySQL 事务
2. 修改业务聚合
3. 写入 outbox_events(status=pending)
4. 提交事务
5. Dispatcher 使用 FOR UPDATE SKIP LOCKED 分批领取到期事件
6. 写入 lease_owner/lease_expires_at，短事务提交
7. 投递 Celery/目标消费者
8. 消费者以 event_id 检查幂等并执行业务
9. 成功后标记 published/消费完成
10. 失败时增加 attempts，计算下一次 available_at
11. 超过阈值进入 dead_letter_events 并告警
```

可靠性要求：

- Dispatcher 崩溃后，Lease 到期的 `processing` 事件可重新领取；
- 投递成功但标记失败会导致重复投递，因此消费者必须幂等；
- 对同一聚合要求顺序的消费者使用 `aggregate_version`，拒绝旧版本覆盖；
- 不假设全系统严格全局顺序；
- Payload Schema 版本化，消费者在兼容窗口内支持当前和上一版本；
- Dead Letter 重放必须记录操作者、原因和新 Trace；
- Outbox 表定期归档已发布记录，在线表保留足够的排障窗口；
- 支付、退款等事件的业务审计不能仅依赖已归档的 Outbox Payload。

#### 3.5.10 数据删除与归档策略

数据删除区分四种语义：

| 方式 | 含义 | 适用数据 |
| :--- | :--- | :------- |
| **用户侧隐藏** | 用户界面不再展示，但后台仍按权限/政策保留 | 已删除会话、部分已完成订单入口 |
| **逻辑删除** | 设置 `deleted_at`，普通查询自动排除，可审计 | 地址、收藏、普通内容配置 |
| **匿名化** | 保留交易/统计结构，清除或替换可识别个人字段 | 注销用户关联的历史交易、长期分析数据 |
| **物理删除** | 从在线库、备份生命周期、向量、缓存和对象存储中最终移除 | 临时数据、到期记忆、无保留要求文件 |

##### 用户注销

```text
提交注销并二次验证
  → 检查未完成订单、售后、余额等阻断条件
  → 进入冷静期/待注销状态（如产品策略要求）
  → 撤销 Session 和授权
  → 停止个性化记忆与营销
  → 匿名化允许匿名化的数据
  → 保留法定/财务/争议处理所需交易记录并严格限制访问
  → 删除长期记忆正文和向量
  → 删除/匿名化非必要消息与文件
  → 通过 Tombstone 清理 PostgreSQL、Redis 和对象存储派生数据
  → 写入不可包含原始敏感值的注销审计
```

##### 会话删除

用户点击店铺会话的“删除会话”后，前台立即隐藏，但不删除该用户与店铺之间的唯一会话 ID；对方发送新消息或用户再次从店铺、商品、订单入口咨询时，恢复原会话并明确记录 `Reopened`，不创建第二个会话。后端不得把支付、退款、人工客服处理和安全审计所需记录无条件级联物理删除。消息正文根据隐私与客服保留策略进入隔离、匿名化或定期物理清理流程，专属客服系统会话不得由用户删除或隐藏。

##### 业务记录归档

- 完成且超过在线查询窗口的订单/支付/退款可迁移到归档表或归档库；
- 归档前校验无进行中售后、争议或对账任务；
- 用户查询历史订单通过统一 Query Service 合并在线和归档结果；
- 归档记录仍保持公开订单号唯一、金额精度和状态事件可追溯；
- 归档不是删除，仍受访问控制、加密、备份和保留策略约束。

##### 向量与缓存删除

- 来源内容删除/下架不一定立即删除交易快照，但应按知识可见性停止检索；
- 知识 Tombstone 到达后删除/失效对应 Chunk 和向量；
- 长期记忆删除时同时删除结构化内容、Embedding 和相关缓存；
- Redis 删除失败依赖 TTL 兜底，同时告警高风险缓存失效失败；
- 向量清理后运行对账，确保不存在失去来源的孤儿向量。

##### 对象删除

- 先把数据库文件状态改为 `pending_delete`，阻止新访问；
- 异步删除对象并记录结果；
- 删除成功后更新 `deleted`，失败重试；
- 临时上传按 TTL 清理；
- 共享同一哈希的对象需要引用计数，不能因一个业务引用删除而影响其他合法引用；
- 私有文件的 CDN/签名 URL 缓存需同步失效。

所有保留期限属于可配置的数据治理策略，正式生产前必须结合适用法律、财务、支付渠道、争议处理和企业内部制度完成审查。开发环境中的测试数据不得混入生产备份或评估数据集。

---

### 3.6 MySQL 数据库设计规范

本节是 MySQL 建表、SQLAlchemy Model、Repository 和 Alembic 迁移的强制基线。3.7 中的“建议字段”需在迁移文件中固化；若实现需偏离本节，必须在 ADR 中记录理由、风险、兼容方案和恢复方案。

#### 3.6.1 字符集与存储引擎

- 数据库、表和普通文本列统一使用 `utf8mb4`，默认排序规则使用 `utf8mb4_0900_ai_ci`。
- 用户名、店铺名等需人类语义搜索的列使用语义排序；状态码、幂等键、公开 ID、哈希和签名使用 ASCII/二进制比较，避免大小写折叠。
- 所有业务表使用 `InnoDB`，不使用 MyISAM。
- 默认行格式使用 `DYNAMIC`；超长文本、大 JSON 和二进制文件不应塞入交易主表，原文件放对象存储。
- 表名、字段名、索引名只使用小写英文、数字和下划线，不使用保留字，不依赖操作系统的大小写行为。
- 连接建立时显式设置时区为 `+00:00`，并开启严格 SQL Mode；不允许依赖隐式截断、零日期或非确定性类型转换。

#### 3.6.2 主键与公开 ID

每张实体表默认同时使用内部主键和公开 ID：

| 用途 | 字段示例 | 类型 | 规则 |
| :--- | :--- | :--- | :--- |
| 内部关联主键 | `id` | `BIGINT UNSIGNED` | 数据库自增，仅限内部关联，不对外暴露 |
| 公开资源 ID | `user_no` / `product_no` / `conversation_no` | `VARCHAR(40) CHARACTER SET ascii COLLATE ascii_bin` | `<资源前缀>_<26 位单调 ULID>`，全表唯一 |
| 可直接定位的业务号 | `trade_no` / `order_no` / `payment_no` / `refund_no` | `VARCHAR(32)` ASCII | 首版使用注册前缀 + 26 位 ULID，不携带用户手机号等隐私 |
| 外部系统 ID | `provider_trade_no` | `VARCHAR(128)` | 保留原值，按渠道维度建唯一/普通索引 |

规则：

- API 路径、日志和前端状态只使用公开 ID/业务号，不返回自增 `id`。
- 所有外键列与被引用列类型、有无符号保持一致。
- 公开资源 ID 由统一 ID Generator 生成，格式固定为 `<resource_prefix>_<26-char-ulid>`，例如 `usr_01K...`、`prd_01K...`、`sku_01K...`、`conv_01K...`；前缀来自受版本控制的资源前缀注册表，大小写敏感，不允许同一资源出现多种前缀。
- ULID 部分在同一毫秒内需单调并检测时钟回拨；普通公开 ID 总长度不得超过 40 个 ASCII 字符，不接受客户端自行传入服务端主体 ID。首版 `trade_no/order_no/payment_no/refund_no` 分别使用 `trd_/ord_/pay_/ref_ + 26-char-ulid`，长度不超过 32；若未来另增面向人工抄录的短号，必须使用独立字段和唯一约束，不能替换既有规范 ID。
- 纯关联表可使用复合主键；若需审计、逻辑删除或被其他表引用，则仍使用独立 `id`。

公开 ID 的唯一机器可读来源是 `docs/id_registry.yaml`；本表为其生成的规范摘要，不允许手工创造未注册前缀。Registry 以 `resources` 的 Map Key 表示 `resource_type`，并通过顶层默认值补全通用属性；展开默认值后的每项必须完整包含 `resource_type、prefix、ulid_length、storage_length、url_allowed、client_generated、owner、status`。Pydantic 类型、OpenAPI Pattern、DDL 容量检查、Fixture 和本文示例扫描均从该 Registry 生成或由 CI 比对。

| 资源类型 | 前缀 | 存储上限 | URL | 客户端生成 | 资源类型 | 前缀 | 存储上限 | URL | 客户端生成 |
| :--- | :--- | :---: | :---: | :---: | :--- | :--- | :---: | :---: | :---: |
| User | `usr_` | 40 | 是 | 否 | Store | `sto_` | 40 | 是 | 否 |
| Store Certification | `cert_` | 40 | 是 | 否 | Store Product Group | `grp_` | 40 | 是 | 否 |
| Store Service Policy | `pol_` | 40 | 是 | 否 | Shipping Template | `sht_` | 40 | 是 | 否 |
| Store Announcement | `ann_` | 40 | 是 | 否 | Category | `cat_` | 40 | 是 | 否 |
| Brand | `brd_` | 40 | 是 | 否 | Inventory Reservation | `irs_` | 40 | 否 | 否 |
| Product | `prd_` | 40 | 是 | 否 | SKU | `sku_` | 40 | 是 | 否 |
| Product Content Version | `pcv_` | 40 | 是 | 否 | Product FAQ Version | `fqv_` | 40 | 是 | 否 |
| Auth Session | `ses_` | 40 | 否 | 否 | Address | `addr_` | 40 | 是 | 否 |
| Verification | `ver_` | 40 | 否 | 否 | Reset Ticket | `rst_` | 40 | 否 | 否 |
| Agreement Acceptance | `agr_` | 40 | 否 | 否 | Auth Attempt | `aat_` | 40 | 否 | 否 |
| Cart | `cart_` | 40 | 否 | 否 | Cart Item | `ci_` | 40 | 是 | 否 |
| Checkout | `chk_` | 40 | 是 | 否 | Eligibility Token/Ref | `ret_` | 40 | 否 | 否 |
| Trade Order | `trd_` | 32 | 是 | 否 | Store Order | `ord_` | 32 | 是 | 否 |
| Order Item | `oit_` | 40 | 是 | 否 | Payment | `pay_` | 32 | 是 | 否 |
| Shipment | `shp_` | 40 | 是 | 否 | Refund | `ref_` | 32 | 是 | 否 |
| Refund Draft | `rfd_` | 40 | 否 | 否 | Refund Policy Snapshot | `rsp_` | 40 | 否 | 否 |
| Refund Appeal | `rap_` | 40 | 是 | 否 | Review | `rev_` | 40 | 是 | 否 |
| Product FAQ | `faq_` | 40 | 是 | 否 | Contact Ref | `ctk_` | 40 | 否 | 否 |
| Conversation | `conv_` | 40 | 是 | 否 | Conversation Context | `ctx_` | 40 | 否 | 否 |
| Message | `msg_` | 40 | 是 | 否 | Client Message | `cmsg_` | 40 | 否 | **是** |
| Human Ticket | `tkt_` | 32 | 是 | 否 | Human Assignment | `asg_` | 40 | 否 | 否 |
| Agent Run | `run_` | 40 | 是 | 否 | Agent / Skill / Tool | `agt_` / `skl_` / `tol_` | 40 | 是 | 否 |
| User Agent Consent | `con_` | 40 | 是 | 否 | Agent Approval / Action | `apr_` / `act_` | 40 | 否 | 否 |
| Admin Approval Request | `aar_` | 40 | 是 | 否 | Admin Approval Decision | `aad_` | 40 | 否 | 否 |
| Role Grant | `rgr_` | 40 | 否 | 否 | MFA Authenticator | `mfa_` | 40 | 否 | 否 |
| Sensitive Access Grant | `sag_` | 40 | 否 | 否 | AI Feedback | `fdb_` | 40 | 否 | 否 |
| File | `file_` | 40 | 是 | 否 | Upload Session | `upl_` | 40 | 否 | 否 |
| Outbox Event | `evt_` | 40 | 否 | 否 | Audit | `aud_` | 40 | 否 | 否 |
| Notification Task | `ntf_` | 40 | 否 | 否 | Batch Job / Dead Letter | `job_` / `dlq_` | 40 | 是 | 否 |
| Knowledge Document | `doc_` | 40 | 是 | 否 | Knowledge Chunk | `kch_` | 40 | 否 | 否 |
| Index Job | `kjob_` | 40 | 否 | 否 | Retrieval | `rtv_` | 40 | 否 | 否 |
| Memory Item | `mem_` | 40 | 是 | 否 | Memory Event | `mev_` | 40 | 否 | 否 |
| Conversation Summary | `sum_` | 40 | 否 | 否 | Content / Content Version | `cnt_` / `cver_` | 40 | 是 | 否 |

除 `cmsg_` 外，客户端不得生成任何主体 ID。`Idempotency-Key、Cursor、Approval Token、Upload Ticket、Request ID` 与配置/法务版本号不是资源 ID，其格式也由 `docs/id_registry.yaml.non_resource_identifiers` 登记：Request ID 固定使用 `req_<26-char-ulid>`，其他不透明值不得伪装成资源 ID；`regcfg_*/username_v*/password_v*/terms_*/privacy_*` 仅是受注册表管理的语义版本格式，不可作为资源定位符。`url_allowed=否` 不意味不可在鉴权后响应中引用，只表示不得放入浏览器 URL、可分享链接或外部重定向。

Pydantic 为每类 ID 提供独立受约束类型并校验前缀；不得只校验“是字符串”。数据库列使用统一容量不表示不同资源 ID 可以互换，Service 仍须同时验证资源类型、归属和权限。

#### 3.6.3 字段命名规范

| 类型 | 命名规则 | 示例 |
| :--- | :--- | :--- |
| 主键/外键 | 主键 `id`，外键使用单数表语义 `_id` | `user_id`、`order_id` |
| 公开编号 | 业务主体 `_no` | `store_no`、`refund_no` |
| 布尔 | `is_` / `has_` / `can_` 开头 | `is_default`、`has_read` |
| 时间点 | 过去分词 + `_at` | `paid_at`、`expires_at` |
| 日期 | `_date` | `birth_date` |
| 数量 | `_count` 或 `_quantity` | `retry_count`、`reserved_quantity` |
| 金额 | 语义 + `_amount` | `payable_amount`（单位由表注释固定为分） |
| 状态/类型 | `_status` / `_type` | `order_status`、`sender_type` |
| 哈希/密文 | `_hash` / `_ciphertext` | `token_hash`、`phone_ciphertext` |
| JSON 快照 | `_snapshot` / `_config` / `_payload` | `price_snapshot`、`rule_config` |
| 乐观锁 | `version` | `version` |

禁止 `data`、`info`、`value`、`flag1` 等无语义命名。单位容易混淆时必须写入字段名，如 `weight_grams`、`size_bytes`、`timeout_seconds`。

#### 3.6.4 金额字段规范

- 交易金额统一使用 `BIGINT UNSIGNED`，存储 ISO 4217 货币最小单位；本项目人民币默认为“分”。
- 可为负数的调整/差额使用 `BIGINT SIGNED`，不允许用浮点数保存或计算金额。
- 每个金额聚合根必须有 `currency CHAR(3)`；同一订单/支付/退款内不混用币种。
- 首版商品价格采用含税价，不单列税额；订单实付不得小于 0：`payable_amount = goods_amount + freight_amount + adjustment_amount`。若未来支持独立税额，必须先新增 `tax_amount` 及其分摊、退款和发票规则，再升级金额公式与 API Schema。
- 订单项必须保存下单时单价、数量、原价、订单级改价分摊和实付快照，历史订单不回读当前 SKU 价格。订单级 `adjustment_amount` 必须按统一分摊算法落到订单项，退款上限只使用分摊后的订单项实付，不允许直接按改价前金额退款。
- 跨行分摊采用明确舍入策略（默认最大余数法），尾差分配结果入快照，保证各行之和精确等于订单金额。
- 接收第三方金额时先验证币种和小数位，使用 `Decimal` 转换为最小单位，不允许通过 `float` 转换。

#### 3.6.5 时间字段规范

- 时间点统一使用 `DATETIME(6)` 并以 UTC 写入；API 使用带 `Z`/时区偏移的 RFC 3339 字符串。
- 禁止使用无时区语义的客户端时间作为服务器业务事实；`created_at` 等由服务端/数据库生成。
- `created_at` 默认 `CURRENT_TIMESTAMP(6)`；`updated_at` 通过 ORM 和数据库约定一致维护，不依赖未审核的触发器。
- 到期时间使用 `expires_at`，处理条件为 `expires_at <= db_now`；批处理需使用同一数据库时钟。
- 纯日期使用 `DATE`，持续时长使用带单位整数，不把时长伪装成时间点。
- 计时和性能指标使用单调时钟，不存入业务时间列。

#### 3.6.6 状态字段规范

- 状态、类型和来源统一使用 `VARCHAR(32)`/`VARCHAR(64)` ASCII 码，不使用 MySQL `ENUM` 或用数字猜测语义。
- 代码中使用 `StrEnum`，数据库可对稳定核心状态增加 `CHECK`；新增值须先完成兼容性迁移。
- 状态改变只能由 Domain 状态机执行，同一事务追加 `*_status_logs`/`*_events`。
- 主表保存当前状态以便查询，流水表保存迁移原因、执行者、前后状态、请求/Trace ID 和发生时间。
- 不使用一个含义模糊的 `status` 代替多个正交状态；订单可分别保存 `order_status`、`payment_status`、`fulfillment_status` 和 `after_sale_status`。

#### 3.6.7 JSON 字段使用规范

JSON 适用于下单快照、外部渠道脱敏报文、低频可扩展配置和已版本化的事件 Payload。不适用于需高频过滤/关联/唯一约束的主业务字段。

每个 JSON 列必须：

- 有 Pydantic Schema 和 `schema_version`，对输入进行白名单校验；
- 设置大小/层级/数组长度上限，禁止任意报文无界增长；
- 明确敏感等级，禁止保存密码、Token、支付密钥及未脱敏回调头；
- 如一个 JSON 路径成为常用查询条件，将其提升为普通列或受控生成列并建索引；
- 更改 Schema 使用双读/兼容读，不在一次上线中使旧数据立即不可读。

#### 3.6.8 逻辑删除规范

- 需用户恢复/审计的可变主数据使用 `deleted_at DATETIME(6) NULL`，必要时增加 `deleted_by` 和 `delete_reason`。
- 金额流水、状态事件、审计日志不做普通逻辑删除，采用追加更正、匿名化或归档。
- Repository 默认排除 `deleted_at IS NOT NULL`，必须使用显式管理员方法读取已删除数据。
- 唯一约束与逻辑删除不得只依赖 `(business_key, deleted_at)`，因为 MySQL 唯一索引允许多个 `NULL`。需“仅活跃唯一”时使用受控生成列：

```sql
active_unique_key VARCHAR(255)
  GENERATED ALWAYS AS (
    CASE WHEN deleted_at IS NULL THEN normalized_business_key ELSE NULL END
  ) STORED,
UNIQUE KEY uk_xxx_active (active_unique_key)
```

- 高风险删除先改变可见性/状态，再交由 Worker 按 3.5.10 执行派生数据清理。

#### 3.6.9 乐观锁与版本字段

- 可被并发修改的聚合根使用 `version BIGINT UNSIGNED NOT NULL DEFAULT 0`。
- 更新语句必须包含 `WHERE id = :id AND version = :expected_version`，同时 `version = version + 1`；影响行数为 0 返回业务冲突，不静默覆盖。
- HTTP 可将版本映射为 ETag/`If-Match`；前端冲突后重新获取，不盲目重放管理员编辑。
- 库存等计数型资源可使用原子条件更新，但仍记录版本/流水；追加型表无需乐观锁。

#### 3.6.10 唯一约束与普通索引

索引命名：主键 `pk_<table>`，唯一索引 `uk_<table>_<columns>`，普通索引 `idx_<table>_<columns>`。名称过长时保留业务语义而非机械截断。

- 唯一性尽量由数据库约束保证，不使用“先查后写”代替唯一索引。
- 联合索引按“等值条件 → 范围/排序 → 覆盖列”设计，验证最左前缀。
- 所有高频外键/关联列主动建索引；不为低选择性状态列单独建索引，应结合租户/主体和时间。
- 列表查询使用稳定排序如 `(created_at DESC, id DESC)` 和游标分页，索引与之对应；避免大偏移 `OFFSET`。
- 上线前用真实数据分布执行 `EXPLAIN ANALYZE`；冗余索引会增加写放大，不“为每列都加索引”。
- 字符串索引长度和排序规则必须显式评估；哈希建议以 `BINARY(32)` 存 SHA-256，而不是 64 字符十六进制字符串。

#### 3.6.11 外键使用原则

- 同库、中低写入量、关系稳定的主数据默认使用外键，如地址→用户、SKU→商品。
- 库存/消息/审计/事件等高写入或将来可能分区的表，可经 ADR 决定不建物理外键，但必须保留索引、应用校验和孤儿对账任务。
- 禁止跨数据库外键，不对 PostgreSQL/Redis/对象存储引用伪造外键。
- 交易、支付、退款、消息和审计不使用 `ON DELETE CASCADE`；默认 `RESTRICT/NO ACTION`。仅对无独立保留价值的纯关联表考虑级联删除。
- 正向迁移添加外键前必须先清理孤儿数据，评估锁表时间。

#### 3.6.12 表分区与历史归档

- 项目初期不预先分区所有表；只在有真实容量、保留和查询证据时引入。
- 候选表为 `messages`、`*_logs`、`*_events`、`payment_callbacks`、`outbox_events`，按 `created_at` 月/季度 RANGE 分区。
- MySQL 分区表的每个唯一键必须包含分区键；如果这会破坏 `event_id` 等全局唯一性，优先使用归档表/归档库，不强行分区。
- 分区必须预创建并监控，保留 `MAXVALUE` 安全分区，禁止因缺失未来分区导致写入失败。
- 归档以不可变时间窗批处理：标记范围→复制→行数/哈希/金额对账→切换查询→分批删除在线副本。
- 归档数据仍可被 Query Service 查询，不得在业务代码中临时拼接归档表。

#### 3.6.13 敏感字段加密与脱敏

| 数据 | 存储方式 | 查询/展示 |
| :--- | :--- | :--- |
| 密码 | Argon2id 带独立 Salt 哈希 | 永不可回显，不可解密 |
| Refresh Token/验证码 | SHA-256/HMAC 等不可逆验证值 | 只比较哈希，日志不记录原文 |
| 手机/邮箱 | 应用层信封加密 + 归一化 HMAC 查询列 | 默认脱敏，仅授权场景解密 |
| 地址/姓名/物流单号 | AES-256-GCM 或 KMS 托管信封加密 | 展示最小必要字段，记录访问审计 |
| 签名/内容哈希 | `BINARY(32/64)` | 常量时间比较 |
| 第三方密钥 | Secret Manager/KMS | 不写入 MySQL 业务表 |

密文必须保存 `key_version`/加密元数据，支持密钥轮换；AAD 绑定表、字段和业务主体，防止密文跨行替换。脱敏在 Schema/序列化层统一完成，不将 ORM 对象直接返回 API。L2/L3 数据禁止进入 SQL 日志、异常栈、Trace 属性或 Agent Prompt。

#### 3.6.14 数据库事务边界

- 一个 Application Service 用例对应一个 MySQL Unit of Work；事务在 Service 层开启/提交，Router、Agent 和 Repository 不自行提交。
- 事务内只包含必要读写、状态校验、流水和 Outbox；不在持锁期间调用支付、物流、模型、MCP 或对象存储。
- 默认隔离级别使用 InnoDB `READ COMMITTED`，降低范围锁/间隙锁风险；特定业务若需更强语义，使用显式行锁、唯一约束或乐观锁，不随意提高全局隔离级别。
- 支付下单使用“本地意图事务 → 事务外渠道调用 → 新本地事务更新”，以幂等键和回调对账闭环。
- 创建订单的本地事务同时消费 Checkout、预占库存、写订单/快照/流水和 Outbox；这些表均在同一 MySQL 实例和 Unit of Work 内。价格与运费计算在加锁前完成，事务内按 SKU、配送模板和计价版本复核结果，避免长时间持锁。
- MySQL 与 PostgreSQL/Redis 不共享事务，按 3.5.8/3.5.9 使用 Outbox 和幂等消费。
- 事务失败后 ORM Session 必须回滚并丢弃脏状态；不在未知提交结果上盲目重试非幂等写操作。

#### 3.6.15 并发锁与死锁处理

并发控制选择顺序：唯一约束/原子条件更新 → 乐观锁 → 必要的 `SELECT ... FOR UPDATE`。分布式锁不替代 MySQL 交易约束。

- 库存使用单条原子条件更新，影响行数为 0 即库存不足；不先读取数量再无条件更新。
- 批量锁定按稳定顺序（如 `sku_id ASC`、`order_id ASC`），所有代码路径使用同一顺序。
- 工作队列领取使用小批量 `FOR UPDATE SKIP LOCKED`，事务内只完成领取，耗时任务在事务外执行。
- 事务设置合理的锁等待/语句超时；慢 SQL 与大批量修改不与在线高并发事务共用路径。
- 仅对 MySQL 死锁或锁等待超时进行有上限、指数退避加抖动的整个事务重试；前提是请求已有幂等键。
- 监控死锁次数、锁等待时间和受影响用例，保留脱敏的死锁图；重复死锁必须修复锁顺序/索引/事务边界，不以无限重试掩盖。

#### 3.6.16 数据库迁移与回滚

所有 DDL 和受控数据变更通过 Alembic 进入代码库，生产环境禁止手工修表。每个 Revision 单一目的、可审查，明确锁表风险和预估耗时。

在线变更采用 Expand–Migrate–Contract：

```text
1. Expand：先添加可空列/新表/新索引，旧代码仍可运行
2. Deploy：上线兼容新旧 Schema 的代码，必要时双写
3. Migrate：Worker 分批、限速、可断点地回填和对账
4. Switch：切换读取新列，观察指标并停止双写
5. Contract：跨过至少一个安全发布窗口后再删旧列/约束
```

对大表加索引、改列类型、添加非空列前必须在近似数据量环境演练并选择在线 DDL 能力。不将“执行 downgrade”当作唯一回滚：破坏性 DDL/数据转换优先通过应用版本回退、前向修复迁移和备份恢复。每次发布前必须验证备份可恢复、Revision 单头、迁移可重入检查和新旧应用兼容窗口。

---

### 3.7 MySQL 业务数据表设计

本节给出逻辑数据字典，作为 SQLAlchemy Model 和 Alembic DDL 的输入。“唯一”、“外键”、“检查约束”不是文档建议，而是建表时应实现的数据不变式；仅有 3.6.11 所述高写入场景可经 ADR 取消物理外键。

#### 通用字段模板

为避免下文重复，普通可变实体默认包含：

| 字段 | 类型 | 约束/含义 |
| :--- | :--- | :--- |
| `id` | `BIGINT UNSIGNED` | PK，自增，仅内部使用 |
| `created_at` | `DATETIME(6)` | NOT NULL，UTC，创建时间 |
| `updated_at` | `DATETIME(6)` | NOT NULL，UTC，最后修改时间 |
| `version` | `BIGINT UNSIGNED` | NOT NULL DEFAULT 0，乐观锁 |

可逻辑删除主数据另含 `deleted_at DATETIME(6) NULL`。追加型流水/事件表默认只含 `id` 和 `created_at`，不含 `updated_at/version/deleted_at`。MySQL 中带资源前缀的公开 ULID 统一为 `VARCHAR(40)`、ASCII Character Set/Binary Collation；跨库引用可使用 `VARCHAR(64)`，但值不变。币种为 `CHAR(3)`，金额单位为分，时间为 UTC。下文的 FK 列均为 `BIGINT UNSIGNED`。表格中如写 `paid_at / closed_at`，表示两个独立列；类型中只写一个类型时表示这些列同类型，以 `/` 分隔多个类型时则与字段按顺序一一对应。

#### 表间主链路

```text
users → carts → checkout_sessions → trade_orders ─┬→ payments
                 ↑ pricing/shipping snapshots     └→ orders ─┬→ order_items → reviews
                                                               ├→ shipments → shipment_items → order_items
                                                               └→ refund_applications → refund_items → order_items
                                                                                         └→ refund_appeals

users → conversations → messages → human_service_tickets
AI Agent/Skill/Tool 配置 → 工具授权/确认/审计
业务聚合修改 → outbox_events → Worker/派生存储
```

Payment 只归属 Trade Order 支付尝试，不向 `orders/order_items` 增加虚假外键；通过 `trade_orders → orders` 映射一次跨店支付所覆盖的店铺订单。Shipment 与 Refund 通过各自 Item 关联订单项，Review 直接绑定订单项；图中箭头只表达业务聚合导航，真实 FK 以各表字段为准。

#### 3.7.1 用户与认证表

##### 3.7.1.1 users 用户表

保存用户主体和可展示资料，不保存密码或 Token。采用通用可变实体字段。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `user_no` | `VARCHAR(40)` | NOT NULL，UK，对外用户 ID |
| `username` | `VARCHAR(64)` | NOT NULL，展示/登录账号 |
| `username_normalized` | `VARCHAR(64)` | NOT NULL，大小写/全半角归一化，UK |
| `nickname` | `VARCHAR(64)` | NOT NULL，用户昵称；API/Service 按 Unicode 字符实施 2～20 字符、去除首尾空白及内容安全校验，数据库保留较宽容量用于兼容演进 |
| `avatar_object_key` | `VARCHAR(512)` | NULL，头像对象引用 |
| `user_status` | `VARCHAR(32)` | NOT NULL：`active/suspended/pending_close/closed`；业务冻结使用 `suspended`，不与凭证尝试锁定混用 |
| `status_reason_code` | `VARCHAR(64)` | NULL，当前业务状态的安全原因码投影 |
| `status_expires_at` | `DATETIME(6)` | NULL，定时冻结到期时间；永久冻结为 NULL |
| `current_status_record_id` | `BIGINT UNSIGNED` | NULL，FK `user_status_records.id`，当前状态变更依据 |
| `locale` | `VARCHAR(16)` | NOT NULL DEFAULT `zh-CN` |
| `timezone` | `VARCHAR(64)` | NOT NULL DEFAULT `Asia/Shanghai` |
| `permission_version` | `BIGINT UNSIGNED` | NOT NULL DEFAULT 1，角色/权限/数据范围变更时递增 |
| `registered_at` | `DATETIME(6)` | NOT NULL |
| `last_login_at` | `DATETIME(6)` | NULL |
| `closed_at` | `DATETIME(6)` | NULL，注销完成时间 |

索引：`uk_users_user_no`、`uk_users_username_normalized`、`idx_users_status_created_at(user_status, created_at, id)`、`idx_users_status_expiry(user_status, status_expires_at, id)`。注销后用户号不复用，用户名是否可释放由产品策略决定并需防止冒充。`user_credentials.locked_until` 只表示单凭证的防爆破尝试锁定；`users.user_status=suspended` 表示账号业务冻结，在所有登录、Refresh、API 和实时连接入口统一拒绝。

##### 3.7.1.2 user_credentials 用户凭证表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `user_id` | `BIGINT UNSIGNED` | FK `users.id`，NOT NULL |
| `credential_type` | `VARCHAR(32)` | `password/phone/email/oauth` |
| `identifier_ciphertext` | `VARBINARY(512)` | NULL，手机/邮箱/外部标识密文 |
| `identifier_hash` | `BINARY(32)` | NULL，归一化 HMAC，用于精确查找 |
| `secret_hash` | `VARBINARY(512)` | NULL，Argon2id 密码哈希或 OAuth 关联验证值 |
| `algorithm` | `VARCHAR(32)` | NULL，如 `argon2id` |
| `key_version` | `SMALLINT UNSIGNED` | NULL，密文密钥版本 |
| `is_primary` | `BOOLEAN` | NOT NULL DEFAULT FALSE |
| `is_verified` | `BOOLEAN` | NOT NULL DEFAULT FALSE |
| `failed_attempts` | `SMALLINT UNSIGNED` | NOT NULL DEFAULT 0 |
| `locked_until` | `DATETIME(6)` | NULL |
| `credential_version` | `BIGINT UNSIGNED` | NOT NULL DEFAULT 1，密码/关键凭证变更时递增 |
| `must_change_password` | `BOOLEAN` | NOT NULL DEFAULT FALSE，仅密码凭证使用；管理员只能置为 TRUE，用户完成安全重置后清除 |
| `verified_at` / `password_changed_at` | `DATETIME(6)` | NULL |
| `credential_status` | `VARCHAR(32)` | `active/disabled/revoked` |

约束：密码凭证 `UNIQUE(user_id, credential_type)`；可登录标识通过活跃生成列约束 `UNIQUE(credential_type, active_identifier_hash)`。禁止在任何列保存明文密码。

##### 3.7.1.3 auth_sessions 登录会话表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `session_no` | `VARCHAR(40)` | UK，对外 Session ID |
| `user_id` | `BIGINT UNSIGNED` | FK `users.id` |
| `refresh_token_hash` | `BINARY(32)` | NOT NULL，UK，只存哈希 |
| `token_family_no` | `VARCHAR(40)` | NOT NULL，轮换链 ID |
| `parent_session_id` | `BIGINT UNSIGNED` | NULL，上一次轮换 Session |
| `device_no` | `VARCHAR(128)` | NULL，服务端生成/经验证的设备标识 |
| `client_type` | `VARCHAR(32)` | `web/mobile/admin` |
| `ip_ciphertext` | `VARBINARY(256)` | NULL，IP 密文 |
| `user_agent_hash` | `BINARY(32)` | NULL |
| `authenticated_at` | `DATETIME(6)` | NOT NULL，本次主认证完成时间；近期认证成功时受控更新 |
| `authentication_methods` | `JSON` | NOT NULL，如 `password+webauthn`；Schema 固定且不保存 Secret |
| `assurance_level` | `VARCHAR(16)` | `aal1/aal2/aal3`，管理端至少达到发布策略要求的等级 |
| `issued_at` / `expires_at` | `DATETIME(6)` | NOT NULL |
| `last_seen_at` | `DATETIME(6)` | NOT NULL |
| `revoked_at` | `DATETIME(6)` | NULL |
| `revoke_reason` | `VARCHAR(64)` | NULL |

索引：`uk_auth_sessions_session_no`、`uk_auth_sessions_refresh_token_hash`、`idx_auth_sessions_user_expires(user_id, expires_at)`、`idx_auth_sessions_family(token_family_no)`。Refresh Token 重放时撤销整个 Token Family。Redis 只缓存 Session 状态，MySQL 保留撤销和安全审计事实。

##### 3.7.1.4 verification_codes 验证码记录表

短生命周期安全状态记录；创建后只允许原子增加尝试次数或填入消费/失效时间，不作普通编辑。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `verification_no` | `VARCHAR(40)` | UK |
| `user_id` | `BIGINT UNSIGNED` | NULL，未注册验证时可空 |
| `purpose` | `VARCHAR(32)` | `register/login/reset_password/change_phone/change_email` 等；必须与后续动作和目标类型绑定 |
| `target_type` | `VARCHAR(16)` | `phone/email` |
| `target_hash` | `BINARY(32)` | NOT NULL，归一化 HMAC |
| `code_hash` | `BINARY(32)` | NOT NULL，带服务端 Pepper |
| `attempt_count` / `max_attempts` | `SMALLINT UNSIGNED` | NOT NULL |
| `send_channel` | `VARCHAR(32)` | `sms/email` |
| `delivery_status` | `VARCHAR(16)` | `accepted/sent/failed/suppressed`；匿名响应不能据此泄露目标状态 |
| `provider_reference_hash` | `BINARY(32)` | NULL，渠道消息引用的 HMAC，不保存可直接查询的原始回执号 |
| `expires_at` | `DATETIME(6)` | NOT NULL |
| `sent_at` / `last_attempt_at` | `DATETIME(6)` | NULL，发送完成与最近校验尝试时间 |
| `consumed_at` / `invalidated_at` / `superseded_at` | `DATETIME(6)` | NULL，修改目标/重发后旧记录可显式失效 |
| `request_ip_hash` | `BINARY(32)` | NULL |

索引：`idx_verification_target_purpose(target_hash, purpose, created_at)`、`idx_verification_expires(expires_at)`。验证码只能原子消费一次；`verification_no` 是 API 返回的 `verification_id`，后续请求必须同时匹配该 ID、Purpose、Target Type、规范化 Target Hash 和验证码。发送频率主要在 Redis 限制，MySQL 用于最终审计与防重放；Redis 丢失不能让已消费、已失效或已超过最大尝试次数的验证码重新有效。

##### 3.7.1.5 password_reset_records 密码重置记录表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `reset_no` | `VARCHAR(40)` | UK |
| `user_id` | `BIGINT UNSIGNED` | FK `users.id` |
| `verification_id` | `BIGINT UNSIGNED` | FK `verification_codes.id` |
| `reset_token_hash` | `BINARY(32)` | NOT NULL，UK |
| `credential_version_before` | `BIGINT UNSIGNED` | NOT NULL，防止旧流程覆盖新密码 |
| `expires_at` | `DATETIME(6)` | NOT NULL |
| `consumed_at` / `invalidated_at` | `DATETIME(6)` | NULL |
| `request_ip_hash` | `BINARY(32)` | NULL |

成功重置在同一事务中更新凭证、消费记录并撤销所有旧 Session。

##### 3.7.1.6 user_addresses 收货地址表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `address_no` | `VARCHAR(40)` | UK |
| `user_id` | `BIGINT UNSIGNED` | FK `users.id` |
| `recipient_name_ciphertext` | `VARBINARY(512)` | NOT NULL |
| `phone_ciphertext` | `VARBINARY(512)` | NOT NULL |
| `phone_last4` | `CHAR(4)` | NOT NULL，仅脱敏展示 |
| `country_code` | `CHAR(2)` | NOT NULL DEFAULT `CN` |
| `province_code` / `city_code` / `district_code` | `VARCHAR(32)` | NOT NULL，标准行政区划码 |
| `address_ciphertext` | `VARBINARY(2048)` | NOT NULL，街道和详细地址密文 |
| `postal_code` | `VARCHAR(16)` | NULL |
| `label` | `VARCHAR(32)` | NULL，如“家” |
| `is_default` | `BOOLEAN` | NOT NULL DEFAULT FALSE |
| `key_version` | `SMALLINT UNSIGNED` | NOT NULL |

索引：`idx_user_addresses_user(user_id, deleted_at, id)`。默认地址通过生成列保证每个用户最多一条活跃默认地址，切换默认值必须在事务中完成。User/Address Domain 固定 `MAX_ACTIVE_ADDRESSES_PER_USER=20`；创建时锁定当前用户地址聚合并在事务内计数 `deleted_at IS NULL`，超限返回 409 `ADDRESS_LIMIT_REACHED`。该常量同步生成 OpenAPI 与前端提示，但客户端计数不代替服务端并发约束。订单使用 3.7.7.4 地址快照，后续修改本表不影响历史订单。

##### 3.7.1.7 credential_change_records 联系方式换绑记录表

保存手机号/邮箱换绑的短时安全 Ticket 和完整审计，不把验证码本身写入本表。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `change_no` | `VARCHAR(40)` | UK，对外换绑 Ticket ID |
| `user_id` | `BIGINT UNSIGNED` | FK |
| `credential_type` | `VARCHAR(16)` | `phone/email` |
| `old_credential_id` | `BIGINT UNSIGNED` | NULL，FK；首次绑定时可空 |
| `credential_version_before` | `BIGINT UNSIGNED` | NOT NULL，防止旧 Ticket 覆盖新凭证 |
| `new_identifier_ciphertext` | `VARBINARY(512)` | NULL，提交新目标后写入 |
| `new_identifier_hash` | `BINARY(32)` | NULL，归一化 HMAC，用于唯一性校验 |
| `old_verification_id` / `new_verification_id` | `BIGINT UNSIGNED` | NULL，FK `verification_codes.id` |
| `change_status` | `VARCHAR(16)` | `pending/target_bound/confirmed/expired/cancelled` |
| `expires_at` / `confirmed_at` / `cancelled_at` | `DATETIME(6)` | NOT NULL / NULL / NULL |
| `request_ip_hash` | `BINARY(32)` | NULL |

确认换绑时锁定记录并核对 `credential_version_before`、新目标验证码用途和目标 Hash，在一个事务内停用旧凭证、写入并验证新凭证、消费 Ticket、递增凭证版本并写安全审计。约束 `UNIQUE(change_no)`；索引 `idx_credential_changes_user_status(user_id, change_status, expires_at)`。过期、已消费或目标已被其他账号占用的 Ticket 不得重放。

##### 3.7.1.8 user_status_records 用户状态变更记录表

追加型权威历史，用于冻结、解冻、注销申请和关闭；`users` 只保存当前投影。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `status_record_no` | `VARCHAR(40)` | UK，对外状态变更 ID |
| `user_id` | `BIGINT UNSIGNED` | FK `users.id` |
| `from_status` / `to_status` | `VARCHAR(32)` | NOT NULL，必须命中允许的状态迁移 |
| `reason_code` / `reason` | `VARCHAR(64)` / `VARCHAR(1000)` | NOT NULL，稳定原因码与脱敏说明 |
| `effective_at` / `expires_at` | `DATETIME(6)` | NOT NULL / NULL，定时冻结时设到期时间 |
| `actor_type` / `actor_user_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | `user/admin/system` / NULL |
| `scope_type` / `scope_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | NULL，管理命令的 Scope 快照 |
| `expected_user_version` / `result_user_version` | `BIGINT UNSIGNED` | NOT NULL，并发与审计依据 |
| `idempotency_key` | `VARCHAR(128)` | NOT NULL |
| `idempotency_scope_key` | `BINARY(32) GENERATED` | 由 `actor_type + COALESCE(actor_user_id,0) + user_id + idempotency_key` 的规范编码生成 |
| `request_id` / `trace_id` | `VARCHAR(64)` | NOT NULL |

约束：`UNIQUE(status_record_no)`、`UNIQUE(idempotency_scope_key)`；索引 `idx_user_status_records_user(user_id, created_at, id)`、`idx_user_status_records_expiry(to_status, expires_at, id)`。不直接对可空 `actor_user_id` 建幂等唯一键，避免 MySQL 的多个 NULL 绕过系统任务去重。状态记录、`users` 投影、Session 撤销、管理员操作日志和 Outbox 在同一 MySQL 事务内写入。定时冻结到期由 Scheduler 投递候选，Worker 重新校验当前记录、版本和无新冻结后幂等恢复 `active`；人工提前解冻会使旧定时任务失效。

##### 3.7.1.9 user_agreement_acceptances 用户协议接受记录表

追加型事实表，保存用户在注册或后续重新告知时主动接受的协议版本。它不同于可撤销的 AI Consent：用户协议/隐私政策接受用于证明当时告知和接受事实，不能以修改原记录模拟撤销或版本更新。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `acceptance_no` | `VARCHAR(40)` | UK，对外审计引用 ID |
| `user_id` | `BIGINT UNSIGNED` | FK `users.id`，NOT NULL |
| `document_type` | `VARCHAR(32)` | `terms_of_service/privacy_policy`；首版注册必须各有一条 |
| `content_entry_id` / `content_version_id` | `BIGINT UNSIGNED` | FK `platform_content_entries/versions`，绑定当时 Published 不可变版本 |
| `document_version` | `VARCHAR(40)` | NOT NULL，面向 API 的稳定版本标识快照 |
| `content_hash` | `BINARY(32)` | NOT NULL，接受时安全正文 Hash 快照 |
| `acceptance_context` | `VARCHAR(32)` | `registration/reauth_policy_update` |
| `accepted_at` | `DATETIME(6)` | NOT NULL，服务端时间 |
| `locale` / `region_code` | `VARCHAR(16)` | NOT NULL，展示版本的语言和适用地区 |
| `ip_hash` / `user_agent_hash` | `BINARY(32)` | NULL，使用独立审计 HMAC，不保存原值 |
| `request_id` / `trace_id` | `VARCHAR(64)` | NOT NULL |

约束：`UNIQUE(user_id, document_type, content_version_id, acceptance_context)`；索引 `idx_agreement_acceptances_user_time(user_id, accepted_at, id)`、`idx_agreement_acceptances_version(content_version_id, accepted_at, id)`。注册事务必须校验提交版本仍是当前适用的 Published Version，并与用户、凭证、默认角色、专属会话、Session 和 Outbox 同事务写入接受记录。协议更新生成新 Content Version 和新接受记录，历史行不可修改或删除；账号注销后的保留/匿名化按法律与隐私政策执行。

##### 3.7.1.10 auth_attempts 认证尝试安全记录表

追加型、短期留存的安全事件明细，用于撞库检测、限流复核和登录可观测性；它不是业务登录历史页面，也不能存储密码、验证码、Token、完整账号或挑战答案。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `attempt_no` | `VARCHAR(40)` | UK |
| `attempt_type` | `VARCHAR(24)` | `password_login/code_login/registration/code_send/code_verify/token_refresh` |
| `auth_method` | `VARCHAR(24)` | `password/verification_code/refresh_token` 等 |
| `user_id` | `BIGINT UNSIGNED` | NULL，只有安全识别到主体时关联 |
| `identifier_monitoring_hash` | `BINARY(32)` | NULL，独立且可轮换的监控 HMAC，不能替代凭证查找 Hash |
| `session_id` | `BIGINT UNSIGNED` | NULL，成功创建/轮换的 Session |
| `result` | `VARCHAR(24)` | `succeeded/invalid/blocked/rate_limited/challenge_required/system_error` |
| `reason_code` | `VARCHAR(64)` | NOT NULL，低基数内部安全原因；匿名响应另做安全映射 |
| `risk_level` / `risk_policy_version` | `VARCHAR(16)` / `VARCHAR(40)` | NOT NULL，风险决定快照 |
| `challenge_type` | `VARCHAR(32)` | NULL，只记录类型，不记录答案/Token |
| `ip_hash` / `device_hash` / `user_agent_hash` | `BINARY(32)` | NULL，轮换 HMAC |
| `occurred_at` | `DATETIME(6)` | NOT NULL |
| `request_id` / `trace_id` | `VARCHAR(64)` | NOT NULL |

索引：`idx_auth_attempts_identifier_time(identifier_monitoring_hash, occurred_at, id)`、`idx_auth_attempts_ip_time(ip_hash, occurred_at, id)`、`idx_auth_attempts_user_time(user_id, occurred_at, id)`、`idx_auth_attempts_result_time(result, occurred_at, id)`。认证成功、Refresh 重放和高风险阻断必须与认证事务同步落表，或在同一事务写入不可丢失的安全 Outbox；写入失败时按风险 Fail Closed。只有允许短暂延迟的低风险失败明细可先写可靠安全事件流再幂等批量落库。明细按安全政策短期保留并聚合后删除，不提供普通管理员按账号任意浏览。

#### 3.7.2 RBAC 权限表

RBAC 同时支持平台级和店铺级作用域。归属关系不等于授权：访问店铺资源时同时检查 Permission 和 `scope_type/scope_id`。

##### 3.7.2.1 roles 角色表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `role_no` | `VARCHAR(40)` | UK |
| `role_code` | `VARCHAR(64)` | NOT NULL，如 `platform_admin/store_operator` |
| `role_name` | `VARCHAR(64)` | NOT NULL |
| `scope_type` | `VARCHAR(16)` | `platform/store` |
| `role_type` | `VARCHAR(16)` | `system/custom` |
| `description` | `VARCHAR(500)` | NULL |
| `role_status` | `VARCHAR(16)` | `active/disabled` |

约束：`UNIQUE(scope_type, role_code)`。System Role 不可删除或改变其基本语义。`store` Role 是平台维护的可复用权限模板，具体租户边界只存在于 `user_roles.scope_id`；因此 Role Definition 的创建、修改和 Permission 替换仅接受 Platform Scope，店铺范围的 `rbac:manage` 只能在自身 Store Scope 内授予、撤销已有 Store Role，避免某店修改共享模板后影响其他店铺。

##### 3.7.2.2 permissions 权限表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `permission_code` | `VARCHAR(128)` | UK，如 `refunds:review` |
| `resource` | `VARCHAR(64)` | `orders/products/users/ai_tools` 等，与权限码中稳定复数资源名一致 |
| `action` | `VARCHAR(64)` | `read/create/update/approve` 等 |
| `risk_level` | `VARCHAR(16)` | `low/medium/high/critical` |
| `description` | `VARCHAR(500)` | NOT NULL |
| `permission_status` | `VARCHAR(16)` | `active/deprecated` |

权限码统一采用 `resource:action`，并满足正则 `^[a-z][a-z0-9_]*:[a-z][a-z0-9_]*$`，例如 `orders:read`、`refunds:review`、`support:internal_notes_read`。`resource` 使用权限注册表中的稳定复数资源名，`action` 使用单一动作或下划线连接的原子动作；禁止点号格式、斜杠合并、通配符和把 `read/manage` 写成一个权限码。一个 API Operation 可要求多个权限，但每个 `permission_code` 只表达一个可审计动作。权限码一旦发布不重用；废弃使用 `deprecated`，不直接改为新语义。退款的批准、拒绝和补件统一是 `refunds:review` 下的 Decision Enum，不产生第二个权限码。

Permission 的唯一机器可读来源是 `docs/permission_registry.yaml`，由它 Seed `permissions` 表，并生成后端常量、Route Meta 类型、管理菜单、审批策略和本节摘要。每条必须包含 `code、resource、action、risk_level、allowed_scope_types、delegation_policy、requires_mfa、requires_recent_auth、approval_policy、owner、status`；任何 Operation、Vue Route 或 Role Seed 引用未注册/已废弃权限时 CI 失败。首版资源组和约束如下：

| 资源组 | 首版 Permission | Scope | 关键风险控制 |
| :--- | :--- | :--- | :--- |
| 概览 | `dashboard:read` | `platform/store` | 指标按 Scope 聚合 |
| 用户 | `users:read、users:manage、users:read_sensitive、users:sessions_revoke、users:force_password_reset` | `platform` | `manage/read_sensitive` 高风险，MFA+近期认证；默认不可委派 |
| RBAC | `rbac:read、rbac:manage` | `platform/store` | `manage` 要求 MFA、SoD，默认不可委派 |
| 店铺/政策 | `stores:read、stores:review、stores:manage、store_policies:read、store_policies:create、store_policies:update、store_policies:publish` | `platform/store` | 发布/审核需近期认证，必要时双人复核 |
| 商品/分类 | `products:read、products:create、products:update、products:review、products:publish、catalog_taxonomy:manage` | `platform/store` | 店铺数据隔离，发布留痕 |
| 库存 | `inventories:read、inventories:adjust` | `platform/store` | 调整需原因、数量边界、ETag 和审计 |
| 订单/支付 | `orders:read、orders:adjust、orders:cancel、payments:read、payments:reconcile` | `platform/store` | 金额/状态/SoD；对账不伪造渠道结果 |
| 履约 | `shipments:read、shipments:create、shipments:correct、shipments:void、shipments:refresh` | `platform/store` | 只在合法履约状态下执行 |
| 退款/申诉 | `refunds:read、refunds:review、refund_appeals:read、refund_appeals:review` | `platform/store` | 金额阈值、政策快照和复核；审核结果为 Decision Enum |
| 评价 | `reviews:read、reviews:reply、reviews:moderate` | `platform/store` | 屏蔽/恢复要求规则码和历史保留 |
| 人工客服 | `support:queue_read、support:claim、support:reply、support:wait、support:resume、support:transfer、support:resolve、support:internal_notes_read、support:internal_notes_write` | `platform/store/queue` | Assignment+Queue Scope，用户可见与内部备注隔离 |
| AI 配置 | `ai_agents:read、ai_agents:manage、ai_agents:publish、ai_skills:read、ai_skills:manage、ai_skills:publish、ai_tools:read、ai_tools:manage、ai_tools:publish、ai_policies:read、ai_policies:manage、ai_policies:publish、ai_runtime:kill` | `platform` | 版本不可变；发布/终止需近期认证与审计 |
| 知识/评估 | `knowledge:read、knowledge:manage、knowledge:publish、ai_evaluations:read、ai_evaluations:run、ai_observability:read、observability:read` | `platform/store` | 文档 Scope 和索引版本隔离，不提供 Dataset CRUD |
| 内容 | `content:read、content:manage、content:publish` | `platform` | 安全富文本、外链白名单、发布版本 |
| 审批/审计/任务 | `admin_approvals:read、admin_approvals:decide、audit:read、jobs:read、events:read、events:operate` | `platform/store` | 发起人不可自批；重放需 Schema/幂等/复核 |

##### 3.7.2.3 user_roles 用户角色关联表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `user_id` / `role_id` | `BIGINT UNSIGNED` | FK，NOT NULL |
| `grant_no` | `VARCHAR(40)` | UK，每次授权的不可复用公开 ID |
| `scope_type` | `VARCHAR(16)` | 必须与角色作用域一致 |
| `scope_id` | `BIGINT UNSIGNED` | 平台作用域为 0，店铺作用域为 `store_id` |
| `grant_status` | `VARCHAR(16)` | `active/revoked/expired`，只有 `active` 进入授权判定 |
| `active_grant_key` | `BINARY(32) GENERATED` | 仅 Active 时为 `user_id + role_id + scope` 的规范 Hash，其他状态为 NULL |
| `granted_by` | `BIGINT UNSIGNED` | FK `users.id` |
| `granted_at` / `expires_at` | `DATETIME(6)` | NOT NULL / NULL |
| `revoked_at` / `revoked_by` | `DATETIME(6)` / `BIGINT UNSIGNED` | NULL，过期不伪装成人工撤销 |
| `grant_reason` / `revoke_reason` | `VARCHAR(500)` | NOT NULL / NULL |

约束：`UNIQUE(grant_no)`、`UNIQUE(active_grant_key)`；索引 `idx_user_roles_scope(scope_type, scope_id, role_id, grant_status)`、`idx_user_roles_expiry(grant_status, expires_at, id)`。授权判定必须同时满足 `grant_status=active AND (expires_at IS NULL OR expires_at > db_now)`。再次授予已撤销/过期的同角色会新建 `grant_no`，不恢复或覆盖旧行；续期在同一事务锁定旧 Active Grant，将旧行置为 `revoked`（原因码为 `superseded`）并追加 `replaced` 事件，再创建新 Grant，确保任何时刻至多一条 Active 且保留完整操作人和时间语义。

##### 3.7.2.4 role_permissions 角色权限关联表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `role_id` / `permission_id` | `BIGINT UNSIGNED` | FK，复合主键/UK |
| `condition_config` | `JSON` | NULL，版本化额外限制，不替代店铺作用域 |
| `granted_by` | `BIGINT UNSIGNED` | FK `users.id` |
| `created_at` | `DATETIME(6)` | NOT NULL |

权限缓存 Key 包含角色版本；关联修改后通过 Outbox 失效缓存。

##### 3.7.2.5 admin_operation_logs 管理员操作日志表

追加型、不可普通更新/删除。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `operation_no` | `VARCHAR(40)` | UK |
| `operator_user_id` | `BIGINT UNSIGNED` | NOT NULL |
| `scope_type` / `scope_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | 操作作用域 |
| `permission_code` | `VARCHAR(128)` | NOT NULL，当时权限快照 |
| `action` | `VARCHAR(64)` | NOT NULL |
| `target_type` / `target_no` | `VARCHAR(64)` / `VARCHAR(64)` | NOT NULL |
| `before_snapshot` / `after_snapshot` | `JSON` | NULL，字段级脱敏且限大小 |
| `result_status` | `VARCHAR(16)` | `success/denied/failed` |
| `reason` | `VARCHAR(500)` | NULL，高风险操作必填 |
| `request_id` / `trace_id` | `VARCHAR(64)` | NOT NULL |
| `ip_hash` | `BINARY(32)` | NULL |

索引：`idx_admin_logs_operator_time(operator_user_id, created_at, id)`、`idx_admin_logs_target(target_type, target_no, created_at)`。

##### 3.7.2.6 admin_mfa_authenticators 管理员 MFA 认证器表

保存管理员第二因素的公开元数据和受保护凭证，不保存可直接使用的明文 TOTP Secret、恢复码或私钥。采用通用可变实体字段。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `authenticator_no` | `VARCHAR(40)` | UK，对外认证器 ID |
| `user_id` | `BIGINT UNSIGNED` | FK，必须具有有效管理身份 |
| `authenticator_type` | `VARCHAR(16)` | `webauthn/totp/recovery_codes` |
| `display_name` | `VARCHAR(128)` | NOT NULL，经安全过滤的设备名称 |
| `credential_id_hash` | `BINARY(32)` | NULL，WebAuthn Credential ID 的带密钥查找值 |
| `public_key` | `VARBINARY(2048)` | NULL，WebAuthn 公钥/COSE Key，不含私钥 |
| `sign_count` | `BIGINT UNSIGNED` | NULL，WebAuthn Counter |
| `secret_ciphertext` | `VARBINARY(1024)` | NULL，仅 TOTP 使用，Envelope Encryption |
| `recovery_codes_hashes` | `JSON` | NULL，只保存逐码带盐强哈希及消费状态，限制数量 |
| `key_version` | `SMALLINT UNSIGNED` | NULL，密文密钥版本 |
| `authenticator_status` | `VARCHAR(16)` | `pending/active/revoked/compromised` |
| `last_used_at` / `revoked_at` | `DATETIME(6)` | NULL |

约束：`UNIQUE(authenticator_no)`；WebAuthn 活跃 Credential Hash 全局唯一，单个管理员至少保留一个满足策略的 Active 认证器后才允许撤销其他认证器。绑定、恢复码生成、重置、使用和撤销全部写安全审计；超级管理员也不能通过普通管理 API 读取 Secret 或恢复码原文。管理员近期认证事实记录在 `auth_sessions.authenticated_at/authentication_methods/assurance_level`，高风险命令同时保存当次认证等级快照。

##### 3.7.2.7 admin_approval_requests 管理员审批申请表

该表是管理员职责分离和双人复核的权威聚合，与面向用户的 `ai_tool_approvals` 完全分离。业务命令命中审批策略时由领域 Service 创建审批申请；管理端不得通过通用接口任意拼装可执行命令。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `approval_request_no` | `VARCHAR(40)` | UK，对外审批申请 ID，`aar_` 前缀 |
| `approval_type` | `VARCHAR(32)` | `rbac_change/refund_exception/ai_release/content_release/sensitive_export/dead_letter_replay` 等白名单 |
| `action_code` | `VARCHAR(64)` | NOT NULL，领域命令码，不是自由文本 |
| `initiator_user_id` | `BIGINT UNSIGNED` | FK，发起管理员 |
| `scope_type` / `scope_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | 当时 Platform/Store Scope |
| `required_permission_code` | `VARCHAR(128)` | NOT NULL，执行命令所需规范权限码快照 |
| `target_type` / `target_no` | `VARCHAR(64)` / `VARCHAR(64)` | NOT NULL，主要目标资源 |
| `command_schema_version` | `SMALLINT UNSIGNED` | NOT NULL |
| `command_payload_ciphertext` | `MEDIUMBLOB` | NOT NULL，执行所需规范化最小参数，Envelope Encryption |
| `command_arguments_hash` | `BINARY(32)` | NOT NULL，绑定审批展示和最终执行参数 |
| `display_snapshot` | `JSON` | NOT NULL，脱敏后的对象、Scope、金额/数量、原因和影响 |
| `resource_versions` | `JSON` | NOT NULL，目标资源 ETag/Version 快照 |
| `approval_policy_snapshot` | `JSON` | NOT NULL，策略 ID/版本、审批人数、允许审批角色/Scope、职责分离规则 |
| `required_approval_count` / `approved_count` | `SMALLINT UNSIGNED` | NOT NULL / NOT NULL DEFAULT 0 |
| `request_status` | `VARCHAR(24)` | `pending/approved/executing/succeeded/rejected/cancelled/expired/failed/outcome_unknown` |
| `idempotency_key` / `execution_no` | `VARCHAR(128)` / `VARCHAR(40)` | NOT NULL / NULL；发起和执行分别幂等 |
| `expires_at` / `approved_at` / `execution_started_at` / `completed_at` | `DATETIME(6)` | NOT NULL / NULL / NULL / NULL |
| `result_resource_type` / `result_resource_no` | `VARCHAR(64)` / `VARCHAR(64)` | NULL |
| `error_code` / `trace_id` | `VARCHAR(64)` | NULL / NOT NULL |
| `key_version` | `SMALLINT UNSIGNED` | NOT NULL，命令密文密钥版本 |

约束：`UNIQUE(approval_request_no)`、`UNIQUE(initiator_user_id, action_code, idempotency_key)`；索引 `idx_admin_approvals_status_expiry(request_status, expires_at, id)`、`idx_admin_approvals_target(target_type, target_no, created_at, id)`、`idx_admin_approvals_scope(scope_type, scope_id, request_status, created_at, id)`。`approved_count <= required_approval_count`；最终执行参数 Hash 和资源版本不一致时审批失效并返回领域冲突，不允许替换参数后沿用旧审批。

##### 3.7.2.8 admin_approval_decisions 管理员审批决定表

决定表只追加，保存每位审批人的独立决定和当时安全上下文。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `decision_no` | `VARCHAR(40)` | UK |
| `approval_request_id` | `BIGINT UNSIGNED` | FK `admin_approval_requests.id` |
| `approver_user_id` | `BIGINT UNSIGNED` | FK，不能等于发起人 |
| `decision` | `VARCHAR(16)` | `approve/reject` |
| `reason_code` / `reason` | `VARCHAR(64)` / `VARCHAR(1000)` | NOT NULL / NOT NULL |
| `permission_code_snapshot` | `VARCHAR(128)` | NOT NULL，决定时使用的 `admin_approvals:decide` 或领域审批权限 |
| `scope_snapshot` | `JSON` | NOT NULL，决定时的 Platform/Store Scope |
| `assurance_level` / `authenticated_at` | `VARCHAR(16)` / `DATETIME(6)` | NOT NULL，MFA/近期认证证据 |
| `decision_hash` | `BINARY(32)` | NOT NULL，绑定 Request、参数 Hash、决定和审批人 |
| `decided_at` / `request_id` / `trace_id` | `DATETIME(6)` / `VARCHAR(64)` / `VARCHAR(64)` | NOT NULL |

约束：`UNIQUE(approval_request_id, approver_user_id)`，同一有效管理员账号对同一申请只能决定一次；发起人、已参与目标变更的人和策略禁止的职责角色不能审批。管理账号执行一人一号和禁止共享策略；接入企业 IdP 时，应在 Service 层额外校验不可变 Workforce Subject，阻止同一主体通过多账号占位。任一有效拒绝使 Pending Request 原子进入 `rejected`；达到不同审批人的门槛后才进入 `approved`。审批时和执行前均重新校验账号、Permission、Scope、Assurance 与职责分离，不以本表快照继续已撤销权限。

##### 3.7.2.9 admin_approval_events 管理员审批状态事件表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `event_no` | `VARCHAR(40)` | UK |
| `approval_request_id` | `BIGINT UNSIGNED` | FK |
| `event_type` | `VARCHAR(32)` | `created/decision_added/approved/rejected/cancelled/expired/execution_started/succeeded/failed/outcome_unknown/reconciled` |
| `from_status` / `to_status` | `VARCHAR(24)` | NULL / NOT NULL |
| `actor_type` / `actor_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | `admin/service/system` / NULL |
| `snapshot_redacted` | `JSON` | NULL，不复制命令密文或敏感原文 |
| `request_version` | `BIGINT UNSIGNED` | NOT NULL |
| `request_id` / `trace_id` | `VARCHAR(64)` | NULL / NOT NULL |

索引：`idx_admin_approval_events_request(approval_request_id, created_at, id)`。审批请求、决定、状态事件、管理员操作日志和 Outbox 在各自状态迁移事务中原子写入。批准后由专用 Executor 以 `execution_no` 抢占执行租约，重新构建可信参数并调用原领域 Command；`outcome_unknown` 必须先对账，禁止再次创建申请或盲目重放。

##### 3.7.2.10 user_role_events 用户角色授权事件表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `event_no` | `VARCHAR(40)` | UK |
| `grant_id` | `BIGINT UNSIGNED` | FK `user_roles.id` |
| `event_type` | `VARCHAR(16)` | `granted/revoked/expired/replaced` |
| `actor_user_id` | `BIGINT UNSIGNED` | NULL，系统过期为 NULL |
| `reason` | `VARCHAR(500)` | NOT NULL |
| `grant_snapshot` | `JSON` | NOT NULL，角色、Scope、有效期和不可委派校验摘要 |
| `permission_version_after` | `BIGINT UNSIGNED` | NOT NULL |
| `request_id` / `trace_id` | `VARCHAR(64)` | NOT NULL |

约束：`UNIQUE(event_no)`；索引 `idx_user_role_events_grant(grant_id, created_at, id)`。授权、撤销、到期或替代必须在同一事务内更新 Grant 状态、追加 Event、递增 `users.permission_version`并写 Outbox。

##### 3.7.2.11 admin_sensitive_access_grants 管理员敏感字段临时访问凭据表

MySQL 是 Grant 的权威来源，Redis 只缓存短时允许/撤销结果。Grant 不是可转移 Bearer Token，必须与管理员、Admin Session、目标资源和字段集同时匹配。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `grant_no` | `VARCHAR(40)` | UK，不透明 `sag_` 公开 ID |
| `admin_user_id` / `auth_session_id` | `BIGINT UNSIGNED` | FK，绑定当前管理员和 Admin Session |
| `target_type` / `target_no` | `VARCHAR(32)` / `VARCHAR(64)` | NOT NULL，如 `user/usr_...` |
| `field_set` | `JSON` | NOT NULL，只允许注册表中的最小字段白名单 |
| `purpose_code` / `reason` | `VARCHAR(64)` / `VARCHAR(1000)` | NOT NULL，业务用途与说明 |
| `grant_status` | `VARCHAR(16)` | `active/consumed/revoked/expired` |
| `assurance_level` / `authenticated_at` | `VARCHAR(16)` / `DATETIME(6)` | NOT NULL，近期认证快照 |
| `expires_at` / `consumed_at` / `revoked_at` | `DATETIME(6)` | NOT NULL / NULL / NULL |
| `revoked_by` / `revoke_reason` | `BIGINT UNSIGNED` / `VARCHAR(500)` | NULL |
| `request_id` / `trace_id` | `VARCHAR(64)` | NOT NULL |

约束：`UNIQUE(grant_no)`；索引 `idx_sensitive_grants_admin_session(admin_user_id, auth_session_id, grant_status, expires_at)`、`idx_sensitive_grants_target(target_type, target_no, grant_status, expires_at)`。读取敏感字段时必须再次校验当前 Permission/Scope、Session 未撤销、字段集、Purpose 和 TTL，每次成功/拒绝都写审计。首版 Grant 一次消费；不得用于批量导出、更换目标或跨 Session 复用。

#### 3.7.3 店铺表

##### 3.7.3.1 stores 店铺表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `store_no` | `VARCHAR(40)` | UK，对外 ID |
| `owner_user_id` | `BIGINT UNSIGNED` | FK `users.id` |
| `store_name` / `store_name_normalized` | `VARCHAR(128)` | NOT NULL，归一化名称按产品策略 UK |
| `logo_object_key` | `VARCHAR(512)` | NULL |
| `description` | `VARCHAR(2000)` | NULL |
| `store_status` | `VARCHAR(32)` | `pending/active/suspended/closed` |
| `service_phone_ciphertext` | `VARBINARY(512)` | NULL |
| `rating_score` | `DECIMAL(3,2)` | NOT NULL DEFAULT 0，可重建展示聚合值 |
| `rating_count` / `follower_count` / `sales_count` | `BIGINT UNSIGNED` | NOT NULL DEFAULT 0，派生计数 |
| `opened_at` / `suspended_at` / `closed_at` | `DATETIME(6)` | NULL |

索引：`uk_stores_store_no`、`idx_stores_owner(owner_user_id)`、`idx_stores_status_created(store_status, created_at, id)`。评分/数量字段不是原始事实，需定期与评价/关注/订单对账。

##### 3.7.3.2 store_certifications 店铺认证表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `certification_no` | `VARCHAR(40)` | UK |
| `store_id` | `BIGINT UNSIGNED` | FK |
| `certification_type` | `VARCHAR(32)` | `business_license/brand_authorization` 等 |
| `subject_name_ciphertext` / `certificate_no_ciphertext` | `VARBINARY(1024)` | NOT NULL，L3 密文 |
| `certificate_no_hash` | `BINARY(32)` | NULL，防重复/精确匹配 |
| `current_material_version` | `INT UNSIGNED` | NOT NULL DEFAULT 1，当前补件版本 |
| `evidence_object_key` | `VARCHAR(512)` | NOT NULL，当前私有材料对象投影；历史在 Event 中引用受控文件 |
| `review_status` | `VARCHAR(32)` | `pending/more_info_required/approved/rejected/expired/revoked` |
| `valid_from` / `valid_until` | `DATE` | NULL |
| `reviewed_by` | `BIGINT UNSIGNED` | NULL，管理员 |
| `reviewed_at` | `DATETIME(6)` | NULL |
| `decision_reason_code` / `decision_reason` | `VARCHAR(64)` / `VARCHAR(1000)` | NULL，拒绝或要求补件时必填 |
| `resubmitted_at` | `DATETIME(6)` | NULL，最近补件重新提交时间 |
| `key_version` | `SMALLINT UNSIGNED` | NOT NULL |

索引：`idx_store_cert_store_type(store_id, certification_type, review_status)`、`idx_store_cert_expiry(review_status, valid_until)`。证件访问必须写审计。允许的主链为 `pending → more_info_required → pending → approved/rejected`，可多轮补件；已批准证件到期进入 `expired`，受控撤销进入 `revoked`，不改写旧材料与审核事件。

##### 3.7.3.3 store_categories 店铺分类表

表示店铺获准经营的平台分类，不是店内自定义分组。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `store_id` / `category_id` | `BIGINT UNSIGNED` | FK，UK |
| `approval_status` | `VARCHAR(16)` | `pending/approved/rejected/revoked` |
| `approved_by` | `BIGINT UNSIGNED` | NULL |
| `approved_at` / `revoked_at` | `DATETIME(6)` | NULL |

约束：`UNIQUE(store_id, category_id)`。商品上架时校验店铺对目标分类仍有效。

##### 3.7.3.4 store_follows 店铺收藏表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `user_id` / `store_id` | `BIGINT UNSIGNED` | FK |
| `followed_at` | `DATETIME(6)` | NOT NULL |
| `deleted_at` | `DATETIME(6)` | NULL，取消关注 |
| `active_user_id` | `BIGINT UNSIGNED GENERATED` | 活跃时为 `user_id`，否则 NULL |

约束：`UNIQUE(store_id, active_user_id)`；索引 `idx_store_follows_user(user_id, deleted_at, followed_at)`。重新关注可恢复旧行或新增行，但统计流程必须与实现一致。

##### 3.7.3.5 store_service_policies 店铺服务政策表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `policy_no` | `VARCHAR(40)` | UK |
| `store_id` | `BIGINT UNSIGNED` | FK |
| `policy_type` | `VARCHAR(32)` | `shipping/return/warranty/customer_service` |
| `title` | `VARCHAR(128)` | NOT NULL |
| `content` | `MEDIUMTEXT` | NOT NULL，草稿/发布版本正文，已发布行不可原地改写 |
| `content_hash` | `BINARY(32)` | NOT NULL |
| `policy_version` | `INT UNSIGNED` | NOT NULL |
| `policy_status` | `VARCHAR(16)` | `draft/published/withdrawn/expired` |
| `effective_at` / `expires_at` / `published_at` / `withdrawn_at` | `DATETIME(6)` | NULL |
| `supersedes_policy_id` | `BIGINT UNSIGNED` | NULL，指向同店铺同类型的旧版本 |
| `created_by` / `published_by` / `withdrawn_by` | `BIGINT UNSIGNED` | NOT NULL / NULL / NULL |

约束：`UNIQUE(store_id, policy_type, policy_version)`；同店铺同类型发布时锁定策略家族，校验时间窗不重叠，不允许两个当前有效版本。公开查询以 `policy_status=published AND effective_at <= db_now AND (expires_at IS NULL OR expires_at > db_now)` 为权威条件，不依赖 Scheduler 改状态才生效。发布、替换、撤回都写 Outbox，驱动公开缓存失效和 RAG 影子重建/撤回；历史订单/退款引用当时策略 ID+版本+快照，永不跟随新版本改变。

##### 3.7.3.6 store_product_groups 店铺商品分组表

店铺商品分组服务于店铺首页的“手机/电脑/配件”等自定义导航，与 `store_categories` 的平台经营许可完全分离。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `group_no` | `VARCHAR(40)` | UK，对外分组 ID |
| `store_id` | `BIGINT UNSIGNED` | FK |
| `parent_id` | `BIGINT UNSIGNED` | NULL，同店铺自关联；首版最多两级 |
| `group_name` | `VARCHAR(64)` | NOT NULL |
| `group_name_normalized` | `VARCHAR(64)` | NOT NULL，Unicode、空白与大小写归一化结果 |
| `group_status` | `VARCHAR(16)` | `active/hidden` |
| `sort_order` | `INT` | NOT NULL DEFAULT 0 |
| `active_parent_scope_id` | `BIGINT UNSIGNED GENERATED` | `active` 时为 `COALESCE(parent_id, 0)`，否则 NULL；0 为保留根节点值 |
| `active_group_name_normalized` | `VARCHAR(64) GENERATED` | `active` 时为 `group_name_normalized`，否则 NULL |

约束：`UNIQUE(store_id, active_parent_scope_id, active_group_name_normalized)`，保证同店铺同父节点下活跃规范化名称唯一；父节点必须属于同一店铺且不能形成环。索引 `idx_store_groups_navigation(store_id, group_status, parent_id, sort_order, id)`。

##### 3.7.3.7 store_product_group_items 店铺分组商品关联表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `store_product_group_id` / `product_id` | `BIGINT UNSIGNED` | FK |
| `store_id` | `BIGINT UNSIGNED` | NOT NULL，冗余用于范围约束和索引 |
| `sort_order` | `INT` | NOT NULL DEFAULT 0 |

约束：`UNIQUE(store_product_group_id, product_id)`；关联时必须校验 Group 和 Product 属于相同 `store_id`。商品下架不删除关联，只在公开查询中排除。

##### 3.7.3.8 shipping_templates 配送与运费模板表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `template_no` | `VARCHAR(40)` | UK |
| `template_family_no` | `VARCHAR(40)` | NOT NULL，同一运费模板各版本共享的稳定 ID |
| `store_id` | `BIGINT UNSIGNED` | FK |
| `template_name` | `VARCHAR(128)` | NOT NULL |
| `delivery_type` | `VARCHAR(32)` | `express/pickup/local_delivery`，首版至少支持 `express` |
| `charge_mode` | `VARCHAR(16)` | `free/fixed/by_quantity/by_weight` |
| `currency` | `CHAR(3)` | NOT NULL DEFAULT `CNY` |
| `template_status` | `VARCHAR(16)` | `draft/effective/disabled` |
| `dispatch_min_hours` / `dispatch_max_hours` | `SMALLINT UNSIGNED` | NOT NULL |
| `policy_version` | `INT UNSIGNED` | NOT NULL |
| `current_template_family_no` | `VARCHAR(40) GENERATED` | `effective` 时为 `template_family_no`，否则 NULL |

约束：`UNIQUE(template_no)`、`UNIQUE(template_family_no, policy_version)`、`UNIQUE(store_id, current_template_family_no)`；`template_family_no` 创建后不得换店。商品资料绑定具体有效版本行；发布同一 Family 的新版本时，在一个事务内把所有仍引用旧版本的当前 `product_fulfillment_profiles` 迁移到新版本，再把旧版本置为 `disabled`、新版本置为 `effective`。历史 Checkout/订单只保留旧版本快照，不跟随迁移；事务提交后通过 Outbox 失效受影响 Checkout 和缓存。

##### 3.7.3.9 shipping_template_rules 运费区域规则表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `shipping_template_id` | `BIGINT UNSIGNED` | FK |
| `region_scope` | `JSON` | NOT NULL，版本化行政区划代码集合/排除集合，不存自由文本 |
| `first_unit` / `additional_unit` | `INT UNSIGNED` | NOT NULL，按件或克解释 |
| `first_fee_amount` / `additional_fee_amount` | `BIGINT UNSIGNED` | NOT NULL，最小货币单位 |
| `estimated_min_days` / `estimated_max_days` | `SMALLINT UNSIGNED` | NULL |
| `rule_status` | `VARCHAR(16)` | `active/disabled` |

服务端按收货地区、商品数量/重量和模板版本确定唯一命中规则；未命中时返回不可配送，不能让前端自行计算运费。`charge_mode=free` 时四个单位/费用字段统一写 0；`fixed` 时 `first_fee_amount` 为固定运费，其余写 0；按件/重量计费时首段和续段单位均须大于 0，并以 CHECK 与 Service 校验保持一致。

##### 3.7.3.10 store_announcements 店铺公告表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `announcement_no` | `VARCHAR(40)` | UK |
| `store_id` | `BIGINT UNSIGNED` | FK |
| `title` / `content` | `VARCHAR(128)` / `VARCHAR(2000)` | NOT NULL，经内容安全过滤 |
| `announcement_status` | `VARCHAR(16)` | `draft/published/expired/hidden` |
| `starts_at` / `ends_at` | `DATETIME(6)` | NULL |
| `sort_order` | `INT` | NOT NULL DEFAULT 0 |

##### 3.7.3.11 store_featured_products 店铺推荐商品表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `store_id` / `product_id` | `BIGINT UNSIGNED` | FK |
| `slot_type` | `VARCHAR(16)` | `recommended/featured`；热销榜默认由销量派生 |
| `sort_order` | `INT` | NOT NULL DEFAULT 0 |
| `starts_at` / `ends_at` | `DATETIME(6)` | NULL |

约束：`UNIQUE(store_id, product_id, slot_type)`；公开接口只返回当前有效且仍为 `on_sale` 的本店商品。

##### 3.7.3.12 store_certification_events 店铺认证事件与材料版本表

追加型事件同时表达材料版本与审核决定，不用新补件覆盖旧文件。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `event_no` | `VARCHAR(40)` | UK |
| `certification_id` | `BIGINT UNSIGNED` | FK `store_certifications.id` |
| `event_type` | `VARCHAR(32)` | `submitted/more_info_requested/materials_resubmitted/approved/rejected/expired/revoked` |
| `material_version` | `INT UNSIGNED` | NOT NULL，当次决定所见材料版本 |
| `evidence_file_ids` | `JSON` | NULL，只保存受控私有 `file_id` 引用和 Hash |
| `reason_code` / `reason` | `VARCHAR(64)` / `VARCHAR(1000)` | NULL，补件/拒绝/撤销必填 |
| `required_materials` | `JSON` | NULL，要求补件时的结构化材料项和截止时间 |
| `actor_type` / `actor_user_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | `store/admin/system` / NULL |
| `certification_version` | `BIGINT UNSIGNED` | NOT NULL |
| `request_id` / `trace_id` | `VARCHAR(64)` | NOT NULL |

约束：`UNIQUE(event_no)`、`UNIQUE(certification_id, material_version, event_type)`（允许的单次类型）；索引 `idx_store_cert_events_cert(certification_id, created_at, id)`。重新提交必须来自 `more_info_required`、材料版本递增且所有文件 Active/归属当前店铺；审核决定使用 If-Match 绑定当前认证与材料版本。

#### 3.7.4 商品表

##### 3.7.4.1 categories 平台分类表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `category_no` | `VARCHAR(40)` | UK |
| `parent_id` | `BIGINT UNSIGNED` | NULL，自关联 |
| `category_name` / `category_code` | `VARCHAR(64)` | NOT NULL，`category_code` UK |
| `path` | `VARCHAR(1024)` | NOT NULL，祖先 ID 路径快照 |
| `level` / `sort_order` | `SMALLINT UNSIGNED` / `INT` | NOT NULL |
| `icon_object_key` | `VARCHAR(512)` | NULL |
| `category_status` | `VARCHAR(16)` | `active/disabled` |

索引：`idx_categories_parent_sort(parent_id, sort_order, id)`。移动分类需防止环、事务内更新子树路径，大子树采用后台任务与维护态。

##### 3.7.4.2 brands 品牌表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `brand_no` | `VARCHAR(40)` | UK |
| `brand_name` / `brand_name_normalized` | `VARCHAR(128)` | NOT NULL，归一化名称 UK |
| `logo_object_key` | `VARCHAR(512)` | NULL |
| `description` | `VARCHAR(2000)` | NULL |
| `brand_status` | `VARCHAR(16)` | `active/disabled` |

品牌删除不级联商品；已被引用时只能停用。

##### 3.7.4.3 products 商品表

商品表代表 SPU，价格和可销库存以 SKU 为准。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `product_no` | `VARCHAR(40)` | UK |
| `store_id` / `category_id` / `brand_id` | `BIGINT UNSIGNED` | FK，`brand_id` 可空 |
| `product_name` | `VARCHAR(255)` | NOT NULL |
| `subtitle` | `VARCHAR(500)` | NULL |
| `description` | `VARCHAR(2000)` | NULL，右侧购买区使用的安全纯文本摘要，不承载富文本 |
| `current_detail_content_version_id` | `BIGINT UNSIGNED` | NULL，FK `product_content_versions.id`，当前编辑版本 |
| `published_detail_content_version_id` | `BIGINT UNSIGNED` | NULL，FK `product_content_versions.id`，公开详情固定版本 |
| `product_status` | `VARCHAR(32)` | `draft/pending_review/on_sale/off_shelf/rejected/archived` |
| `default_sku_id` | `BIGINT UNSIGNED` | NULL，SKU 创建后设置 |
| `min_price_amount` / `max_price_amount` | `BIGINT UNSIGNED` | NOT NULL DEFAULT 0，可重建展示聚合 |
| `currency` | `CHAR(3)` | NOT NULL DEFAULT `CNY` |
| `sales_count` / `review_count` | `BIGINT UNSIGNED` | NOT NULL DEFAULT 0，派生值 |
| `rating_score` | `DECIMAL(3,2)` | NOT NULL DEFAULT 0，派生值 |
| `published_at` / `off_shelf_at` | `DATETIME(6)` | NULL |

索引：`uk_products_product_no`、`idx_products_store_status(store_id, product_status, created_at, id)`、`idx_products_category_status(category_id, product_status, id)`。上架必须至少有一个可售 SKU、主图、有效分类和店铺经营权，并且 `published_detail_content_version_id` 指向扫描通过、已批准且属于本商品的不可变内容版本。因 Version 表反向引用 Product，外键通过 Alembic 后续 Add Constraint 建立，不能省略归属校验。

##### 3.7.4.4 product_skus SKU 表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `sku_no` | `VARCHAR(40)` | UK |
| `product_id` | `BIGINT UNSIGNED` | FK |
| `merchant_sku_code` | `VARCHAR(64)` | NULL，店铺内活跃唯一 |
| `sku_name` | `VARCHAR(255)` | NOT NULL |
| `spec_values` | `JSON` | NOT NULL，规格 ID/名称/值快照，Schema 版本化 |
| `sale_price_amount` / `market_price_amount` | `BIGINT UNSIGNED` | NOT NULL |
| `currency` | `CHAR(3)` | NOT NULL |
| `weight_grams` | `INT UNSIGNED` | NULL |
| `barcode` | `VARCHAR(64)` | NULL |
| `sku_status` | `VARCHAR(16)` | `active/disabled` |

约束：`CHECK(market_price_amount >= sale_price_amount)`；索引 `idx_skus_product_status(product_id, sku_status, id)`、店铺 SKU Code 需通过冗余 `store_id` 或生成业务键实现活跃唯一。价格变更递增 `version` 并发送缓存/索引失效事件。

##### 3.7.4.5 product_images 商品图片表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `product_id` / `sku_id` | `BIGINT UNSIGNED` | FK，`sku_id` NULL 表示 SPU 图 |
| `object_key` | `VARCHAR(512)` | NOT NULL |
| `image_type` | `VARCHAR(16)` | `main/gallery/detail/spec` |
| `alt_text` | `VARCHAR(255)` | NULL |
| `width` / `height` / `sort_order` | `INT UNSIGNED` | NOT NULL |
| `image_status` | `VARCHAR(16)` | `active/hidden` |

索引：`idx_product_images_product_sort(product_id, sku_id, image_type, sort_order, id)`。`sku_id` 非 NULL 时必须属于同一 `product_id`；SPU 公共图和每个 SKU 专属图都是独立的有序集合，同一作用域的 `sort_order` 不得重复。仅允许一张 `sku_id IS NULL AND image_type=main AND image_status=active` 的 SPU 活跃主图，通过可空生成列唯一约束保证；每个 SKU 可有多张 `gallery/spec` 图。查询时按 `sort_order,id` 稳定排序，SKU 图集为空时由公开 DTO 明确回退 SPU 公共图集，不复制图片行。

##### 3.7.4.6 product_attributes 商品属性表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `product_id` | `BIGINT UNSIGNED` | FK |
| `attribute_code` / `attribute_name` | `VARCHAR(64)` / `VARCHAR(128)` | NOT NULL |
| `value_text` | `VARCHAR(1000)` | NOT NULL |
| `value_normalized` | `VARCHAR(500)` | NULL，用于精确筛选 |
| `unit` | `VARCHAR(32)` | NULL |
| `is_searchable` | `BOOLEAN` | NOT NULL DEFAULT FALSE |
| `sort_order` | `INT` | NOT NULL DEFAULT 0 |

约束：`UNIQUE(product_id, attribute_code)`；高频筛选属性后续应进入专用搜索索引，不在 MySQL 上无限叠加 EAV 查询。

##### 3.7.4.7 product_favorites 商品收藏表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `user_id` / `product_id` | `BIGINT UNSIGNED` | FK |
| `favorited_at` | `DATETIME(6)` | NOT NULL |
| `deleted_at` | `DATETIME(6)` | NULL |
| `active_user_id` | `BIGINT UNSIGNED GENERATED` | 活跃时为 `user_id` |

约束：`UNIQUE(product_id, active_user_id)`；索引 `idx_product_favorites_user(user_id, deleted_at, favorited_at)`。

##### 3.7.4.8 product_faqs 商品常见问题表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `faq_no` | `VARCHAR(40)` | UK |
| `product_id` | `BIGINT UNSIGNED` | FK |
| `question` | `VARCHAR(1000)` | NOT NULL，纯文本归一化和安全过滤，不接受 HTML |
| `current_content_version_id` | `BIGINT UNSIGNED` | NULL，FK `product_faq_versions.id`，当前答案编辑版本 |
| `published_content_version_id` | `BIGINT UNSIGNED` | NULL，FK `product_faq_versions.id`，公开答案固定版本 |
| `faq_status` | `VARCHAR(16)` | `draft/published/hidden` |
| `sort_order` | `INT` | NOT NULL DEFAULT 0 |
| `published_at` | `DATETIME(6)` | NULL |

索引：`idx_product_faqs_product_status(product_id, faq_status, sort_order)`。FAQ 发布要求 `published_content_version_id` 属于本 FAQ 且安全检查通过；发布/隐藏写 Outbox，供 PostgreSQL RAG 按 `faq_no + content_version + content_hash` 幂等更新。公开响应不返回原始答案 Source。

##### 3.7.4.9 product_status_logs 商品状态记录表

追加型。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `product_id` | `BIGINT UNSIGNED` | NOT NULL |
| `from_status` / `to_status` | `VARCHAR(32)` | NULL / NOT NULL |
| `event_type` | `VARCHAR(64)` | NOT NULL，如 `submitted/review_started/approved/rejected/off_shelf/archived` |
| `actor_type` | `VARCHAR(16)` | `merchant/admin/system` |
| `actor_id` | `BIGINT UNSIGNED` | NULL |
| `reason_code` / `reason` | `VARCHAR(64)` / `VARCHAR(500)` | NULL |
| `product_version` | `BIGINT UNSIGNED` | NOT NULL |
| `request_id` / `trace_id` | `VARCHAR(64)` | NULL |

索引：`idx_product_status_logs_product(product_id, created_at, id)`。主表状态、状态流水和 Outbox 必须同事务提交。

##### 3.7.4.10 product_fulfillment_profiles 商品履约资料表

用于向商品详情、结算和 AI 客服提供一致的结构化发货信息；不能只从商品富文本或 RAG 中猜测。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `product_id` | `BIGINT UNSIGNED` | FK，UK，每个 SPU 一条当前资料 |
| `shipping_template_id` | `BIGINT UNSIGNED` | FK，必须为本店当前有效模板版本；模板发布事务负责迁移当前引用 |
| `origin_region_code` | `VARCHAR(32)` | NOT NULL，公开接口按城市级展示，仓库详细地址不公开 |
| `dispatch_min_hours` / `dispatch_max_hours` | `SMALLINT UNSIGNED` | NOT NULL，可在商品级覆盖模板默认值 |
| `purchase_notice` | `VARCHAR(3000)` | NULL，退换限制、使用禁忌和保养等经审核说明 |
| `profile_version` | `INT UNSIGNED` | NOT NULL |

模板或资料变更需递增版本并使受影响 Checkout/RAG 缓存失效；历史订单保存当时的配送与售后政策快照，不回读当前值。

##### 3.7.4.11 product_content_versions 商品详情内容版本表

商品富文本采用不可变版本，分离商家原始编辑内容与可公开/可供 AI 使用的安全派生内容。原始 Source 仅管理编辑和安全审查可见，绝不直接进入公开 DTO。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `content_version_no` | `VARCHAR(40)` | UK，对外版本 ID |
| `product_id` | `BIGINT UNSIGNED` | FK `products.id` |
| `content_version` | `INT UNSIGNED` | NOT NULL，同商品单调递增 |
| `source_format` | `VARCHAR(16)` | `structured/html/plain_text`，均按不可信输入保存 |
| `source_content` | `MEDIUMTEXT` | NOT NULL，私有原始编辑内容；加大小上限，不进入公开/Agent DTO |
| `source_hash` | `BINARY(32)` | NOT NULL，原始规范化内容 Hash |
| `public_content_format` | `VARCHAR(24)` | `structured_v1/safe_html_v1` |
| `safe_blocks` | `JSON` | NULL，版本化结构化块；Schema 拒绝未知 Block/属性 |
| `safe_html` | `MEDIUMTEXT` | NULL，服务端 Sanitizer 生成，不接受客户端作为权威值 |
| `safe_text` | `MEDIUMTEXT` | NOT NULL，纯文本降级及 AI/RAG 的不可信数据输入 |
| `content_hash` | `BINARY(32)` | NOT NULL，安全公开内容的 Canonical Hash |
| `sanitizer_policy_version` / `content_schema_version` | `INT UNSIGNED` | NOT NULL |
| `security_scan_status` | `VARCHAR(16)` | `pending/passed/rejected/error` |
| `version_status` | `VARCHAR(16)` | `draft/reviewing/approved/published/rejected/superseded` |
| `created_by` / `approved_by` | `BIGINT UNSIGNED` | NOT NULL / NULL，店铺员工/审核员 |
| `approved_at` / `published_at` | `DATETIME(6)` | NULL |

约束：`UNIQUE(product_id, content_version)`、`UNIQUE(content_version_no)`；`public_content_format=structured_v1` 时只允许 `safe_blocks` 非空，`safe_html_v1` 时只允许 `safe_html` 非空，二者必须恰有一个公开载荷，`safe_text` 始终存在。Approved/Published 版本不可修改 Source 或安全派生内容；修改必须新建版本。发布事务重新运行当前 Sanitizer/安全扫描，验证所有图片为本店 Active `product_detail` 文件、外链/标签/属性白名单和 Hash，再更新 `products.published_detail_content_version_id`、写状态日志与 Outbox。Sanitizer 策略升级时旧版本先后台重建/复核，未通过的新派生版本不得覆盖当前公开版本。

##### 3.7.4.12 product_faq_versions 商品 FAQ 答案版本表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `faq_version_no` | `VARCHAR(40)` | UK |
| `product_faq_id` | `BIGINT UNSIGNED` | FK `product_faqs.id` |
| `content_version` | `INT UNSIGNED` | NOT NULL，同 FAQ 单调递增 |
| `source_format` / `source_content` / `source_hash` | `VARCHAR(16)` / `TEXT` / `BINARY(32)` | NOT NULL，私有不可信 Source |
| `public_content_format` | `VARCHAR(24)` | `structured_v1/safe_html_v1` |
| `safe_blocks` / `safe_html` / `safe_text` | `JSON` / `TEXT` / `TEXT` | NULL / NULL / NOT NULL，公开/AI 只用安全派生字段 |
| `content_hash` | `BINARY(32)` | NOT NULL |
| `sanitizer_policy_version` / `content_schema_version` | `INT UNSIGNED` | NOT NULL |
| `security_scan_status` | `VARCHAR(16)` | `pending/passed/rejected/error` |
| `version_status` | `VARCHAR(16)` | `draft/reviewing/approved/published/rejected/superseded` |
| `created_by` / `approved_by` | `BIGINT UNSIGNED` | NOT NULL / NULL |
| `approved_at` / `published_at` | `DATETIME(6)` | NULL |

约束：`UNIQUE(product_faq_id, content_version)`、`UNIQUE(faq_version_no)`；安全规则、不可变发布、图片归属和 Sanitizer 升级策略与 3.7.4.11 相同。问题标题使用纯文本；答案发布事务更新 `product_faqs.published_content_version_id` 并以 Outbox 触发缓存失效和 RAG 影子重建。RAG 只摄取 `safe_text + content_hash + Store/Product ACL`，原始 Source 永不进入 Prompt。

---

#### 3.7.5 库存表

库存领域的权威数量是 `inventories.on_hand_quantity` 和 `reserved_quantity`；“可售”为 `on_hand_quantity - reserved_quantity - safety_stock_quantity`。结算预览不锁库存，提交订单才创建预占。

##### 3.7.5.1 inventories 库存表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `sku_id` | `BIGINT UNSIGNED` | FK `product_skus.id`，UK，每 SKU 一行 |
| `on_hand_quantity` | `INT UNSIGNED` | NOT NULL DEFAULT 0，实际在库 |
| `reserved_quantity` | `INT UNSIGNED` | NOT NULL DEFAULT 0，未支付订单预占 |
| `safety_stock_quantity` | `INT UNSIGNED` | NOT NULL DEFAULT 0，安全库存 |
| `sold_quantity` | `BIGINT UNSIGNED` | NOT NULL DEFAULT 0，累计辅助值 |
| `inventory_status` | `VARCHAR(16)` | `active/frozen` |
| `last_reconciled_at` | `DATETIME(6)` | NULL |

检查约束：`reserved_quantity <= on_hand_quantity`；销售路径还必须满足 `on_hand_quantity - reserved_quantity - safety_stock_quantity >= requested_quantity`。`version` 每次数量变更递增。

##### 3.7.5.2 inventory_reservations 库存预占表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `reservation_no` | `VARCHAR(40)` | UK |
| `inventory_id` / `sku_id` | `BIGINT UNSIGNED` | FK/NOT NULL |
| `order_id` / `order_item_id` | `BIGINT UNSIGNED` | FK，NOT NULL |
| `quantity` | `INT UNSIGNED` | NOT NULL，`> 0` |
| `reservation_status` | `VARCHAR(16)` | `active/confirmed/released/expired` |
| `idempotency_key` | `VARCHAR(128)` | NOT NULL |
| `expires_at` | `DATETIME(6)` | NOT NULL |
| `confirmed_at` / `released_at` | `DATETIME(6)` | NULL |
| `release_reason` | `VARCHAR(64)` | NULL |

约束：`UNIQUE(order_item_id)`、`UNIQUE(idempotency_key)`；索引 `idx_inventory_reservations_expiry(reservation_status, expires_at, id)`。一条预占只能从 `active` 转为一个终态，确认/释放必须幂等。

##### 3.7.5.3 inventory_logs 库存流水表

追加型，不通过修改历史流水调整库存。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `inventory_id` / `sku_id` | `BIGINT UNSIGNED` | NOT NULL |
| `operation_type` | `VARCHAR(32)` | `inbound/reserve/confirm/release/adjust/freeze` |
| `on_hand_delta` / `reserved_delta` | `INT` | NOT NULL，允许负值 |
| `on_hand_before` / `on_hand_after` | `INT UNSIGNED` | NOT NULL |
| `reserved_before` / `reserved_after` | `INT UNSIGNED` | NOT NULL |
| `reference_type` / `reference_no` | `VARCHAR(32)` / `VARCHAR(64)` | NOT NULL，订单/预占/盘点等 |
| `idempotency_key` | `VARCHAR(128)` | NOT NULL，UK |
| `actor_type` / `actor_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | NOT NULL / NULL |
| `reason` | `VARCHAR(500)` | NULL |
| `inventory_version` | `BIGINT UNSIGNED` | NOT NULL，变更后版本 |

索引：`idx_inventory_logs_sku_time(sku_id, created_at, id)`、`idx_inventory_logs_reference(reference_type, reference_no)`。

##### 3.7.5.4 库存扣减与释放规则

```text
订单提交：available 减少
  inventories.reserved_quantity += quantity
  + inventory_reservations(active)
  + inventory_logs(reserve)

支付成功：实际在库与预占同时减少
  on_hand_quantity -= quantity
  reserved_quantity -= quantity
  reservation active → confirmed

取消/超时：只释放预占
  reserved_quantity -= quantity
  reservation active → released/expired

退货入库：经收货验货后单独入库
  on_hand_quantity += accepted_quantity
```

库存主表、预占、流水和 Outbox 在同一 MySQL 事务中写入。支付回调与超时关单竞争时，先按 `order_id` 锁定订单，只有成功状态迁移的一方处理库存。已过期但渠道后续确认支付的订单不得盲目重扣库存，应进入异常支付补偿/原路退款流程。

##### 3.7.5.5 库存超卖防护

预占必须使用原子条件更新，不允许先查后扣：

```sql
UPDATE inventories
SET reserved_quantity = reserved_quantity + :qty,
    version = version + 1,
    updated_at = UTC_TIMESTAMP(6)
WHERE sku_id = :sku_id
  AND inventory_status = 'active'
  AND on_hand_quantity - reserved_quantity - safety_stock_quantity >= :qty;
```

影响行数为 0 时再区分库存不足/SKU 冻结，返回统一业务错误。多 SKU 按 `sku_id ASC` 锁定；任一 SKU 预占失败则整个订单事务回滚。库存缓存仅用于页面展示，不参与最终是否可下单的判断。定时以 `inventories` 与活跃预占汇总对账，差异只生成告警/受控调整，不自动无痕修改。

#### 3.7.6 购物车与结算表

##### 3.7.6.1 carts 购物车表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `cart_no` | `VARCHAR(40)` | UK |
| `user_id` | `BIGINT UNSIGNED` | FK，UK，每用户唯一永久购物车 |
| `cart_status` | `VARCHAR(16)` | 首版固定为 `active`；仅为未来游客购物车迁移保留扩展位 |
| `item_count` | `INT UNSIGNED` | NOT NULL DEFAULT 0，购物车行数派生值；导航角标使用 `SUM(cart_items.quantity)` 得到的 `cart_total_quantity`，二者不可混用 |
| `last_activity_at` | `DATETIME(6)` | NOT NULL |

首版采用永久购物车模型：注册用户始终复用同一条 Cart，清空购物车只删除 Cart Item，不将 Cart 置为 `abandoned`，订单创建也不删除 Cart。`UNIQUE(user_id)` 表达“每个用户恰有至多一个永久购物车”，首次加购时幂等创建。匿名购物车首版不建表；未来引入时使用独立 Guest Cart/Guest Token，登录后把条目幂等合并到用户永久购物车并记录合并审计，不复用本表状态保存历史匿名购物车。

##### 3.7.6.2 cart_items 购物车商品表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `cart_item_no` | `VARCHAR(40)` | NOT NULL，UK，对外购物车条目 ID，使用 `ci_` 前缀 |
| `cart_id` / `sku_id` | `BIGINT UNSIGNED` | FK |
| `quantity` | `INT UNSIGNED` | NOT NULL，`1..per_sku_limit` |
| `is_selected` | `BOOLEAN` | NOT NULL DEFAULT TRUE |
| `added_price_amount` | `BIGINT UNSIGNED` | NOT NULL，加车时价格，只用于提示变价 |
| `currency` | `CHAR(3)` | NOT NULL |
| `sku_version` | `BIGINT UNSIGNED` | NOT NULL，加车/最后刷新时版本 |
| `invalid_reason` | `VARCHAR(64)` | NULL，下架/无库存等展示提示 |

约束：`UNIQUE(cart_item_no)`、`UNIQUE(cart_id, sku_id)`；索引 `idx_cart_items_cart_selected(cart_id, is_selected, id)`。API 的 `cart_item_id` 和 `cart_item_ids[]` 均承载 `cart_item_no`，不得暴露通用自增 `id`。商品价格/库存必须在结算时重新校验，不以 `added_price_amount` 成交。

##### 3.7.6.3 checkout_sessions 结算会话表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `checkout_no` | `VARCHAR(40)` | UK，前端结算 Token |
| `user_id` | `BIGINT UNSIGNED` | FK |
| `source_type` | `VARCHAR(16)` | `buy_now/cart` |
| `checkout_status` | `VARCHAR(16)` | `active/submitted/expired/cancelled` |
| `selected_address_id` | `BIGINT UNSIGNED` | NULL，FK |
| `goods_amount` / `freight_amount` / `payable_amount` | `BIGINT UNSIGNED` | NOT NULL |
| `currency` | `CHAR(3)` | NOT NULL |
| `pricing_version` | `VARCHAR(32)` | NOT NULL，计价策略版本 |
| `snapshot_hash` | `BINARY(32)` | NOT NULL，当前快照集哈希 |
| `expires_at` | `DATETIME(6)` | NOT NULL |
| `submitted_at` | `DATETIME(6)` | NULL |

索引：`idx_checkout_user_status(user_id, checkout_status, created_at)`、`idx_checkout_expiry(checkout_status, expires_at)`。提交时使用状态条件更新，一个 Checkout 最多创建一个 `trade_order`。`source_type=buy_now` 的输入快照必须恰含一个 SKU Item；`source_type=cart` 允许 1–100 个不重复 Cart Item。该不变式在 Pydantic 判别联合、Service 和快照 Schema 三层校验，不依赖前端。

##### 3.7.6.4 checkout_snapshots 结算快照表

追加型。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `checkout_session_id` | `BIGINT UNSIGNED` | FK |
| `snapshot_version` | `INT UNSIGNED` | NOT NULL |
| `schema_version` | `SMALLINT UNSIGNED` | NOT NULL |
| `snapshot_payload` | `JSON` | NOT NULL，店铺分组、SKU、数量、价格、运费、服务政策版本 |
| `snapshot_hash` | `BINARY(32)` | NOT NULL |
| `invalidated_at` | `DATETIME(6)` | NULL |
| `invalid_reason` | `VARCHAR(64)` | NULL |

约束：`UNIQUE(checkout_session_id, snapshot_version)`、`UNIQUE(checkout_session_id, snapshot_hash)`。快照不含完整密码/Token，地址只保存引用；成交时另写不可变订单地址快照。

##### 3.7.6.5 结算数据失效规则

以下任一条件成立时，结算不得直接提交：

- `expires_at` 已到期；
- 结算会话已提交/取消；
- SKU 下架、冻结、库存不足或购买数量超限；
- SKU/Policy/Pricing Version 变化导致商品价、运费或售后条件改变；
- 收货地址已删除、不完整或不在配送范围；
- 店铺/用户状态不允许交易。

提交端点在同一请求内重新计价。如仅是价格/运费变化，返回新快照和差异，要求用户再次确认；不在用户不知情时自动提交更高金额。

#### 3.7.7 订单表

一次跨店结算创建一个 `trade_orders`，按店铺拆分为多个 `orders`。支付可针对合并交易单，履约/物流/售后以店铺订单和订单项为边界。

##### 3.7.7.1 trade_orders 合并交易单表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `trade_no` | `VARCHAR(32)` | UK，对外合并交易号 |
| `checkout_session_id` | `BIGINT UNSIGNED` | NULL，FK/UK；Checkout 在线期防重复消费，清理时 `ON DELETE SET NULL` |
| `checkout_no_snapshot` | `VARCHAR(40)` | NOT NULL，UK，下单时 Checkout 公开 ID 快照 |
| `checkout_snapshot_hash` | `BINARY(32)` | NOT NULL，最终确认的结算快照哈希 |
| `user_id` | `BIGINT UNSIGNED` | FK |
| `order_source` | `VARCHAR(16)` | `buy_now/cart`，从 Checkout 复制的不可变来源；历史查询不得回读短期 Checkout |
| `trade_status` | `VARCHAR(32)` | `pending_payment/paid/closed/partially_refunded/refunded` |
| `goods_amount` / `freight_amount` / `payable_amount` | `BIGINT UNSIGNED` | NOT NULL |
| `adjustment_amount` | `BIGINT SIGNED` | NOT NULL DEFAULT 0，各店铺订单改价之和 |
| `paid_amount` / `refunded_amount` | `BIGINT UNSIGNED` | NOT NULL DEFAULT 0 |
| `currency` | `CHAR(3)` | NOT NULL |
| `order_count` | `SMALLINT UNSIGNED` | NOT NULL |
| `expires_at` / `paid_at` / `closed_at` | `DATETIME(6)` | NOT NULL / NULL / NULL |

不变式：`checkout_no_snapshot` 唯一且不随短期 Checkout 清理而消失；各店铺订单金额及 `adjustment_amount` 之和等于合并交易单；`payable_amount = goods_amount + freight_amount + adjustment_amount` 且结果不得小于 0；`0 <= refunded_amount <= paid_amount <= payable_amount`。索引 `idx_trade_orders_user_time(user_id, created_at, id)`、`idx_trade_orders_status_expiry(trade_status, expires_at)`。

##### 3.7.7.2 orders 店铺订单表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `order_no` | `VARCHAR(32)` | UK，前端“我的订单”主键 |
| `trade_order_id` / `user_id` / `store_id` | `BIGINT UNSIGNED` | FK |
| `order_status` | `VARCHAR(32)` | `pending_payment/paid/pending_shipment/shipped/completed/cancelled/closed` |
| `payment_status` | `VARCHAR(32)` | `unpaid/processing/paid/partially_refunded/refunded` |
| `fulfillment_status` | `VARCHAR(32)` | `unfulfilled/partial/shipped/received` |
| `after_sale_status` | `VARCHAR(32)` | `none/in_progress/partial/completed` |
| `goods_amount` / `freight_amount` / `payable_amount` | `BIGINT UNSIGNED` | NOT NULL |
| `adjustment_amount` | `BIGINT SIGNED` | NOT NULL DEFAULT 0，订单级人工调整，可正可负 |
| `paid_amount` / `refunded_amount` | `BIGINT UNSIGNED` | NOT NULL DEFAULT 0 |
| `currency` | `CHAR(3)` | NOT NULL |
| `adjustment_reason_code` / `adjustment_reason` | `VARCHAR(64)` / `VARCHAR(500)` | NULL；非零改价时必填 |
| `adjusted_by` / `adjusted_at` | `BIGINT UNSIGNED` / `DATETIME(6)` | NULL，管理员与时间；非零改价时必填 |
| `buyer_remark` | `VARCHAR(500)` | NULL，存储冗余容量；用户 API 首版最多接受 200 个 Unicode 字符，Trim 后空串写 NULL，并经内容安全处理 |
| `policy_snapshot` | `JSON` | NOT NULL，下单时售后/配送策略版本 |
| `expires_at` / `paid_at` / `shipped_at` / `completed_at` / `closed_at` | `DATETIME(6)` | 按状态可空 |
| `user_hidden_at` | `DATETIME(6)` | NULL，仅允许符合策略的终态订单在用户侧隐藏 |

金额不变式：`payable_amount = goods_amount + freight_amount + adjustment_amount` 且结果不得小于 0。改价只允许在未支付且不存在活动支付尝试时通过专用命令执行；如已有支付尝试，必须先取得渠道确定关闭结果，结果未知时禁止改价。改价事务使用 3.6.4 的统一舍入策略把订单级调整额重新分摊到全部 Order Item，保证 `orders.adjustment_amount = SUM(order_items.adjustment_amount)`、`orders.payable_amount = SUM(order_items.payable_amount) + freight_amount`；任何订单项实付不得小于 0。随后重新计算 Trade Order、递增 Order/Trade/Item Version 并写 `order_operation_logs/system_audit_logs`，不得用通用 PATCH 修改金额。索引：`idx_orders_user_status_time(user_id, order_status, created_at, id)`、`idx_orders_store_status_time(store_id, order_status, created_at, id)`、`idx_orders_trade(trade_order_id)`。前端的“删除订单”只写 `user_hidden_at`，不物理删除交易记录；是否可隐藏统一由 `OrderHidePolicy` 判定。

##### 3.7.7.3 order_items 订单商品表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `order_item_no` | `VARCHAR(40)` | UK |
| `order_id` / `product_id` / `sku_id` | `BIGINT UNSIGNED` | NOT NULL，为历史追溯保留引用 |
| `product_no` / `sku_no` | `VARCHAR(40)` | NOT NULL，公开 ID 快照 |
| `product_name` / `sku_name` | `VARCHAR(255)` | NOT NULL，下单时快照 |
| `spec_snapshot` | `JSON` | NOT NULL |
| `image_object_key` | `VARCHAR(512)` | NULL，主图快照引用 |
| `quantity` | `INT UNSIGNED` | NOT NULL |
| `unit_price_amount` / `market_price_amount` | `BIGINT UNSIGNED` | NOT NULL |
| `gross_amount` / `payable_amount` | `BIGINT UNSIGNED` | NOT NULL |
| `adjustment_amount` | `BIGINT SIGNED` | NOT NULL DEFAULT 0，订单级改价分摊到本订单项的金额，可正可负；仅未支付受控改价命令可更新，支付后冻结 |
| `refunded_quantity` / `refunded_amount` | `INT UNSIGNED` / `BIGINT UNSIGNED` | NOT NULL DEFAULT 0 |
| `currency` | `CHAR(3)` | NOT NULL |
| `review_status` | `VARCHAR(16)` | `pending/reviewed/closed` |
| `after_sale_status` | `VARCHAR(16)` | `none/in_progress/completed` |

检查：`gross_amount = unit_price_amount * quantity`，`payable_amount = gross_amount + adjustment_amount`，且结果不得小于 0；同一订单全部订单项的 `adjustment_amount` 之和必须等于订单级调整额。`refunded_amount` 不得超过包含改价分摊后的订单项 `payable_amount`，已退数量不得超过购买数量。索引 `idx_order_items_order(order_id, id)`、`idx_order_items_sku(sku_id, created_at)`。

##### 3.7.7.4 order_addresses 订单地址快照表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `order_id` | `BIGINT UNSIGNED` | FK，UK，每店铺订单一份快照 |
| `source_address_no` | `VARCHAR(40)` | NULL，原地址公开 ID，不依赖原表存续 |
| `recipient_name_ciphertext` / `phone_ciphertext` | `VARBINARY(512)` | NOT NULL |
| `phone_last4` | `CHAR(4)` | NOT NULL |
| `country_code` | `CHAR(2)` | NOT NULL |
| `province_code` / `city_code` / `district_code` | `VARCHAR(32)` | NOT NULL |
| `address_ciphertext` | `VARBINARY(2048)` | NOT NULL |
| `postal_code` | `VARCHAR(16)` | NULL |
| `address_hash` | `BINARY(32)` | NOT NULL，快照完整性 |
| `key_version` | `SMALLINT UNSIGNED` | NOT NULL |

地址快照不提供用户编辑，密钥轮换除外。解密仅限本人展示、店铺履约、物流发货和受权售后场景。

##### 3.7.7.5 order_status_logs 订单状态流水表

追加型。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `order_id` | `BIGINT UNSIGNED` | NOT NULL |
| `state_dimension` | `VARCHAR(32)` | `order/payment/fulfillment/after_sale` |
| `from_status` / `to_status` | `VARCHAR(32)` | NULL / NOT NULL |
| `event_code` | `VARCHAR(64)` | NOT NULL |
| `actor_type` / `actor_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | NOT NULL / NULL |
| `reason` | `VARCHAR(500)` | NULL |
| `order_version` | `BIGINT UNSIGNED` | NOT NULL |
| `request_id` / `trace_id` | `VARCHAR(64)` | NULL |

索引：`idx_order_status_logs_order(order_id, created_at, id)`。由此表可重建状态时间线，但在线当前状态仍以 `orders` 为准。

##### 3.7.7.6 order_operation_logs 订单操作日志表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `operation_no` | `VARCHAR(40)` | UK |
| `order_id` | `BIGINT UNSIGNED` | NOT NULL |
| `operation_type` | `VARCHAR(64)` | `create/cancel/confirm_receive/delete_view/contact_store/amount_adjust` 等 |
| `actor_type` / `actor_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | NOT NULL / NULL |
| `request_payload_hash` | `BINARY(32)` | NULL |
| `result_status` | `VARCHAR(16)` | `success/rejected/failed` |
| `error_code` | `VARCHAR(64)` | NULL |
| `request_id` / `trace_id` | `VARCHAR(64)` | NOT NULL |

与状态流水的区别：Operation Log 记录用户/管理员“尝试做什么”，Status Log 记录“状态实际如何改变”。

##### 3.7.7.7 idempotency_records 幂等记录表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `scope` | `VARCHAR(64)` | 用例范围，如 `order.create` |
| `actor_type` / `actor_no` | `VARCHAR(16)` / `VARCHAR(64)` | NOT NULL |
| `idempotency_key` | `VARCHAR(128)` | NOT NULL |
| `request_hash` | `BINARY(32)` | NOT NULL，相同 Key 不允许不同请求 |
| `record_status` | `VARCHAR(16)` | `processing/succeeded/failed` |
| `resource_type` / `resource_no` | `VARCHAR(32)` / `VARCHAR(64)` | NULL，成功产物 |
| `response_code` | `SMALLINT UNSIGNED` | NULL |
| `response_snapshot` | `JSON` | NULL，仅存可安全重放的最小响应 |
| `error_code` | `VARCHAR(64)` | NULL |
| `expires_at` | `DATETIME(6)` | NOT NULL |

约束：`UNIQUE(scope, actor_type, actor_no, idempotency_key)`；索引 `idx_idempotency_expiry(expires_at)`。记录的创建与业务结果尽可能处于同一事务；卡在 `processing` 的记录使用 Lease/超时恢复，不盲目再次执行外部副作用。

#### 3.7.8 支付表

支付单表示一次支付尝试，一个合并交易单可因用户重试/切换渠道产生多个支付单，但成功入账总额不得超过应付。

##### 3.7.8.1 payments 支付单表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `payment_no` | `VARCHAR(32)` | UK |
| `trade_order_id` / `user_id` | `BIGINT UNSIGNED` | FK |
| `provider` / `payment_method` | `VARCHAR(32)` | NOT NULL，如 `wechat/pay` |
| `provider_trade_no` | `VARCHAR(128)` | NULL，渠道内唯一 |
| `payment_status` | `VARCHAR(32)` | `created/pending/succeeded/failed/closed/partially_refunded/refunded` |
| `requested_amount` / `paid_amount` / `refunded_amount` | `BIGINT UNSIGNED` | NOT NULL |
| `currency` | `CHAR(3)` | NOT NULL |
| `client_ip_hash` | `BINARY(32)` | NULL |
| `provider_request_id` | `VARCHAR(128)` | NULL |
| `expires_at` / `paid_at` / `closed_at` | `DATETIME(6)` | NOT NULL / NULL / NULL |
| `failure_code` / `failure_message` | `VARCHAR(64)` / `VARCHAR(500)` | NULL，仅脱敏信息 |

约束：`UNIQUE(provider, provider_trade_no)`（NULL 时不冲突）；`0 <= refunded_amount <= paid_amount <= requested_amount`。索引 `idx_payments_trade_status(trade_order_id, payment_status, created_at)`、`idx_payments_status_expiry(payment_status, expires_at)`。

##### 3.7.8.2 payment_events 支付事件表

追加型业务时间线。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `event_no` | `VARCHAR(40)` | UK |
| `payment_id` | `BIGINT UNSIGNED` | NOT NULL |
| `event_type` | `VARCHAR(64)` | `created/provider_requested/callback_succeeded/closed` 等 |
| `from_status` / `to_status` | `VARCHAR(32)` | NULL / NOT NULL |
| `amount` / `currency` | `BIGINT UNSIGNED` / `CHAR(3)` | NOT NULL |
| `source_type` / `source_no` | `VARCHAR(32)` / `VARCHAR(64)` | API/callback/reconciliation 及其 ID |
| `provider_occurred_at` | `DATETIME(6)` | NULL，渠道声称时间 |
| `trace_id` | `VARCHAR(64)` | NULL |

索引：`idx_payment_events_payment(payment_id, created_at, id)`。回调渠道时间不替代本系统 `created_at`。

##### 3.7.8.3 payment_callbacks 支付回调表

每次 HTTP 回调一行，包括重复/非法回调。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `callback_no` | `VARCHAR(40)` | UK |
| `provider` | `VARCHAR(32)` | NOT NULL |
| `provider_event_id` | `VARCHAR(128)` | NULL，渠道事件 ID |
| `payment_id` | `BIGINT UNSIGNED` | NULL，解析后关联 |
| `headers_hash` / `payload_hash` | `BINARY(32)` | NOT NULL |
| `payload_redacted` | `JSON` | NULL，限大小且移除密钥/个人信息 |
| `signature_status` | `VARCHAR(16)` | `valid/invalid/error` |
| `process_status` | `VARCHAR(16)` | `received/processed/duplicate/rejected/failed` |
| `attempt_count` | `SMALLINT UNSIGNED` | NOT NULL DEFAULT 0 |
| `processed_at` | `DATETIME(6)` | NULL |
| `error_code` / `last_error` | `VARCHAR(64)` / `VARCHAR(1000)` | NULL |
| `request_id` | `VARCHAR(64)` | NOT NULL |

约束：如渠道提供稳定 Event ID，则 `UNIQUE(provider, provider_event_id)`；否则使用经审核的业务复合键并以 Payload Hash 辅助去重。索引 `idx_payment_callbacks_status(process_status, created_at, id)`。

##### 3.7.8.4 payment_refunds 支付渠道退款表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `payment_refund_no` | `VARCHAR(32)` | UK，本系统渠道退款号 |
| `payment_id` | `BIGINT UNSIGNED` | FK |
| `refund_application_id` | `BIGINT UNSIGNED` | FK，业务售后单 |
| `provider` | `VARCHAR(32)` | NOT NULL，必须与原支付单渠道一致 |
| `provider_refund_no` | `VARCHAR(128)` | NULL，渠道唯一 |
| `refund_status` | `VARCHAR(32)` | `created/processing/succeeded/failed/closed` |
| `requested_amount` / `refunded_amount` | `BIGINT UNSIGNED` | NOT NULL |
| `currency` | `CHAR(3)` | NOT NULL |
| `idempotency_key` | `VARCHAR(128)` | NOT NULL，UK |
| `requested_at` / `succeeded_at` | `DATETIME(6)` | NOT NULL / NULL |
| `failure_code` / `failure_message` | `VARCHAR(64)` / `VARCHAR(500)` | NULL |

约束：`UNIQUE(provider, provider_refund_no)`（`provider_refund_no` 为 NULL 时不冲突）；`refunded_amount <= requested_amount`。一个售后单可因拆分支付或重试有多条渠道退款，成功合计不得超过批准金额。

##### 3.7.8.5 支付回调验签与幂等

```text
读取原始请求体（限大小）
  → 按渠道规则验证证书/签名/时间窗
  → 写 payment_callbacks（包括验签失败）
  → 以 provider_event_id/业务复合键去重
  → 根据本地 payment_no 查找支付单
  → 对比商户号、币种、金额、订单号
  → 锁定 payment/trade_order 执行单向状态迁移
  → 同事务写 payment_events、订单流水、库存确认、Outbox
  → 按渠道协议快速响应
```

验签失败不查信回调中声称的成功状态，并生成安全指标。已成功回调再到达时返回成功响应但不重复扣库存/发送通知。回调金额不符、未知支付单、本地已关闭但渠道成功等进入异常对账队列，不在回调线程中自作主张改写终态。

---

#### 3.7.9 物流表

一个店铺订单可拆成多个包裹，同一订单项也可分批发货。“订单已发货”由包裹和包裹商品数量派生，不以前端按钮为事实。

##### 3.7.9.1 shipments 物流包裹表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `shipment_no` | `VARCHAR(40)` | UK，本系统包裹 ID |
| `order_id` / `store_id` | `BIGINT UNSIGNED` | FK |
| `carrier_code` / `carrier_name` | `VARCHAR(32)` / `VARCHAR(64)` | NOT NULL |
| `tracking_no_ciphertext` | `VARBINARY(512)` | NOT NULL，物流单号密文 |
| `tracking_no_hash` | `BINARY(32)` | NOT NULL，归一化 HMAC |
| `tracking_no_masked` | `VARCHAR(64)` | NOT NULL，脱敏展示 |
| `shipment_status` | `VARCHAR(32)` | `created/picked_up/in_transit/delivered/exception/returned/closed/voided`；`created` 表示已确认发货而非草稿 |
| `provider_status` | `VARCHAR(64)` | NULL，渠道原状态 |
| `estimated_delivery_min_at` / `estimated_delivery_max_at` | `DATETIME(6)` | NULL，归一化预计送达时间范围；不得伪造 |
| `estimate_source` | `VARCHAR(32)` | NULL，`shipping_template/carrier` |
| `estimate_updated_at` | `DATETIME(6)` | NULL，最近一次预计时间更新时间 |
| `shipped_at` / `delivered_at` / `last_track_at` / `voided_at` | `DATETIME(6)` | NULL |
| `void_reason_code` / `void_reason` | `VARCHAR(64)` / `VARCHAR(1000)` | NULL，受控作废时必填 |
| `key_version` | `SMALLINT UNSIGNED` | NOT NULL |

约束：通过二进制业务键保证 `carrier_code + tracking_no_hash` 唯一（特殊物流渠道可经 ADR 允许复用）；预计时间必须同时为空或满足 `estimated_delivery_min_at <= estimated_delivery_max_at`，物流渠道更新时保留来源和接收时间，过期估计置空且不能继续当作实时承诺。首版不允许人工填写预计到达日期：初始范围只能来自订单配送模板，承运商返回更可靠结果后替换为 `carrier` 来源。索引 `idx_shipments_order(order_id, created_at)`、`idx_shipments_status_sync(shipment_status, last_track_at)`。首版不提供包裹草稿；创建成功即固化 `shipment_items`、推进履约数量并向用户展示发货事件。

##### 3.7.9.2 shipment_items 包裹商品表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `shipment_id` / `order_item_id` | `BIGINT UNSIGNED` | FK |
| `quantity` | `INT UNSIGNED` | NOT NULL，`> 0` |
| `created_at` | `DATETIME(6)` | NOT NULL |

约束：`UNIQUE(shipment_id, order_item_id)`；同一订单项的所有非 `voided` 包裹数量合计不得超过 `order_items.quantity`。分配数量创建后不可 PATCH；与订单履约状态在同一事务内更新。仅尚未揽收、渠道可确定撤销且无进行中售后的 `created` 包裹可用专用命令整包作废；作废事务原子恢复未履约数量、重算订单履约投影、追加日志与 Outbox，不删除包裹或原运单证据。

##### 3.7.9.3 shipment_tracks 物流轨迹表

追加型，允许渠道迟到事件，展示按业务时间排序。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `shipment_id` | `BIGINT UNSIGNED` | NOT NULL |
| `provider_event_id` | `VARCHAR(128)` | NULL |
| `track_status` | `VARCHAR(32)` | 归一化状态 |
| `provider_status` | `VARCHAR(64)` | NULL |
| `description` | `VARCHAR(1000)` | NOT NULL，经脱敏/过滤 |
| `location_text` | `VARCHAR(255)` | NULL，不保存额外收件人信息 |
| `occurred_at` | `DATETIME(6)` | NOT NULL，渠道业务时间 |
| `payload_hash` | `BINARY(32)` | NOT NULL |

约束：有事件 ID 时 `UNIQUE(shipment_id, provider_event_id)`，否则以 `(shipment_id, occurred_at, payload_hash)` 去重。索引 `idx_shipment_tracks_timeline(shipment_id, occurred_at, id)`。

##### 3.7.9.4 logistics_sync_logs 物流同步记录表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `shipment_id` | `BIGINT UNSIGNED` | NOT NULL |
| `sync_type` | `VARCHAR(16)` | `poll/webhook/reconcile` |
| `sync_status` | `VARCHAR(16)` | `success/no_change/retry/failed` |
| `provider_request_id` | `VARCHAR(128)` | NULL |
| `response_hash` | `BINARY(32)` | NULL |
| `track_count` | `SMALLINT UNSIGNED` | NOT NULL DEFAULT 0 |
| `attempt_count` / `duration_ms` | `SMALLINT UNSIGNED` / `INT UNSIGNED` | NOT NULL |
| `next_retry_at` | `DATETIME(6)` | NULL |
| `error_code` / `last_error` | `VARCHAR(64)` / `VARCHAR(1000)` | NULL，脱敏 |
| `trace_id` | `VARCHAR(64)` | NULL |

索引：`idx_logistics_sync_retry(sync_status, next_retry_at, id)`、`idx_logistics_sync_shipment(shipment_id, created_at)`。物流查询失败不回退包裹已知状态；持续失败进死信/告警。

#### 3.7.10 退款与售后表

“退款申请”是业务聚合，“渠道退款”是资金执行。业务审核通过不等于钱已到账，页面需分别展示售后状态与资金状态。

##### 3.7.10.1 refund_applications 退款申请表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `refund_no` | `VARCHAR(32)` | UK |
| `order_id` / `user_id` / `store_id` | `BIGINT UNSIGNED` | FK |
| `refund_type` | `VARCHAR(32)` | `refund_only/return_and_refund` |
| `refund_status` | `VARCHAR(32)` | `submitted/merchant_review/approved/rejected/waiting_return/returning/received/refunding/succeeded/closed/cancelled` |
| `reason_code` / `reason_detail` | `VARCHAR(64)` / `VARCHAR(1000)` | NOT NULL |
| `contact_phone_ciphertext` | `VARBINARY(512)` | NOT NULL，申请时联系方式快照 |
| `contact_phone_last4` | `CHAR(4)` | NOT NULL，仅用于脱敏展示 |
| `contact_key_version` | `SMALLINT UNSIGNED` | NOT NULL |
| `requested_amount` / `approved_amount` / `refunded_amount` | `BIGINT UNSIGNED` | NOT NULL，后两者默认 0 |
| `currency` | `CHAR(3)` | NOT NULL |
| `eligibility_rule_id` / `eligibility_rule_version` | `BIGINT UNSIGNED` / `INT UNSIGNED` | NOT NULL，命中规则 |
| `eligibility_snapshot` | `JSON` | NOT NULL，申请时的资格判定快照 |
| `review_mode` | `VARCHAR(16)` | `automatic/manual` |
| `reviewed_by` / `reviewed_at` | `BIGINT UNSIGNED` / `DATETIME(6)` | NULL |
| `merchant_deadline_at` / `return_deadline_at` | `DATETIME(6)` | NULL |
| `completed_at` / `cancelled_at` | `DATETIME(6)` | NULL |

检查：`0 <= refunded_amount <= approved_amount <= requested_amount`。索引 `idx_refunds_user_status(user_id, refund_status, created_at, id)`、`idx_refunds_store_status(store_id, refund_status, created_at, id)`、`idx_refunds_order(order_id, created_at)`。一个订单可有多次售后，资格与占用以 `refund_items.order_item_id` 为最小维度；禁止建立“一个订单只能有一条活动申请”的唯一约束。

##### 3.7.10.2 refund_items 退款商品表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `refund_application_id` / `order_item_id` | `BIGINT UNSIGNED` | FK |
| `requested_quantity` / `approved_quantity` / `received_quantity` | `INT UNSIGNED` | NOT NULL，后两者默认 0 |
| `requested_amount` / `approved_amount` | `BIGINT UNSIGNED` | NOT NULL |
| `condition_status` | `VARCHAR(32)` | NULL，`unopened/opened/damaged/missing` 等收货检查结果 |
| `inspection_remark` | `VARCHAR(500)` | NULL |

约束：`UNIQUE(refund_application_id, order_item_id)`；批准数量不超申请数量，同一订单项“已成功 + 活动申请占用”数量不超过购买数量，相应金额不超过该订单项含改价分摊的可退实付金额。活动状态为 `submitted/merchant_review/approved/waiting_return/returning/received/refunding`，`rejected/cancelled/closed` 不占用，`succeeded` 进入 `order_items.refunded_quantity/refunded_amount`；外部资金结果未知仍保持 `refunding` 占用。

不能用普通唯一索引表达“部分数量区间不重叠”。创建申请时事务按 `order_item_id ASC` 锁定 `order_items`，汇总同项相关 `refund_items + refund_applications.refund_status` 后执行条件校验，再插入本次行；因此同订单不同商品可以并发，同一商品的剩余容量必须串行分配。资格接口返回的占用仅是快照，创建事务是最终防线。拒绝/撤销/关闭后再次申请总是新建主申请和明细行，旧记录及事件不可复用或改写。

##### 3.7.10.3 refund_evidences 售后凭证表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `evidence_no` | `VARCHAR(40)` | UK |
| `refund_application_id` | `BIGINT UNSIGNED` | FK |
| `refund_appeal_id` | `BIGINT UNSIGNED` | NULL，FK；申诉阶段补充凭证时填写 |
| `submitted_by_type` / `submitted_by_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | `user/merchant/admin` |
| `evidence_type` | `VARCHAR(16)` | `image/video/document/text` |
| `object_key` | `VARCHAR(512)` | NULL，严格私有 Bucket |
| `text_content` | `VARCHAR(2000)` | NULL，文本凭证 |
| `sha256` | `BINARY(32)` | NULL |
| `mime_type` / `size_bytes` | `VARCHAR(128)` / `BIGINT UNSIGNED` | NULL |
| `scan_status` | `VARCHAR(16)` | `pending/safe/rejected` |
| `evidence_status` | `VARCHAR(16)` | `active/withdrawn/hidden` |

只有安全扫描通过的文件才可供审核人员访问；每次查看/下载凭证写安全审计。`refund_appeal_id` 非空时必须属于同一 `refund_application_id`，不能把其他售后单凭证挂入当前申诉。

##### 3.7.10.4 refund_events 售后状态流水表

追加型。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `event_no` | `VARCHAR(40)` | UK |
| `refund_application_id` | `BIGINT UNSIGNED` | NOT NULL |
| `event_type` | `VARCHAR(64)` | NOT NULL |
| `from_status` / `to_status` | `VARCHAR(32)` | NULL / NOT NULL |
| `actor_type` / `actor_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | NOT NULL / NULL |
| `reason_code` / `remark` | `VARCHAR(64)` / `VARCHAR(1000)` | NULL |
| `snapshot` | `JSON` | NULL，当次审核/收货/资金结果的最小快照 |
| `refund_version` | `BIGINT UNSIGNED` | NOT NULL |
| `trace_id` | `VARCHAR(64)` | NULL |

索引：`idx_refund_events_application(refund_application_id, created_at, id)`。

##### 3.7.10.5 refund_shipments 退货物流表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `refund_application_id` | `BIGINT UNSIGNED` | FK |
| `carrier_code` / `carrier_name` | `VARCHAR(32)` / `VARCHAR(64)` | NOT NULL |
| `tracking_no_ciphertext` / `tracking_no_hash` | `VARBINARY(512)` / `BINARY(32)` | NOT NULL |
| `tracking_no_masked` | `VARCHAR(64)` | NOT NULL |
| `shipment_status` | `VARCHAR(32)` | `submitted/in_transit/delivered/received/exception` |
| `shipped_at` / `delivered_at` / `received_at` | `DATETIME(6)` | NULL |
| `key_version` | `SMALLINT UNSIGNED` | NOT NULL |

默认一个售后单一条主退货物流，`UNIQUE(refund_application_id)`；如业务允许多包裹，删除该唯一约束并增加包裹明细表。

##### 3.7.10.6 refund_payment_records 退款支付记录表

追加型对账关联表，不取代 `payment_refunds`。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `refund_application_id` / `payment_refund_id` | `BIGINT UNSIGNED` | FK |
| `payment_id` | `BIGINT UNSIGNED` | FK |
| `record_type` | `VARCHAR(32)` | `requested/provider_accepted/succeeded/failed/reconciled` |
| `amount` / `currency` | `BIGINT UNSIGNED` / `CHAR(3)` | NOT NULL |
| `provider_event_no` | `VARCHAR(128)` | NULL |
| `record_status` | `VARCHAR(16)` | `success/failed/pending` |
| `occurred_at` | `DATETIME(6)` | NOT NULL |
| `trace_id` | `VARCHAR(64)` | NULL |

可通过本表对账“批准退款金额、渠道请求金额、实际成功金额”；任何不相等进人工对账队列。

##### 3.7.10.7 售后资格规则表

建议表名 `after_sale_eligibility_rules`，将可配置业务窗口与代码中的强不变式分开。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `rule_no` | `VARCHAR(40)` | UK |
| `rule_name` | `VARCHAR(128)` | NOT NULL |
| `scope_type` / `scope_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | `platform/category/store/product`，平台为 0 |
| `refund_type` | `VARCHAR(32)` | 适用售后类型 |
| `rule_version` | `INT UNSIGNED` | NOT NULL |
| `priority` | `INT` | NOT NULL，越小越优先 |
| `condition_config` | `JSON` | NOT NULL：可申请订单状态、收货后天数、是否要求凭证、不退类目等 |
| `auto_decision_config` | `JSON` | NOT NULL：自动批准上限、风险阈值等 |
| `rule_status` | `VARCHAR(16)` | `draft/effective/expired` |
| `effective_at` / `expired_at` | `DATETIME(6)` | NULL |

约束：`UNIQUE(scope_type, scope_id, refund_type, rule_version)`。规则发布后不原地修改；新建版本并保留旧版本。资格判定输出 `eligible/reason_code/rule_no/rule_version/evidence_requirements/deadline`，并存入申请快照。

代码中不可被 JSON 覆盖的强不变式包括：申请人必须是订单用户；退款币种与订单一致；累计金额/数量不超可退上限；支付未成功不发起渠道退款；终态申请不可逆向改回审核中。

##### 3.7.10.8 自动处理与人工审核边界

只有同时满足以下条件才允许自动批准：规则版本已发布；订单/支付/数量校验全部通过；金额不超过配置上限；无风控、争议、重复申请、异常物流或凭证矛盾；决策输入可完整快照和解释。

以下任一情况必须转人工：

- 高金额、高频售后、多账号/设备关联等风控命中；
- 已发货但申请“仅退款”且店铺不支持；
- 超出政策窗口但存在平台介入理由；
- 凭证安全扫描失败、图文矛盾或商家拒绝；
- 渠道退款金额不符、多次失败或订单资金对账不平。

AI 可做资料摘要、规则解释和补充材料提示，不能自主改写退款金额或越过确定性规则。高风险操作必须由授权管理员审批，且记录决策输入、规则版本、操作者和理由。

##### 3.7.10.9 refund_appeals 退款申诉与平台复核表

退款申请被拒绝后，用户可以创建新的平台申诉资源；申诉不修改原退款事件，不把原申请从终态重新改回审核中。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `appeal_no` | `VARCHAR(40)` | UK |
| `refund_application_id` / `user_id` / `store_id` | `BIGINT UNSIGNED` | FK |
| `appeal_status` | `VARCHAR(24)` | `submitted/reviewing/upheld/rejected/cancelled/closed` |
| `reason_code` / `reason_detail` | `VARCHAR(64)` / `VARCHAR(2000)` | NOT NULL |
| `reviewed_by` / `reviewed_at` | `BIGINT UNSIGNED` / `DATETIME(6)` | NULL，平台审核员 |
| `resolution_code` / `resolution_detail` | `VARCHAR(64)` / `VARCHAR(2000)` | NULL |
| `review_deadline_at` / `completed_at` | `DATETIME(6)` | NULL |

首版同一退款申请最多一条未终结申诉，通过 Active 生成列唯一约束保证。申诉成立后创建受审计的纠错、补偿或重新退款命令，不直接覆盖原拒绝事件和已发生资金事实。

##### 3.7.10.10 refund_appeal_events 退款申诉事件表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `appeal_id` | `BIGINT UNSIGNED` | NOT NULL |
| `event_type` | `VARCHAR(64)` | NOT NULL |
| `from_status` / `to_status` | `VARCHAR(24)` | NULL / NOT NULL |
| `actor_type` / `actor_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | NOT NULL / NULL |
| `reason_code` / `remark` | `VARCHAR(64)` / `VARCHAR(1000)` | NULL |
| `appeal_version` | `BIGINT UNSIGNED` | NOT NULL |
| `trace_id` | `VARCHAR(64)` | NULL |

该表追加写，索引 `idx_refund_appeal_events(appeal_id, created_at, id)`；创建、补证、受理、决定、撤销和补偿动作均写事件与 Outbox。

#### 3.7.11 评价表

##### 3.7.11.1 reviews 商品评价表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `review_no` | `VARCHAR(40)` | UK |
| `order_id` / `order_item_id` / `user_id` / `store_id` / `product_id` / `sku_id` | `BIGINT UNSIGNED` | NOT NULL |
| `rating` | `TINYINT UNSIGNED` | NOT NULL，`1..5` |
| `content` | `VARCHAR(500)` | NULL，过滤后文本，与前端和 API 字数上限一致 |
| `is_anonymous` | `BOOLEAN` | NOT NULL DEFAULT FALSE |
| `review_status` | `VARCHAR(16)` | `pending/published/hidden/rejected` |
| `moderation_status` | `VARCHAR(16)` | `pending/passed/blocked/manual` |
| `published_at` / `hidden_at` | `DATETIME(6)` | NULL |
| `helpful_count` | `INT UNSIGNED` | NOT NULL DEFAULT 0，可重建派生值 |

约束：`UNIQUE(order_item_id)`，一个订单项只有一条首次评价；必须由订单用户在评价窗口内创建。索引 `idx_reviews_product_published(product_id, review_status, published_at, id)`、`idx_reviews_user_time(user_id, created_at, id)`。匿名只影响公开展示，不去除后台权限内的用户关联。`rejected` 表示首次公开前未通过审核，`hidden` 表示曾发布内容被平台屏蔽；用户主动隐藏订单/页面不改变评价事实。普通治理不得物理删除正文；确因法律、隐私或安全义务必须清除时，由独立合规流程加密擦除/匿名化正文与媒体，治理记录只保留最小必要 Hash、依据、批准人和时间，不提供常规管理员按钮。

分阶段迁移约束：第三阶段为支持公开评价只读页，可先创建 `order_id`、`order_item_id` 业务引用列，但在订单表尚未落库前不得建立伪外键；第四阶段创建 `orders`、`order_items` 后，须通过后续迁移校验存量引用并补齐真实 FK 与 `UNIQUE(order_item_id)` 约束。生产上线时禁止长期停留在“有引用列、无外键”的过渡状态。

##### 3.7.11.2 review_images 评价图片表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `review_id` | `BIGINT UNSIGNED` | FK |
| `object_key` | `VARCHAR(512)` | NOT NULL |
| `sha256` | `BINARY(32)` | NOT NULL |
| `width` / `height` / `sort_order` | `INT UNSIGNED` | NOT NULL |
| `scan_status` / `image_status` | `VARCHAR(16)` | `pending/safe/rejected` / `active/hidden` |

索引：`idx_review_images_review(review_id, sort_order, id)`。首版每条评价最多 6 张图片，数量由 Service 再校验，公开前必须安全扫描通过。

##### 3.7.11.3 review_replies 商家回复表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `review_id` / `store_id` | `BIGINT UNSIGNED` | FK |
| `replier_user_id` | `BIGINT UNSIGNED` | FK，需有店铺权限 |
| `content` | `VARCHAR(2000)` | NOT NULL |
| `reply_status` | `VARCHAR(16)` | `published/hidden` |
| `published_at` / `hidden_at` | `DATETIME(6)` | NULL |

默认每条评价一条当前活跃官方回复，通过生成列唯一约束。修改回复需保留管理员/店铺操作日志。

##### 3.7.11.4 review_append_records 追评表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `review_id` / `user_id` | `BIGINT UNSIGNED` | FK |
| `content` | `VARCHAR(500)` | NOT NULL |
| `append_status` | `VARCHAR(16)` | `pending/published/hidden/rejected` |
| `moderation_status` | `VARCHAR(16)` | NOT NULL |
| `published_at` | `DATETIME(6)` | NULL |

约束：`UNIQUE(review_id)`，默认只允许一次追评；追评窗口与评价展示规则由 Domain 校验。如追评支持图片，`review_images` 需增加 `owner_type/owner_id` 或建立独立 `review_append_images`，不将图片无引用塞入 JSON。

##### 3.7.11.5 review_moderation_records 评价治理记录表

追加型，保存平台/店铺对主评价、追评和商家回复的受控治理决定，不能用覆盖正文或物理删除代替。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `moderation_no` | `VARCHAR(40)` | UK |
| `target_type` / `target_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | `review/append/reply` 及目标内部 ID |
| `store_id` | `BIGINT UNSIGNED` | NOT NULL，目标店铺 Scope 快照 |
| `action` | `VARCHAR(16)` | `hide/restore/reject/escalate` |
| `rule_code` / `reason` | `VARCHAR(64)` / `VARCHAR(1000)` | NOT NULL，规则条款和安全说明 |
| `operator_user_id` | `BIGINT UNSIGNED` | NOT NULL，FK |
| `permission_code` | `VARCHAR(128)` | NOT NULL，当时权限快照 |
| `target_version_before` / `target_version_after` | `BIGINT UNSIGNED` | NOT NULL |
| `request_id` / `trace_id` | `VARCHAR(64)` | NOT NULL |

索引：`idx_review_moderation_target(target_type, target_id, created_at, id)`、`idx_review_moderation_store_time(store_id, created_at, id)`。治理事务更新目标可见状态、写本记录与 Outbox；恢复产生新记录，不删除原处置历史。商家仅能在本店范围提交回复或举报/升级，平台屏蔽/恢复需要独立权限。

#### 3.7.12 消息与人工客服表

会话分为 `exclusive`（专属客服）和 `store`（店铺客服）。完整消息以 MySQL 为准；Redis 只保存在线、未读和热上下文，PostgreSQL 只保存摘要/记忆/Checkpoint。

##### 3.7.12.1 conversations 会话表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `conversation_no` | `VARCHAR(40)` | UK |
| `conversation_type` | `VARCHAR(16)` | `exclusive/store` |
| `user_id` | `BIGINT UNSIGNED` | FK |
| `store_id` | `BIGINT UNSIGNED` | NULL；`store` 必填，`exclusive` 必为 NULL |
| `conversation_status` | `VARCHAR(16)` | `active/human_pending/human_active/closed` |
| `last_message_id` | `BIGINT UNSIGNED` | NULL，冗余指针 |
| `last_message_at` | `DATETIME(6)` | NULL |
| `last_sequence_no` | `BIGINT UNSIGNED` | NOT NULL DEFAULT 0，会话内消息序号分配器 |
| `user_hidden_at` | `DATETIME(6)` | NULL，店铺会话用户侧删除/隐藏 |
| `human_ticket_id` | `BIGINT UNSIGNED` | NULL，当前人工工单 |
| `exclusive_user_key` | `BIGINT UNSIGNED GENERATED` | 专属会话时为 `user_id`，否则 NULL |
| `store_user_key` | `BIGINT UNSIGNED GENERATED` | 店铺会话时为 `user_id`，否则 NULL |

约束：`UNIQUE(exclusive_user_key)`、`UNIQUE(store_user_key, store_id)`；检查会话类型与 `store_id` 一致。索引 `idx_conversations_user_last(user_id, user_hidden_at, last_message_at, id)`、`idx_conversations_store_status(store_id, conversation_status, last_message_at)`。

##### 3.7.12.2 messages 消息表

追加为主；撤回/隐藏只修改可见性字段，不覆盖审计时间线。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `message_no` | `VARCHAR(40)` | UK |
| `conversation_id` | `BIGINT UNSIGNED` | NOT NULL |
| `sequence_no` | `BIGINT UNSIGNED` | NOT NULL，会话内单调序号 |
| `client_message_no` | `VARCHAR(40)` | NULL，客户端生成的 `cmsg_` 前缀公开幂等 ID |
| `sender_type` | `VARCHAR(16)` | `user/agent/human/system/tool` |
| `sender_id` | `BIGINT UNSIGNED` | NULL，用户/人工客服 ID |
| `agent_version_id` | `BIGINT UNSIGNED` | NULL，AI 消息所用版本 |
| `message_type` | `VARCHAR(32)` | `text/image/product_card/order_card/logistics_card/refund_card/system` |
| `text_content` | `TEXT` | NULL，普通文本 |
| `content_payload` | `JSON` | NULL，版本化卡片/工具结果的安全快照 |
| `reply_to_message_id` | `BIGINT UNSIGNED` | NULL |
| `ai_run_no` | `VARCHAR(40)` | NULL，关联 Agent Run/可观测记录 |
| `message_status` | `VARCHAR(16)` | `sending/sent/failed/recalled/hidden` |
| `moderation_status` | `VARCHAR(16)` | `not_required/pending/passed/blocked` |
| `sent_at` / `recalled_at` | `DATETIME(6)` | NOT NULL / NULL |

约束：`UNIQUE(conversation_id, sequence_no)`、`UNIQUE(conversation_id, client_message_no)`（NULL 除外）。索引 `idx_messages_conversation_timeline(conversation_id, sequence_no)`、`idx_messages_ai_run(ai_run_no)`。发送事务锁定 Conversation、递增 `last_sequence_no` 后分配序号；会话展示使用 `(conversation_id, sequence_no)` 游标，不使用大 OFFSET。卡片 Payload 是展示快照，点击查订单/物流/退款仍通过授权 API 获取实时状态。

##### 3.7.12.3 message_reads 消息已读记录表

不为每条消息和每个参与者创建一行，而是保存“会话参与者最后已读位点”。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `conversation_id` | `BIGINT UNSIGNED` | FK |
| `reader_type` | `VARCHAR(16)` | `user/human/store` |
| `reader_id` | `BIGINT UNSIGNED` | NOT NULL，用户/客服/店铺 ID |
| `last_read_message_id` | `BIGINT UNSIGNED` | NOT NULL，必须属于同会话 |
| `last_read_sequence_no` | `BIGINT UNSIGNED` | NOT NULL，与目标消息序号一致 |
| `last_read_at` | `DATETIME(6)` | NOT NULL |

约束：`UNIQUE(conversation_id, reader_type, reader_id)`。更新只允许 Read Cursor 单调增大，且 Message ID、Sequence 必须指向同一条目标消息；Redis 未读计数丢失时可从位点与消息表重建。

##### 3.7.12.4 conversation_contexts 会话业务上下文表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `context_no` | `VARCHAR(40)` | UK |
| `conversation_id` | `BIGINT UNSIGNED` | FK |
| `context_type` | `VARCHAR(32)` | `product/order/shipment/refund/store/checkout_store_group` |
| `resource_no` | `VARCHAR(64)` | NOT NULL，公开资源 ID |
| `resource_version` | `VARCHAR(64)` | NULL，Checkout/政策等可变资源的权威版本；静态主体可空 |
| `resource_version_key` | `VARCHAR(64) GENERATED` | `COALESCE(resource_version, '@static')`，用于 NULL 安全唯一约束 |
| `context_version` | `BIGINT UNSIGNED` | NOT NULL，同会话同类型每次激活/替换单调递增，Agent Run 固定该值 |
| `context_source` | `VARCHAR(16)` | `user_click/tool/system` |
| `display_snapshot` | `JSON` | NULL，最小脱敏展示快照 |
| `context_status` | `VARCHAR(16)` | `active/inactive/expired` |
| `active_context_key` | `BINARY(32) GENERATED` | Active 时为 `conversation_id + context_type` 的规范 Hash，否则 NULL |
| `scope_snapshot_hash` | `BINARY(32)` | NOT NULL，绑定校验时的 User/Store/Resource 关系，不保存敏感原文 |
| `last_validated_at` | `DATETIME(6)` | NOT NULL，最近一次权威归属校验时间 |
| `attached_at` / `expires_at` | `DATETIME(6)` | NOT NULL / NULL |
| `attached_by_message_id` | `BIGINT UNSIGNED` | NULL |

约束：`UNIQUE(conversation_id, context_type, resource_no, resource_version_key)`、`UNIQUE(active_context_key)`；索引 `idx_contexts_conversation_status(conversation_id, context_status, attached_at)`。同一会话同一 Context Type 至多一个 Active；切换订单、商品或 Checkout Version 时在一个事务中锁定 Conversation、把旧 Active 置为 `inactive`、激活/创建新 Context 并递增 `context_version`。本表只表示当前关注主体，不单独授予访问权；Agent/人工每次读取仍重新校验当前用户、会话店铺、资源归属、Active 状态和 Version。订单卡片可填写 `attached_by_message_id` 作为来源证据，但 NULL 的订单入口上下文同样有效。

`checkout_store_group` 使用 `resource_no=checkout_no`、`resource_version=checkout_sessions.version`，店铺范围来自 Conversation 的 `store_id`；`display_snapshot` 只允许该店铺组内的公开商品/SKU 名称、规格和数量。`expires_at` 不得晚于 Checkout 到期时间。Checkout 重新计价、成功消费、过期或放弃时通过同步校验与 Outbox 使旧 Context 失活/过期；即使异步事件延迟，Gateway 发现 Checkout Version、状态或过期时间不匹配也必须 Fail Closed。严禁在 Snapshot/Scope Hash 原文中写入其他店铺组、完整地址、手机号、优惠明细、支付偏好或跨店总额。

##### 3.7.12.5 human_service_tickets 人工服务工单表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `ticket_no` | `VARCHAR(32)` | UK |
| `conversation_id` / `user_id` | `BIGINT UNSIGNED` | FK |
| `store_id` | `BIGINT UNSIGNED` | NULL，店铺客服必填 |
| `ticket_type` | `VARCHAR(32)` | `general/order/logistics/refund/complaint` |
| `priority` | `VARCHAR(16)` | `low/normal/high/urgent` |
| `ticket_status` | `VARCHAR(32)` | `queued/assigned/active/waiting_user/resolved/closed` |
| `queue_code` | `VARCHAR(64)` | NOT NULL，平台/店铺技能队列 |
| `handoff_summary` | `VARCHAR(2000)` | NOT NULL，经授权与脱敏、供本工单处理使用的问题摘要 |
| `handoff_message_refs` | `JSON` | NOT NULL，必要且用户可见的消息公开 ID、Sequence 范围及用途，不复制消息正文 |
| `handoff_policy_version` | `VARCHAR(40)` | NOT NULL，生成摘要所用脱敏/授权策略版本 |
| `source` | `VARCHAR(16)` | `user/agent/system/admin` |
| `current_assignee_user_id` | `BIGINT UNSIGNED` | NULL |
| `sla_due_at` | `DATETIME(6)` | NULL |
| `waiting_started_at` | `DATETIME(6)` | NULL，进入等待用户时间 |
| `sla_remaining_seconds` | `INT UNSIGNED` | NULL，等待前剩余 SLA，用于恢复而不重置 |
| `waiting_reason_code` | `VARCHAR(64)` | NULL，进入等待用户时的结构化原因 |
| `resolved_at` / `closed_at` | `DATETIME(6)` | NULL |
| `resolution_code` / `resolution_summary` | `VARCHAR(64)` / `VARCHAR(1000)` | NULL，结构化结果与可向用户/后续 Agent 暴露的脱敏处理摘要 |
| `resolution_note` | `VARCHAR(1000)` | NULL，仅人工内部备注，不进入普通消息、Agent Context、Memory 或用户导出 |

索引：`idx_tickets_queue_status(queue_code, ticket_status, priority, created_at)`、`idx_tickets_assignee(current_assignee_user_id, ticket_status, updated_at)`。一个会话同时最多一个非终态工单，使用活跃生成列唯一约束。`handoff_message_refs` 只保存必要引用和授权用途，读取引用消息时仍重新校验 Ticket、Assignment、RBAC、Queue/Store Scope 与业务理由；不得借引用扩大到整段会话。`resolution_summary` 与 `resolution_note` 必须由不同 DTO 白名单投影，禁止依赖前端隐藏字段实现隔离。

##### 3.7.12.6 human_service_assignments 客服分配表

追加型，记录每次分配/转派。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `ticket_id` | `BIGINT UNSIGNED` | NOT NULL |
| `assignee_user_id` | `BIGINT UNSIGNED` | NOT NULL，客服用户 |
| `assignment_type` | `VARCHAR(16)` | `auto/manual/transfer` |
| `assigned_by_type` / `assigned_by_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | NOT NULL / NULL |
| `assignment_status` | `VARCHAR(16)` | `assigned/accepted/rejected/released/completed` |
| `assigned_at` / `accepted_at` / `ended_at` | `DATETIME(6)` | NOT NULL / NULL / NULL |
| `end_reason` | `VARCHAR(64)` | NULL |

一个工单同时最多一个活跃分配；用生成列唯一约束保证，不只依赖队列消费者互斥。客服必须具有工单作用域权限。

##### 3.7.12.7 conversation_status_logs 会话状态流水表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `conversation_id` | `BIGINT UNSIGNED` | NOT NULL |
| `from_status` / `to_status` | `VARCHAR(16)` | NULL / NOT NULL |
| `event_type` | `VARCHAR(64)` | `created/human_requested/assigned/resolved/hidden/reopened` 等 |
| `actor_type` / `actor_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | NOT NULL / NULL |
| `ticket_id` | `BIGINT UNSIGNED` | NULL |
| `reason` | `VARCHAR(500)` | NULL |
| `conversation_version` | `BIGINT UNSIGNED` | NOT NULL |
| `trace_id` | `VARCHAR(64)` | NULL |

索引：`idx_conversation_logs_conversation(conversation_id, created_at, id)`。为 `(conversation_id, event_type, conversation_version)` 建唯一约束，保证并发 Reopen 只写一条事件；`event_type=reopened` 必须满足 `from_status=closed AND to_status=active`。“用户删除店铺会话”仅写 `hidden` 事件并更新 `user_hidden_at`，`from_status/to_status` 均保持当前会话状态，不级联删除工单和审计。

##### 3.7.12.8 专属客服唯一会话约束

每个用户全局只有一个专属客服会话，由 `exclusive_user_key` 生成列唯一索引保证：

```sql
exclusive_user_key BIGINT UNSIGNED
  GENERATED ALWAYS AS (
    CASE WHEN conversation_type = 'exclusive' THEN user_id ELSE NULL END
  ) STORED,
UNIQUE KEY uk_conversations_exclusive_user (exclusive_user_key)
```

用户首次进入“消息”或注册初始化时可幂等创建；并发创建以唯一约束收敛后读取已有会话。专属客服固定置顶属于前端展示规则，不需在表中写可修改的 `is_pinned`；用户不能删除/隐藏该系统会话。

##### 3.7.12.9 店铺会话唯一约束

每个“用户 + 店铺”只有一个店铺会话：

```sql
store_user_key BIGINT UNSIGNED
  GENERATED ALWAYS AS (
    CASE WHEN conversation_type = 'store' THEN user_id ELSE NULL END
  ) STORED,
UNIQUE KEY uk_conversations_store_user (store_user_key, store_id)
```

无论用户从商品页“客服”、店铺页或订单页“联系商家”进入，都通过同一 `get_or_create_store_conversation(user_id, store_id)` 用例。若 `user_hidden_at` 非空，再次进入时恢复可见并将新业务主体写入 `conversation_contexts`，不创建第二个会话。

##### 3.7.12.10 human_service_internal_notes 人工客服内部备注表

追加型，内部备注不是会话消息，用户、Agent、普通消息接口和会话导出均不可见。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `note_no` | `VARCHAR(40)` | UK |
| `ticket_id` | `BIGINT UNSIGNED` | FK `human_service_tickets.id` |
| `author_user_id` | `BIGINT UNSIGNED` | FK，当前客服/主管 |
| `store_id` | `BIGINT UNSIGNED` | NULL，店铺工单时保存 Scope 快照 |
| `note_type` | `VARCHAR(16)` | `handling/transfer/risk/resolution` |
| `content_ciphertext` | `VARBINARY(4096)` | NOT NULL，受控加密正文 |
| `content_hash` | `BINARY(32)` | NOT NULL，完整性/去重辅助，不用于全文搜索 |
| `visibility_scope` | `VARCHAR(16)` | `current_queue/supervisors/platform_escalation` |
| `key_version` | `SMALLINT UNSIGNED` | NOT NULL |
| `request_id` / `trace_id` | `VARCHAR(64)` | NOT NULL |

索引：`idx_internal_notes_ticket_time(ticket_id, created_at, id)`、`idx_internal_notes_store_time(store_id, created_at, id)`。写入前校验当前工单关系、队列和 Purpose；读取需要独立 `support:internal_notes_read` 权限并记录审计。内部备注不得复制密码、验证码、支付凭证、完整地址、隐藏推理或无必要 PII；不进入 `messages`、对话摘要、长期记忆、RAG、Agent Context 或用户数据导出。

##### 3.7.12.11 human_service_ticket_events 人工客服工单状态事件表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `event_no` | `VARCHAR(40)` | UK |
| `ticket_id` | `BIGINT UNSIGNED` | FK `human_service_tickets.id` |
| `event_type` | `VARCHAR(32)` | `created/claimed/activated/waiting_user/resumed/transferred/resolved/closed` |
| `from_status` / `to_status` | `VARCHAR(32)` | NULL / NOT NULL |
| `actor_type` / `actor_user_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | `user/agent/human/system` / NULL |
| `reason_code` / `reason` | `VARCHAR(64)` / `VARCHAR(1000)` | NULL，等待/转派/结束时必填 |
| `sla_due_at_before` / `sla_due_at_after` | `DATETIME(6)` | NULL，SLA 暂停/恢复快照 |
| `ticket_version` | `BIGINT UNSIGNED` | NOT NULL |
| `request_id` / `trace_id` | `VARCHAR(64)` | NOT NULL |

约束：`UNIQUE(event_no)`；索引 `idx_human_ticket_events_ticket(ticket_id, created_at, id)`。`active → waiting_user` 暂停客服响应 SLA 并保存剩余时长；用户发送新消息或客服执行恢复命令时以条件更新进入 `active`、恢复 SLA，重复事件不得重复扣减/累加时长。

---

#### 3.7.13 AI 配置与业务审计表

本组表保存 AI 运行“允许做什么、当时运行了哪个版本、用户是否授权、高风险动作是否确认、最终业务结果是什么”。Prompt、Graph、Skill 指令和 Tool Schema 发布后必须版本不可变，不在已发布行上直接覆盖。

##### 3.7.13.1 ai_agent_definitions Agent 定义表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `agent_no` | `VARCHAR(40)` | UK |
| `agent_code` / `agent_name` | `VARCHAR(64)` / `VARCHAR(128)` | NOT NULL，`agent_code` UK |
| `agent_type` | `VARCHAR(32)` | `exclusive_service/store_service/specialist` |
| `scope_type` / `scope_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | `platform/store`，平台为 0 |
| `description` | `VARCHAR(1000)` | NULL |
| `agent_status` | `VARCHAR(16)` | `draft/active/disabled/archived` |
| `current_version_id` | `BIGINT UNSIGNED` | NULL，当前发布版本 |
| `created_by` / `updated_by` | `BIGINT UNSIGNED` | NOT NULL |

约束：`UNIQUE(agent_code)`；店铺 Agent 必须关联店铺作用域，专属客服必须为平台作用域。`current_version_id` 只允许指向同一 Agent 的 `published` 版本。

##### 3.7.13.2 ai_agent_versions Agent 版本表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `agent_id` / `version_no` | `BIGINT UNSIGNED` / `INT UNSIGNED` | NOT NULL，联合 UK |
| `version_status` | `VARCHAR(16)` | `draft/testing/published/deprecated` |
| `graph_schema_version` | `VARCHAR(32)` | NOT NULL |
| `graph_config` | `JSON` | NOT NULL，节点/边/路由/子 Agent 白名单 |
| `system_prompt` | `MEDIUMTEXT` | NOT NULL，受审查系统指令 |
| `prompt_hash` / `config_hash` | `BINARY(32)` | NOT NULL |
| `model_policy` | `JSON` | NOT NULL，模型类别、参数上限、降级顺序，不含密钥 |
| `context_policy` / `memory_policy` | `JSON` | NOT NULL，上下文预算、摘要、长期记忆范围/期限 |
| `guardrail_policy` | `JSON` | NOT NULL，输入/输出/工具安全策略 |
| `release_note` | `VARCHAR(2000)` | NULL |
| `created_by` / `published_by` | `BIGINT UNSIGNED` | NOT NULL / NULL |
| `published_at` | `DATETIME(6)` | NULL |

发布后除状态迁移外不可修改配置正文；运行时在 Message/Trace/Audit 记录 `agent_version_id`。`UNIQUE(agent_id, version_no)`，索引 `idx_agent_versions_status(agent_id, version_status, version_no)`。

##### 3.7.13.3 ai_skills Skill 定义表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `skill_no` | `VARCHAR(40)` | UK |
| `skill_code` / `skill_name` | `VARCHAR(64)` / `VARCHAR(128)` | NOT NULL，Code UK |
| `skill_category` | `VARCHAR(32)` | `knowledge/query/transaction/support/orchestration` |
| `scope_type` / `scope_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | `platform/store` |
| `risk_level` | `VARCHAR(16)` | `low/medium/high/critical` |
| `description` | `VARCHAR(1000)` | NOT NULL |
| `skill_status` | `VARCHAR(16)` | `draft/active/disabled/archived` |
| `current_version_id` | `BIGINT UNSIGNED` | NULL |
| `created_by` / `updated_by` | `BIGINT UNSIGNED` | NOT NULL |

店铺私有 Skill 不得绑定到其他店铺 Agent；作用域检查不能仅依赖管理后台 UI。

##### 3.7.13.4 ai_skill_versions Skill 版本表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `skill_id` / `version_no` | `BIGINT UNSIGNED` / `INT UNSIGNED` | NOT NULL，联合 UK |
| `version_status` | `VARCHAR(16)` | `draft/testing/published/deprecated` |
| `instruction_content` | `MEDIUMTEXT` | NOT NULL，Skill 指令 |
| `input_schema` / `output_schema` | `JSON` | NOT NULL，JSON Schema |
| `execution_config` | `JSON` | NOT NULL，步数/超时/重试/失败策略 |
| `knowledge_scope_config` | `JSON` | NULL，RAG 命名空间和过滤规则 |
| `instruction_hash` / `schema_hash` | `BINARY(32)` | NOT NULL |
| `created_by` / `published_by` | `BIGINT UNSIGNED` | NOT NULL / NULL |
| `published_at` | `DATETIME(6)` | NULL |

发布规则与 Agent Version 一致。Skill 说明不能扩大 Tool Permission；最终权限取 Agent、Skill、Tool Policy、用户授权和当前资源权限的交集。

##### 3.7.13.5 ai_agent_skill_bindings Agent-Skill 关联表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `agent_version_id` / `skill_version_id` | `BIGINT UNSIGNED` | FK，联合 UK |
| `binding_status` | `VARCHAR(16)` | `enabled/disabled` |
| `priority` | `INT` | NOT NULL DEFAULT 100 |
| `trigger_config` | `JSON` | NULL，意图/前置条件，不允许任意代码 |
| `runtime_overrides` | `JSON` | NULL，只允许预定义可覆盖项 |
| `created_by` | `BIGINT UNSIGNED` | NOT NULL |

发布 Agent Version 时冻结绑定集；修改能力集需发布新 Agent Version，不让正在运行的 Checkpoint 突然获得新技能。

##### 3.7.13.6 ai_tools 工具注册表

`ai_tools` 只保存稳定的 Tool Definition，不保存会被发布流程修改的 Schema、传输配置或风险参数。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `tool_no` | `VARCHAR(40)` | UK |
| `tool_code` / `tool_name` | `VARCHAR(128)` / `VARCHAR(128)` | NOT NULL，Code UK |
| `scope_type` / `scope_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | `platform/store`，店铺私有 Tool 必须绑定店铺 |
| `owner_module` | `VARCHAR(64)` | NOT NULL，负责契约和运行 Adapter 的领域模块 |
| `description` | `VARCHAR(1000)` | NOT NULL |
| `tool_status` | `VARCHAR(16)` | `active/disabled/archived` |
| `current_version_id` | `BIGINT UNSIGNED` | NULL，FK `ai_tool_versions.id`，当前 Published Version |
| `created_by` / `updated_by` | `BIGINT UNSIGNED` | NOT NULL |

约束：`UNIQUE(tool_no)`、`UNIQUE(tool_code)`。`current_version_id` 只能指向本 Tool 的 `published` 版本；Disable/Kill Switch 可阻断全部版本，但不得修改历史版本正文。本表不保存 URL、密钥或认证 Token，连接端点和 Secret 引用只存在于受控 Version/部署配置与 Secret Manager。

##### 3.7.13.7 ai_tool_versions 工具版本表

Tool Version 是运行、绑定、权限、审计和回滚的不可变单位。任何 Schema、MCP 映射、风险级别、超时、幂等或结果策略基础语义变化都必须创建新版本。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `tool_id` / `version_no` | `BIGINT UNSIGNED` / `INT UNSIGNED` | FK / NOT NULL，联合 UK |
| `version_status` | `VARCHAR(16)` | `draft/testing/published/deprecated` |
| `transport_type` | `VARCHAR(16)` | `local/mcp/http` |
| `server_code` / `remote_tool_name` | `VARCHAR(64)` / `VARCHAR(128)` | NULL，MCP/远程 Tool 必填 |
| `protocol_version` | `VARCHAR(32)` | NULL，MCP/HTTP Contract 版本 |
| `input_schema` / `output_schema` | `JSON` | NOT NULL，严格 JSON Schema |
| `side_effect_type` | `VARCHAR(16)` | `none/read/write/external_write` |
| `risk_level` | `VARCHAR(16)` | `low/medium/high/critical` |
| `timeout_ms` | `INT UNSIGNED` | NOT NULL |
| `idempotency_support` | `VARCHAR(16)` | `none/required/provider` |
| `data_classification` | `VARCHAR(16)` | 最高可处理 `L0..L3` |
| `network_policy_ref` / `secret_ref_names` | `VARCHAR(128)` / `JSON` | NULL，只保存策略/Secret 引用名，不保存值 |
| `schema_hash` / `manifest_hash` | `BINARY(32)` | NOT NULL，规范化契约与完整 Manifest Hash |
| `release_note` | `VARCHAR(2000)` | NULL |
| `created_by` / `published_by` | `BIGINT UNSIGNED` | NOT NULL / NULL |
| `published_at` | `DATETIME(6)` | NULL |

约束：`UNIQUE(tool_id, version_no)`、`UNIQUE(tool_id, manifest_hash)`。`published` 后除迁移到 `deprecated` 外禁止 UPDATE；回滚只把 `ai_tools.current_version_id` 原子指向仍可用的历史 Published Version，并写管理员审批、操作审计和 Outbox，不复制或改写旧行。发布前必须通过 Schema、MCP Contract、安全、权限、Unknown Outcome、成本和回归测试。

##### 3.7.13.8 ai_skill_tool_bindings Skill-Tool Version 关联表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `skill_version_id` / `tool_version_id` | `BIGINT UNSIGNED` | FK，联合 UK；两端均为不可变 Published Version |
| `binding_status` | `VARCHAR(16)` | `enabled/disabled` |
| `argument_policy` | `JSON` | NOT NULL，可传字段、固定值、最大数量/金额 |
| `result_policy` | `JSON` | NOT NULL，返回字段白名单/脱敏/最大长度 |
| `requires_confirmation` | `BOOLEAN` | NOT NULL，绑定层可要求更严，不得放宽 Tool Policy |
| `created_by` | `BIGINT UNSIGNED` | NOT NULL |

约束：`UNIQUE(skill_version_id, tool_version_id)`。工具返回不可直接整体进入 Prompt；按 `result_policy` 结构化裁剪并标注不可信数据边界。Tool 发布新版本不会自动改变既有 Skill Version 的绑定；需要升级时创建新的 Skill Version 并重新测试发布。

##### 3.7.13.9 ai_tool_permission_policies 工具权限策略表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `policy_no` | `VARCHAR(40)` | UK |
| `subject_type` / `subject_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | `agent_version/skill_version` |
| `tool_version_id` | `BIGINT UNSIGNED` | FK，策略绑定不可变 Tool Version |
| `scope_type` / `scope_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | `platform/store/user`，不允许跨店 |
| `effect` | `VARCHAR(8)` | `allow/deny`，Deny 优先 |
| `condition_config` | `JSON` | NOT NULL，资源归属、订单状态、金额/次数/时间限制 |
| `confirmation_level` | `VARCHAR(16)` | `none/session/action/admin` |
| `policy_version` | `INT UNSIGNED` | NOT NULL |
| `policy_status` | `VARCHAR(16)` | `draft/effective/expired` |
| `effective_at` / `expired_at` | `DATETIME(6)` | NULL |
| `created_by` | `BIGINT UNSIGNED` | NOT NULL |

策略发布后不原地改正文。授权评估顺序：工具开关 → 显式 Deny → Agent/Skill 绑定 → 作用域/资源归属 → 用户 Consent → Action Approval → 参数限制。任一步不通过都不调用工具。

##### 3.7.13.10 ai_user_consents 用户授权记录表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `consent_no` | `VARCHAR(40)` | UK |
| `user_id` | `BIGINT UNSIGNED` | FK |
| `consent_type` | `VARCHAR(32)` | `long_term_memory/order_access/personalization` 等 |
| `scope_type` / `scope_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | `platform/store/conversation` |
| `policy_version` | `VARCHAR(32)` | NOT NULL，用户看到的告知版本 |
| `consent_version` | `INT UNSIGNED` | NOT NULL，同一 User+Type+Scope 从 1 递增 |
| `supersedes_consent_id` | `BIGINT UNSIGNED` | NULL，FK 前一版；再授权/续期建新行，不改写旧证据 |
| `consent_status` | `VARCHAR(16)` | `granted/revoked/expired` |
| `active_consent_key` | `BINARY(32) GENERATED` | 仅 `granted` 且未过期时为 `SHA256(user_id,consent_type,scope_type,scope_id)`，其他为 NULL |
| `granted_at` / `expires_at` / `revoked_at` | `DATETIME(6)` | NOT NULL / NULL / NULL |
| `grant_source` / `revoke_reason` | `VARCHAR(32)` / `VARCHAR(255)` | NOT NULL / NULL |
| `evidence_hash` | `BINARY(32)` | NOT NULL，授权页面/选项快照哈希 |

约束：`UNIQUE(user_id, consent_type, scope_type, scope_id, consent_version)` 保留版本链，`UNIQUE(active_consent_key)` 保证每个 User+Type+Scope 至多一个当前 Granted 版本。续期/恢复一律建新版并原子撤销旧 Active；撤销不删除证据行，而是条件更新状态与 `revoked_at`、发送 Tombstone。命令必须携 If-Match，并在同一 MySQL 事务写 `ai_user_consent_events(event_no, consent_id, event_type, from_status, to_status, actor_type, actor_no, reason_code, policy_version, created_at)` 追加审计；Event 不保存用户偏好明文。

`ai_memory_cleanup_tasks` 作为用户可查的异步清理资源，不与管理员批处理 Job 混用权限：

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `task_no` | `VARCHAR(40)` | UK，使用已注册 `job_` 前缀；API 的 `task_id` 承载此值 |
| `user_id` | `BIGINT UNSIGNED` | FK，仅本人可查 |
| `command_type` | `VARCHAR(32)` | `memory_delete/consent_revoke/scope_pause/disable_all` |
| `scope_type` / `scope_no` | `VARCHAR(16)` / `VARCHAR(64)` | `platform/store/all` 及公开 Scope ID |
| `source_resource_type` / `source_resource_no` | `VARCHAR(32)` / `VARCHAR(64)` | Consent/Memory 来源；只保存公开 ID |
| `task_status` | `VARCHAR(20)` | `queued/running/succeeded/partial_failed/failed` |
| `total_count` / `processed_count` / `failed_count` | `INT UNSIGNED` | NOT NULL DEFAULT 0，只向用户显示聚合进度 |
| `retry_count` / `max_retries` | `SMALLINT UNSIGNED` | NOT NULL |
| `idempotency_key_hash` | `BINARY(32)` | NOT NULL，同一命令重放返回原 Task |
| `error_code` | `VARCHAR(64)` | NULL，稳定最小原因，不包含 Object/Vector Key |
| `started_at` / `completed_at` | `DATETIME(6)` | NULL |

索引 `idx_ai_cleanup_user_time(user_id, created_at, id)`；约束 `UNIQUE(user_id, idempotency_key_hash)`。创建 Task 的原命令必须先在业务事务中停止召回/写 Tombstone 并写 Outbox，Worker 只负责删除 PostgreSQL Embedding、Redis 副本和其他派生数据。异步失败不得恢复 Consent 或 Memory Active 状态。

##### 3.7.13.11 ai_tool_approvals 工具操作确认表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `approval_no` | `VARCHAR(40)` | UK，前端确认 Token 的业务 ID |
| `user_id` / `conversation_id` / `tool_version_id` | `BIGINT UNSIGNED` | FK，确认绑定实际执行的不可变版本 |
| `agent_version_id` | `BIGINT UNSIGNED` | NOT NULL |
| `action_type` | `VARCHAR(64)` | NOT NULL，人类可理解的动作码 |
| `resource_type` / `resource_no` | `VARCHAR(32)` / `VARCHAR(64)` | NOT NULL |
| `arguments_canonical_hash` | `BINARY(32)` | NOT NULL，绑定确认参数 |
| `display_snapshot` | `JSON` | NOT NULL，用户看到的动作、金额、对象、风险 |
| `approval_status` | `VARCHAR(16)` | `pending/approved/rejected/expired/consumed` |
| `expires_at` / `decided_at` / `consumed_at` | `DATETIME(6)` | NOT NULL / NULL / NULL |
| `decision_ip_hash` | `BINARY(32)` | NULL |

确认只对完全相同的规范化参数有效，一次消费。任何金额、订单、地址或工具变化都需新确认；“用户说了可以”的自然语言文本不代替结构化 Approval。

##### 3.7.13.12 ai_tool_action_audits 工具操作审计表

按尝试追加，每次尝试一行，包括被拒绝和超时；单行只允许从运行中状态补全为终态，不可修改已完成的输入快照。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `action_no` | `VARCHAR(40)` | UK，同时作为工具幂等关联 ID |
| `user_id` / `conversation_id` / `message_id` | `BIGINT UNSIGNED` | NOT NULL / NOT NULL / NULL |
| `agent_version_id` / `skill_version_id` / `tool_version_id` | `BIGINT UNSIGNED` | NOT NULL，记录实际不可变版本 |
| `approval_id` / `consent_id` | `BIGINT UNSIGNED` | NULL |
| `policy_version_snapshot` | `JSON` | NOT NULL，命中策略 ID/版本列表 |
| `arguments_hash` / `result_hash` | `BINARY(32)` | NOT NULL / NULL |
| `arguments_redacted` / `result_redacted` | `JSON` | NULL，白名单且限大小 |
| `action_status` | `VARCHAR(16)` | `authorized/denied/running/succeeded/failed/unknown` |
| `side_effect_type` | `VARCHAR(16)` | NOT NULL |
| `business_resource_type` / `business_resource_no` | `VARCHAR(32)` / `VARCHAR(64)` | NULL，最终产物 |
| `idempotency_key` | `VARCHAR(128)` | NULL |
| `started_at` / `completed_at` | `DATETIME(6)` | NOT NULL / NULL |
| `error_code` / `trace_id` | `VARCHAR(64)` | NULL / NOT NULL |

索引：`idx_ai_actions_user_time(user_id, created_at, id)`、`idx_ai_actions_conversation(conversation_id, created_at)`、`idx_ai_actions_resource(business_resource_type, business_resource_no)`。工具超时不能直接标记 `failed`；若外部副作用结果未知，标记 `unknown` 并以幂等键查询/对账。

##### 3.7.13.13 ai_feedback 用户反馈表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `feedback_no` | `VARCHAR(40)` | UK |
| `user_id` / `conversation_id` / `message_id` | `BIGINT UNSIGNED` | FK，Message 必须属于该会话/用户 |
| `ai_run_no` | `VARCHAR(40)` | NULL |
| `feedback_type` | `VARCHAR(32)` | `thumb_up/thumb_down/report/correction` |
| `reaction_slot` | `TINYINT GENERATED` | 简单 Reaction 时为 1，Report/Correction 为 NULL |
| `active_reaction_key` | `BINARY(32) GENERATED` | 仅当前未撤回 Reaction 为 `SHA256(user_id,message_id,reaction_slot)`，其他 NULL |
| `reason_code` | `VARCHAR(64)` | NULL |
| `comment` | `VARCHAR(2000)` | NULL，过滤后文本 |
| `feedback_status` | `VARCHAR(16)` | `submitted/withdrawn/reviewed/resolved/dismissed` |
| `withdrawn_at` | `DATETIME(6)` | NULL，撤回 Reaction 时填写；不物理删审计事件 |
| `reviewed_by` / `reviewed_at` | `BIGINT UNSIGNED` / `DATETIME(6)` | NULL |
| `resolution_code` | `VARCHAR(64)` | NULL |

约束：`UNIQUE(active_reaction_key)` 保证同一用户对同一 AI 消息至多一个当前赞/踩；PUT 切换时条件撤回旧 Reaction 并建新行，DELETE 幂等置 `withdrawn`。Report/Correction 可多次提交但需用户/Message/Reason Hash 去重与限流。创建前必须从 Message 回溯 Conversation Owner 且校验 `sender_type=agent`；不能反馈他人、Human/System/Tool 或不可见消息。反馈进入评估数据集前必须脱敏、授权、去重、分层采样和人工标注，不把点赞直接当作正确答案，不把纠错写入长期记忆。

#### 3.7.14 系统事件与审计表

##### 3.7.14.1 outbox_events 事务事件表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `event_no` | `VARCHAR(40)` | UK，全局事件 ID |
| `aggregate_type` / `aggregate_no` | `VARCHAR(64)` / `VARCHAR(64)` | NOT NULL |
| `aggregate_version` | `BIGINT UNSIGNED` | NOT NULL |
| `event_type` | `VARCHAR(128)` | NOT NULL |
| `schema_version` | `SMALLINT UNSIGNED` | NOT NULL |
| `payload` | `JSON` | NOT NULL，最小必要、版本化 |
| `event_status` | `VARCHAR(16)` | `pending/processing/published/dead` |
| `attempt_count` | `INT UNSIGNED` | NOT NULL DEFAULT 0 |
| `available_at` | `DATETIME(6)` | NOT NULL |
| `lease_owner` / `lease_expires_at` | `VARCHAR(128)` / `DATETIME(6)` | NULL |
| `published_at` | `DATETIME(6)` | NULL |
| `last_error_code` / `last_error` | `VARCHAR(64)` / `VARCHAR(1000)` | NULL，脱敏 |
| `trace_id` | `VARCHAR(64)` | NULL |

索引：`idx_outbox_dispatch(event_status, available_at, id)`、`idx_outbox_lease(event_status, lease_expires_at)`、`idx_outbox_aggregate(aggregate_type, aggregate_no, aggregate_version)`。写业务聚合与写 Outbox 处于同一事务；消费者必须以 `event_no` 幂等。3.5.9 中的领取、Lease、重试和归档规则全部适用。

##### 3.7.14.2 notification_tasks 通知任务表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `task_no` | `VARCHAR(40)` | UK |
| `event_no` | `VARCHAR(40)` | NULL，来源 Outbox Event |
| `user_id` | `BIGINT UNSIGNED` | NULL |
| `channel` | `VARCHAR(16)` | `in_app/sms/email/push` |
| `template_code` / `template_version` | `VARCHAR(64)` / `INT UNSIGNED` | NOT NULL |
| `recipient_ciphertext` | `VARBINARY(512)` | NULL，站内信可空 |
| `variables_payload` | `JSON` | NOT NULL，脱敏且仅模板白名单变量 |
| `notification_status` | `VARCHAR(16)` | `pending/processing/sent/retry/cancelled/dead` |
| `idempotency_key` | `VARCHAR(128)` | NOT NULL，UK |
| `attempt_count` | `SMALLINT UNSIGNED` | NOT NULL DEFAULT 0 |
| `available_at` / `sent_at` | `DATETIME(6)` | NOT NULL / NULL |
| `provider_message_id` | `VARCHAR(128)` | NULL |
| `last_error` | `VARCHAR(1000)` | NULL |

索引：`idx_notification_dispatch(notification_status, available_at, id)`。工作任务不在数据库事务内调用短信/邮件提供商；渠道返回未知时以幂等键查询，不无限发送。

##### 3.7.14.3 system_audit_logs 系统审计日志表

用于服务身份、安全管理、数据导出/解密等跨领域高风险事件；不取代领域专用流水。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `audit_no` | `VARCHAR(40)` | UK |
| `event_category` / `event_type` | `VARCHAR(32)` / `VARCHAR(64)` | NOT NULL |
| `actor_type` / `actor_no` | `VARCHAR(16)` / `VARCHAR(64)` | `user/admin/service/agent/system` |
| `scope_type` / `scope_no` | `VARCHAR(16)` / `VARCHAR(64)` | NULL |
| `target_type` / `target_no` | `VARCHAR(64)` / `VARCHAR(64)` | NULL |
| `action` | `VARCHAR(64)` | NOT NULL |
| `result_status` | `VARCHAR(16)` | `success/denied/failed` |
| `reason_code` | `VARCHAR(64)` | NULL |
| `metadata_redacted` | `JSON` | NULL，不存原始敏感值 |
| `request_id` / `trace_id` | `VARCHAR(64)` | NULL |
| `source_ip_hash` | `BINARY(32)` | NULL |
| `prev_hash` / `record_hash` | `BINARY(32)` | NULL，可选哈希链防篡改 |

索引：`idx_system_audit_actor(actor_type, actor_no, created_at)`、`idx_system_audit_target(target_type, target_no, created_at)`、`idx_system_audit_category(event_category, created_at)`。严格限制表的 UPDATE/DELETE 权限，定期归档到只读存储并验证哈希链/签名。

##### 3.7.14.4 scheduled_job_logs 定时任务记录表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `run_no` | `VARCHAR(40)` | UK |
| `job_code` | `VARCHAR(64)` | NOT NULL |
| `schedule_time` | `DATETIME(6)` | NOT NULL，本次调度槽位 |
| `shard_key` | `VARCHAR(64)` | NOT NULL DEFAULT `default` |
| `job_status` | `VARCHAR(16)` | `running/succeeded/failed/partial/skipped` |
| `lease_owner` | `VARCHAR(128)` | NULL |
| `started_at` / `finished_at` | `DATETIME(6)` | NOT NULL / NULL |
| `processed_count` / `success_count` / `failure_count` | `BIGINT UNSIGNED` | NOT NULL DEFAULT 0 |
| `checkpoint_payload` | `JSON` | NULL，只存断点游标，不存海量结果 |
| `error_code` / `error_summary` | `VARCHAR(64)` / `VARCHAR(1000)` | NULL |
| `trace_id` | `VARCHAR(64)` | NULL |

约束：`UNIQUE(job_code, schedule_time, shard_key)`，防止 Scheduler 多副本对同一时间槽重复运行。业务任务本身仍要幂等，因为 Lease 过期会导致合法重跑。

##### 3.7.14.5 dead_letter_events 死信事件表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `dead_letter_no` | `VARCHAR(40)` | UK |
| `source_type` / `source_no` | `VARCHAR(32)` / `VARCHAR(64)` | `outbox/notification/logistics/indexing/memory` 及来源 ID |
| `event_type` | `VARCHAR(128)` | NOT NULL |
| `schema_version` | `SMALLINT UNSIGNED` | NOT NULL |
| `payload_redacted` | `JSON` | NULL，可安全重放的最小 Payload |
| `payload_hash` | `BINARY(32)` | NOT NULL |
| `failure_count` | `INT UNSIGNED` | NOT NULL |
| `first_failed_at` / `last_failed_at` | `DATETIME(6)` | NOT NULL |
| `last_error_code` / `last_error` | `VARCHAR(64)` / `VARCHAR(1000)` | NOT NULL，脱敏 |
| `dead_status` | `VARCHAR(16)` | `open/replaying/resolved/ignored` |
| `resolved_by` / `resolved_at` | `BIGINT UNSIGNED` / `DATETIME(6)` | NULL |
| `resolution_note` | `VARCHAR(1000)` | NULL |
| `replay_count` / `last_replay_at` | `INT UNSIGNED` / `DATETIME(6)` | NOT NULL DEFAULT 0 / NULL |
| `original_trace_id` / `replay_trace_id` | `VARCHAR(64)` | NULL |

约束：活跃死信按 `(source_type, source_no)` 唯一，避免同一失败无限创建告警行。重放必须先验证当前 Schema、目标资源版本和消费者幂等性，使用新 Trace ID，记录操作者/理由；禁止在生产管理页“一键无条件重放全部”。

##### 3.7.14.6 admin_batch_jobs 管理端批处理任务表

保存商品/库存导入、受控导出、知识索引、评估等由管理端发起的长任务资源。采用通用可变实体字段。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `job_no` | `VARCHAR(40)` | UK，对外 `job_id` |
| `job_type` | `VARCHAR(32)` | `product_import/inventory_import/data_export/knowledge_index/evaluation` 等白名单 |
| `requested_by` | `BIGINT UNSIGNED` | FK，发起管理员 |
| `scope_type` / `scope_id` | `VARCHAR(16)` / `BIGINT UNSIGNED` | 当时平台/店铺数据范围快照 |
| `permission_code` | `VARCHAR(128)` | NOT NULL，发起时权限快照 |
| `input_file_id` | `BIGINT UNSIGNED` | NULL，FK `file_objects.id`，仅 Active 私有文件 |
| `request_config` | `JSON` | NOT NULL，版本化白名单参数和筛选摘要，不保存 Token/Secret |
| `request_hash` | `BINARY(32)` | NOT NULL，幂等和影响预览一致性 |
| `job_status` | `VARCHAR(32)` | `created/validating/awaiting_confirmation/queued/running/succeeded/partial/failed/cancelled/expired`；长度必须覆盖最长状态值，禁止因状态机扩展发生截断 |
| `execution_backend` / `execution_job_no` | `VARCHAR(32)` / `VARCHAR(40)` | NULL，知识索引为 `postgres_indexing/kjob_...`，只保存跨库公开 ID |
| `execution_status_version` | `BIGINT UNSIGNED` | NOT NULL DEFAULT 0，消费执行进度事件的单调版本 |
| `total_count` / `success_count` / `failure_count` | `BIGINT UNSIGNED` | NOT NULL DEFAULT 0 |
| `cancel_requested_at` / `cancel_requested_by` | `DATETIME(6)` / `BIGINT UNSIGNED` | NULL，协作取消意图；不等同于任务已取消 |
| `cancel_reason` | `VARCHAR(500)` | NULL，发起取消时必填并脱敏 |
| `result_file_id` / `error_file_id` | `BIGINT UNSIGNED` | NULL，FK `file_objects.id`，私有短期产物 |
| `started_at` / `finished_at` / `expires_at` | `DATETIME(6)` | NULL |
| `error_code` / `error_summary` | `VARCHAR(64)` / `VARCHAR(1000)` | NULL，脱敏 |
| `trace_id` | `VARCHAR(64)` | NOT NULL |

约束：`UNIQUE(job_no)`、`UNIQUE(execution_backend, execution_job_no)`（NULL 除外）；索引 `idx_admin_batch_jobs_requester(requested_by, created_at, id)`、`idx_admin_batch_jobs_status(job_status, created_at, id)`、`idx_admin_batch_jobs_scope(scope_type, scope_id, job_type, created_at, id)`。导入必须先 `validating → awaiting_confirmation` 展示影响摘要，再由带 ETag/幂等键的确认命令进入队列；导出筛选和实际执行都重新校验 Scope。取消只对未开始或支持协作取消的任务生效，不伪造已执行副作用回滚。对知识索引，本表是“管理端命令/可见生命周期”权威，不保存 Chunk 级执行检查点。

##### 3.7.14.7 admin_batch_job_items 批处理逐项结果表

大批次可按保留策略分区/归档；用户界面只读取汇总和 Cursor 分页错误明细。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `job_id` | `BIGINT UNSIGNED` | FK `admin_batch_jobs.id` |
| `item_key` | `VARCHAR(128)` | NOT NULL，源文件行号/业务键的安全表示 |
| `item_status` | `VARCHAR(16)` | `valid/invalid/succeeded/failed/skipped` |
| `resource_type` / `resource_no` | `VARCHAR(32)` / `VARCHAR(64)` | NULL，成功资源引用 |
| `error_code` / `error_message` | `VARCHAR(64)` / `VARCHAR(1000)` | NULL，面向管理员且脱敏 |
| `input_hash` | `BINARY(32)` | NOT NULL，不复制完整敏感输入 |

约束：`UNIQUE(job_id, item_key)`；索引 `idx_batch_job_items_status(job_id, item_status, id)`。任务重试以业务键和输入 Hash 幂等，不重复创建成功资源；失败报告通过对象存储私有文件生成，过期后删除文件但按审计策略保留汇总。

#### 3.7.15 文件与上传表

文件二进制保存在对象存储，MySQL 只保存文件身份、归属、安全状态和生命周期事实。业务表可以保存已激活对象的 `object_key` 快照，但上传、扫描、绑定、下载授权和清理必须以本组表为权威依据。

##### 3.7.15.1 file_objects 文件对象表

采用通用可变实体字段。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `file_no` | `VARCHAR(40)` | NOT NULL，UK，对外 `file_id`，使用 `file_` 前缀 |
| `bucket` / `object_key` | `VARCHAR(64)` / `VARCHAR(512)` | NOT NULL，服务端生成，不包含用户隐私或自增 ID |
| `purpose` | `VARCHAR(32)` | `product/store_logo/brand_logo/category_icon/store_certification/avatar/review/refund/knowledge/platform_content/admin_import/admin_export` |
| `owner_type` / `owner_no` | `VARCHAR(32)` / `VARCHAR(64)` | 当前归属主体类型和公开 ID；待绑定时归属 Upload Session |
| `upload_session_id` | `BIGINT UNSIGNED` | NULL，FK `file_upload_sessions.id` |
| `parent_file_id` | `BIGINT UNSIGNED` | NULL，FK `file_objects.id`，派生图指向受控原图 |
| `variant` | `VARCHAR(32)` | NOT NULL DEFAULT `original`，如 `original/w320/w960/thumbnail` |
| `processor_version` | `VARCHAR(32)` | NULL，派生文件记录图像/OCR/转码处理器版本；原图可空 |
| `original_filename_ciphertext` | `VARBINARY(1024)` | NULL，仅确有业务需要时加密保存 |
| `declared_mime_type` / `detected_mime_type` | `VARCHAR(128)` | NOT NULL，声明类型与检测类型分离 |
| `size_bytes` | `BIGINT UNSIGNED` | NOT NULL |
| `sha256` / `provider_checksum` | `BINARY(32)` / `VARCHAR(128)` | NOT NULL / NULL |
| `width` / `height` / `duration_ms` / `page_count` | `INT UNSIGNED` | NULL，按媒体类型填写 |
| `visibility` / `sensitivity_level` | `VARCHAR(24)` / `VARCHAR(4)` | `private/authenticated/public_derivative` 与 `L0..L3` |
| `scan_status` | `VARCHAR(16)` | `pending/processing/safe/rejected/error`；`processing` 只表示 Worker 已原子领取任务 |
| `file_status` | `VARCHAR(16)` | `pending_upload/uploaded/scanning/active/rejected/pending_delete/deleted` |
| `storage_version_id` / `encryption_key_version` | `VARCHAR(128)` / `SMALLINT UNSIGNED` | NULL |
| `reference_count` | `INT UNSIGNED` | NOT NULL DEFAULT 0，可重建的引用计数 |
| `expires_at` / `activated_at` / `deleted_at` | `DATETIME(6)` | NULL |

约束：`UNIQUE(file_no)`、`UNIQUE(bucket, object_key)`；派生文件使用 `UNIQUE(parent_file_id, variant, processor_version)` 防止同一处理版本重复生成。索引 `idx_file_objects_owner(owner_type, owner_no, file_status, id)`、`idx_file_objects_lifecycle(file_status, expires_at, id)`、`idx_file_objects_hash(sha256, size_bytes)`。只有 `scan_status=safe AND file_status=active` 且 Purpose/Owner 匹配的文件才能绑定业务资源；引用计数只作清理加速，删除前仍须对业务引用做权威核验。

##### 3.7.15.2 file_upload_sessions 文件上传会话表

采用通用可变实体字段，保存一次直传或分段上传的安全租约，不保存 Presigned URL、Secret Key 或完整授权 Header。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `upload_no` | `VARCHAR(40)` | NOT NULL，UK，对外 `upload_id`，使用 `upl_` 前缀 |
| `uploader_user_id` | `BIGINT UNSIGNED` | FK，当前认证用户/管理员主体 |
| `purpose` | `VARCHAR(32)` | 与 `file_objects.purpose` 同枚举 |
| `target_owner_type` / `target_owner_no` | `VARCHAR(32)` / `VARCHAR(64)` | 预期绑定主体；服务端从权限上下文生成/验证 |
| `provider` | `VARCHAR(32)` | `minio/s3/oss/cos` 等受控适配器代码 |
| `reserved_bucket` / `reserved_object_key` | `VARCHAR(64)` / `VARCHAR(512)` | NOT NULL，服务端随机生成且不可复用 |
| `upload_type` | `VARCHAR(16)` | `single/multipart` |
| `provider_upload_id` | `VARCHAR(256)` | NULL，仅 Multipart 使用，日志必须脱敏 |
| `expected_mime_types` | `JSON` | NOT NULL，受版本化上传策略限制的白名单 |
| `max_size_bytes` | `BIGINT UNSIGNED` | NOT NULL |
| `expected_sha256` | `BINARY(32)` | NULL，提供时 Complete 必须匹配 |
| `part_manifest` | `JSON` | NULL，分段号、大小和校验和；限制总项数与大小 |
| `upload_status` | `VARCHAR(16)` | `created/uploading/uploaded/completed/expired/aborted/rejected` |
| `expires_at` / `completed_at` / `aborted_at` | `DATETIME(6)` | NOT NULL / NULL / NULL |

约束：`UNIQUE(upload_no)`、`UNIQUE(reserved_bucket, reserved_object_key)`，Provider 支持稳定 Upload ID 时增加 `UNIQUE(provider, provider_upload_id)`。索引 `idx_upload_sessions_user_status(uploader_user_id, upload_status, created_at, id)`、`idx_upload_sessions_expiry(upload_status, expires_at, id)`。Complete 必须锁定 Session、校验状态/归属/过期时间，并通过对象存储 HEAD 复核 Key、大小、校验和和 Metadata；重复 Complete 返回同一结果。过期 Worker 幂等 Abort Multipart、删除临时对象并推进为 `expired/aborted`。

#### 3.7.16 平台内容运营表

平台 Banner、公告、帮助中心文章、页脚链接以及用户协议/隐私政策使用统一内容聚合，但不同内容类型的 Payload 必须通过独立版本化 JSON Schema 校验。公开页面只读取已发布且在生效时间窗内的版本，不直接读取管理草稿。`legal_document` 比普通运营内容增加地区、语言、强制重新告知和接受策略校验，发布/撤回必须经过合规权限与职责分离。

##### 3.7.16.1 platform_content_entries 平台内容条目表

采用通用可变实体字段，保存内容身份、类型和当前发布指针。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `content_no` | `VARCHAR(40)` | UK，对外内容 ID |
| `content_type` | `VARCHAR(32)` | `banner/announcement/help_article/footer_link/legal_document` |
| `content_key` | `VARCHAR(128)` | NOT NULL，稳定业务键；帮助文章可使用安全 Slug |
| `title` | `VARCHAR(255)` | NOT NULL，经安全过滤 |
| `audience` | `VARCHAR(32)` | `public/authenticated/admin`，首版不接受任意表达式 |
| `content_status` | `VARCHAR(16)` | `draft/published/withdrawn/archived` |
| `current_version_id` | `BIGINT UNSIGNED` | NULL，FK `platform_content_versions.id` |
| `sort_order` | `INT` | NOT NULL DEFAULT 0 |
| `effective_at` / `expires_at` | `DATETIME(6)` | NULL，必须满足有效时间关系 |
| `created_by` | `BIGINT UNSIGNED` | FK，管理员 |

约束：`UNIQUE(content_no)`、`UNIQUE(content_type, content_key)`；索引 `idx_platform_content_public(content_type, content_status, effective_at, expires_at, sort_order, id)`。归档内容业务键不复用；时间窗到达由查询条件立即生效，Scheduler 只负责预热/通知，不能成为可见性的唯一依据。

##### 3.7.16.2 platform_content_versions 平台内容版本表

已发布版本不可更新，修改必须创建新草稿版本并重新审核/发布。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `content_entry_id` / `version_no` | `BIGINT UNSIGNED` / `INT UNSIGNED` | FK / NOT NULL，联合 UK |
| `version_status` | `VARCHAR(16)` | `draft/reviewing/approved/published/rejected/withdrawn` |
| `content_format` | `VARCHAR(16)` | `structured/safe_html/plain_text` |
| `content_payload` | `JSON` | NOT NULL，按 `content_type + schema_version` 严格校验 |
| `safe_html` | `MEDIUMTEXT` | NULL，由服务端 Sanitizer 生成，不接收客户端权威值 |
| `schema_version` | `SMALLINT UNSIGNED` | NOT NULL |
| `content_hash` | `BINARY(32)` | NOT NULL，规范化安全内容 Hash |
| `created_by` / `reviewed_by` / `published_by` | `BIGINT UNSIGNED` | NOT NULL / NULL / NULL |
| `review_reason` | `VARCHAR(1000)` | NULL，拒绝/撤回时必填 |
| `reviewed_at` / `published_at` | `DATETIME(6)` | NULL |

约束：`UNIQUE(content_entry_id, version_no)`；一个 Entry 同时最多一个 Published Current Version，由发布事务锁定 Entry、校验审批状态/生效时间/资源文件、更新指针、写管理员操作日志与 Outbox。Banner 图片只绑定 Purpose/Owner 匹配的 Active `file_id`；跳转链接使用站内路由或受控 HTTPS 域名白名单。公开缓存键包含 Version，撤回事件立即失效 CDN/Redis。

`legal_document` 的 `content_key` 固定使用注册表值，如 `terms_of_service`、`privacy_policy`；Payload 至少包含 `document_version、locale、region_code、effective_at、acceptance_required、requires_reacceptance、summary、safe_body`。注册配置只能返回对当前地区/语言处于 Published 且已生效的必要版本；不存在完整版本组合时注册 Fail Closed。已被 `user_agreement_acceptances` 引用的版本不得物理删除或覆盖正文，撤回只阻止新接受并切换到替代版本。

至此，3.7 表结构形成九条可验证的闭环：认证闭环（注册配置/验证码→用户与凭证→协议接受→Session→认证尝试审计）、计价闭环（SKU 价格/店铺配送→结算快照→订单实付与改价分摊→退款上限）、交易闭环（结算→订单→支付→库存→履约/售后→申诉）、客服闭环（会话→消息→业务上下文→人工工单）、AI 治理闭环（版本→权限→授权/确认→操作审计）、文件闭环（上传会话→对象校验/扫描→业务绑定→引用对账→清理）、可靠性闭环（业务事务→Outbox→重试→死信→受控重放）、管理批处理闭环（上传/筛选→预检→确认→执行→逐项结果→短时产物）和内容发布闭环（条目→不可变版本→审核→定时发布/撤回→缓存失效）。

---

### 3.8 PostgreSQL 与 pgvector 设计

PostgreSQL 是 AI 子系统的持久化与检索库，不是第二个交易主库。商品价格、库存、订单、支付、物流和退款的当前状态一律调用 MySQL 业务 API/Tool 获取，不得从向量副本回答为实时事实。

本节使用三类 ID：PostgreSQL 内部主键使用应用层生成的 UUIDv7 `UUID`；需要进入 API、引用、日志或 Agent 上下文的 PostgreSQL 资源公开 ID 使用 3.6.2 前缀注册表的 `VARCHAR(40)`；跨库引用使用 MySQL 原始公开 ID/业务号 `VARCHAR(64)`，不改变值，也不复制 MySQL 自增主键。所有时间列使用 `TIMESTAMPTZ`并以 UTC 语义写入，动态元数据使用有 Schema 版本的 `JSONB`。

#### 3.8.1 PostgreSQL Schema 划分

| Schema | 职责 | 主要写入者 | 禁止内容 |
| :--- | :--- | :--- | :--- |
| `extensions` | `vector` 等受控扩展对象 | 迁移管理角色 | 业务表、用户自定义对象 |
| `knowledge` | 知识文档、切片、Embedding、索引任务与检索记录 | Indexing/Retrieval Worker | 交易当前状态 |
| `memory` | 长期记忆、Embedding、变更事件与对话摘要 | Memory/Summary Worker | L3 数据、无授权记忆 |
| `agent_runtime` | Agent Checkpoint 和中间 Channel Write | Agent Runtime | 订单/支付事务日志 |

权限与安全基线：

- 撤销 `PUBLIC` 对 `public` Schema 的 `CREATE`，不把可由非受信角色写入的 Schema 放入 `search_path`。
- DDL Owner、Migration Role、Runtime Read/Write Role、Read-only/Evaluation Role 分离；运行帐号不拥有 Schema/Extension。
- 代码中显式使用 `knowledge.documents` 等限定名；扩展类型/操作符所在 Schema 只由受信角色管理。
- 本项目不依赖 RLS 代替 Service 授权；若开启 RLS，必须有带店铺/用户上下文的连接约定、Policy 测试和备份/管理角色设计。
- 每个 Schema 使用独立 Alembic Version Table 或同一 Revision Graph 中明确分组，不手工修改生产表。

#### 3.8.2 pgvector 扩展初始化

迁移管理角色执行：

```sql
CREATE SCHEMA IF NOT EXISTS extensions;
REVOKE CREATE ON SCHEMA public FROM PUBLIC;
CREATE EXTENSION IF NOT EXISTS vector WITH SCHEMA extensions;

SELECT extname, extversion, extnamespace::regnamespace
FROM pg_extension
WHERE extname = 'vector';
```

部署前确认 PostgreSQL 主版本与 pgvector 镜像/扩展版本兼容，生产镜像固定摘要。Migration 需检查扩展存在和最低版本，但不在应用每次启动时尝试升级扩展。扩展升级需在恢复副本上演练，并验证 HNSW 索引、查询计划和 Recall 评估。

#### 3.8.3 embedding 模型与维度管理

新增支撑表 `knowledge.embedding_models`：

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `id` / `model_no` | `UUID` / `VARCHAR(64)` | PK / UK，内部 ID 与稳定配置码 |
| `provider` / `model_name` / `model_revision` | `VARCHAR(64)` / `VARCHAR(128)` / `VARCHAR(128)` | NOT NULL，精确锁定提供方模型版本 |
| `dimension` | `INTEGER` | NOT NULL，`1..2000` 的建议基线 |
| `storage_type` | `VARCHAR(16)` | `vector/halfvec`，默认 `vector` |
| `distance_metric` | `VARCHAR(16)` | `cosine/inner_product/l2`，发布后不可改 |
| `is_normalized` | `BOOLEAN` | 模型输出是否单位化 |
| `max_input_tokens` | `INTEGER` | NOT NULL |
| `purpose` | `VARCHAR(32)` | `knowledge/memory/query`，可多用途时用关联配置 |
| `model_status` | `VARCHAR(16)` | `testing/building/active/retiring/retired/failed` |
| `config_hash` | `BYTEA` | NOT NULL，预处理/维度/距离策略哈希 |
| `activated_at` / `retired_at` | `TIMESTAMPTZ` | NULL |
| `created_at` | `TIMESTAMPTZ` | NOT NULL |

核心规则：

- 模型名不足以确定向量语义；必须同时固定 Provider、Revision、Dimension、归一化方法、文本预处理和距离算子。
- 文档向量与 Query 向量必须使用同一兼容 Profile；禁止把不同模型向量直接计算距离。
- 生成后验证维度、所有元素有限、范数符合预期；零向量不进入 Cosine HNSW 索引。
- 向量不直接写应用日志/Trace；记录 Model No、维度、Token、耗时和向量输入哈希。
- pgvector HNSW 对 `vector` 索引支持的维度上限为 2,000，如模型超出上限，不得截断向量；应评估 `halfvec`、降维或更换模型，并通过离线评估验证精度。

#### 3.8.4 knowledge.documents 知识文档表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `id` | `UUID` | PK，UUIDv7 |
| `document_no` | `VARCHAR(40)` | UK，知识文档公开 ID，`doc_` 前缀 |
| `source_type` / `source_no` | `VARCHAR(32)` / `VARCHAR(64)` | `product/product_faq/store_policy/platform_policy/upload` 及 MySQL/文件 ID |
| `source_version` | `BIGINT` | NOT NULL，单调业务版本 |
| `scope_type` / `scope_no` | `VARCHAR(16)` / `VARCHAR(64)` | `platform/store/product`，店铺知识必须带 Store No |
| `title` | `TEXT` | NOT NULL |
| `source_object_key` | `TEXT` | NULL，原始文件对象引用 |
| `mime_type` / `language` | `VARCHAR(128)` / `VARCHAR(16)` | NOT NULL |
| `content_hash` | `BYTEA` | NOT NULL，SHA-256 |
| `parser_name` / `parser_version` | `VARCHAR(64)` / `VARCHAR(32)` | NOT NULL |
| `visibility` | `VARCHAR(16)` | `public/store_private/platform_internal` |
| `sensitivity_level` | `VARCHAR(4)` | `L0/L1/L2`，禁止 L3 进知识库 |
| `metadata` | `JSONB` | NOT NULL DEFAULT `{}`，Schema 版本化 |
| `document_status` | `VARCHAR(16)` | `pending/indexing/active/failed/superseded/deleted` |
| `effective_at` / `deleted_at` | `TIMESTAMPTZ` | NULL |
| `created_at` / `updated_at` | `DATETIME(6)` | NOT NULL，UTC；本表属于 MySQL，不使用 PostgreSQL `TIMESTAMPTZ` |

约束：`UNIQUE(source_type, source_no, source_version)`；索引 `(source_type, source_no, source_version DESC)`、`(scope_type, scope_no, document_status)`。同一来源只有一个当前 Active Version，通过部分唯一索引保证。原始内容不必全部存本表：大文件放对象存储，本表保存来源、哈希、版本和索引状态。

#### 3.8.5 knowledge.chunks 知识切片表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `id` / `chunk_no` | `UUID` / `VARCHAR(40)` | PK / UK，`chunk_no` 使用 `kch_` 前缀 |
| `document_id` | `UUID` | FK `knowledge.documents.id` |
| `chunk_index` | `INTEGER` | NOT NULL，文档内顺序 |
| `content` | `TEXT` | NOT NULL，清洗后切片正文 |
| `content_hash` | `BYTEA` | NOT NULL |
| `token_count` | `INTEGER` | NOT NULL，按指定 Tokenizer |
| `char_start` / `char_end` | `INTEGER` | NULL，原文偏移 |
| `heading_path` | `JSONB` | NOT NULL DEFAULT `[]`，标题层级 |
| `language` | `VARCHAR(16)` | NOT NULL |
| `search_lexemes` | `TEXT` | NOT NULL，受控分词结果 |
| `search_vector` | `TSVECTOR` | NOT NULL，由受控转换生成 |
| `metadata` | `JSONB` | NOT NULL DEFAULT `{}` |
| `chunk_status` | `VARCHAR(16)` | `active/superseded/deleted` |
| `created_at` / `updated_at` | `TIMESTAMPTZ` | NOT NULL |

约束：`UNIQUE(document_id, chunk_index)`，并对 `search_vector` 建 GIN 索引。Chunk 在标题/段落边界上切分，设定可配置的目标 Token 数和小量 Overlap；表格、问答、政策条款优先保持语义完整，不仅按字符数机械截断。每个 Chunk 保留可回到文档和业务来源的引用。

为支持多模型/多维度并存，新增 `knowledge.chunk_embeddings`：

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `id` | `UUID` | PK |
| `chunk_id` / `embedding_model_id` | `UUID` | FK，联合 UK |
| `embedding` | `extensions.vector` | NOT NULL，可变 Typmod，由检查保证模型维度 |
| `dimension` | `INTEGER` | NOT NULL，与 Model Profile 一致 |
| `input_hash` | `BYTEA` | NOT NULL，预处理后输入哈希 |
| `embedding_status` | `VARCHAR(16)` | `building/active/failed/retiring` |
| `created_at` / `updated_at` | `TIMESTAMPTZ` | NOT NULL |

查询只在一个 `embedding_model_id` 内执行，并对每个 Active Model 建部分 HNSW 表达式索引。数据库添加 `CHECK (dimension = extensions.vector_dims(embedding))` 检查行内实际维度；由于 CHECK 不应跨表查 Model Registry，Worker 在写入前再根据 `knowledge.embedding_models.dimension` 校验一次。

#### 3.8.6 knowledge.indexing_jobs 索引任务表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `id` / `job_no` | `UUID` / `VARCHAR(40)` | PK / UK，`job_no` 使用 `kjob_` 前缀 |
| `document_id` | `UUID` | NULL，单文档任务 |
| `source_type` / `source_no` / `source_version` | `VARCHAR(32)` / `VARCHAR(64)` / `BIGINT` | NOT NULL，幂等来源 |
| `admin_job_no` | `VARCHAR(40)` | NULL，来自管理端时指向 MySQL `admin_batch_jobs.job_no`，自动索引事件可为 NULL |
| `execution_version` | `BIGINT` | NOT NULL DEFAULT 0，每次状态/进度变更单调递增 |
| `embedding_model_id` | `UUID` | NULL，删除任务可空 |
| `job_type` | `VARCHAR(16)` | `upsert/delete/rebuild/verify` |
| `job_status` | `VARCHAR(16)` | `pending/processing/succeeded/retry/dead/cancelled` |
| `priority` / `attempt_count` | `SMALLINT` / `INTEGER` | NOT NULL |
| `available_at` | `TIMESTAMPTZ` | NOT NULL |
| `lease_owner` / `lease_expires_at` | `VARCHAR(128)` / `TIMESTAMPTZ` | NULL |
| `chunk_count` / `embedded_count` / `failed_count` | `INTEGER` | NOT NULL DEFAULT 0 |
| `input_content_hash` | `BYTEA` | NULL |
| `last_error_code` / `last_error` | `VARCHAR(64)` / `TEXT` | NULL，脱敏并限长 |
| `trace_id` | `VARCHAR(64)` | NULL |
| `created_at` / `started_at` / `finished_at` | `TIMESTAMPTZ` | NOT NULL / NULL / NULL |

约束：活跃任务按 `(source_type, source_no, source_version, embedding_model_id, job_type)` 部分唯一，`admin_job_no` 非空时唯一。领取使用 `FOR UPDATE SKIP LOCKED`和 Lease；耗时解析/Embedding 在领取事务外执行。旧 Source Version 任务启动前/提交前各检查一次，不得把新版本覆盖回旧版本。本表是 Parse/Chunk/Embedding/Index/Activate 执行进度和错误的权威；不反向决定管理员是否有权查看/取消任务。

**MySQL/PostgreSQL 父子任务契约**：管理员发起索引时，Knowledge Service 先在 MySQL 事务中创建 `admin_batch_jobs(job_type=knowledge_index, queued)` 和 Outbox；Consumer 按 `admin job_no` 幂等创建本表子任务，再通过事件回填 `execution_job_no`。本表每次状态/进度变更发出带 `admin_job_no + execution_version` 的事件，MySQL 仅消费更大版本并按 `pending→queued、processing→running、retry→running（同时投影下次可重试时间和脱敏错误）、succeeded→succeeded、dead→failed、cancelled→cancelled` 映射管理状态；累计计数同样只接受不小于当前执行版本且满足边界的值。管理端查询 MySQL Job，详细执行页再经后端组合脱敏 PostgreSQL 进度；不让前端直连两库或自行合并状态。

取消先在 MySQL Job 上写 `cancel_requested_at/cancel_requested_by/cancel_reason` 并发送 Outbox，父任务仍保持 `queued/running`，界面以独立 `cancellation_requested=true` 投影“取消处理中”；只有收到 PostgreSQL `cancelled` 终态才把父任务置为 `cancelled`。PostgreSQL Worker 只在未 Activate 时协作取消；已原子切换 Active Index 时返回 `succeeded` 和“已完成，不能伪装取消”。Reconciler 定期检查：MySQL 长时无子任务、PG 有 `admin_job_no` 但 MySQL 无链接、状态版本缺口、取消意图未送达和终态不一致；按公开 ID/Version 补链或重放事件，不直接猜测终态。由商品/政策变更自动触发的索引可只有 PostgreSQL Job，`admin_job_no=NULL`，但仍必须有 Source Event 与 Trace。

#### 3.8.7 knowledge.retrieval_logs 检索记录表

追加型，用于可观测、离线评估和召回调优，不保存无限期原始用户问题。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `id` / `retrieval_no` | `UUID` / `VARCHAR(40)` | PK / UK，`retrieval_no` 使用 `rtv_` 前缀 |
| `trace_id` / `ai_run_no` | `VARCHAR(64)` | NOT NULL / NULL |
| `conversation_no` / `user_no_hash` | `VARCHAR(64)` / `BYTEA` | NULL，最小化身份关联 |
| `agent_version_no` | `VARCHAR(64)` | NOT NULL |
| `embedding_model_id` | `UUID` | NULL，纯关键词降级可空 |
| `query_hash` | `BYTEA` | NOT NULL，规范化 Query Hash |
| `query_redacted` | `TEXT` | NULL，脱敏、截断且按策略短期保留 |
| `query_token_count` | `INTEGER` | NOT NULL |
| `scope_filters` | `JSONB` | NOT NULL，平台/店铺/商品可见性过滤 |
| `top_k` / `candidate_count` / `returned_count` | `SMALLINT` | NOT NULL |
| `result_refs` | `JSONB` | NOT NULL，Chunk No、文档版本、各路分数、融合名次 |
| `retrieval_mode` | `VARCHAR(16)` | `hybrid/vector/keyword/exact/fallback` |
| `latency_ms` / `embedding_latency_ms` | `INTEGER` | NOT NULL |
| `outcome` | `VARCHAR(16)` | `hit/empty/degraded/error` |
| `error_code` | `VARCHAR(64)` | NULL |
| `created_at` | `TIMESTAMPTZ` | NOT NULL |

按月分区/归档，索引 `(created_at)`、`(trace_id)`、`(outcome, created_at)`。用户原文默认不进长期评估集；需用于人工标注时经授权/治理流程复制到独立评估数据集。

#### 3.8.8 memory.items 长期记忆表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `id` / `memory_no` | `UUID` / `VARCHAR(40)` | PK / UK，`memory_no` 使用 `mem_` 前缀 |
| `user_no` | `VARCHAR(64)` | NOT NULL，MySQL 公开用户 ID |
| `namespace` | `VARCHAR(32)` | `exclusive/store`，影响可见范围 |
| `store_no` | `VARCHAR(64)` | NULL，店铺记忆必填，专属记忆可空 |
| `memory_type` | `VARCHAR(32)` | `preference/stable_fact/constraint/service_history` |
| `memory_key` | `VARCHAR(128)` | NOT NULL，稳定语义键，如 `shopping.preferred_colors`，用于冲突检测与定向修改 |
| `content_ciphertext` | `BYTEA` | NOT NULL，应用层信封加密 |
| `content_hash` / `dedupe_fingerprint` | `BYTEA` | NOT NULL，完整性/语义归一去重 |
| `key_version` | `SMALLINT` | NOT NULL |
| `source_type` / `source_ref` | `VARCHAR(32)` / `VARCHAR(128)` | NOT NULL，`user_message/user_confirmation/business_signal/user_settings` 及对应审计引用 |
| `source_conversation_no` / `source_message_no` | `VARCHAR(64)` | NULL，对话来源时填写并校验归属 |
| `consent_no` / `consent_policy_version` | `VARCHAR(64)` / `VARCHAR(32)` | NOT NULL，MySQL 授权快照 |
| `validation_snapshot` | `JSONB` | NOT NULL DEFAULT `{}`，脱敏保存来源类型、显式程度、规则结果与冲突标记 |
| `confidence` / `salience` | `NUMERIC(4,3)` | NOT NULL，`0..1` |
| `data_classification` | `VARCHAR(4)` | 用户关联偏好固定为 `L2`；L3 永不写长期记忆 |
| `memory_risk_level` | `VARCHAR(16)` | `low/medium/high/prohibited`；与数据分级独立，首版仅 `low` 可激活 |
| `valid_from` / `valid_until` / `expires_at` | `TIMESTAMPTZ` | NOT NULL / NULL / NOT NULL |
| `memory_status` | `VARCHAR(16)` | `candidate/active/superseded/revoked/expired/deleted` |
| `supersedes_memory_id` | `UUID` | NULL，冲突更新链 |
| `last_accessed_at` / `access_count` | `TIMESTAMPTZ` / `BIGINT` | NULL / NOT NULL DEFAULT 0 |
| `created_at` / `updated_at` | `TIMESTAMPTZ` | NOT NULL |

记忆是“有来源、有授权、可更正、可过期”的候选用户信息，不是事实汇总表。订单状态、收货地址、密码、证件、支付信息和一次性敏感问题不写入。用户偏好的数据分级始终是 L2，绝不因“低记忆风险”降为 L0/L1。店铺记忆不得跨店被检索；专属客服如需使用店铺交互历史，必须符合授权和最小化策略。

对 `active` 记忆建立部分唯一索引：`(user_no, namespace, COALESCE(store_no, ''), memory_type, memory_key) WHERE memory_status = 'active'`。同一语义键的新值先通过冲突流程，再以 `supersedes_memory_id` 替换旧版本；不得依赖向量相似度决定唯一性。

新增 `memory.item_embeddings`，字段为 `id、memory_id、embedding_model_id、embedding、dimension、input_hash、embedding_status、created_at`，且 `UNIQUE(memory_id, embedding_model_id)`。它与 `knowledge.chunk_embeddings` 使用相同的多模型、部分 HNSW 和双版本升级规则，但索引和访问权限完全分离。

#### 3.8.9 memory.events 记忆变更记录表

追加型，不保存无必要的记忆明文。

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `id` / `event_no` | `UUID` / `VARCHAR(40)` | PK / UK，`event_no` 使用 `mev_` 前缀 |
| `memory_id` / `user_no` | `UUID` / `VARCHAR(64)` | NOT NULL |
| `event_type` | `VARCHAR(32)` | `proposed/activated/accessed/corrected/superseded/revoked/expired/deleted` |
| `from_status` / `to_status` | `VARCHAR(16)` | NULL / NOT NULL |
| `actor_type` / `actor_no` | `VARCHAR(16)` / `VARCHAR(64)` | `user/agent/system/admin` |
| `reason_code` | `VARCHAR(64)` | NOT NULL |
| `content_hash_before` / `content_hash_after` | `BYTEA` | NULL |
| `consent_no` | `VARCHAR(64)` | NULL |
| `source_message_no` | `VARCHAR(64)` | NULL |
| `trace_id` / `ai_run_no` | `VARCHAR(64)` | NULL |
| `metadata_redacted` | `JSONB` | NOT NULL DEFAULT `{}` |
| `created_at` | `TIMESTAMPTZ` | NOT NULL |

索引 `(memory_id, created_at, id)`、`(user_no, created_at)`。用户更正/删除、Consent 撤销和 TTL 过期都必须产生 Event，供缓存、Embedding 和备份生命周期清理对账。

#### 3.8.10 memory.summaries 对话摘要表

| 字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `id` / `summary_no` | `UUID` / `VARCHAR(40)` | PK / UK，`summary_no` 使用 `sum_` 前缀 |
| `conversation_no` / `user_no` | `VARCHAR(64)` | NOT NULL |
| `store_no` | `VARCHAR(64)` | NULL，店铺会话必填 |
| `start_message_no` / `end_message_no` | `VARCHAR(64)` | NOT NULL，摘要覆盖的闭区间 |
| `message_count` / `source_token_count` / `summary_token_count` | `INTEGER` | NOT NULL |
| `summary_ciphertext` | `BYTEA` | NOT NULL |
| `summary_hash` / `source_hash` | `BYTEA` | NOT NULL |
| `key_version` | `SMALLINT` | NOT NULL |
| `model_name` / `prompt_version` | `VARCHAR(128)` / `VARCHAR(32)` | NOT NULL |
| `summary_status` | `VARCHAR(16)` | `active/superseded/revoked/deleted` |
| `supersedes_summary_id` | `UUID` | NULL |
| `quality_flags` | `JSONB` | NOT NULL DEFAULT `{}`，事实冲突/敏感信息等标记 |
| `expires_at` / `created_at` | `TIMESTAMPTZ` | NOT NULL |

约束：`UNIQUE(conversation_no, end_message_no, prompt_version)`。摘要是上下文压缩结果，不替代 MySQL `messages`；生成时必须保留未解决任务、当前业务引用和不确定性，不把模型推断写成用户已确认事实。

#### 3.8.11 agent_runtime.checkpoints Agent 检查点

实际 DDL 优先由已锁版本的 LangGraph PostgreSQL Checkpointer 官方迁移生成，本节规定逻辑字段和安全边界，不在业务迁移中擅自更改框架内部序列化协议。

| 逻辑字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `thread_id` / `checkpoint_ns` / `checkpoint_id` | `TEXT` | 复合主键或唯一键，与 LangGraph Config 一致 |
| `parent_checkpoint_id` | `TEXT` | NULL，Checkpoint 链 |
| `run_no` / `conversation_no` / `user_no` | `VARCHAR(64)` | NOT NULL，运行/权限关联 |
| `agent_version_no` / `graph_version` | `VARCHAR(64)` | NOT NULL，恢复时必须兼容 |
| `checkpoint_payload` | `BYTEA/JSONB` | 按官方 Serializer 保存，不接收不可信 Pickle |
| `metadata` | `JSONB` | NOT NULL，Step、Source、Parents 等最小元数据 |
| `serializer_version` | `VARCHAR(32)` | NOT NULL |
| `checkpoint_status` | `VARCHAR(16)` | `active/completed/failed/expired/deleted` |
| `expires_at` / `created_at` | `TIMESTAMPTZ` | NOT NULL |

运行中的业务写操作必须使用 `ai_tool_action_audits.action_no` 作为幂等键；Checkpoint 恢复后先查审计/业务结果，不仅凭 Graph Step 重新调用工具。Checkpoint 不保存密码、Token、支付密钥和完整退款凭证。

#### 3.8.12 agent_runtime.checkpoint_writes 中间状态

| 逻辑字段 | 类型 | 约束/内容 |
| :--- | :--- | :--- |
| `thread_id` / `checkpoint_ns` / `checkpoint_id` | `TEXT` | 对应 Checkpoint |
| `task_id` / `task_path` | `TEXT` | NOT NULL，节点任务标识 |
| `channel` | `TEXT` | NOT NULL，状态 Channel |
| `write_index` | `INTEGER` | NOT NULL，同任务顺序 |
| `value_type` | `TEXT` | NOT NULL，Serializer 类型标识 |
| `value_payload` | `BYTEA` | NOT NULL |
| `created_at` / `expires_at` | `TIMESTAMPTZ` | NOT NULL |

复合唯一键至少包含 `(thread_id, checkpoint_ns, checkpoint_id, task_id, write_index)`。中间写入只服务于 Graph 恢复，不对 API 直接暴露。Run 进入终态并超过恢复窗口后，先清理 Writes，再清理 Checkpoint；审计和消息不随之删除。

#### 3.8.13 HNSW 索引设计

默认使用 Cosine Distance，只在模型官方建议不同指标且离线评估通过时更改。每个模型使用部分表达式索引，示例中的维度和 Model UUID 由迁移生成器写死，不通过查询参数动态替换：

```sql
CREATE INDEX CONCURRENTLY idx_chunk_embeddings_model_v1_hnsw
ON knowledge.chunk_embeddings
USING hnsw ((embedding::extensions.vector(1536)) extensions.vector_cosine_ops)
WITH (m = 16, ef_construction = 64)
WHERE embedding_model_id = '00000000-0000-0000-0000-000000000001'
  AND embedding_status = 'active';
```

参数基线：`m=16`、`ef_construction=64`作为起点，Query `hnsw.ef_search=40`作为起点，不是生产最终值。调优基于真实数据量和标注 Query Set 的 Recall@K、MRR/nDCG、P95/P99 耗时、索引大小和写入成本。

约束：

- HNSW 是近似检索，小数据集/高价值评估保留无索引精确检索对照。
- 店铺、可见性、状态和 Model Filter 必须放在检索 SQL 中，不先取全平台 Top K 再在应用中过滤。
- 近似索引在过滤后可能返回不足 K 条；pgvector 0.8.x 启用 `hnsw.iterative_scan = strict_order` 或经评估的 `relaxed_order`，并设置 `hnsw.max_scan_tuples`/语句超时上限。
- 大型多租户中，共享 HNSW 会使租户 Filter 影响 Recall；当单店/店铺组数据达到门槛时评估 LIST Partition/独立表，但不为每个小店创建独立索引。
- `CREATE INDEX CONCURRENTLY` 不放在普通事务迁移中；建立后检查 Invalid Index、索引使用率和查询计划。

#### 3.8.14 关键词与向量混合检索

中文检索不盲目依赖 PostgreSQL 默认英文词典。统一 Text Analyzer 对文档和 Query 使用同版本中文分词、同义词、型号/品牌保留和停用词规则，生成 `search_lexemes`，再转为 `TSVECTOR`。如选用第三方 PostgreSQL 中文分词扩展，必须固定版本、做供应链审查和备份恢复演练；否则在应用 Worker 分词。

检索流程：

```text
1. 鉴权并确定 platform/store/product 可见范围
2. 规范化 Query，识别商品型号、订单号等精确实体
3. 关键词路：GIN + ts_rank_cd，取 Top N_keyword
4. 向量路：当前 Model HNSW，取 Top N_vector
5. 使用 RRF 按名次融合，可加业务可见性/新鲜度的有界权重
6. 去重、文档/来源多样性约束，取 Top K
7. 可选 Reranker 重排，超时则回退至 RRF
8. 返回 Chunk + source_no + source_version + score，记 retrieval_log
```

RRF 参考公式：`score(d) = Σ 1 / (k + rank_i(d))`，`k` 与候选数通过评估集确定。不直接混加 Cosine Similarity 与 `ts_rank` 原始分数，因其尺度不同且随模型/词典变化。精确商品编号、政策条款和品牌型号对关键词路有优先权；语义问法主要依靠向量路。

#### 3.8.15 向量模型升级与重新索引

```text
注册 Candidate Model Profile（testing）
  → 用固定评估集检查维度/质量/成本/延迟
  → 创建新 Model 的 Embedding 行，不覆盖旧行
  → 建立新部分 HNSW 索引
  → 全量对账：Active Chunk/Memory 都有新向量
  → Shadow Query 对比 Recall、排名变化、延迟和空结果率
  → 1% → 10% → 50% → 100% 灰度切换 Query Model
  → 观察窗口内保留旧模型可一键回退
  → 停止旧模型新写，标记 retiring
  → 过保留窗口后删除旧向量/索引，标记 retired
```

切换配置按 Agent/Knowledge Namespace 版本化，不仅使用一个全局环境变量。升级期间新增/变更文档对新旧 Active/Candidate Model 双写向量；任一方失败不阻塞 MySQL 业务发布，但阻止检索流量切换。模型升级与 Chunking/Tokenizer 升级分开实验，避免无法归因质量变化。

#### 3.8.16 MySQL 与向量库同步

```text
MySQL 业务事务
  ├→ 修改 Product/FAQ/Policy/Consent
  └→ outbox_events（同事务）
          → Dispatcher / Redis Stream（可选传输缓冲）
          → PostgreSQL indexing_jobs
          → 按 Service API/对象引用获取当前原文
          → documents/chunks/embeddings
          → 标记 Document Active
```

同步规则：

- 以 MySQL `event_no` 作为消费幂等键，以 `source_version` 防止旧事件覆盖新数据。
- Event Payload 只含来源号、版本、内容哈希、作用域和文件引用；Worker 用服务身份获取内容，不把大文本/L2 数据放消息总线。
- PostgreSQL 写入完成不回写 MySQL 业务状态；索引运行状态在 PostgreSQL/可观测平台查询。
- 只有文档、切片、所需 Model Embedding 全部成功并完成对账后，才将新 Document 切为 Active；切换与旧版 Supersede 在同一 PostgreSQL 事务。
- 定时对账 MySQL Active Source Version 与 PostgreSQL Active Document Version，发现 Missing/Stale/Orphan 创建 Repair Job。
- 知识索引延迟进入 SLI；延迟超阈值时 Agent 对可能变化的信息改用实时 Tool 或明确提示无法确认。

#### 3.8.17 向量数据删除和重建

删除使用 Tombstone，不依赖“最新内容为空”来猜测。

```text
MySQL 来源删除/隐藏/Consent 撤销
  → Outbox Tombstone
  → 立即将 Document/Memory 标记 deleted/revoked（检索不可见）
  → 删除/隔离 Embedding
  → 按保留策略删除切片正文/密文
  → 失效 Redis 缓存
  → 写清理事件并对账孤儿向量
```

来源下架但仍有合法回滚窗口时，先改为不可检索状态，延迟物理删除；用户撤销长期记忆授权时优先停止可见并删除记忆 Embedding，再按政策清理密文。物理删除后依赖 PostgreSQL VACUUM 回收空间，监控死元组和 HNSW 索引膨胀；达到阈值时用 `REINDEX CONCURRENTLY`/新索引切换维护，不在业务高峰盲目全量重建。

全量重建从 MySQL/对象存储权威来源出发，写入新 `index_generation`或新 Model 数据集，校验行数、Source Version、Content Hash、Embedding 覆盖率和评估指标后原子切换路由。不对当前在线数据集做“先清空再重建”。

---

### 3.9 Redis 设计

Redis 承载可丢失的缓存、可重建的热点状态、有边界的协调信息和内部事件传输缓冲。业务正确性不依赖某个 Key 永不丢失；订单、支付、库存、退款、购物车、完整消息和授权事实仍以 MySQL 为准，Agent Checkpoint 以 PostgreSQL 为准。

生产环境建议至少拆分两个 Redis 部署/端点：

| 部署 | 内容 | 建议淘汰策略 | 故障影响 |
| :--- | :--- | :--- | :--- |
| `redis-cache` | 商品/店铺/购物车视图、AI 热上下文、未读计数 | `allkeys-lfu`（经容量测试） | 命中率下降，回源重建 |
| `redis-state` | 限流、会话撤销缓存、在线 Lease、Streams/PEL、少量锁 | `noeviction` | 拒绝/降级部分功能，不静默丢状态 |

两者使用独立连接池、容量告警和备份策略。仅用 Redis Logical DB 数字不能提供独立淘汰策略和资源隔离，不作为生产拆分方案。

#### 3.9.1 Redis Key 命名规范

统一格式：

```text
ecom:<env>:<domain>:<purpose>:<identity>[:<subkey>]:v<schema_version>
```

示例：

```text
ecom:prod:auth:session:01J...:v1
ecom:prod:cache:product:01J...:v3
ecom:prod:cart:view:01J_USER...:v1
ecom:prod:checkout:snapshot:01J_CHECKOUT...:v1
ecom:prod:agent:hot-context:01J_CONVERSATION...:v2
ecom:prod:rate:api:user:01J_USER...:orders-create:v1
ecom:prod:stream:business-events:v1
```

命名规则：

- `<...>` 只是文档占位符，不写入实际 Key。Redis Cluster 中的 `{...}` 专用于 Hash Tag，仅当 Lua/事务确需多 Key 同 Slot 时使用，如 `ecom:prod:cart:{01J_USER}:view:v1`。
- Identity 使用公开 ID/不可逆哈希，不放手机、邮箱、姓名、Token、商品名和任意用户输入。
- Schema Version 表示 Value 序列化协议，不是业务数据版本；升级时双读短期兼容，旧 Key 靠 TTL 清理。
- 每类 Key 在 Key Registry 文档登记 Owner、Redis 端点、数据类型、TTL，最大尺寸/基数、生产者、消费者和故障行为。
- 禁止在在线路径执行 `KEYS`、全量 `SCAN` 或无上限集合读取；运维清理使用限速 SCAN 和精确前缀。

#### 3.9.2 登录会话缓存

```text
Key:  ecom:prod:auth:session:<session_no>:v1
Type: Hash
TTL:  min(MySQL auth_sessions.expires_at - now, configured_max_ttl)
```

Value 只包含 `user_no、session_status、token_family_no、credential_version、permission_version、client_type、expires_at_ms、last_seen_bucket`，不保存 Refresh Token 原文、密码哈希、IP 原文或完整 User-Agent。

验证流程：验签短期 Access Token → 查 Session Cache/撤销标记 → Cache Miss 查 MySQL `auth_sessions` → 仅对活跃未过期 Session 回填。退出、修改密码、用户封禁和 Token 重放检测在 MySQL 事务提交后立即删除 Session Key，并写短期 `revoked:<session_no>` 拒绝 Key。高风险管理/支付/退款操作可强制回源 MySQL 或校验 Credential Version，不只信缓存。

Last Seen 按 5–15 分钟时间桶限频回写，不每个 API 请求写 Redis/MySQL。Redis 全丢失只导致回源/重新登录，不允许已撤销 Session 因缓存恢复而复活。

#### 3.9.3 商品和店铺缓存

| 缓存 | Key 示例 | 建议 TTL | 来源版本 |
| :--- | :--- | :--- | :--- |
| 商品详情 | `cache:product:<product_no>:v3` | 5–15 分钟 + Jitter | `products.version` + SKU/Policy 版本组 |
| SKU 展示价 | `cache:sku:<sku_no>:v2` | 1–5 分钟 + Jitter | `product_skus.version` |
| 店铺主页 | `cache:store:<store_no>:v2` | 10–30 分钟 + Jitter | `stores.version` + Policy Version |
| 短空结果 | `cache:miss:<type>:<no>:v1` | 10–30 秒 | 无，只防穿透 |

采用 Cache-Aside：读 Cache Miss 后查 MySQL，使用短 Lease/单航刷新抑制热点击穿，不影响没抢到 Lease 的请求直接回源。Value 必须携带 `source_version、generated_at、schema_version`。MySQL 修改后 Outbox Consumer 删除/更新缓存；回填用 Lua 只允许更高 Source Version 覆盖低版本，防止旧查询结果在失效后重新写回。

商品缓存中的库存是近似展示值，可使用更短 TTL；提交订单必须执行 3.7.5.5 的 MySQL 原子预占。禁止为每种价格/库存组合创建无界缓存 Key。

#### 3.9.4 购物车缓存策略

MySQL `carts/cart_items` 是购物车事实，Redis 只缓存已组装的展示视图：

```text
Key:  ecom:prod:cart:view:<user_no>:v1
Type: String（版本化 JSON）
TTL:  15–60 分钟 + Jitter
```

添加、改数量、勾选和删除流程是“MySQL 幂等事务 → 提交 → 精确删除用户购物车缓存”，不采用 Redis 先写、定时异步落库的 Write-behind。缓存视图保存 `cart_version`，旧版本不能覆盖新版本。

查询时价格/上下架/库存变化可在视图中标记，但不修改用户数量；结算时重新计价和校验。Redis 不可用时直接读写 MySQL，保证用户购物车可用，仅响应时间变长。

#### 3.9.5 结算临时数据

```text
Key:  ecom:prod:checkout:snapshot:<checkout_no>:v1
Type: String（压缩前限大小的 JSON）
TTL:  不超过 MySQL checkout_sessions.expires_at
```

可缓存 SKU 展示快照、店铺分组、价格/运费结果、`snapshot_hash`和 `pricing_version`；不保存完整地址明文、支付凭证或可被篡改的应付权威值。

提交时即使 Cache Hit，Service 仍需检查 MySQL Checkout 状态/到期时间、服务端重算或核对 Snapshot Hash，并在 MySQL 事务内完成单次提交。创建 Trade Order 后立即删除 Redis Key，TTL 是最终兜底，不是幂等保证。

#### 3.9.6 AI 热点上下文

```text
ecom:prod:agent:hot-context:<conversation_no>:v2   String/Hash，TTL 1–2 小时
ecom:prod:agent:active-run:<conversation_no>:v1   String，TTL 略长于 Run 超时
```

热上下文包含最近消息的 `message_no/sender/message_type/redacted_content/token_count`、当前业务上下文公开 ID、最新 Summary No、Agent Version 和上下文总 Token。约束：

- 按“条数 + Token + 字节数”三重上限裁剪，不将整个会话无界序列化进 Redis。
- 仅缓存 L0–L2 中本次必要且已脱敏的内容，不包含 L3、工具密钥、完整地址和私密凭证。
- MySQL Message/Context 或 PostgreSQL Summary 更新后通过版本化失效；Cache Miss 可重建，不阻止恢复。
- Active Run Key 只用于抑制同会话重复运行/界面状态，实际恢复以 PostgreSQL Checkpoint 为准，业务工具幂等以 MySQL Audit 为准。

#### 3.9.7 WebSocket 在线状态

每个连接生成随机 `connection_no`，不以 User No 当连接 ID：

```text
ecom:prod:ws:connection:<connection_no>:v1       Hash，TTL 60–90 秒
ecom:prod:ws:user-connections:<user_no>:v1      Sorted Set，score=lease_expires_at_ms
ecom:prod:ws:instance-connections:<instance>:v1 Set/Sorted Set，TTL/Lease 受控
```

建立连接后校验 Session，写连接 Hash 和用户/Instance 索引；心跳通过 Lua 原子刷新 Hash TTL 与 ZSET Score。查在线状态时先清理 Score 过期成员，一个用户可有多设备/页签。断线主动删除，Lease 超时兜底。

聊天消息先写 MySQL 并提交，再发布跨实例 WebSocket 通知；推送丢失时客户端使用最后 Message No/游标从 API 补拉。Pub/Sub 可用于这类可补拉瞬时信号，不用于唯一交付业务通知。

#### 3.9.8 未读消息计数

```text
Key:  ecom:prod:chat:unread:<user_no>:v2
Type: Hash（field=conversation_no, value=count:last_event_message_no）
TTL:  7–30 天，活跃用户滑动刷新
```

MySQL `message_reads.last_read_message_id/last_read_sequence_no` 是权威已读位点。新消息事务提交后，通过幂等 Event No 增加收件方缓存；仅当前端满足 2.4.3.2 的页面可见、消息已渲染和视口可见条件并提交最高位点后，服务端才原子推进 MySQL Read Cursor，再按新 Cursor 重算该 Hash Field。打开/点击会话本身、浏览器后台加载或 Skeleton 完成均不触发清零。如果“新消息”与“已读”竞争或 Redis 版本不连续，删除字段并从 MySQL 重建，不将负数/不确定计数展示给用户。

顶部总未读可在同一 Hash 计算后短缓存，不为所有消息永久保存一个单独计数事实。`unread.updated` Payload 返回服务端计算的绝对 `conversation_unread、total_unread、last_read_sequence_no、cursor_version`，多页签按 Version 接受新值而不做增减运算。专属客服置顶不影响计数语义。

#### 3.9.9 API 限流

限流按端点风险分层，同时支持 IP Hash、User No、Session No、Store No 和 Route Group 维度。默认使用 Redis Function/Lua 实现原子 Token Bucket（读取→补充→判断→扣减→设 TTL）；固定窗口只用于验证码等允许边界突发的低复杂度场景。

```text
ecom:prod:rate:api:ip:<ip_hash>:<route_group>:v1
ecom:prod:rate:api:user:<user_no>:<route_group>:v1
ecom:prod:rate:verify:<target_hash>:<purpose>:v1
```

一个请求需通过所有适用维度，不在其中选最宽松者。响应返回统一 429、`Retry-After`和不泄露安全阈值的错误码。配置版本化并可在管理面做受审计的紧急调整；登录/验证码另结合账户锁定、CAPTCHA/风控，不仅靠 IP 限流。

#### 3.9.10 Agent 调用限流

Agent 限流区分：

| 限制 | 维度 | 目的 |
| :--- | :--- | :--- |
| Run 请求数 | User/Conversation/Store/Agent Version | 防刷和避免同会话风暴 |
| 并发 Run | User/Conversation/Store/Global | 保护 Worker、Model 和下游 Tool |
| Input/Output Token | User/Store/Model/Day | 成本预算与滥用控制 |
| Tool Call | Tool/User/Store/Minute | 保护订单/物流/外部渠道 |
| 高风险写操作 | User/Resource/Action | 防止重复确认/重放 |

并发额度使用带 Lease 的 Semaphore：启动时按预估 Token 预留配额，完成后用实际 Usage 结算，超时/Worker 崩溃由 Lease 回收。预估过低时在输出 Token 上限停止生成，不允许负预算无界运行。拒绝原因区分频率、并发、Token/成本和工具配额，便于观测与用户提示。

高风险工具的业务幂等和权限始终在 MySQL 校验，Redis 额度通过不代表允许执行。

#### 3.9.11 分布式锁适用范围

适用场景：热点缓存单航重建、同一会话同时一个 Agent Run、同一文档/模型的重索引抑制、无严格事务要求的定时任务抑制。不适用于库存防超卖、支付/退款幂等、订单状态迁移、唯一会话创建和唯一数据约束；这些使用 MySQL/PostgreSQL 事务与唯一/版本条件。

锁实现基线：

```text
SET lock_key random_owner_token NX PX lease_ms
释放：Lua 比较 owner_token 一致后 DEL
续租：Lua 比较 owner_token 一致后 PEXPIRE
```

任务不得假设锁一定持续到完成；超过 Lease 后可出现两个持有者，因此最终写入仍需幂等键/版本检查，严格顺序写需额外 Fencing Token。获锁失败不默认忙等，根据场景回源、合并请求或稍后重试。

#### 3.9.12 Redis Streams 事件流

Streams 用于 Outbox 后的低延迟内部传输和可补拉异步任务，不作为 MySQL Outbox 的替代品。

```text
ecom:prod:stream:business-events:v1
ecom:prod:stream:notification-events:v1
ecom:prod:stream:ai-indexing-events:v1
ecom:prod:stream:retry-schedule:v1       # 如使用 Stream 而非 ZSET 调度
```

Entry 只包含 `event_no、event_type、schema_version、aggregate_type、aggregate_no、aggregate_version、occurred_at、trace_id、payload_ref/minimal_payload`。禁止放大文本、Embedding、完整地址、Token 或凭证。Dispatcher 在 MySQL 事务提交后 `XADD`；投递成功但 Outbox 未标记会产生重复 Entry，消费者必须用 `event_no` 幂等。

使用 `MAXLEN ~`/按时间 `MINID ~` 近似裁剪以限制内存，但保留窗口必须长于最大合理消费停顿。如 Redis 版本未锁定为支持 Consumer-group-aware Trim 的版本，不依赖新命令；先检查每个 Group 的 Pending/Lag 后再裁剪。可长期审计/重放的事件仍来自 MySQL Outbox/Archive，不依赖 Redis Stream 无限保留。

#### 3.9.13 消费者组与消息确认

不同业务目的使用不同 Group，如 `search-indexer-v1`、`cache-invalidator-v1`、`notification-dispatcher-v1`；同 Group 内多 Consumer 分担工作。Consumer Name 使用 `<service-instance>:<worker-no>` 且在实例内唯一。

```text
XREADGROUP GROUP <group> <consumer> COUNT <n> BLOCK <ms> STREAMS <stream> >
  → 验证 Schema Version/最大 Payload
  → 用 event_no 检查消费幂等
  → 执行 PostgreSQL/MySQL/外部写入
  → 事务提交并记录消费结果
  → XACK
```

只在业务结果成功持久化后 ACK。消费者崩溃会留在 PEL；监控 `XPENDING`、Group Lag、最老 Pending Age，用 `XAUTOCLAIM` 取回超时且原 Consumer 不健康的消息。Claim 不代表原执行一定失败，因此目标写入必须幂等。滚动发布期间同一 Group 的消费者必须兼容当前和上一 Event Schema。

#### 3.9.14 失败重试和死信处理

将失败分类：

| 类型 | 处理 |
| :--- | :--- |
| 瞬时错误 | 有上限指数退避 + Jitter，并遵守下游 `Retry-After` |
| 限流/过载 | 延长退避，不在 Stream Consumer 紧循环重试 |
| 永久数据错误 | 不重试，直接死信，如 Schema 不支持/必填字段缺失 |
| 权限/来源已删除 | 标记已忽略/成功消费 Tombstone，不无限重试 |
| 外部写结果未知 | 以幂等键查询/对账，未确认前不重复发起 |

不在 Consumer 进程中 Sleep 长时间占用 Pending Message；将下次时间写入 Retry ZSET/持久化任务表，到期再重新投递。超过次数/总时间上限后，以原 `event_no`、Payload Hash、脱敏错误、次数和 Trace 写入 MySQL `dead_letter_events`，再 ACK 原 Stream Entry；可选另写短期 DLQ Stream 供告警，但 MySQL 是受控重放事实。

#### 3.9.15 TTL 与内存淘汰策略

| 数据 | 建议 TTL/保留 | 过期后行为 |
| :--- | :--- | :--- |
| 空结果缓存 | 10–30 秒 | 重新回源 |
| 商品/SKU 缓存 | 1–15 分钟 + Jitter | 回源 MySQL |
| 店铺缓存 | 10–30 分钟 + Jitter | 回源 MySQL |
| 购物车视图 | 15–60 分钟 | 从 MySQL 重建 |
| 结算缓存 | 不超 MySQL Expires At（通常约 30 分钟） | 返回过期/重新结算 |
| Agent 热上下文 | 1–2 小时 | 从消息/摘要重建 |
| WebSocket Connection | 60–90 秒，心跳续租 | 视为离线 |
| API 限流 | 窗口/桶清空时间 + 小余量 | 重置额度 |
| Lock/Semaphore Lease | 略长于正常操作，有严格上限 | 允许新持有者，目标写仍幂等 |
| Stream | 按长度/时间裁剪，非 Key TTL | 从 Outbox/死信补偿 |

所有缓存 TTL 加 5%–20% Jitter 防集中过期，但安全/业务截止时间（Session、Checkout、Approval）不可因 Jitter 延长超过权威到期时间。对大 Value、高基数 Key、过期 Key 比例、Evicted Keys、Fragmentation Ratio、Hot Key 和 Big Key 设告警。压缩只对经测试的较大 JSON，并限制解压后大小防止压缩炸弹。

#### 3.9.16 Redis 故障降级

| 功能 | Redis 故障时 | 一致性/安全策略 |
| :--- | :--- | :--- |
| Session Cache | 限流回源 MySQL；持续故障可要求重新登录 | 不因故障接受已撤销 Session |
| 商品/店铺 | 直接查 MySQL，本地超短 Single-flight | 限制并发防回源风暴 |
| 购物车/结算 | 查 MySQL，响应可变慢 | 不从本地旧缓存创建订单 |
| AI 热上下文 | 从 MySQL Message/PostgreSQL Summary 重建，可减少历史窗口 | 不影响 Checkpoint/工具幂等 |
| 在线/未读 | 在线状态显示“未知”，未读从 MySQL 重算 | 不丢完整消息 |
| 普通读 API 限流 | 网关本地保守限流/有界 Fail-open | 按场景配置，指标告警 |
| 登录/验证码/高风险写 | Fail-closed 或使用 MySQL 保守计数 | 安全优先，不无限放行 |
| Agent/Model 调用 | 停止新高成本 Run 或使用实例级小额度 | 避免跨实例预算失控 |
| Streams | Dispatcher 不标记 Outbox Published，恢复后重投 | MySQL Outbox 保证最终可恢复 |
| Lock | 停止依赖该锁的可重复副作用，或转数据库 Lease | 不假装单实例锁等价分布式锁 |

客户端连接必须设置短 Connect/Command Timeout、有上限重试和 Circuit Breaker，不让所有 API Worker 阻塞等 Redis。故障恢复后分批预热，不全量扫 MySQL 立即回填所有缓存；首先恢复 Session/限流/Streams，再按实际访问热度恢复普通缓存。

---

### 3.10 对象存储设计

对象存储采用 S3 兼容协议，本地/私有化可使用 MinIO，云环境可使用兼容的托管服务。业务代码只依赖 `ObjectStorage` 适配器，不把供应商 SDK 调用分散到 Product/Review/Refund/AI Service。

文件划分：

| Bucket | 可见性 | 内容 | 基本控制 |
| :--- | :--- | :--- | :--- |
| `public-assets` | 公开/CDN | 已审核商品图、店铺 Logo、可公开头像/评价图派生版 | 只读公开派生文件，原图不直接公开 |
| `private-image-sources` | 私有 | 已完成上传校验、等待扫描或用于重建派生图的商品/Logo 原件 | 仅文件处理 Worker 可读，不复用客户端可覆盖的临时 Key |
| `private-certifications` | 严格私有 | 店铺认证证件原件 | 仅认证审核服务和文件处理 Worker 可读，禁止公开派生 |
| `private-user-assets` | 私有 | 用户头像原图、待处理用户文件 | 服务身份/短期签名 |
| `review-assets` | 受控原图 | 评价图原件和审核中文件 | 扫描/内容审核后才发布派生版 |
| `after-sale-evidence` | 严格私有 | 退款图片、视频、PDF 凭证 | 业务鉴权、短签名、访问审计、独立密钥 |
| `knowledge-source` | 严格私有 | AI 知识库原文件与解析中间件 | 仅 Indexing/管理角色访问 |
| `admin-job-artifacts` | 严格私有 | 商品/库存导入模板、错误报告、受控导出、评估报告 | Admin Audience、任务 Scope、短时下载 Ticket、访问审计 |
| `platform-content-source` | 私有原件/公开派生 | Banner、公告和帮助内容所引用的原图/附件 | 审核发布后仅公开安全派生文件 |
| `temporary-uploads` | 私有 | 尚未绑定、扫描或完成分段上传的文件 | 短生命周期、不允许业务页面直接读 |

全部 Bucket 默认 Block Public Access/私有 ACL，仅 `public-assets` 通过 CDN Origin Identity/等价机制读取，不给客户端 Bucket List 权限。开启服务端加密：普通公开资产使用托管加密，售后凭证/知识原文件使用 KMS/独立密钥并限制 Decrypt Role。

#### 统一对象 Key

```text
v1/<purpose>/<yyyy>/<mm>/<random-ulid>/<variant>.<normalized-ext>
```

例如 `v1/product/2026/08/01K.../w960.webp`。Key 由服务端生成，只含用途、时间分组、随机 ID 和受控 Variant，不包含用户上传文件名、手机号、用户名、店铺名或可枚举的 MySQL 自增 ID。文件一旦 Active 便不覆盖同 Key；更新产生新 Key，业务引用原子切换，CDN 可对内容寻址/版本 Key 使用长缓存。

#### 统一文件元数据

MySQL 权威表统一使用 3.7.15 的 `file_objects` 与 `file_upload_sessions`，本节不复制第二份字段定义。业务表保存已激活 Object Key/文件公开 ID 引用，`file_objects` 保存完整元数据、安全状态和生命周期，`file_upload_sessions` 保存一次上传租约、限制和 Complete/Abort 状态。3.10 的上传、压缩、访问和清理流程必须只使用 3.7.15 的字段与状态；如需扩展字段，先修改 3.7.15 和迁移，再更新本节流程说明。

#### 3.10.1 商品图片

商品图分为主图、图集、详情图和 SKU 规格图。流程：原图上传到 `temporary-uploads` → 格式/尺寸/像素检查 → 恶意内容和商品内容审核 → 生成多尺寸派生图 → 发布到 `public-assets` → 事务内绑定 `product_images`。

要求：

- 对象 Key 不包含 Product Name，原图和派生图各有独立 File Object，通过 `parent_file_id/variant/processor_version` 关系追踪；公开响应仍只返回 `file_id`。
- `product_images.object_key` 只能绑定 `purpose=product、scan_status=safe、file_status=active`且归属当前店铺的文件。
- 商家不能引用其他店铺 Object Key；同内容去重只是存储优化，不共享访问授权。
- 商品下架不立即删历史订单需要的展示快照；已无引用的图按 Grace Period 清理。
- 图片 Alt Text 存 MySQL，AI 可辅助生成，商家/管理员可修改，不写入图片文件名。

#### 3.10.2 店铺 Logo

店铺 Logo 与商品图使用同一扫描/派生管线，另增以下规则：

- 建议上传正方形或可裁剪图，生成 `64x64、128x128、256x256、512x512` 等方形版本；不在前端临时等比压缩代替后端标准化。
- 更换 Logo 先生成并验证新版，再原子更新 `stores.logo_object_key`；旧版过 CDN/Client 缓存窗口后清理。
- 涉嫌仿冒品牌/违规内容的 Logo 不发布，审核结果与管理员操作写 MySQL 审计。
- 公开 Logo 不应包含可利用 EXIF/GPS 元数据，派生时统一移除。

#### 3.10.3 用户头像

- 头像原图存 `private-user-assets`，仅处理 Worker 可读；公开/登录后展示使用移除 EXIF/GPS 且已安全处理的派生图。
- 产品需明确头像的可见性；若只在登录界面可见，派生图也使用短期签名/CDN Token，不放公开 Bucket。
- 头像输出固定为方形、限制分辨率，可保存用户裁剪参数，但需在服务端重新执行裁剪。
- 用户修改头像不覆盖旧 Key；切换后缓存失效。用户注销时按 3.5.10 删除非必要头像和派生版。
- 头像不默认用于人脸识别、训练或 Embedding；此类新用途需独立授权和数据治理审查。

#### 3.10.4 评价图片

- 评价图先存 `review-assets`，安全扫描、去 EXIF/GPS、内容审核完成后生成公开派生版并关联 `review_images`。
- 审核前评价文本可与图片共同处于 Pending；无论前端是否拿到临时 URL，公开 API 不返回未审核原图。
- 同一图片如可能包含收货单、手机号、人脸或地理信息，内容审核应支持拒绝/人工处理，不仅扫病毒。
- 评价被隐藏时公开派生图立即不再通过 API 返回并失效 CDN；是否保留私有原图按申诉/审计窗口执行。
- 评价图引用必须属于提交评价的用户 Upload Session，防止猜 Key 绑定他人图片。

#### 3.10.5 退款凭证

退款凭证属 L2/L3 高敏感数据，全程私有：

- 只有申请人、订单履约范围内的授权店铺客服、平台售后管理员和审核 Worker 可按用例访问；店铺权限还需匹配 `refund_application.store_id`。
- 下载/预览每次经 API 检查当前权限，返回 1–5 分钟短期 GET 签名 URL；记录文件、操作者、售后单、目的、结果和 Trace，日志不记签名 Query String。
- 原文件使用服务端加密/KMS 和独立密钥访问权；处理中间件不落公共临时目录，处理完立即清理。
- 图片预览生成脱敏/低分辨率版，视频转码在隔离 Worker 执行；不让浏览器直接打开未知类型原文件。
- 售后完成后仍按争议/合规保留窗口保留，到期后使用双阶段删除；不因用户在前端撤回一张图而破坏正在审核的完整性，但需记录撤回请求和可见性。

#### 3.10.6 AI 知识库原始文件

- 文件上传后状态为 `uploaded/scanning`，完成 MIME/哈希/恶意内容/压缩炸弹/密码保护检查后才创建 `knowledge.documents` 索引任务。
- Parser 在网络受限、文件系统隔离、CPU/内存/耗时有上限的 Worker 中运行；PDF/Office 宏、外部链接、嵌入对象和公式不被执行。
- 原文件、解析文本、OCR 结果和切片使用不同 Purpose/Key，各自保存 Content Hash 和 Parser/OCR Version；中间件只保留调试窗口。
- 店铺上传的知识文件绑定 Store No 和可见范围，不能被其他店铺 Agent 检索；平台知识文件由平台权限发布。
- 文件不可被 Prompt 中的文本指令视为系统指令；解析内容始终标记为不可信知识，用于检索证据而非 Agent 配置。
- 新版文件使用新 Key/Source Version，新向量全部就绪后切换；旧版经回滚窗口后清理。

#### 3.10.7 文件类型与大小限制

以下是初始安全基线，最终参数配置化并经容量/业务评审：

| 用途 | 允许格式 | 单文件上限 | 额外上限 |
| :--- | :--- | :--- | :--- |
| 商品图 | JPEG、PNG、WebP | 10 MiB | 40 MP，单商品数量按产品规则 |
| 店铺 Logo | JPEG、PNG、WebP | 5 MiB | 20 MP，默认不接受动图 |
| 用户头像 | JPEG、PNG、WebP | 5 MiB | 20 MP，默认不接受动图 |
| 评价图 | JPEG、PNG、WebP | 10 MiB | 40 MP，首版每评价最多 6 张 |
| 退款图片 | JPEG、PNG、WebP | 10 MiB | 首版图片/视频合计最多 6 个，并限制总容量 |
| 退款文档 | PDF | 20 MiB | 页数上限，不执行嵌入脚本 |
| 退款视频（如开启） | MP4（视频 H.264/H.265 需评估兼容） | 100 MiB | 图片/视频合计最多 6 个，时长默认最多 3 分钟 |
| 知识文件 | PDF、DOCX、TXT、Markdown、CSV | 100 MiB | PDF 页数/解析后字符数/表格行数上限 |
| 管理导入 | XLSX、CSV（按 Job 类型白名单） | 100 MiB | 行数、列数、公式、共享字符串和解压比上限；不执行宏/公式 |
| 管理导出 | 服务端生成 CSV/XLSX | 按异步任务配额 | 私有、短时保留；防 CSV 公式注入，下载需一次性 Ticket |
| 平台内容图片 | JPEG、PNG、WebP | 10 MiB | 40 MP；发布前生成安全派生图，不接受用户可执行 SVG |

不相信扩展名或客户端 `Content-Type`；同时检查扩展名、声明 MIME、Magic Bytes、实际 Decoder/Parser 结果和文件结构。默认拒绝 SVG/HTML/脚本/可执行文件/宏启用 Office/未在白名单的压缩包；如需 SVG，必须服务端严格消毒并栅格化后发布，不直接托管用户 SVG。

限制上传字节还不足以防止解码炸弹；图片限制总像素/帧数，视频限制时长/码率/轨道数，PDF/Office 限制页数、嵌入对象、解压比和解析资源。超限即拒绝，不在 Web Worker 中无界处理。

#### 3.10.8 上传签名和访问权限

直传标准流程：

```text
1. POST /api/v1/file-upload-sessions：鉴权、用途、数量/额度、MIME/大小声明
2. 服务端生成随机临时 Object Key 和单次 Upload Session
3. 返回 5–10 分钟 Presigned PUT/POST，绑定 Key、大小范围、Content-Type/校验和元数据条件
4. 客户端上传，不持有 Bucket Credential
5. POST /api/v1/file-upload-sessions/{upload_id}/complete：提交 Checksum/分段清单
6. 后端 HEAD 验证 Bucket/Key/Size/Checksum/Metadata，不信任 Complete 请求声明
7. 文件进入 scanning，Worker 执行类型检测、病毒/恶意文件、内容和解码限制
8. Complete 先将已校验字节复制到服务端控制的不可变私有 Key；安全后生成派生文件，由业务接口再次校验后绑定并标记 active
9. 临时对象到期清理
```

Presigned URL 是 Bearer Token，在到期前可能被重复使用，且对同 Key 上传可覆盖现有对象。因此每次 Session 使用新随机临时 Key、短有效期、应用层单次 Complete，并在供应商支持时使用条件写防覆盖。即使同一 URL 被再用，已完成 Session 也不重新绑定；最终 Active Object 使用不可变新 Key。

大文件根据阈值使用 Multipart Upload，后端为各 Part 签名并在 Complete 时校验 Part Number/ETag/Checksum；记录 Upload ID，失败/过期调用 Abort，并设 Bucket Lifecycle 清理未完成 Multipart。ETag 尤其在分段上传时不等于完整文件 MD5，不以 ETag 代替 SHA-256/供应商 Checksum。

访问规则：公开派生资产使用 CDN 域名；私有文件每次经 API 校验资源归属和权限后生成短期 GET URL。不接受客户端传入任意 Bucket/Key 请求签名；客户端传 File No/业务资源 No，Service 从元数据解析真实 Key。CORS 只允许明确前端 Origin、必要 Method/Header，不用 `*` 与凭证并用。

#### 3.10.9 图片压缩与缩略图

图片 Worker 管线：

```text
读取私有原图（限字节）
  → 校验 Decoder/尺寸/总像素/帧数
  → 按 EXIF Orientation 旋转后移除 EXIF/GPS/ICC 非必要信息
  → 转标准 sRGB，正确处理 Alpha
  → 以不放大原图为原则生成 320/640/960/1280 等宽度
  → 输出 WebP/AVIF（经兼容测试）+ JPEG/PNG Fallback
  → 写 Checksum/尺寸/编码版本并标记 Active
```

不通过 Query String 让图片服务无界生成任意尺寸；只提供预定义 Variant 白名单，防止资源消耗和 CDN 缓存爆炸。压缩质量按内容类型配置，不多次有损转码；新编码策略生成新 Variant/Processor Version，可灰度切换和回退。

文件处理必须幂等：`source_sha256 + processor_version + variant` 唯一。Worker 超时或重试不创建无界重复对象。图片处理失败时业务资源保持 Pending/原图私有，不将未处理原图当作公开降级。

#### 3.10.10 文件清理策略

清理使用双阶段状态机：

```text
active/rejected/uploaded
  → pending_delete（禁止新签名、业务不再返回）
  → Object Delete/Version Delete
  → HEAD 确认不可见
  → deleted（记录删除时间/结果）
```

基线规则：

| 对象 | 建议策略 |
| :--- | :--- |
| 未完成 Upload Session/孤儿临时文件 | 24 小时内过期，定时清理 |
| 未完成 Multipart | Lifecycle 默认 1–7 天 Abort，与最大合法上传时间匹配 |
| 扫描拒绝文件 | 隔离最小排障窗口后删除，仅保留哈希/错误审计 |
| 旧商品/Logo/头像派生版 | 引用为 0 且缓存/Order Snapshot Grace Period 结束后删除 |
| 管理导入原文件/错误报告/导出产物 | 使用 Job `expires_at`，通常 1～7 天；过期禁止下载并删除对象，审计和汇总按政策保留 |
| 已撤回平台内容派生文件 | CDN 失效后进入 Grace Period；仍被历史版本/审计引用的私有原件按内容政策保留 |
| 隐藏评价原图 | 申诉/审计保留窗口后按策略删除 |
| 退款凭证 | 售后、争议、合规保留期结束后受控删除 |
| 知识原文件旧版 | 新索引成功、回滚窗口结束且无引用后删除 |

删除 Worker 按 File No 幂等，使用小批量、限速和有上限重试；失败进入 `dead_letter_events` 并告警。`reference_count` 只是快速判断派生值，删除前必须向各业务引用表执行对账，防止计数漂移误删共享对象。业务事务与对象删除无法原子提交：MySQL 先标记 `pending_delete` 并写 Outbox，Worker 再删对象并回写结果。

开启 Bucket Versioning 可用于误覆盖/灾备，但“删除当前版本”可能只产生 Delete Marker；隐私/合规物理删除必须处理所有相关 Object Version 并纳入备份保留策略。Lifecycle Rule 是兜底，不取代业务删除事件和审计。定期对账“数据库 Active 但对象不存在”、“对象存在但无元数据”、“Pending Delete 超时”和“未完成 Multipart 超期”四类差异。

---

### 3.11 API 设计规范

本节是 Vue 3 前端、FastAPI、Agent/MCP Tool、管理后台和第三方回调共同遵守的接口合同。API 必须表达 Domain 真实能力，不将数据库表结构原样暴露给前端；前端和 Agent 都不能通过伪造状态、金额或权限字段绕过业务规则。

#### 3.11.1 REST API 基础规范

- 所有生产 API 只通过 HTTPS 提供；默认请求/响应使用 UTF-8 JSON，`Content-Type: application/json`。
- 用户 API 使用 OAuth2 Bearer Access Token/等价短期凭证；Cookie 方案如开启，必须使用 `HttpOnly + Secure + SameSite` 和 CSRF 防护。
- `GET` 读取、`POST` 创建/命令、`PUT` 幂等整体设置/确定性子资源、`PATCH` 局部修改、`DELETE` 删除或用户侧隐藏。不使用 `GET` 执行写操作。
- `GET/DELETE` 不使用 Request Body；筛选放 Query Parameter，复杂查询/资格检查使用命名清晰的 `POST ...-checks` 且明确无业务副作用。
- 订单取消、确认收货、人工接管等使用“命令子资源”，例如 `POST /orders/{order_id}/cancellations`，避免无语义的 `/doAction`。
- 每个请求都在服务端校验身份、作用域、资源归属和状态机；“已登录”不等于可读取任意 ID 对应资源。
- API 只接受/返回公开 ID；字段名可使用 `id、user_id、order_id`，但其值必须是 3.6.2 的 ULID/业务号，永不是 MySQL 自增主键。
- 设置 Request Body、Query String、Header 数量、嵌套深度和数组长度上限；文件不通过 JSON Base64 上传。
- CORS 只允许明确的前端 Origin、Method 和 Header，不将带凭证请求与 `Access-Control-Allow-Origin: *` 并用。

#### 3.11.2 `/api/v1` 版本规范

- 公开业务 API 基础路径为 `/api/v1`；例如 `/api/v1/products/{product_id}`。
- `v1` 是对外合同大版本，不是后端微服务版本、数据库版本或前端发布号。
- 同一 v1 允许添加可选字段、新端点、新可选枚举值（客户端必须对未知值安全降级）；不允许删字段、改类型、改语义或将可选改成必填。
- 内部 Agent/MCP 如复用 HTTP API，仍使用相同 v1 语义和 Service Identity，不开一套可绕过权限的“AI 内部接口”。
- `/health/live`、`/health/ready`、`/metrics` 等运维端点不属于业务 API 版本，只向内网/监控身份开放。
- 外部 Webhook 也带合同版本，本项目使用 `/api/v1/webhooks/...`，与对外页面 API 分配独立限流/鉴权策略。

#### 3.11.3 URL 与资源命名规范

- 资源路径使用小写英文、复数名词和连字符 `-`：`/refund-applications`、`/agent-runs`。
- Path Parameter 在 OpenAPI/Code 中使用 `snake_case`：`{order_id}`；实际 URL 值必须 URL Encode。
- 层级只表达强归属，通常不超过 3–4 层：`/orders/{order_id}/items`。可独立定位的资源提供顶层路径：`/refund-applications/{refund_id}`。
- 用户自身资源使用 `/users/me`，不接受请求体中的 `user_id` 决定操作对象。
- 搜索使用 `/products?q=...`，不使用 `/searchProducts`；复杂预检使用 `/refund-eligibility-checks`。
- 确定性唯一子资源可用单数语义：`PUT /stores/{store_id}/customer-service-conversation` 表示“获取或创建该用户与该店铺的唯一客服会话”。
- URL 不放 Access Token、手机、邮箱、收货地址、Prompt 原文和私有文件 Key。WebSocket 特殊场景使用一次性短 Ticket，不使用长期 Bearer Token 查询参数。

#### 3.11.4 请求字段命名规范

- JSON、Query Parameter 和 OpenAPI Schema 统一使用 `snake_case`；Vue/TypeScript 生成类型不再做全局 CamelCase 转换。
- Vue Router 的占位符可使用 `:storeId/:orderId/:checkoutId` 等 camelCase 以符合组件习惯，但它只存在于路由层；调用生成 Client 时必须显式映射为 `store_id/order_id/checkout_id`。URL Query 与 API Query 不做该例外，统一使用 `q、group_id、sort、cursor` 等 snake_case；禁止兼容 `keyword/groupId` 别名造成双缓存键。
- 公开主键字段使用 `id`，引用其他资源使用 `store_id、order_id、order_item_id`；号码有人类业务语义时可返回 `order_no/payment_no/refund_no`。
- 本文和 OpenAPI 中的 `*_id` 默认指带资源前缀的公开 ID；数据库内部自增键必须写成 `internal_id/表名.id` 并只存在 Repository/迁移说明，严禁进入公开 Schema、Vue Route、日志业务引用或前端状态。
- 布尔使用 `is_/has_/can_`，时间点使用 `_at`，日期使用 `_date`，数量使用 `_count/_quantity`，状态使用 `_status`。
- 创建/更新 Request Schema 与 Response Schema 分离；不允许客户端传 `created_at、paid_at、user_id、reviewed_by、payment_status、refunded_amount` 等服务端字段。Pydantic 对未定义字段默认拒绝，防止 Mass Assignment。
- 枚举值使用小写 `snake_case`，例如 `pending_payment`；客户端对未知值显示通用状态并上报监控，不直接崩溃。
- 文本字段明确最大长度和空白归一化规则；不用 `null`、空字符串和缺失字段表达三种未文档化语义。PATCH 中“缺失”表示不修改，`null` 只在 Schema 明确允许时表示清空。

#### 3.11.5 时间和时区规范

- API 时间点使用 RFC 3339 字符串，服务端默认输出 UTC `Z`，例如 `2026-08-21T08:30:15.123456Z`。
- 请求可接收有明确偏移的时间，入库前转 UTC；拒绝无时区时间 `2026-08-21 16:30:00`。
- 纯日期使用 `YYYY-MM-DD`，如生日/有效期；不附加伪时区。时长使用整数且字段名带单位，如 `timeout_seconds`。
- 列表筛选的 `created_from/created_to` 是时间点，建议半开区间 `[from, to)`，防止微秒边界重复/遗漏。
- 前端使用用户 Timezone 展示，不修改原始 Instant；物流渠道无时区时间由 Integration 按渠道文档解释并保存原值/解析结果。

履约预计时间使用公共 `ServiceEstimate` Schema，商品详情、Checkout Store Group、Order/Shipment 投影和 Agent Tool Result 只能复用该 Schema，不再定义简化字符串或第二套字段：

```json
{
  "estimate_type": "delivery",
  "status": "available",
  "min_at": "2026-08-22T00:00:00+08:00",
  "max_at": "2026-08-23T23:59:59+08:00",
  "source": "carrier",
  "source_updated_at": "2026-08-20T09:15:00+08:00",
  "calculated_at": "2026-08-20T09:15:01+08:00",
  "timezone": "Asia/Shanghai",
  "disclaimer_code": "ESTIMATE_ONLY"
}
```

- `estimate_type` 只允许 `dispatch/delivery`；`source` 只允许 `store_fulfillment_profile/shipping_template/carrier`。
- `status=available` 要求 `min_at/max_at/source/source_updated_at/timezone/disclaimer_code` 完整且 `min_at <= max_at`；`status=unavailable` 时两个范围字段必须为 NULL，可返回安全 `unavailable_reason_code`。
- 单点上游值只能经发布的确定性业务策略转为范围；无合理策略时返回 `unavailable`。窗口超过来源新鲜度阈值必须失效，不继续对外显示旧日期。
- 该结构是预计而非承诺；API/Tool 不返回模型生成的预计日期。前端和 Agent 统一按 2.2.7 映射来源、更新时间、范围和“仅供参考”。

#### 3.11.6 金额传输规范

API 使用统一 `Money` Schema：

```json
{
  "minor_units": "899900",
  "currency": "CNY"
}
```

- `minor_units` 是十进制字符串，表示 ISO 4217 最小货币单位；CNY `899900` 表示 8,999.00 元。使用字符串避免 JavaScript Number 精度与未来超范围问题。
- `currency` 是大写三字母币种码。不使用浮点值 `8999.00`、带货币符号字符串或前端格式化值参与计算。
- 请求中的金额只是用户选择/预期，支付、退款、结算由服务端从权威订单重新校验。客户端不传单价/总价创建订单。
- 折扣率/评分不复用 Money Schema；另用受约束 Decimal String/整数基点。

#### 3.11.7 成功响应结构

单资源：

```json
{
  "data": {
    "id": "ord_01K3...",
    "order_no": "ord_01K3...",
    "order_status": "pending_payment",
    "version": 3
  },
  "meta": {
    "request_id": "req_01K3..."
  }
}
```

列表：

```json
{
  "data": [
    { "id": "ord_01K3...", "order_status": "shipped", "version": 7 }
  ],
  "meta": {
    "request_id": "req_01K3...",
    "pagination": {
      "previous_cursor": "opaque-token-or-null",
      "next_cursor": "opaque-token-or-null",
      "has_previous": false,
      "has_next": true,
      "limit": 20
    }
  }
}
```

规则：

- `data` 是业务结果，`meta` 是请求/分页元数据；不再添加永远为 0/true 的 `code/success`。
- 对 Order/Payment/Refund 等直接以业务号定位的资源，通用 `id` 与 `order_no/payment_no/refund_no` 可承载同一个规范值；保留具名号码字段是为了业务展示和搜索语义，不得生成一个数据库中不存在的第二公开 ID。
- 创建资源返回 `201 Created`、完整资源和 `Location` Header；普通读取/更新返回 `200`。
- 无响应体成功删除可返回 `204 No Content`，不同时返回 JSON。如前端需要最新聚合状态，返回 `200` 而非 204。
- 文件流、SSE、WebSocket 和第三方 Webhook 响应遵守各自 Media Type/协议，不套 JSON Envelope。
- 可变资源的 GET/Mutation 成功响应返回 `ETag` 和最新 `version`。

#### 3.11.8 错误响应结构

错误统一使用 RFC 9457 `application/problem+json`，并扩展稳定业务错误码：

```json
{
  "type": "https://api.example.com/problems/order-state-conflict",
  "title": "Order state conflict",
  "status": 409,
  "detail": "当前订单状态不允许取消。",
  "instance": "urn:request:01K3REQ...",
  "code": "ORDER_STATE_CONFLICT",
  "request_id": "req_01K3...",
  "retryable": false,
  "errors": [
    {
      "pointer": "/order_id",
      "code": "INVALID_STATE",
      "message": "订单已发货。"
    }
  ]
}
```

- HTTP Status 是协议语义，`code` 是客户端稳定分支依据，`detail/message` 是可本地化人类文本；前端不解析中文文本判断业务。
- `type` 是稳定、可查文档的 Problem Type URI；通用错误可使用 `about:blank`。`status` 必须与实际 HTTP Status 一致。
- `errors` 只在字段/批量对象错误时提供，`pointer` 使用 JSON Pointer 语义；不返回 Pydantic 内部结构、SQL、堆栈、文件路径、模型 Prompt 或第三方密钥。
- 对无权访问且暴露资源存在性会造成风险的场景返回 404，不区分“存在但无权”。
- `retryable=true` 仅表示在满足 `retry_after`/新鲜状态/幂等条件后可重试，不授权前端无上限自动重放写操作。

#### 3.11.9 业务错误码规范

格式：`<DOMAIN>_<REASON>`，大写 `SNAKE_CASE`，一旦发布不改语义/不重用。

| Domain | 示例错误码 | HTTP Status |
| :--- | :--- | :---: |
| `AUTH` | `AUTH_INVALID_CREDENTIALS`、`AUTH_SESSION_REVOKED`、`AUTH_VERIFICATION_EXPIRED` | 401/422/429 |
| `USER` | `USER_ACCOUNT_LOCKED`、`USER_PASSWORD_MISMATCH`、`USER_IDENTIFIER_ALREADY_BOUND`、`USER_CONTACT_CHANGE_EXPIRED` | 403/409/410/422 |
| `ADDRESS` | `ADDRESS_NOT_FOUND`、`ADDRESS_NOT_SERVICEABLE` | 404/422 |
| `PRODUCT` | `PRODUCT_NOT_FOUND`、`PRODUCT_NOT_ON_SALE`、`SKU_DISABLED` | 404/409 |
| `CART` | `CART_QUANTITY_EXCEEDED`、`CART_VERSION_CONFLICT` | 422/412 |
| `CHECKOUT` | `CHECKOUT_SESSION_EXPIRED`、`CHECKOUT_PRICE_CHANGED`、`CHECKOUT_SNAPSHOT_MISMATCH` | 410/409 |
| `INVENTORY` | `INVENTORY_INSUFFICIENT`、`INVENTORY_FROZEN` | 409 |
| `ORDER` | `ORDER_NOT_FOUND`、`ORDER_STATE_CONFLICT`、`ORDER_NOT_HIDEABLE` | 404/409 |
| `PAYMENT` | `PAYMENT_AMOUNT_MISMATCH`、`PAYMENT_ALREADY_SUCCEEDED`、`PAYMENT_RESULT_UNKNOWN` | 409/503 |
| `SHIPMENT` | `SHIPMENT_NOT_AVAILABLE`、`SHIPMENT_PROVIDER_UNAVAILABLE` | 404/503 |
| `REFUND` | `REFUND_NOT_ELIGIBLE`、`REFUND_DUPLICATE_ACTIVE`、`REFUND_AMOUNT_EXCEEDED` | 409/422 |
| `REVIEW` | `REVIEW_WINDOW_CLOSED`、`REVIEW_ALREADY_EXISTS` | 409 |
| `CONVERSATION` | `CONVERSATION_NOT_FOUND`、`CONVERSATION_NOT_DELETABLE` | 404/409 |
| `AGENT` | `AGENT_UNAVAILABLE`、`AGENT_QUOTA_EXCEEDED`、`AGENT_RUN_CONFLICT` | 429/503/409 |
| `TOOL` | `TOOL_NOT_AUTHORIZED`、`TOOL_APPROVAL_REQUIRED`、`TOOL_RESULT_UNKNOWN` | 403/409/503 |
| `FILE` | `FILE_TYPE_NOT_ALLOWED`、`FILE_TOO_LARGE`、`FILE_SCAN_REJECTED` | 413/415/422 |
| `IDEMPOTENCY` | `IDEMPOTENCY_KEY_REUSED`、`IDEMPOTENCY_IN_PROGRESS` | 409 |
| `RATE_LIMIT` | `RATE_LIMIT_EXCEEDED` | 429 |

错误码目录需记录含义、HTTP Status、是否可重试、前端建议动作和首次引入版本。异常类与 Error Code 分离，同一个内部异常不应在不同 Router 随意映射为不同对外码。

#### 3.11.10 HTTP 状态码规范

| Status | 使用场景 |
| :---: | :--- |
| `200 OK` | 查询、更新、命令同步完成并返回最新资源 |
| `201 Created` | 创建订单、退款申请、评价、工单等 |
| `202 Accepted` | 已可靠接受但需异步完成，返回 Task/Run 资源查询方式 |
| `204 No Content` | 成功取消收藏/无需响应体的删除 |
| `400 Bad Request` | JSON 无法解析、Cursor 伪造、参数组合无法理解 |
| `401 Unauthorized` | 缺少/失效/过期认证，必要时带 `WWW-Authenticate` |
| `403 Forbidden` | 身份有效但没有权限，且允许暴露资源存在 |
| `404 Not Found` | 资源不存在，或为防止 IDOR 隐藏无权资源 |
| `409 Conflict` | 业务状态冲突、重复资源、幂等 Key 复用/处理中 |
| `410 Gone` | 客户端依赖的短期资源/不可恢复流已明确过期，如无法重建的旧 SSE Cursor |
| `412 Precondition Failed` | `If-Match`/ETag 失配 |
| `413 Content Too Large` | Request/File 超过限制 |
| `415 Unsupported Media Type` | Content-Type/文件格式不支持 |
| `422 Unprocessable Content` | Schema/字段业务值校验失败，请求结构可解析 |
| `428 Precondition Required` | 可并发修改资源缺少必需 `If-Match` |
| `429 Too Many Requests` | API/Agent/Tool 配额超限，带 `Retry-After` |
| `502 Bad Gateway` | 已获得下游无效响应，且当前请求无法正常完成 |
| `503 Service Unavailable` | 暂时不可用/断路降级，可带 `Retry-After` |
| `504 Gateway Timeout` | 调用下游超时且尚未取得确定结果 |

不将业务错误包在 `200 OK` 中。支付/退款外部超时时必须区分“确定失败”和“结果未知”，未知状态返回可查询资源且禁止客户端无条件重建交易。

#### 3.11.11 请求 ID 与链路 ID

| 标识 | 来源 | 传播 |
| :--- | :--- | :--- |
| `X-Request-ID` | 网关验证合法 `req_<26-char-ulid>` 或标准 UUID，无/不合法时重新生成 | 每个 HTTP 请求唯一，响应 Header/Meta/Problem 返回 |
| W3C `traceparent` | 受信网关/服务创建或继续 | FastAPI、Worker、Agent、MCP、数据库、外部适配层传播 |
| `event_no` | Outbox/事件生产者 | 异步消费和死信幂等 |
| `ai_run_no` / `action_no` | Agent Runtime/MySQL Audit | 关联消息、SSE、Checkpoint和 Tool Audit |

不盲信互联网客户端传入的 Trace Sampling/Trace ID；由边界层校验/重建并保留必要 Baggage 白名单。日志使用公开资源 ID、Request/Trace/Event/Run ID 关联，不放 Token、签名 URL、地址和 Prompt 原文。返回给用户的 Request ID 可供客服定位，不允许用 Request ID 未鉴权查内部 Trace。

#### 3.11.12 分页、排序与筛选

- 默认使用 Cursor Pagination：`?limit=20&cursor=<opaque>`，默认 20，普通 API 上限 100，消息上限更低。
- Cursor 是服务端签名的 Base64URL 不透明 Token，包含排序键、方向、Filter Hash、Schema Version 和可选到期时间；不暴露 SQL/内部主键细节。
- 普通列表响应统一返回 `previous_cursor、next_cursor、has_previous、has_next、limit`。首批的 `previous_cursor=null/has_previous=false`，末批的 `next_cursor=null/has_next=false`；前端只能原样回传对应 Cursor，不能自行解析或拼装。仅支持向后加载的端点必须在 OpenAPI 明确声明，并省略上一批控件。
- 伪造/损坏 Cursor 返回 400 `PAGINATION_CURSOR_INVALID`；过期返回 410 `PAGINATION_CURSOR_EXPIRED`，前端从首页重试。
- Cursor 只能与生成时相同 Filter/Sort 使用；修改筛选条件时前端清空 Cursor。
- 排序使用白名单 `sort=-created_at` 或预定义值 `sort=newest/price_asc`，不将客户端字符串直接拼到 `ORDER BY`。每个排序增加唯一 Tie-breaker。
- 筛选字段显式定义，如 `status、store_id、category_id、created_from、created_to`；空值、多值、区间语义在 OpenAPI 文档化。
- 默认不返回精确 `total_count`，避免大表每页 Count；确有产品需求时提供独立聚合值/近似计数并标注语义。
- 管理后台小型稳定字典表可使用 Offset Pagination，必须在端点文档单独声明，不与 Cursor 参数混用。

#### 3.11.13 Cursor 消息分页

消息使用专用方向 Cursor：

```http
GET /api/v1/conversations/{conversation_id}/messages?before_cursor=<cursor>&limit=30
GET /api/v1/conversations/{conversation_id}/messages?after_cursor=<cursor>&limit=30
```

- 首次进入不传 Cursor，返回最新 N 条，`data` 仍按旧→新排列，前端无需反转气泡。
- 上滑历史使用 `before_cursor`，断线补拉使用 `after_cursor`，两者不同时传。Cursor 绑定 Conversation ID、方向和 Message Sort Key。
- `before_cursor` 响应返回 `previous_cursor/has_older`；`after_cursor` 返回 `next_cursor/has_newer`。可统一放 `meta.pagination`，但字段语义必须清晰。
- 新消息插入不会使用户上滑页面重复/跳页；数据库使用 `(conversation_id, sequence_no)` 稳定顺序。
- 撤回/隐藏消息仍保留位点，返回可见性占位事件，不因物理缺号破坏 Cursor。
- 读取任何页前校验当前用户是会话参与者/授权客服；Cursor 不是授权凭证。

#### 3.11.14 幂等请求规范

以下接口必须携带 `Idempotency-Key`：注册/密码修改与重置、订单创建、支付创建、退款申请与退款申诉、确认收货、订单取消、订单恢复、再次购买、评价提交、人工工单创建、Agent Run 创建、Agent 写工具执行和文件 Complete。

规则：

- Key 为 16–128 字符 ASCII，由客户端生成高随机 ULID/UUID，不使用用户 ID+时间秒等可猜组合。
- 幂等作用域为 `(authenticated_actor, HTTP method, route operation_id, Idempotency-Key)`；服务端保存规范化 Request Hash。
- 同 Key+同 Request Hash：已成功时重放原结果并返回 `Idempotent-Replayed: true`；处理中返回 409 `IDEMPOTENCY_IN_PROGRESS` 和 `Retry-After`。
- 同 Key+不同 Request Hash：返回 409 `IDEMPOTENCY_KEY_REUSED`，不执行新请求。
- 确定业务校验失败可按策略重放相同错误；结果未知的外部写不标记可重新执行，先查渠道/对账。
- 幂等记录保留时间至少覆盖客户端重试窗口；订单/支付/退款等高价值操作可保留至业务终态/审计窗口，不统一仅24小时就删除。
- GET/PUT/DELETE 的 HTTP 幂等语义不取代业务并发校验；DELETE 可返回已隐藏的当前结果，不重复产生流水。

#### 3.11.15 乐观锁与版本冲突

可并发编辑的用户资料、地址、购物车项、商品、店铺、AI 配置等返回强 ETag，如 `ETag: "v7"`。`PATCH/PUT/DELETE` 必须携带 `If-Match: "v7"`：

```text
无 If-Match              → 428 PRECONDITION_REQUIRED
If-Match 与当前版本不匹配 → 412 RESOURCE_VERSION_MISMATCH
匹配                    → WHERE id=? AND version=7 更新，返回 ETag "v8"
```

412 响应可返回 `current_version` 和安全的 `current_resource_url`，但不返回未授权最新数据。前端重新 GET 并展示冲突，管理员编辑不自动覆盖。

订单/支付/退款命令的主要冲突是 Domain State，服务端始终再校验状态并在不允许时返回 409；如端点同时要求 `If-Match`，先完成鉴权/资源存在检查，再按 HTTP 前置条件语义评估。幂等用于防重复，ETag 用于防丢失更新，两者不可互相替代。

#### 3.11.16 文件上传规范

使用 3.10.8 的三步 API，不用业务表接口直接传 Object Key：

```text
POST   /api/v1/file-upload-sessions
POST   /api/v1/file-upload-sessions/{upload_id}/parts     # 分段上传签名，如需
POST   /api/v1/file-upload-sessions/{upload_id}/complete
DELETE /api/v1/file-upload-sessions/{upload_id}
GET    /api/v1/files/{file_id}
GET    /api/v1/file-upload-policies/{purpose}
```

- Create Request 只声明 `purpose、filename、size_bytes、content_type、sha256`和可选业务上下文 ID；服务端决定 Bucket/Key/单文件与分段方式。
- 返回的 Presigned URL 是短期秘密，前端不写 Local Storage/日志/埋点，不通过聊天消息转发。
- Complete 必须携 `Idempotency-Key`，服务端 HEAD/Checksum 复核后返回 `file_id、file_status=scanning`；上传 HTTP 200 不代表文件已 Active。
- 前端轮询 File 或接收 WebSocket/SSE 状态；只有 `active` 才能传 `file_id` 绑定商品/评价/退款/知识文档。
- 业务绑定接口再次校验 Purpose、Owner、Status、数量与资源作用域；不因知道 File ID 就可引用。
- 传统 `multipart/form-data` 经 API 上传仅保留为小文件/内部工具备选，同样限制字节并进入扫描管线。
- `GET /file-upload-policies/{purpose}` 返回当前允许 MIME、扩展名、单文件大小、数量、总容量、像素/时长及策略版本。Vue3 在打开评价或退款表单时读取策略并生成 `accept`、提示和前端预校验；服务端仍执行同一版本规则，配置变化时返回稳定错误码并要求刷新策略，避免前后端硬编码漂移。

#### 3.11.17 OpenAPI 文档

- FastAPI 代码生成 OpenAPI 3.1.x，产物固定到代码库供 CI Diff/客户端生成；生产环境是否公开 Swagger UI 由安全策略决定。
- 每个 Operation 显式定义全局唯一、稳定 `operation_id`，如 `orders_list_my_orders`；路由函数改名不应自动改 Operation ID。
- 完整标注 Summary/Description/Tag、Security Scope、Path/Query/Header、Request/Response Schema、Money/DateTime/Cursor/Problem Details、所有主要 Status/Error Code 和可执行示例。
- 统一 Component Schema 避免同义不同名；Create/Update/Read/Admin Read 分开，不因复用一个 ORM Schema 泄露敏感字段。
- 标记 `readOnly/writeOnly/deprecated`，为枚举定义未知值处理策略，为多态使用 Discriminator；不用无约束 `object/additionalProperties: true` 代替实际 Schema。
- SSE 在 OpenAPI 中标注 `text/event-stream` 和事件类型链接；WebSocket 不能仅靠 OpenAPI 完整表达，需另维护版本化消息协议/AsyncAPI 或等价文档。
- CI 执行 OpenAPI Schema Validation、Lint、Operation ID 唯一、破坏性变更 Diff、示例验证和未文档化 5xx/Problem 检查。

#### 3.11.18 TypeScript API 客户端生成

从 CI 确认的 OpenAPI Artifact 生成 Vue 3 TypeScript Client，不手写重复 Interface。工具可选 OpenAPI Generator、Orval、openapi-typescript 等，但项目必须只选一条主生成链、锁定版本和配置。

建议目录：

```text
frontend/src/api/
├── generated/          # CI 生成，禁止手改
├── http-client.ts     # baseURL、Token、Request ID、超时、Problem 解析
├── query-keys.ts      # TanStack Query/等价查询 Key
├── adapters/          # 极少量 UI ViewModel 适配，不复制业务规则
└── realtime/          # SSE/WebSocket 版本化客户端
```

生成类型必须确保 `Money.minor_units` 是 `string`，DateTime 默认为 `string` 并在展示边界解析，Unknown Enum 有安全兜底。HTTP Wrapper 只对 GET/明确可重试请求进行有上限退避；写操作必须由调用方提供/保持同一 Idempotency Key，超时不自动换 Key 重发。

CI 先生成客户端再执行 Type Check/前端测试，检查 Git Worktree 无未提交的 Generated Diff；任何 OpenAPI 更改都必须与对应前端变更在同 PR 或有明确兼容计划。

#### 3.11.19 API 废弃与兼容策略

废弃分为“宣布”、“迁移窗口”和“下线”三阶段：

1. 先提供替代端点/字段和迁移文档，发布不破坏当前客户端的服务端版本。
2. OpenAPI 标记 `deprecated: true`，响应添加标准 `Deprecation` Header 和 `Link: <...>; rel="deprecation"`，文档说明替代方案。
3. 如已确定停服时间，增加 `Sunset` Header；Sunset 不早于 Deprecation，并留足 Vue 发布、回滚、移动端/第三方升级窗口。
4. 监控旧 Operation ID 的调用量、客户版本和错误，联系仍在使用的受管理消费者。
5. Sunset 到期后可返回 410 和迁移链接；彻底删除代码前继续观察边界请求。

必须升 `/api/v2` 的情况包括：删除/重命名客户依赖字段、改字段类型/含义、改鉴权模式、改金额/时间基础表示、重构资源边界或使旧枚举处理产生不同业务结果。添加新字段也不得改变旧字段的业务不变式。数据库迁移版本与 API 版本独立，不因 Alembic Revision 变化就对外升版。

---

### 3.12 API 接口清单

本节是前端、FastAPI 路由、领域服务和自动化测试共同遵循的接口基线。除支付、物流等第三方 Webhook 和 WebSocket 握手外，路径均以 `/api/v1` 为前缀；表格中省略此前缀。`用户`表示已登录买家，`店铺员工`必须同时通过 RBAC 与 `store_id` 数据范围校验，`平台管理员`必须通过后台权限点校验。公开读取接口仍执行限流、资源状态和字段可见性检查。

所有 ID 均为公开 ID；所有写操作统一使用 3.11 的 Problem Details、幂等、ETag 和审计规范。接口返回的 `available_actions` 是服务端根据资源状态、当前身份、时限和售后状态计算出的操作集合，前端只据此展示按钮，但服务端在真正执行命令时仍须重新校验，不能把按钮隐藏当作权限控制。

接口实现遵循以下共同约定：

- 列表接口默认只返回摘要 DTO；详情、管理端详情和内部模型分开，禁止直接序列化 ORM 对象。
- `POST` 创建成功通常返回 201；异步受理返回 202 并返回可查询的任务/运行资源；命令已同步完成可返回 200；无响应体删除返回 204。
- 表格中的“幂等”表示必须携带 `Idempotency-Key`；“If-Match”表示必须携带当前资源 ETag。
- 用户只能访问自己的地址、订单、退款、会话、文件和授权。不能以请求体中的 `user_id` 决定归属，归属必须来自 Access Token。
- 店铺订单、店铺客服和店铺知识仅能由对应店铺人员访问；平台管理员也必须有明确权限和访问理由，不默认穿透所有租户数据。
- 下列接口是首版契约清单，不代表每个接口都在首个迭代实现；每个迭代以 OpenAPI Artifact 和自动化契约测试为实际交付物。

#### 3.12.1 注册、登录与退出接口

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `GET` | `/auth/registration-config` | 匿名 | 返回当前地区/语言可用验证渠道、用户名/密码策略、验证码展示参数和必须接受的 Published 协议版本；不返回风控阈值或内部规则 |
| `GET` | `/content/legal-documents/{document_type}` | 匿名 | 按 `version、locale、region_code` 返回注册配置引用的精确不可变用户协议/隐私政策安全正文；不得用新版静默替换旧版本 |
| `POST` | `/auth/verification-codes` | 匿名/用户 | 发送注册、登录验证、找回密码或敏感操作验证码；换绑验证码必须携有效 `change_ticket_id` 并与新目标 Hash、`purpose` 绑定；按 IP、账号标识、设备限流，响应不泄露账号是否存在 |
| `POST` | `/auth/registrations` | 匿名 | 使用验证码注册并创建用户；幂等；密码仅通过 TLS 传输，服务端立即做强哈希，不记录明文 |
| `POST` | `/auth/login` | 匿名 | 账号密码或验证码登录；成功在 JSON 返回短期 Access Token 与当前用户/Session 摘要，Refresh Token 仅通过安全 Cookie 返回 |
| `POST` | `/auth/token-refresh` | 持有刷新凭证 | Refresh Token 轮换；旧 Token 进入重用检测，发现重放时撤销该 Token Family |
| `POST` | `/auth/logout` | 用户 | 撤销当前会话和刷新凭证；多次调用结果一致 |
| `GET` | `/auth/sessions` | 用户 | 查询当前账号活跃设备/会话，不返回 Token；标记当前会话 |
| `DELETE` | `/auth/sessions/{session_id}` | 用户 | 撤销指定会话；不可操作其他用户会话 |
| `DELETE` | `/auth/sessions` | 用户 | 撤销除当前会话外的全部会话；敏感操作需近期认证 |
| `POST` | `/auth/password-reset-tickets` | 匿名 | 验证验证码后签发一次性、短时密码重置 Ticket |
| `POST` | `/auth/password-resets` | 持有重置 Ticket | 重置密码并撤销既有会话；幂等且写安全审计 |

注册配置响应至少包含：

```json
{
  "config_version": "regcfg_2026_08_01",
  "locale": "zh-CN",
  "region_code": "CN",
  "verification_channels": ["phone", "email"],
  "username_policy": {
    "min_length": 4,
    "max_length": 32,
    "allowed_pattern": "^[A-Za-z0-9_]+$",
    "policy_version": "username_v1"
  },
  "password_policy": {
    "min_length": 15,
    "max_length": 128,
    "allow_unicode": true,
    "breached_password_check": true,
    "policy_version": "password_v3"
  },
  "verification_policy": {
    "code_length": 6,
    "expires_in_seconds": 600,
    "resend_hint_seconds": 60
  },
  "required_agreements": [
    {
      "document_type": "terms_of_service",
      "document_version": "terms_2026_08",
      "title": "用户协议",
      "content_url": "/content/legal-documents/terms_of_service?version=terms_2026_08",
      "content_hash": "sha256-base64url"
    },
    {
      "document_type": "privacy_policy",
      "document_version": "privacy_2026_08",
      "title": "隐私政策",
      "content_url": "/content/legal-documents/privacy_policy?version=privacy_2026_08",
      "content_hash": "sha256-base64url"
    }
  ]
}
```

`username_policy.allowed_pattern` 是供 UI 提示和生成客户端校验的受控字符串，不允许由前端当作可执行代码；服务端始终按 `policy_version` 的规则重新验证。`content_url` 只允许本站相对 URL 或配置白名单中的公开法务域名。注册页面仅可在当前页面内存中短暂复用配置，不写 Service Worker、持久缓存或离线包；提交必须携 `config_version`，服务端发现关键策略/协议已变化时返回可恢复冲突。

验证码发送请求和响应：

```json
{
  "purpose": "register",
  "target_type": "phone",
  "target": "+8613800000000",
  "locale": "zh-CN",
  "challenge_token": null
}
```

```json
{
  "verification_id": "ver_01K...",
  "delivery_status": "accepted",
  "target_masked": "+86 138****0000",
  "expires_at": "2026-08-22T10:10:00Z",
  "retry_after_seconds": 60
}
```

接口统一返回 202；`delivery_status=accepted` 只表示平台已接受发送请求，不证明目标存在、已注册或渠道最终送达。`target_masked` 只能由本次请求规范化后生成，不能回读数据库中的其他绑定目标。重发可生成新的 `verification_id` 并使同 Purpose/Target 的旧记录 `superseded_at` 生效；后续验证不能仅凭“最新验证码字符串”查找。

注册请求使用固定 Schema：

```json
{
  "username": "zhangsan_2026",
  "target_type": "phone",
  "target": "+8613800000000",
  "verification_id": "ver_01K...",
  "verification_code": "123456",
  "password": "由密码管理器生成的长密码",
  "config_version": "regcfg_2026_08_01",
  "agreement_acceptances": [
    {"document_type": "terms_of_service", "document_version": "terms_2026_08"},
    {"document_type": "privacy_policy", "document_version": "privacy_2026_08"}
  ],
  "locale": "zh-CN",
  "timezone": "Asia/Shanghai"
}
```

- 请求必须携 `Idempotency-Key`；幂等 Scope 绑定规范化 Target Hash 和 `config_version`，相同 Key 但关键 Payload Hash 不同返回 409 `IDEMPOTENCY_KEY_REUSED_WITH_DIFFERENT_PAYLOAD`。
- API 不接收 `confirm_password、role、user_status、is_admin、store_id、permission`。默认用户角色和所有 Scope 由服务端固定创建；Mass Assignment 字段由 Pydantic `extra=forbid` 拒绝。
- `agreement_acceptances` 必须与配置中所有 Required Agreement 精确覆盖，不允许缺失、重复、增加未知类型或接受尚未生效/已撤回版本。服务端按 Content Version 重新取得 Hash，不信任客户端传正文/Hash。
- 验证码先校验但只在核心注册事务内最终消费；用户名、Target 唯一约束处理并发。冲突后不得把验证码错误标为已消费，除非安全策略因尝试上限主动失效。

注册成功返回 201：

```json
{
  "user": {
    "user_id": "usr_01K...",
    "username": "zhangsan_2026",
    "nickname": "zhangsan_2026",
    "avatar_url": null,
    "account_status": "active"
  },
  "session": {
    "session_id": "ses_01K...",
    "client_type": "web",
    "authenticated_at": "2026-08-22T10:02:00Z",
    "expires_at": "2026-09-21T10:02:00Z"
  },
  "access_token": "仅本次响应返回的短期令牌",
  "token_type": "Bearer",
  "expires_in": 900,
  "csrf_token": "与新 Session 绑定的公开 CSRF Token"
}
```

Refresh Token 仅通过 `Set-Cookie` 返回，不出现在 JSON、URL 或日志。`nickname` 首版可在服务端使用通过内容安全校验的 Username 初始化，后续由资料页修改。专属客服会话无需塞进注册响应，消息页通过确定性获取接口读取；注册事务仍保证唯一会话已存在。

登录请求使用 `auth_method` 判别联合，不能让一个 Payload 同时携密码和验证码：

```json
{
  "auth_method": "password",
  "identifier": "zhangsan_2026",
  "password": "用户输入的密码",
  "client": {"client_type": "web", "device_name": "Chrome on macOS"},
  "challenge_token": null
}
```

```json
{
  "auth_method": "verification_code",
  "target_type": "email",
  "target": "user@example.com",
  "verification_id": "ver_01K...",
  "verification_code": "123456",
  "client": {"client_type": "web", "device_name": "Chrome on macOS"},
  "challenge_token": null
}
```

`client_type` 必须与使用的公开 Client/Origin 匹配，不能由 Payload 把 User 登录变成 Admin Audience；`device_name` 只作经安全过滤的用户会话展示，服务端设备/风险标识由可信上下文生成。登录成功使用与注册相同的 Session Response Schema 和 Cookie 策略，并额外返回安全 `redirect_capabilities` 时也只能是后端枚举，不接受任意 URL。

认证错误码和泄漏边界：

| 错误码 | HTTP | 可公开含义与字段 |
| :--- | :---: | :--- |
| `AUTH_INVALID_CREDENTIALS` | 401 | 匿名统一提示；不区分账号不存在、密码错误、验证码目标不存在或凭证禁用 |
| `VERIFICATION_CODE_INVALID` | 422 | 验证码不正确；只在已持有对应 `verification_id` 的流程返回 |
| `VERIFICATION_CODE_EXPIRED/CONSUMED` | 410/409 | 当前验证流程已过期/使用，允许重新发送；不泄露其他流程 |
| `AUTH_CHALLENGE_REQUIRED` | 428 | 返回批准的 `challenge_type、challenge_id、expires_at`，不返回风险分/规则 |
| `AUTH_RATE_LIMITED` | 429 | 返回 `Retry-After` 和安全 `retry_after_seconds` |
| `AUTH_ACCOUNT_UNAVAILABLE` | 403 | 只有凭证已经正确验证后才可返回账号冻结/关闭的安全说明与申诉入口 |
| `AUTH_PASSWORD_CHANGE_REQUIRED` | 403 | 返回短时受限 Ticket 与重置路由，不签发普通 Access/Refresh Token |
| `REGISTRATION_USERNAME_UNAVAILABLE` | 409 | 用户名不可用；用户名是公开标识但仍需限流防批量枚举 |
| `REGISTRATION_TARGET_ALREADY_BOUND` | 409 | 只有注册验证码已正确验证后返回，并提供登录/找回入口 |
| `AGREEMENT_VERSION_CHANGED` | 409 | 返回新的 `config_version` 和重新加载指示，不自动代用户勾选 |

验证码接口请求中的 `purpose` 必须与后续动作完全一致，不能把注册验证码用于重置密码。登录错误统一返回模糊提示；锁定、风控挑战和验证码倒计时可通过安全字段表达，但不能暴露用户是否注册。密码凭证命中 `must_change_password=true` 时，不签发普通业务会话，只签发短时、仅可调用密码重置接口的受限 Ticket；成功重置后原子清除该标志。浏览器优先把 Refresh Token 放在 `HttpOnly + Secure + SameSite` Cookie 中，并对刷新/退出接口执行 CSRF 防护。登录、注册和验证码响应必须设置 `Cache-Control: no-store`、`Pragma: no-cache`，不得被 CDN/Service Worker/浏览器页面缓存保存。

#### 3.12.2 用户信息与密码接口

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `GET` | `/users/me` | 用户 | 获取“我的”页面资料、头像、绑定账号掩码和安全状态；返回 ETag |
| `GET` | `/users/me/dashboard` | 用户 | 获取“我的”首页聚合摘要：订单状态计数、默认地址摘要、待评价/已评价计数、最近评价、收藏和未读入口计数；只返回小型投影，不返回完整列表 |
| `PATCH` | `/users/me` | 用户 | 修改昵称、头像等可编辑资料；If-Match；账号标识不能由此接口直接修改 |
| `PUT` | `/users/me/password` | 用户 | 使用旧密码或一次性安全 Ticket 修改密码；幂等；成功后按策略撤销其他会话 |
| `GET` | `/users/me/security` | 用户 | 查询密码设置状态、绑定方式、最近修改时间和活跃会话数量，不返回凭证摘要 |
| `POST` | `/users/me/contact-change-tickets` | 用户 | 经近期认证、原密码或旧联系方式验证码校验后创建手机号/邮箱换绑 Ticket；幂等、短时有效，不在响应中回显完整目标 |
| `POST` | `/users/me/contact-changes` | 用户 | 提交 `change_ticket_id` 与新目标验证码完成换绑；幂等，原子替换凭证并撤销旧 Ticket |
| `DELETE` | `/users/me/contact-change-tickets/{change_ticket_id}` | 用户 | 取消本人尚未消费的换绑 Ticket；重复取消结果一致 |
| `POST` | `/users/me/account-closure-requests` | 用户 | 发起账号注销/删除申请；近期认证、风险提示、冷静期和未完结交易检查 |

`users/me` 不返回 `password_hash`、加密密钥、完整手机号/邮箱等字段；界面中的“账号”使用掩码显示。`PATCH /users/me` 对昵称按 Unicode 字符执行 2～20 字符、去除首尾空白、禁止纯空白及内容安全校验。换绑必须验证新目标所有权，且 `change_phone` 验证码不能用于 `change_email`；确认时再次检查目标唯一性和凭证版本，成功后推送安全通知并按风险策略撤销其他会话。密码修改成功也应推送安全通知并写 `system_audit_logs`，但通知内容不得包含新旧密码。用户头像先走 3.11.16 文件上传，资料更新只提交已激活且 Purpose 为 Avatar 的 `file_id`。

`GET /users/me/dashboard` 的计数均以当前用户、`user_hidden_at IS NULL` 为范围，返回固定字段：`pending_payment` 对应未过期 `order_status=pending_payment`，`pending_shipment` 对应 `pending_shipment`，`in_transit` 对应 `shipped` 且 `fulfillment_status!=received`，`pending_review` 对应 `completed` 且至少一个订单项 `review_status=pending`，`after_sale` 对应存在进行中售后。订单角标按店铺订单计数，评价角标按可评价订单项计数；同一订单可同时计入 `pending_review` 与 `after_sale`，响应必须在 OpenAPI 明示该重叠语义。

#### 3.12.3 收货地址接口

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `GET` | `/users/me/addresses` | 用户 | 获取地址列表，默认地址置顶；列表仅供本人使用 |
| `POST` | `/users/me/addresses` | 用户 | 新增地址；校验地区编码、联系电话和 `MAX_ACTIVE_ADDRESSES_PER_USER=20`；幂等；超限返回 409 `ADDRESS_LIMIT_REACHED` |
| `GET` | `/users/me/addresses/{address_id}` | 用户 | 获取单个地址；返回 ETag |
| `PATCH` | `/users/me/addresses/{address_id}` | 用户 | 修改地址；If-Match；不回写已创建订单的地址快照 |
| `DELETE` | `/users/me/addresses/{address_id}` | 用户 | 逻辑删除地址；If-Match；删除默认地址时按规则选择新默认项 |
| `PUT` | `/users/me/default-address` | 用户 | 把请求中的 `address_id` 设为唯一默认地址；幂等 |

地址响应可包含页面展示所需的拼接文本，但行政区划仍使用稳定代码与分级字段传输。`GET /users/me/addresses` 固定返回 `items、active_count、max_count=20、can_create`；这些字段只用于 UI，服务端创建时仍强制不变式。Address Domain 以固定锁顺序锁定当前 User Address Aggregate（或使用同等的计数/槽位约束），在同一事务内计数 `deleted_at IS NULL`并插入；19 条时并发新建两条只能一条成功，逻辑删除后可重新创建，不同用户不共用锁。结算选择地址时只提交 `address_id`；创建订单时服务端再次读取并生成不可变 `order_addresses` 快照。地址被修改或删除不影响历史订单。

#### 3.12.4 店铺接口

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `GET` | `/stores/{store_id}` | 公开/用户 | 正常店铺返回完整公开资料；关闭/暂停店铺按历史交易关系和治理策略返回受限 DTO 或 404 |
| `GET` | `/stores/{store_id}/products` | 公开 | 店铺在售商品列表；固定使用 `q、group_id、sort、cursor`（可另带已定义价格筛选），`group_id` 必须属于当前店铺；不接受 `keyword/groupId` 别名 |
| `GET` | `/stores/{store_id}/product-groups` | 公开 | 返回店铺自定义商品分组树及当前可见商品数量；不返回平台经营许可 `store_categories` |
| `GET` | `/stores/{store_id}/home-content` | 公开 | 返回当前有效公告、人工推荐商品和派生热销商品；无运营内容时返回空集合并由前端展示全部商品 |
| `GET` | `/stores/{store_id}/service-policies` | 公开 | 运费、发货、退换货和客服时间等当前有效政策 |
| `GET` | `/users/me/followed-stores` | 用户 | “我关注的店铺”列表 |
| `PUT` | `/users/me/followed-stores/{store_id}` | 用户 | 关注店铺；重复请求不重复计数 |
| `DELETE` | `/users/me/followed-stores/{store_id}` | 用户 | 取消关注；重复请求结果一致 |

`GET /stores/{store_id}` 对正常可见店铺返回完整公开 DTO；对 `closed/suspended` 店铺，拥有该店历史订单的当前用户可获得 `visibility_mode=historical_limited` 的受限 DTO 和历史订单客服入口，不返回可购买商品与关注动作。其他公开访问按治理策略返回受限停业页或 404，响应不得泄露内部停业原因。前端无论从商品卡片、商品详情、订单详情还是消息列表点击店铺名称/Logo，均导航到同一 `/stores/{store_id}` 页面并调用上述接口。客服入口不在店铺详情响应中直接创建会话，只返回 `customer_service_enabled` 和入口能力；真正的获取/创建会话使用 3.12.18 的确定性接口。

#### 3.12.5 商品搜索与详情接口

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `GET` | `/homepage` | 公开/用户 | 首页专用聚合 Read Model：返回有效 Banner/公告、分类摘要及 `recommended/hot/new_arrival` 商品区块、各区块 opaque Cursor、`feed_version` 和局部降级状态；未授权个性化时使用通用推荐 |
| `GET` | `/products` | 公开 | 搜索/浏览商品；支持 `q、category_id、brand_id、store_id、price_min、price_max、sort、cursor` |
| `GET` | `/products/{product_id}` | 公开 | 商品详情、店铺摘要、规格模型、结构化履约资料、服务承诺、销量展示值和当前用户关系；下架/删除按可见性返回 404 或受限状态 |
| `GET` | `/products/{product_id}/skus` | 公开 | SKU 组合、可售状态、Money、`stock_status`、可选 `low_stock_remaining` 和 `max_purchase_quantity`；不返回真实仓库库存、安全库存或预占量 |
| `GET` | `/products/{product_id}/faqs` | 公开 | 商品常见问题；可作为进入店铺客服前的快捷问题 |
| `GET` | `/products/{product_id}/reviews` | 公开 | 已发布评价摘要与列表；Cursor 分页、评分/有图/规格筛选 |
| `GET` | `/categories` | 公开 | 平台分类树，可按父分类增量读取 |
| `GET` | `/brands` | 公开 | 品牌列表和搜索 |
| `GET` | `/search/suggestions` | 公开 | 输入联想；最小字符数、敏感词与高频限流 |

`GET /homepage` 由单个查询服务并行读取/预计算首页投影，禁止 Router 在请求内串行调用 Banner、分类和每个商品详情形成 N+1；局部失败以 `sections[].status=available/unavailable` 表示，公共 `ProductCard` DTO 不含后台字段。Cursor 绑定 `feed_version、section、identity/consent_scope`，失效返回 410 `CURSOR_EXPIRED`，前端保留区块并从首 Cursor 重载。

搜索结果是可变快照，不能作为下单价格依据。详情响应中的 `selected_sku` 只可作为前端初始选择；创建结算会话必须提交明确 `sku_id` 与数量，由结算服务重新校验商品、SKU、店铺、区域和库存。履约资料至少返回城市级发货地、`dispatch_estimate: ServiceEstimate`、购买注意事项和资料版本；商品页面与 AI 客服均读取同一 DTO/Tool Result，不从富文本或模型常识重复推断。

商品详情 DTO 同时返回 `public_images[]` 与 `skus[].images[]`，每张图片仅包含公开 `file_id/url/thumbnail_url/alt_text/width/height/sort_order`，不返回对象存储内部 Key。`skus[].images[]` 只放该 SKU 专属图；为空时返回 `image_fallback=product_public_images`，前端使用 `public_images[]`，不自行推测或混合其他 SKU 图片。商品及图片发布、隐藏或更换后必须通过 Outbox 使详情缓存与 CDN 派生图失效。

商品详情公开 DTO 的富文本字段固定为版本化判别联合，示意：

```json
{
  "detail_content": {
    "content_format": "safe_html_v1",
    "content_version": 7,
    "content_hash": "sha256-base64url...",
    "safe_html": "<h2>商品特点</h2><p>...</p>",
    "safe_blocks": null,
    "safe_text_fallback": "商品特点\n..."
  },
  "faqs": [
    {
      "faq_id": "faq_01K...",
      "question": "是否支持快充？",
      "answer_content": {
        "content_format": "structured_v1",
        "content_version": 3,
        "content_hash": "sha256-base64url...",
        "safe_blocks": [{"type": "paragraph", "text": "支持，具体功率以所选 SKU 参数为准。"}],
        "safe_html": null,
        "safe_text_fallback": "支持，具体功率以所选 SKU 参数为准。"
      }
    }
  ]
}
```

`content_format` 仅允许 `structured_v1/safe_html_v1`；`safe_blocks/safe_html` 按格式恰有一个非空，`safe_text_fallback` 始终存在，未知格式客户端只降级显示该安全纯文本。公开接口不得返回 `source_content/source_format`，也不得返回待审核、扫描失败或非 `published_detail_content_version_id/published_content_version_id` 指向的版本。响应设置与 2.3.4.4 一致的 CSP/`X-Content-Type-Options: nosniff`；商品实时价格、库存、履约和政策继续使用结构化字段，不从安全 HTML 解析。

#### 3.12.6 商品收藏接口

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `GET` | `/users/me/favorite-products` | 用户 | 查询收藏商品；下架商品返回受限摘要以支持用户删除，不泄露后台字段 |
| `PUT` | `/users/me/favorite-products/{product_id}` | 用户 | 收藏商品；天然幂等 |
| `DELETE` | `/users/me/favorite-products/{product_id}` | 用户 | 取消收藏；天然幂等 |

商品详情和卡片的收藏按钮采用乐观 UI 时，失败必须回滚；收藏计数为派生展示值，不能在收藏事务中锁定商品主记录造成热点。

#### 3.12.7 购物车接口

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `GET` | `/users/me/cart` | 用户 | 获取按店铺分组的购物车、失效原因、价格提示、选中状态与 ETag |
| `POST` | `/users/me/cart/items` | 用户 | 增量加入商品；提交 `sku_id、quantity`，同 SKU 已存在时原子增加数量；Idempotency-Key，允许创建首个 Cart，不要求预先取得 ETag |
| `PATCH` | `/users/me/cart/items/{cart_item_id}` | 用户 | 把数量设置为明确的最终值或修改选中状态；If-Match，不提供增量语义 |
| `DELETE` | `/users/me/cart/items/{cart_item_id}` | 用户 | 删除购物车项；If-Match |
| `PUT` | `/users/me/cart/selection` | 用户 | 原子更新一组商品的选中状态；If-Match，避免逐项请求产生中间态 |
| `DELETE` | `/users/me/cart/invalid-items` | 用户 | 清理当前所有无效项；If-Match |

所有购物车读取和写入响应都返回服务端计算的 `cart_total_quantity、selected_quantity、valid_item_count、amount_summary`。导航角标使用 `cart_total_quantity`，不得根据按钮点击次数本地固定加一。增量加入必须在事务中锁定/创建用户唯一 Cart 和目标 SKU 行，校验“现有数量 + 本次数量”不超过购买上限；相同 Idempotency-Key 重放返回首次结果，不得再次累加。`PATCH`、删除和批量选中以 ETag 防止并发标签页无提示覆盖。购物车响应中的价格和库存只用于提示；写入 MySQL 成功后失效 Redis 缓存。游客购物车如要支持，应使用独立匿名 Cart Token，登录合并必须有显式合并接口和冲突结果，首版不隐式覆盖用户购物车。

#### 3.12.8 结算预览接口

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `POST` | `/checkout-sessions` | 用户 | 从 `buy_now` 或 `cart` 创建结算会话；幂等；锁定输入快照但不表示订单已创建 |
| `GET` | `/checkout-sessions/{checkout_id}` | 用户 | 获取结算页按店铺分组的商品、地址、配送、金额明细、过期时间和 ETag |
| `PATCH` | `/checkout-sessions/{checkout_id}` | 用户 | 修改地址、配送选项或买家留言；If-Match，服务端重新计价 |
| `POST` | `/checkout-sessions/{checkout_id}/repricings` | 用户 | 主动刷新价格、库存和运费；幂等；生成新快照与 ETag |

创建请求使用以 `source_type` 为判别字段的 `oneOf` Schema。立即购买示例：

```json
{
  "source_type": "buy_now",
  "items": [
    {"sku_id": "sku_01K...", "quantity": 1}
  ],
  "address_id": "addr_01K..."
}
```

购物车结算示例：

```json
{
  "source_type": "cart",
  "cart_item_ids": ["ci_01K...", "ci_01M..."],
  "address_id": "addr_01K..."
}
```

`source_type=buy_now` 时 `items` 必填且必须恰好一项（OpenAPI/Pydantic `minItems=1、maxItems=1`），`cart_item_ids` 禁止出现；唯一 Item 必须包含一个明确 SKU 和正整数数量。`source_type=cart` 时 `cart_item_ids` 必填且 `items` 禁止出现，允许 1–100 个 ID；ID 不得重复、必须属于当前用户唯一永久购物车且对应条目有效。`buy_now` 零项/多项和 `cart` 零项/超过 100 项均在进入 Service 前返回 422。`cart_item_ids` 的每个值均为 `ci_` 前缀公开 ID。`address_id` 可选，提供时必须属于当前用户；未提供则服务端尝试选择有效默认地址，否则把 `ADDRESS_REQUIRED` 放入 `blocking_issues`。请求 Schema 使用 `extra=forbid`：价格、商品名、店铺 ID、运费等非契约字段返回 422，绝不能静默写入权威快照。

响应必须给出 `checkout_id、expires_at、version、store_groups、amounts、warnings、blocking_issues、available_actions`。每个 `store_group` 包含服务端根据地址和运费模板计算的 `delivery_options、selected_delivery_option、freight_amount、delivery_estimate: ServiceEstimate`；不可配送必须进入 `blocking_issues`。金额至少拆分为商品小计、运费和应付总额，均采用 Money；不能让前端自行相加得出最终应付。`blocking_issues` 包含 SKU 下架、库存不足、区域不可配送、地址缺失和价格变化等机器可处理原因。会话过期返回 410 `CHECKOUT_SESSION_EXPIRED`；创建订单前必须在同一事务/受控锁顺序中再次校验并生成最终订单快照。

每个店铺组的 `buyer_remark` 首版最多接受 200 个 Unicode 字符，先规范化换行并 Trim，Trim 后为空写 NULL；拒绝控制字符、危险双向文本和超过限制的输入，并通过内容安全策略处理。`orders.buyer_remark VARCHAR(500)` 只是数据库存储冗余与未来迁移容量，不代表 API 可接受 500 字；OpenAPI/Pydantic、生成的 TypeScript 表单计数和服务端校验必须共同固定 200 字，不能按 UTF-8 字节数误截断。

结算响应还为每个店铺组返回 `customer_service_context` 的安全创建参数摘要（不含签名 Secret），前端点击 [联系客服] 时把 `type=checkout_store_group、checkout_id、checkout_version` 提交给 `PUT /stores/{store_id}/customer-service-conversation`。服务端根据 Checkout 权威快照重建本店最小商品摘要并写入短期 Context；不接受客户端上传商品名、地址、优惠或金额作为上下文。重新计价使 Version 增长并失效旧 Context；订单创建消费、Checkout 过期/放弃后 Context 过期。任何读取都再次校验 User、Store、Checkout Status/Version/Expiry，事件延迟不能形成授权窗口。

---


#### 3.12.9 订单创建与查询接口

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `POST` | `/orders` | 用户 | 使用有效 `checkout_id` 创建订单；幂等；返回合并交易单和按店铺拆分的订单 |
| `GET` | `/users/me/orders` | 用户 | “我的订单”列表；Cursor 分页，支持固定复合视图 `view` 及 `q、created_from、created_to`；`q` 可匹配本人订单号、订单商品名称和店铺名称 |
| `GET` | `/orders/{order_id}` | 订单所属用户 | 用户投影的单个店铺订单详情；返回订单来源、支付摘要、商品、金额/改价、地址快照、物流摘要、售后摘要、状态时间轴和动态操作集合 |
| `GET` | `/trade-orders/{trade_order_id}` | 用户 | 查询一次结算生成的合并交易单与所属店铺订单，用于支付结果页 |
| `GET` | `/orders/{order_id}/events` | 订单所属用户 | 查询用户可见的订单状态时间轴；不直接暴露内部操作日志 |
| `DELETE` | `/users/me/orders/{order_id}` | 用户 | 按统一 `OrderHidePolicy` 把符合条件的终态订单从用户列表隐藏；If-Match，不物理删除审计数据 |
| `POST` | `/users/me/orders/{order_id}/restorations` | 用户 | 在响应给出的短时 `undo_until` 内恢复刚隐藏的订单；幂等且携 If-Match，超过窗口返回 409 并仍可由客服按政策协助 |
| `POST` | `/orders/{order_id}/repurchases` | 用户 | 将仍可售的原订单 SKU 和数量批量加入购物车；Idempotency-Key + 当前购物车 If-Match，返回 `added_items、unavailable_items、requires_reselection` 和最新购物车汇总 |

创建订单请求只接受 `checkout_id` 和客户端确认的 `checkout_version`；不接受支付渠道/支付偏好，也不重新接受价格、店铺、收货人或任意商品名称。支付方式只能在订单创建成功后的收银台中选择，并作为 `POST /payments` 的受控参数。后端验证 Checkout 未过期、归属正确、版本一致后，创建 `trade_orders、orders、order_items、order_addresses`，预占库存，写 Outbox，并消耗结算会话。相同幂等 Key 返回首次的同一订单结果；不同 Key 重复消费同一个 Checkout 也必须由唯一约束阻止。

订单创建与 Payment 创建是两个资源、两个请求和两个 Idempotency Scope。`POST /orders` 成功只返回 201 `trade_order_id、order_ids、payment_deadline_at、available_actions`，不内嵌调用支付 Provider。网络超时后客户端必须以同一个 `Idempotency-Key` 重放 `POST /orders`；首次仍在处理中返回 409 `IDEMPOTENCY_REQUEST_IN_PROGRESS` 和 `Retry-After`，已完成则返回首次结果。不同 Key 再消费 Checkout 返回 409 `CHECKOUT_ALREADY_CONSUMED` 并在当前用户范围内返回已创建 `trade_order_id`；过期返回 410 `CHECKOUT_SESSION_EXPIRED`，版本变化返回 412 `CHECKOUT_VERSION_MISMATCH` 和最新 Checkout ETag/安全差异。订单已创建后无论支付创建是否成功，都不得恢复 Checkout 或创建第二组订单。

订单详情中的 `order_source` 从持久化的 `trade_orders.order_source` 返回，不回读可能已经删除的 Checkout；订单金额返回 `goods_amount、freight_amount、adjustment_amount、payable_amount、paid_amount、refunded_amount、currency`，每个订单项返回自己的 `gross_amount、adjustment_amount、payable_amount、refunded_amount`，两层汇总必须满足 3.7.7 的不变式。`adjustment_amount != 0` 时同时返回面向用户的安全原因。支付方式和支付时间来自成功支付摘要；物流预计时间使用 3.11.5 的 `ServiceEstimate`，不存在可靠范围时返回 `status=unavailable`，前端显示“暂无可靠预计时间”。

`OrderHidePolicy` 是前端按钮和 DELETE 命令的唯一规则来源。允许隐藏的情形为：① `completed` 且所有可评价订单项均为 `reviewed/closed`；② `cancelled/closed`；③售后已完成、资金结果终态且无进行中争议。任一活动售后、支付结果未知、退款处理中、举证/申诉窗口内争议或风控保全状态均禁止隐藏。列表与详情的 `available_actions.delete_order` 必须由同一策略计算，不能由 Router 复制条件。

订单搜索对订单号支持精确/前缀匹配，对商品和店铺名称使用规范化受控查询；始终附加当前 `user_id` 和 `user_hidden_at IS NULL`，限制关键词长度与频率，不能把后台全文检索变成跨用户枚举通道。隐藏订单响应返回 `undo_until` 和新 ETag；恢复只清空符合窗口的 `user_hidden_at` 并写操作流水。再次购买不保证全部成功，不可售 SKU 不得静默替换为其他规格。

`view` 是面向用户页面的稳定复合筛选契约，不等同于数据库 `order_status`。OpenAPI 必须固定以下枚举和服务端映射：

| `view` | 服务端包含规则 |
| :--- | :--- |
| `all` | 当前用户且 `user_hidden_at IS NULL` 的全部店铺订单 |
| `pending_payment` | `order_status=pending_payment` 且尚未到 `expires_at` |
| `pending_shipment` | `order_status=pending_shipment` |
| `in_transit` | `order_status=shipped` 且 `fulfillment_status!=received` |
| `completed` | `order_status=completed`，无论是否已评价或发生过售后 |
| `pending_review` | `order_status=completed` 且存在 `order_items.review_status=pending` 的可评价订单项 |
| `after_sale` | 存在当前用户对该订单创建的任意 `refund_applications`，包含处理中、成功、拒绝、撤销和关闭历史 |
| `cancelled` | `order_status IN (cancelled, closed)` 且订单从未成功支付，用于用户取消、支付超时或管理员受控取消 |

除 `all` 外，同一订单可以同时命中多个视图，例如已完成订单同时属于 `completed、pending_review、after_sale`。列表响应返回 `matched_views[]`，便于前端调试和埋点，但前端仍只按当前请求视图展示。无效 `view` 返回 422；用户端不得自行组合 `order_status/after_sale_status/review_status` 推导上述视图，管理后台的原始状态筛选使用独立管理接口契约。

订单列表每项都返回以下结构，前端从上到下展示订单，并按 `available_actions` 渲染一排可操作按钮：

```json
{
  "order_id": "ord_01K...",
  "order_status": "shipped",
  "after_sale_status": "none",
  "review_status": "pending",
  "matched_views": ["in_transit"],
  "available_actions": [
    {"code": "view_logistics", "enabled": true},
    {"code": "confirm_receipt", "enabled": true},
    {"code": "apply_after_sale", "enabled": true},
    {"code": "review", "enabled": false, "reason_code": "ORDER_NOT_RECEIVED", "reason_message": "确认收货后才能评价"},
    {"code": "delete_order", "enabled": false, "reason_code": "ORDER_NOT_TERMINAL", "reason_message": "订单尚未结束"}
  ]
}
```

按钮建议固定视觉顺序为“售后、查看物流、评价、删除订单、确认收货”，但是否显示/可用完全依据响应；不可用时必须展示 `reason_code/reason_message` 对应的说明。订单详情页使用同一能力模型，并增加 `contact_store`、`cancel_order`、`pay`、`apply_after_sale` 等动作，避免列表和详情各写一套状态判断。缺失 Action 表示不展示，不使用 `enabled=false` 来暴露安全敏感原因。

#### 3.12.10 订单取消与确认收货接口

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `POST` | `/orders/{order_id}/cancellations` | 订单所属用户 | 发起用户取消命令；幂等；请求含原因码与可选说明，按订单状态决定直接取消或引导售后 |
| `POST` | `/orders/{order_id}/receipt-confirmations` | 用户 | 确认收货；幂等；要求当前状态允许，更新订单并启动评价/售后期限 |

命令响应返回更新后的订单摘要、ETag 和状态事件，不由前端预判下一状态。已发货订单不能经普通取消接口绕过退款/退货状态机；存在多包裹时，是否允许整单确认由领域规则决定。确认收货属于高影响操作，前端弹出二次确认，后端校验本人、订单状态与幂等；首次成功后的重复提交返回相同结果，不重复触发结算事件。

#### 3.12.11 支付创建与支付回调接口

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `POST` | `/payments` | 用户 | 为待支付 Trade Order 创建支付单；幂等；返回渠道跳转参数/二维码信息和过期时间 |
| `GET` | `/payments/{payment_id}` | 用户 | 查询支付单状态、金额、渠道和关联交易单；不返回渠道密钥/原始敏感报文 |
| `GET` | `/trade-orders/{trade_order_id}/payments` | 用户 | 查询该交易单的支付尝试记录，确定当前可继续的支付单 |
| `POST` | `/payments/{payment_id}/closures` | 用户/系统 | 关闭尚未完成的支付单；幂等；不能关闭渠道已成功但本地尚未同步的支付 |
| `POST` | `/webhooks/payments/{provider}` | 支付渠道 | 支付/退款异步通知；验签、去重、落原始摘要并快速应答，详见 3.12.25 |

支付创建请求包含 `trade_order_id、provider、return_url_key` 等受控字段，不接受客户端传入支付金额。后端从订单快照读取金额并验证订单状态，再创建支付单。前端从渠道返回页不能把 URL 参数当作支付成功依据，必须轮询支付资源或接收实时事件；状态仍为 Processing 时展示“支付结果确认中”。

Payment Idempotency Key 与订单 Key 分开保存，Scope 至少绑定当前用户、Trade Order、Provider 和本次逻辑支付尝试。相同 Key 重放返回同一 `payment_id`；同一 Trade Order 存在 `created/pending` 尝试（包括 Provider 结果未知、正在主动查询）时，换 Key 创建返回 409 `PAYMENT_ATTEMPT_IN_PROGRESS` 及现有 `payment_id`，前端只能进入结果确认页。只有旧尝试明确 `failed/closed` 且订单仍返回 `available_actions.pay=true`，才允许使用新 Key 创建下一次支付尝试。订单已支付返回 409 `TRADE_ORDER_ALREADY_PAID` 并返回安全成功摘要；支付窗口截止返回 410 `PAYMENT_WINDOW_EXPIRED`；Provider 暂不可用且未创建 Payment 返回 503 `PAYMENT_PROVIDER_UNAVAILABLE`，页面回订单详情显示“订单已创建，可继续支付”。

渠道返回页仅携带平台生成的 `payment_id`/短时 State 引用，先校验 State 再查询 Payment；`created/pending`（包括同步结果未知）统一映射为前端 `confirming`，有界轮询遵循 `Retry-After` 并可由 SSE/WebSocket 终态事件结束。用户关闭收银台只调用指定 Payment Closure（如状态允许），不取消订单、不重新消费 Checkout；结果未知时 Closure 也不得盲目覆盖为失败。

回调处理先验证原始请求签名、时间戳、商户号、金额、币种与 Provider Event ID，再以 `payment_callbacks`/`payment_events` 唯一约束保证幂等。数据库事务内更新支付与订单并写 Outbox，提交后再异步通知前端和执行后续任务。重复成功、成功后失败、退款回调先于主动查询等乱序事件必须按领域状态机处理，不能直接覆盖为回调携带的最新字符串。

#### 3.12.12 物流查询接口

物流响应采用角色化 DTO，不让同一序列化模型按调用者临时多返回字段：

| DTO | 调用路径/访问者 | 运单与预计字段 |
| --- | --- | --- |
| `UserShipmentDetail` | `/shipments/{shipment_id}`，订单本人 | `tracking_no`（服务端临时解密，仅鉴权详情与复制使用）、`tracking_no_masked`、状态、轨迹、可空预计范围/来源/更新时间 |
| `UserOrderShipmentSummary` | `/orders/{order_id}`、`/orders/{order_id}/shipments`，订单本人概览 | 仅 `tracking_no_masked`；按包裹返回商品、状态和预计元数据，不返回完整单号 |
| `AdminShipmentDetail` | `/admin/shipments/{shipment_id}`，`shipments:read` + Store Scope | 仅 `tracking_no_masked`、状态、商品、轨迹和预计元数据；非订单本人投影中不存在完整单号字段 |
| `AgentShipmentResult` | MCP Tool、AI Prompt/Checkpoint、消息/物流卡片 | 仅 `shipment_id、carrier、tracking_no_masked、status、safe_tracks、delivery_estimate: ServiceEstimate`；Schema 中不存在完整单号字段 |

DTO 必须使用不同 Pydantic Response Model/OpenAPI Schema 和 `operationId`，不能先构造含完整单号的通用对象后靠前端隐藏。店铺员工、平台管理员、人工客服和 Agent 都属于非订单本人视图，只能获得掩码投影。物流创建或纠错接口可接受写入专用的完整 `tracking_no`，但它是 Write-only 字段：完成加密、HMAC、掩码计算后，成功响应和后续管理查询仍只返回掩码，不把完整值复制进聊天、审计正文或管理端持久状态。

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `GET` | `/orders/{order_id}/shipments` | 订单所属用户 | 获取本人订单全部非作废包裹及所含商品的概览投影；只返回运单掩码，用于拆包展示 |
| `GET` | `/shipments/{shipment_id}` | 包裹所属用户 | 返回 `UserShipmentDetail`；仅订单本人获得临时解密的 `tracking_no`，同时返回掩码、状态、预计送达时间范围、预计来源/更新时间和最新轨迹；无可靠预计时字段为 NULL |
| `GET` | `/shipments/{shipment_id}/tracks` | 包裹所属用户 | 按发生时间返回标准化物流轨迹；支持增量 Cursor |
| `POST` | `/shipments/{shipment_id}/refreshes` | 包裹所属用户 | 请求异步刷新物流；幂等且限频，返回 202 与任务状态 |

“查看物流”按钮导航到订单的包裹页；只有一个包裹时可直接打开该包裹详情。预计时间固定返回 `delivery_estimate: ServiceEstimate`，无可靠范围时为 `status=unavailable`，前端/Agent 必须显示“暂无可靠预计时间”；禁止由当前轨迹、富文本或模型补日期。轨迹响应同时保留 `occurred_at` 和可选 `received_at`，按实际发生时间展示，并标注数据来源与最近同步时间。承运商原始状态通过适配层映射成平台枚举，未知状态安全降级为 `in_transit/unknown`，不因新增外部枚举导致接口反序列化失败。

`tracking_no` 属于 L2/L3 敏感字段：只允许在校验订单本人所有权后、构造 `UserShipmentDetail` 时临时解密，禁止进入任何非订单本人 DTO、Agent Prompt、RAG、Memory、Checkpoint、消息 Payload、普通日志、Trace Attribute/Event、指标 Label、异常报文、前端错误采集和分析埋点。上述位置只能记录 `shipment_no、carrier_code、tracking_no_masked` 或不可逆 HMAC；日志 Redaction 对常见运单格式另设兜底检测。

#### 3.12.13 评价与追评接口

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `GET` | `/review-eligibilities/{order_item_id}` | 订单项所属用户 | 获取商品/SKU/订单快照、评价窗口、已存在评价和 `available_actions`；不创建评价，不泄露其他订单项 |
| `GET` | `/users/me/reviews` | 用户 | 查询待评价、已评价记录；支持订单/状态筛选 |
| `POST` | `/reviews` | 用户 | 对有资格的订单商品提交评价；幂等；支持评分、文本和已激活图片 `file_id` |
| `GET` | `/reviews/{review_id}` | 公开/评价本人 | 获取已发布评价；审核中内容仅评价本人可见，管理员使用 `/admin/reviews/{review_id}` 的脱敏治理投影 |
| `PATCH` | `/reviews/{review_id}` | 评价本人 | 在允许窗口修改可编辑内容；If-Match，保留审核/修改记录 |
| `POST` | `/reviews/{review_id}/append-records` | 评价本人 | 在规则窗口内追评；幂等；一条主评价的追评次数受限 |

评价创建以 `order_item_id` 为资格依据，服务端校验归属、确认收货/完成状态、评价窗口和唯一约束；商品/SKU/店铺信息从订单快照读取。首版主评价和追评正文均最多 500 个 Unicode 字符，每条评价最多绑定 6 张 Active 图片，单文件规则读取 3.11.16 的 `review_image` 上传策略。匿名评价只隐藏前台昵称，不对后台审计匿名。图片绑定前校验 Purpose、Owner、扫描状态和数量。商家回复只走 3.12.22.7 的 Admin Audience 命令；屏蔽/恢复等治理操作保留规则与原因，不物理抹去交易事实。

#### 3.12.14 退款资格检查接口

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `POST` | `/refund-eligibility-checks` | 用户 | 根据订单和拟申请商品实时检查退款/退货资格；只读评估，不创建售后单 |

请求包含 `order_id、items[{order_item_id, quantity}]、requested_type、reason_code`。响应至少包含：

```json
{
  "eligible": true,
  "eligibility_token": "ret_01K...",
  "expires_at": "2026-08-21T09:30:00.000Z",
  "allowed_types": ["refund_only", "return_and_refund"],
  "items": [
    {
      "order_item_id": "oit_01K...",
      "purchased_quantity": 3,
      "succeeded_refund_quantity": 1,
      "active_reserved_quantity": 1,
      "available_quantity": 1,
      "succeeded_refund_amount": {"minor_units": "299966", "currency": "CNY"},
      "active_reserved_amount": {"minor_units": "299967", "currency": "CNY"},
      "available_refundable_amount": {"minor_units": "299967", "currency": "CNY"},
      "active_refund_refs": [{"refund_id": "ref_01K...", "status": "merchant_review"}],
      "available_actions": ["apply_after_sale", "view_active_after_sale"]
    }
  ],
  "amount_editable": false,
  "min_refundable_amount": {"minor_units": "899900", "currency": "CNY"},
  "max_refundable_amount": {"minor_units": "899900", "currency": "CNY"},
  "suggested_refund_amount": {"minor_units": "899900", "currency": "CNY"},
  "refundable_freight_amount": {"minor_units": "0", "currency": "CNY"},
  "refund_arrival_estimate_text": "预计原路退回，具体到账时间以支付渠道为准",
  "default_contact": {"contact_ref": "ctk_01K...", "masked_phone": "138****8888", "source": "account"},
  "required_evidence_types": [],
  "return_required": true,
  "blocking_reasons": [],
  "policy_snapshot_id": "rsp_01K..."
}
```

资格由订单状态、订单项级可售后数量/金额、已成功退款、活动申请占用、物流、商品政策、申请期限共同计算。活动状态固定为 `submitted/merchant_review/approved/waiting_return/returning/received/refunding`；`rejected/cancelled/closed` 释放占用，`succeeded` 转入成功累计，渠道结果未知时继续占用。`eligible` 是本次所选订单项集合的结果；某一行 `available_quantity=0` 不应让接口隐去同订单其他可申请行。

金额由服务端返回可编辑性、最小值、最大值、建议值、可退运费和预计到账说明；`amount_editable=false` 时客户端不能提交不同金额。`contact_ref` 是绑定当前用户、用途和短期有效期的一次性不透明引用，不包含手机号明文。`eligibility_token` 短期有效并绑定用户、订单、逐项数量/金额/占用快照、类型、规则版本和资源版本；它提升申请体验但不冻结资格，创建退款申请时必须再次锁定订单项校验。不可申请时返回 200 和 `eligible=false` 及订单项级稳定原因码；请求资源本身不存在/越权仍返回 404，而不是借资格结果泄露订单。

#### 3.12.15 退款申请与进度接口

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `POST` | `/refund-applications` | 用户 | 使用资格 Token 创建退款/退货退款申请；幂等 |
| `GET` | `/users/me/refund-applications` | 用户 | 查询“我的售后”列表；Cursor 与状态筛选 |
| `GET` | `/refund-applications/{refund_id}` | 申请用户 | 获取用户可见的申请详情、商品、金额、凭证、退货要求、资金进度和 `available_actions` |
| `GET` | `/refund-applications/{refund_id}/events` | 申请用户 | 获取用户可见的售后进度时间轴 |
| `POST` | `/refund-applications/{refund_id}/evidences` | 申请用户 | 追加凭证 `file_id`；幂等，按状态、Purpose、数量和归属校验 |
| `PUT` | `/refund-applications/{refund_id}/return-shipment` | 申请用户 | 提交/更正退货承运商和运单；If-Match，仅允许在等待退货阶段 |
| `POST` | `/refund-applications/{refund_id}/cancellations` | 申请用户 | 撤销仍可撤销的申请；幂等 |
| `POST` | `/refund-applications/{refund_id}/appeals` | 申请用户 | 对已拒绝且仍在申诉窗口的申请创建唯一 Active 平台申诉；幂等，可携申诉理由和 Active 凭证 `file_id` |
| `GET` | `/refund-appeals/{appeal_id}` | 申请用户 | 获取用户可见的申诉状态、截止时间、决定和可用操作 |
| `GET` | `/refund-appeals/{appeal_id}/events` | 申请用户 | 获取申诉时间线；隐藏平台内部备注和风控字段 |
| `POST` | `/refund-appeals/{appeal_id}/cancellations` | 申请用户 | 撤销仍未决定的申诉；幂等 |

申请页面先调用资格检查，再让用户选择原因、数量、退款类型、说明、联系方式和凭证。创建请求包含 `eligibility_token、items、refund_type、reason_code、reason_detail、requested_amount、contact、evidence_file_ids、policy_accepted`。未修改默认联系方式时提交短期 `contact.contact_ref`，服务端据此读取当前用户的原始号码；用户修改时提交 `contact.phone` 并进行格式校验，二者必须且只能提供一种，前端不需要获取账号完整手机号。服务端从 Token/订单重建用户、店铺、币种和上限，规范化后加密保存联系方式快照，只返回掩码。`amount_editable=false` 时 `requested_amount` 必须等于建议金额；允许编辑时也必须处于服务端最小/最大值内，且创建时重新计价。响应明确给出下一步、截止时间、退货地址掩码和可用动作。

创建时按稳定 `order_item_id ASC` 锁定涉及的 Order Item，在同一事务重新汇总成功与活动 `refund_items`；两个并发申请只能有一个占用同一剩余数量/金额。冲突返回 409 `REFUND_ITEM_CAPACITY_CHANGED`，同时返回最新逐项占用投影供页面刷新。不能使用“订单存在任意活动售后”作为拒绝条件。被拒绝、撤销或关闭后重新申请必须调用 `POST /refund-applications` 创建新的 `refund_id`；旧聚合及 `refund_events` 不更新。平台申诉只走 `/refund-applications/{refund_id}/appeals` 并引用原拒绝申请，二者依据详情响应中独立的 `available_actions.create_new_refund_application/create_refund_appeal` 展示。

退款和申诉附件当前图片不超过 10 MiB、视频不超过 100 MiB、合计最多 6 个，最终以 `GET /file-upload-policies/refund_evidence` 的版本化响应为准。申诉是独立复核资源，原退款申请保持 `rejected`；申诉成立后的退款、补偿或重新审核必须产生新命令和事件，不能重写原始历史。

退款领域状态与支付渠道退款状态分离：售后获批后创建 `refund_payment_records/payment_refunds`，渠道处理中不能把售后直接标记为已退款；渠道最终成功后才进入资金完成态。所有决定写 `refund_events`，高金额、超时、争议、异常账号或规则不能确定的申请必须进入人工审核，Agent 不得自行批准或执行资金退款。

---

#### 3.12.16 消息会话接口

本节 `/conversations/*` 全部只接受 User Audience，且身份必须是会话所属用户。人工客服查看/发送/已读使用 3.12.22.8 的 `/support/conversations/*`，不让同一 Operation 同时接受 User/Admin Token。

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `GET` | `/conversations` | 用户 | 获取当前用户会话列表；Cursor 分页；专属客服作为独立固定项说明 |
| `GET` | `/conversations/{conversation_id}` | 会话所属用户 | 获取用户投影的会话元数据、当前模式、业务上下文、未读数与 ETag |
| `GET` | `/conversations/{conversation_id}/messages` | 会话所属用户 | 按 3.11.13 获取用户可见消息历史；支持 `before/after/limit` |
| `POST` | `/conversations/{conversation_id}/messages` | 会话所属用户 | 以 `sender_type=user` 发送文本、图片/文件、商品卡片、订单卡片；幂等，请求含 `client_message_id` |
| `PUT` | `/conversations/{conversation_id}/read-cursor` | 会话所属用户 | 提交已实际可见的 `last_read_message_id + last_read_sequence_no`；服务端校验同会话并单调推进，返回绝对会话/总未读数和 `cursor_version`，较旧位点幂等无变化 |
| `PUT` | `/conversations/{conversation_id}/contexts/{context_type}` | 会话所属用户 | 设置唯一 Active 商品、订单、退款等上下文；If-Match，原子停用旧同类 Context，校验资源归属/店铺并返回新的 `context_id/context_version`；内部 Agent 通过 Command Bus 更新，不调用本 User API |
| `DELETE` | `/conversations/{conversation_id}/contexts/{context_type}` | 会话所属用户 | 清除指定上下文；If-Match；内部清理由 Command Bus 执行 |
| `POST` | `/conversations/{conversation_id}/archivals` | 用户 | 把普通店铺会话从个人列表归档；不删除消息，专属客服不允许归档 |

创建消息请求的 `content` 使用带版本的判别联合，不用一个无约束 JSON：

```json
{
  "client_message_id": "cmsg_01K4...",
  "content": {
    "type": "text",
    "text": "这个订单什么时候发货？"
  },
  "reply_to_message_id": null
}
```

服务端验证参与关系和内容后，先将用户消息持久化为 `messages`，提交事务并写 Outbox，再触发 AI Run/人工分发；不能等待大模型完成才保存用户消息。成功返回 201 与服务端 `message_id、sequence_no、created_at`；相同 `(conversation_id, sender_id, client_message_id)` 重试返回首次消息。附件消息只引用 Active `file_id`，商品/订单卡片只引用可见资源，由服务端生成安全展示快照，不能让客户端伪造价格或店铺信息。

卡片发送和 Context 激活是两个相关但不同的用例：卡片负责把安全快照展示给会话参与者，Context 负责标识本次咨询对象。用户可从订单/商品入口激活 Context 而不发送卡片；发送卡片时可在同一 Application Transaction/Command 中把验证通过的资源设为 Active。无论来源为何，Tool 调用都必须携 Runtime 读取的 `context_id + context_version` 并重新校验归属；卡片 ID、自然语言中的资源号或历史 Context 不能替代该校验。

删除对话记录不是首版普通用户能力；消息是客服、纠纷和审计依据。若提供“清空聊天”体验，只能记录用户侧隐藏游标，不影响其他参与者、业务审计和依法保留的数据。未读计数以 MySQL Read Cursor 为真相、Redis 为加速；只有客户端满足 2.4.3.2 的可见条件才请求推进，成功后通过 WebSocket 发布含绝对计数和 Version 的 `unread.updated`。接口失败或响应未知时客户端保留当前服务端角标并查询游标，不先行清零。

##### 3.12.16.1 AI 消息反馈接口

| 方法 | 路径 | Operation | 关键规则 |
| :--- | :--- | :--- | :--- |
| `PUT` | `/conversations/{conversation_id}/messages/{message_id}/reaction` | `AiFeedbackReaction_Put` | 幂等设置 `thumb_up/thumb_down`；同一 User+Message 至多一个当前 Reaction，更换为覆盖新版 |
| `DELETE` | `/conversations/{conversation_id}/messages/{message_id}/reaction` | `AiFeedbackReaction_Delete` | 幂等撤回本人当前 Reaction，保留最小审计事件 |
| `POST` | `/conversations/{conversation_id}/messages/{message_id}/reports` | `AiFeedbackReport_Create` | 限流提交 `reason_code/comment`；内容安全、最小化和幂等 Key |
| `POST` | `/conversations/{conversation_id}/messages/{message_id}/corrections` | `AiFeedbackCorrection_Create` | 限流提交用户认为正确的说法/来源；只是待审反馈，不写 Memory/Golden Dataset |

四个 Operation 都必须同时验证当前用户是 Conversation Owner、Message 属于该 Conversation、`sender_type=agent`且消息对用户可见。他人会话、Human/System/Tool 消息或不存在消息统一按不泄漏资源的 404/403 策略处理。Report/Correction 限制长度、频率和重复 Hash，不自动变成长期记忆、Prompt、Skill 或 Golden Case；只有脱敏、授权、人工标注后才能进入评估治理流程。

#### 3.12.17 专属客服接口

专属客服是“消息”左栏固定会话列表中的第一项，名称固定为“专属客服”，不是首页独立 AI，也不属于任何店铺。每个用户全生命周期恰有一条专属客服 Conversation 记录；`active/closed/human_*` 只表示服务阶段，任何状态都不释放唯一键。

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `PUT` | `/users/me/exclusive-conversation` | 用户 | 幂等获取或创建唯一专属客服会话；数据库唯一键兜住并发创建 |
| `GET` | `/users/me/exclusive-conversation` | 用户 | 获取已存在的专属客服会话；账号初始化未完成时可返回 404，由前端调用 PUT 自愈 |
| `GET` | `/users/me/ai-consents` | 用户 | `AiConsent_ListMine`；按 `consent_type、scope_type、store_id、status` 筛选，返回用途、版本、ETag、到期时间和可用动作 |
| `POST` | `/users/me/ai-consents` | 用户 | `AiConsent_Grant`；请求显式包含 `consent_type、scope_type、scope_id、policy_version`；授权/续期创建新版本，幂等 |
| `GET` | `/users/me/ai-consents/{consent_id}` | 用户 | `AiConsent_GetMine`；只能读本人 Consent，返回 ETag 和 Scope 安全摘要 |
| `POST` | `/users/me/ai-consents/{consent_id}/pauses` | 用户 | `AiConsent_Pause`；If-Match，立即停止该 Scope 召回/候选提取，不删历史证据 |
| `POST` | `/users/me/ai-consents/{consent_id}/resumptions` | 用户 | `AiConsent_Resume`；重验告知版本/到期时间并新建 Consent Version，不原地篡改旧行 |
| `DELETE` | `/users/me/ai-consents/{consent_id}` | 用户 | `AiConsent_Revoke`；If-Match，撤销唯一 Scope，同步阻断召回，返回 202 Cleanup Task |
| `POST` | `/users/me/ai-personalization/disable-all` | 用户 | `AiPersonalization_DisableAll`；结构化确认+幂等，原子撤销全部当前 Consent 并返回聚合清理任务 |
| `GET` | `/users/me/ai-memory-items` | 用户 | `AiMemory_ListMine`；按 Namespace/Store/Type/Status 筛选，返回可见描述、来源、用途、到期时间和 ETag，不返回向量/内部分数 |
| `POST` | `/users/me/ai-memory-items/{memory_id}/revisions` | 用户 | `AiMemory_Revise`；结构化新值+用户确认+If-Match，追加新 Item 并 Supersede 旧版，返回 201 |
| `DELETE` | `/users/me/ai-memory-items/{memory_id}` | 用户 | `AiMemory_Delete`；确认+If-Match，同步 Tombstone/停止召回，异步清向量/缓存，返回 202 `cleanup_task_id` |
| `GET` | `/users/me/ai-cleanup-tasks/{task_id}` | 用户 | `CleanupTask_GetMine`；返回 `queued/running/succeeded/partial_failed/failed`、计数、可重试状态，不泄漏底层 Object/Vector Key |
| `POST` | `/users/me/ai-cleanup-tasks/{task_id}/retries` | 用户 | `CleanupTask_RetryMine`；仅本人可重试项，幂等，不恢复已撤销的召回 |

Consent 不再使用含糊的 `{consent_type}` 路径定位店铺 Scope；外部命令一律使用 `con_` 公开 ID，创建时的 `scope_id` 是受约束资源 ID。跨店 Scope、旧 ETag、已撤销后召回、重复删除和清理部分失败均有稳定 Problem Code，前端不根据开关状态猜测成功。

`PUT /users/me/exclusive-conversation` 的响应必须包含 `conversation_type=exclusive、display_name=专属客服、is_fixed=true、fixed_rank=0、store_id=null`。前端始终单独渲染此项，再渲染按最近消息排序的普通会话；不能依赖 `pinned` 或后端列表恰好排在第一位。

专属客服可用技能范围是跨店铺公开商品、当前用户个人事务和平台公共服务，例如：解释平台规则；全平台搜索/比较商品；在授权范围内查询本人订单、支付状态、物流和售后；执行售后资格预检、生成退款草稿，并在用户结构化确认后提交退款申请；管理可见长期偏好；转平台人工客服。首版不开放下单、支付、取消订单、确认收货和修改地址 Tool。它不能读取店铺私有经营数据、代替商家作出售后承诺、直接执行渠道退款，也不能把 A 店铺私有信息提供给 B 店铺。

接口层在每次 Tool 调用时都重新校验用户身份、Consent、资源归属、Agent/Skill/Tool 版本和风险策略；“专属”只表示服务范围，不表示获得管理员权限。跨店铺读取默认只返回当前用户自己的最小必要信息，敏感字段继续脱敏。

#### 3.12.18 店铺客服接口

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `PUT` | `/stores/{store_id}/customer-service-conversation` | 用户 | 幂等获取或创建当前用户与该店铺全生命周期唯一会话；任何状态不释放唯一键；可携安全业务上下文 |
| `GET` | `/stores/{store_id}/customer-service-profile` | 公开/用户 | 获取客服启用状态、服务时间、AI/人工能力、欢迎语版本和预计响应提示 |
| `GET` | `/stores/{store_id}/customer-service-faqs` | 公开/用户 | 获取店铺当前公开快捷问题，可按 `product_id` 筛选 |

会话唯一约束为 `(user_id, store_id, conversation_type=store)`；并发 PUT 只能得到同一个 `conversation_id`。从商品页可提交 `context={type: product, resource_id: ...}`，从订单详情提交订单上下文；结算页提交 `context={type: checkout_store_group, checkout_id: ..., checkout_version: ...}`。服务端分别校验商品属于该店、订单属于该用户和该店，或 Checkout 属于当前用户、仍有效、版本匹配且包含该店铺组，再写 `conversation_contexts`。点击不同入口始终进入相同店铺会话，只更新经过验证的当前上下文，不创建重复会话。

入口响应返回 `conversation_id、active_context{id,type,resource_id,version,display_snapshot}` 和消息页 URL。切换同类型资源时旧 Context 立即失活；在途 Agent Run 若绑定旧 Version，只能安全终止/要求用户重试，不能继续读取旧订单。订单卡片为可选展示，不是访问令牌；没有发送卡片但由订单入口创建的 Active Context 可用于本次最小读取，发送过卡片但已失活/跨店的 Context 也不能继续授权。

`checkout_store_group` 的 `display_snapshot` 由服务端从当前 Checkout 重建，只含本店 `product_id、sku_id、name、spec_values、quantity` 和必要的本店配送阻断原因；响应/Tool Schema 不含其他店铺组、完整地址/手机号、优惠明细、支付偏好或跨店总额。Context TTL 不超过 Checkout `expires_at`；重新计价后旧 `checkout_version`，以及 `checkout_status=submitted/expired/cancelled` 的 Checkout 均不可继续读取。Context 过期不删除会话或历史消息，只要求用户回结算页刷新后重新选择 [联系客服]。

店铺 `closed/suspended` 时关闭新售前咨询，但有该店历史订单且仍在售后、争议或法定支持窗口内的用户，仍可用订单上下文恢复原店铺会话或创建受限售后会话；响应返回 `service_mode=historical_after_sale`，只开放订单/售后解释和相应人工队列，不开放商品推荐与购买引导。是否符合历史支持条件由服务端订单和政策快照判断，前端不能仅凭停业页面自行放开。

店铺客服技能限定在当前店铺范围，例如：回答该店商品/规格/展示库存、店铺政策与活动；查询用户在本店的订单和物流；解释页面可用操作并提供售后/订单入口；转该店人工客服。首版除创建本店人工工单外不开放业务写 Tool。它不得检索其他店铺知识、跨店铺订单、用户完整地址簿、平台风控标签或平台管理员数据，也不能代表平台裁决争议。

店铺员工维护的知识文档必须带 `store_id`，检索过滤条件在服务端强制注入而非由模型生成。店铺客服访问本店用户订单仍需同时满足“当前会话用户 + 当前店铺订单”两项约束，不能只凭 `order_id` 查询。

#### 3.12.19 人工客服接管接口

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `POST` | `/conversations/{conversation_id}/human-service-tickets` | 会话所属用户 | 创建人工服务工单；幂等；原因、紧急度由规则校验；Agent 建议经内部 Command Bus 触发，不伪造 User API 请求 |
| `GET` | `/conversations/{conversation_id}/human-service-ticket` | 会话所属用户 | 查询用户可见的当前未终结工单、队列位置/预计响应和接管状态 |
| `POST` | `/human-service-tickets/{ticket_id}/cancellations` | 申请用户 | 取消仍在排队且允许取消的请求；幂等 |
| `GET` | `/support/human-service-tickets` | 有权客服/主管 | 按店铺/平台队列、状态、技能和优先级领取列表；Cursor 分页与数据域隔离 |
| `POST` | `/support/human-service-tickets/{ticket_id}/claims` | 有权客服 | 原子领取工单；幂等/并发唯一，避免多人同时接管 |
| `POST` | `/support/human-service-tickets/{ticket_id}/transfers` | 当前客服/主管 | 转队列或转客服；原因必填并审计 |
| `POST` | `/support/human-service-tickets/{ticket_id}/waits` | 当前客服 | 进入 `waiting_user`；原因、If-Match、幂等，暂停 SLA 并通知用户 |
| `POST` | `/support/human-service-tickets/{ticket_id}/resumptions` | 当前客服 | 人工操作 `waiting_user → active`，恢复剩余 SLA；幂等；用户回复由内部消息事件直接调用同一 Domain Command，不伪造 Admin HTTP 请求 |
| `POST` | `/support/human-service-tickets/{ticket_id}/resolutions` | 当前客服/主管 | 提交 Resolution Code、用户可见摘要及内部备注；幂等；只恢复 AI 可用状态，不自动运行模型 |

Conversation 接管状态统一为 `active、human_pending、human_active、closed`；Ticket 使用自身的 `queued/assigned/active/waiting_user/resolved/closed` 状态表达更细阶段。进入 `human_active` 后，AI 停止面向用户自动回复，但可在授权范围内为人工客服生成建议；建议必须清楚标识且不直接发送。人工客服发送消息固定使用 3.12.22.8 的 `/support/conversations/{conversation_id}/messages`，Sender Type 由服务端固定为 Human，不允许客户端传入。

专属客服工单只能进入平台人工客服队列，店铺客服工单只能进入对应店铺人工客服/平台代理队列；涉及平台争议仲裁时通过受审计的升级流程转平台队列，不能让店铺员工读取平台内部工单。所有 Claim/Transfer/Resolve 都写 Assignment 和状态流水，并通过 WebSocket 通知用户。

#### 3.12.20 Agent Run 接口

`Agent Run` 是一次从已持久化输入消息开始，到生成答复、等待确认、转人工、失败或完成为止的可恢复执行实例。它不是让客户端提交任意 Prompt/Tool Name 的通用代理端点。

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `POST` | `/conversations/{conversation_id}/agent-runs` | 会话所属用户 | 为已持久化的 `trigger_message_id` 创建运行；幂等；消息 Outbox 的自动触发直接调用内部 Application Command，不伪造 User HTTP 请求 |
| `GET` | `/agent-runs/{run_id}` | 发起会话所属用户 | 查询用户可见状态、阶段、公开输出、等待原因、错误摘要、时间和可用动作；运维使用 `/admin/ai/runs/{run_id}` |
| `POST` | `/agent-runs/{run_id}/cancellations` | 发起会话所属用户 | 请求取消尚未终结运行；幂等；已提交的外部动作不承诺回滚；系统取消走内部 Command Bus |
| `POST` | `/agent-runs/{run_id}/resumptions` | 发起会话所属用户 | 仅当 `available_actions` 明确允许时从可公开恢复点恢复；幂等；授权决定/内部事件由内部 Command Bus 恢复 |
| `GET` | `/agent-runs/{run_id}/events` | 发起会话所属用户 | User Audience SSE 事件流，详见 3.12.23；管理观测使用 `/admin/ai/runs/*` |

创建请求只允许选择后端已发布的入口能力（通常由会话类型自动确定），不能传 System Prompt、模型密钥、任意 MCP Server URL 或跳过权限。服务端固定绑定 `agent_version_id、skill_versions、policy_version、model_profile、conversation_id、trigger_message_id`，并记录 Trace ID，以便重放和评估。

Run 状态建议为：

```text
queued → running → waiting_approval ─┐
                 → waiting_human    ├→ running → completed
                 → cancelling       │          → failed
                 → cancelled        ┘          → expired
```

Run 创建返回 202：

```json
{
  "data": {
    "run_id": "run_01K...",
    "status": "queued",
    "events_url": "/api/v1/agent-runs/run_01K.../events",
    "conversation_id": "conv_01K..."
  }
}
```

Agent 内部 Thought/Chain-of-Thought、密钥、完整工具参数和内部策略不得通过查询或事件流暴露。用户可见内容限于答复增量、引用来源、操作草稿、确认说明、进度和经过脱敏的错误。运行超时、模型失败或工具不确定时进入可恢复失败/人工流程，绝不能伪造成功消息。

#### 3.12.21 Agent 操作确认接口

Tool 权限分为：只读自动执行、低风险可撤销写入、需用户确认写入、禁止 Agent 执行。创建订单、取消订单、确认收货、发起售后、修改地址、暴露额外个人数据等如果未来向某类 Agent 开放，至少必须按策略确认；是否实际开放以具体 Agent 的 Tool Matrix 为准。首版店铺客服无交易写 Tool，专属客服只开放确认后提交退款申请。支付密码、实际付款、直接资金退款、管理员审批等不得由普通客服 Agent 代替用户完成。

| 方法 | 路径 | 访问者 | 用途与关键规则 |
| --- | --- | --- | --- |
| `GET` | `/agent-action-approvals/{approval_id}` | 被请求用户 | 获取结构化操作摘要、影响、参数掩码、到期时间、风险级别和当前状态；管理审计走 `/admin/ai/runs/*` |
| `GET` | `/conversations/{conversation_id}/agent-action-approvals` | 会话参与者 | 查询当前会话待确认/历史确认；Cursor 与状态筛选 |
| `POST` | `/agent-action-approvals/{approval_id}/decisions` | 被请求用户 | 明确 `approve` 或 `reject`；幂等；高风险操作要求近期认证/二次验证，管理员不能代用户批准 |
| `POST` | `/agent-action-approvals/{approval_id}/cancellations` | 被请求用户 | 在未决定前取消过时提案；幂等；系统失效走内部 Command Bus |

确认对象必须保存规范化 `action_type、tool_version、arguments_digest、resource_versions、policy_version、expires_at`。页面向用户展示可理解摘要和最终影响，例如“将订单 ord_x 的 1 件商品申请仅退款，最高 ¥89.90”，而不是仅显示工具名称。用户决策请求不允许修改参数；若用户想改数量/地址，应拒绝原提案并生成新 Approval。

批准后执行器再次校验身份、Consent、资源归属、资格、ETag/状态、金额和 Approval 是否未过期，并校验实际参数摘要与批准摘要完全一致。Approval 状态通过条件更新从 `pending` 进入 `approved/rejected/expired`，只有一个执行租约能进入 `executing`；外部动作使用独立幂等 Key。最终记录 `succeeded、failed` 或 `outcome_unknown`，后者先对账，禁止盲目重试。审计保存谁、何时、看到什么摘要、批准了什么版本和实际工具结果。

---

#### 3.12.22 管理后台接口

管理接口统一位于 `/admin`；客服实时工作台使用 `/support`，两者都只接受 Admin Audience。`/conversations`、`/reviews`、`/refund-applications`、`/shipments`、`/agent-runs`、`/agent-action-approvals` 等用户领域路径只接受 User Audience；管理/客服端不得为了复用路径而让同一 OpenAPI Operation 兼容两种 Token。两类路径可收敛到同一 Application Use Case，但必须使用不同 Operation ID、Audience Dependency、Permission/Ownership Policy 和响应字段投影。内部 Worker/Agent 通过服务身份和内部 Command Bus，不伪造用户或管理 Bearer Token。管理端使用独立登录入口、短时 Token、MFA/近期认证、RBAC Permission 和 Data Scope。任何审核、冻结、发布、改价、发货、库存调整、重放都使用显式命令资源，不提供可任意修改状态或金额的万能 PATCH。列表默认 Cursor 分页，批量导入/导出和长任务使用 `admin_batch_jobs`。

管理 API 通用响应除 3.11 Envelope 外，应在 `meta.authorization` 或等价位置返回当前请求采用的 `scope_type、scope_id、permission_version`，供界面展示“全平台/当前店铺”等可解释范围。该字段只说明本次结果范围，不授权后续请求。所有管理写操作记录 `admin_operation_logs`；失败的敏感读取、越权尝试和导出同样记录安全审计。

##### 3.12.22.1 管理端认证、当前身份与导航

| 方法 | 路径 | 用途与关键规则 |
| --- | --- | --- |
| `POST` | `/admin/auth/login` | 验证管理账号密码并创建 MFA Challenge；不直接签发普通管理 Session |
| `POST` | `/admin/auth/mfa-verifications` | 原子验证 WebAuthn/TOTP/恢复码并签发 `client_type=admin` Session；幂等、限流、防重放 |
| `POST` | `/admin/auth/reauthentications` | 为当前 Session 完成短时近期认证；返回 `reauth_expires_at`，不返回可转移万能 Ticket |
| `POST` | `/admin/auth/logout` | 撤销当前管理 Session；重复调用一致 |
| `GET` | `/admin/me` | 返回管理员公开资料、Assurance、Permission/Scope 摘要和 Session 安全状态 |
| `GET` | `/admin/navigation` | 返回按权限裁剪的菜单、路由 Code 和可选 Scope；不包含任意前端代码或外部 URL |
| `GET` | `/admin/auth/sessions` | 列出当前管理员自己的有效 Admin Audience 会话；不混入用户端会话 |
| `DELETE` | `/admin/auth/sessions/{session_id}` | 撤销当前管理员自己的指定管理会话；跨 Audience 或跨用户统一按不存在处理 |

管理登录与用户 `/auth/login` 使用独立 Operation、限流、Cookie Path、Audience 和审计事件。管理端 Token 调用用户购物接口返回 403 `AUTH_AUDIENCE_MISMATCH`；不能为了“管理员也能测试”放开生产购物权限。MFA 绑定/撤销和恢复码管理使用独立安全流程、近期认证及 3.7.2.6，不允许管理员替其他管理员查看 Secret。

##### 3.12.22.2 管理仪表盘

| 方法 | 路径 | 权限 | 用途与关键规则 |
| --- | --- | --- | --- |
| `GET` | `/admin/dashboard/summary` | `dashboard:read` | 当前 Scope 的交易、商品、履约、售后、客服、AI 和系统小型聚合；返回口径、窗口、更新时间 |
| `GET` | `/admin/dashboard/timeseries` | `dashboard:read` | 按白名单 `metric、interval、from、to` 返回时序；限制范围和点数 |
| `GET` | `/admin/dashboard/work-queues` | 各领域 Read Permission | 返回待处理计数及安全筛选链接参数，不返回完整业务记录 |

仪表盘从只读聚合/指标存储读取，不在请求中全表扫描交易表。金额、订单和退款以业务事实为准；监控采样指标必须标注 `estimated`，不能与财务口径混用。不同卡片可以独立失败并返回 `unavailable_sections`。

##### 3.12.22.3 用户治理与 RBAC

| 方法 | 路径 | 权限与行为 |
| --- | --- | --- |
| `GET` | `/admin/users`、`/admin/users/{user_id}` | `users:read`；默认脱敏，按 Scope/Cursor 查询 |
| `POST` | `/admin/users/{user_id}/status-changes` | `users:manage`；显式 `suspend/resume`，原因、可选冻结期限、If-Match 和幂等键必填；写 `user_status_records` |
| `GET` | `/admin/users/{user_id}/status-events` | `users:read`；Cursor 读取冻结/解冻/注销时间线的脱敏投影 |
| `POST` | `/admin/users/{user_id}/session-revocations` | `users:sessions_revoke`；选择当前/全部范围并通知用户 |
| `POST` | `/admin/users/{user_id}/password-reset-requirements` | `users:force_password_reset`；只设置 `must_change_password`、撤销会话，管理员不得提交新密码 |
| `POST` | `/admin/users/{user_id}/sensitive-field-access-grants` | `users:read_sensitive`；近期认证、字段白名单、Purpose、业务理由和服务端上限内 TTL；返回绑定当前 Session 的 `grant_id` |
| `GET` | `/admin/users/{user_id}/sensitive-fields` | `users:read_sensitive` + 同 Session Active Grant；通过 `X-Sensitive-Access-Grant` 指定一次消费 Grant，只返回白名单字段并加水印/审计 |
| `POST` | `/admin/sensitive-field-access-grants/{grant_id}/revocations` | Grant 所属管理员需 `users:read_sensitive`，安全主管可用 `users:manage` + 目标 Scope；原因、If-Match、幂等，立即失效 Redis 缓存 |
| `GET/POST` | `/admin/roles` | GET 要求 `rbac:read`，POST 要求 `rbac:manage`；创建角色使用明确 Scope Type |
| `GET/PATCH` | `/admin/roles/{role_id}` | `rbac:manage`；If-Match；System Role 基本语义不可改 |
| `PUT` | `/admin/roles/{role_id}/permissions` | `rbac:manage`；完整目标集 + If-Match，禁止不可委派权限 |
| `GET` | `/admin/users/{user_id}/role-grants` | `rbac:read`；Cursor 读取 Active/历史 Grant 与 Event，不用当前角色列表伪装历史 |
| `POST` | `/admin/users/{user_id}/role-grants` | `rbac:manage`；创建新 Grant，明确 Role/Scope/有效期/原因；唯一 Active 约束、If-Match、幂等 |
| `POST` | `/admin/users/{user_id}/role-grants/{grant_id}/revocations` | `rbac:manage`；撤销指定 Active Grant，原因、If-Match、幂等，不删除旧行 |

角色授予/撤销/到期同时递增目标用户 `permission_version`、追加 `user_role_events`、失效缓存并按风险撤销管理 Session。到期 Worker 将已到期的 Active Grant 条件迁移为 `expired`；授予同角色/同 Scope 时新建 Grant，不恢复历史行。不能删除最后一个安全管理员；操作者不能批准自己的权限提升，Critical 权限按策略进入双人复核。

##### 3.12.22.4 店铺审核与运营

| 方法 | 路径 | 权限与行为 |
| --- | --- | --- |
| `GET` | `/admin/store-certifications`、`/admin/store-certifications/{certification_id}` | `stores:review`；材料下载鉴权、水印和审计 |
| `POST` | `/admin/store-certifications/{certification_id}/decisions` | `stores:review`；`approve/reject/request_more_info`、If-Match、原因和幂等 |
| `GET` | `/admin/store-certifications/{certification_id}/events` | `stores:review` 或 `stores:manage` + 本店 Scope；Cursor 返回材料版本与审核时间线 |
| `POST` | `/admin/store-certifications/{certification_id}/material-versions` | `stores:manage` + 本店 Scope；仅 `more_info_required`，提交完整 Active 私有文件集、材料版本、If-Match 与幂等，成功后回到 `pending` |
| `GET` | `/admin/stores`、`/admin/stores/{store_id}` | `stores:read` + Data Scope |
| `PATCH` | `/admin/stores/{store_id}` | `stores:manage` + 本店 Scope；修改名称、简介或已通过扫描且归属本店的 `store_logo` 派生文件，If-Match 必填 |
| `POST` | `/admin/stores/{store_id}/status-changes` | `stores:manage`；展示/提交影响码，不能覆盖认证历史 |
| `GET/POST/PATCH` | `/admin/stores/{store_id}/product-groups` | 店铺运营权限；If-Match、店铺归属校验 |
| `PUT` | `/admin/stores/{store_id}/product-groups/{group_id}/products` | 完整目标商品集；所有 Product 必须属于本店 |
| `GET/POST` | `/admin/stores/{store_id}/service-policies` | GET 需 `store_policies:read`，POST 需 `store_policies:create`，均需 Store Scope；GET 读取家族/历史，POST 创建新草稿版本 |
| `GET/PATCH` | `/admin/stores/{store_id}/service-policies/{policy_id}` | GET 需 `store_policies:read`；PATCH 需 `store_policies:update`，仅 Draft，If-Match，已发布行不可改 |
| `POST` | `/admin/stores/{store_id}/service-policies/{policy_id}/publications` | `store_policies:publish`；时间窗/内容/引用校验，生成不可变 Published Version、快照关联和缓存/RAG Outbox |
| `POST` | `/admin/stores/{store_id}/service-policies/{policy_id}/withdrawals` | `store_policies:publish`；仅当前/未来 Published，原因、If-Match、幂等，不影响历史订单快照 |
| `GET/POST` | `/admin/stores/{store_id}/shipping-templates` | 配送模板读/建草稿 |
| `PATCH` | `/admin/stores/{store_id}/shipping-templates/{template_id}` | 只修改草稿；If-Match |
| `POST` | `/admin/stores/{store_id}/shipping-templates/{template_id}/publications` | 影响预览版本一致后发布不可变版本并失效 Checkout 缓存 |
| `GET/POST/PATCH` | `/admin/stores/{store_id}/announcements` | 本店公告草稿与时段管理 |
| `PUT` | `/admin/stores/{store_id}/featured-products` | 完整有序目标集；商品本店且在允许状态 |

##### 3.12.22.5 商品、SKU、分类、品牌与库存

| 方法 | 路径 | 权限与行为 |
| --- | --- | --- |
| `GET/POST` | `/admin/products` | GET 要求 `products:read`，POST 要求 `products:create`，均校验 Store Scope；创建只接受业务字段，不接受状态/销量 |
| `GET/PATCH` | `/admin/products/{product_id}` | GET 要求 `products:read`，PATCH 要求 `products:update`；仅草稿/可编辑状态，If-Match，富文本服务端消毒 |
| `POST` | `/admin/products/{product_id}/review-submissions` | 店铺提交审核；完整性检查、幂等 |
| `POST` | `/admin/products/{product_id}/moderation-decisions` | `products:review`；通过/拒绝/补充，原因和 If-Match |
| `POST` | `/admin/products/{product_id}/publications` | `products:publish`；校验 SKU/主图/分类/履约/店铺状态并写 Outbox |
| `POST` | `/admin/products/{product_id}/off-shelf-commands` | `products:publish`；原因、影响和幂等，不改历史订单快照 |
| `POST` | `/admin/products/{product_id}/skus` | `products:update`；规格组合唯一，Money/店铺 SKU Code 校验 |
| `PATCH` | `/admin/products/{product_id}/skus/{sku_id}` | `products:update`；If-Match；订单已引用 SKU 不允许物理删除 |
| `POST` | `/admin/products/{product_id}/skus/{sku_id}/status-changes` | 启用/禁用命令；影响预览、If-Match |
| `GET` / `PUT` | `/admin/products/{product_id}/fulfillment-profile` | 读取当前履约资料或以 If-Match 整体设置；配送模板必须属于本店且为生效版本 |
| `PUT` | `/admin/products/{product_id}/images`、`/attributes` | 分区整体设置；File Owner/Purpose、Schema、Store Scope 和 If-Match |
| `POST` | `/admin/products/{product_id}/detail-content-versions` | `products:update` + Store Scope；提交不可信 Source，服务端创建不可变候选版本、执行 Sanitization/安全扫描，绝不接受客户端 `safe_html` 为权威 |
| `GET` | `/admin/products/{product_id}/detail-content-versions/{version_id}` | `products:read` + Store Scope；返回 Source 仅供有权编辑/审核，预览固定使用服务端安全派生内容并加管理水印 |
| `POST` | `/admin/products/{product_id}/faqs` | `products:update` + Store Scope；创建 FAQ 定义和首个答案候选版本，问题为纯文本 |
| `POST` | `/admin/products/{product_id}/faqs/{faq_id}/versions` | `products:update` + Store Scope；新建答案版本，不原地改已发布答案 |
| `POST` | `/admin/products/{product_id}/faqs/{faq_id}/publications` | `products:publish` + Store Scope；重新 Sanitization/扫描后原子切换公开版本、写 Outbox |
| `GET/POST/PATCH` | `/admin/categories`、`/admin/categories/{category_id}` | `catalog_taxonomy:manage`；移动节点防环，使用 If-Match |
| `GET/POST/PATCH` | `/admin/brands`、`/admin/brands/{brand_id}` | `catalog_taxonomy:manage`；已引用品牌只能停用 |
| `GET` | `/admin/inventories`、`/admin/inventories/{sku_id}` | `inventories:read` + Store Scope；返回可展示库存和 Version |
| `POST` | `/admin/inventory-adjustments` | `inventories:adjust`；提交 SKU、有符号增量、原因码、业务引用、预期版本；幂等 |
| `GET` | `/admin/inventory-adjustments` | `inventories:read`；读取库存流水，不返回内部风控阈值 |

图片、FAQ、富文本、SKU 和履约资料分区更新成功不等于商品已发布；Product Publication 是独立命令，并固定一个扫描通过的商品详情版本和各 Published FAQ Version。发布时服务端用当前 `sanitizer_policy_version` 重跑标签/属性/URL Scheme/图片归属/内容安全检查，任何脚本、事件属性、任意 iframe/表单、可执行 SVG 或不可信外链均阻断；通过后写不可变版本、状态流水、缓存/RAG Outbox。库存调整服务端锁定 `inventories`、验证结果不为负、更新 Version、写 `inventory_logs/admin_operation_logs/outbox_events`；不提供“直接把库存设置为任意最终值且无原因”的接口。

##### 3.12.22.6 订单、支付与履约

| 方法 | 路径 | 权限与行为 |
| --- | --- | --- |
| `GET` | `/admin/orders`、`/admin/orders/{order_id}` | `orders:read` + Store Scope；用户/地址按 Purpose 投影和审计 |
| `POST` | `/admin/orders/{order_id}/amount-adjustments` | `orders:adjust`；仅未支付且不存在进行中/结果未知的支付尝试；金额、原因、订单版本、幂等和职责分离 |
| `POST` | `/admin/orders/{order_id}/cancellations` | `orders:cancel`；只允许领域状态机定义的受控取消，支付后不能绕过售后 |
| `GET` | `/admin/payments`、`/admin/payments/{payment_id}` | `payments:read`；不返回渠道 Secret/完整报文 |
| `POST` | `/admin/payments/{payment_id}/reconciliations` | `payments:reconcile`；创建异步查询/对账任务，不能直接标记成功 |
| `POST` | `/admin/orders/{order_id}/shipments` | `shipments:create` + Store Scope；按包裹提交商品数量、承运商、运单；幂等 |
| `GET` | `/admin/shipments/{shipment_id}` | `shipments:read` + Store Scope；返回独立 `AdminShipmentDetail`，只含运单掩码，Schema 中不得出现 `tracking_no` |
| `POST` | `/admin/shipments/{shipment_id}/tracking-corrections` | `shipments:correct`；仅允许规则窗口内纠错，完整 `tracking_no` 为 Write-only，If-Match、原因和审计，不覆盖轨迹历史；响应只返回掩码 |
| `POST` | `/admin/shipments/{shipment_id}/voids` | `shipments:void` + Store Scope；仅 `created`、未揽收、渠道已确定撤销且无阻止性售后，整包作废并原子回退履约分配 |
| `POST` | `/admin/shipments/{shipment_id}/refreshes` | `shipments:refresh`；异步、限频、幂等 |

首版无包裹草稿和通用 PATCH；`POST /admin/orders/{order_id}/shipments` 成功就是“确认发货”。事务校验订单已支付、Store Scope、可发数量、重复运单、当前履约状态和订单 Version，原子写 `shipments/shipment_items/order_status_logs/order_operation_logs/outbox_events`。包裹商品和数量创建后不可编辑；运单录入错误使用纠错命令，包裹整体错误使用揽收前作废命令。外部面单创建/撤销结果未知时返回可查询资源并进入对账，禁止盲目重试。

##### 3.12.22.7 退款、申诉与评价治理

| 方法 | 路径 | 权限与行为 |
| --- | --- | --- |
| `GET` | `/admin/refund-applications`、`/admin/refund-applications/{refund_id}` | `refunds:read` + Store Scope；形成审核队列；执行决定另需 `refunds:review` |
| `POST` | `/admin/refund-applications/{refund_id}/claims` | 原子领取；If-Match，防止重复审核 |
| `POST` | `/admin/refund-applications/{refund_id}/decisions` | 同意/拒绝/补材料；资格、金额、状态、原因、版本和职责分离校验 |
| `GET` | `/admin/refund-appeals`、`/admin/refund-appeals/{appeal_id}` | `refund_appeals:read`，平台范围；执行决定另需 `refund_appeals:review` |
| `POST` | `/admin/refund-appeals/{appeal_id}/claims`、`/decisions` | 原子领取与独立复核；不覆盖原退款历史 |
| `GET` | `/admin/reviews`、`/admin/reviews/{review_id}` | `reviews:read` + Scope；治理决定另需 `reviews:moderate`；公开匿名不影响有权审计 |
| `POST` | `/admin/reviews/{review_id}/moderation-decisions` | 屏蔽/恢复/升级；规则码、原因、If-Match、幂等 |
| `POST` | `/admin/review-append-records/{append_id}/moderation-decisions` | 追评治理，规则同上 |
| `POST` | `/admin/review-replies/{reply_id}/moderation-decisions` | 商家回复治理；平台/店铺权限分层 |
| `POST` | `/admin/reviews/{review_id}/replies` | `reviews:reply` + Store Scope；商家回复本店评价，幂等/内容审核，不冒充用户或改写主评价 |

退款审核不是客服工单；普通 `merchant_review` 直接由退款聚合投影成队列。高金额、平台申诉、超规则补偿或同人发起并审批命中职责冲突时，接口返回 `approval_required`/复核资源，不执行资金副作用。Agent 身份永远不能调用审核决定接口。

##### 3.12.22.8 人工客服工作台

管理前端使用 3.12.19 的 `/support/human-service-tickets` Claim/Transfer/Resolution 接口，并补充：

| 方法 | 路径 | 权限与行为 |
| --- | --- | --- |
| `GET` | `/support/queues` | 返回当前客服可见平台/店铺队列、技能和统计摘要 |
| `GET` | `/support/human-service-tickets/{ticket_id}/workspace` | 返回 `handoff_summary`、必要用户可见消息引用、脱敏用户摘要和经授权业务卡片；逐次校验工单/分配关系、RBAC、Queue/Store Scope 与访问理由，不返回无关完整历史 |
| `POST` | `/support/human-service-tickets/{ticket_id}/priority-changes` | 主管权限、原因、If-Match |
| `POST` | `/support/human-service-tickets/{ticket_id}/internal-notes` | 内部备注独立存储/可见性，不创建用户消息，不进入 Agent Context |
| `GET` | `/support/conversations/{conversation_id}/messages` | `support:queue_read` + 当前工单/分配关系；返回客服可见投影 |
| `POST` | `/support/conversations/{conversation_id}/messages` | `support:reply` + 当前 Active Assignment；固定 `sender_type=human`，幂等、内容审核、事务后 ACK |
| `PUT` | `/support/conversations/{conversation_id}/read-cursor` | 当前客服/队列参与者；单调推进客服 Reader Cursor |
| `POST` | `/support/human-service-tickets/{ticket_id}/waits` | `support:wait` + 当前客服；`active → waiting_user`，原因、If-Match、幂等，暂停 SLA 并通知用户 |
| `POST` | `/support/human-service-tickets/{ticket_id}/resumptions` | `support:resume` + 当前客服；`waiting_user → active`，恢复剩余 SLA，幂等；用户消息 Consumer 以内建服务身份调用同一 Domain Command，不调用本 Admin API |

`/support/*` 只接受 Admin Audience，不接受 User Token；用户消息只使用 3.12.16 的 `/conversations/*`。内部备注固定写入 `human_service_internal_notes`，不得写入 `messages.text_content`。平台与店铺队列、消息字段投影和业务卡片权限均由后端派生。

##### 3.12.22.9 Agent、Skill、Tool 与策略

| 方法 | 路径 | 权限与行为 |
| --- | --- | --- |
| `GET/POST` | `/admin/ai/agents` | GET 要求 `ai_agents:read`，POST 要求 `ai_agents:manage`；创建 Definition |
| `POST` | `/admin/ai/agents/{agent_id}/versions` | 创建不可变候选版本草稿 |
| `GET` | `/admin/ai/agent-versions/{version_id}/diff` | 与当前发布版本进行结构化脱敏 Diff |
| `POST` | `/admin/ai/agent-versions/{version_id}/publications`、`/rollbacks` | `ai_agents:publish`；评估门槛、近期认证、审批、原因 |
| `GET/POST` | `/admin/ai/skills`、`/admin/ai/skills/{skill_id}/versions` | GET 要求 `ai_skills:read`，POST 要求 `ai_skills:manage` |
| `POST` | `/admin/ai/skill-versions/{version_id}/publications`、`/rollbacks` | Skill 准入、发布与回滚 |
| `GET/POST` | `/admin/ai/tools` | GET 要求 `ai_tools:read`；POST 要求 `ai_tools:manage`，只创建稳定 Tool Definition |
| `GET/PATCH` | `/admin/ai/tools/{tool_id}` | GET 要求 `ai_tools:read`；PATCH 要求 `ai_tools:manage`，仅修改名称、描述、Owner 等 Definition 元数据，If-Match；不得修改已发布契约 |
| `POST` | `/admin/ai/tools/{tool_id}/versions` | `ai_tools:manage`；创建包含 Schema、MCP/网络/风险/超时/幂等配置的不可变候选 Version |
| `GET` | `/admin/ai/tool-versions/{version_id}` | `ai_tools:read`；查看不可变 Tool Version 详情 |
| `GET` | `/admin/ai/tool-versions/{version_id}/diff` | `ai_tools:read`；查看相对当前 Published Version 的脱敏结构化 Diff |
| `POST` | `/admin/ai/tool-versions/{version_id}/publications` | `ai_tools:publish`；Contract/安全/评估门槛、近期认证和必要的管理员双人审批 |
| `POST` | `/admin/ai/tools/{tool_id}/rollbacks` | `ai_tools:publish`；请求体引用历史 Published `target_version_id`，通过审批后原子切换当前指针 |
| `PUT` | `/admin/ai/skill-versions/{version_id}/tool-bindings` | `ai_skills:manage`；只接受 Published `tool_version_id`，不允许绑定 Definition 或运行时动态扩权 |
| `GET/POST` | `/admin/ai/tool-permission-policies` | GET 要求 `ai_policies:read`，POST 要求 `ai_policies:manage`；创建策略草稿 |
| `POST` | `/admin/ai/tool-permission-policies/{policy_id}/publications` | `ai_policies:publish`；默认拒绝、影响 Diff、测试门槛和双人复核 |
| `POST` | `/admin/ai/kill-switch-actions` | `ai_runtime:kill`；按 Agent/Skill/Tool/Server/Model/Store 阻断，原因与告警必填 |

##### 3.12.22.10 知识库、评估与可观测性

| 方法 | 路径 | 权限与行为 |
| --- | --- | --- |
| `GET/POST` | `/admin/knowledge/documents` | GET 要求 `knowledge:read`，POST 要求 `knowledge:manage`，均校验平台/店铺 Scope；文件扫描通过后才创建可索引版本 |
| `GET` | `/admin/knowledge/documents/{document_id}` | 返回版本、状态、Hash、Scope、抽样切片和引用摘要 |
| `POST` | `/admin/knowledge/documents/{document_id}/indexing-jobs` | 创建 MySQL 3.7.14.6 管理父 Job 并返回 `job_id`；幂等、固定文档/模型版本，PG `kjob_` 由 Outbox Consumer 幂等创建并回填 |
| `POST` | `/admin/knowledge/documents/{document_id}/publications`、`/withdrawals` | Active/撤回显式命令；ACL 和抽样验证 |
| `GET` | `/admin/ai/runs`、`/admin/ai/runs/{run_id}` | `ai_observability:read`；脱敏节点/工具/成本，不返回隐藏推理或 Secret |
| `GET` | `/admin/ai/evaluations`、`/admin/ai/evaluations/{evaluation_id}` | `ai_evaluations:read`；读取由离线治理流程固定基线后生成的评估报告，不提供数据集管理接口 |
| `POST` | `/admin/ai/evaluation-runs` | `ai_evaluations:run`；只允许引用部署注册表中的只读基线 Code，创建异步评估 Job并冻结配置和预算 |
| `GET` | `/admin/observability/summary` | `observability:read`；返回聚合指标和到 Grafana/Trace 的受控引用，不接受任意查询语言 |

##### 3.12.22.11 平台内容运营

| 方法 | 路径 | 权限与行为 |
| --- | --- | --- |
| `GET/POST` | `/admin/content-entries` | GET 要求 `content:read`，POST 要求 `content:manage`；按 Banner/公告/帮助/页脚/法务文档类型创建 Entry |
| `GET` | `/admin/content-entries/{content_id}` | 返回当前版本、历史版本摘要和 ETag |
| `POST` | `/admin/content-entries/{content_id}/versions` | 创建草稿版本；严格内容 Schema，服务端生成 Safe HTML |
| `PATCH` | `/admin/content-versions/{version_id}` | 仅 Draft；If-Match |
| `POST` | `/admin/content-versions/{version_id}/review-submissions`、`/decisions` | 内容审核与职责分离 |
| `POST` | `/admin/content-versions/{version_id}/publications` | `content:publish`；时间窗、受众、文件、链接白名单和影响校验 |
| `POST` | `/admin/content-entries/{content_id}/withdrawals` | 撤回并立即失效缓存；原因、If-Match、幂等 |

用户端公开读取契约固定如下（全部只返回当前有效 Published Version）：

| Operation | 方法与路径 | 关键规则 |
| :--- | :--- | :--- |
| `ContentBanner_ListPublished` | `GET /content/banners` | 按 Audience/时间窗返回安全展示字段 |
| `ContentAnnouncement_ListPublished` | `GET /content/announcements` | 只返回有效公告与 Version |
| `HelpArticle_ListPublished` | `GET /content/help-articles` | `q/category/cursor`；标题/摘要/内容 Key，不返回草稿 |
| `HelpArticle_GetPublished` | `GET /content/help-articles/{content_key}` | 撤回/过期返回 410；Safe HTML+CSP+Version |
| `AboutContent_GetPublished` | `GET /content/about` | 关于我们的当前发布版 |
| `FooterContent_GetPublished` | `GET /content/footer` | 返回帮助/关于站内 Route 和已发布外链 Allowlist；不返回任意 URL |
| `LegalDocument_GetPublished` | `GET /content/legal-documents/{document_type}` | `version/locale/region_code` 精确版本语义 |

法务文档省略 `version` 时只返回当前地区/语言的有效版本，携 `?version=...` 时仅在该不可变版本曾合法发布且允许公开留档时返回精确正文、`document_version、content_hash、effective_at、locale、region_code`，否则返回 404/410，绝不能静默替换成新版。注册配置中的 `content_url` 必须指向此精确版本接口，注册提交仍由服务端按版本和 Hash 校验。管理端草稿、审核意见和操作者不得进入公开 DTO。友情链接只能来自版本化 `https` URL Allowlist，服务端拒绝内网/localhost/用户信息 URL，前端固定 `rel="noopener noreferrer"`。

法务文档创建/发布沿用内容 Version API，但提交 Schema 必须包含 `document_type、locale、region_code、effective_at、safe_html/safe_text`。发布要求 `content:publish`、近期认证和职责分离；已被 `user_agreement_acceptances` 引用的 Published Version 只允许到期/撤回并保留公开归档策略，不允许覆盖正文、重算 Hash 或物理删除。

##### 3.12.22.12 批量任务、导入与导出

| 方法 | 路径 | 权限与行为 |
| --- | --- | --- |
| `POST` | `/admin/batch-jobs` | 创建导入/导出/其他白名单 Job；请求含 `job_type、input_file_id/filter、scope、schema_version`，幂等 |
| `GET` | `/admin/batch-jobs`、`/admin/batch-jobs/{job_id}` | 当前管理员/有权主管读取状态和汇总；Cursor |
| `GET` | `/admin/batch-jobs/{job_id}/items` | Cursor 读取逐项验证/执行结果；限制字段和大小 |
| `POST` | `/admin/batch-jobs/{job_id}/confirmations` | 预检通过后确认执行；If-Match、近期认证/审批按风险决定 |
| `POST` | `/admin/batch-jobs/{job_id}/cancellations` | 协作取消；已执行副作用不伪装回滚 |
| `POST` | `/admin/batch-jobs/{job_id}/download-tickets` | 为结果/错误文件签发一次性短时下载 Ticket；下载继续鉴权和审计 |

同步 API 不返回无上限 CSV/Excel。导出创建时和执行时都重新校验 Permission/Scope；如果发起人已离职、权限被撤销或筛选范围扩大，任务 Fail Closed。导入使用官方模板 Schema Version，预检逐行返回稳定错误码，确认后以业务键/输入 Hash 幂等执行。

##### 3.12.22.13 审计、系统任务与死信

| 方法 | 路径 | 权限与行为 |
| --- | --- | --- |
| `GET` | `/admin/audit-logs`、`/admin/ai/tool-action-audits` | `audit:read`；筛选、详情和导出本身审计；只读 |
| `GET` | `/admin/scheduled-job-logs` | `jobs:read`；查看调度槽、计数、断点和脱敏错误 |
| `GET` | `/admin/dead-letter-events`、`/admin/dead-letter-events/{dead_letter_id}` | `events:read`；返回脱敏 Payload 摘要、Schema、当前资源影响和 ETag；重放/处置另需 `events:operate` |
| `POST` | `/admin/dead-letter-events/{dead_letter_id}/replay-previews` | 只读检查当前 Schema、资源版本、幂等能力和预计副作用 |
| `POST` | `/admin/dead-letter-events/{dead_letter_id}/replays` | `events:operate`；必须引用未过期 Preview、If-Match、原因、幂等和必要审批 |
| `POST` | `/admin/dead-letter-events/{dead_letter_id}/resolutions` | 标记解决/忽略并填写原因；不修改或删除原事件 |

禁止提供修改死信 Payload、删除审计记录、“一键重放全部”或无影响预览的批量重放接口。支付、库存、退款和权限事件需要更高权限/双人复核；重放生成新 Trace 并关联 Original Trace。

##### 3.12.22.14 管理员审批与双人复核

管理员审批使用 3.7.2.7～3.7.2.9 的独立资源，不复用 Agent 用户确认。没有通用 `POST /admin/approval-requests`：只有退款例外、RBAC 提升、AI/内容发布、敏感导出和死信重放等领域命令在命中已发布策略后，才能以可信参数创建申请并返回 `202 Accepted`：

```json
{
  "data": {
    "command_status": "approval_required",
    "approval_request_id": "aar_01K...",
    "required_approval_count": 2,
    "approved_count": 0,
    "expires_at": "2026-08-21T10:30:00Z"
  }
}
```

| 方法 | 路径 | 权限与行为 |
| --- | --- | --- |
| `GET` | `/admin/approval-requests` | `admin_approvals:read` + Scope；按类型、状态、发起人、待本人处理和时间 Cursor 查询 |
| `GET` | `/admin/approval-requests/{approval_request_id}` | `admin_approvals:read` 或申请发起人；返回脱敏展示快照、参数 Hash、资源版本、门槛、决定摘要和执行结果 |
| `GET` | `/admin/approval-requests/{approval_request_id}/events` | 同详情可见性；返回追加型审批/执行时间线，不返回命令密文 |
| `POST` | `/admin/approval-requests/{approval_request_id}/decisions` | `admin_approvals:decide` + 领域审批 Permission + Scope；提交 `approve/reject、reason_code、reason`，If-Match、Idempotency-Key、MFA/近期认证必需 |
| `POST` | `/admin/approval-requests/{approval_request_id}/cancellations` | 仅发起人或有权主管取消仍为 Pending 的申请；原因、If-Match、幂等必需 |

决定接口不接受业务参数、金额、目标或 `required_approval_count`。服务端拒绝发起人自批、同一 `approver_user_id` 重复占位、Permission/Scope 不匹配、职责角色冲突和过期申请；如部署已接入企业 IdP，还必须按 Workforce Subject 阻止同一主体的多账号占位。任一拒绝使申请终止；达到门槛后只把申请推进为 `approved` 并写 Outbox，由专用 Executor 重新加载当前操作者/审批人权限、解密并重建白名单参数、校验 Hash、资源 Version 和领域状态，再以 `execution_no` 调用原 Domain Command。

执行成功进入 `succeeded` 并关联结果资源；确定失败进入 `failed`；外部副作用结果不明进入 `outcome_unknown`，只允许由原领域对账流程推进为确定终态。参数、资源版本或审批策略发生不兼容变化时返回 409/412 并使旧申请不可执行，必须回到原业务页面重新发起。

##### 3.12.22.15 管理 API 并发、确认和错误处理

- 列表批量操作逐项授权并设数量上限；超限转换为 Batch Job。不能相信前端传入的 `store_id、permission_code、operator_id`，这些值由管理 Session 和目标资源派生。
- 可变资源写使用 ETag/If-Match；412 返回安全 Current Version 和可选 Diff Ref。缺 If-Match 返回 428，前端不得自动以最新版本重放旧意图。
- 创建/命令使用 Idempotency-Key；超时且结果未知返回可查询资源。前端不得把 504 当作确定失败后无条件再执行。
- 高风险命令返回 `confirmation_requirements`；只需当前操作者确认的命令完成近期认证后执行，需要职责分离的命令按 3.12.22.14 创建 `admin_approval_requests` 并返回 202。自然语言确认或普通弹窗不能替代审批资源。
- 对订单、支付、退款、会话和用户详情的越权访问采用资源不可见策略，同时记录拒绝审计。允许暴露功能但缺权限时返回 403 和稳定 Permission Code，不返回其他角色成员信息。
- 管理接口不返回密码、MFA Secret、恢复码、支付密钥、对象存储 Secret、完整第三方回调报文、隐藏推理或无业务必要的 PII。临时敏感字段访问使用短时 Grant、字段白名单、水印和审计。

#### 3.12.23 SSE 流式响应接口

Agent 输出使用 Server-Sent Events：

```http
GET /api/v1/agent-runs/{run_id}/events
Accept: text/event-stream
Authorization: Bearer <access_token>
Last-Event-ID: evt_01K...
```

Vue 3 客户端优先使用支持 `Authorization` Header、AbortController 和可控重连的 Fetch Streaming 客户端；若使用原生 `EventSource`，应先以 User Audience 调用 `POST /api/v1/realtime-tickets` 获取作用域限于本人 Run、约 30 秒过期、单次使用的 Ticket，禁止把长期 Access Token 放入 URL。管理端不复用本 SSE Operation，其脱敏 Run 观测走 `/admin/ai/runs/*`。

事件帧遵循标准 `id/event/data` 格式，`data` 是单行或多行 UTF-8 JSON：

```text
id: evt_01K...
event: message.delta
data: {"run_id":"run_01K...","message_id":"msg_01K...","delta":"正在查询物流","sequence":12}

```

首版公开事件类型如下：

| Event | 含义与主要 Payload |
| --- | --- |
| `run.snapshot` | 建连/恢复时的当前 Run 状态、已生成公开内容和最新 Event ID |
| `run.started` | Run 开始执行，含版本化阶段名 |
| `message.started` | Assistant 消息占位已持久化，含 `message_id` |
| `message.delta` | 文本增量；按 `sequence` 合并，不单独作为最终事实 |
| `message.completed` | 最终消息已持久化，客户端应用最终正文替换增量缓冲 |
| `citation.added` | 增加可见知识来源，含文档标题和安全引用 ID |
| `tool.progress` | 可向用户公开的工具进度，不包含密钥或完整敏感参数 |
| `approval.required` | 需要用户确认，含 `approval_id` 和可理解摘要 |
| `human_service.requested` | 已进入人工队列，含工单公开状态 |
| `run.completed` | Run 成功终结 |
| `run.failed` | Run 失败，含安全错误码、是否可重试和 Request ID |
| `run.cancelled` | Run 已取消 |
| `heartbeat` | 保持连接；不推进业务状态，可不持久化 |

每个持久化事件具有 Run 内单调序号和全局公开 Event ID。客户端按 Event ID 去重；断线后携 `Last-Event-ID` 重连，服务端从短期 Redis Stream/事件存储补发。若短期流已裁剪但 Run 仍可查询，则先发送 `run.snapshot` 再继续；不得从头重复执行 Tool。鉴权失败在建立流前返回普通 Problem JSON；流建立后错误通过 `run.failed` 表达并关闭。代理层必须关闭响应缓冲，设置心跳和空闲超时，客户端采用有上限指数退避。

SSE 只承载一次 Run 的单向进度，不承担全站未读消息、人工客服新消息或多设备同步；这些使用 WebSocket。生成中止后，服务端仍应持久化明确的消息/Run 终态，前端不能把最后一个 `delta` 自动当作完整答案。

#### 3.12.24 WebSocket 实时消息接口

WebSocket 用于消息会话、未读计数、人工接管和跨设备状态。用户端与客服端分别签发 Ticket，避免同一 HTTP Operation 兼容两种 Audience；底层可复用同一网关，但 Subprotocol、Channel Allowlist 和发送命令不同：

```text
User:  POST /api/v1/realtime-tickets             (User Audience)
       → {ticket, expires_at, websocket_url, subprotocol: "ecom.user-messages.v1"}

Admin: POST /api/v1/support/realtime-tickets     (Admin Audience + support:queue_read)
       → {ticket, expires_at, websocket_url, subprotocol: "ecom.support-messages.v1"}

GET  /api/v1/ws/messages?ticket=<single-use-ticket>
Sec-WebSocket-Protocol: <ticket-bound-subprotocol>
```

Realtime Ticket 必须绑定 Audience、用户/客服身份、设备、允许的 Channel、发送能力和 Token Session，约 30 秒过期且消费一次即失效；服务端验证 HTTPS Origin、Subprotocol、当前账号状态与 Permission Version。User Ticket 只能订阅本人会话并以 `sender_type=user` 发送；Support Ticket 只能订阅当前 Queue/Assignment 会话并以 `sender_type=human` 发送，不能由 Frame 覆盖 Sender/Audience。Ticket 出现在接入日志时必须脱敏。连接建立后发送 `connection.ready`，包含 `connection_id、heartbeat_interval_seconds、server_time`。

所有 Frame 使用统一 Envelope：

```json
{
  "type": "message.send",
  "event_id": null,
  "request_id": "req_01K...",
  "conversation_id": "conv_01K...",
  "occurred_at": "2026-08-21T09:00:00.000Z",
  "payload": {}
}
```

用户端可发送 `conversation.subscribe/unsubscribe、message.send、message.read、typing.start/stop、ping`；客服端使用对应的 `support.conversation.subscribe/unsubscribe、support.message.send、support.message.read、support.typing.start/stop、ping`。两套 Frame 可共享 Envelope 结构，但使用不同 Schema/Handler/Operation Code，不按运行时 Token 类型在一个 Handler 中切换权限语义：

- `conversation.subscribe/unsubscribe` 或 `support.conversation.subscribe/unsubscribe`：订阅或取消当前有权会话；服务端不接受通配订阅。
- `message.send` 与用户 REST Schema 一致；`support.message.send` 与 `/support/conversations/{conversation_id}/messages` Schema 一致；两者都使用 `client_message_id`，高可靠发送也可只走 REST。
- `message.read` / `support.message.read`：分别单调推进用户或客服 Reader Cursor；用户端只能在页面可见、目标消息已渲染且消息气泡至少 50% 连续可见 500ms 后发送，服务端返回该会话和全局的绝对未读数及 `cursor_version`，客户端不得自行做减法。
- `typing.start/typing.stop` 与 `support.typing.start/typing.stop`：短期临时事件，限频、不持久化、超时自动清除。
- `ping`：应用层保活和延迟测量。

服务端可发送 `request.ack、request.error、message.created、message.updated、conversation.updated、unread.updated、human_service.updated、approval.updated、file.updated、pong`。`request.ack` 只在 MySQL 事务成功提交后发送；若结果未知返回可查询的 Request/Message ID，客户端不能换 `client_message_id` 盲目重发。每个持久事件带可恢复 Event ID，重连后客户端使用 REST `messages?after_cursor=` 与会话列表同步缺口，WebSocket 本身不承诺无限历史重放。

协议要求：单 Frame 大小、发送频率和订阅数有硬上限；慢消费者先丢弃 Typing 等非关键事件，再关闭连接并要求增量同步；使用 Ping/Pong 检测半开连接；同一账号/设备连接数限额；部署时通过 Redis Pub/Sub/Streams 或专用 Broker 跨实例分发，但数据库提交记录始终是真相。消息内容同样经过鉴权、内容安全、文件与资源作用域验证，不能因使用 WebSocket 绕过 REST 规则。

#### 3.12.25 第三方 Webhook 接口

首版入站 Webhook：

| 方法 | 路径 | 调用方 | 用途与关键规则 |
| --- | --- | --- | --- |
| `POST` | `/webhooks/payments/{provider}` | 支付渠道 | 支付、关闭和退款结果通知；最高安全等级 |
| `POST` | `/webhooks/logistics/{provider}` | 物流聚合商/承运商 | 运单状态与轨迹通知；按运单和外部事件去重 |
| `POST` | `/webhooks/object-storage/{provider}` | 对象存储/安全扫描服务 | 上传完成、病毒扫描或异步处理结果；只更新受控文件状态 |

Webhook 路由不使用用户 Bearer Token，必须对每个 Provider 实现独立 Adapter，并遵守以下顺序：

1. 读取受限大小的原始 Body，保留用于验签的原始字节；解析 JSON 前验证方法、Content Type 和尺寸。
2. 使用配置中心中的当前/轮换密钥或渠道证书验证签名，同时校验 Timestamp 允许偏差、Nonce/事件 ID、商户/租户标识；IP Allowlist 只能作为辅助，不替代签名。
3. 将外部 Event Type 映射为内部事件，验证订单/支付/运单归属、金额、币种和不可变量。未知事件安全记录并按渠道要求应答，不猜测业务状态。
4. 以 `(provider, provider_event_id)` 或渠道等价唯一键落回调记录；相同 Payload 重放返回首次处理结果，不重复执行；同 ID 不同摘要进入安全告警。
5. 在短事务内保存已验证事件/收件箱记录，需要同步推进时使用状态机条件更新并写 Outbox；随后尽快返回 Provider 要求的 2xx/确认文本。
6. 慢任务、通知、物流刷新和向量同步全部异步执行。处理失败按可重试性退避，超过上限进入 Dead Letter 并告警；人工重放仍使用原事件幂等键。

Webhook 响应不返回内部堆栈、订单详情或验签差异。失败是否返回 4xx/5xx 必须按渠道重试契约适配：永久无效签名拒绝，已可靠持久化但业务异步中的事件通常返回成功以阻止风暴。原始报文若含敏感数据必须加密/脱敏并按最短审计期限保存；日志只记录摘要、Provider Event ID、处理状态、Request ID 和 Trace ID。

出站 Webhook 如后续开放给企业商家，必须另行设计 Endpoint 验证、签名 Secret 轮换、事件订阅、重放保护、重试与停用机制，不能复用入站 Provider Secret，也不能让商家自定义内网/云元数据地址造成 SSRF。

---

### 3.13 身份认证、权限与安全

本节采用“身份认证、会话连续性、权限决策、业务不变式”四层安全模型。通过 Token 验证只证明请求来自某个当前会话，不证明该用户有权访问目标资源，也不证明当前业务状态允许执行操作。所有 HTTP、WebSocket、Worker、Agent、MCP 和管理后台入口最终调用同一组授权与领域校验组件，不能为 AI 或内部服务保留绕过路径。

安全默认原则：默认拒绝、最小权限、短期凭证、服务端权威、纵深防御、敏感操作重新认证、所有副作用可审计。生产环境的 Token 密钥、字段加密密钥、Webhook Secret、支付证书和 MCP 凭证统一存入 Secret Manager/KMS，不写源码、镜像、数据库普通配置、日志或 Agent 上下文。

#### 3.13.1 登录认证方案

首版支持“账号 + 密码”和“手机/邮箱验证码”两种用户登录方式；管理员必须使用独立管理入口的账号密码 + MFA，推荐优先接入 WebAuthn/Passkey 等抗钓鱼认证器。第三方 OAuth/OIDC 登录作为可选扩展，使用 Authorization Code + PKCE、精确 Redirect URI 和 `state/nonce` 校验，不采用 Implicit Flow。

##### 3.13.1.1 用户注册安全

注册由“权威配置 → 目标所有权验证 → 核心事务 → 建立 Session”组成，不能让 Vue 页面自行决定用户名、密码、协议或角色策略：

1. `GET /auth/registration-config` 只投影已发布 Username/Password/Verification Policy 和当前地区/语言必须接受的法务 Content Version；配置带版本，注册提交时重验。
2. 验证码发送前执行 IP、Target Monitoring Hash、设备、渠道和风险多维限流。无论目标是否已绑定，匿名响应状态、Payload 形状和耗时保持近似；实际渠道发送与不存在账号的安全抑制不能被响应区分。
3. 注册验证码只证明当前请求者控制该手机号/邮箱，不证明用户名可用、不授予任何已有账号访问权，也不能用于登录、重置密码或联系方式换绑。
4. 用户名先做确定性 Unicode 规范化、保留词/仿冒/内容安全和长度检查，再以数据库唯一约束处理并发。用户名可公开不表示允许无限 Availability 枚举，相关接口仍需限流和异常扫描。
5. 密码策略遵循 3.13.11；在 Argon2id 哈希完成前，密码只存在于当前请求受控内存，异常、APM、审计、队列和 Outbox 均不得包含原文。泄露密码检查优先使用本地/隐私保护查询，不向第三方发送完整密码。
6. Required Agreement 必须逐项主动接受。服务端按 `document_type + document_version + locale + region` 重新加载 Published 不可变版本和 Hash；缺失、撤回、未生效或版本变化一律拒绝，不从旧勾选推定接受新版。
7. 客户端不得传 Role/Permission/Store/Admin 字段；注册用例固定授予普通 User Role。邮箱/手机号、密码凭证、协议接受记录、默认角色、专属客服会话、Session 和 `UserRegistered` Outbox 的原子范围以 3.14.1 为准。
8. 防自动化挑战只能作为纵深措施，不替代限流、唯一约束和审计；挑战 Token 绑定 Purpose、匿名 Flow、Target Hash、Client、过期时间和单次使用，不能跨注册/登录复用。

##### 3.13.1.2 用户登录安全

登录流程：

```text
规范化登录标识
  → 账号/IP/设备限流与风险预检
  → 常量时间验证密码或原子消费验证码
  → 检查用户、凭证和管理员主体状态
  → 必要时完成 MFA/风险挑战
  → 创建 auth_sessions 与 Token Family
  → 签发短期 Access Token + 轮换型 Refresh Token
  → 更新 last_login_at、写安全审计和登录通知
```

账号不存在、密码错误、凭证被禁用对匿名调用方统一返回 `AUTH_INVALID_CREDENTIALS`，避免账号枚举。密码路径即使账号不存在也执行成本受控的 Dummy Argon2id 验证，使响应时序不形成明显存在性旁路；Dummy Hash 参数与当前策略同步，但必须通过舱壁和限流防止被用作 CPU DoS。验证码发送接口也返回一致外观和近似耗时。风险判断可以增加验证步骤、拒绝高危登录或通知用户，但不能只依赖可伪造的 User-Agent/IP，也不能把“新设备”自动当作恶意。

认证成功必须创建全新 Session ID、Token Family、CSRF Token 和 Refresh Cookie，忽略并撤销匿名预会话中可由攻击者固定的标识，防止 Session Fixation。登录响应设置 `Cache-Control: no-store`；Access Token 只返回 User Audience，不能根据账号拥有后台角色自动升级为 Admin Audience。管理身份必须重新经过独立管理端登录/MFA。

登录与注册页面的 `redirect` 由前端先做体验过滤，后端/网关仍必须使用同一站内 Route Allowlist 验证；只允许 User Audience 的相对路径。认证页面校验 HTTPS Origin/Fetch Metadata，并为登录、注册、验证码和挑战接口设置严格 CORS；这可降低 Login CSRF 和跨站滥用，但不以 Origin 代替凭证验证。认证失败只记录 3.7.1.10 的 HMAC/低基数安全事件，不把输入标识或错误细节写入普通日志。

#### 3.13.2 Access Token 与 Refresh Session

Access Token 推荐使用非对称签名 JWT（如 EdDSA/ES256，具体算法经 ADR 锁定），有效期默认 10–15 分钟；后端只接受显式允许的算法和当前/轮换公钥，拒绝 `alg=none`、算法混淆和未知 `kid`。Claims 最少包含：

| Claim | 含义与校验 |
| :--- | :--- |
| `iss/aud` | 固定签发者与目标 API；每次严格匹配 |
| `sub` | 用户公开 ID，不使用数据库自增 ID |
| `sid` | `auth_sessions.session_no`，用于撤销与设备会话定位 |
| `jti` | Token 唯一 ID，用于高风险事件追踪/短期 Deny List |
| `iat/nbf/exp` | 签发、生效、过期时间；只允许很小的时钟偏差 |
| `client_type` | `web/mobile/admin`，防止管理 Token 用于错误入口 |
| `auth_time/amr` | 最近认证时间和认证方法，用于二次认证判断 |
| `credential_version` | 密码重置/账号安全事件后使旧 Token 失效 |
| `permission_version` | 权限/店铺数据范围变更后识别旧授权快照 |

Access Token 不承载地址、手机号等个人信息，不把长期完整权限列表作为唯一事实。管理员角色/权限和店铺数据范围仍从短 TTL 授权缓存/数据库校验，并使用 `permission_version` 快速失效。

Refresh Token 使用至少 256 bit CSPRNG 随机 opaque 值，只向客户端展示一次；数据库只保存其 HMAC/哈希，不保存明文。每次刷新在事务中原子消费旧 Session、创建子 Session、轮换 Refresh Token，并保留 `token_family_no` 关系。旧 Token 再次出现视为可能泄露，撤销整个 Family、清理缓存并要求重新登录。Refresh Session 设置绝对有效期和闲置有效期，不做永不过期滑动续期。

#### 3.13.3 Cookie、CSRF 与 CORS

浏览器推荐：Access Token 仅保存在内存；Refresh Token 放 `HttpOnly; Secure; SameSite=Lax/Strict; Path=/api/v1/auth` Cookie。不得存入 Local Storage、可被第三方脚本读取的 IndexedDB 或 URL。若业务必须跨站使用 `SameSite=None`，必须同时启用 `Secure` 并提高 CSRF 防护等级。

所有依赖 Cookie 的状态变更端点采用 Signed Double-Submit 或服务端 Session 绑定 CSRF Token：Token 至少绑定当前 Session，客户端通过自定义 Header 提交；同时验证 `Origin`，缺失时按策略验证 `Referer`。`SameSite` 只是附加防线，不取代 CSRF Token。GET/HEAD/OPTIONS 不执行副作用。

CORS 使用环境级 Origin Allowlist，生产不允许 `*` 搭配 Credentials，不反射任意 Origin；只开放必要 Method/Header，合理设置 Preflight Cache，并加 `Vary: Origin`。WebSocket 握手单独验证 Origin。服务端只信任由受信反向代理清洗后写入的 Forwarded Header。

#### 3.13.4 Token 撤销与强制下线

MySQL `auth_sessions` 是会话撤销事实，Redis 是快速判断层：

- 用户退出：撤销当前 Session；“退出所有设备”撤销该用户其他 Session。
- 密码修改/重置、账号业务冻结/注销、管理员权限降级、Refresh 重放：按策略撤销当前 Family 或全部 Session。冻结同事务写 `user_status_records`、更新 `users` 投影并发出 Session/WebSocket 撤销事件；定时解冻不自动恢复任何已撤销 Session。
- Access Token 短生命周期自然到期；高风险事件将 `sid/jti/user credential_version` 写 Redis Deny/Version Cache 立即阻断。
- Redis 缺失/故障时，高风险写与管理接口回源 MySQL；不能把缓存故障解释为“有效”。普通只读可按风险执行短暂降级。
- WebSocket 定期校验 Session Version；收到 `session.revoked` 广播后立即关闭对应连接。长时间 SSE 在心跳/事件边界复核会话。

撤销接口本身幂等，记录发起人、原因、范围和 Request/Trace ID。管理员强制下线必须具有独立权限和明确原因，并向用户发送不泄露内部风控细节的安全通知。

#### 3.13.5 用户权限校验

普通用户授权采用“身份 + 资源归属 + 资源状态 + 动作能力”而非简单角色判断。FastAPI Dependency 只完成 Token/Session 基础验证；Application/Domain 层继续校验：

```text
authenticated_user
  ∩ resource.user_id == authenticated_user.id
  ∩ resource is visible
  ∩ domain_state allows action
  ∩ risk/reauth/consent requirements satisfied
```

请求体中的 `user_id、owner_id、store_id` 不参与确定当前用户身份；能从 Token 或目标聚合推导的字段不允许客户端传入。对越权访问通常返回 404 防止 IDOR 枚举。批量接口逐项验证归属且设置数量上限，不能只校验列表第一项。`available_actions` 供 UI 展示，执行命令时仍重新校验。

#### 3.13.6 管理员 RBAC

RBAC 基于 `users → user_roles → roles → role_permissions → permissions`。Permission 严格遵循 3.7.2.2 的 `resource:action` 单动作规范，如 `orders:read`、`refunds:review`、`ai_tools:publish`；点号、斜杠合并和通配符均不是合法权限码。角色只聚合权限，路由和 Service 依赖 Permission，不写死“角色名称等于管理员”。

管理授权的完整判定为：

```text
Admin Audience Session 有效且达到要求的 Assurance
  ∩ Permission 显式允许且未被 Deny
  ∩ Platform/Store Data Scope 覆盖目标资源
  ∩ 近期认证仍有效（如操作要求）
  ∩ 职责分离/双人审批条件满足
  ∩ 目标资源版本与业务状态允许
```

用户端和管理端使用不同 `client_type/aud`、Cookie Path、CORS Origin、前端应用和路由依赖。生产 Admin Token 调用购物、加购、用户下单等消费者 Operation 必须返回 `AUTH_AUDIENCE_MISMATCH`；普通 Web Token 调用 `/admin` 或 `/support` 同样拒绝。测试购物通过独立 Sandbox 用户，不能给管理员生产 Session 附加消费者权限。

- 默认无权限；显式 Deny/账号冻结优先于 Allow。
- 平台、组织、店铺等 Data Scope 与 Permission 分开计算，`orders:read` 不自动表示全平台订单。
- 超级管理员不用于日常运营；关键配置发布、资金调整、权限提升和审计导出采用 MFA、近期认证与职责分离。
- MFA 认证器以 3.7.2.6 为事实；管理登录最低 Assurance、允许认证方法和近期认证窗口按风险策略版本化。恢复码单次消费，认证器重置使相关管理 Session 失效并触发安全通知。
- 修改角色、权限或店铺数据范围使用 ETag；用户角色以不可复用 `grant_no` 授予/撤销/到期，只有 `grant_status=active` 且未过期的行进入授权判定。每次变更追加 `user_role_events`、递增 `permission_version`并失效缓存；再次授权创建新 Grant，不覆盖旧历史。高风险降权同时撤销相关管理 Session。
- 禁止操作者授予自己未拥有/不可委派的权限，禁止删除最后一个可恢复系统的安全管理员。
- 命中职责分离的操作必须创建 `admin_approval_requests`；发起人不能审批，同一有效管理员账号只能占一个审批席位，管理账号必须一人一号且禁止共享。达到门槛只表示批准执行，Executor 仍须重新校验当前 Permission/Scope、审批人有效性、参数 Hash、资源 Version 和领域状态。
- 管理员审批与用户对 Agent 的 `ai_tool_approvals` 是两个不同信任域：前者不能由 Agent 创建或决定，后者不能满足管理员发布、退款审核、权限提升、敏感导出或死信重放的双人审批要求。
- 后台列表、详情、导出和失败的越权尝试均写管理员操作/安全审计。
- 临时查看敏感字段使用 MySQL `admin_sensitive_access_grants` 作权威，绑定管理员、Admin Session、字段集、目标资源、Purpose 和短 TTL；Redis 只作快速 Deny/Cache。首版 Grant 一次消费，读取时重新校验 `users:read_sensitive`、Scope、Session 和字段白名单；撤销/Session 失效/到期立即 Fail Closed。前端遮罩和水印不是权限控制，Grant 不可用于批量导出或跨目标/跨 Session 复用。
- 管理 Batch Job 在创建、确认、Worker 执行和产物下载各阶段重新校验身份状态、Permission、Scope 和资源版本。发起人权限被撤销后任务 Fail Closed，不能依赖创建时快照继续扩大访问。
- 管理菜单由 `/admin/navigation` 投影，但隐藏菜单不代替 API 授权；直接访问旧 URL 或复用旧响应时仍按当前 `permission_version` 处理。

#### 3.13.7 店铺数据隔离

店铺隔离采用共享库行级租户键方案。所有店铺域表必须可直接或经聚合可靠解析出 `store_id`；Repository 方法要求显式 `StoreScope`，禁止提供无范围的 `get_by_public_id()` 给店铺业务复用。

店铺员工访问同时满足：有效管理员/员工身份、目标 Permission、与目标 `store_id` 的有效成员关系、资源自身 `store_id` 一致、业务状态允许。店铺 ID 不从前端下拉框直接信任；平台管理员跨店访问也需权限、理由与审计。

缓存 Key、对象存储 Prefix、搜索索引、pgvector Knowledge Filter、Redis Stream Consumer 和导出文件均携店铺作用域。异步事件必须保存可信 `store_id` 快照，Consumer 不能从不可信 Payload 临时拼接查询。自动化测试必须包含跨店同名商品/相似 ID/伪造 Store Header 的负向用例。

#### 3.13.8 订单归属校验

用户订单操作以 `orders.user_id` 为最终归属；店铺处理以 `orders.store_id` 为数据范围。查询订单项、包裹、退款、评价时通过关联到订单再次验证，不因知道子资源公开 ID 就可访问。

订单详情、地址快照、物流和售后使用不同字段投影：用户可看自己的完整业务展示，店铺只在履约必要期看最小收货信息，物流适配器只获得发货所需字段，平台人工客服按工单理由受控查看。用户侧订单卡片的 `order_id` 只是导航引用，不授予会话中的 Agent/人工权限。

每个命令在锁定聚合后复核归属与状态，不能先鉴权后长时间排队再用旧结论执行。订单从未支付转为已支付、售后中或终态时，原先生成的 Approval/资格 Token 可因资源版本变化而失效。

#### 3.13.9 AI 工具权限校验

模型不能直接决定自己是否有权限。Tool Gateway 在每次调用前执行确定性授权：

```text
Tool/Agent/Skill 均处于已发布可用状态
  → Tool 已绑定到当前不可变 Agent/Skill Version
  → 显式 Deny 优先，命中有效 Permission Policy
  → Conversation Type 与 platform/store Scope 匹配
  → 当前用户/店铺/资源归属与业务状态校验
  → Consent 仍有效且覆盖所需数据用途
  → 参数满足字段、数量、金额、频率白名单
  → 所需 Action Approval 未过期、参数哈希一致且可消费
  → 使用派生 Service Identity 调用，记录 Action Audit
```

专属客服只能读取当前用户自己的跨店铺业务数据；店铺客服只能读取当前用户在当前店铺的数据。Tool 返回按 `result_policy` 裁剪和脱敏后再进入模型上下文。Tool Name、参数和资源 ID 即使来自模型，也全部按不可信输入验证。权限拒绝不能被模型“解释”后再次绕开。

#### 3.13.10 用户授权与二次确认

Consent 表达“允许某类数据在某用途和范围被 AI 持续使用”，Approval 表达“允许现在以确定参数执行一次具体动作”，两者不能互相代替。

- Consent 分为长期记忆、订单读取、个性化等独立选项，展示目的、数据范围、保留期、Policy Version 和撤销方式；默认不勾选。
- 撤销后立即停止新使用，清理热点上下文并发送长期记忆删除/失效事件；依法留存的授权证据和业务审计不伪装成可继续使用的记忆。
- 低风险只读操作可在有效 Consent 下执行；只有当前 Agent Version/Skill/Tool Allowlist 已明确开放的写操作才可生成结构化 Approval。首版专属客服仅开放确认后提交退款申请，修改地址、取消订单、确认收货、下单和支付只导航到权威页面，不生成 Approval。
- Approval 展示最终资源、数量、Money、影响与风险，一次使用、短期过期并绑定参数哈希和资源版本；自然语言“可以”不代替点击确认。
- 修改密码、支付、敏感资料导出、管理员高危操作还需近期认证/MFA。Agent 不收集支付密码、短信验证码或管理员 MFA Secret。

#### 3.13.11 密码安全

- 单因素密码最少 15 个字符，允许至少 64 个字符、空格和 Unicode；如未来密码只作为 MFA 的一部分，最低仍不得少于 8 个字符。
- 不强制“大小写 + 数字 + 特殊符号”组合，不定期强迫修改；检测已泄露/常用/上下文相关弱密码，确认泄露或高风险事件时强制重置。
- 允许 Password Manager、粘贴和浏览器自动填充；不使用安全问题/KBA，不截断密码。Unicode 如支持，哈希前采用明确且稳定的 NFC 归一化策略并在客户端提示。
- 使用 Argon2id 独立随机 Salt 哈希，参数通过目标生产硬件压测确定并版本化；登录成功时可渐进 Rehash。可选 Pepper 仅存 Secret Manager，不能与哈希同库。
- 密码比较使用常量时间；登录失败采用账号/IP/设备多维限流和渐进延迟，防止暴力破解但避免被攻击者轻易永久锁死账号。
- 密码不写日志、Trace、审计 Payload、异常、消息、Analytics 或模型上下文；前后端错误采集必须对相关字段强制 Redact。

#### 3.13.12 敏感字段加密

手机、邮箱、收货人、详细地址、IP、运单号、退款凭证引用等 L2/L3 字段使用应用层 Envelope Encryption：数据密钥执行 AEAD（如 AES-256-GCM），主密钥/KEK 在 KMS，密文保存 Nonce、Auth Tag、Key Version 和必要 AAD。AAD 绑定表/字段/业务主键，防止密文跨行替换。

需要精确查找/唯一约束的字段另存“规范化值的带密钥 HMAC”，不能使用无密钥 SHA 代替。展示用 Last4/掩码字段与密文分离，禁止为模糊搜索解密全表。解密集中在受审计 Service，按 Purpose 最小返回，不让 ORM 日志、Pydantic `repr` 和 Agent Trace 输出明文。

密钥按版本轮换：新写使用新版本，读兼容旧版本，后台 Worker 小批量重加密；轮换前验证备份和回滚。删除密钥属于高破坏操作，必须在保留义务、备份和所有密文迁移完成后双人审批。

#### 3.13.13 接口限流

网关做连接/粗粒度限制，FastAPI/Redis 做身份和业务语义限制。使用 Redis Lua/原子算法实现 Token Bucket 或 Sliding Window，Key 至少区分环境、Operation、IP/用户/Session/店铺/Agent，TTL 覆盖窗口。

| 接口类别 | 主要维度 | 策略重点 |
| :--- | :--- | :--- |
| 登录/验证码/重置 | IP + 标识 HMAC + 设备 | 严格发送/尝试次数、冷却与风险挑战；响应防账号枚举 |
| 搜索/商品读取 | IP + 用户 | 较宽限额，防爬和昂贵组合查询 |
| 下单/支付/退款 | 用户 + 订单/交易 | 低频 + 幂等，限流不能替代状态机 |
| 消息/WebSocket | 用户 + 会话 + 连接 | 消息速率、Frame、连接和订阅数限制 |
| Agent/Tool/RAG | 用户 + Agent + Tool + Token/成本 | 并发、步骤、模型 Token、工具次数和日配额 |
| 管理/导出/重放 | 管理员 + 组织 + Operation | 极低频、MFA/审批和审计 |

超限返回 429、稳定错误码和 `Retry-After`，不泄露其他用户配额。Redis 故障时登录/验证码/高风险写 Fail Closed 或进入更严格本地限额；公开只读可限速降级，绝不能无界放行昂贵 Agent/搜索请求。

#### 3.13.14 防重放与签名验证

防重放分层实现：TLS 防窃听；短期 Token 和 `jti/sid` 限制凭证重用窗口；Refresh Rotation 检测 Token 重放；业务写使用 Idempotency Key；Webhook/MCP/内部服务签名使用 Timestamp + Nonce/Event ID + Body Digest。

签名验证必须基于原始请求字节和规范文档指定的 Canonical String，先验证算法/Key ID/证书链与允许时间偏差，再用常量时间比较 MAC/签名，最后原子占用 Nonce/Event ID。Nonce Store TTL 至少覆盖签名时间窗口。相同事件相同摘要按幂等返回；相同事件 ID 不同摘要拒绝并告警。

禁止自创弱签名（参数排序后简单 MD5、Secret 拼接 Hash）、禁止接受多个歧义 Canonical 版本、禁止在失败日志输出待签名全文或 Secret。服务间调用优先 mTLS + 短期、Audience-Bound Service Token；来自公网的 `X-User-ID/X-Store-ID` 等身份 Header 必须由网关移除并重建。

#### 3.13.15 支付回调安全

每个支付 Provider 使用独立 Adapter 和商户配置，按渠道官方算法验证原始 Body/Header、证书序列号/Key ID、Timestamp、Nonce 和签名。证书/公钥支持重叠轮换，未知或过期 Key 触发安全告警而非临时关闭验签。

验签后仍须验证 `merchant_id/app_id、provider_trade_no、trade_order_id、amount、currency` 与本地支付单完全匹配。回调只允许经支付状态机前进；重复、乱序、成功后失败不能直接覆盖。`payment_callbacks` 先以 Provider Event ID 去重，支付、订单、库存确认和 Outbox 在受控事务中处理。

前端 Return URL 不表示付款成功；以验签回调或主动查询对账为准。回调入口独立限流和 Body 上限，IP Allowlist 只是辅助。完整卡号、CVV、支付密码不进入本系统；优先使用合规渠道托管页面/SDK，缩小 PCI 数据范围。

#### 3.13.16 文件上传安全

文件从 `uploading → scanning → active/rejected`，只有 Active 文件能绑定业务：

- 按 Purpose 设置扩展名、实际 MIME、Magic Bytes、大小、数量、像素/时长/页数白名单；不信客户端 Content-Type/文件名。
- Object Key 由服务端随机生成，原文件名仅作限长展示元数据并转义；禁止路径穿越、覆盖和用户指定 Bucket/Key。
- 临时文件进入隔离私有 Bucket，不可执行、不可公开读；病毒扫描、图像重编码，适用时做 CDR。压缩包/文档解析在无凭证、无网络、CPU/内存/时间/解压比受限的 Sandbox。
- Complete 时校验对象大小与 Checksum；业务绑定时再次验证 Owner、Purpose、Status 和资源作用域，防止换 ID 引用他人文件。
- 下载使用鉴权 API 或极短 Presigned URL，设置安全 Content-Type、`Content-Disposition`、`nosniff` 和独立静态域；SVG/HTML 等 Active Content 默认不允许用户直接公开展示。
- 扫描器故障时保持 Pending，不得“超时自动通过”。恶意文件隔离、告警并按最短排障期限清除。

商品详情/FAQ 图片只允许安全扫描通过、归属当前 Store/Product 且 Purpose 匹配的 Raster Image；上传 SVG 即使 MIME 合法也不得作为可内联商品富文本发布。发布 Sanitizer 把业务文件 ID 转成受控图片 CDN URL并固化宽高/Alt，不接受商家 HTML 中的任意 `src/srcset`。浏览器端 `SafeRichContent`、CSP、Trusted Types 和 `nosniff` 是纵深防御，不能替代服务端写入/发布时的 Sanitization。

#### 3.13.17 Prompt Injection 防护

Prompt Injection 包括用户直接注入，也包括商品描述、评价、网页、知识文档、工具结果和其他 Agent 消息中的间接注入。无法仅靠一条 System Prompt 或关键字过滤彻底解决，必须把模型视为不可信决策建议者。

防护层：

1. 指令与数据分离：System/Skill 指令由已发布版本加载；外部内容使用结构化字段、来源标签和不可信边界包裹，明确其中的命令无授权效力。
2. 检索隔离：RAG 强制平台/店铺/用户 ACL Filter，只索引扫描与审核通过内容；引用返回来源，限制单来源占比、长度和递归抓取。
3. 最小能力：Agent 只看完成当前任务的 Tool，读写分离，默认只读；Tool Gateway 独立做权限、Schema、状态和参数约束。
4. 输出不执行：模型文本不能直接成为 SQL、Shell、模板、URL 或 Tool 调用；结构化输出经严格 Schema、Allowlist 和业务重建。
5. 高风险确认：涉及订单、地址、售后等副作用使用参数绑定 Approval；付款、资金退款和管理员审批不交给普通 Agent。
6. 泄露防护：Prompt、Tool Result、Memory 和输出执行 Secret/PII Redaction；禁止让模型复述 System Prompt、内部策略、其他租户内容和隐藏推理。
7. 检测与响应：对越权指令、敏感数据探测、异常工具链和重复拒绝做风险评分、限流、隔离/转人工和审计；保留安全评估样本。

内容安全分类器是辅助信号，误报/漏报时不能成为唯一授权机制。即使模型被完全注入，确定性授权层也应保证它无法跨用户、跨店铺或执行未经确认的副作用。

商品描述与 FAQ 采用“双输出、双边界”：浏览器只获得 3.7.4.11/12 的 `safe_blocks/safe_html/safe_text` 发布版本，阻断 XSS；Agent/RAG 只获得同版本 `safe_text`，并标注 `source_type=product_content、trust_level=untrusted_data、store_no、product_no、content_version、content_hash`，阻断把自然语言当系统指令。HTML Sanitizer 不负责判断 Prompt Injection，Prompt 检测也不负责 HTML 安全；两条流水线必须各自通过。命中隐藏指令、数据外带或工具诱导时可从 AI 索引隔离该版本，但页面仍只能按浏览器安全策略决定是否展示，二者状态和原因分别审计。

#### 3.13.18 MCP Server 安全

只连接管理员注册、审核和版本锁定的 MCP Server；运行时不接受用户/模型提供任意 Server URL。平台内置高风险业务优先使用本地 Tool Adapter，外部 MCP 通过独立 Gateway、网络出口代理和 Service Identity 隔离。

- Streamable HTTP 仅使用 HTTPS，校验 Origin；远程授权采用当前 MCP 规范与 OAuth 安全实践，Token 必须绑定 Audience/Resource 和最小 Scope。
- MCP Server 必须验证 Token 确实签发给自己；禁止把客户端 Access Token 原样透传给下游，访问下游时使用独立授权，防止 Confused Deputy。
- STDIO Server 运行在非特权容器/进程，使用只读文件系统、资源限额、最小环境变量和显式挂载；不继承整个后端 Secret/网络权限。
- 出站 DNS/IP/Port Allowlist，解析后再次校验并阻断 Loopback、私网、Link-Local、云元数据地址、重定向跳转和 DNS Rebinding，防止 SSRF。
- 固定 Server/Tool Version、Schema Hash、Capability 和超时/输出上限；Tool List 动态变化需重新审核，未知 Tool 默认拒绝。
- 工具输入输出视为不可信；执行 Schema 校验、大小限制、脱敏和内容标记。MCP 日志不得包含 Token、Elicitation Secret 或完整敏感结果。
- 连接失败使用断路器和有界重试；外部写结果未知时对账，不盲目重放。紧急 Kill Switch 可按 Server/Tool/Agent Version 立即禁用。

#### 3.13.19 操作审计与追溯

审计覆盖登录/失败/刷新/撤销、密码与 MFA 变更、RBAC 和店铺成员变更、敏感字段临时查看/导出、商品/库存/订单/发货/支付/退款命令、管理员审批申请/决定/执行、评价治理、平台内容审核/发布、批量任务创建/确认/下载、文件下载、Agent/Skill/Tool 发布、Consent/Approval、每次 Tool 尝试、人工接管/内部备注访问和死信预览/重放。

统一审计字段：`event_no、event_type、actor_type/actor_no、effective_subject_no、resource_type/resource_no、store_scope、action、result、reason_code、occurred_at、request_id、trace_id、session_no、source_ip_hash、user_agent_hash、policy/version、before/after 摘要`。敏感值只保存白名单脱敏快照/Hash，不保存密码、Token、验证码、密钥、完整 Prompt 或隐藏推理。

业务状态流水、管理员操作日志、AI Tool Audit 和系统安全日志职责分离但通过 Trace/Event/Resource ID 关联。审计记录 Append-Only，写入失败时高风险操作 Fail Closed；日志集中存储、访问受控、传输/静态加密、设置完整性保护与分级保留期。管理端审计查看和导出本身产生新的审计事件；平台内容版本、批处理输入 Hash/结果、内部备注访问和重放 Preview ID 均可关联追溯，但审计中不复制正文或文件。时间同步异常、审计写入失败、同一资源异常高频失败和未知外部副作用必须告警。提供按 Request ID → Outbox Event → Worker → Agent Run → Tool Action → 业务资源的追踪链，但任何查询仍受 RBAC 和数据范围约束。

---

### 3.14 核心业务流程与状态机

核心交易使用“聚合内强一致、聚合间事件驱动最终一致”。MySQL 单事务只修改有明确一致性边界的主表、明细、状态流水、幂等记录和 Outbox；支付渠道、物流、通知、Redis、搜索和向量库不加入分布式事务。所有流程必须定义：前置条件、锁定顺序、状态转换、幂等键、事务内写入、事务后事件、失败补偿和审计。

状态不能由 Router/Worker 随意赋值。Domain State Machine 暴露命令方法，内部执行条件更新或行锁，并返回 Domain Event；Repository 不提供通用 `update_status(new_status)`。图中 `→` 只表示允许方向，实际转换还需满足角色、金额、数量、时限和资源版本等 Guard。

#### 3.14.1 用户注册登录流程

注册验证码发送：

```text
读取 Registration Config/Policy Version
  → 规范化 Target（phone/email），计算 Credential HMAC 与 Monitoring HMAC
  → IP + Target + Device + Channel 限流/风险判断
  → 必要时完成可访问挑战
  → 创建 verification_codes（只存 Code Hash/Purpose/Target Hash/TTL）
  → 事务外调用短信/邮件 Provider
  → 更新 delivery_status，向匿名端始终返回统一 202 投影
```

注册提交：

```text
接收 Idempotency-Key + RegistrationRequest
  → 锁定/读取 Idempotency Record；相同 Key 不同 Payload 立即冲突
  → 校验 config_version、Username/Password Policy 和 Required Agreements
  → 锁定 verification_codes
  → 校验 Purpose=register、Target Type/Hash、TTL、Attempts、未消费/未失效
  → 常量时间校验验证码并增加失败尝试或准备消费
  → 规范化用户名/手机号/邮箱，预检查冲突
  → Argon2id 哈希密码（进入数据库事务前完成）
  → Transaction:
       users(active)
       + user_credentials(password active)
       + user_credentials(phone/email active + verified)
       + user_agreement_acceptances(required versions)
       + user_roles(default user Role)
       + conversations(exclusive, unique user)
       + verification_codes.consumed_at
       + auth_sessions(new User Audience family)
       + idempotency_records(completed user/session reference，不含 Token 明文)
       + outbox_events(UserRegistered)
  → Commit
  → 签发短期 Access Token、设置 Refresh/CSRF Cookie
  → 异步欢迎通知/安全通知/非关键初始化
```

密码哈希属于 CPU 密集步骤，在受限线程池/Worker 舱壁中执行，不长时间占用 Event Loop 或数据库行锁；但生成的 Hash 仅在当前请求内存保留到事务完成。用户标识与凭证活跃标识唯一约束是并发最终裁判；冲突时整个事务回滚，验证码是否继续可用由尝试/风险策略决定，不产生半个账号。专属客服会话以唯一约束保证一个用户一条；在同一注册事务内新用户不应发生正常冲突，异常冲突必须回滚并告警，不能静默关联其他用户会话。

协议 Version 在验证码发送后可能变化，因此只以注册提交事务内重新读取的 Published/Effective 版本为准；版本不匹配在消费验证码和创建用户之前返回 `AGREEMENT_VERSION_CHANGED`。短信/邮件发送、欢迎通知、风控外部查询不得放在持有核心数据库锁的事务内。欢迎通知失败不回滚注册，Outbox 负责重试。

密码登录：

```text
规范化 Identifier，计算查找/监控 Hash
  → 多维限流与风险预检
  → 查询用户与 Password Credential；不存在时执行 Dummy Hash
  → 常量时间验证 Argon2id
  → 凭证正确后检查 User/Credential Status、must_change_password
  → 必要时校验单次 Challenge
  → Transaction:
       创建 auth_sessions + Token Family
       + users.last_login_at
       + 重置成功凭证 failed_attempts/locked_until
       + auth_attempts(succeeded)
       + security Outbox（新设备/异常登录按策略）
  → Commit → 签发 Access/Refresh/CSRF → 返回 User Session DTO
```

验证码登录先以 `purpose=login` 发送验证码，提交时锁定 Verification Record 并匹配 Target；消费验证码、创建 Session、更新 Last Login、写 Auth Attempt/Outbox 在同一事务完成。验证码正确但 Target 未绑定任何 Active Credential 时对匿名端仍映射为 `AUTH_INVALID_CREDENTIALS`，不自动注册新用户；“验证码登录即注册”不在首版范围内。

失败和并发处理：

| 场景 | 服务端处理 |
| :--- | :--- |
| 密码/验证码错误 | 原子增加凭证或 Verification Attempt；达到阈值后设置短时锁定/失效，写 `auth_attempts`；匿名响应保持模糊 |
| 账号业务冻结/关闭 | 仅在凭证正确后返回安全 `AUTH_ACCOUNT_UNAVAILABLE`；不创建 Session |
| 并发注册同一用户名/Target | 数据库唯一约束只允许一个事务成功；其他请求回滚并返回稳定冲突，不关联成功用户 ID |
| 相同注册幂等 Key 重放 | Processing 返回 409 + `Retry-After`；Completed 返回同一 `user_id` 且不重复验证码消费、协议、角色或会话初始化。若需恢复登录态，必须在再次校验相同 Payload Hash 与客户端绑定后撤销原 Bootstrap Session、创建替代 Session 并轮换认证材料；不能保存/重放 Refresh Token 明文 |
| 登录响应在网络中丢失 | 已创建 Session 保持可见；客户端不自动重发密码，用户重试可创建新 Session，旧同设备 Session 可由策略合并/撤销并在安全页可见 |
| Session/Token 签发前 Commit 失败 | 不返回 Token/Cookie；清理请求内 Token 材料，按同一注册 Key安全重试 |
| Commit 成功但响应中断 | 注册重放通过 Idempotency Record 查到成功账号，并按上一行的固定 Bootstrap Session Replacement 策略恢复登录态；超过恢复窗口则返回“账号已创建，请登录”，不得再接受已消费验证码创建会话 |
| 审计/高风险安全事件无法可靠写入 | 认证/注册 Fail Closed；普通异步欢迎通知失败仅进入 Outbox 重试 |

登录按 3.13.1 的身份/安全契约执行；登录和 Token Refresh 不自动创建重复角色、地址、协议接受记录或会话。Refresh Token 重放触发 Family 撤销流程，而不是简单返回一个新 Token。所有分支使用同一 Clock、规范化库和 Reason Registry，Router 不复制用户名、验证码、锁定或协议版本规则。

#### 3.14.2 商品发布流程

```text
draft（商家编辑）
  → pending_review（提交审核并冻结待审版本）
      ├→ rejected（驳回原因 + 回到 draft 修改后重新提交）
      └→ on_sale（审核通过且满足上架条件）
on_sale → off_shelf（商家下架/平台治理/经营状态变化）
off_shelf → pending_review → on_sale（影响审核内容发生变化时重新审核）
draft/rejected/off_shelf → archived（停止经营，逻辑归档）
```

首版直接采用 3.7.4.3 已定义的 `draft/pending_review/on_sale/off_shelf/rejected/archived`，不再引入 `submitted/reviewing/approved/deleted` 等第二套主状态。审核领取、开始处理、批准等细粒度过程写 `product_status_logs.event_type` 和审核任务，不扩张主状态；如未来确需独立 Moderation 状态，必须先通过数据库迁移和 API 版本兼容方案正式引入。

发布命令校验店铺有效、分类/品牌、标题/详情内容安全、主图 Active、至少一个启用 SKU、SKU 价格/规格唯一和服务政策。Transaction 更新版本与状态、追加 `product_status_logs`、写 Outbox；提交后失效商品/店铺缓存并更新搜索/向量知识。搜索同步失败不回滚主库发布，通过 Outbox 重试；前端商品详情始终以 MySQL/API 状态为准。

#### 3.14.3 SKU 与库存更新流程

商品内容、SKU 售卖定义和库存数量是不同权限/事务：

- SKU 变更：店铺员工校验商品作用域与 ETag；已产生订单的 SKU 不物理删除，只禁用。规格组合有规范化唯一键，价格为整数最小货币单位。
- 库存入库/盘点：调用库存 Domain Command，必须给出数量、原因、业务引用和幂等键；不允许直接编辑 `on_hand_quantity/reserved_quantity`。
- 调整时锁定 `inventories`，验证调整后 `on_hand >= reserved + safety_stock`；写主表、`inventory_logs` 和 Outbox。若盘亏会破坏现有预占，拒绝普通调整并进入异常处理。
- 多 SKU 批量更新按 `sku_id ASC` 锁定，限制批量大小；任一失败按接口语义整批回滚或返回逐项结果，不能未声明部分成功。
- Redis/搜索库存展示异步失效；下单永远以 MySQL 条件更新为准。

管理员冻结库存只阻止新预占，不无痕释放已有预占；已有未支付订单由独立风控/取消流程处理。任何库存修正都必须能由流水重放前后值并追到 Actor/Reference。

#### 3.14.4 加入购物车流程

```text
鉴权 → 校验 Idempotency-Key → 读取/幂等创建用户唯一永久 Cart（`cart_status=active`）
  → 校验 Product/SKU 可见且属于有效店铺
  → 锁定目标 Cart Item（不存在则准备插入）
  → 计算 existing_quantity + requested_quantity
  → 校验单 SKU/购物车上限
  → 原子 UPSERT 增量数量，记录当前展示价与 sku_version
  → 更新 item_count/version/last_activity_at
  → 保存幂等结果并提交 → 删除/更新 Redis Cart Cache → 返回按店铺聚合购物车
```

商品页的“加入购物车”使用 `POST` 增量语义，依靠 Idempotency-Key 防止网络重试重复累加，首次购物车写入不要求 ETag。购物车页的加减、输入数量使用 `PATCH` 最终值语义并携 `If-Match`，防止并发标签页覆盖。两种语义不得共用同一个端点或请求模型。加入购物车不预占库存、不冻结价格；下架/库存不足商品可留在购物车并标记 `invalid_reason` 供用户处理，但不能进入有效结算项。

#### 3.14.5 结算预览流程

结算输入分为 `buy_now` 恰好一个明确 SKU/数量，和 `cart` 的 1–100 个选中 Cart Item；两者进入同一 Checkout Service：

1. 校验输入归属、商品/SKU/店铺状态、购买限制和地址归属。
2. 批量读取权威价格、库存展示、店铺政策、商品履约资料和运费模板；不锁库存。
3. 使用版本化 Pricing Engine 计算各店铺小计、配送选项、运费和总应付，校验 Money 汇总不变式。
4. 生成 `checkout_sessions(active)` 与不可变 `checkout_snapshots`，记录 Pricing/Policy/SKU Version、Hash 和较短 `expires_at`。
5. 返回 `warnings、blocking_issues、available_actions`。存在 Blocking Issue 时不允许创建订单。
6. 用户更改地址或配送方式时使用 If-Match 更新会话并追加新 Snapshot，不覆盖旧快照。

结算页面显示“以提交订单时为准”。价格降低可按策略自动采用并提示；价格升高或运费增加必须返回差异让用户再次确认。Checkout 过期/已提交不可复活，重新创建新会话。

用户点击某店铺组 [联系客服] 时不创建订单，也不把整个 Checkout 发给模型。Conversation Service 校验 Checkout User/Active/Version/Expiry 与 Store Group 后，生成该店唯一会话的短期 `checkout_store_group` Context；重新计价原子提升 Checkout Version，使旧 Context 校验失败并经事件转为 Inactive，订单创建消费/过期/放弃则转 Expired。事件是清理加速，Tool Gateway 的同步版本/状态校验才是授权最终防线。

#### 3.14.6 创建订单流程

`POST /orders` 是强一致交易核心，使用 Idempotency Key 和 Checkout 唯一约束：

```text
鉴权与 Idempotency Claim
  → 锁定 checkout_session，校验 user/active/not_expired/version
  → 重新读取并锁定必要商品/SKU/库存（sku_id ASC）
  → 重新计价；若金额/政策发生需确认变化则中止并返回新 Snapshot
  → 锁定/条件更新库存并创建 Reservations
  → 创建 trade_orders(pending_payment)，固化 checkout_no、snapshot_hash 与 order_source
  → 按 store_id 拆 orders(pending_payment) 与 order_items；Order 和每个 Order Item 的 adjustment_amount 初始均为 0
  → 写 order_addresses、policy/product/shipping/price snapshots
  → checkout active → submitted；Cart 来源移除/取消选中成交项
  → 写库存/订单状态流水、Idempotency Result、Outbox(OrderCreated)
  → COMMIT 后返回 Trade Order；异步发送待支付通知/缓存失效（不创建 Payment）
```

任一 SKU 预占失败，整个订单事务回滚，不生成“部分成功”的跨店订单。跨店金额之和、数量和运费必须在服务端确定并写快照。响应超时后客户端以相同 Idempotency Key 重试/查询，不能创建新 Checkout 盲下第二单。

订单事务成功后 `POST /orders` 即结束；Router/Application Service 不调用 `POST /payments` 或支付 Provider。前端持久保存成功响应并进入 `/pay/:tradeOrderId`，支付创建失败只影响 Payment Attempt，不回滚订单、库存预占或复活 Checkout。

#### 3.14.7 库存预占流程

预占与订单创建同事务，通过 3.7.5.5 条件更新：

```text
available = on_hand - reserved - safety_stock
available >= qty:
  inventories.reserved += qty
  inventory_reservations = active(expires_at = trade_order.expires_at)
  inventory_logs(reserve)
否则：INVENTORY_INSUFFICIENT，整单回滚
```

状态：`active → confirmed | released | expired`，三个终态互斥。支付成功将预占 Confirm：`on_hand -= qty、reserved -= qty`；用户取消将其 Released；超时 Worker 将其 Expired。每个动作以 Reservation/Order Item 唯一键幂等，只有状态条件更新成功者修改库存。

支付回调与超时任务竞争时统一先锁定 Trade Order/Order，再按 SKU 升序锁库存。若支付已成功，超时任务退出；若订单已关闭而渠道稍后确认成功，不重新占用/扣减可能已售出的库存，进入“异常支付待对账 → 原路退款/人工处理”。

#### 3.14.8 支付流程

1. 用户针对 `trade_orders.pending_payment` 创建 Payment；Service 从 Trade Order 读取金额/币种，不接受客户端金额。
2. Idempotency 和“当前可用支付尝试”规则防止重复支付；创建 `payments.created`，事务提交后调用 Provider。
3. Provider 明确受理后更新 Payment 为 `pending`，保存渠道交易号和过期时间；所属 Orders 的 `payment_status` 聚合为 `processing`，向前端返回托管收银台/二维码参数。
4. 前端跳转/展示支付控件，回到商城后查询 Payment 或订阅状态；Return URL 只表示浏览器返回。
5. 渠道调用超时标记结果未知并安排主动查询，不能直接新建第二支付单。用户再次支付前先关闭/查询旧尝试。
6. 最终状态只由验签回调或可信主动查询驱动。支付成功后确认库存、更新 Trade/Store Orders、写状态流水和 Outbox。

第三方网络调用不能包在持有订单/库存锁的长事务中。推荐“本地创建 → 调渠道 → 短事务回写”，并用 Payment 状态和渠道幂等单号收敛不确定性。

#### 3.14.9 支付回调流程

```text
接收 Raw Request
  → Provider Adapter 验签/时效/Nonce/商户校验
  → payment_callbacks 按 provider_event_id 去重并保存摘要
  → 解析关联 Payment，校验金额/币种/渠道单号
  → Transaction（锁 Trade Order → Store Orders → Inventory by SKU）:
       payment created|pending → succeeded
       trade pending_payment → paid
       orders payment_status unpaid|processing → paid
       orders order_status pending_payment → paid → pending_shipment
       reservations active → confirmed
       inventory on_hand/reserved 同时扣减
       append payment/order/inventory logs
       outbox PaymentSucceeded/OrdersPaid
  → COMMIT → 按渠道契约快速应答
  → 异步通知、商家待发货、缓存更新和对账
```

重复成功返回首次结果；成功后收到失败事件只记录而不回退。若金额/商户/币种不符，不推进状态并进入高优先级安全告警。若订单已因超时关闭，记录 `late_payment` 异常，不恢复订单、不扣库存，查询渠道确认后创建原路退款或人工工单。

#### 3.14.10 支付超时取消流程

Scheduler 只负责扫描并投递候选，Worker 以小批量处理 `trade_orders.pending_payment AND expires_at <= now`：

1. 领取任务后锁定 Trade Order，复核仍未支付；查询存在 `pending/unknown` Payment 时，先主动向渠道确认或延迟处理。
2. 条件更新 Trade Order 为 `closed`，所属未支付 Orders 为 `cancelled` 且 `payment_status=unpaid`，未成功 Payments 为 `closed`。
3. 将 Active Reservation 转 `expired`，原子减少 `reserved_quantity`，写库存/订单/支付流水。
4. 写 Outbox `TradeOrderExpired/InventoryReleased` 后提交；通知前端并失效订单/购物车相关缓存。
5. 重复 Worker 只看到终态并成功退出。执行失败按游标/主键重试，不扩大扫描事务。

超时任务与回调使用一致锁顺序和状态条件；数据库时间为判断基准。不能仅依赖 Redis TTL 删除预占，因为 Redis 丢失/延迟会造成永久占库或错误释放。

---

#### 3.14.11 订单发货流程

店铺员工只能为本店 `payment_status=paid` 且 `order_status=paid/pending_shipment` 的订单发货。首版选择“创建即确认发货”，不存在可编辑包裹草稿；请求以“包裹”为单位提交承运商、运单号和 `order_item_id + quantity`：

1. 校验 Permission、Store Scope、Order Version、地址可履约、无阻止发货的全额售后/风控冻结。
2. 锁定 Order 和本次涉及 Order Item，核对累计已装包数量不超过已购数量扣除已关闭数量。
3. 对运单号加密并保存 Lookup HMAC/掩码，创建 `shipments(created)` 和 `shipment_items`；先以订单配送快照生成可选初始预计范围并标记 `estimate_source=shipping_template`，Provider 返回更可靠预计时更新为 `carrier` 并记录 `estimate_updated_at`；Provider/渠道支持时提交电子面单。
4. 根据全部有效商品装包情况更新 `fulfillment_status=partial/shipped`；全部应履约数量已发出时，`order_status → shipped` 并写 `shipped_at`，否则保持 `pending_shipment`。
5. 追加状态/操作流水和 Outbox，提交后通知用户、启动物流订阅/轮询。

发货请求必须幂等，重复运单/重复商品分配由唯一约束和数量条件拒绝。创建后不能修改 `shipment_items`；运单错误在允许窗口内走 Tracking Correction，包裹整体错误仅能在未揽收且渠道确认撤销后执行 Shipment Void。Void 锁定 Order/Shipment/Items，条件迁移 `created → voided`，将包含数量返回未履约投影，重算 `fulfillment_status/order_status`，追加操作流水与 Outbox。任何轨迹已表明揽收、渠道撤销结果未知或售后正在依赖该包裹时都拒绝作废并进入对账/人工处理。平台不允许普通管理员直接把订单字符串改为 Shipped；所有发货均产生 Package 与 Item 证据。

#### 3.14.12 确认收货流程

用户点击“确认收货”前，接口返回的 `available_actions` 必须包含 `confirm_receipt`；前端展示订单/包裹摘要并二次确认。命令要求订单属于用户、已全部发货、未处于阻止确认的争议状态，并携 Idempotency Key。

Transaction 锁定订单，条件更新：`order_status shipped → completed`、`fulfillment_status shipped → received`，写 `completed_at`，将相关已交付包裹保持/补正为 Delivered，开启 Review 与售后时间窗，追加状态流水和 Outbox。积分、结算、通知等跨聚合动作在提交后异步执行。

重复确认返回首次完成结果；若已有售后，不简单禁止所有确认，应由政策决定“未售后部分是否可确认”或要求先完成售后。首版建议订单级确认仅在没有阻塞型全单售后时开放，复杂部分确认待引入订单项收货状态后实现。

#### 3.14.13 自动完成订单流程

自动完成不是“定时把所有 Shipped 改 Completed”。Scheduler 扫描候选，Worker 逐单复核：

- 所有有效包裹已由可信轨迹 Delivered，且超过平台/店铺 Policy Snapshot 中的自动确认宽限期；或已超过明确的最大发货后保护期并通过风险规则。
- 没有进行中的拒收、退货、平台争议、物流异常或阻止自动完成的售后。
- Order 仍为 `shipped` 且版本未变。

满足后复用与用户确认相同的 Domain Command，Actor 为 System，幂等更新 `completed/received` 并记录 `auto_confirmed` 原因。不能因物流接口短暂不可用推测已送达；异常单进入人工队列。用户在临界时刻发起售后和自动完成竞争时，以锁与状态/售后 Guard 决定，失败的一方重新读取，不覆盖对方结果。

#### 3.14.14 评价流程

评价资格按 Order Item 计算：本人订单、订单已完成、`review_status=pending`、在评价窗口内、未有主评价。创建时锁定 Order Item，写 `reviews` 与图片绑定，把 `review_status → reviewed`，写 Outbox；`UNIQUE(order_item_id)` 保证并发只能成功一次。

文本/图片先做安全审核。可采用“Pending 对本人可见，Passed 后 Published”的流程；首次公开前审核失败进入 `rejected`，曾公开后被平台处置进入 `hidden`，恢复只允许 `hidden → published` 并追加新治理记录。审核失败记录原因并允许合规修改，不物理删除审计事实；用户隐藏订单/页面不等于删除评价。匿名仅影响前台昵称。追评校验主评价归属、追评窗口和 `UNIQUE(review_id)`；商家回复校验 Store Scope，不能修改用户评价。依法必须清除内容时走不可由普通运营调用的合规擦除/匿名化流程，并保留最小必要审批与审计证明。

商品评分/评价数是派生聚合，由 Outbox Worker 更新并可重建，失败不回滚已提交评价。评价按钮同样来自 `available_actions`，前端不自行按日期计算最终资格。

#### 3.14.15 退款资格检查流程

资格检查无业务副作用，但结果必须可解释和绑定版本：

```text
鉴权/订单归属
  → 校验订单项与申请数量
  → 按 order_item_id 汇总已成功退款 + 活动申请占用数量/金额
  → 读取 order/payment/fulfillment/物流状态
  → 按 Policy Snapshot + 已发布 Eligibility Rule 匹配
  → 风控/时间窗/类目/原因/证据要求计算
  → 逐项输出购买量、成功退款量、活动占用量、当前可申请量/金额与 active_refund_refs
  → 输出 allowed_types、amount_editable、min/max/suggested_refundable_amount、可退运费、预计到账说明、blocking_reasons
  → 签发短期 eligibility_token（绑定输入哈希、规则和资源版本）
```

资格检查不锁定退款额度，不保证后续必定创建成功；申请接口再次锁定并计算。某订单项被占满只使该项不可选，不阻断同订单其他项；同一项部分占用后剩余数量仍可申请。`eligible=false` 使用 200 返回稳定原因码；资源不存在/越权仍按 404。Rule JSON 不能覆盖“申请人必须是订单本人、逐项成功+活动占用不得超购买量/可退实付、整单总退款不得超已付金额”等代码级不变式。

#### 3.14.16 退款申请流程

1. 用户提交 `eligibility_token`、类型、原因、订单项数量、符合金额策略的 `requested_amount`、联系方式和 Active Evidence File ID，并提供 Idempotency Key。
2. 验证 Token 签名、归属、过期、输入哈希和资源版本；按 `order_item_id ASC` 锁定所涉 Order Item，基于订单项的改价不可变分摊、历史成功退款和活动申请占用重算可售后数量/金额。仅当本次请求与同项剩余容量冲突时拒绝；同订单其他项的售后不能成为订单级阻断。禁止回退使用订单级总改价或当前商品价格。
3. 规范化并加密联系方式快照，创建 `refund_applications(submitted)`、`refund_items`、Evidence 关联与 `refund_events`；原子把相关 Order Item `after_sale_status → in_progress`，聚合 Order `after_sale_status → in_progress/partial`。
4. 命中完整、低风险、确定性自动规则时可在同事务/后续确定性 Worker 推进 `submitted → approved`；其余 `submitted → merchant_review`，由管理后台按 `refund_applications` 的状态、创建时间、`merchant_deadline_at` 和 Store Scope 形成审核队列，不创建 `human_service_tickets` 或第二份重复业务任务。
5. 写 Outbox 后提交，通知店铺/平台队列。AI 只可解释、收集材料和生成草稿，不决定最终金额。

同一订单项的“活动占用 + 已成功退款数量/金额”通过锁定和权威汇总禁止超限；不能以订单级 Active Bool 或仅按 `order_item_id` 的普通唯一键误伤合法部分数量。店铺决定使用幂等命令和 If-Match：同意后按退款类型进入 `approved → refunding` 或 `approved → waiting_return`；拒绝进入 `rejected` 并释放活动占用，记录原因与两个独立动作——有剩余资格时新建申请、申诉窗口内新建 Appeal。任何动作都不把原申请逆转回 `submitted`。用户仅能在允许阶段撤销为 `cancelled`。

#### 3.14.17 退货退款流程

仅 `return_and_refund` 使用：

```text
approved → waiting_return（生成脱敏退货地址、return_deadline_at）
  → 用户提交退货物流：returning
  → Provider 轨迹更新 submitted/in_transit/delivered
  → 商家签收验货：received
      ├→ 数量/状态一致：refund_application → refunding
      └→ 差异/损坏/缺件：进入协商/平台人工，不直接改申请金额
  → 调用退款支付流程 → succeeded
```

用户提交运单时校验本人、阶段、承运商、运单格式并加密；同一申请默认唯一主退货物流。超过退货期限由 Worker 根据 Policy 进入 `closed` 或人工延期，不能静默驳回。验货记录逐 Refund Item 保存 `received_quantity、condition_status、remark` 和证据；店铺不能在批准后单方面扩大用户责任或把金额改为负值。

确认退货入库是独立库存命令：只有验收可再次销售的数量才增加 `on_hand_quantity`，写 `inventory_logs(inbound)`；退款成功不等于商品自动回库。

#### 3.14.18 退款支付流程

售后 `approved/received` 只表示业务同意，资金仍需独立执行：

1. Refund Service 验证原 Payment 已成功、可退余额、币种和批准金额；按原支付资金分摊创建 `payment_refunds.created` 与 `refund_payment_records(requested)`。
2. 提交本地事务后以稳定 Provider Refund No 调渠道；明确受理更新 `processing`，网络超时标记结果未知并主动查询。
3. 验签回调/查询确认成功后，锁定 Payment、Trade Order、Order、Refund Application，更新渠道退款 `succeeded`、累计退款金额和 `payment_status=partially_refunded/refunded`。
4. 售后 `refunding → succeeded`，相关 Order Item 数量/金额累加并进入 `after_sale_status=completed`；聚合 Order/Trade Refund 状态重新计算。
5. 写资金/售后/订单流水和 Outbox，提交后通知用户。只有本步成功页面才显示“退款成功”。

Provider 明确失败可在相同业务退款单上有界重试；结果未知先查询/对账。任何累计金额超过 Paid Amount、币种不符、回调金额不同或本地/渠道状态分叉都停止自动处理并进入资金对账队列。禁止通过修改数据库状态人工伪造到账。

#### 3.14.19 订单状态机

订单、Payment、履约、售后与会话状态枚举及合法迁移的唯一机器可读来源为 `docs/domain_registry.yaml`；本节 3.14.19～3.14.23 与 3.15.4 的表格是其中文展示。Domain Enum、数据库 Check Constraint、OpenAPI Enum、前端生成类型、`available_actions` 和正反向状态机测试均由 Registry 生成或在 CI 中逐项比对，禁止在代码、DDL 或页面中另建同名异义枚举。

`orders.order_status` 表示订单主生命周期；支付、履约和售后使用正交字段补充：

```text
pending_payment ──payment_succeeded──> paid ──fulfillment_initialized──> pending_shipment
       │                                  │                                  │
       └──user_cancel/timeout──> cancelled └──compensation──> closed          ├──all_items_shipped──> shipped
                                                                              │                          │
                                                                              └──full_close──> closed    ├──confirm_receipt──> completed
                                                                                                         └──auto_confirm────> completed
```

| 当前状态 | 允许目标 | 主要 Guard/说明 |
| :--- | :--- | :--- |
| `pending_payment` | `paid` | Payment 可信成功、金额币种一致、预占仍可 Confirm |
| `pending_payment` | `cancelled` | 用户取消/超时，Payment 无成功/未知结果，释放预占 |
| `paid` | `pending_shipment` | 初始化履约；通常与支付成功在同一事务连续记录两次状态事件 |
| `paid/pending_shipment` | `closed` | 全额取消/补偿退款已按规则落定且未继续履约 |
| `pending_shipment` | `shipped` | 全部应履约数量已进入有效包裹；部分发货仅更新 Fulfillment |
| `shipped` | `completed` | 用户确认或满足自动完成规则 |
| `shipped` | `closed` | 正常完成前发生全单退回/异常终止且资金处理完毕 |

`completed/cancelled/closed` 是主状态终态。订单完成后发生售后，不把 `completed` 逆转为处理中；通过 `after_sale_status` 和 `payment_status` 展示。若全额退款发生在完成后，主状态仍可保持 Completed，资金字段显示 Refunded，从而保留正常履约事实。

#### 3.14.20 支付状态机

单个 `payments`：

```text
created → pending → succeeded → partially_refunded → refunded
   │         ├────→ failed
   └─────────┴────→ closed
```

| 转换 | 触发条件 |
| :--- | :--- |
| `created → pending` | Provider 已明确受理并返回可追踪交易号 |
| `created/pending → succeeded` | 验签回调/可信查询，金额币种一致 |
| `created/pending → failed` | Provider 明确最终失败；不包含网络超时 |
| `created/pending → closed` | 本地/渠道确认关闭且从未成功 |
| `succeeded → partially_refunded` | 成功退款累计金额在 `(0, paid_amount)` |
| `succeeded/partially_refunded → refunded` | 成功退款累计金额等于 Paid Amount |

`failed/closed/refunded` 是单支付单终态；需要重新支付创建新 Payment Attempt。Succeeded 不得转 Failed/Closed，退款不复用支付失败字段。网络不确定状态保持 `pending` 并以同步记录表达，而不是新增一个可能被误当终态的 Failed。

#### 3.14.21 履约状态机

订单聚合 `fulfillment_status`：

```text
unfulfilled → partial → shipped → received
      └────────────────→ shipped
```

- `unfulfilled`：没有有效发出数量。
- `partial`：至少一部分商品已发出，但仍有应履约数量未发出；订单主状态仍 Pending Shipment。
- `shipped`：所有应履约数量已发出，订单主状态为 Shipped。
- `received`：订单级确认/自动完成成功。

包裹 `shipment_status` 独立为：`created → picked_up → in_transit → delivered`，异常可进入 `exception`，退回进入 `returned`，无需继续跟踪可 `closed`；只有未揽收 `created` 可经受控作废进入终态 `voided`。`voided` 包裹不计入履约数量，但仍在审计和管理时间线中可见；普通用户只看到“发货信息已更正”的安全事件和后续有效包裹。物流 Provider 的乱序轨迹以事件发生时间和允许转换合并；“轨迹更晚到达”不等于“业务状态一定更后”。Exception 解决后可回 In Transit，但必须追加轨迹，不覆盖异常证据。

未揽收包裹作废后必须重新计算有效发货数量：若已发数量从全部降为部分或零，履约状态仅允许由 `VoidShipment` 补偿命令执行 `shipped → partial/unfulfilled` 或 `partial → unfulfilled`；订单主状态同时由 `shipped → pending_shipment`，并清空原发货完成时间。上述逆向转换不向通用更新接口开放，且必须记录状态流水、管理员审计、幂等记录与 Outbox 事件。

#### 3.14.22 售后状态机

```text
submitted → merchant_review ──reject──> rejected
    │             │
    │             └──approve refund_only──────> approved → refunding → succeeded
    │             └──approve return_and_refund→ approved → waiting_return → returning → received → refunding → succeeded
    ├──rule_auto_approve───────────────────────> approved
    └──user_cancel─────────────────────────────> cancelled

waiting_return ──deadline/policy_close──> closed
merchant_review/approved ──controlled_close──> closed
```

`rejected、succeeded、closed、cancelled` 为终态，不允许改回审核中；申诉创建新的 `refund_appeals` 平台复核资源，不修改原事件历史。申诉状态为 `submitted → reviewing → upheld/rejected`，用户在作出决定前可进入 `cancelled`；`upheld/rejected/cancelled/closed` 为申诉终态。支持申诉后由受控补偿命令创建新的退款/补偿记录，不能把原 `refund_application` 从 `rejected` 直接改回 `approved`。`approved_amount` 在 Approved 后只允许通过受审计的纠错/复核流程减少到不低于已退款金额，首版建议冻结不再修改并通过新补偿单处理差异。

Order Item 的 After Sale 状态由活跃/完成申请聚合，Order 的 `none/in_progress/partial/completed` 由各 Item 派生；不能让单个部分退款把整单标成全额完成。

#### 3.14.23 非法状态转换处理

未在 `docs/domain_registry.yaml` 登记的转换一律非法，并统一返回 409 `*_STATE_CONFLICT`；Problem Details 包含安全的 `current_status、attempted_action、allowed_actions` 和 Request ID，不返回内部堆栈或其他租户数据。ETag 失配优先按 412，缺 If-Match 按 428，归属不符按 404。CI 必须对 Registry 中每条合法边生成至少一个成功用例，并对每个状态到全部未登记目标生成无写入断言；正文图表与 Registry 漂移时构建失败。

Domain Command 使用以下模板：

```sql
UPDATE aggregate
SET status = :to_status, version = version + 1, updated_at = UTC_TIMESTAMP(6)
WHERE id = :id
  AND status IN (:allowed_from_statuses)
  AND version = :expected_version;
```

影响行数为 0 时重新读取并区分“幂等已完成、版本冲突、状态冲突、资源不可见”。只有成功转换才写对应状态流水/Outbox；重复请求若命中幂等记录返回首次结果，不重复产生事件。

禁止的恢复手段包括：管理员直接 UPDATE 状态、Consumer 强制覆盖、收到晚到回调就把终态倒退、删除状态流水后重跑。确需纠错时使用显式 Compensation Command，记录原事件、原因、审批人和新事件；资金/库存结果未知先对账。非法转换率、乐观锁冲突率、晚到支付和人工补偿量进入监控与告警。

---

### 3.15 消息与客服系统设计

消息系统既承载普通聊天，也承载商品/订单上下文、AI Run、工具确认和人工工单。MySQL 是会话、消息、Read Cursor、工单和状态的唯一事实；Redis 只提供在线状态、未读缓存、Typing、热点上下文和跨实例实时分发；PostgreSQL 保存经过策略处理的摘要、长期记忆与 Agent Checkpoint，不代替原消息。

#### 3.15.1 消息系统总体架构

```text
Vue 3
 ├─ REST：会话列表、历史消息、可靠发送、已读、工单、Approval
 ├─ WebSocket：新消息、未读、人工状态、跨设备实时事件
 └─ SSE：单次 Agent Run 的文本增量、进度和确认事件
         │
API Gateway / Auth / Rate Limit
         │
Conversation Service ── MySQL(conversations/messages/reads/tickets/outbox)
         │                         │
         │                         └─ Outbox Relay → Redis Streams/Worker
         │                                              ├─ Agent Runtime/LangGraph
WebSocket Gateway ← Redis Pub/Sub/Streams               ├─ Content Moderation
         │                                              ├─ Human Queue Routing
         └──────────────── committed events ────────────└─ Notification
```

消息写路径不能依赖 WebSocket 节点本地内存。REST 与 WebSocket `message.send` 最终调用同一个 Application Use Case；事务提交后才发 ACK/广播。Redis 丢失可从 MySQL Cursor 补拉，Agent/通知消费可从 Outbox 重放。任何实时事件都不能比数据库事实更权威。

部署时 WebSocket Gateway 无状态水平扩展；连接映射/Presence 放 Redis 短 TTL，跨节点事件带 `event_id、conversation_id、recipient`。Agent Worker、人工客服工作台和消息 API 共享 DTO/权限规则，但不能共享可任意绕过授权的数据库账号。

#### 3.15.2 专属客服固定会话

专属客服为每个用户唯一 `conversation_type=exclusive、store_id=NULL` 的平台会话。注册时创建，历史用户首次进入时通过 `PUT /users/me/exclusive-conversation` 幂等补建；数据库 `exclusive_user_key` 唯一约束处理并发。

响应固定包含 `display_name=专属客服、is_fixed=true、fixed_rank=0`。Vue 左栏分两段渲染：先渲染专属客服固定区，再渲染按 `last_message_at DESC` 的店铺会话；不通过普通 `pinned` 排序，也不允许用户取消置顶、隐藏或删除。无消息时仍展示欢迎态，欢迎语由版本化配置生成，不每次进入重复写一条消息。

专属客服使用平台 Agent、平台知识和当前用户授权范围内的跨店业务工具；不存在 Store Scope。切换到人工只进入平台人工客服队列。用户撤销长期记忆授权不删除会话/原始业务消息，而是停止记忆使用并按生命周期清理 Memory 派生数据。

#### 3.15.3 店铺客服会话

店铺客服为每个“用户 + 店铺”唯一 `conversation_type=store` 会话。从商品详情、店铺页、订单详情“联系商家”、退款页进入都调用同一 Get-or-Create Use Case；入口可安全更新当前 Product/Order/Refund Context，不创建多条会话。

会话响应展示店铺当前可见名称/Logo、安全服务状态和最后消息。店铺注销/冻结后保留历史会话和交易证据，但禁止新 AI 自动承诺/普通消息，页面进入只读或平台人工客服引导状态。用户曾隐藏店铺会话后再次从业务入口进入，清除 `user_hidden_at` 恢复原会话。

店铺 AI 的知识、工具、人工队列和数据查询强制绑定该 `store_id`。订单上下文还必须同时满足 Order User 为当前用户、Order Store 为当前店铺。店铺人员只能查看其 Data Scope 内会话；平台争议升级创建受控工单，不把其他平台会话暴露给商家。

#### 3.15.4 会话唯一性约束

唯一性由 MySQL 生成列/唯一索引兜底，不依赖“先 SELECT 没有再 INSERT”：

```text
exclusive: UNIQUE(exclusive_user_key)
  exclusive_user_key = user_id when conversation_type='exclusive' else NULL

store: UNIQUE(store_user_key, store_id)
  store_user_key = user_id when conversation_type='store' else NULL
```

Get-or-Create 先尝试读取；不存在则 INSERT，捕获唯一冲突后读取获胜行。恢复 Hidden、更新 Context 和返回会话可在随后短事务完成。会话 `closed` 表示当前服务阶段结束，不释放唯一键；新问题复用同一会话并记录 Reopened 状态事件，避免历史被拆散。

Conversation Domain 的唯一 Transition Table 位于 `docs/domain_registry.yaml`，其中显式登记 `ReopenConversation: closed → active`；`reopened` 日志固定 `from_status=closed、to_status=active`。用户从业务入口进入已关闭会话，或向已关闭会话发送第一条新消息时，必须执行该命令。若会话仍关联 `queued/assigned/active/waiting_user` 非终态人工工单，禁止直接重开为 AI Active，应按 Ticket 状态恢复人工路由。`user_hidden_at` 只是用户列表可见性，清除 Hidden 不生成伪状态迁移。

一个会话同时最多一个非终态人工工单、一个活跃人工 Assignment；用 Active Generated Key 唯一约束。一个用户可有多个店铺会话，但只有一个专属客服。

#### 3.15.5 消息发送和持久化

可靠发送流程：

```text
客户端生成符合 `cmsg_<26-char-ulid>` 的稳定 client_message_id
  → REST/WS 鉴权、会话参与者与当前发送模式校验
  → Schema/长度/文件/Card 资源校验 + 快速内容审核
  → Transaction:
       锁定 Conversation；若 closed 则条件执行 ReopenConversation，version+1，写唯一 reopened 流水
       若有非终态人工 Ticket，路由人工状态而不重开 AI
       清除 user_hidden_at（仅可见性）；更新 conversation.last_sequence_no
       INSERT messages(sequence_no, sender, content, moderation_status)
       UPDATE conversation.last_message_id/at, version
       INSERT outbox(ConversationReopened，若发生) + outbox(MessageCreated)
       写 Idempotency Result（如适用）
  → COMMIT
  → 返回/发送 request.ack
  → Outbox Worker 广播 WebSocket、更新未读、触发 AI/人工路由和异步深度审核
```

用户文本先持久化再触发 Agent，避免模型完成而输入丢失。若同步规则明确阻断，仍可保存为 `message_status=hidden、moderation_status=blocked` 的受限审计记录，不向其他参与者/模型广播，并返回安全提示。附件只保存 Active `file_id`/对象引用；不把 Base64 或 Presigned URL写消息正文。

Reopen 与第一条新消息必须位于同一事务和同一 Conversation Lock 内。并发两条新消息只能一条执行 `closed→active`并写一条 Reopened Event，两条消息分配不同 Sequence；Outbox Consumer 按 Conversation Version/Event ID 去重，只对当前 Active 阶段序列触发恰好一个 Agent Run。非法迁移、已有人工 Ticket 和版本冲突均在写 Message/Outbox 前得到确定处理。

数据库提交成功但 ACK 丢失时，客户端以同一 `client_message_id` 重试，服务端返回原消息。提交前失败可安全重试；提交结果未知先查询该 ID。广播、Push、Agent 失败不回滚消息，通过 Outbox 重试。

#### 3.15.6 消息顺序与去重

每个 Conversation 维护 `last_sequence_no`；发送事务锁定会话并递增，消息保存 `(conversation_id, sequence_no)` 唯一值。`message_no` 是公开 ID，Sequence 只在该会话内单调，不承诺无间隙，也不用于授权。

排序规则统一为 `sequence_no ASC`；Cursor 内部绑定 Conversation/Sequence/方向并签名。WebSocket/SSE 事件另有 Event Sequence，两者不可混用。客户端处理：

- `client_message_id` 关联本地 Pending 气泡与服务端消息，重复 ACK 合并。
- 按 `message_id/event_id` 去重，按 Sequence 插入；发现缺口先缓存后续事件并调用 REST `after_cursor` 补拉。
- 服务器时间只展示，不用于客户端决定先后；多设备同时发送由数据库分配顺序。
- 撤回/隐藏保留原 Sequence 和占位，不重新编号。

约束至少包括 `UNIQUE(conversation_id, sequence_no)` 和 `UNIQUE(conversation_id, client_message_no)`；API `client_message_id` 映射到数据库 `client_message_no`，要求在该会话内由客户端生成 `cmsg_<26-char-ulid>`，服务端验证前缀、长度与 ULID 格式。Outbox Consumer 按 Event No 去重，广播重复是允许的，前端必须幂等。

#### 3.15.7 未读消息统计

MySQL `message_reads.last_read_message_id` 与 `last_read_sequence_no` 是事实，Read Cursor 只能单调前进且目标消息属于该会话。打开/点击会话不等于已读；前端必须同时满足：当前会话激活、Document 可见、消息加载成功并完成渲染、目标气泡在滚动容器中至少 50% 可见持续 500 ms，才提交最高已读位点。用户停留在历史消息、页面后台、加载失败或目标未渲染时不得推进；接口失败保留角标并用相同目标幂等重试。

Redis 保存：

```text
unread:user:{user_no}:conversation:{conversation_no} → count
unread:user:{user_no}:total                          → count
```

新消息提交后，给除 Sender 外的参与者增加未读；专属/店铺 AI 发出的可见最终消息计未读，Typing、Delta、Heartbeat、隐藏/审核阻断消息不计。推进 Read Cursor 后以 Lua 原子更新缓存并广播 `unread.updated={conversation_no, conversation_unread, total_unread, last_read_sequence_no, cursor_version}`。为避免重复 Consumer 累加，更新携 Event ID/Sequence Watermark；客户端仅接受更高 Version 的绝对值，不做本地减法。

缓存缺失或计数异常时通过 `messages.sequence_no > message_reads.last_read_sequence_no AND visible_to(reader)` 重建；总数可由各会话聚合。显示值允许短暂最终一致，但不能为负；会话列表响应返回数据库/缓存校正后的权威未读。归档/隐藏是否清零只影响显示，不改变审计 Read Cursor，策略需统一。

#### 3.15.8 SSE 与 WebSocket 分工

| 能力 | SSE | WebSocket |
| :--- | :--- | :--- |
| 连接范围 | 单个 `agent_run_id` | 当前账号的多会话/多设备实时通道 |
| 方向 | Server → Client | 双向 |
| 内容 | AI Token Delta、引用、Tool Progress、Approval、Run 终态 | 已提交消息、未读、会话、人工工单、Approval/File 状态 |
| 恢复 | `Last-Event-ID` + Run Snapshot | 重连后 REST Cursor 补拉；短实时事件可不重放 |
| 权威终态 | `message.completed/run.completed` 后的 MySQL 资源 | MySQL Message/Conversation/Read/Ticket |

发送聊天优先用 REST 获得天然 HTTP 幂等和错误语义；WebSocket 发送只是相同 Use Case 的低延迟入口。AI 文本增量不通过全局 WebSocket 广播，避免多设备重复拼接；其他设备只在最终 `message.created/completed` 后收到完整消息。用户关闭 SSE 不取消 Run，除非显式调用 Run Cancellation。

#### 3.15.9 用户发送商品卡片

客户端只提交结构化引用：

```json
{
  "client_message_id": "cmsg_01K...",
  "content": {
    "type": "product_card",
    "product_id": "prd_01K...",
    "sku_id": "sku_01K..."
  }
}
```

服务端校验商品对当前用户可见；店铺客服会话还要求 Product Store 等于 Conversation Store。随后从权威数据生成最小 `display_snapshot`（名称、主图版本、规格、展示 Money、状态），并保存 Resource ID。客户端不能提交/覆盖店铺名、价格、库存和跳转 URL。

历史气泡显示 Snapshot，标注“发送时信息”；点击时调用商品详情接口获取实时状态。卡片可成为 `conversation_contexts.product`，但上下文不授予 Agent 额外权限。下架后保留安全历史摘要，点击显示已下架而非跳到其他商品。

#### 3.15.10 用户发送订单卡片

订单卡片仅允许订单本人发送；店铺会话还要求 Order Store 匹配，专属客服允许当前用户自己的任意店铺订单：

```json
{
  "content": {
    "type": "order_card",
    "order_id": "ord_01K..."
  }
}
```

服务端生成脱敏 Snapshot：Order No 后缀/公开号、店铺、商品摘要、Money、当前状态，不包含完整地址、电话、支付凭证和后台备注。人工/AI 再查询实时订单时仍执行归属、Store Scope、Consent 和字段投影，不能因为卡片存在就跳过授权。

从订单详情点击“联系商家”时，先 Get-or-Create 店铺会话，再以同样验证逻辑设置 Order Context；前端可以展示卡片预览，但发送成功以服务端 Message 为准。

#### 3.15.11 AI 回复消息

MessageCreated Outbox 仅在 `conversation_status=active`、输入可见且没有人工接管时创建/获取 Agent Run。Run 以 Trigger Message ID 幂等，固定 Agent/Skill/Policy Version：

1. Run 开始时在短事务中创建 Assistant Placeholder `messages(sending)` 并分配 Sequence，SSE 发 `message.started`。
2. Agent 加载受预算限制的近期消息、摘要、经授权记忆和已验证 Context；检索/Tool 均经过 Scope 与安全层。
3. 文本 Delta 通过 SSE 和 Checkpoint/短期 Buffer 传输，不把每个 Token 单独写 MySQL/Outbox。
4. 需要工具确认时 Run 进入 `waiting_approval`，发送 Approval 卡片；未确认前不执行写 Tool。
5. 完成时原子写最终正文、引用和安全元数据，`message_status → sent`，写 Outbox `AssistantMessageCompleted`，SSE 发送 Final/Completed。
6. 失败/取消把 Placeholder 标为 `failed/hidden`，根据策略写用户可见系统提示或转人工；不能把最后一个 Delta 当完整答复。

输出在持久化和渲染前做安全审核/Markdown Sanitization。前端只渲染允许的 Markdown 子集，链接经 Scheme/Domain 检查，禁止执行模型生成 HTML/JS。AI 消息明确标识 AI 身份、可提供引用/反馈入口，不展示 Chain-of-Thought。

AI 气泡底部固定显示可键盘访问的 [有帮助] [没帮助] [举报/纠错] 和 [查看引用]；图标必须有可见 Tooltip、`aria-label`、`aria-pressed` 和焦点样式。赞/踩采用幂等 PUT，再次点击已选项调用 DELETE，切换反应不同时保留两个当前值；失败回滚乐观 UI 并保留用户未提交的说明。[举报/纠错] 对话框先选类型/原因，Correction 可填正确说法与公开来源，完成后只提示“已收到并待审核”。人工/System/Tool 气泡不显示这些入口，前端隐藏不代替后端 `sender_type=agent` 校验。

#### 3.15.12 人工客服消息

只有当前 Active Assignment 的客服或有监督权限人员可以发送人工消息。发送前验证 Ticket、Conversation、Queue/Store Scope 和 Assignment 状态；Sender 固定为 `human`，`sender_id` 来自认证主体，客户端不能伪造昵称/身份。

人工消息复用统一持久化/Sequence/Outbox/审核流程，并在用户离线时触发站内/Push 通知。客服工作台可见信息按工单 Purpose 最小投影；查看完整地址、凭证或历史敏感会话需要额外 Permission/理由并审计。

AI 可为客服生成“仅坐席可见建议”，它存入独立 Internal Note/Assist 资源而非用户消息表，必须由人工点击发送并可编辑；最终发给用户的内容 Actor 是人工客服，审计保留 AI 建议版本与人工修改摘要。

#### 3.15.13 AI 转人工流程

触发来源：用户明确要求、Agent 低置信/连续失败、工具/外部服务不可用、情绪/投诉/安全规则命中、资金争议或政策要求人工。流程：

```text
AI/User 请求转人工
  → 幂等创建唯一 Active human_service_ticket
  → 根据 exclusive/store、问题类型、语言、技能、风险确定可信 queue_code
  → conversation active → human_pending
  → 生成脱敏问题摘要/相关 Resource 引用，不复制无关全量上下文
  → Outbox 投递队列并通知用户
  → 客服原子 Claim/Accept
  → conversation human_pending → human_active
  → 停止 AI 面向用户自动回复，推送 human_service.updated
```

Queue/Priority 最终由规则决定，模型建议只能作为输入。排队期间可由 AI 提供明确标注的非交易性等待提示，但不继续发起写 Tool；首版建议完全暂停自动回答，避免用户不知道当前由谁负责。Claim 冲突只允许一个 Active Assignment。

交接包固定落入 `human_service_tickets.handoff_summary、handoff_message_refs、handoff_policy_version`：只包含用户目标、已确认事实、必要资源公开 ID、已完成动作、尚待处理项和安全限制。消息引用必须是用户可见消息且与本 Ticket Purpose 相关；完整会话、隐藏推理、Tool 原始报文、风控字段和 `human_service_internal_notes` 不得进入交接包。客服读取每条引用仍执行 Ticket/Assignment、RBAC、Queue/Store Scope、业务理由与审计校验。

人工处理中需用户补充信息时，客服调用 Wait Command：仅允许 `ticket.active → waiting_user`，必填结构化原因，保存剩余 SLA，追加 `human_service_ticket_events`、写 Outbox 并向用户展示“等待您回复”。此时 Conversation 仍为 `human_active`，AI 仍不自动回复。用户在该会话发送新可见消息后，Message Outbox Consumer 以工单版本幂等调用 Resume；客服也可主动恢复。Resume 只允许 `waiting_user → active`，按剩余时长重新计算 SLA，并通过 `human_service.updated` WebSocket 事件更新用户与工作台。重复用户消息、重复 Resume 和与 Resolve/Transfer 并发必须以 If-Match+条件迁移收敛，不重复恢复 SLA。

#### 3.15.14 人工结束服务流程

当前客服提交 Resolution Code、必要备注和“后续是否允许 AI”建议；Domain 校验工单状态与 Assignment 后：

1. Assignment → completed，Ticket → resolved/closed，写状态流水。
2. Conversation `human_active → active`，清除当前 Ticket Pointer，但不删除工单。
3. 给用户发送明确系统消息“人工服务已结束”；WebSocket 更新工作台和用户端。
4. 下一条用户消息默认重新进入 AI，若投诉/风险策略要求冷却或平台跟进，则路由新人工工单。
5. 生成 `resolution_summary` 的用户可见/Agent 安全投影；`resolution_note` 与 `human_service_internal_notes` 始终留在人工工作台受限投影中。
6. 不因人工结束自动创建或恢复 Agent Run；用户下一条消息到达后才创建新 Run，并在当前 Purpose/授权仍有效时装配 `resolution_summary` 与必要用户可见消息引用。

人工超时离线先 Transfer/Release Assignment，不直接结束用户问题。用户主动取消排队只适用于尚未 Human Active 的 Ticket；人工接管后由客服结束或主管强制转派并审计。

#### 3.15.15 会话删除、隐藏和审计

专属客服不能删除/隐藏。普通店铺会话的“删除”定义为用户侧隐藏：写 `user_hidden_at` 和 `conversation_status_logs(hidden)`，不删除 Conversation、Message、Context、Ticket、Approval、Tool Audit。对方新消息或用户再次从店铺、商品、订单入口进入时按产品规则恢复可见并记录 Reopened。

消息默认不允许普通用户逐条物理删除。撤回如后续支持，仅在短窗口内把 `message_status → recalled`、保留 Sequence 和安全占位，并写 Actor/Reason；已经触发的订单、工具、人工工单不会因撤回自动回滚。客服/管理员隐藏违规内容必须有 Permission、Reason 和审计，原文进入限制访问的合规存储。

账号注销按 3.5/3.10 生命周期执行匿名化、法定留存和派生数据 Tombstone；不是级联删除交易客服证据。用户导出/访问会话只包含其有权数据和安全附件链接，导出行为审计。平台人员查询会话必须有工单/治理目的，禁止日常无理由全量浏览。

#### 3.15.16 消息内容安全审核

审核对象包括用户、AI、人工文本，图片/文件，商品/订单卡片引用和公开昵称。采用“确定性校验 + 安全模型/规则 + 人工复核”分层：

- 输入阶段：Schema、长度、Unicode/控制字符、URL、垃圾消息、频率、文件状态和资源归属；命中明确恶意规则可同步阻断。
- AI 前：对外部内容标记不可信来源，检测 Prompt Injection/数据外泄意图；阻断内容不进入 Agent Context/Memory/RAG。
- 输出阶段：检查个人信息泄露、跨租户内容、危险链接、违规承诺和不当内容；Tool 结果先字段白名单/脱敏。
- 展示阶段：前端按纯文本/安全 Markdown 渲染，HTML Escape，链接加安全属性；图片独立域和安全 Header。
- 异步深审：可将先通过的低风险消息进一步审核；后续判违规时发送 `message.updated` 改为占位并保留审计。

`moderation_status` 至少为 `not_required/pending/passed/blocked`，与 `message_status` 分离。Blocked 内容不通知对方、不计未读、不触发 Agent/Memory；本人看到可申诉提示。人工客服和管理员消息同样审核，不能因角色跳过。审核 Provider 只接收最小内容，跨境/隐私策略允许时才调用外部服务；样本进入评估集前去标识化并控制访问。

安全审核失败、超时和服务不可用按风险降级：高风险文件/链接 Fail Closed，普通短文本可进入 Pending 或转人工，不默认全部放行。持续误报/漏报、申诉推翻率、Prompt Injection 命中后的 Tool Deny 情况纳入观测与评估体系。

---

### 3.16 AI 智能客服总体设计

AI 智能客服由“会话入口 + Agent Runtime + Context/RAG/Memory + Model Gateway + Skill/Tool Registry + MCP Permission Gateway + Human-in-the-loop + 观测评估”组成。店铺客服与专属客服使用两个独立顶层 Graph，共享安全、检索、模型和工具基础设施，但不共享数据作用域或能力白名单。

项目不采用“把所有接口都交给一个万能 Agent”的设计。模型负责语言理解、规划建议和自然语言生成；权限、金额、资源归属、状态机、幂等、确认和最终业务结果由确定性后端负责。即使模型行为完全异常，也不应越过 3.13 的权限边界。

#### 3.16.1 AI 模块建设目标

| 目标 | 可验收结果 |
| :--- | :--- |
| 正确回答 | 商品/政策/订单回答可引用权威来源；不知道时明确说明，不编造库存、物流和售后结论 |
| 安全可控 | 跨用户/跨店访问和未确认写操作被 Tool Gateway 确定性拒绝；敏感信息最小返回 |
| 可恢复 | 进程重启、网络中断和用户延迟确认后可从 Checkpoint 恢复，不重复副作用 |
| 可治理 | Agent、Prompt、Graph、Skill、Tool、模型路由和知识索引均版本化，可灰度和回滚 |
| 可观测 | 每个 Run 可关联消息、模型、检索、Tool、Approval、Token、延迟、成本和结果 |
| 可评估 | 上线前离线回归，上线后抽样/用户反馈/业务结果评估，失败指标阻断发布 |
| 可降级 | 模型、RAG、MCP、Redis 或 PostgreSQL 局部故障时返回可信有限能力/人工入口，不伪造成功 |
| 成本受控 | 按意图和风险选择模型，限制上下文、步骤、Tool 与 Token，预算超限安全终止 |

首版优先覆盖高频、可验证、低到中风险场景。多 Agent 数量不是建设目标；只有能通过独立权限、评估和成本证明价值的专业 Subgraph 才引入。

#### 3.16.2 AI 模块边界

AI Runtime 拥有：意图识别、上下文组装、RAG、记忆候选、Skill 路由、受控 Tool 调用、回复生成、流式事件、Interrupt、运行状态和 AI 观测数据。

AI Runtime 不拥有：用户身份、RBAC、商品/库存/订单/支付/退款事实、业务状态转换、支付渠道密钥、文件访问授权和管理员审批。它只能通过版本化 Port/MCP Tool 调用相应 Application Service。

```text
模型输出 = 建议（untrusted proposal）
Tool Gateway 授权 = 是否允许尝试调用
Domain Service = 是否允许当前业务动作
MySQL Commit/外部对账 = 动作是否实际完成
```

回答中的“已提交、已取消、已退款、已修改”只能来自成功 Tool Result 中的最终业务资源；仅生成了草稿、Approval 或调用超时必须使用对应措辞。模型不得根据 UI 卡片、RAG 文档或旧 Memory 推断实时交易状态。

#### 3.16.3 Agent Runtime 架构

LangGraph 1.2.x 作为主 Runtime，顶层 Graph 采用显式节点和条件边：

```text
load_run_and_identity
  → input_guard
  → load_conversation_scope
  → classify_intent_and_select_skill
  → build_context
      ├─ recent_messages + summary
      ├─ verified business context
      ├─ consented memory
      └─ scoped hybrid RAG
  → reason_and_draft
      ├─ direct_answer
      ├─ read_tool_subgraph ─→ verify_tool_result ─┐
      ├─ write_proposal ─→ permission/approval ───┤
      └─ handoff_subgraph                         │
  → output_guard + citations                     │
  → persist_final_message                        │
  → memory_candidate/event/evaluation hooks      │
  → complete_run ◄───────────────────────────────┘
```

Graph State 使用版本化、JSON 可序列化 TypedDict/Pydantic 数据，至少包含 `run_no、conversation_no、trigger_message_no、response_message_no、user_no、store_no、conversation_type、agent_version_no、graph_version、selected_skill_versions、intent、business_context_refs、consent_refs、context_budget、tool_attempt_refs、approval_no、public_draft、error_state`。不写数据库 Session、HTTP Client、Secret、Access Token、完整文件或不可序列化对象。

专业能力以受限 Subgraph 实现，例如商品搜索、订单解释、售后预检；父 Graph 传入最小 State，子图不能自行扩大 Tool 集和数据范围。并行仅用于互不产生副作用的检索/只读查询，写 Tool 严格串行。设置最大 Graph Step、模型调用、Tool 调用、递归/委派深度和总预算，防止循环。

#### 3.16.4 模型供应商抽象层

`app/ai/model_providers/` 提供内部 `ModelGateway`，业务 Graph 不直接依赖供应商 SDK。核心接口：

```text
invoke(ModelRequest) -> ModelResponse
stream(ModelRequest) -> AsyncIterator<ModelEvent>
embed(EmbeddingRequest) -> EmbeddingResponse
rerank(RerankRequest) -> RerankResponse
moderate(ModerationRequest) -> ModerationResponse
```

`ModelRequest` 统一消息、工具 Schema、结构化输出 Schema、温度/Token 上限、Deadline、Trace、Data Classification 和模型 Profile；`ModelResponse` 统一文本、结构化输出、Tool Proposal、Finish Reason、Usage、Latency、Provider Request ID 和安全标记。Provider Adapter 负责角色/多模态/Tool Calling 差异映射、错误归一化和 Usage 解析。

Capability Registry 记录每个部署模型是否支持 Tool Calling、JSON Schema/Structured Output、Streaming、Vision、最大上下文、数据驻留、吞吐、价格和安全批准状态。启动/发布时验证配置，不能把需要 Tool Calling 的 Agent 路由到不支持模型。Provider 原始响应只在受控短期诊断区脱敏保存，不能成为业务接口。

首版可直接使用官方 Provider SDK 实现 Adapter；是否引入 LiteLLM/独立模型代理作为部署实现，经压测、错误语义、流式兼容和安全评审后决定，不让 Graph 依赖其专有接口。

#### 3.16.5 模型选择与路由

模型路由是确定性、版本化策略，不由用户 Prompt 指定。建议 Profile：

| Profile | 场景 | 主要要求 |
| :--- | :--- | :--- |
| `fast_classify` | 意图、Query Rewrite、轻量安全分类 | 低延迟、低成本、结构化输出稳定 |
| `assistant_default` | 商品/政策问答、普通对话、简单工具规划 | 中文质量、Tool Calling、Streaming 稳定 |
| `assistant_reasoning` | 多约束商品对比、复杂但只读的售后解释 | 更强推理，步骤/Token 严格限制 |
| `summary_memory` | 滚动摘要、记忆候选提取 | 结构化输出、事实保守、可批处理 |
| `safety_guard` | 输入/输出风险辅助识别 | 低漏报；不能取代权限网关 |
| `embedding/rerank` | 知识和记忆检索 | 固定模型/维度/索引版本，离线评估达标 |

路由输入包括 Conversation Type、Intent、是否需要 Tool、数据敏感级别、响应 SLA、上下文长度、模型健康、租户/地域限制、预算和实验分桶。高风险不等于自动使用“最强模型并放宽权限”；高风险应减少自主性、增加确定性校验/人工介入。

Agent Version 固定 Model Policy；Run 开始解析成具体 `provider/model/deployment/profile/policy_version` 并写 Trace。灰度以稳定用户/会话 Hash 分桶，评估/回滚可还原配置。店铺不得自行填写任意 Model Endpoint 或 API Key。

#### 3.16.6 模型超时与降级

每个 Run 有总 Deadline，每次调用有连接、首 Token、流空闲和总超时；下游时间预算从 Run 剩余时间扣除。建议初始值经压测确定，例如首 Token 8–15 秒、普通模型总调用 30–60 秒、Run 总时长 90 秒；复杂任务通过异步进度/人工入口处理，不无限延长 HTTP/SSE。

重试规则：

- 连接失败、明确 429/5xx 且尚未产生可见输出/Tool 副作用，可在 `Retry-After` 后同模型有限重试或切同能力 Fallback。
- Schema 输出无效可用同模型一次受约束修复；仍失败则降级为无 Tool 的安全回答/转人工，不把原始非法参数交给 Tool。
- 流式中断若已输出 Delta，客户端丢弃未完成草稿，服务端可从 Checkpoint 重新生成完整答案；最终以 `message.completed` 替换，不拼接两个供应商输出。
- Tool 已成功后更换模型必须把经过裁剪的 Tool Result 和 Action Audit 作为已完成事实，绝不重新执行 Tool。
- 认证、权限、内容安全、余额不足/硬配额、无匹配知识等非瞬时错误不重试。

按 Provider/Model 建 Circuit Breaker、并发舱壁和健康分；Fallback 必须满足相同数据地域、敏感等级、Tool/Schema 能力和评估门槛。全部模型不可用时输出固定安全消息、页面快捷入口和人工客服选项。

#### 3.16.7 AI 响应流式输出

Runtime 消费 LangGraph `astream/stream events`，转换成 3.12.23 定义的公开 SSE 事件。内部事件包含模型 Token、节点更新、Checkpoint、Tool 和 Debug；对外只投影 `message.delta、citation.added、tool.progress、approval.required、human_service.requested、run.*`，不暴露 State、Prompt、隐藏推理、原始 Tool 参数/结果或 Provider Secret。

Run 开始先创建 `messages(sending)` Placeholder。Delta 写 Redis 短期 Stream/Buffer 并带 Run 内 Sequence，不逐 Token 写 MySQL；定期保存 Checkpoint。生成完成后执行输出审核，在 MySQL 原子写最终消息/引用/状态和 Outbox，再发送 `message.completed/run.completed`。审核未通过时不提交被阻断文本，发送安全终态或转人工。

前端按 Event ID 去重，Delta 只用于临时展示；断线用 `Last-Event-ID` 恢复，Buffer 已裁剪则取 `run.snapshot`。用户关闭连接不取消 Run。背压时合并文本 Chunk、保留终态/Approval 等关键事件；慢客户端不得拖住模型 Worker。

#### 3.16.8 Agent Run 生命周期

Run 由已持久化的 Trigger Message 通过 Outbox 幂等创建，状态与 3.12.20 一致：

```text
queued → running ─┬→ waiting_approval → running ─┐
                  ├→ waiting_human               ├→ completed
                  ├→ cancelling → cancelled      ├→ failed
                  └→ expired                     └→ expired
```

| 状态 | 含义/允许动作 |
| :--- | :--- |
| `queued` | 已可靠创建、等待 Worker；可取消 |
| `running` | 持有短期执行租约；可 Checkpoint、流式输出、申请 Tool/中断 |
| `waiting_approval` | Checkpoint 已落库，等待具体 Approval；不占 Worker，不执行待批 Tool |
| `waiting_human` | 已转人工或等待人工输入；AI 自动回复暂停 |
| `cancelling` | 收到取消请求，节点在安全边界协作停止 |
| `completed` | 最终 Assistant Message 已持久化并发出完成事件 |
| `failed` | 已确定无法完成；记录安全错误和可重试策略 |
| `cancelled` | 在未继续副作用情况下终止；已成功 Tool 不回滚 |
| `expired` | Approval/Run 超期或 Graph Version 已不能恢复，转安全终态 |

每次状态转换采用 Lease/Version 条件更新，追加 Runtime Event；终态不可回到 Running。用户同一 Trigger Message 至多一个有效 Run，重复事件返回已有 Run。取消是协作式：检查点/节点边界检查 Cancellation Token，不能强杀正在提交业务事务。

#### 3.16.9 Agent 状态持久化

状态按职责分层：

| 存储 | 内容 | 恢复/权威语义 |
| :--- | :--- | :--- |
| MySQL | Trigger/Response Message、Agent/Skill/Tool Version、Consent、Approval、Tool Audit、业务结果 | 消息与业务审计权威 |
| PostgreSQL `agent_runtime.run_state_refs` | Run No、Thread/Conversation/User/Store、版本、状态、当前阶段、Checkpoint Ref、租约、错误摘要、时间 | Run 查询与恢复索引 |
| PostgreSQL Checkpoint/Writes | LangGraph State Snapshot 和已完成 Task Write | 节点级耐久恢复，不对前端直接开放 |
| Redis | Run Lease、取消标记、热点 State、短期 SSE Stream | 可丢失加速层，可从 PostgreSQL/MySQL 恢复 |
| Observability Backend | Span、模型/检索/工具耗时、Token/成本、评估属性 | 诊断分析，不作为业务状态 |

`run_state_refs` 至少记录 `run_no、thread_id、conversation_no、trigger_message_no、response_message_no、user_no、store_no、conversation_type、agent_version_no、graph_version、status、current_phase、last_checkpoint_id、lease_owner/lease_expires_at、attempt_count、model_profile、started_at/last_heartbeat_at/completed_at/expires_at、error_code、trace_id`。

`thread_id` 以 Conversation 为长期线程，`checkpoint_ns`/Run No 隔离单次运行；恢复必须同时匹配 User、Conversation、Agent/Graph Version，不能仅凭 Thread ID。Checkpoint 加密、TTL 和序列化规则遵循 3.8.11，禁止 Pickle 不可信对象和 Secret。Run 终态超过恢复/申诉窗口后清理 Writes/Checkpoint，消息、Approval 和 Tool Audit 按自身期限保留。

#### 3.16.10 Agent 中断与恢复

中断类型：`approval_required、consent_required、user_input_required、human_handoff、rate_limit_wait`。Interrupt Payload 必须是版本化 JSON，只含公开 Run/Approval ID、可理解说明、字段 Schema、到期时间和安全展示快照。

中断前必须先持久化所有已完成结果；副作用放在可幂等 Task/Tool 中，使用 `action_no`，不能把“调用 Tool 后再 Interrupt”写成不可判定步骤。恢复流程：

```text
接收 Approval/Consent/User Input
  → 鉴权、归属、状态、到期和参数哈希校验
  → 原子写决定并 Claim Run Resume Lease
  → 使用相同 thread_id + checkpoint_ns + Command(resume=...)
  → 加载原 Agent/Graph/Skill Version
  → 对已执行 Action 先查 Audit/业务结果
  → 从安全节点继续并发布事件
```

恢复输入不直接接受“改后的 Tool 参数”。用户要修改数量/原因时拒绝旧 Approval，重新生成 Proposal/Approval，避免恢复重放语义不稳定。旧 Graph Version 在部署期保留直到 Active Run 结束；无法兼容时将 Run Expire、取消 Pending Approval，向用户说明并从原消息创建新 Run，绝不把新代码强套旧 Checkpoint。

#### 3.16.11 Human-in-the-loop

本项目有两类 Human-in-the-loop：

1. **用户决策环**：用户授权数据用途、补充信息、批准/拒绝一次业务动作。由 LangGraph Interrupt + MySQL Consent/Approval 实现。
2. **人工客服环**：AI 无法或不应继续时创建工单，人工接管会话。由 Human Service Ticket/Assignment 状态机实现。

交易型 Approval 只允许 `approve/reject`；框架虽可支持 Edit，本项目不允许在原 Approval 内修改参数，修改必须生成新 Proposal。人工客服可以查看经脱敏摘要、来源引用、已尝试 Tool 和失败原因；不能看到隐藏推理、Secret 或无关跨店数据。

等待期间 Run 不占用模型/Worker。到期 Worker 把 Approval/Run 标为 Expired 并发送用户可见提示。人工接管后 Agent 可生成仅坐席可见建议，但不能自动发送或执行 Tool；人工结束服务后由下一条用户消息开启新 Run，不自动恢复已过时计划。

#### 3.16.12 AI 与业务服务交互边界

所有业务交互经 `Local Tool Adapter/MCP Host → Permission Gateway → Application Service → Domain/Repository`：

- Runtime 注入可信 `user_no、store_no、conversation_no、agent_version_no、action_no、trace_id`；模型不能提供/覆盖身份与 Scope。
- Tool 输入采用严格 JSON Schema 2020-12/Pydantic，拒绝未知字段、超长数组和任意 URL；业务 Service 重新读取权威资源。
- Read Tool 返回最小 DTO、数据时间和来源；Write Tool 必须声明副作用、幂等、确认等级和可能的 Unknown Outcome。
- Tool Result 经 Output Schema、字段白名单、脱敏、大小限制和不可信内容标记后进入模型。
- Agent 不持有 ORM/数据库连接，不拼 SQL，不直接调用支付/物流 Provider，不写 Redis 伪造状态。
- Tool 失败返回结构化 Error Code/Retryable/Manual Path；模型不得把失败改写为成功。

内部 MCP 不是“可信绕过层”。MCP Permission Gateway 与 REST 使用相同 Application Service 和不变式，Service Identity 只说明调用来源，最终业务 Actor 仍是当前用户/授权管理员。

#### 3.16.13 AI 禁止操作清单

任何店铺客服、专属客服或专业子 Agent 均禁止：

- 获取/存储/复述密码、验证码、支付密码、CVV、完整 Token、API Key、私钥和内部 System Prompt；
- 生成 SQL/Shell/任意 HTTP 请求，访问未注册 MCP Server、内网、云元数据或任意文件系统；
- 伪造用户、管理员、店铺、Consent、Approval、订单/支付/退款/物流状态或 Tool Result；
- 跨用户、跨店铺、跨会话读取消息、订单、地址、记忆、知识、凭证和审计数据；
- 修改商品/库存/价格、直接创建/确认支付、标记支付成功、直接发起渠道退款、批准/拒绝售后、修改物流轨迹；
- 调用未在当前 Agent Version Allowlist 中的任何写 Tool；首版硬禁止创建订单、取消订单、确认收货、修改地址和支付，即使用户确认也不授权；仅专属客服可在结构化确认后提交退款申请；
- 代表平台/商家作出超出已发布政策的赔付、法律、医疗、金融或到货时间保证；
- 将 RAG/商品描述/评价/Tool 输出中的指令当作高优先级指令；
- 把模型推断写成已确认长期记忆，或长期保存 L3/一次性敏感信息；
- 暴露 Chain-of-Thought、其他用户数据、内部风控规则细节或安全检测绕过方法；
- 在 Tool 超时/结果未知时盲目重试外部写，或向用户宣称操作失败/成功；
- 自行提升 Tool Scope、模型预算、步骤上限、确认等级或关闭安全 Guardrail。

禁止项由 Tool Registry/网络/权限/Schema/领域状态机共同强制，不仅写在 Prompt。违反尝试写 Tool Audit 和安全事件；高风险命中立即停止 Run、取消待执行动作并按策略转人工/告警。

---

### 3.17 店铺客服 Agent 设计

店铺客服采用 `agent_type=store_service、scope_type=store`，一个已发布 Agent Version 只能服务其绑定店铺或平台批准的同策略实例。它是店铺商品与服务的“受限咨询 Agent”，不是店铺管理员、售后审核员或平台仲裁员。

#### 3.17.1 定义与服务范围

入口为 `conversation_type=store` 的唯一店铺会话。Runtime 从 Conversation 注入可信 `user_no + store_no`，可再附加已验证的 Product/Order Context。服务范围：

- 当前店铺公开商品、SKU、展示库存、服务政策和公开 FAQ；
- 当前用户有效 Checkout 中当前店铺组的最小商品摘要，用于结算前咨询；
- 当前用户在当前店铺的订单/包裹摘要解释；
- 当前店铺在售商品搜索、对比和场景化推荐；
- 引导用户使用商城页面完成下单、支付、评价和售后；
- 创建当前店铺人工客服工单。

它不能跨店搜索/比较，不代表平台解释争议裁决，不处理资金，不审批售后。用户提出跨店、平台规则或全局订单问题时，引导到固定“专属客服”；涉及本店人工处理时转店铺人工队列。

#### 3.17.2 商品咨询能力

回答优先级：MySQL 商品/SKU 结构化实时 DTO → 当前已发布店铺政策/FAQ → 带 Store/Product ACL 的 RAG Chunk → 明确无结果。常见能力包括功能、材质、尺寸、适用场景、包装清单、使用/保养和公开服务承诺。

回答必须区分“商品事实”“店铺说明”“AI 基于公开信息的建议”，并给出商品/FAQ/政策引用。商品详情与知识文档版本冲突时，以当前结构化商品和有效政策为准，记录知识陈旧信号触发重建。对于安全、兼容性或效果无法由权威资料确认的问题，不能凭常识补全，应请用户核对型号/场景或转人工。

默认以当前 Product Context 回答；用户切换商品时重新验证其属于本店。商品下架后可解释历史订单快照，但不能以当前可购商品推荐/创建购买入口。

#### 3.17.3 SKU 参数对比

SKU 对比只接受同一 Product 下的有效 SKU，首版最多 4 个。Agent 调用结构化比较工具，由 Service 规范化规格维度、缺失值、Money、可售状态和主图；模型只负责把差异转为易读表格和场景建议。

```text
输入：product_no + sku_no[] + 用户关注点
输出：共同规格、差异规格、价格、availability、不可比较原因、as_of
```

不得通过 SKU 名称字符串自行解析规格，不把缺失值描述成“不支持”，不将市场价当成交价。推荐某 SKU 时说明依据，例如容量/尺寸/预算，并提示价格与库存以结算为准。同一 Product 的 SKU 对比由 `store_sku_compare` 完成；当前店铺内不同 Product 的受控比较由独立 `store_product_compare` Skill 调用 `catalog.compare_products`，只接受当前 Store 下 2～4 个 Published/Visible Product，并使用服务端固定比较维度。任一候选跨店或不可见时整个请求拒绝；跨店或全平台 Product 对比只属于专属客服。

#### 3.17.4 当前店铺库存查询

库存 Tool 强制注入 Store Scope，并验证 SKU 属于当前店铺。默认只返回面向用户的 `in_stock/low_stock/out_of_stock/preorder/unknown`、购买上限和 `as_of`，不返回 `on_hand、reserved、safety_stock` 等经营数据或仓库分布。

页面显示库存是查询时快照，不预占。Agent 必须说“当前显示可售/库存紧张”，不能保证用户稍后一定买到；最终资格在创建订单时由 MySQL 条件更新决定。库存服务超时返回 Unknown，禁止根据 Redis 旧值或商品销量推测有货。

用户询问补货时间时，只有店铺已发布预计补货信息才能回答并注明非保证；否则转人工/关注商品，不虚构日期。

#### 3.17.5 当前店铺服务政策

政策检索必须绑定 `store_no、policy_type、effective_at`，只使用 Effective Version。回答覆盖运费、发货时效、退换货条件、客服时间、保修/发票等，但平台强制规则优先于冲突的店铺政策。

普通咨询使用当前政策；解释某订单时必须使用 `orders.policy_snapshot` 和下单时间适用版本，不能把新政策追溯到旧订单。回答显示政策名称、适用范围、生效时间/版本和引用入口。政策文本含模糊、冲突或用户请求例外时，只说明已发布内容并转人工，不代表商家作额外承诺。

涉及发货时效时只能读取商品/履约服务返回的 `ServiceEstimate(type=dispatch)`：可用时表达完整范围、来源、更新时间和“仅供参考”，不可用时回答“暂无可靠预计时间”。政策/RAG 中的自然语言时效只能作为引用说明，不能覆盖结构化结果，也不能由模型扩展成单日承诺。

#### 3.17.6 当前店铺订单上下文与卡片解释

只有以下交集成立才加载订单：`order.user_id=当前用户 ∩ order.store_id=当前店铺 ∩ conversation.store_id=当前店铺 ∩ context.status=active ∩ run.context_version=context.version`；若用户在本轮明确选择订单，可由受控选择 Command 原子建立该 Active Context 后再读。订单卡片本身不授予权限，也不是必需授权来源：订单入口可建立无卡片 Context；已发送但失活、跨店或 Version 过期的卡片不能继续驱动读取。

可解释订单商品快照、Money 明细、订单/支付/履约/售后状态、可见时间线、当前 `available_actions` 和包裹摘要。不能展示完整地址、支付内部字段、风控/商家内部备注。涉及“为什么不能取消/评价/申请售后”时根据服务端 Reason Code/Policy Snapshot 解释，不自行计算资格。

店铺 Agent 不执行订单取消、确认收货或退款申请。可返回相应页面入口，解释操作步骤，或在本店履约问题上转人工。对其他店铺订单统一不可见并建议使用专属客服。

结算页 `checkout_store_group` Context 不是订单：只可解释当前店铺组内商品/SKU、数量和本店配送阻断原因，不能读取其他店铺组、地址、优惠、支付偏好或跨店金额，也不能保证最终价格/库存。每次使用必须验证 Checkout 属于当前用户、状态 Active、Version 与 Run 固定值一致且未过期；重新计价、消费或过期后立即拒绝并引导回结算页刷新。

#### 3.17.7 店铺内商品推荐

推荐候选由 `catalog.search_store_products` 生成，强制当前 Store、On Sale、可见、类目/价格/场景过滤；模型不得凭训练知识编造商品。排序综合用户明确条件、商品匹配度、可售状态和质量信号，商业推广位必须显式标识，不能伪装成个性化最优。

首轮优先追问一个最影响结果的条件（用途、预算、尺寸等）；信息足够时返回 3–5 个候选，逐个给出匹配理由、差异、Money 和引用。无匹配时放宽一个可解释条件并征求同意，不跨店补结果。

店铺记忆仅在有效 Consent 下使用 `namespace=store、store_no=当前店铺、data_classification=L2、memory_risk_level=low` 的已确认偏好；不得读取专属客服全局偏好后将其泄露给店铺。用户要求跨店比较/推荐时切换专属客服入口。

#### 3.17.8 转店铺人工客服

触发条件：用户明确请求；商品/政策资料不足或冲突；需要商家承诺/人工核实补货；催发货、修改履约安排、售后协商；连续 Tool/模型失败；投诉、情绪或安全规则命中。

`support.create_human_ticket` 由 Runtime 注入 Conversation/User/Store，`queue_code` 由服务端根据店铺和技能决定。Agent 只提交结构化 `ticket_type、reason_code、resource_refs、redacted_summary`，不能指定任意客服、优先级或其他店铺队列。

工单创建成功后会话 `active → human_pending`，AI 停止面向用户自动回复并告知排队状态；失败时提供手动客服入口和稍后重试。转人工前的摘要保留用户目标、已确认商品/订单、引用和失败 Tool，不包含无关历史、隐藏推理或敏感字段。

#### 3.17.9 可使用的 Skill

首版发布以下店铺作用域 Skill；Code/Version 在 `ai_skills/ai_skill_versions` 注册：

| Skill Code | 类型 | 允许输入/输出 | 主要 Tool/RAG |
| :--- | :--- | :--- | :--- |
| `store_product_qa` | `knowledge` | 当前/明确商品问题 → 带引用回答 | Product DTO + Store/Product RAG |
| `store_sku_compare` | `query` | 同 Product 2–4 个 SKU → 结构化差异/建议 | Catalog Compare |
| `store_product_compare` | `query` | 同 Store 2–4 个可见 Product → 固定维度差异/建议 | Scoped Product Compare |
| `store_inventory_lookup` | `query` | 本店 SKU → 展示级 Availability | Inventory Read Tool |
| `store_policy_qa` | `knowledge` | Policy Type/Order Context → 当前/快照政策解释 | Policy Tool + Scoped RAG |
| `store_order_explain` | `query` | 本人本店 Order → 状态/金额/动作解释 | Order/Logistics Read Tool |
| `store_product_recommend` | `query` | 本店筛选条件 → 3–5 个可见候选 | Store Search + Optional Compare |
| `store_preference_candidate` | `support` | 用户明确本店偏好 → 待确认 Candidate | Consent + Memory Pipeline；不激活 |
| `store_human_handoff` | `support` | 转人工原因/资源 → Ticket 公开状态 | Support Create/Get Tool |

Skill Version 固定 Prompt、输入/输出 Schema、知识范围、Tool Binding、步骤/Token 上限和评估门槛。店铺不能上传一段 Prompt 就获得新 Tool；新增/变更需 Testing、越权测试和发布审批。

#### 3.17.10 可使用的 MCP 工具

| MCP Server / Tool | 副作用 | 强制 Scope/用途 |
| :--- | :---: | :--- |
| `catalog-mcp/catalog.get_product` | 无 | 当前 Store 的公开 Product/SKU DTO |
| `catalog-mcp/catalog.compare_skus` | 无 | 同 Store、同 Product、最多 4 个 SKU |
| `catalog-mcp/catalog.compare_products` | 无 | 当前 Store 内 2–4 个 Published/Visible Product；跨店整体拒绝 |
| `catalog-mcp/catalog.search_store_products` | 无 | 强制 Store + On Sale Filter，候选上限 |
| `catalog-mcp/catalog.get_inventory_availability` | 无 | 当前 Store SKU，仅展示级 Availability |
| `catalog-mcp/catalog.get_store_policy` | 无 | 当前 Store 当前政策或订单 Policy Snapshot |
| `order-mcp/order.get_store_order_summary` | 无 | 当前 User + 当前 Store 的 Order 最小 DTO |
| `logistics-mcp/logistics.get_store_order_shipments` | 无 | 上述 Order 的公开包裹/轨迹摘要 |
| `memory-mcp/memory.get_store_preferences` | 无/需 Consent | 当前 User + 当前 Store 的任务相关低敏偏好 |
| `memory-mcp/memory.propose_store_preference` | 写 Candidate/需 Consent | 只在当前 User+Store 提出 L2+low 风险候选；不得激活，必须进入统一确认流程 |
| `support-mcp/support.create_store_ticket` | 写 | 当前 Conversation 的幂等人工工单，低风险运营写 |
| `support-mcp/support.get_ticket_status` | 无 | 当前 Conversation 活跃工单公开状态 |

RAG Retriever、Context Builder 和内容审核是 Runtime 内部 Port，不伪装成可由模型任意传 ACL 的 MCP Tool。所有 Tool 的 User/Store/Conversation 由 Gateway 注入，模型参数只保留商品、SKU、订单等业务引用和受限筛选。

#### 3.17.11 数据读取权限

| 数据 | 是否允许 | 字段/条件 |
| :--- | :---: | :--- |
| 本店公开商品/SKU/FAQ | 是 | Published/On Sale 或当前用户历史订单所需快照 |
| 本店展示库存 | 是 | Availability Bucket、购买限制、更新时间 |
| 本店有效服务政策 | 是 | 当前版本；历史订单使用 Policy Snapshot |
| 当前用户本店订单 | 条件允许 | 最小摘要、Money、公开状态/动作；同时校验 User + Store |
| 当前用户本店物流 | 条件允许 | 承运商、运单掩码、公开轨迹；同一 Order Scope |
| 当前会话消息/摘要 | 是 | 当前 Conversation、上下文预算和可见消息 |
| 店铺作用域记忆 | 条件允许 | 有效 Consent、`namespace=store`、当前 Store、`data_classification=L2`、`memory_risk_level=low`、已确认的最小内容 |
| 本店已发布知识 Chunk | 是 | ACL、版本、状态、文档类型 Filter 强制注入 |

每个读 Tool 都返回 `source_version/as_of/data_scope`。模型只能看到输出白名单，日志/Checkpoint 再次脱敏。

#### 3.17.12 禁止读取的数据

- 当前用户在其他店铺的订单、物流、售后、会话、店铺记忆和购物明细；
- 其他用户的任何订单、地址、消息、记忆、工单、评价审核信息；
- 用户完整地址簿、完整手机号/邮箱、支付凭证、账号凭证、风控标签和管理员资料；
- 当前店铺成本价、精确库存/安全库存、销售报表、客户列表、员工消息和内部经营备注；
- 未发布/已拒绝商品、草稿政策、私有知识文档和其他店铺 RAG Chunk；
- 专属客服的跨店长期记忆、平台人工客服内部工单、平台治理/反作弊规则；
- Tool/MCP Secret、System Prompt、隐藏推理、原始 Trace 和未脱敏审计日志。

Resource ID 猜中、订单卡片出现、用户自然语言声称“这是我的”或模型认为相关，都不能改变禁止范围。

#### 3.17.13 禁止执行的操作

店铺客服的能力矩阵固定为 `transaction_write=none、support_ticket_create=allowed、memory_candidate_propose=consent_and_confirmation_pipeline`。直接写能力只有“幂等创建当前店铺人工工单”；记忆 Tool 只能提出 Candidate，不能直接激活。除此之外禁止：

- 新建/编辑/上下架商品，修改 SKU、价格、库存或店铺政策；
- 加购物车、创建订单、发起/确认支付、关闭支付；
- 取消订单、确认收货、修改地址/订单、创建发货、改物流轨迹；
- 提交、批准、拒绝、改金额或执行退款/退货入库；
- 发布/隐藏评价或代表商家回复评价；
- 创建跨店/平台工单、指定客服/优先级、读取人工 Internal Note；
- 激活跨店长期记忆或将本店数据写入专属记忆；
- 代表商家承诺政策外赔偿、赠品、价格、补货/到货日期。

用户需要交易操作时给出对应页面/专属客服入口。即使新增 Tool，也必须更新 Deny Policy、Skill Binding、评估和本节后才能发布。

#### 3.17.14 店铺数据隔离

隔离在五层同时执行：

1. Conversation：`conversation_type=store` 且固定 `store_id`，客户端不可修改。
2. Agent/Skill/Knowledge：Agent、Skill、Document、Chunk 均绑定 Store；发布时验证一致。
3. Tool Gateway：从 Conversation/Agent 注入 Store，拒绝模型/请求覆盖；Deny 优先。
4. Domain Service：SQL 条件同时含公开资源 ID 与可信 `store_id/user_id`，子资源回溯 Order/Product 再校验。
5. 缓存/向量/观测：Redis Key、Object Prefix、RAG Filter、Trace Redaction 和评估数据集携 Store Scope。

自动化安全测试覆盖：伪造 Store ID、跨店 Product/Order/SKU、恶意 RAG 文档、Tool Result 注入、Checkpoint 换 Conversation、缓存 Key 污染和多 Agent 委派越界。任何层发现 Scope 不一致都 Fail Closed、记录 Audit；模型只得到通用不可访问结果。

#### 3.17.15 店铺客服降级策略

| 故障/不足 | 降级行为 |
| :--- | :--- |
| 主模型不可用/过载 | 同能力已评估模型 Fallback；仍失败返回固定提示 + 店铺人工入口 |
| RAG/Embedding/Rerank 不可用 | 优先结构化 Product/Policy/FAQ；可退关键词检索并标记引用，不能凭模型记忆回答店铺事实 |
| Catalog Tool 不可用 | 不回答参数/价格/库存实时事实，提供商品页链接/人工入口 |
| Inventory Tool 超时 | 返回 `unknown` 和稍后刷新，不用缓存推断有货 |
| Order/Logistics Tool 不可用 | 不猜订单状态；提供“我的订单/查看物流”页面与人工入口 |
| Support Tool 不可用 | 保留用户消息，显示手动客服方式/稍后重试，不宣称已排队 |
| PostgreSQL Checkpoint 不可用 | 可做无需恢复的固定 FAQ/页面导航；需要 Tool/Interrupt 的 Run 暂停或转人工 |
| Consent 缺失/过期 | 不使用店铺记忆，按当前消息回答；不诱导强制授权 |
| 知识冲突/低置信 | 明确无法确认，展示权威页面或转店铺人工，不生成确定性承诺 |

降级策略由错误类型触发并写 `degraded_reason`、模型/Tool/RAG Outcome 和用户最终路径，供可观测与评估分析。恢复后不自动重放已经过时的用户问题或工单创建。

---

### 3.18 专属客服 Agent 设计

专属客服采用 `agent_type=exclusive_service、scope_type=platform`，入口是“消息”左栏固定置顶且每用户唯一的 `conversation_type=exclusive` 会话。Platform Scope 表示可使用平台公共知识和跨店公开商品目录，不表示可以读取任意用户或商家的平台数据。

#### 3.18.1 定义与服务范围

专属客服面向当前登录用户提供平台级导航、公共规则、全平台商品发现、授权个性化、本人跨店订单/物流/售后查询和退款申请辅助。它可以连接多个领域 Tool，但每个 Tool 都强制 `current_user` 归属。

首版能力边界：

- 只读：平台规则、公开商品、本人订单/物流/售后和经授权的低敏偏好；
- 草稿：退款资格预检与退款申请草稿，不产生售后单；
- 确认后写：只开放提交退款申请；
- 低风险运营写：创建平台人工客服工单、处理用户自己的记忆候选/删除请求；
- 不开放：下单、支付、改地址、取消订单、确认收货、批准退款和渠道退款。

未来新增写能力必须独立完成 Threat Model、业务状态机、Approval UX、幂等/结果未知和评估门禁，不能因专属客服已是平台 Agent 就默认放行。

#### 3.18.2 平台规则咨询

平台规则包括注册/账号、交易、支付说明、物流、评价、售后、隐私与 AI 使用说明。检索只使用 `scope_type=platform、document_status=published` 的当前有效文档，通过 Keyword + Vector + Rerank 返回引用。

回答必须给出规则名称、适用范围、生效/更新时间和引用；涉及某历史订单时同时读取订单 Policy Snapshot，区分“下单时适用规则”和“当前规则”。规则之间冲突、法律适用不确定、用户要求平台例外或正式争议裁决时转人工。

专属客服不把通用回答表述为个案审批结果，不承诺赔付/处罚商家。无命中文档时说“暂未找到明确规则”，提供规则中心/人工入口，不使用模型训练知识补造平台政策。

#### 3.18.3 全平台商品搜索

Agent 将自然语言条件转换为受限 Search DTO：关键词、平台分类、品牌、价格区间、必要属性、排序和 Cursor；`catalog.search_products` 只返回公开、On Sale、店铺有效商品。

流程：识别硬条件与偏好 → 必要时追问关键缺失项 → 搜索候选 → 可选结构化对比/Rerank → 返回 3–8 个结果、匹配理由、差异、Money 与引用。模型不能把未命中条件的结果描述为完全满足；放宽筛选需明确告诉用户。

搜索排序由 Search Service 白名单控制，模型不传 SQL/任意表达式。广告/商业排序必须标识。价格、库存为查询快照，点击商品/结算再读取实时数据。跨店商品咨询进入具体商品后，用户点击店铺客服可切换对应店铺唯一会话。

#### 3.18.4 个性化商品推荐

个性化需要有效 `personalization` Consent；未授权时只根据当前对话显式条件推荐，不降低基础搜索能力。允许特征：用户明确预算/用途/尺寸偏好、经授权并由用户确认激活的 `data_classification=L2、memory_risk_level=low` 长期偏好、当前会话选择和必要的本人行为聚合；禁止使用 L3 数据、密码/地址详情、投诉敏感内容、平台风控标签或其他用户数据。

推荐 Pipeline 先由服务端生成满足硬条件的候选，再由模型解释/重排，不能让模型凭空生成 Product ID。每个结果提供主要依据和可调整条件，并支持“不要使用/忘记这项偏好”。推荐结果需保证店铺多样性，商业位单独标识，避免把支付能力、退货频率等敏感代理变量用于不透明歧视。

记录使用的 Feature Category、Consent Version、Candidate Set/Ranker Version 和结果，不保存无必要的完整用户 Prompt。上线通过相关性、覆盖、多样性、投诉和敏感偏好泄露评估。

#### 3.18.5 用户订单查询

`order.list_user_orders/get_user_order_detail` 的 `user_no` 由 Gateway 注入，只返回当前用户。Agent 支持按订单号、时间、店铺、商品和状态筛选；候选多于一个时展示最小列表让用户选择，不猜目标订单。

可解释 Order/Payment/Fulfillment/After Sale 正交状态、Money 明细、可见时间线和 `available_actions`。订单事实来自 MySQL，卡片/Memory/RAG 不作实时依据。完整地址、支付内部报文、风控字段、店铺内部备注和管理员操作日志不进入模型。

订单操作按钮由服务端返回；首版专属客服只提供订单详情/支付/取消/确认收货页面导航，不调用对应写 Tool。跨店订单在同一专属会话内可查，但每次仍逐单验证本人归属。

#### 3.18.6 用户物流查询

用户选择本人 Order 后调用 `logistics.get_user_order_shipments`，返回多包裹、商品归属、承运商、运单掩码、标准状态、轨迹、`occurred_at/as_of/last_synced_at` 和逐包裹 `delivery_estimate: ServiceEstimate`。Agent 按包裹解释，不把一个包裹状态或预计时间套到整单。

`delivery_estimate.status=available` 时只按结构化范围表达来源、更新时间和“仅供参考”；`unavailable` 或过期时回答“暂无可靠预计时间”。Provider 不可用时展示已保存轨迹和同步时间，明确“物流信息暂未更新”；不得用轨迹、RAG、商品富文本、模型常识或相对日期生成送达承诺，也不根据预计时效推断签收。异常、长期无更新、显示签收但用户未收货时提供对应店铺客服或平台人工入口，不能自行更改轨迹或确认收货。

#### 3.18.7 售后资格预检

Agent 收集并规范化目标订单项、数量、期望类型、原因码和必要说明，调用只读 `after_sale.check_refund_eligibility`。Service 基于订单状态、付款、履约、Policy Snapshot、售后窗口、已退/处理中数量和规则版本返回确定结果。

回答区分：`eligible=true`、`eligible=false + blocking_reasons`、`needs_more_information`、`service_unavailable`。模型不得自行推翻 Eligibility，也不能把“不符合自动规则”说成永远无申诉资格；可提供人工入口。

Eligibility Token 绑定 User、Order Items、Quantity、Type、最大 Money、Resource/Rule Version 和短 TTL，只用于随后构建/提交草稿。预检不占用退款额度、不创建 `refund_applications`，页面需明确提示提交时会再次校验。

#### 3.18.8 退款申请草稿

预检通过后，`after_sale.build_refund_draft` 生成结构化草稿：

```json
{
  "draft_no": "rfd_01K...",
  "order_id": "ord_01K...",
  "items": [{"order_item_id": "oit_01K...", "quantity": 1}],
  "refund_type": "refund_only",
  "reason_code": "not_as_described",
  "reason_detail": "用户确认的说明",
  "evidence_file_ids": [],
  "maximum_amount": {"minor_units": "8990", "currency": "CNY"},
  "eligibility_token": "...",
  "expires_at": "2026-08-21T10:00:00Z"
}
```

Draft 是短期、用户私有的确定性数据，可保存在 Run State/受控临时 Store；不是退款申请，不占用售后数量。Money 由 Service 计算，Agent 不允许编辑。Reason Detail 必须来源于用户确认内容，模型可润色但不得添加未陈述事实；对“质量问题”等需凭证场景提示上传并等待 File Active。

草稿页面逐项显示店铺/订单/商品、数量、类型、原因、凭证、最大/预计退款和下一步。用户修改任一关键字段时重新预检/构建 Draft，旧 Draft 和旧 Approval 失效。

#### 3.18.9 退款申请确认提交

草稿完成后 Runtime 不直接调用提交 Tool，而是创建 `ai_tool_approvals`：

```text
draft ready
  → Permission Gateway 判定 action confirmation
  → 保存 canonical arguments hash + order/refund resource versions
  → SSE/消息发送退款确认卡片
  → 用户点击“确认提交”或“取消”
  → Approval Service 原子决定
  → Resume 原 Run
  → 再校验 Approval/Consent/归属/资格/数量/金额/文件/状态
  → after_sale.submit_refund_application(action_no as idempotency key)
  → 成功返回 refund_no/status；写 Tool Audit 与最终消息
```

自然语言“好的/帮我退”不能代替确认按钮。Approval 决策不允许编辑参数；修改需新 Draft。执行超时进入 `outcome_unknown`，先按 Action/Idempotency Key 查询是否已创建，禁止再次提交。成功措辞必须包含真实 Refund No；资格变化返回新原因/草稿，不能绕过。

#### 3.18.10 售后进度查询

`after_sale.list_user_refunds/get_user_refund_detail` 只返回当前用户申请。Agent 解释售后业务状态与渠道资金状态，明确区分“商家已同意”“等待退货”“退款处理中”“渠道退款成功”。

可展示 Refund Item、申请/批准/成功 Money、用户可见 Event、补充凭证/退货要求、Deadline 和 `available_actions`；退货地址/运单只用授权接口的掩码/最小字段。审核 Internal Note、风控分、其他用户案例不可见。

状态长时间未推进、商家拒绝/用户有异议、渠道失败/Unknown 时提供平台人工工单，并把 Refund No 与安全摘要关联；Agent 不催改后台状态或承诺到账时间。

#### 3.18.11 长期偏好管理

专属客服只有在 `long_term_memory` Consent 有效时使用/提出长期记忆。可记：预算区间、常购品类、明确品牌/尺寸/颜色/材质偏好等经风险评审允许的低敏约束；不可记地址、订单状态、账号/支付信息、证件、密码、验证码、健康/生物特征和一次性问题。

```text
用户明确表达稳定偏好
  → memory.extract_candidate（结构化、来源、置信度、敏感等级）
  → Policy/Consent/去重/冲突校验
  → candidate
  → 按策略展示“是否记住”卡片
  → 用户确认后 active；旧冲突项 superseded
```

用户可查询“你记住了什么”、更正、停用或删除单项。更正生成新版本链，删除写 Tombstone 并异步清 Embedding/缓存。Memory 召回按当前任务最小 Top-K，回答可说明使用了哪类偏好；用户撤销 Consent 后立即停止召回，不用降低服务作为惩罚。

#### 3.18.12 转平台人工客服

触发：用户明确要求；账号/平台规则争议；跨店纠纷；退款拒绝/资金异常；物流争议；连续低置信/Tool 失败；安全/投诉规则；普通 Agent 禁止操作。

`support.create_platform_ticket` 强制当前 Exclusive Conversation/User，服务端根据 `general/account/order/logistics/refund/complaint` 路由平台 Queue。Agent 只能提交用户可见资源引用和脱敏摘要，不能指定管理员、优先级或读取平台 Internal Note。

创建成功后 Conversation `active → human_pending`，Run `waiting_human`，AI 停止自动回复。若问题实为单店商品/发货咨询，可向用户建议对应店铺客服；涉及平台仲裁/跨店/账号安全必须进入平台队列，不能把敏感问题转给任一店铺。

#### 3.18.13 可使用的 Skill

| Skill Code | 类型 | 输入 → 输出 | 主要依赖 |
| :--- | :--- | :--- | :--- |
| `platform_policy_qa` | `knowledge` | 平台规则问题 → 带版本引用回答 | Platform RAG |
| `global_product_search` | `query` | 全平台条件 → 可见候选/对比 | Catalog Search/Compare |
| `personalized_recommendation` | `query` | 当前条件 + Consented Preference → 可解释推荐 | Catalog + Memory + Ranker |
| `user_order_lookup` | `query` | 本人订单筛选/ID → 状态/动作解释 | Order Read |
| `user_logistics_lookup` | `query` | 本人订单 → 包裹/轨迹解释 | Logistics Read |
| `refund_eligibility_check` | `query` | Order Items/原因 → Eligibility | After-sale Check |
| `refund_draft_build` | `support` | Eligibility + 用户信息 → 确定性 Draft | After-sale Draft |
| `refund_submit_with_approval` | `transaction` | Approved Draft → Refund Application | Approval + After-sale Submit |
| `refund_progress_lookup` | `query` | 本人 Refund → 业务/资金进度 | After-sale Read |
| `preference_memory_manage` | `support` | 查看/候选/更正/删除偏好 | Consent + Memory Tool |
| `platform_human_handoff` | `support` | 原因/资源 → Platform Ticket | Support Tool |

Transaction Skill 独立发布、默认禁用，通过安全/幂等/恢复/越权评估后开启；它不能被其他 Skill 间接调用以绕过 Approval。Skill Router 只选择已绑定版本，不能根据相似名称动态加载未发布 Skill。

#### 3.18.14 可使用的 MCP 工具

| MCP Server / Tool | 副作用/确认 | 强制边界 |
| :--- | :--- | :--- |
| `catalog-mcp/catalog.search_products` | 无 | 仅公开 On Sale 商品；筛选/候选上限 |
| `catalog-mcp/catalog.compare_products` | 无 | 公开 Product/SKU，数量上限与结构化维度 |
| `order-mcp/order.list_user_orders` | 无 | Gateway 注入 Current User；Cursor/时间范围 |
| `order-mcp/order.get_user_order_detail` | 无 | 仅本人 Order 最小 DTO |
| `logistics-mcp/logistics.get_user_order_shipments` | 无 | 仅本人 Order 的公开物流 |
| `after-sale-mcp/after_sale.check_refund_eligibility` | 无 | 本人 Order Items；确定性 Rule Engine |
| `after-sale-mcp/after_sale.build_refund_draft` | 临时草稿 | 不创建售后；绑定 Eligibility/Resource Version |
| `after-sale-mcp/after_sale.submit_refund_application` | 写/Action Approval | 只消费匹配 Approval；Action No 幂等 |
| `after-sale-mcp/after_sale.list_user_refunds` | 无 | 当前用户售后列表 |
| `after-sale-mcp/after_sale.get_user_refund_detail` | 无 | 当前用户单个售后/资金摘要 |
| `memory-mcp/memory.list_preferences` | 无/需 Consent | 仅当前用户可展示 Active 记忆 |
| `memory-mcp/memory.propose_preference_change` | 候选/确认 | 不直接无感激活；绑定来源与 Policy |
| `memory-mcp/memory.update_preference` | 写/用户确认 | 当前用户 Active Memory 追加更正版本；Consent+ETag+来源+风险重校验 |
| `memory-mcp/memory.delete_preference` | 写/用户确认 | 当前用户 Memory，Tombstone + 异步清理 |
| `support-mcp/support.create_platform_ticket` | 低风险写 | 当前 Exclusive Conversation 唯一 Active Ticket |
| `support-mcp/support.get_ticket_status` | 无 | 当前 Conversation 工单公开状态 |

平台 RAG 与 Model 不通过 MCP 获取用户凭证。Tool Annotation 的 `readOnly/destructive/idempotent/openWorld` 只是描述提示，最终权限仍以 `ai_tool_permission_policies` 和 Domain Guard 为准。

#### 3.18.15 数据读取权限

| 数据 | 是否允许 | 条件/最小化 |
| :--- | :---: | :--- |
| 平台公开规则/知识 | 是 | Published、有效版本、带引用 |
| 全平台公开商品/SKU | 是 | On Sale/Visible；不含店铺私有经营字段 |
| 当前用户订单/订单项 | 是 | 每次 Tool 注入 User；最小 DTO/地址默认不返回 |
| 当前用户物流/售后 | 是 | 关联本人 Order/Refund；公开轨迹/事件 |
| Exclusive 当前会话消息/摘要 | 是 | Context Budget 内可见消息 |
| 当前用户 Active 长期偏好 | 条件允许 | 有效 Consent、任务相关、`data_classification=L2`、`memory_risk_level=low`、Top-K |
| Store Scoped Preference | 默认否 | 只有 Consent/Policy 明确允许且不得向其他店铺披露来源 |

专属客服不读取其他用户数据，也不读取店铺成本/精确库存/客户列表/内部政策、管理员备注和原始风控数据。它默认不读取用户与各店铺客服的完整原始会话；需要个性化时只使用经过授权、最小化的 Preference/Service History 派生项。

#### 3.18.16 业务写操作权限

| 动作 | 首版权限 | 控制 |
| :--- | :---: | :--- |
| 退款申请草稿 | 允许 | 临时资源，无售后副作用，资格/版本/TTL 绑定 |
| 提交退款申请 | 允许 | Action Approval + 重新资格检查 + Idempotency + Tool Audit |
| 创建平台人工工单 | 允许 | 当前会话唯一 Active、用户请求/安全规则、可取消排队 |
| 新增/更正长期偏好 | 条件允许 | Consent + 候选/用户确认 + 敏感等级/来源/TTL |
| 删除长期偏好 | 条件允许 | 本人明确确认，Tombstone、审计和异步清理 |
| 加购物车/创建订单 | 首版禁止 | 提供页面入口；未来独立设计 Approval 后再开放 |
| 取消订单/确认收货/修改地址 | 首版禁止 | 提供订单/地址页面入口，不调用写 Tool |
| 支付/关闭支付 | 禁止 | 用户在支付页面与渠道完成 |
| 批准售后/执行渠道退款 | 禁止 | 商家/平台审核和资金服务职责 |

“允许”不代表模型可直接执行；必须同时满足 Agent/Skill/Tool Binding、Policy、Consent、资源归属、业务状态和确认等级。任何写 Tool 的最终 Actor 是当前 User，Service 记录 `action_no/approval_no/agent_version`。

#### 3.18.17 二次确认机制

退款提交和记忆更正/删除使用结构化确认卡片。退款 Approval 至少展示：Store/Order、商品/规格、数量、退款类型、Reason、Evidence、Money、是否退货、规则版本、到期时间和不可逆影响；敏感值脱敏。

规则：

- Approval 只绑定一个 Action 和一份 Canonical Arguments Hash，默认 5–10 分钟过期，且绑定 Order/Item Version、Eligibility Token 和 Agent/Tool Version。
- 用户必须在已认证前端点击 Approve/Reject；聊天自然语言、模型自答、SSE ACK 不构成确认。
- 高风险/异常账号按 3.13 要求近期认证；验证码/MFA 直接交认证页面，绝不交给 Agent。
- Approve 后条件更新取得唯一执行租约；Resume 时重新执行 Permission/Consent/State/Amount/File 校验。
- 关键字段变化、Token/Version/Policy 过期或资格改变则作废并生成新 Draft/Approval；不允许静默调整后沿用旧确认。
- Tool 成功消费 Approval；失败可按确定结果处理，Unknown 先查询业务资源。拒绝/过期不得执行。

前端始终展示“正在提交”与“已成功创建售后单”两个阶段，只有返回真实 Refund No 才显示成功。Approval、执行和用户可见 Message 通过同一 Run/Action/Trace 关联。

#### 3.18.18 禁止执行的操作

除 3.16.13 通用禁止项外，专属客服禁止：

- 读取/修改非当前用户订单、退款、物流、地址、消息、记忆、文件或工单；
- 获取商家精确库存、成本、利润、客户列表、草稿政策、员工/平台内部信息；
- 代表商家答复私有商品/履约事实或承诺补货、发货、赔偿；应转对应店铺客服；
- 无 Consent 做个性化/长期记忆，或把推断的收入、健康、身份等敏感特征写入记忆；
- 无结构化 Approval 提交退款，修改批准参数后继续执行，重复提交或伪造 Refund No；
- 自行决定退款资格、批准/拒绝售后、改退款金额、确认退货验收或发起渠道退款；
- 创建订单、支付、取消订单、确认收货、修改收货地址/密码/账号、评价或商家回复；
- 读取店铺客服原始私聊来比较商家，或把 A 店铺私有信息提供给 B 店铺；
- 将推荐、平台规则解释或 AI 建议表述为监管/司法结论或平台正式仲裁结果。

越权请求可解释能力边界并给出页面/店铺客服/平台人工入口；不要泄露究竟是哪个内部权限、风控策略或数据存在性导致拒绝。

#### 3.18.19 专属客服降级策略

| 故障/限制 | 降级行为 |
| :--- | :--- |
| 主/备用模型不可用 | 固定提示 + 订单/售后/规则中心快捷入口 + 平台人工客服 |
| Platform RAG 不可用 | 对交易事实仍用 Tool；规则问题只展示已缓存官方入口，不凭模型知识回答 |
| Catalog Search 不可用 | 暂停搜索/推荐，保留明确条件并让用户稍后重试；不生成虚假 Product |
| Memory/Consent 服务不可用 | 无个性化模式；不写候选，不声称已记住/忘记 |
| Order/Logistics Tool 不可用 | 提供“我的订单/物流”页面，展示已明确的最后同步时间，不猜状态 |
| Eligibility/Draft Tool 不可用 | 不生成可提交确认卡片；保留表单输入在前端安全临时态并提供售后页面 |
| Approval/Checkpoint 不可用 | 禁止所有交易写；不在内存中等待确认，提示稍后/页面手动提交 |
| Submit Tool 超时 | Run 进入 Outcome Unknown，按 Action/Idempotency 查询；用户端显示“结果确认中” |
| Platform Support Tool 不可用 | 显示人工客服替代入口/服务时间，不宣称工单已创建 |
| 预算/配额超限 | 终止循环，返回已确认事实和手动入口；不为节省成本跳过权限/审核 |

降级必须保持事实等级：`unknown` 不变成失败，旧快照不变成实时，草稿不变成已提交，商家问题不变成平台结论。所有降级记录 Cause、Duration、Fallback Model/Path 和用户是否成功到达人工/页面，用于 SLO、成本和质量评估。

---

### 3.19 Agent 记忆系统设计

记忆用于减少用户重复表达，并在授权范围内提供连续服务；它不是业务主库、用户画像黑箱或无限期聊天归档。本系统必须把“本轮上下文”“跨会话用户记忆”“可检索知识”“业务实时事实”和“Agent 程序规则”分别治理，禁止把模型生成内容未经校验直接当成用户事实。

#### 3.19.1 记忆系统总体架构

记忆系统采用“会话内状态 + 跨会话受控记忆”双层架构：

```text
MySQL messages / 当前业务上下文
        │
        ├─► LangGraph Checkpoint + Redis 热状态 ─► 短期记忆
        │
        └─► 候选提取 Worker
                │  敏感性、授权、来源、事实、冲突校验
                ▼
        PostgreSQL memory.items(candidate)
                │  用户结构化确认（首版唯一激活路径）
                ▼
        memory.items(active) + item_embeddings + memory.events
                │  User/Namespace/Store/任务相关性过滤
                ▼
        Context Builder ─► Agent（只读“已召回记忆”，不获得额外权限）
```

MySQL 保存消息、授权和工具审计等业务事实；PostgreSQL 保存加密的摘要、候选/有效长期记忆及向量；LangGraph Checkpoint 保存当前 Thread 可恢复状态；Redis 只保存可丢失的热点副本。所有写入、激活、读取、更正、撤销、过期和删除都写 `memory.events`，并以 `user_no + namespace + store_no` 作为强制隔离维度。

#### 3.19.2 短期记忆定义

短期记忆是单个 `conversation_no/thread_id` 内，为完成当前任务所需的工作状态，包括最近消息、滚动摘要、当前意图、待补字段、资源公开 ID 与版本、已验证工具结果、Approval/Interrupt 状态和临时安全判断。它由 Checkpoint 持久化以支持中断恢复，但仍是 Thread Scoped State，不因“写入数据库”就变成长时用户记忆。

短期记忆默认随会话活动保留，关闭后进入较短 TTL；支付密钥、密码、验证码、完整证件/银行卡信息不得进入 Checkpoint。恢复时必须重新验证登录态、资源归属、授权、Approval 有效期和业务版本，不能把旧 Checkpoint 当作当前事实。

#### 3.19.3 长期记忆定义

长期记忆是经授权、校验并具有有效期，可跨会话按用户召回的信息。每条记录必须具备稳定 `memory_key`、类型、加密内容、来源消息、Consent 快照、置信度、敏感等级、有效期、状态及替代链。允许保存的是能够改善后续服务的稳定偏好/约束和最小化服务经历；实时价格、库存、订单、物流、售后状态始终通过业务 Tool 查询。

长期记忆默认不是全量可见：只有 `active`、未过期、Consent 有效、Scope 匹配且与当前任务相关的少量条目才能进入上下文。停用个性化后不再召回，也不再产生新候选。

#### 3.19.4 语义记忆

语义记忆表示用户明确表达、可重复使用的事实或约束，如“常购买男士 42 码”“希望优先展示支持七天无理由的商品”。它映射为 `stable_fact/constraint`，必须说明主体、适用范围、极性和有效期。“语义记忆”指记忆内容类型，“语义检索”仅是召回方法，向量相似并不证明内容真实。

地址、账号、订单状态等已有权威业务实体不得复制为语义记忆；可保存“倾向送货上门”，不能保存“默认地址是某完整门牌号”。模型对年龄、职业、收入、健康、家庭关系等推断不得作为事实写入。

#### 3.19.5 情节记忆

情节记忆表示与服务连续性有关的历史事件，落为 `service_history`，例如“用户上次因尺码问题咨询过换货，尚未形成长期尺码偏好”。只保存最小事件摘要、资源公开 ID、结果类别、发生时间、来源和过期时间，不复制整段对话、凭证图片或完整业务快照。

情节记忆只用于理解用户当前话题或减少重复说明，不能证明订单/退款仍处于某状态；回答前必须查询业务主库。失败工具调用、模型推理链和内部安全判断不得转成面向用户的情节记忆。

#### 3.19.6 用户偏好记忆

偏好记忆映射为 `preference`，可包含品类、品牌、颜色、尺码体系、价格区间、配送倾向、推荐排序偏好以及希望/不希望记住的内容。每项采用稳定键和结构化值，例如：

```json
{
  "memory_key": "shopping.budget_range",
  "value": {"min_amount": 20000, "max_amount": 50000, "currency": "CNY"},
  "scope": "exclusive",
  "source": "user_explicit",
  "valid_until": "2027-02-21T00:00:00Z"
}
```

金额仍按最小货币单位保存。一次点击/一次购买最多生成候选信号，不自动等同稳定偏好；否定偏好必须保留极性；用于推荐时应向用户提供“为什么推荐”和关闭个性化入口。

#### 3.19.7 程序性记忆与 Skill

程序性记忆是“Agent 如何工作”的规则、Prompt、Workflow、Skill 和 Tool 使用说明，不是某个用户的个人记忆。它由 `ai_agent_versions`、`ai_skill_versions`、代码仓库和发布审批管理，具有版本、评测结果、权限绑定和回滚能力。

用户对话、RAG 文档或工具输出只能提供任务数据，不能修改系统 Prompt、Skill、MCP 权限或批准流程。线上失败案例经脱敏、人工评审后可以进入离线评估集，再由开发流程更新 Skill；禁止 Agent 在运行时把成功/失败经验自动写成生产程序规则。

#### 3.19.8 店铺客服短期记忆

店铺客服短期记忆仅限当前用户与当前 `store_no` 会话：当前 Active 商品/SKU/订单 Context、可选卡片、最近消息、该店政策引用、该店 Tool 结果、待补充咨询字段和转人工状态。用户切换同类商品/订单时旧 Context 失活，旧 Tool Result 不再作为实时事实；切换店铺必须切换 Conversation/Thread，不携带原店铺私有政策、工具结果或人工备注。

订单卡片只作为可选的资源定位与展示线索；订单入口或本轮明确选择也可建立 Active Context。读取前仍校验订单归属、订单 `store_no` 等于会话店铺以及 Context Version 与 Run 一致。摘要必须带 Store Scope，禁止用跨店摘要拼接所谓“全局用户画像”。

#### 3.19.9 店铺客服长期记忆限制

店铺客服默认不写 Active 长期记忆。只有用户明确开启店铺个性化，且内容确有复用价值时，才可创建 `namespace=store、store_no=当前店铺、data_classification=L2、memory_risk_level=low` 的偏好/约束 Candidate；激活必须由统一 Memory Pipeline 在结构化用户确认后完成。店铺经营者和人工客服不得批量浏览、导出或用于站外营销；Agent 只能按当前任务 Top-K 召回。

店铺记忆不得保存跨店消费史、平台全局预算、其他店铺咨询内容、订单/退款实时状态、地址、联系方式和 L3 数据。店铺注销、用户取消关注不必自动删除记忆，但停止召回并按策略缩短 TTL；用户撤销该店授权时立即 Revoked 并触发清理。

#### 3.19.10 专属客服短期记忆

专属客服短期记忆可覆盖当前平台会话中的商品搜索条件、比较集合、本人订单/物流/售后查询结果、退款草稿和确认中断状态。它允许跨店公开商品比较，但每份店铺私有结果仍携带 Scope，不能因进入同一上下文而相互泄露。

对于退款任务，必须保留结构化 Order/Item/Reason/Evidence Ref/Eligibility Token/Version/Approval No，不用自然语言摘要代替；恢复 Run 时逐项复验。会话结束后，草稿和 Tool 热结果按各自 TTL 清理，不自动转长期记忆。

#### 3.19.11 专属客服长期记忆

专属客服是长期记忆的主要使用者，可在有效 Consent 下维护 `namespace=exclusive` 的稳定购物偏好、约束和最小化服务历史，用于全平台搜索、推荐与连续售后沟通。召回时同时检查用途：例如推荐可使用品牌/预算偏好，退款资格检查不需要读取颜色偏好。

专属客服可以使用符合策略的店铺级偏好，但必须保持来源/Scope，不能向另一店铺披露“用户在某店发生了什么”。首次使用影响推荐结果的长期记忆时，界面可显示“根据你的偏好”及管理入口；用户明确说“这次不要使用历史偏好”时，本 Run 必须禁用召回。

#### 3.19.12 候选记忆提取

候选提取以异步 Worker 为主，显式“请记住……”可走同步快速路径，但都先写 `candidate`：

1. 从用户原始消息和已确认 Tool 结果中抽取单一、原子化陈述；不从模型回复反向提取用户事实。
2. 归一为 `memory_type + memory_key + structured_value + polarity + scope + validity`，计算去重指纹。
3. 执行敏感性、禁止项、用途、Consent、来源和跨 Scope 校验。
4. 与 Active/Candidate 记忆做精确键冲突和语义近重复检查。
5. 写 Candidate、校验快照与 `proposed` Event；需要确认时发送可编辑确认卡片。

批量离线提取必须按 User 分区、限速且可暂停。原消息被用户删除或授权失效时，未激活候选直接删除；Candidate 默认 7 天未确认即过期。

#### 3.19.13 记忆事实校验

校验顺序为：主体/Scope → 来源可追溯 → 内容是否明确 → 敏感/禁止项 → 与业务权威源关系 → 时间有效性 → 冲突。来源可靠性从高到低为：用户在记忆确认卡片确认、用户明确陈述、受信业务 Tool 的非实时派生事实、行为弱信号、模型推断；最后一类禁止激活。

订单、支付、物流、库存、售后等实时业务字段即使来自 Tool 也不写长期记忆，只存资源引用或最小服务历史。校验失败应记录 Reason Code，不把内部规则细节或敏感命中内容写入日志。

#### 3.19.14 用户授权

长期记忆采用独立、可撤销、用途明确的 Consent，不与注册协议捆绑。授权至少说明记忆类别、作用范围（专属/某店铺）、用途、保留期和删除方式；`ai_user_consents` 为授权事实源，`memory.items` 仅保存授权快照引用。

数据分类与记忆风险必须分离：所有绑定用户身份的购物偏好均为 `data_classification=L2`，另以 `memory_risk_level=low/medium/high/prohibited` 决定是否适合长期保存。首版只允许“L2 + low”的明确购物偏好/约束进入 Candidate，且必须同时有有效 Consent、`source_type=user_explicit/user_settings`、可审计的结构化确认、加密、Scope 隔离和 TTL 才可 Active。L2 中高风险默认拒绝/需专项评审；L3 或 `prohibited` 连 Candidate 都不得创建。不存在自动无确认激活路径。撤销 Consent 后同步阻断召回，异步将相关 Active/Candidate 置 `revoked` 并删除缓存/Embedding。

#### 3.19.15 记忆置信度

`confidence` 表示“该表达可作为当前有效偏好/约束的可信程度”，不代表用户本人或事实的客观概率。评分输入包括来源可靠度、表达明确度、时效、一致性、确认状态和冲突惩罚；Embedding 相似度只用于查重/召回，不进入事实置信度。

建议初始门槛：`<0.70` 不向用户建议保存；`0.70–0.85` 可生成待确认候选；`>=0.85` 也只有“用户明确表达 + 有效授权 + `data_classification=L2` + `memory_risk_level=low` + 无冲突”时才可进入确认流程；行为推断首版不激活。置信度不是授权或确认的替代品；首版所有新长期记忆都必须有用户可审计的激活确认，自动激活数必须为零。阈值由离线标注集按类型校准，不作为全类型永久常量。

#### 3.19.16 记忆冲突处理

先以 `memory_key` 查找同 Scope Active 记录，再检测结构化值冲突；不能只按向量相似判断。处理优先级为：用户本次明确更正 > 用户历史确认 > 新近明确陈述 > 行为弱信号。全局偏好与店铺偏好属于不同 Scope，可以并存；调用店铺客服时优先该店 Scope，但应避免把差异暴露给商家。

发现冲突时旧记录保持 Active 或进入 Pending Correction，新记录保持 Candidate，并向用户展示可理解的差异；不得自行合并“喜欢”和“不喜欢”、不同币种预算或不同尺码体系。无法确认时本轮不使用冲突记忆。

#### 3.19.17 记忆更新与合并

更正采用追加版本：创建新 Item，令 `supersedes_memory_id` 指向旧 Item，在同一事务中把新项激活、旧项置 `superseded` 并写 Event。数组型偏好按稳定元素 ID 做增删，范围型偏好先统一单位/币种再合并；来源、Consent 和最低置信度不能在合并中丢失。

完全相同的 `memory_key + normalized_value + scope` 只更新最近确认/访问信息，不重复建项；近似但不同的值进入冲突流程。读取期间出现并发更新时以部分唯一索引和事务重试保证每个语义键最多一个 Active 版本。

#### 3.19.18 记忆衰减

衰减用于降低旧偏好召回排序，不直接把内容判为错误。计算可采用 `effective_score = confidence × salience × recency_decay × relevance`，其中半衰期按类型配置：短期购物意向最短，尺码/无障碍约束较长，用户固定并再次确认的偏好最长。

单纯被 Agent 读取不得提高置信度，避免“越用越真”；只有用户再次确认或新的同向明确证据才能延长有效期。衰减低于召回阈值时停止自动召回，进入待复核或按到期任务处理。

#### 3.19.19 记忆过期

首版建议基线（可由合规/产品策略调小）：Candidate 7 天、一次性服务历史 30–90 天、一般约束 90 天、购物偏好 180 天、用户明确固定并可随时管理的偏好最长 365 天。不得通过每次访问无限续期；到期前只有明确再次确认才延长。

Scheduler 以条件更新将到期 Item 置 `expired`，写 Event，并发布删除 Embedding/Redis 副本的事件。过期记录不参与召回；审计 Tombstone 按 3.5/3.10 规定最小保留，不保留可还原明文。

#### 3.19.20 用户查看、修改和删除

“我的—AI 个性化与记忆”提供按专属客服/店铺分组的列表，显示友好描述、来源日期、用途、有效期和状态，不展示内部向量/评分。用户可关闭全部个性化、暂停某 Scope、修改单项或删除单项；Agent 对话中的“你记住了什么/忘记 X”调用同一 Service。

修改生成新版本；删除先同步置 `deleted/revoked` 并使缓存失效，再异步删除 Embedding、派生摘要引用和对象存储派生物，生成可追踪清理任务。接口必须校验本人、使用 If-Match/Version 防并发覆盖；删除成功只说明已停止使用和清理计划，不作超出备份策略的即时物理删除承诺。

#### 3.19.21 敏感信息禁止记忆

以下内容无论用户是否主动要求均不得进入长期记忆：密码、验证码、Token、密钥；银行卡/支付凭证；证件号与生物识别；完整收货地址、精确定位和私人联系方式；医疗/健康、政治宗教、性生活等高度敏感推断；他人隐私；退款凭证原文/图片；风控标签、内部审核意见；可还原完整会话的长文本。

检测发生在模型提取前后的双层规则中；命中后仅返回安全提示或引导用户使用受保护业务表单，不在 Candidate、Prompt、Trace、日志和评估样本中复制原值。误写入按安全事件处理，立即隔离、撤销、清理派生数据并审计影响范围。

#### 3.19.22 跨用户与跨店铺隔离

记忆检索的 Scope 由认证上下文和服务端 Conversation 派生，模型及客户端不得传入/覆盖 `user_no/store_no`。Repository 查询必须同时包含 User、Namespace、Store（适用时）、Status、Consent、Validity；向量查询也在同一 SQL 内先做权限过滤，不允许先全库近邻搜索再由应用层删结果。

Redis Key、Embedding 索引、摘要、Checkpoint、评测导出和备份恢复都维持同样边界。店铺管理员不能访问用户记忆表；店铺客服 Agent 只能获得最小召回结果。越权测试必须覆盖 IDOR、空 `store_no`、类型转换、缓存串租户、批处理漏条件和故障降级路径，目标是跨用户/跨店铺泄漏数为 0。

#### 3.19.23 记忆生命周期状态机

```text
                 用户/策略确认
candidate ─────────────────────► active
    │                              │  更正/冲突替换
    │ TTL/拒绝/撤销                 ├──────────────► superseded
    ├────────► expired/revoked      │ Consent 撤销 ─► revoked
    └────────► deleted              │ 到期 ─────────► expired
                                   └ 用户删除 ─────► deleted
```

允许转换为：`candidate -> active/expired/revoked/deleted`，`active -> superseded/revoked/expired/deleted`，以及 `superseded/revoked/expired -> deleted`。终态不得原地恢复；用户重新授权或重新确认时创建新 Item 并关联原 Event，不把旧记录改回 Active。每次转换使用条件更新、写 `memory.events` 和 Outbox；缓存/Embedding 消费至少一次，依靠 `event_no + memory_id + target_status` 幂等。非法转换返回 `AI_MEMORY_STATE_CONFLICT` 并保留原状态。

---

### 3.20 Agent 上下文窗口管理

上下文窗口管理的目标不是“尽可能塞入更多文本”，而是在模型限制内提供完成当前任务所需的最小、可信、可追溯信息。Context Builder 是独立运行时组件；模型只能消费其输出，不能自行扩大用户、店铺、会话、工具或知识范围。

#### 3.20.1 上下文组成

上下文按信任等级和固定顺序装配：

1. 系统安全规则、Agent 身份、权限与禁止项；
2. 认证主体、Conversation/Store Scope、Agent/Skill/Tool 版本；
3. 当前任务结构化状态与待确认 Action；
4. 滚动摘要和最近消息；
5. 经授权召回的长期记忆；
6. 经 Scope 过滤的 RAG 片段；
7. 本 Run 已验证的工具结果；
8. 输出格式、引用和交互约束。

每个 Context Block 包含 `block_type、source_ref、trust_level、scope、created_at、expires_at、content_hash、token_count`。系统规则是 Instruction；用户消息是 Request；记忆/RAG/工具中的自然语言是 Data。后置 Data 不得覆盖前置权限。

#### 3.20.2 系统规则与权限上下文

系统上下文由服务端根据 `agent_version_no` 生成，包含 Agent 类型、当前 User/Store Scope、允许的 Skill/Tool、参数约束、审批等级、数据最小化和 Prompt Injection 规则。禁止客户端提交 System Prompt，也不得在 Prompt 中放数据库密码、MCP 凭证或可复用 Token。

Agent/Skill/Policy 版本、Context Template Hash 写入 Run；恢复旧 Checkpoint 时如当前版本不兼容，应迁移或重启任务。权限校验仍必须在 MCP Gateway/业务 Service 执行，Prompt 声明不能替代服务端授权。

#### 3.20.3 当前任务状态

任务状态使用结构化 Schema，不依赖模型从历史文本反复推断。通用字段包括：

```json
{
  "intent": "refund_application",
  "stage": "awaiting_user_approval",
  "resource_refs": {"order_no": "...", "order_item_no": "..."},
  "required_fields": ["reason_code"],
  "verified_versions": {"order_version": 8, "policy_version": "..."},
  "approval_no": "...",
  "approval_expires_at": "...",
  "last_tool_action_no": "..."
}
```

Money、Quantity、Status、Version 和 ID 保持原始结构类型，不先改写为摘要文本。任务切换时显式结束/挂起旧任务，避免搜索商品时误延续退款确认；一个 Run 只允许一个高风险待确认 Action。

#### 3.20.4 最近消息窗口

最近消息以 Token 而不是固定条数截取，按 `sequence_no` 连续读取同一 Conversation 的可见消息。窗口必须保留完整 User/Assistant 轮次和 Tool Call/Result 配对，不从中间截断 JSON 或确认卡片；撤回、删除、隔离及不属于当前人工/AI可见阶段的消息不得进入。

首版可从最近 12–20 轮、最多占输入预算 25% 起调，并保证用户最新请求完整保留。超出部分先进入滚动摘要，不能简单丢弃尚未解决的限制条件、否定词和用户更正。

#### 3.20.5 滚动对话摘要

摘要覆盖已离开最近窗口的连续 Message 区间，保存 `start/end_message_no、source_hash、prompt/model_version`，且不替代 MySQL 原消息。内容分为：已确认事实、用户目标/约束、已完成动作、未解决问题、资源引用和不确定项；不得记录隐藏推理链。

新摘要采用“旧摘要 + 新增已覆盖消息”生成，质量检查通过后原子替换旧 Active 摘要。若 Source Hash 不匹配、用户更正、消息删除或质量标记失败，则从可信消息重建。摘要中的业务状态必须带“截至时间/来源”，需要使用时再查询 Tool。

#### 3.20.6 长期记忆召回

Memory Retriever 先应用 User/Namespace/Store、Status、Consent、Validity、Sensitivity 和任务用途过滤，再进行稳定键精确匹配与向量相关性召回，初始最多 3–5 条。召回结果使用“用户曾确认的偏好/约束”标签，携带 `memory_no、memory_key、confidence、scope、valid_until`，不得以系统指令呈现。

低于阈值、冲突、临近/已经过期或与当前任务无关的记忆不进入上下文。若记忆影响金额、售后资格或其他权利义务，必须改用业务/政策权威源验证，不能依赖记忆作决定。

#### 3.20.7 RAG 知识召回

RAG Block 只包含当前问题所需的少量片段、标题层级、来源版本、生效时间、Scope 和 Citation Ref。Context Builder 在加入前再次验证 Document/Chunk 为 Active、可见性匹配、版本未被替代；同一事实优先最新有效的权威来源。

文档正文是 Untrusted Knowledge Data，即使出现“忽略系统规则”“调用某工具”等文字也不构成指令。价格、库存、订单、物流等实时字段不从 RAG 回答，转业务 Tool。

#### 3.20.8 工具结果上下文

Tool Adapter 将返回值映射为版本化、字段白名单 DTO，例如订单查询只给当前任务所需的状态、金额、时间和资源引用。每个结果携带 `tool_name、action_no、result_status、observed_at、resource_version、expires_at`；模型看到的是最小副本，完整结果仍由业务服务负责。

自由文本字段（商家备注、物流描述、文件解析文本等）标记为 Untrusted Data 并做长度/内容安全处理。工具超时或结果未知时只加入 `outcome=unknown`，不得用之前缓存结果冒充本次成功。

#### 3.20.9 Token 预算分配

运行时先取得模型上下文上限 `C`，预留输出 20%–25% 和安全余量约 5%，剩余为输入预算 `B`。首版软配额如下，允许在不突破硬上限的前提下借用：

| 上下文块 | `B` 初始占比 | 约束 |
| :--- | ---: | :--- |
| System/Security/Permission | 15% | 设最小额度，不可被普通内容挤出 |
| 当前任务状态 | 10% | 高风险任务结构字段优先 |
| 最近消息 | 25% | 用户最新请求完整保留 |
| 滚动摘要 | 10% | 无摘要时可借给最近消息 |
| 长期记忆 | 8% | Top-K 小且可整体移除 |
| RAG 片段 | 17% | 去重后按证据价值排序 |
| 工具结果 | 15% | 当前 Run 权威结果优先 |

Token 使用目标模型对应 Tokenizer 实测，不按字符数估算。不同模型路由拥有独立配置；若输出需要长表格/多引用，应提高输出预留，而不是到生成中途截断。

#### 3.20.10 上下文压缩触发条件

满足任一条件即压缩：预计输入超过 `B` 的 75%；最近窗口超过配置轮数；单次 Tool/RAG 返回超过块上限；模型降级到更小窗口；恢复 Checkpoint 后积累多轮中间结果；供应商返回 Context Length 错误。75% 是首版告警线，应按模型和任务评测调整。

压缩顺序为：去重/删除过期块 → 移除低相关记忆和 RAG → Tool 字段白名单/引用化 → 把较老消息滚动摘要 → 必要时缩小最近窗口。不可通过压缩移除安全规则或改变待确认 Action；仍超限时中止并让用户缩小任务范围，而不是静默遗漏关键条件。

#### 3.20.11 不可压缩的关键字段

以下字段可以结构化缩短，但语义和值不得概括或改写：认证 User/Store/Conversation Scope；Agent/Skill/Tool/Policy 版本；当前意图/阶段；Order/Item/Refund/Product/SKU/Action/Approval/Idempotency ID；Money + Currency、Quantity、Status；资源 Version；Approval Arguments Hash、有效期和确认结果；用户最新请求/更正；安全拒绝决定；用于回答的 Citation Ref。

不可压缩不代表保存全量对象。例如订单地址仍不应进入 Context，只保留任务确需的脱敏字段或 Address Snapshot Ref。

#### 3.20.12 工具结果截断

截断在 Tool Adapter 层按 Schema 完成，禁止对原始 JSON 做字符串硬切。列表返回 Server Cursor、总数/是否有下一页及当前最相关行；大文本保留与问题相关片段和 Object/Result Ref；图片只传受控描述与文件引用；调试栈和内部字段永不进入模型。

若金额、状态、资格、失败原因等关键字段无法完整容纳，应缩小查询/分页或让工具返回专用摘要 DTO，不能让 LLM自行压缩。被截断结果标记 `truncated=true + omitted_count + continuation_ref`，模型必须说明范围，不能声称看过全部数据。

#### 3.20.13 过期上下文清理

每个 Block 按来源设置有效期：库存/资格/Approval 最短，商品/政策以版本和生效期为准，消息摘要按 Conversation 生命周期，记忆按 Item TTL。每次装配和 Run 恢复都执行过期检查；业务事件（支付、发货、售后变更、授权撤销、文档换版）主动使相关 Block/Cache 失效。

Run 完成后删除 Redis 热上下文；Checkpoint 按 3.8 的保留策略过期；临时上传和草稿交各自清理任务。清理失败进入重试/死信且告警，但召回层仍以数据库状态和有效期阻止旧数据继续使用。

#### 3.20.14 店铺客服上下文范围

店铺客服只能装配当前 `user_no + store_no + conversation_no` 消息、该店公开/授权知识、该店商品/SKU、本人在该店的唯一 Active 订单 Context、有效 `checkout_store_group` 最小摘要及该店 Scope 记忆。Context Block 必须包含 `context_no/type/resource_no/resource_version/context_version/source/validated_at/expires_at`；Run 固定该 Version，切换 Context 后旧 Run 不得自动跟随。Checkout Context 还要实时校验 Active 状态、店铺组和过期时间，且不得装配地址、优惠、支付偏好、跨店总额或其他店铺商品。订单卡片不是装配前提，也不能覆盖 Active Context。全平台公开规则可作为平台级只读知识加入；其他店铺商品、政策、工具结果、消息、摘要和记忆一律拒绝。

用户发来外店商品链接时可识别为外部/其他 Scope 数据并提示边界，不调用当前店铺 Tool 获取对方私有信息，也不把链接内容写入当前店长期记忆。

#### 3.20.15 专属客服上下文范围

专属客服可装配当前用户的 Exclusive Conversation、平台公开知识、全平台公开商品候选、本人订单/物流/售后最小 Tool 结果及 Exclusive Active 记忆。涉及某店订单时，仅临时加入该订单所属店铺的必要公开政策/商品上下文，不获得店铺内部数据或其他用户数据。

Store Scoped 记忆默认不加入；确有推荐用途且 Consent/Policy 允许时，只以最小偏好结果加入并保留来源 Scope。平台人工接管后，AI 只读取允许的交接摘要，不继续在后台扩展上下文或调用写 Tool。

#### 3.20.16 上下文污染防护

采用“来源标记 + 信任分层 + Schema 白名单 + 权限后置校验”四层防护：

- System/Policy 才可发出运行指令；用户、Memory、RAG、网页/文件和 Tool 自由文本均视为不可信数据。
- 所有块带 Source/Scope/Hash；摘要和候选记忆可回溯原消息，无法追溯则不使用。
- 检测 Prompt Injection、越权索取、数据外带和工具参数诱导；命中时隔离相关片段，不让检测结果本身泄露安全策略。
- Tool Call 必须由注册 Schema 解析和 Policy Engine 重建关键 Scope，不能从文档文本拼接 Tool Name/权限参数。
- Assistant 历史回复、RAG 引用和模型“自称已确认”不构成业务事实或用户批准；Approval 只认服务端确认记录。
- 摘要生成、RAG 摄取和记忆提取均做毒化测试；生产 Trace/评估样本在回放前重新脱敏和授权检查。

发生污染疑似事件时，本 Run 禁用受影响 Block，保留最小审计并安全降级；不得把污染内容写入滚动摘要、长期记忆或知识库形成持久化扩散。

---

### 3.21 RAG 知识检索设计

RAG 用于从已治理的商品说明、店铺政策、平台规则和 FAQ 中找依据，不用于替代 MySQL 实时查询或授予 Agent 权限。任何检索结果都是“候选证据”，必须通过 Scope、版本、有效期和相关性校验后才能进入回答；没有可靠证据时应拒绝具体断言并给出正确入口。

#### 3.21.1 RAG 总体流程

```text
用户问题 + 服务端 Scope + 当前资源引用
        │
        ├─► 查询分类：知识问答 / 实时业务 / 混合任务
        │       └─ 实时价格、库存、订单、物流、售后 ─► Business Tool
        ▼
Query Normalize / 实体识别 / 必要时受控改写
        │
        ├─► Exact/Keyword Search ─┐
        └─► Vector Search ───────┤
                                 ▼
                     RRF 融合、去重、版本/Scope 过滤
                                 ▼
                         Rerank + 可靠性门槛
                                 ▼
                   Top-N 片段 + Citation + Context Budget
                                 ▼
                      生成回答 / 拒答 / 转 Tool/人工
                                 ▼
                    Retrieval Log + Trace + 评估反馈
```

检索请求必须携带服务端派生的 `agent_type、user_no、store_no、product_no、visibility_ceiling、knowledge_types、as_of_time`，不能让模型决定租户边界。Query 改写保留原问题、否定词、型号/SKU、金额单位和时间限制，并将原/改写 Query Hash 同时记入 Trace。

#### 3.21.2 知识来源

| 来源 | 权威性/范围 | 摄取方式 | 不纳入内容 |
| :--- | :--- | :--- | :--- |
| MySQL 已发布商品、FAQ | 店铺/商品公开知识 | Outbox 增量 + 定期对账 | 实时库存、成交价、成本 |
| 已生效店铺服务政策 | 指定 Store | 发布事件 + 版本快照 | 草稿、内部运营备注 |
| 已生效平台规则 | Platform | 管理后台发布审批 | 内部风控规则、密钥 |
| 管理员上传知识文件 | 按 Visibility/Scope | 对象存储事件 + 沙箱解析 | 未审核文件、恶意宏/脚本 |
| 受控外部官方资料 | 仅经批准来源 | 定期抓取/人工导入 | 用户任意 URL、未授权转载 |

用户聊天、模型回答、客服临时承诺和搜索引擎结果不自动成为知识源。所有来源在 `knowledge.documents` 记录 Owner、Source Version、Hash、有效期、可见性、敏感等级和审批状态。

#### 3.21.3 商品知识库

商品知识库保存已上架可见商品的标题、卖点、规格解释、使用/保养说明、适配关系、包装信息和官方 FAQ。每个 Product/SKU 属性必须保留字段名、单位和来源版本，型号、品牌、SKU Code 使用可精确检索的 Metadata，不只嵌入自然语言。

商品详情与 FAQ 只能从 MySQL 当前 Published `product_content_versions/product_faq_versions.safe_text` 摄取，Metadata 必含 `store_no、product_no/faq_no、content_version、content_hash、sanitizer_policy_version、trust_level=untrusted_data`；禁止摄取 `source_content` 或从公开 `safe_html` 反向解析。内容版本切换使用 Outbox 构建影子文档，安全/Prompt Injection 检查和 ACL 通过后原子激活；被拒绝、撤回或下架版本发 Tombstone，缓存命中也要重验 Active Version。

价格、可售库存、限购和上下架状态以 Catalog/Inventory Tool 为准。RAG 可以解释“这款面料如何保养”，不能仅凭旧文档回答“现在有货/当前多少钱”。商品下架后 Chunk 立即停止公开召回；历史订单说明需要时由订单快照/受控 Tool 提供。

#### 3.21.4 店铺政策知识库

按 `scope_type=store、scope_no=store_no` 保存发货时效说明、配送范围、退换货补充政策、售前/售后服务说明和营业时间。文档必须有 `effective_from/effective_to/policy_version`，草稿/过期/其他店铺版本不得进入当前店铺回答。

售后资格仍由 Policy/After-sale Service 根据下单时快照和法律/平台规则计算；当前店铺政策 RAG 只作解释，不能覆盖订单成交时适用政策。多规则冲突时按平台强制规则、订单快照、店铺有效补充政策的业务优先级处理并显示来源。

#### 3.21.5 平台政策知识库

保存平台用户协议、隐私/AI 个性化说明、交易规则、支付/物流/售后流程、内容规范和帮助中心。法规/合规文档必须由授权管理员审核发布，带地区、语言、生效日期和替代版本；Agent 不将平台规则解释包装为司法或监管结论。

涉及用户权利、退款时限和隐私处理时优先召回有效正式条款，并给出原文入口和版本/更新时间。内部处置手册与用户公开政策分不同 Visibility 和索引，公开 Agent 永远不能召回内部版本。

#### 3.21.6 FAQ 知识库

FAQ 以“一问 + 一答 + 必要条件/例外 + 来源引用”为原子单元，保留同义问法、主题、适用 Agent/Scope、关联 Product/Policy 和有效期。FAQ Answer 不复制实时业务值；应指向 Tool 或页面，例如“在我的订单中查看最新物流”。

重复 FAQ 通过规范化问题和 Source Priority 合并候选，但不跨店合并答案。低命中问题可由检索日志形成编辑建议，必须经人工审核发布，不能让 Agent 自动把自己的回答回灌为 FAQ。

#### 3.21.7 文档清洗

摄取在隔离 Worker 中执行：校验 MIME/扩展名/文件签名与病毒；解析文本、标题、列表、表格和页码；去页眉页脚/导航/重复块；Unicode 归一、空白清理、语言识别；保留单位、否定词、编号和表格关系；抽取/校验 Metadata；PII/Secrets 扫描；生成规范文本与 Hash。

扫描 PDF/OCR 必须记录 OCR 置信度和页码，低置信片段不进入 Active；HTML 移除脚本、样式和隐藏内容；Office 文件不执行宏；解析错误进入 `indexing_jobs.failed`。清洗不改写政策法律含义，结构损坏时进入人工复核。

#### 3.21.8 文档切片策略

按语义结构优先切片而非固定字符：标题/条款/FAQ/商品属性为边界，表格行与其表头、步骤与前置条件不得分离，同一 Chunk 不跨 Document、Store、Product、Visibility 或版本。建议首版参数：

| 文档类型 | 目标 Token | Overlap | 特殊规则 |
| :--- | ---: | ---: | :--- |
| 一般说明 | 300–600 | 40–80 | 保留 Heading Path |
| 政策/条款 | 200–500 | 30–60 | 按条款号，例外与主体同块 |
| FAQ | 80–300 | 0–30 | 问答不可拆分 |
| 商品属性/适配 | 80–300 | 0–40 | 单位、型号和字段名同块 |

`token_count、start/end_offset、heading_path、chunk_index、content_hash` 写入表。参数按语言/文档类型版本化；改变切片器即创建新索引版本，不能在原 Active Chunk 上无痕覆盖。

#### 3.21.9 Embedding 生成

Embedding Worker 只处理清洗、审核通过的 Chunk，输入采用稳定模板（标题路径 + 关键 Metadata + 正文），记录 `embedding_model_id、dimension、input_hash、status`。同一模型/输入 Hash 幂等；批量调用需限流、超时、指数退避和失败队列，不把 API Key/原始敏感文档写日志。

Query 与 Document 必须使用兼容模型和相同归一策略；维度由 Model Registry 校验，不能在数据库列中静默混用。升级模型按 3.8.15 双写/回填/评测/切流，旧向量在回滚窗口结束后清理。

#### 3.21.10 Metadata 权限标签

每个 Document/Chunk 至少携带并校验：

```json
{
  "scope_type": "platform|store|product",
  "scope_no": "...",
  "store_no": "...",
  "product_no": "...",
  "visibility": "public|store_private|platform_internal",
  "allowed_agent_types": ["exclusive", "store"],
  "language": "zh-CN",
  "source_type": "store_policy",
  "source_version": "...",
  "effective_from": "...",
  "effective_to": null,
  "sensitivity_level": "L0"
}
```

权限关键字段使用正式列和索引，JSONB 只作扩展，不以模型生成标签为准。Metadata 在摄取时从受信业务资源派生，发布时复核；检索 SQL 必须包含 Visibility、Agent Type、Scope、Status 和生效时间过滤。

#### 3.21.11 关键词检索

关键词通道使用 PostgreSQL Full Text Search + GIN，并为中文采用经过许可、安全评审、版本固定的分词方案；若不安装扩展，则由应用侧受控分词生成 `search_lexemes`。标题、型号/SKU/品牌、Heading 和正文设置不同权重，精确型号/条款号优先于普通文本。

使用 `websearch_to_tsquery/plainto_tsquery` 等参数化函数生成 Query，禁止拼接 SQL；可用 `ts_rank_cd` 结合词频、字段权重和邻近度排序。停用词、同义词和行业词典版本化，否定条件/单位不能被错误去除。

#### 3.21.12 向量检索

向量通道默认使用 Cosine Distance 与 HNSW，查询必须限定同一 `embedding_model_id`，并在 SQL 内应用 Active、Visibility、Scope、语言和有效期过滤。HNSW 的 `m=16、ef_construction=64、ef_search=40` 仅为起点，以真实语料 Recall/Latency 校准。

过滤导致候选不足时启用 pgvector Iterative Scan 并受 `hnsw.max_scan_tuples/scan_mem_multiplier` 控制；高选择性租户可用部分索引/分区。离线评测用精确检索作为 Recall 基线，不能只观察近似检索返回速度。

#### 3.21.13 混合检索

关键词和向量通道并行，各自产生带 Rank 的候选；精确 SKU/型号命中作为独立高权重通道。首版用 Reciprocal Rank Fusion，避免直接比较不可比的 BM25/FTS 分数与向量距离：

```text
rrf_score(d) = Σ channel_weight(c) / (k + rank_c(d))
```

`k=60` 可作为起始值，权重按 Query 类型调整：型号查询提高 Exact/Keyword，概念咨询提高 Vector。融合后按 Document Version 去重、控制同文档片段数量并保留来源多样性，再交 Rerank；所有参数写入 `retrieval_logs` 以便回放。

#### 3.21.14 Rerank

Reranker 对融合后的少量候选结合“原始问题 + 标题 + 片段 + 必要 Metadata”重新排序，优先使用专用 Cross-Encoder/受评测重排模型。不得向第三方模型发送超出授权的私有内容；店铺私有知识应选择允许的数据处理区域/本地模型。

建议从 20–50 个融合候选重排为 5–10 个，再按 Context Budget 选最终片段。重排超时/不可用时降级到 RRF 排名，并标记 `retrieval_mode=hybrid_without_rerank`；不得为等待重排拖垮消息 SLO。

#### 3.21.15 Top-K 与阈值

首版可采用 Keyword K=30、Vector K=30、融合后 K=30、Rerank 输出 5–8、最终注入 3–6 个片段作为起点，但不能全类型共用固定值。按 FAQ、型号、政策、长文解释及店铺/平台 Scope 分别在标注集校准。

可靠性门槛同时考虑精确命中、融合/重排分数、文档权威等级、版本/时效、候选间一致性和覆盖度。Top-K 已满不等于可回答；最高分仍低于该 Query Type 阈值时返回 Empty/Low Confidence。阈值变更需版本化和 A/B/离线回归。

#### 3.21.16 店铺范围过滤

店铺客服检索条件固定为：当前店铺 Scope 的 Active 公开/授权文档，加必要的 Platform Public 文档；严禁检索其他店铺文档。`store_no` 从会话和认证上下文派生，客户端 Query/Prompt 中出现的店铺名仅作搜索词，不改变 Scope。

专属客服进行全平台商品搜索时只能访问各店 Public 商品知识；涉及某订单/售后时，可按订单归属临时限定对应 Store 的用户可见政策。`store_private/platform_internal` 不因向量近似或过滤降级而放宽。

#### 3.21.17 商品范围过滤

在商品详情会话中优先以 `product_no` 过滤，并允许召回其所属 Category/Brand 的公开通用知识；SKU 咨询还需精确 `sku_no/model`。跨商品比较时由 Catalog Tool 先返回用户可见 Product Set，RAG 只在该集合内检索，不允许模型构造任意 ID 扩大范围。

商品被下架、删除或 Store 隔离时，Outbox 立即使相关文档/Chunk 失效；缓存命中后也要重验 Active/Visibility。商品知识不能用于证明实时可售性。

#### 3.21.18 引用来源展示

每个最终片段生成不可伪造的 Citation Ref，关联 `document_no、chunk_no、source_version、title/heading、effective_at、public_url/resource_ref`。模型通过结构化引用标记引用，后端校验“引用存在、当前可见、确实在本次 Retrieval Result 中”后再渲染。

前端在回答后展示“依据”列表：商品/FAQ 链接到详情对应位置，政策显示标题、版本和生效日期，上传文件显示文件名/页码（仅在用户有权访问时）。不得暴露对象存储私有 Key、内部 Chunk ID、店铺私有标题或签名 URL；下载链接按需短时签发。

#### 3.21.19 无可靠结果时的拒答

以下情况不作具体事实断言：无结果或低于阈值；来源过期/冲突且无法确定优先级；问题要求实时数据但 Tool 不可用；Scope 不允许；文档解析质量不足；只有模型常识而无所需官方依据。

回复应简明说明“暂时没有找到可靠依据/当前无法确认”，保留用户问题条件，并按场景提供商品页、规则中心、店铺客服、专属客服或人工入口。不得虚构政策、型号、库存和 Citation，也不得用训练知识补齐本应由平台权威源回答的高影响问题。

#### 3.21.20 知识更新与重新索引

MySQL 发布/更新/下架或对象文件审批产生 Outbox Event；Index Worker 创建新 Document Version，完成清洗、切片、Embedding 和完整性校验，在影子索引执行冒烟/回归检索，通过后原子把新版本置 Active、旧版本置 Superseded，并发布缓存失效事件。

失败时旧有效版本继续服务，但若内容被撤回、删除或存在安全风险，必须先立即停止旧版本召回，再处理重建。每天按 Source Hash/Version 对账 MySQL、对象存储与 PostgreSQL；缺失/孤儿/状态漂移进入修复任务和告警。全量重建使用独立 Index Version，可限速、暂停和回滚。

#### 3.21.21 RAG 缓存

Redis 缓存的是候选 Chunk Ref/分数/索引版本，而不是无边界的最终回答。Key 至少包含 `scope_hash、normalized_query_hash、language、embedding_model_id、index_version、retrieval/rerank_version、filters_hash、top_k`；私有 Query 不保存原文，User Scoped 内容需包含不可逆 User Scope Hash。

公开知识 TTL 可从 5–30 分钟起调，版本/下架事件主动失效；用户/店铺私有检索使用更短 TTL 或不缓存。命中后必须从 PostgreSQL 重验 Active、Visibility、Scope、Effective Time，禁止因缓存绕过授权。Redis 不可用时回源检索/限流降级，不影响权限正确性。

#### 3.21.22 RAG 评估指标

评估分三层，且按 Agent 类型、知识类型、语言、店铺规模、查询类型和是否含精确实体分桶：

| 层级 | 核心指标 | 目标/说明 |
| :--- | :--- | :--- |
| Retrieval | Recall@K、Precision@K、MRR、nDCG@K、Hit Rate | 以人工标注相关 Chunk 与精确检索为基线 |
| Answer | Groundedness/Faithfulness、答案正确率、Citation Precision/Completeness、拒答 Precision/Recall | 回答主张必须由所引片段支持 |
| System | P50/P95/P99 Latency、Empty/Degraded Rate、Token/Embedding/Rerank Cost、Freshness Lag | 兼顾质量、时效、成本和可用性 |
| Safety | 跨用户/跨店铺泄漏、越权引用、Prompt Injection 成功率、敏感信息暴露 | 泄漏与越权目标为 0，作为发布阻断项 |

建立版本化 Golden Set，覆盖同义问法、否定/多条件、SKU/型号、过期政策、冲突文档、无答案、恶意文档、跨店越权和实时问题误入 RAG。每次更换 Embedding、Chunker、索引参数、Reranker、词典或阈值均跑离线回归；灰度阶段记录线上点击引用、追问、人工纠错和拒答反馈，但用户内容进入评估集必须经过授权、脱敏和保留期治理。检索日志只保留必要 Query Hash/脱敏文本、候选引用和版本，使一次错误回答能够从 Citation 追溯到 Document、Chunk、Index Job 与 Source Event。

---

### 3.22 MCP 设计

MCP（Model Context Protocol）是 Agent Runtime 与受控领域能力之间的标准协议层，不是绕过 Application Service 的“万能后门”。首版以 MCP `2025-11-25` 规范作为兼容与测试基线，实际连接必须在 `initialize` 阶段协商协议版本和 Capability；协议升级经兼容性测试、灰度和回滚后发布，不能只修改日期配置。

#### 3.22.1 MCP 在本系统中的定位

MCP 解决 Tool/Resource/Prompt 的发现、Schema 和调用互操作；商城权限、交易状态机、Consent、Approval、幂等和审计仍由本系统负责：

```text
LLM Tool Proposal（不可信）
  → Agent Runtime / MCP Host
  → Tool Registry + Permission Gateway + Approval Gate
  → MCP Client（每个 Server 独立会话）
  → MCP Server Adapter
  → Application Service → Domain → Repository/Integration
  → Output Schema + Result Policy + Context Builder
```

内部 MCP 复用 REST 同一 Application Service，不复制业务规则。MCP 不直接连接 MySQL/Redis/支付渠道；MCP Server 即使通过服务身份认证，也必须将最终 Actor 解析为当前用户并重新校验资源归属。RAG Pipeline、模型调用、Context Builder 和 Graph State 属于 Runtime 内部 Port，不必为了“全面 MCP 化”暴露为 Tool。

#### 3.22.2 MCP Host

Agent Runtime 是唯一生产 MCP Host，职责包括：创建/管理多个 MCP Client；协议与 Capability 协商；加载经发布的 Server/Tool 快照；向模型只暴露当前 Agent/Skill 可用工具；执行输入校验、权限、确认、限流、Deadline、审计和结果裁剪；聚合 Resources/Prompts；维护连接健康与 Kill Switch。

Host 不把所有 `tools/list` 结果原样给模型。它以 `ai_tools.current_version_id + ai_tool_versions + ai_skill_tool_bindings + ai_tool_permission_policies` 为发布事实源，将 Server 发现结果与不可变 Version 中的 `server_code/remote_tool_name/version_no/schema_hash/manifest_hash` 比对；未知或发生漂移的 Tool 进入隔离并告警，审核发布前不可调用。首版 Host 不向 MCP Server 开放 Sampling、Elicitation、Roots、任意 Task 等客户端能力，确有需求时按 Capability 单独威胁建模和启用。

#### 3.22.3 MCP Client

每个 Client 只连接一个已注册 MCP Server，维护独立协议版本、Capability、Session、超时、连接池、断路器和指标，不跨 Server 共用会话状态。生产容器间使用 Streamable HTTP；`stdio` 仅用于本地开发/受控测试，以非特权子进程和最小环境运行。

Client 初始化顺序为 `initialize → 校验返回版本/能力/Server Identity → notifications/initialized → Operation`。HTTP 后续请求携协商后的 `MCP-Protocol-Version`；Session ID 只作为连接关联符，必须和服务身份/授权主体绑定，不能单独充当认证凭证。Client 支持协议取消通知和进度，但任何进度都不能无限延长硬 Deadline。

#### 3.22.4 MCP Server

每个 Server 只负责一个清晰领域，保持无业务编排或极少编排：验证服务身份和调用上下文、检查 Tool Schema、调用对应 Application Service、返回结构化最小结果。Server 不读取完整会话、其他 Server 状态、System Prompt、Agent Memory 或无关用户数据，也不能调用另一个 MCP Server 形成隐式链路。

Server Descriptor 固定在代码/部署配置和 Secret Manager 中，包括 `server_code、endpoint、certificate/audience、allowed_protocol_versions、expected_capabilities、network_policy、owner、health_endpoint`；不提供用户/店铺动态填写 Server URL。横向扩容时 Session 路由和恢复必须防劫持，每次请求仍校验 Access Token Audience、Scope 和当前授权，不仅依赖 `MCP-Session-Id`。

#### 3.22.5 MCP Resources

Resources 是由 Host/Application 选择加入上下文的只读内容，适合小型、稳定、可引用资源；它不是批量导库接口。首版可提供受限 URI Template：

- `ecom://catalog/products/{product_no}/public-summary`；
- `ecom://catalog/stores/{store_no}/policy/{policy_type}`；
- `ecom://orders/{order_no}/user-summary`；
- `ecom://support/tickets/{ticket_no}/public-status`。

`resources/list`、`resources/templates/list`、`resources/read`、`resources/subscribe` 只有 Server 声明对应 Capability 且 Registry 允许时使用。URI 必须按 RFC 3986 解析、限制 Scheme/Template/长度并逐次鉴权，不能支持任意 `file://`、对象存储 Key、SQL、HTTP URL 或目录遍历。大规模商品不使用 `resources/list` 全量枚举，应由 Catalog Search Tool 返回受限 Resource Link；缓存命中也要重验 Scope、状态和版本。

#### 3.22.6 MCP Prompts

协议 Prompts 是用户可选择的模板，不等同于 Host System Prompt 或 3.23 Skill。首版只允许发布低风险、无权限效果的交互模板，例如“比较已选商品”“解释当前订单状态”；用户在 UI 主动选择后，Host 校验参数并把内容当作普通 User-level Context。

MCP Prompt 不能绑定/解锁 Tool、改 Agent 身份、提升预算、覆盖安全规则或直接执行动作；Server Instructions 和 Prompt Message 也按不可信外部内容处理。Prompt Version/Hash 纳入发布审核，`notifications/prompts/list_changed` 只触发重新拉取和隔离比对，不自动加入生产。若暂时没有明确 UX，首版可不声明 `prompts` Capability，Skill 仍由内部 Registry 管理。

#### 3.22.7 MCP Tools

Tools 是可由模型提出调用的结构化能力。每个 Tool 必须具有稳定 `name/title/description/inputSchema/outputSchema`，并在内部 Registry 补充 Server、版本、Schema Hash、Side Effect、Risk、Confirmation、Idempotency、Deadline、Data Classification、Result Policy 和 Owner。

Tool Annotation（`readOnlyHint/destructiveHint/idempotentHint/openWorldHint`）只作展示和规划提示，即使来自内部 Server 也不能替代 Policy。`notifications/tools/list_changed` 后若名称、Schema、Annotation 或版本与发布快照不同，Host Fail Closed。Tool Result 优先返回 `structuredContent`，同时可给简短 `content`；业务拒绝/未找到使用稳定业务 Error DTO，协议/Schema 错误使用 JSON-RPC Error。结果中的自由文本始终是不可信数据。

#### 3.22.8 catalog-mcp

Owner 为 Catalog Domain，全部业务操作首版只读：

| Tool | 主要输入 | 主要输出/限制 |
| :--- | :--- | :--- |
| `catalog.get_product` | `product_no`、可选 `sku_no` | 公开 Product/SKU、Money、可见状态、版本；Store Agent 强制当前店 |
| `catalog.compare_skus` | 同商品 `sku_nos` | 2–4 个 SKU 的结构化属性差异 |
| `catalog.search_store_products` | 受限筛选、Cursor | 当前店 On Sale 商品，3–20 条 |
| `catalog.search_products` | 平台公开筛选、Cursor | 专属客服全平台候选，广告标识保留 |
| `catalog.compare_products` | `product_nos` | 2–4 个公开商品的同维度比较 |
| `catalog.get_inventory_availability` | `sku_no`、数量 | `available/low/out_of_stock/unknown` 与 `as_of`，不返回精确库存 |
| `catalog.get_store_policy` | `store_no`/订单引用、Policy Type | 当前政策或订单政策快照引用 |

所有筛选、排序和分页字段白名单化；不得暴露成本、进货价、安全库存、销售报表、草稿商品/政策或任意店铺客户数据。成交价和库存下单时仍由 Checkout Service 重验。

#### 3.22.9 order-mcp

Owner 为 Order Domain，首版仅提供本人订单只读能力：

| Tool | Scope | 结果 |
| :--- | :--- | :--- |
| `order.list_user_orders` | Gateway 注入 Current User；时间/状态/店铺筛选有上限 | Cursor 列表、公开状态、Money 和最小商品摘要 |
| `order.get_user_order_detail` | Current User + `order_no` | 正交状态、订单项、Money、时间线、`available_actions` |
| `order.get_store_order_summary` | Current User + 当前 Store + `order_no` | 该店订单最小解释 DTO |

不返回完整地址、支付报文、风控/管理员/商家内部备注。订单写操作仍走页面或未来专门评审的 Command Tool；不得提供通用 `update_order`。资源版本和 `as_of` 必须返回，卡片中的订单号不代表授权。

#### 3.22.10 logistics-mcp

Owner 为 Logistics Domain，提供 `logistics.get_user_order_shipments` 与受当前 Store 约束的 `logistics.get_store_order_shipments`。输入只接受本人 `order_no`/可选 `shipment_no`，输出为包裹、关联订单项、承运商、运单掩码、标准状态、用户可见轨迹、预计范围和 `last_synced_at`。

Provider 超时可返回数据库最后已知轨迹和明确 `stale=true`，不能推断已签收；不提供创建发货、改运单、改轨迹、确认收货等写 Tool。物流描述作为不可信自由文本裁剪，不能驱动其他工具调用。

#### 3.22.11 after-sale-mcp

Owner 为 After-sales Domain，读写能力严格分离：

| Tool | 副作用 | 关键控制 |
| :--- | :---: | :--- |
| `after_sale.check_refund_eligibility` | 无 | 本人 Order Item、规则/资源版本、短 TTL Eligibility Token |
| `after_sale.build_refund_draft` | 临时 | 不创建售后；Money 由 Service 计算，Draft 有 TTL |
| `after_sale.submit_refund_application` | 写 | Action Approval、Arguments Hash、资源重验、`action_no` 幂等 |
| `after_sale.list_user_refunds` | 无 | Current User、Cursor/时间限制 |
| `after_sale.get_user_refund_detail` | 无 | 本人 Refund，用户可见状态/资金摘要 |

Submit 只创建退款申请，不批准退款、不确认退货、不调用渠道退款。超时后返回 Outcome Unknown 并按 `action_no/idempotency_key` 查询结果，禁止盲目重放。

#### 3.22.12 support-mcp

Owner 为 Support Domain：`support.create_store_ticket` 强制当前 Store Conversation；`support.create_platform_ticket` 强制 Exclusive Conversation；`support.get_ticket_status` 仅返回当前会话工单公开状态。创建操作属于可撤销影响有限的运营写，但仍要求用户明确请求/路由规则、幂等和频率限制。

模型只能传 `ticket_type、reason_code、resource_refs、redacted_summary`，不能指定客服、队列外 Store、优先级、SLA 或读取 Internal Note。创建成功后返回真实 `ticket_no/status/queue_display_name`；失败不能声称已排队。

#### 3.22.13 memory-mcp

Owner 为 Memory Service，能力包括：

- `memory.list_preferences`、`memory.get_store_preferences`：Consent 有效时返回当前用户/Scope 的少量 Active 记忆；
- `memory.propose_preference_change`：只创建 Candidate/确认卡片，不无感激活；
- `memory.propose_store_preference`：店铺 Agent 专用，Gateway 固定当前 User+Store，只创建 `data_classification=L2、memory_risk_level=low` Candidate，不具备 Active 权限；
- `memory.update_preference`：绑定用户确认，创建新版本并 Supersede 旧版本；
- `memory.delete_preference`：绑定本人确认，Tombstone 和派生清理任务；
- `memory.get_operation_status`：查询异步清理公开状态。

身份、Namespace、Store Scope 和 Consent 由 Gateway 注入；模型只能传 `memory_key`、规范化候选值和用户来源引用。用户偏好始终是 L2，仅 memory risk=low 的 Allowlist 类别可进入确认流程。禁止通用全文搜索所有记忆、跨用户/跨店访问、写 L3 信息、返回 Embedding/内部置信规则或把 RAG 文档写成用户记忆。

#### 3.22.14 MCP 输入输出 Schema

Tool Schema 使用 JSON Schema 2020-12（未显式声明时 MCP 当前规范的默认 Draft），并由 Pydantic 生成/校验；发布时保存 Canonical Schema Hash。规则：顶层必须为 Object；`additionalProperties=false`；字段有长度/枚举/格式/数组数量/数值边界；公开 ID 使用 String；Money 使用 `{minor_units: "8990", currency: "CNY"}`；时间为 UTC RFC 3339；Cursor 是不透明字符串；禁止任意 URL、SQL、路径和未限大小的自由 JSON。

统一输出：

```json
{
  "status": "succeeded|not_found|denied|conflict|unknown|degraded",
  "data": {},
  "resource_refs": [{"type": "order", "no": "ord_...", "version": "8"}],
  "as_of": "2026-08-21T10:00:00Z",
  "warnings": [],
  "error": null
}
```

`outputSchema` 验证失败视为 Server Contract Incident，结果不得进入模型。返回 DTO 由 `result_policy` 再裁剪；分页、大文件使用 Cursor/Resource Link，不在 Tool Result 中 Base64 堆入上下文。

#### 3.22.15 用户身份注入

模型和 Tool Arguments 不包含可覆盖的 `user_no/store_no/conversation_no/agent_version_no/skill_version_no`。Host 从已验证 Session、Conversation 和 Run 构造 `TrustedCallContext`，包含上述 Scope、`action_no、trace_id、consent_refs、approval_no、deadline、data_classification`，再用短时、Audience 绑定的内部 Access Token/签名 Header 传给 Server；生产内部链路使用 mTLS。

Server 验证 Issuer、Audience、签名、过期、Nonce/Action、允许 Scope 和服务身份，并重新从 MySQL 解析业务资源归属。禁止把浏览器 Access Token 或 MCP Client Token 原样透传给下游 Provider；Server 调用第三方时使用自己的最小权限凭证，防止 Token Passthrough/Confused Deputy。`stdio` 测试仅注入临时测试身份，不继承开发机全部环境变量。

#### 3.22.16 MCP 工具权限拦截

拦截分两次：Host 调用前执行 Registry/Policy/Consent/Approval/参数/预算校验；Server 执行前验证服务身份、Trusted Context、资源归属和 Domain Guard。有效权限取 `Agent Version ∩ Skill Version ∩ Tool Binding ∩ Permission Policy ∩ Conversation Scope ∩ Consent ∩ Approval`，显式 Deny 优先。

`tools/list` 只表明 Server 声称提供能力，不代表当前 Agent 可以用。Host 给模型的工具集合按本 Run 最小化；Tool Name/Schema/Server Capability 与发布快照不一致、身份缺失、策略服务不可用或 Scope 冲突时 Fail Closed。拒绝返回通用业务码，不向模型暴露内部权限规则或其他资源是否存在。

#### 3.22.17 MCP 工具二次确认

MCP 协议的人机交互建议不能替代本系统 Approval。写 Tool 流程为“模型提出 → Host 创建 Proposal/Approval → Graph Interrupt → 前端结构化确认 → Resume → 重验 → 才发送 `tools/call`”。首版不使用 Server Elicitation 收集交易确认、密码、验证码或支付信息，避免 Server 自行决定确认 UI。

Approval 一次使用，绑定 User、Conversation、Tool/Agent/Skill Version、规范参数 Hash、资源版本、Money/Quantity 和有效期。Server 收到写调用仍验证 Approval Claim/Action Audit；参数变化、资源变化、超时或重复消费均拒绝。只读 Tool 默认无需逐次确认，但首次长期授权、敏感读取及产品策略要求的场景仍走 Consent/Session Confirmation。

#### 3.22.18 MCP 超时与重试

每次调用传播绝对 Deadline，区分连接、首响应、空闲和总超时。首版起点：Catalog 只读 2–3 秒，Order/Logistics/Memory 3–5 秒，资格/草稿 5–8 秒，确认提交 10–15 秒；以压测 SLO 调整且不得超过 Run 剩余预算。超时后 Client 发送 Cancellation Notification 并停止等待，但不能假设远端已停止副作用。

确定未执行的幂等读可按指数退避 + Full Jitter 重试 1–2 次；认证、权限、Schema、业务冲突不重试。写操作只有 Tool 声明并实现幂等、沿用同一 `action_no/idempotency_key` 时才可重试；响应丢失/超时先调用结果查询或对账，状态保持 `unknown`。收到 Progress 可延长空闲计时，硬 Deadline 不延长。

#### 3.22.19 MCP 熔断与降级

按 `server_code + tool_code + failure_class` 建断路器，并配置连接池舱壁、并发/队列上限、Half-open 探测和全局 Kill Switch。权限拒绝/业务 Not Found 不计入服务故障率；连接错误、5xx、Contract Error 和连续超时分别统计。

降级遵循 3.17/3.18：Catalog/Order 不可用则提供页面入口而不猜事实；Logistics 可展示带时间的最后快照；RAG/Memory 不可用进入无个性化/关键词模式；After-sale Submit 不可用不得生成可执行确认。Server Tool List/Schema 异常时只隔离该 Server/Tool，不自动切到名称相似的外部工具。

#### 3.22.20 MCP 调用审计

每次“提议、拒绝、确认等待、开始、成功、失败、取消、Unknown、对账完成”都关联 `ai_tool_action_audits.action_no`。除 3.7 字段外记录 `server_code、remote_tool_name、tool_version、protocol_version、server/session_hash、capability/schema_hash、client_attempt、deadline、transport、request/response_size、latency、retry/circuit_state、result_resource_ref`。

参数/结果只保存白名单脱敏摘要和 Hash，不记录 Token、Session ID 原值、完整 Prompt、地址、证件或凭证。高风险操作审计写入失败则 Fail Closed；只读审计不可用按安全策略降级并告警。Trace 能串联 Run → Skill → Delegation → MCP Call → Domain Transaction/Outbox，但查看审计仍受 RBAC/租户边界约束。

#### 3.22.21 禁止提供的高风险 MCP 工具

生产 Registry 永久拒绝以下通用或越权能力：

- `execute_sql/run_shell/eval_code/arbitrary_http_request/read_file/write_file` 或任意 URL/路径代理；
- 返回数据库 Schema/全表、Secret/环境变量、System Prompt、原始 Trace/日志、完整用户记忆/消息导出；
- 任意用户/店铺身份切换、生成/修改 Consent/Approval/审计记录、提升 Agent/Skill/Tool 权限；
- 通用 `update_order/update_status`、标记支付成功、修改支付金额、伪造回调、直接确认收货；
- 修改库存/价格/商品/店铺政策、创建物流轨迹或绕过 Checkout/Domain State Machine；
- 批准/拒绝售后、修改批准金额、确认退货验收、直接发起渠道退款；
- 动态安装 MCP Server、修改 Server URL/证书/Schema、启用 Sampling/Elicitation/Roots；
- 关闭 Guardrail、限流、审计、脱敏、隔离或 Kill Switch。

确有运营需求的高危能力必须建设为独立后台命令，由管理员 RBAC、MFA、职责分离、确定性表单和业务审计执行，不包装成面向客服 Agent 的 MCP Tool。

---

### 3.23 Skill 系统设计

Skill 是商城 Agent 可按需选择的、版本化的领域能力包。它将“何时使用、需要什么输入、执行哪些确定性/模型步骤、允许访问哪些知识和工具、何时确认、如何失败、如何评估”固化成发布产物，使能力可以独立测试、授权、灰度和回滚。

#### 3.23.1 Skill 定义

一个完整 Skill 由以下不可分割的发布内容组成：

```text
Skill Metadata
  + Instruction / Examples
  + Input Schema / Output Schema
  + Execution Graph/Handler + Step Limits
  + Knowledge Scope
  + MCP Tool Bindings + Argument/Result Policy
  + Permission / Consent / Confirmation Declaration
  + Model / Token / Time / Cost Budget
  + Error / Retry / Degradation Policy
  + Test Set / Evaluation Threshold / Release Record
```

Skill 可以实现为确定性 Service Handler、LangGraph Subgraph 或两者组合。简单订单查询优先确定性 Workflow；只有需要多步消歧、检索和解释时才使用模型循环。Skill 不拥有用户身份、业务数据库或长期记忆；运行时由父 Agent 传入受限 `SkillCallContext`。

#### 3.23.2 Skill 与 Prompt 的区别

| 对比项 | Prompt | Skill |
| :--- | :--- | :--- |
| 本质 | 一段消息/模板 | 有 Schema、执行、权限、预算和评估的能力包 |
| 是否可执行 Tool | 自身不能授权 | 仅可使用显式绑定且策略允许的 Tool |
| 输入输出 | 通常弱结构 | 强 JSON Schema/Pydantic Contract |
| 生命周期 | 可作为版本内容的一部分 | 独立定义、版本、测试、发布、回滚 |
| 安全边界 | 只能表达规则 | Registry/Gateway/Domain 共同强制 |
| 观测 | Prompt Hash/模型输出 | 端到端 Skill Run、步骤、Tool、成本和任务结果 |

System Prompt 定义 Agent 总体行为；MCP Prompt 是用户可选模板；Skill Instruction 只说明某项能力内部如何完成。无论写得多强，Prompt/Instruction 都不能扩大 Tool Permission、Scope 或确认等级。

#### 3.23.3 Skill 输入输出 Schema

Skill 输入分为两部分：`trusted_context` 由 Runtime 注入，不可由模型覆盖；`task_input` 来自用户/模型但经 Schema 校验。示例：

```json
{
  "trusted_context": {
    "run_no": "run_...",
    "user_no": "usr_...",
    "store_no": null,
    "conversation_type": "exclusive",
    "agent_version_no": "agv_...",
    "deadline": "2026-08-21T10:00:30Z"
  },
  "task_input": {
    "order_no": "ord_...",
    "question": "为什么还没有发货"
  }
}
```

输出至少包含 `skill_status、answer_facts、resource_refs、citations、available_actions、warnings、error、handoff_request`，事实与展示文案分离。Schema 顶层禁用额外字段，限制字符串/列表/候选数量；Money/时间/ID 延续 3.22 规范。父 Agent 只接受通过 Output Schema、Scope 和 Source 校验的结果。

#### 3.23.4 Skill 版本管理

`ai_skills` 保存稳定身份与当前版本指针，`ai_skill_versions` 保存不可变版本内容。任何 Instruction、Schema、Graph/Handler、Tool Binding、Knowledge Scope、确认、预算、模型或降级策略变化都创建新版本并重新计算 Hash；只改名称/描述等非执行元数据也要审计。

版本使用单调递增 `version_no`，运行时记录 `skill_version_id/instruction_hash/schema_hash/execution_config_hash`。Agent Version 发布时冻结 Skill Version 集；已开始 Run/Checkpoint 继续使用原版本，不能跟随 `current_version_id` 漂移。Schema 不兼容时发布新 Version/Tool Binding，不在原版本上静默修改。

#### 3.23.5 Skill 生命周期

Definition 状态沿用 `draft → active → disabled → archived`；Version 状态为：

```text
draft → testing → published → deprecated
  │        │          │
  └─修改───┘          └─仍可供存量 Run 恢复，禁止新绑定
```

测试失败的版本保持 `testing` 并关联失败报告，不另造“可绕过”状态。一个 Skill 可有多个 Published 版本用于灰度/存量恢复，但只有一个默认 Current Version。`disabled` 是运行开关：新调用立即拒绝，风险事件可同时取消尚未执行的 Pending Action；`archived` 仅在无运行/绑定依赖后使用。

#### 3.23.6 Skill 发布与回滚

发布流水线：静态检查 → Schema/依赖锁定 → 单元/契约/安全测试 → Golden Evaluation → 沙箱集成/故障注入 → 人工审批 → 小流量 Shadow/Canary → 指标观察 → 扩量。Transaction Skill 需 AI Owner、业务 Owner 和 Security/Compliance 职责分离审批。

回滚通过原子切换 Agent/Skill 发布指针和绑定快照到上一个已通过版本完成，不改历史行。新 Run 使用回滚版本；已在等待 Approval 的 Run 若 Tool/Arguments/Policy 不兼容，应使 Approval 过期并重新生成，不能套用旧确认。紧急 Disable/Kill Switch 优先于常规回滚，操作写管理员审计和 Release Record。

#### 3.23.7 Skill 与 Agent 绑定

`ai_agent_skill_bindings` 只绑定不可变 `agent_version_id + skill_version_id`，并记录 Priority、Trigger 和受控 Override。发布验证：Scope 一致；Agent 类型允许；Skill 风险不超过 Agent Policy；所需模型/知识/Tool 可用；预算不超过父级；所有测试门禁通过。

Router 只能在当前绑定集中选择 Skill。若多个 Skill 匹配，优先确定性意图/资源上下文，再使用轻量分类；低置信时追问或选择无 Tool 的通用回答，不同时运行多个有重叠写权限的 Skill。Skill 不能在运行时加载未绑定 Skill 或自我修改绑定。

#### 3.23.8 Skill 与 MCP Tool 绑定

`ai_skill_tool_bindings` 固定 Tool Version，并用 `argument_policy` 约束可传字段、固定 Scope、数量/金额/时间范围，用 `result_policy` 约束返回字段、脱敏、行数/文本长度。绑定中的 `requires_confirmation` 只能比 Tool Policy 更严格。

Skill 调用 Tool 前由 Runtime 重新解析 Binding，不能仅根据 Instruction 中出现的 Tool Name。Tool 升级时先做 Schema Compatibility/Contract Test，再发布新的 Skill Version；Server 动态发现同名 Tool 不自动替换。一个 Transaction Tool 默认只绑定一个职责明确的 Transaction Skill，防止通过普通查询 Skill 间接调用。

#### 3.23.9 Skill 权限声明

每个 Skill 声明：允许的 Agent Type、Platform/Store Scope、数据分类上限、Read/Write/External Write、资源类型、Consent 类型、Confirmation Level、网络/文件需求和禁止项。权限声明是“申请上限”，最终允许值取 Agent、Skill、Tool、会话、用户授权和业务状态的交集，Deny 优先。

店铺 Skill 必须显式 `store_scope=current_conversation_store`；专属 Skill 即使为 Platform Scope，也只可读取 Current User 数据和公开商品。权限服务不可用、高风险策略版本不匹配或声明与 Tool Binding 冲突时 Fail Closed。

#### 3.23.10 Skill 用户确认策略

| Level | 场景 | 交互 |
| :--- | :--- | :--- |
| `none` | 公开知识、普通商品查询 | 无额外确认，仍鉴权/限流 |
| `session` | 本人订单/记忆等敏感读取 | 有效 Session Consent/一次告知，可撤销 |
| `action` | 提交退款、修改/删除记忆 | 每次结构化 Approval，参数绑定、短 TTL |
| `admin` | 权限/资金/配置高危动作 | 不提供给用户客服 Agent，后台 MFA/职责分离 |

Skill 不能在 Instruction 中降低确认等级，也不能把一句自然语言“可以”当 Approval。只要 Resource、Money、Quantity、Reason、Tool/Skill/Policy Version 变化就重新确认。Skill 返回 `approval_required` 后必须 Interrupt，不继续尝试同一写 Tool。

#### 3.23.11 Skill 限流与调用预算

每个 Version 配置硬预算：最大 Graph Steps、模型调用数、Tool 调用数、并行数、输入/输出 Token、RAG/Memory Top-K、列表行数、单次/总 Wall Time 和模型费用上限。父 Run 的剩余预算是上限，子 Skill 不能通过拆分调用获得新预算。

Redis 按 `user + agent + skill + store（适用）` 限制瞬时速率/并发，Provider/Tool 另设全局舱壁；关键交易防重复仍由 MySQL 幂等约束保证。超预算返回 `AI_SKILL_BUDGET_EXCEEDED`，保留已验证事实和手动入口；禁止为省 Token 跳过权限、引用或确认。

#### 3.23.12 Skill 超时与重试

Skill 接收父 Run 的绝对 Deadline，并把剩余时间分配给模型/RAG/MCP；每一步设超时，结束时保留最少合成/安全输出预算。只读、确定未执行的节点可有限重试；结构化模型输出可修复一次；权限、校验、内容安全和业务冲突不重试。

含写操作时以 `action_no` 为副作用边界：Checkpoint 恢复或节点重试先查 Tool Audit/业务资源；`unknown` 必须对账。整个 Skill 不做“从头重跑”来碰运气，尤其不能重复创建退款/工单/记忆版本。超时是否可重试由 Version Policy 明确，不由模型决定。

#### 3.23.13 Skill 降级策略

每个 Skill 在发布时定义按错误分类的降级矩阵：替代模型、关键词代替向量、去个性化、只读页面链接、人工入口、固定解释或 Fail Closed。降级不得扩大 Tool/数据 Scope，不把旧缓存冒充实时，不把草稿冒充提交，不把低置信知识改成模型常识。

依赖 Tool/Knowledge 缺失时应返回结构化 `degraded_reason + safe_fallback + retry_after/manual_path`。Transaction Skill 的 Approval/Checkpoint/审计/Domain Service 任一不可用都禁止写；普通问答可在仍有可靠来源时继续。

#### 3.23.14 店铺客服 Skill 列表

首版与 3.17.9 保持同一发布清单：

| Skill Code | 风险/副作用 | 主要边界 |
| :--- | :--- | :--- |
| `store_product_qa` | Low/Read | 当前店 Product/FAQ/RAG，带引用 |
| `store_sku_compare` | Low/Read | 同店同商品 2–4 SKU |
| `store_product_compare` | Low/Read | 当前店 2–4 个 Published/Visible Product，跨店整体拒绝 |
| `store_inventory_lookup` | Low/Read | 展示级 Availability，不给精确库存 |
| `store_policy_qa` | Low/Read | 当前店有效政策/订单政策快照 |
| `store_order_explain` | Medium/Read | 本人本店 Order 最小 DTO |
| `store_product_recommend` | Low/Read | 当前店 3–5 个公开候选，个性化需 Consent |
| `store_preference_candidate` | Medium/Candidate | 仅当前 User+Store 的 L2+low 风险候选；用户结构化确认后由 Pipeline 激活 |
| `store_human_handoff` | Medium/Write | 当前会话幂等创建店铺工单 |

店铺 Skill 能力矩阵为 `transaction_write=none、support_ticket_create=allowed、memory_candidate_propose=consent_and_confirmation_pipeline`；不具备跨店搜索、退款提交、订单状态修改、商品/库存/价格维护或全局记忆权限。店铺管理员首版只能配置经批准的展示文案/知识内容，不能自行发布可执行 Skill 或更换 Tool Binding。

#### 3.23.15 专属客服 Skill 列表

首版与 3.18.13 保持一致：

| Skill Code | 风险/副作用 | 主要边界 |
| :--- | :--- | :--- |
| `platform_policy_qa` | Low/Read | Platform Published RAG |
| `global_product_search` | Low/Read | 全平台公开 On Sale 商品 |
| `personalized_recommendation` | Medium/Read | Consent + `data_classification=L2、memory_risk_level=low` 已确认偏好 + 可解释推荐 |
| `user_order_lookup` | Medium/Read | Current User 订单 |
| `user_logistics_lookup` | Medium/Read | Current User 包裹/轨迹 |
| `refund_eligibility_check` | Medium/Read | 确定性规则引擎，不作审批 |
| `refund_draft_build` | Medium/Temporary | 短 TTL 草稿，不创建售后 |
| `refund_submit_with_approval` | High/Write | Action Approval + 幂等提交申请 |
| `refund_progress_lookup` | Medium/Read | Current User 售后/资金摘要 |
| `preference_memory_manage` | Medium/Write | Consent、确认、版本链/删除 |
| `platform_human_handoff` | Medium/Write | 当前专属会话平台工单 |

只有 `refund_submit_with_approval` 能提交退款申请；它仍不能批准/拒绝、改金额或执行渠道退款。未来新增下单、取消、确认收货等能力必须作为新 Skill 独立评审，不复用现有 Skill 扩权。

#### 3.23.16 Skill 测试用例

每个 Version 至少包含：

- Schema Contract：必填/枚举/边界/Unknown Field/超长输入与 Output 验证；
- Happy Path/No Result/Ambiguous/Conflict/Stale/Dependency Failure；
- 当前用户与资源归属、跨用户/跨店/跨会话 IDOR、伪造 Trusted Context；
- Prompt/RAG/Tool Result Injection、恶意商品说明和越权工具诱导；
- Consent 缺失/撤销，Approval 拒绝/过期/参数变化/重复消费；
- 并发、幂等、超时、取消、Unknown Outcome、Checkpoint 恢复；
- Token/Step/Tool/并行预算和 Circuit Breaker；
- 输出事实来源、Citation、错误措辞及“草稿/已提交”状态区分；
- 模型/Embedding/Reranker/Tool Fallback 与完全降级。

测试数据使用合成或脱敏 Fixture，明确多个用户、同名跨店资源和边界状态。Transaction Skill 在沙箱 Provider/隔离数据库运行，禁止测试误调生产写接口；固定 Random Seed/Model Snapshot 或保存可回放输入、版本和容差。

#### 3.23.17 Skill 评估与准入

评估包含任务成功率、结构化输出通过率、事实正确/引用支持、Tool 选择/参数正确、拒答/转人工质量、P95 延迟、Token/费用和用户体验。安全硬门禁为：跨用户/跨店泄漏 0、未确认写操作 0、重复副作用 0、越权 Tool 调用 0、失败/Unknown 被表述为成功 0；任一失败不得发布。

各 Skill 设置独立阈值和分桶 Golden Set，不用一个总平均分掩盖退款等高风险失败。模型 Judge 只作辅助，Schema/业务结果/权限/幂等使用确定性 Evaluator，关键样本人工复核。Canary 比较当前版本的成功率、纠错/转人工、错误码、成本和安全信号；达到回滚阈值自动停止扩量。

#### 3.23.18 Skill 管理后台

平台 AI 管理后台提供：Definition/Version 查看与草稿；Instruction/Schema/Graph/知识范围/Tool Binding/权限/预算的可视化 Diff；依赖与影响分析；测试集运行和失败回放；评估报告；审批、灰度、发布、回滚、Disable/Kill Switch；按 Agent/Store 查看绑定；Release/Audit 查询。

权限至少区分 Viewer、Skill Developer、Evaluator、Business Approver、Security Approver、Publisher，Transaction Skill 禁止同一人开发并最终发布。后台不显示/编辑 Secret，不允许自由填写生产 MCP URL/任意代码；发布操作要求近期认证、ETag、理由和 Append-only 审计。店铺方仅能查看自身可见版本和提交内容/需求，不能修改平台权限、Schema、Tool 或评估门槛。

---

### 3.24 Multi-Agent 设计

首版 Multi-Agent 采用 LangGraph 顶层 Supervisor/Router + 受限专业 Subgraph，不引入可自由互聊、相互创建或共享全部上下文的 Agent 群。面向用户始终只有店铺客服或专属客服一个会话入口和统一回复者；专业子 Agent 是内部执行单元，不形成新的用户会话。

#### 3.24.1 Multi-Agent 使用原则

默认优先级为“确定性代码/单 Tool → 单 Skill → 单 Agent 多 Skill → Multi-Agent”。只有满足以下至少一项且离线评测证明收益时才引入子 Agent：领域工具过多导致选择质量下降；专业上下文很大需要隔离；任务可拆为互相独立的只读子问题并行；不同团队需要以稳定 Schema 独立交付专业能力。

以下情况不使用 Multi-Agent：简单 FAQ/导航、一次 Tool 查询、单一退款状态查询、需要严格顺序的单业务命令、靠增加 Agent 数掩盖 Schema/业务规则不清。Agent 数量不是能力指标；每多一次委派都增加延迟、Token、失败面和审计成本，必须有可度量收益。

#### 3.24.2 Supervisor/Router Agent

店铺客服/专属客服的顶层 Graph 同时承担 Supervisor；Router 是其中一个受限分类节点，不另建拥有全权的“超级 Agent”。路由分两层：

1. 确定性路由：根据 Conversation Type、页面/商品/订单卡片、当前任务状态和明确 Intent 缩小候选；
2. 模型路由：只在白名单 Enum 内输出 `selected_agents、subtasks、dependency_edges、parallelizable、confidence`，通过 Schema/权限/预算校验后生效。

Supervisor 负责拆解、构造 Delegation Packet、调度、冲突处理、结果合成、引用验证、用户确认和最终回复。它不得给子 Agent 动态增加 Tool；低置信、任务简单或预算不足时选择 Skill/追问/人工，不强行 Fan-out。

#### 3.24.3 Catalog Agent

Catalog Agent 负责公开商品/店铺/品牌/SKU 事实检索、结构化搜索、属性比较和展示级库存解释。输入为受限 Query、已解析实体、候选 Product/SKU Ref、Store Scope 和预算；输出为带来源/版本/`as_of` 的 Product Facts、候选集合或“无法确认”。

允许 `catalog-mcp` 只读 Tool 和 Product/Store Public RAG；店铺会话强制当前 Store，专属客服可查全平台公开商品。禁止访问用户订单、记忆、售后、成本/精确库存，禁止生成不存在的 Product ID、修改商品/价格/库存。它只提供事实候选，不基于用户敏感信息做最终推荐。

#### 3.24.4 Order Agent

Order Agent 负责识别本人订单、解释订单/支付/履约/售后正交状态、Money、时间线和 `available_actions`。输入只含 Current User、可选 Store、Order Ref/筛选和用户问题；输出为最小 Order DTO、明确 Source Version、需要其他领域的 Resource Ref。

允许 `order-mcp` 只读 Tool；需要物流时返回 Shipment Query Request 给 Supervisor，而不是自行获得 Logistics Tool。禁止读取完整地址/内部备注/其他订单、执行取消/确认收货/支付或根据旧摘要推断状态。

#### 3.24.5 Logistics Agent

Logistics Agent 负责包裹拆分、订单项归属、标准物流状态、用户可见轨迹、同步时间和异常解释。必须接收已由父级/Order Service 验证归属的 `order_no`，Server 仍二次校验；输出按 Shipment 分组并明确 `last_synced_at/stale`。

只允许 `logistics-mcp` 读取，禁止修改运单/轨迹、确认收货、调用任意承运商 URL 或把预计时间说成保证。轨迹不足/长期异常时返回建议联系对应店铺/平台人工，不直接创建跨 Scope 工单。

#### 3.24.6 After-Sales Agent

After-Sales Agent 负责售后意图消歧、退款资格解释、所需信息/凭证提示、草稿字段准备和售后进度说明。它可调用资格、草稿和售后查询等无最终业务副作用 Tool，返回 `Eligibility/Draft/WriteProposal`；不拥有最终批准权。

`after_sale.submit_refund_application` 由父 Graph 的 `refund_submit_with_approval` Transaction Skill 在结构化 Approval 后，通过专用串行 Tool Executor 执行，而不是由开放式子 Agent 循环直接执行。After-Sales Agent 禁止改 Money、批准/拒绝售后、确认退货或发起渠道退款。

#### 3.24.7 Recommendation Agent

Recommendation Agent 负责把用户当前硬条件和经授权的低敏偏好转换为候选排序/解释。候选 Product Set 必须先由 Catalog Service 产生，它只能在可见集合内筛选、重排和生成理由，不能凭模型知识造商品。

允许 Catalog 只读、经 Consent 的 Memory Read 和受控 Ranker；不读订单明细、地址、投诉/退款频率、支付能力、风控标签或其他用户数据。输出包含满足/未满足条件、使用的 Preference Category、推荐多样性和商业位标识；无 Consent 时退化为当前会话条件推荐。

#### 3.24.8 Policy Agent

Policy Agent 负责平台规则、店铺政策和订单政策快照的检索、版本优先级、适用范围和引用整理。输入明确 `policy_scope、store_no/order_no、as_of_time、question`；输出为 Claims + Citation + Applicability/Uncertainty，不输出法律裁决。

允许 Scoped RAG、`catalog.get_store_policy` 和必要的只读 Policy Snapshot Port；禁止读内部治理/风控规则、其他店铺私有政策，禁止以当前政策覆盖历史订单快照。冲突/无可靠文档时返回拒答/人工建议，不用模型常识补造。

#### 3.24.9 子 Agent 工具权限

子 Agent 使用独立 `agent_type=specialist` 的 Agent Version 和 Tool/Skill Binding；权限为父 Agent 当前权限、子 Agent 声明、Delegation Scope、Skill/Tool Policy 和用户 Consent 的交集。Supervisor 有某 Tool 不代表所有子 Agent 继承它。

首版工具矩阵：Catalog 仅 Catalog Read；Order 仅 Order Read；Logistics 仅 Logistics Read；After-Sales 仅资格/草稿/查询；Recommendation 仅 Catalog + Consented Memory Read；Policy 仅 Scoped RAG/Policy Read。所有业务写集中在父 Graph 确定性 Executor。工具列表在每次委派创建时冻结，子 Agent 请求未绑定 Tool 立即拒绝并记录越权审计。

#### 3.24.10 子 Agent 上下文隔离

父 Agent 不传完整会话，而是构造版本化 `DelegationPacket`：

```json
{
  "delegation_no": "dlg_...",
  "parent_run_no": "run_...",
  "specialist": "logistics",
  "objective": "解释指定订单的两个包裹状态",
  "trusted_scope": {"user_no": "usr_...", "store_no": null},
  "resource_refs": [{"type": "order", "no": "ord_...", "version": "8"}],
  "user_constraints": ["只关心尚未送达的包裹"],
  "allowed_tools": ["logistics.get_user_order_shipments"],
  "deadline": "...",
  "budget": {"model_calls": 1, "tool_calls": 1, "max_output_tokens": 800}
}
```

最近消息只传完成目标必要的用户原句/摘要，敏感字段再次最小化。子 Agent 具有独立 Message/State Namespace 和 Context Budget；返回 `SpecialistResult`，不能直接写父消息历史。并行子图使用稳定唯一 Namespace，避免 Checkpoint Write 冲突。

#### 3.24.11 子 Agent 记忆策略

专业子 Agent 首版按 Invocation Stateless：不维护跨委派会话、不直接读取完整长期记忆、不写 `memory.items/summaries`。Recommendation 只接收父级 Memory Retriever 已按 Consent/用途裁剪的偏好；其他子 Agent 默认不接收长期记忆。

子 Agent 结果不是用户事实或长期记忆候选。父 Supervisor 完成回答后，统一由 Memory Candidate Pipeline 从用户原消息/已确认结果中提取。子图 Checkpoint 仅用于本次 Interrupt/恢复，随 Parent Run 生命周期清理；不得因采用 Checkpointer 把专业 Agent 变成跨用户共享状态。

#### 3.24.12 Agent Delegation

Delegation 表示 Supervisor 保持控制权，把一个有明确输入/输出和 Deadline 的子任务交给专业 Agent；子 Agent 返回结果，不直接面向用户。流程：验证是否值得委派 → 创建 `delegation_no` 和 Packet → 预留预算/权限快照 → 调用 Subgraph → 验证 Output Schema/Scope/引用 → 合并 → 关闭 Delegation。

委派必须是幂等的：同一 `parent_run_no + subtask_key + specialist_version` 重复调度返回已有结果或安全重试；取消 Parent Run 会传播 Cancellation，但已完成 Tool 结果保留审计。子 Agent 不得自行改 Objective、扩大 Resource Set 或把任务再转给另一个 Agent。

#### 3.24.13 Agent Handoff

Handoff 表示对话任务的“当前处理状态/专业配置”发生显式切换并可能跨多轮持续，区别于一次性 Delegation。适用例：专属客服从普通咨询进入需要收集退款材料的 After-sales State；切换通过受控 Graph Command 更新 `active_specialist/task_stage`，记录 From/To、Reason、State Version 和 Resume Owner。

Handoff Package 只含已确认事实、待补字段、资源/引用、已完成动作和安全限制；Tool Call Message 必须有配对结果，避免形成无效历史。用户仍看到专属客服统一身份。店铺客服与专属客服是不同 Conversation/Scope，不能内部静默 Handoff；应展示对应会话入口，由用户进入后重新鉴权。AI 转人工沿用 3.15 的工单状态机，不等同专业 Agent Handoff。

#### 3.24.14 只读任务并行

仅当子任务互不依赖、无副作用、Scope 一致、预算允许且结果可确定性合并时并行，例如已知 Product/Order Ref 后同时读取商品说明与公开政策，或对多个公开商品做独立事实整理。若物流查询依赖 Order Agent 先解析订单，则建立 DAG Edge，不能伪并行。

所有并行任务共享父级 Deadline/数据快照基准 `as_of`，但有独立超时/舱壁。Reducer 按 `delegation_no` 稳定收集并去重 Citation/Resource；一条失败不取消已有可靠结果。不同 Agent 对同一事实冲突时以权威 Tool、资源版本和时间处理，仍无法消解则向用户说明，禁止让合成模型静默投票。

#### 3.24.15 写操作串行

一个 Parent Run 同时最多存在一个待确认/执行的业务写。写路径固定为：专业 Agent/Skill 生成 Proposal → Supervisor 验证 → Approval Interrupt → 用户确认 → 父级 Serial Tool Executor 重新鉴权/状态校验 → 单一写 Tool → 对账/终态。并行读必须在生成 Approval 前结束或明确冻结所依赖版本。

Graph State 使用 `write_intent_no/write_owner/action_no/approval_no/resource_version` 作为写锁语义；任何子 Agent 都不能与 Executor 并行写。写结果 Unknown 时阻断后续依赖动作，先对账；失败补偿由 Domain Workflow/人工处理，不由另一个 Agent“反向操作”。

#### 3.24.16 最大委派深度

首版 `max_delegation_depth=1`：用户入口 Supervisor 可以委派专业子 Agent，子 Agent 不能再委派 Agent。子 Agent 内部可调用已绑定 Tool 或执行确定性 Subgraph Node，但不增加 Delegation Depth。未来确需两层必须提交架构/安全/成本评审，并对每一层定义 Scope 收缩和 Trace。

Depth 由 Runtime 在可信 `DelegationContext` 计数并硬校验，不能依赖 Prompt 自律；超限返回 `AI_DELEGATION_DEPTH_EXCEEDED`，Supervisor 选择直接处理、缩小任务或人工入口。

#### 3.24.17 最大子 Agent 数量

首版单 Run 最多创建 4 个 Delegation、同时运行最多 3 个专业子 Agent；退款/写意图存在时并行专业 Agent 最多 1 个。Router 必须按最小必要选择，不为“交叉验证”重复调用多个同职责 Agent。

上限同时受用户/租户并发、模型配额、Token/费用和下游舱壁约束，可按 Agent Version 配置得更低，不能调高超过平台硬上限。超限时优先保留与用户主目标相关、已有明确资源的任务，其余串行/放弃并说明范围。

#### 3.24.18 防止递归委派

Registry 构建有向 Agent Delegation Graph，发布时必须无环；首版只有 `store/exclusive supervisor → specialist` 边。Subagent Tool Set 不包含 Delegate/Handoff-to-specialist Tool，Runtime 校验 `ancestor_agent_versions、depth、parent_run_no`，同一 Specialist/Subtask Fingerprint 不得在一条链重复。

模型伪造 Delegation Packet、修改 Parent/Depth、要求“让另一个 Agent 再调用我”或通过 MCP Tool 间接调用 Agent 均拒绝。检测到循环/重复工作立即取消未开始任务，返回 `AI_DELEGATION_CYCLE_DETECTED` 并记录路由质量/安全事件。

#### 3.24.19 多 Agent 失败处理

失败按子任务隔离：Schema/权限错误不重试；瞬时只读失败在子任务预算内有限重试；模型失败可切同能力已评估版本；写相关 Unknown 只对账。Supervisor 接收结构化 `succeeded/partial/failed/denied/unknown/cancelled`，不得把子 Agent Error 文本当事实。

若部分成功，最终答复仅使用通过来源/Scope 校验的结果并明确未完成范围；关键依赖失败则降级 Tool/页面/人工，不拼凑。Parent 取消向子 Agent 传播，迟到结果以 Run/Lease/Version 判定是否丢弃；Checkpoint 恢复先读取已完成 Delegation/Tool Audit，避免重复 Fan-out。一个子 Agent 熔断不自动把其工具授予别的 Agent。

#### 3.24.20 多 Agent 成本控制

路由前估算 `expected_value vs model_calls/tokens/latency`，简单任务直接走 Skill。Parent Run 先保留最终合成/安全输出预算，再给子任务分配固定 Envelope；子 Agent 不可借用未授权余额或自行切昂贵模型。专业 Agent 使用符合任务的最小模型 Profile，结构化检索/比较优先代码和 Tool。

控制项包括：相同 Subtask Cache/去重、只传最小 Context、限制 Fan-out/输出 Token/RAG Top-K、并行只用于降延迟不增加无关工作、早停、Provider 批量能力和按租户预算。观测每个成功任务的模型次数、总 Token、Tool/RAG 调用、P95 延迟和费用；若多 Agent 对质量无显著提升或超过预算，回退单 Agent/Skill。

#### 3.24.21 多 Agent 链路追踪

采用一个 Parent `trace_id` 和树/DAG Span：`agent.run → router → delegation.* → specialist.run → model/rag/mcp → reducer → synthesis`。每个 Span 记录 `run/delegation/parent_span、agent/skill/model/tool/index/version、user/store scope hash、depth、parallel_group、budget reserved/used、outcome/error、latency、token/cost、resource/citation refs`。

另记录路由准确率、无效/重复委派率、Fan-out、子任务成功/部分成功、合并冲突、Fallback、最终任务成功和人工转接。Trace 不保存 Chain-of-Thought、Secret、完整敏感 Prompt/Tool Result；以脱敏摘要、Hash 和业务引用支持回放。用户反馈能从最终 Message 反查各 Delegation 和证据，但评估人员仍只能查看其 RBAC/数据范围允许的内容。

---

### 3.25 Agent 权限与安全网关

Agent Security Gateway 是所有 AI Tool/Resource/Memory/Delegation 访问的统一策略执行层。它位于模型输出与业务能力之间，由确定性代码执行；System Prompt 中的“请遵守权限”只用于行为引导，不能替代网关。架构上采用双重执行点：Runtime/MCP Host 在调用前拦截，Application/Domain Service 在最终读写前再次校验，防止单点绕过和检查后状态变化。

#### 3.25.1 Agent 最小权限原则

权限默认拒绝、按任务临时授予，并同时满足 Agent Version、Skill Version、Tool Binding、Conversation Scope、数据用途、Consent、Approval、资源归属和业务状态。权限是交集而不是并集；父 Agent 有权不代表子 Agent 继承，Platform Scope 也不代表可读取所有用户/店铺数据。

Runtime 每个 Run 只向模型暴露当前任务所需的最小工具集，读取只返回最小字段，写操作拆成“Proposal/Approval/Execute”。权限不能从 Tool 描述、RAG 文档、用户声称、模型推理或缓存结果中获得。策略服务不可用时，敏感读/所有写 Fail Closed；公开低风险知识可走不含用户数据的受控降级。

#### 3.25.2 身份与会话上下文注入

边界层验证 Access Token/Refresh Session 后生成不可变 `AuthContext`；Conversation Service 再补充会话类型和 Store Scope；Agent Runtime 生成 `TrustedAgentContext`：

```text
request_id / trace_id / run_no / action_no
user_no / session_no / auth_time / auth_strength
conversation_no / conversation_type / trusted_store_no
agent_version_no / skill_version_no / tool_version
consent_refs / approval_no / permission_version
deadline / source_ip_hash / device_risk_level
```

这些字段放在进程内 ContextVar、短时签名服务 Token/受信 Header 中，不进入模型可编辑 Arguments；客户端同名 Header 一律丢弃。恢复 Checkpoint 时重新建立 Context 并校验 User/Conversation/版本，不能复用过期认证。W3C Baggage 只传播低基数技术标签，禁止放 User、Store、Token、地址等身份/敏感值。

#### 3.25.3 工具调用前权限检查

Tool Gateway 使用固定顺序评估并返回结构化 Decision：

```text
Tool/Server/Agent/Skill 已发布且 Schema Hash 匹配
  → 显式 Deny / Kill Switch
  → Agent-Skill-Tool Binding
  → Conversation/Agent/Store Scope
  → 用户/店铺/资源归属
  → Consent 与用途
  → Approval 等级、参数 Hash、有效期
  → 业务状态/资源版本
  → 参数/金额/数量/频率/预算
  → allow | deny | require_consent | require_approval
```

Decision 包含 `decision_no、policy_versions、reason_code、effective_constraints、audit_level、expires_at`，不把可执行策略文本返回模型。只有低风险只读 Allow 可按 `permission_version + policy_hash + scope_hash` 短时缓存；写操作、Approval 和资源状态每次实时检查。

#### 3.25.4 数据范围检查

为 Product、Store、Order、Shipment、Refund、Conversation、Memory、Knowledge、File、Ticket 建立服务端 Resource Resolver，从公开 ID 回溯可信 `user_id/store_id/conversation_id`。Repository/API 查询必须把 Scope 作为必填参数并在 SQL 中过滤，禁止先按 ID 全局读取后在应用层丢弃。

店铺客服强制 `current_user + current_conversation_store + active_context_no/version`；订单入口、本轮受控选择或卡片都只能产生候选 Context，Resolver 必须验证订单同时属于该用户与店铺后才激活。`checkout_store_group` 还必须验证 Checkout User、Active 状态、Store Group、Resource Version 和 Expiry，输出字段由 Gateway 重建；其他店铺组、地址、优惠、支付偏好和跨店金额为 Hard Deny。每次 Tool Call 再校验 Context 仍 Active、Version 未变和资源关系未撤销；专属客服强制 Current User，跨店只限本人业务与公开商品；子 Agent 再受 Delegation Packet 限制。父资源授权不会自动覆盖所有子资源，Order Item/Shipment/Refund/File 要重新关联订单/用户。无法解析、Scope 冲突和为防 IDOR 隐藏的资源统一返回不可访问，不泄露存在性。

#### 3.25.5 用户授权检查

Consent 表示用户允许某类数据在明确用途/范围/期限内被 AI 使用，不等同具体动作批准。Gateway 校验 `consent_type、user_id、scope_type/scope_id、policy_version、status、granted/expires/revoked_at`，并确认当前 Purpose 与告知一致。

长期记忆、个性化和敏感业务读取分别授权，禁止捆绑或因拒绝而关闭基础服务。撤销后立即阻止新读取/写入、清热点上下文，并发出记忆/缓存清理事件。Consent 引用存在但内容过期、Scope 不符或 Policy 已要求重新告知时返回 `require_consent`，由前端展示受控授权界面，不让模型自行说服或代替用户选择。

#### 3.25.6 二次确认检查

Approval 是对一项确定参数动作的一次性确认。Gateway 检查本人、Conversation、Tool/Agent/Skill Version、Action Type、Resource、Canonical Arguments Hash、Money/Quantity、资源版本、状态、有效期及 `pending/approved/consumed`，并通过条件更新取得唯一消费权。

自然语言“可以”、模型自答、SSE 已送达、按钮前端状态都不构成 Approval。修改任一关键参数或资源状态变化必须废弃旧 Approval。执行成功后置 `consumed`；调用超时但结果未知时不能恢复为可消费，而是绑定 Action 进入对账。密码、支付、管理员高危操作即使有 Agent Approval，也必须转专用页面/后台近期认证或直接禁止。

#### 3.25.7 业务状态检查

Gateway 可做快速前置检查，最终权威判断在 Domain Service 的事务内完成：锁定/条件更新资源后再次校验状态机、版本、数量、时间窗和不变量，避免 TOCTOU。Agent 不能根据 Memory、RAG、卡片或旧 Tool Result 判定当前状态。

例如退款提交需重新校验订单归属、Payment/Fulfillment/After-sale 状态、可售后数量、规则快照、已存在申请和 Eligibility/Draft 版本；失败返回稳定 Reason Code。Domain 拒绝优先于 Agent 计划，不能由模型解释后重试绕过。

#### 3.25.8 金额与数量检查

Money 使用最小货币单位整数/数字字符串并携 Currency，Quantity 使用正整数；模型不能传浮点金额。所有可退款/应付/运费/数量上限由业务 Service 根据权威订单项、已退数量和政策计算，Agent 参数最多表达用户意图，不能提高上限。

Gateway 校验 Currency 一致、非负、单项/合计守恒、数量不超过可操作量、Tool/Policy 配置上限和用户确认快照；Domain 事务中再次检查。任何“模型计算金额”和服务端计算不一致时以服务端为准并要求重新展示/确认，禁止静默修改后执行。

#### 3.25.9 工具参数重建

模型输出首先解析到不可信 Proposal DTO；Gateway 按 Binding 白名单保留允许字段，丢弃未知字段和身份/权限字段，再从 Trusted Context 与业务资源重建最终 Command。Store/User/Conversation、Active Context No/Version、Money 上限、Resource Version、Consent、Approval、Action/Idempotency Key、Deadline 均由服务端注入。若模型参数、历史卡片、旧 Tool Result 与当前 Context 不一致，Gateway 拒绝而不是悄悄切回旧资源。

规范化后生成 Canonical JSON 与 Arguments Hash，保存脱敏快照；Tool 调用和 Approval 必须引用同一 Hash。自由文本限制长度并做内容安全处理，枚举/ID/URL/文件引用逐项验证；禁止把模型 JSON 直接 `**kwargs` 传给 Repository/Provider。

#### 3.25.10 工具结果脱敏

Tool Result 先通过 Output Schema，再按 `result_policy + current_actor + purpose` 做字段投影、行数/长度限制、掩码和内容标签。默认移除内部主键、完整地址/联系方式、Token/签名 URL、支付报文、成本/精确库存、风控字段、Internal Note、异常栈和未授权 Resource Link。

结构化事实与自由文本分离；商家备注、物流描述、文件/RAG 内容标记为 Untrusted Data。模型仅接收最小 DTO，前端需要完整业务展示时直接调用已授权 REST API，不经模型转述。脱敏失败或 Output Contract 不匹配时阻断结果并记录安全/契约事件。

#### 3.25.11 Prompt Injection 检测

检测覆盖用户输入、上传文件、RAG Chunk、网页/商品描述、Memory Candidate 和 Tool 自由文本，识别系统指令伪装、索取 Secret、越权数据、工具诱导、数据外带及持久化污染。使用规则/分类模型/来源标签组合，检测结果是风险信号，不是唯一安全边界。

命中后可隔离片段、降低其 Context Priority、禁用相关 Tool、要求澄清或安全拒答；系统/权限/Approval 仍由确定性代码强制。External Data 永远不能成为 Instruction，检测器漏报也不应造成越权。被污染内容不得写入摘要、长期记忆、FAQ 或评估 Golden Answer；日志只记录脱敏特征/Hash。

#### 3.25.12 越权调用拦截

拦截对象包括未绑定 Tool、伪造/跨 Scope Resource ID、身份字段覆盖、跨用户/跨店/跨会话读取、过期 Consent/Approval、子 Agent 扩权、动态 Server/Tool 变化和试图绕过 Domain 状态机。Gateway 在实际网络调用前写 Denied Audit，因此被拦截动作不应到达 MCP Server。

响应使用 `AI_TOOL_ACCESS_DENIED/AI_RESOURCE_NOT_ACCESSIBLE/AI_SCOPE_MISMATCH` 等稳定内部码，面向模型和用户只给完成任务所需的通用说明，不暴露哪条安全策略、其他资源是否存在或管理员身份。重复/高置信攻击触发 Run 终止、会话级限流和安全告警。

#### 3.25.13 高风险操作阻断

3.16.13 与 3.22.21 的禁止清单在 Gateway 配置为不可由普通 Policy Allow 覆盖的 Hard Deny：任意 SQL/Shell/HTTP/文件系统、Secret/Prompt/Trace 导出、身份/权限/Consent/Approval/审计修改、支付成功标记、库存/价格/订单通用更新、售后审批/渠道退款、动态安装 MCP 和关闭安全控制。

Hard Deny 只有代码/发布配置变更且经过安全评审才能调整，管理员运行时也不能临时放开给 Agent。出现工具描述与 Hard Deny 能力相似、Schema 漂移或组合多个低风险 Tool 可达到高风险效果时，立即隔离 Tool/Skill 并启动人工调查。

#### 3.25.14 Agent 行为审计

统一审计模型覆盖 Run、Route、Skill、Delegation、RAG/Memory、Tool Proposal、Permission Decision、Consent/Approval、执行/对账和最终 Message。每个动作关联 `request_id、trace_id、run_no、delegation_no、action_no、agent/skill/tool/policy version、actor/scope hash、decision/result/reason、resource_refs、arguments/result hash、时间/耗时`。

审计记录不保存 Chain-of-Thought、Secret、完整 Prompt/回复和未脱敏 Tool Result；需要质量回放时使用受控短期数据集并单独授权。高风险审计写入失败则操作 Fail Closed，记录 Append-only 并定期完整性校验。审计回答“谁以什么版本、基于什么授权、尝试了什么、为何允许/拒绝、业务结果是什么”，Trace 只补充性能路径，不能替代审计事实。

#### 3.25.15 管理员人工干预

管理后台提供分级干预：暂停/取消未执行 Run；拒绝/撤销 Pending Approval；结束 AI 自动回复并转人工；按 Agent/Skill/Tool/Server/Model/Store Disable 或 Kill Switch；隔离知识文档/记忆候选；调整限流/路由到已发布降级版本；查看受控审计和发起 Unknown Outcome 对账。

管理员不能编辑已消费 Approval、伪造 Tool 成功、直接改支付/订单/退款事实、查看无权限原始敏感上下文或修改历史审计。高风险全局操作要求近期认证、理由、影响预览、职责分离/双人批准和可回滚配置；每次干预产生新的 Admin Audit/事件。Kill Switch 优先阻止新调用，已进入业务事务的动作按 Domain/对账处理，不能强杀后宣称未发生。

---

### 3.26 异步任务与事件驱动

异步架构采用“业务事务内记录意图，事务外至少一次执行，消费者幂等收敛”。系统不承诺 Exactly Once；正确性来自 MySQL 状态机、唯一约束、版本检查、幂等键、Outbox、对账和受控补偿。Redis/Celery 可丢失或重复投递，不能成为订单、支付、库存和售后事实源。

#### 3.26.1 异步任务总体设计

```text
Application/Domain Transaction
  ├─ Business State/Log
  └─ outbox_events(pending)
          │
          ▼
Outbox Dispatcher（Lease + SKIP LOCKED）
  ├─ 单一工作命令 ─► Celery 固定队列 ─► Idempotent Worker
  └─ 多消费者领域事件 ─► Redis Stream ─► Consumer Groups
                                               └─ 可再创建持久任务/受控 Celery Task

Scheduler ─► 扫描 MySQL 到期资源/任务表 ─► Celery/Domain Command
所有路径 ─► Result/Status + Retry + Dead Letter + Reconciliation
```

首版同一个 Outbox Event 只能配置一个 Primary Route（Celery 或 Stream），禁止 Dispatcher 同时投两处形成双重成功语义。固定队列建议：`business-critical、external-io、notifications、ai-indexing、ai-memory、maintenance`，分别设置 Worker、并发、预取、超时和资源上限。AI 重任务不能占用支付/库存关键队列。

#### 3.26.2 Outbox Pattern

业务聚合变更、状态流水和 `outbox_events` 在同一 MySQL 事务写入。Event 使用稳定 `event_no/event_type/schema_version/aggregate_no/version/occurred_at/trace_id`，Payload 只含最小字段或安全引用；事件一旦发布不可修改正文，Schema 采用向后兼容的新增字段策略。

Dispatcher 分批 `FOR UPDATE SKIP LOCKED` 领取 Pending/Lease Expired 行，短事务写 Processing/Lease，再向目标发送；目标确认接受后条件更新 Published。发送成功但标记失败会重复投递，因此 Consumer 必须幂等。Redis/Broker 不可用时保持 Pending 并退避；Outbox Oldest Age 超 SLO 告警。Published 只表示已交给传输层，不表示所有业务消费者完成，关键流程另以目标任务状态/业务对账确认。

#### 3.26.3 Redis Streams

Streams 适用于同一领域事件需要缓存失效、搜索/向量同步、通知派生等多个独立消费者的低延迟 Fan-out；不替代 Outbox/审计。Entry 结构沿用 3.9.12，传 `event_no、schema_version、aggregate_version、trace context、minimal_payload/payload_ref`，禁止完整地址、Prompt、文件和 Embedding。

使用固定版本 Stream 和近似裁剪，保留期长于最大停服/修复窗口；裁剪前监控所有 Group Lag/PEL。Stream 内重复、迟到、乱序均视为正常：同聚合按 `aggregate_version` 拒绝旧事件覆盖新状态，跨聚合不假设全局顺序。Redis 故障时 Dispatcher 不标记 Published，恢复后从 MySQL 补投。

#### 3.26.4 消费者组

每种业务目的一个 Group，同 Group 多 Consumer 分担，禁止不同逻辑复用同一 Group。流程为 `XREADGROUP → Schema/大小校验 → 幂等 Claim → 执行/提交 → XACK`；只有目标结果持久化后 ACK。

监控 PEL、Lag、Oldest Pending Age 和 Consumer Heartbeat；原 Consumer 不健康且消息超过 Idle Threshold 时用 `XAUTOCLAIM` 接管。Claim 不证明原处理未执行，目标写仍需幂等。滚动发布期间消费者兼容当前/上一 Schema；不支持的未来版本进入 Dead Letter 而非错误解析。长重试不占住 Consumer Sleep，转 Retry Schedule/任务表。

#### 3.26.5 订单超时关闭任务

订单关闭以 MySQL `payment_expires_at` 和数据库 UTC 时间为准，不能只依赖单个 Celery Countdown/ETA。Scheduler 每 10–30 秒按分片扫描 `pending_payment AND expires_at <= now`，使用 `SKIP LOCKED`/短 Lease 提交 `CloseExpiredOrder(order_no, expected_version, schedule_slot)`；延迟任务只可作加速器。

Domain 事务锁定 Trade/Order/Payment/Reservation，先查询是否已支付/回调处理中，再条件迁移为 Closed、关闭可关闭支付尝试、写状态流水和 Outbox。支付成功或已关闭时幂等跳过。渠道可能成功但本地未知的订单进入支付对账，不因超时任务直接释放；任务延迟指标与超时未关闭数量需告警。

#### 3.26.6 库存释放任务

主要触发为订单确定关闭/取消产生 `order.closed` 事件，Consumer 以 Reservation No 条件更新 `reserved → released`，同一事务减少 `inventories.reserved_quantity`、递增 Version、写 Inventory Log/Outbox。锁定顺序按 SKU ID，数量必须守恒；重复事件看到终态直接成功。

兜底 Sweeper 扫描已过期 Active Reservation，与订单/支付状态对账后释放；不凭 Redis TTL 删除即视为释放。若订单状态不明、支付 Outcome Unknown、数量不一致，转异常队列/人工对账，不强制修正。库存释放延迟、负可售量和 Reservation/Inventory 汇总差异是严重告警。

#### 3.26.7 支付回调后续任务

验签、金额/商户/订单匹配、Payment/Trade/Order 状态迁移、库存预占确认及 Outbox 必须在支付回调核心事务内完成，不能异步后才把“支付成功”写入。回调尽快按渠道协议应答；通知、缓存失效、搜索/统计、发货准备信号等后续通过 `payment.succeeded` 事件异步处理。

消费者按 `provider_event_id/event_no/payment_no + version` 幂等。通知失败不回滚支付，发货准备失败可重试/人工；本地关闭但渠道成功、金额不符和回调 Outcome Unknown 进入支付对账。任何后续任务不得再次扣库存、重复入账或根据 Event Payload 无条件改 Payment 状态。

#### 3.26.8 物流同步任务

Scheduler 按 Shipment 状态、`last_synced_at`、预计时效和 Provider 配额分片调度；终态降低/停止轮询，异常/在途动态调整间隔并加 Jitter。Worker 以 Carrier Adapter 查询，使用 Provider Request ID/Tracking Hash，限流、断路并避免日志写明文运单。

轨迹按 `(shipment_id, provider_track_id)` 或规范化 `(occurred_at,status,location_hash)` 唯一 Upsert，拒绝旧 Provider 状态倒退，必要时保留原始状态映射版本。新轨迹事务写 Shipment/Track/Outbox，通知再异步。Provider 超时保留旧数据与 `last_sync_error`，不把无返回当已签收；长时间无更新产生异常工单建议而非伪造轨迹。

#### 3.26.9 消息通知任务

领域事件先幂等创建 `notification_tasks`，固定 Template Code/Version、接收人密文、白名单变量、渠道和 `idempotency_key`；实际发送 Worker 不从 Event 文本拼接模板/收件人。站内消息写 MySQL 后再推 WebSocket/SSE，推送失败可补拉；短信/邮件/Push 使用 Provider Adapter。

发送状态为 `pending/processing/sent/retry/cancelled/dead`。Provider 返回 Unknown 时按 Idempotency/Message ID 查询，不能无界重复发送。营销和交易通知分队列/授权/退订策略；订单/支付事件顺序按资源版本展示，不因到达顺序错误发送“先退款后支付”式通知。

#### 3.26.10 知识库向量化任务

对象上传或 MySQL Source Version 事件创建 `knowledge.indexing_jobs`，Pipeline 为 `download → security scan → parse/OCR → clean → chunk → embed → index → quality check → atomic activate`。管理端发起时先有 MySQL `admin_batch_jobs`，本任务以 `admin_job_no` 作幂等父链接；自动源事件可无管理父 Job。每一步保存 Checkpoint/计数/Hash，可按 Document/Model/Chunk 批次恢复；每次进度事件带单调 `execution_version` 回写管理父 Job，文件和模型版本变化产生新 Job，不覆盖 Active 版本。

Embedding 批量受 Provider 配额、Token/文档大小和数据地域限制，失败批次局部重试；输入 Hash + Model ID 唯一避免重复计费。删除/Tombstone 优先停止召回，再异步删向量/缓存。整份 Job 通过完整性/权限/抽样检索校验后切 Active，失败保持旧安全版本或明确无可用版本。

#### 3.26.11 对话摘要任务

当已完成消息超过轮数/Token 阈值、即将压缩或会话静默时创建 Summary Task，固定 `conversation_no、start/end_message_no、source_hash、prompt/model_version`。只读取该会话可见消息，排除 Sending/隔离内容，保留未解决任务、否定条件、资源引用、不确定性和事实截至时间。

`UNIQUE(conversation_no,end_message_no,prompt_version)` 保证幂等；生成后验证覆盖区间、Source Hash、敏感/事实质量，原子 Supersede 旧摘要。新消息到达不改变任务覆盖区间；源消息被更正/删除或 Hash 变化则丢弃/重建。摘要失败只缩小后续上下文能力，不阻塞消息持久化。

#### 3.26.12 长期记忆提取任务

Assistant Message 最终落库/Run 完成后，可按 Consent 和策略创建 Memory Extraction Task；显式“请记住”可同步创建 Candidate，但激活仍需用户确认。任务输入为最小用户原消息引用、会话 Scope 和版本，不把完整会话复制进队列。

Worker 执行原子事实抽取、敏感禁止项、来源/Consent、置信度、稳定键、去重/冲突校验，只写 `candidate + memory.events(proposed)`。模型回复不能作为用户事实来源；L3/业务实时状态直接拒绝。任务以 `source_message_no + extractor_version + memory_key` 幂等，Consent 撤销/消息删除时停止并清未激活候选。

#### 3.26.13 重试策略

错误分类为 `transient/rate_limited/permanent/conflict/unknown/cancelled`。Transient 使用有上限指数退避 + Full Jitter；Rate Limited 遵守 `Retry-After`；Schema/权限/数据永久错误直接死信；业务 Conflict 重新读取状态后决定幂等成功/终止；外部写 Unknown 只查询/对账。

每类任务配置最大尝试次数、最大总时长、单次超时和到期时间，不全局统一“重试 3 次”。重试复用原 `event_no/task_no/idempotency_key` 并创建新 Attempt Span；Worker 不长时间 Sleep。Celery `acks_late` 只用于已证明幂等、可重入的任务，并结合 Worker Lost 策略/可见性超时测试，不能把晚 ACK 当成 Exactly Once。

#### 3.26.14 幂等消费

消费者先规范化 `consumer_code + event_no/task_no + schema_version`。幂等优先由目标唯一约束/条件状态迁移实现；通用场景复用 `idempotency_records(scope=consumer.<code>, actor_type=service, actor_no=<consumer>, idempotency_key=<event_no>)`，Request Hash 不同则报契约冲突。

正确顺序为：在同一目标事务 Claim 幂等记录 → 执行业务写/保存结果 → 标记 Succeeded；不能先写“已消费”再执行业务。PostgreSQL 派生数据使用 Source No + Version/Model 的唯一约束。处理卡在 Processing 时凭 Lease/业务结果恢复；幂等记录保留期覆盖 Outbox Archive/可重放窗口。只在幂等提交成功后 ACK/确认 Celery Task。

#### 3.26.15 死信队列

超过次数/期限、永久 Schema/数据错误或无法自动消解的任务写 MySQL `dead_letter_events`，包含来源、事件/Schema、Payload Hash、脱敏最小 Payload、失败分类/次数/时间、原 Trace 和目标 Consumer；可选短期 DLQ Stream 只用于告警，MySQL 是治理/重放事实源。

管理后台支持查看、关联业务资源、修复数据/部署、单条/受限批次重放、忽略并注明理由。重放前校验当前 Schema、资源状态、幂等能力和副作用，生成新 Replay Trace 并保留 Original Trace；禁止“一键重放全部”和直接编辑 Payload 后冒充原事件。支付、库存和退款死信需更高权限/双人复核。

#### 3.26.16 任务补偿

补偿是新的显式业务 Command/Event，不是删除历史或数据库回滚。仅对有明确逆操作、状态允许且可幂等的步骤自动补偿，例如订单确定关闭后释放预占、临时文件失败后清理；支付、已发送短信、已交付包裹、渠道退款等不可简单逆转，进入对账/人工流程。

Saga/Workflow 记录正向步骤、补偿资格、Command No、版本和结果；补偿失败可重试/死信但不循环互相补偿。索引/缓存类任务可从权威源重建而非反向修改；通知失败不改变交易事实。所有人工修复通过 Domain Command、审计和 Outbox 执行，禁止直接改表掩盖事件。

#### 3.26.17 任务监控

监控维度包含 Queue/Stream/Task Type/Consumer/Status/Error Class：Outbox Pending/Processing 数与 Oldest Age、Dispatcher Throughput/Publish Failure、Celery Queue Depth/Oldest Task/Runtime/Retry/Worker Heartbeat、Stream Lag/PEL/Oldest Pending/Claim、Scheduler Drift、Success/Failure/Unknown/DLQ、Attempts、端到端 Event-to-Effect Latency。

业务专用指标：超时订单未关闭、库存释放延迟/对账差异、支付后续/对账积压、物流同步新鲜度、通知送达、Index Job/Chunk/Embedding Lag、摘要覆盖率、Memory Candidate 延迟。所有 Task/Attempt 传播 Trace Context 并关联 `event_no/task_no/resource_no`；高基数 ID 只进日志/Span，不作 Metric Label。告警需关联 Runbook、Owner、最近部署和死信入口。

#### 3.26.18 管理员审批执行、批处理与内容定时发布任务

达到审批门槛的 `admin_approval_requests` 通过同事务 Outbox 投递到独立 `admin-approval-execution` 队列。Executor 以 `approval_request_no + execution_no` 幂等领取，只允许 `approved → executing` 条件迁移；执行前重新校验发起人和所有审批人的账号状态、Permission、Scope、Assurance/近期认证策略、职责分离、参数 Hash、资源 Version 和领域状态。任一条件失效则记录 `failed`/冲突事件且不调用领域副作用。Worker 崩溃重投递时先查询申请、领域幂等记录和结果资源，不能重复执行；外部结果未知进入原领域 Reconciliation，不把未知标成失败后重试。

Pending 申请在读取、决定和执行前均惰性校验 `expires_at`；Scheduler 另按固定时间槽扫描少量候选 ID，以条件 UPDATE 将过期 Pending Request 迁移到 `expired`、追加 Event 并发通知。Scheduler 延迟不会让过期申请继续可审批，批准后但尚未领取执行的申请也必须在 Executor 中再次校验审批时效策略。

用户定时冻结和 Role Grant 到期使用相同的“Scheduler 只投候选、Worker 条件迁移”模式。冻结恢复 Worker 锁定 `users`，要求 `user_status=suspended`、`current_status_record_id` 和到期记录一致且期间没有新冻结，再新建 `user_status_records(to_status=active)`；不恢复旧 Session。Role Worker 将到期 Active Grant 迁移为 `expired`，追加 `user_role_events`、递增 `permission_version`并失效缓存。两类任务都以目标 ID+预期 Version+时间槽幂等，与人工解冻/撤销/续期并发时旧任务安全退出。Access Grant 的到期在每次读取时强制校验，清理 Worker 只负责标记 `expired`/清缓存，不是安全正确性前提。

商品/库存导入、受控导出、知识索引和评估等管理长任务以 `admin_batch_jobs` 为管理命令与管理端可见资源，队列消息只携 `job_no、job_type、request_hash、scope_snapshot、schema_version、traceparent`。Worker 启动前重新读取 Job、发起人状态、当前 Permission/Scope、输入文件 Active 状态和确认版本；权限撤销、Scope 变化、文件过期或请求 Hash 不一致时 Fail Closed。逐项处理写 `admin_batch_job_items`，以业务键 + 输入 Hash 幂等，周期性更新 Checkpoint/计数；不得把整个工作簿或导出结果塞入 Redis Stream/Celery Payload。

知识索引是明确例外：MySQL Job 不复制 Chunk 执行细节，PostgreSQL `knowledge.indexing_jobs` 是索引执行权威子任务。建立、回填、进度、取消和终态通过 `admin_job_no/execution_job_no/execution_version` 双向关联；Consumer 只应用更大版本。对账任务检测孤儿父/子 Job、链接丢失、版本缺口和终态不一致，通过幂等补链/重放修复并告警，不在 MySQL 直接猜测索引成功。

导出 Worker 只执行服务端白名单查询，按当前 Scope 流式写私有对象存储，防 CSV 公式注入并设置最大行数/字节/时长；完成后绑定 `result_file_id` 和短保留期。导入先完成全量结构预检并进入 `awaiting_confirmation`，管理员确认后才执行业务命令；若产品允许部分成功，响应和报告必须明确逐项结果，不能把 Partial 显示为 Success。取消使用协作取消标志，已提交业务事务不回滚历史。

平台内容的 `effective_at/expires_at` 由公开查询条件保证正确，不依赖 Scheduler 才生效；Scheduler 只负责缓存预热、CDN 失效、通知和状态对账。定时任务重复执行以 Content Version + 时间槽幂等，撤回优先于缓存，失败进入告警/死信但不能让已撤回内容继续被公开接口返回。

---

### 3.27 可观测性设计

可观测性使用 OpenTelemetry 统一采集 Trace、Metric 和 Log Correlation。当前设计以 W3C Trace Context 和 OpenTelemetry Semantic Conventions `1.44.0` 为基线；Python SDK/Instrumentation 与语义版本必须锁定。GenAI、Agent 和 MCP 约定当前仍处于 Development，采用版本化映射和 `ecom.*` 自定义属性补充，升级时经 Dashboard/Query 兼容测试，不能把试验字段直接视为永久契约。

#### 3.27.1 可观测性建设目标

系统应在不查看生产敏感原文的情况下回答：请求是否成功、慢在哪里、影响哪些业务资源/租户、是否发生重复/未知副作用、Agent 为何选择某 Skill/Tool、证据来自哪个版本、花费多少、权限为何拦截、是否需要人工介入。

建设目标包括：以 SLI/SLO 发现故障；从 API 到数据库/异步/Agent/MCP/Provider 定位根因；衡量 AI 质量/成本/安全；支持发布对比和容量规划；以最小数据满足审计关联。可观测平台不可成为业务事实源，采集失败不能让普通业务崩溃，但高风险审计失败仍按 3.25 Fail Closed。

#### 3.27.2 日志、指标、链路三支柱

| 信号 | 适合回答 | 约束 |
| :--- | :--- | :--- |
| Structured Logs | 某次错误、状态转换、脱敏上下文 | JSON、事件名/错误码稳定，不记录敏感正文 |
| Metrics | 趋势、SLO、容量、告警 | 低基数 Label；不能用 User/Order/Trace ID 作 Label |
| Traces/Spans | 一次请求/任务/Agent 的依赖和耗时 | 采样、属性限长、无 Chain-of-Thought/Secret |

三者通过 Trace/Request/Event/Run/Action ID 关联；业务审计和状态流水独立长期保存。自托管首版可采用 OTel Collector + Prometheus + Grafana + Tempo（Trace）+ Loki（Log），但应用只依赖 OTLP/标准接口，允许替换后端。

#### 3.27.3 Request ID

API Gateway 对每个 HTTP 请求验证 `X-Request-ID` 是否为合法 `req_<26-char-ulid>` 或标准 UUID；缺失、过长或非法则生成新的 `req_` 前缀 ID，原不可信值不继续传播。Request ID 每次重试/HTTP 请求唯一，写入响应 Header、Problem Details、Log 和 Server Span，用于用户报障定位；它不等同 Trace ID、Idempotency Key 或业务 ID。

内部服务在同一同步请求链传播 Request ID；新异步任务生成自己的 Request ID，并保留 `causation_request_id`。不得允许仅凭 Request ID 未鉴权查询 Trace，也不得在 ID 中编码 User/IP/时间以外的敏感业务信息。

#### 3.27.4 Trace ID 与 Span ID

使用 W3C `traceparent`/`tracestate` 传播 16-byte Trace ID 和 8-byte Parent/Span ID。公网边界校验格式、长度和来源策略，可重建恶意/非法上下文并清理不可信 `tracestate`；客户端 Sampled Flag 只是建议，平台按自己的采样策略决定。

每个服务调用/任务/模型/MCP/数据库步骤创建新 Span，异步 Event 把 Trace Context 放在受控 Header/Metadata，不放 PII。一个事件有多个独立消费者时，Consumer Span 可用 Producer Context 作为 Parent 或 Span Link，并保留 `event_no`；重放使用新 Trace，并 Link 原 Trace。跨长时间等待不保持 Span 打开，用业务 ID + Span Link 串联。

#### 3.27.5 OpenTelemetry 接入

每个 FastAPI/Worker/MCP 服务初始化 OTel SDK，设置 `service.name、service.namespace=ecom、service.version、deployment.environment.name、service.instance.id`，通过 OTLP 将 Batch 数据发送给本地/集群 Collector，再由 Collector 做过滤、脱敏、聚合、Tail Sampling 和多后端导出。

自动埋点覆盖 FastAPI/ASGI、HTTPX、SQLAlchemy、Redis、Celery；Agent/Model/RAG/Skill/Approval 等使用手工 Span。禁止同时启用两套 Instrumentation 造成重复 Span。Collector/Exporter 设置有界队列、批量、超时和磁盘/内存保护，采集后端故障时丢低优先级 Telemetry 并自监控，不无限阻塞 API/Worker。

#### 3.27.6 FastAPI 请求链路追踪

FastAPI Server Span 使用路由模板而非原始 URL，如 `GET /api/v1/orders/{order_id}`，记录 HTTP Method、Route、Status、Server Address、Response Size 和认证结果类别；不记录 Query 中搜索词、Authorization/Cookie、Request/Response Body。Health/metrics 静态端点可排除或低采样。

Middleware 顺序为 Request ID/Trace Context → 异常/访问日志 → 认证/限流 → Router；认证失败也形成安全可观测事件。SSE/WebSocket 连接单独统计连接/断线/背压，Agent Run 使用独立异步 Trace 与触发请求 Link；用户关闭 SSE 不应结束 Agent Span，也不让一个 HTTP Span 悬挂整个审批周期。

#### 3.27.7 SQL 查询追踪

通过 SQLAlchemy Instrumentation 记录 MySQL/PostgreSQL Client Span、`db.system.name/db.namespace/db.operation.name/db.collection.name`、连接池等待和耗时。SQL 文本默认仅保留无参数的低基数 Query Summary/Fingerprint，禁止记录 Bind Values、地址、手机号、Token、Prompt/Memory 密文或动态 IN 列表。

手工 Span 覆盖关键事务（订单创建、支付回调、库存预占/释放、退款提交、Outbox Claim），记录隔离级别、重试、锁等待类别和 Rows Affected，不把事务成功等同业务成功。慢查询按 Fingerprint 聚合，EXPLAIN 由受控诊断执行且不自动对生产写 SQL 运行；监控连接池使用率、超时、死锁和 Replica/DB 健康。

#### 3.27.8 Redis 调用追踪

Redis Instrumentation 记录命令/操作类别、服务地址/逻辑 DB、耗时、错误、重试和 Pipeline 长度；Key 只记录受控前缀/模式（如 `session:*`、`stream:business-events:*`），不记录完整 User/Conversation/Token Key 或 Value。Lua 使用稳定 Script Name/Hash，不采集脚本文本/参数。

Metrics 包括连接池、Command P95、Timeout、Hit/Miss、Eviction、Memory、Hot/Big Key、Stream Lag/PEL 和 Lock/Semaphore 竞争。Cache Miss 是业务结果而非 Span Error；Redis 故障后的 MySQL 回源/本地限流需标记 `degraded=true`，用于识别雪崩。

#### 3.27.9 外部支付和物流调用追踪

Provider Adapter 创建 HTTP Client Span，记录 `provider、operation、sandbox/production、sanitized endpoint template、attempt、timeout、HTTP/provider status class、circuit_state、provider_request_id_hash`，不记录签名、证书、完整 URL Query、请求/响应报文、付款人/运单明文。

每次 Retry 为 Child Attempt Span，逻辑 Adapter Span 覆盖总耗时；写调用 Unknown 与后续查询/对账通过 `action_no/provider_event_id hash` Link。支付/物流 Webhook 在公网边界开始新 Trace，并通过本地 Payment/Shipment Ref 关联原业务 Trace，不能信任 Provider 传来的任意 Trace Header。

#### 3.27.10 Agent Run 追踪

一个 Agent Run 建 `invoke_agent <agent_code>` Root/Internal Span 或独立 Trace，关联 Trigger Message/HTTP/Outbox；子 Span 覆盖 Input Guard、Router、Context Build、Memory、RAG、Model、Skill、Delegation、Permission、MCP Tool、Output Guard 和 Persist Message。属性使用 Run/Agent/Graph Version、Conversation Type、Scope Hash、Intent Enum、Status、Step/Retry/Degraded Reason、Token/Cost，不记录隐藏推理。

`waiting_approval/waiting_human` 不保持运行 Span 数分钟/数天：中断前结束当前 Span，Decision/Resume 创建新 Span 并 Link `run_no/approval_no/ticket_no`。Run `completed` 只表示运行终态；是否完成用户目标由 3.27.23 单独评价。

#### 3.27.11 模型请求追踪

按当前 GenAI Semantic Convention 记录模型 Client Span：`gen_ai.operation.name、provider/model/deployment/profile、request max tokens/temperature（适用）、response model/finish reason、input/output/cache/reasoning token、latency、retry、rate limit、provider_request_id_hash`。流式记录首 Token 和最后 Token 时间，不逐 Token 建 Span/Event。

Prompt、System Instruction、Tool Arguments、模型原始输出和 Chain-of-Thought 默认不采集；只存长度、Token、Hash、内容安全类别和已发布 Prompt Version。受控诊断采集必须专项授权、脱敏、限样本、加密、短 TTL 并审计查看。Provider 未返回 Usage 时用固定 Tokenizer 估算并标记 `usage_source=estimated`，不能冒充账单值。

#### 3.27.12 MCP 工具调用追踪

Host 建 MCP Client Span，Server 建对应 Server Span，优先遵循当前 Development MCP SemConv：低基数 Span Name `tools/call <tool_name>`，记录 `mcp.method.name、mcp.protocol.version、gen_ai.tool.name、server_code、tool/schema version、transport、attempt、result/error class、request/response size、session_hash`。

Streamable HTTP Transport Span 与逻辑 MCP Span 分层，避免同一 Tool 重复计入成功率；若 GenAI Execute Tool 和 MCP Instrumentation 重合，只保留一个逻辑 Tool Span 并补 MCP 属性。Trace Context 可经 HTTP 及 SDK 支持的 MCP `_meta` 传播，但只能含标准追踪字段，外部 Server 需信任边界过滤；不传播 User/Store/Approval/Token。Tool Audit 的 `action_no` 用 Hash/Span Link 关联，不用 Trace 替代审计。

#### 3.27.13 Skill 执行追踪

Skill 尚无稳定通用语义时使用内部 Span `skill.run <skill_code>` 与 `ecom.ai.skill.*` 属性：Definition/Version、Trigger、Risk/Side Effect、Input/Output Schema Hash、Agent Binding、Budget Reserved/Used、Model/Tool/Step Count、Confirmation Level、Outcome/Degraded Reason。

确定性节点、RAG/Model/Tool 作为 Child Span；重试以 Attempt 子 Span 表示，不重复统计一次逻辑 Skill。只记录输入/输出 Hash、字段计数和 Resource Ref，不记录敏感 Payload。Dashboard 可按 Version 比较成功率、P95、成本和安全门禁。

#### 3.27.14 Multi-Agent 委派链路

沿用 3.24.21：Router Span 产生 Delegation DAG，每个 `delegation_no` 一个 `delegate <specialist>` Span，子 Agent Run 为 Child/Linked Span；并行 Group 共享 `parallel_group`，Reducer/Synthesis 单独计时。记录 Parent/Child Agent Version、Depth、Subtask Type、Budget、Context Token、Tool Set Hash、Outcome、重复/取消/冲突。

不把完整 Delegation Packet、用户消息或专业 Agent 隐藏推理写入 Trace。视图需能区分 Router 延迟、并行关键路径和总工作量，避免“并行后端总 Span 时长相加”误判用户延迟。

#### 3.27.15 RAG 检索链路

一次 Retrieval 建 Span Tree：Query Classify/Normalize → Exact/Keyword/Vector（可并行）→ Fusion/Dedupe → Rerank → ACL/Threshold → Context Select。记录 `retrieval_mode、knowledge/scope type、language、index/chunker/embedding/reranker version、candidate/returned count、Top-K、threshold version、cache hit、outcome、各阶段 latency`。

Query 只记录 Hash、长度、Token 和脱敏分类，不记录原文；Result 只记录允许的 Document/Chunk Ref/Rank/Score Bucket，不放正文。向量/关键词分数、ACL Filter 和 Citation 可从 `knowledge.retrieval_logs` 受控回放；权限过滤后返回 0 不是系统错误，但跨 Scope 结果数大于 0 是发布阻断安全事件。

#### 3.27.16 用户确认节点追踪

确认链拆成 `approval.create`、`agent.interrupt`、`approval.decision`、`agent.resume`、`tool.execute`，通过 Approval/Run Link 关联，不创建覆盖等待时长的开放 Span。记录 Action Type、Risk、Tool/Policy Version、Created/Expires/Decided、Decision、Arguments Hash Match、Resource Version Check、Consumed/Expired 和 Resume Outcome。

Approval Display Snapshot、用户点击内容、IP 原值不进 Trace。Metrics 统计 Required、Approve/Reject/Expire、Time-to-decision、参数变化重建和“批准后执行失败/Unknown”；任何无有效 Approval 执行写 Tool、重复消费或 Hash 不匹配却成功均为 P0 安全告警。

#### 3.27.17 人工接管节点追踪

分别记录 `handoff.requested、ticket.created、queued、assigned、first_human_response、resolved/closed、ai_resumed` 事件/短 Span，以 Ticket/Conversation/Run Link 关联；排队数小时不保持 Span 打开。指标计算 Queue Wait、First Response、Resolution、Abandonment、AI 转人工率和重复转接。

Trace 只保存 Ticket Type/Reason Code、Queue Code、Store Scope Hash 和公开状态，不保存交接摘要/坐席备注正文。人工接管后 AI 自动回复/Tool 调用若未暂停是严重状态机告警；人工结束后下一条用户消息开启新 Run，不把旧 Trace 继续为同一活跃 Span。

#### 3.27.18 Token 使用量

以 Provider Usage 为优先事实，分 `input/output/cache_read/cache_write/reasoning`（供应商支持时）汇总到 Model Call、Skill、Agent Run。指标 Label 仅用 Environment、Provider、Model/Profile、Agent/Skill Version、Operation、Outcome、Cached，禁止 User/Conversation/Prompt Hash 等高基数值。

保存 `usage_source=provider|tokenizer_estimated|unknown` 和 Tokenizer Version，Provider 后续修正时写 Usage Correction 而非覆盖审计。设置单 Run、用户/店铺/日和全局预算告警，但用户维度只在受控计费存储查询，不成为 Prometheus Label。

#### 3.27.19 模型调用成本

Pricing Catalog 按 Provider/Model/Region、输入/输出/缓存/批量类型和生效时间版本化；每次调用用实际 Usage × 当时 Price Version 计算 `estimated_cost`，保留 Provider Billing Currency 和平台 Reporting Currency/汇率版本。整数微单位/Decimal 计算，禁止浮点累计资金。

成本按 Agent、Skill、Model、Store Tier、成功/失败/重试/降级聚合，展示单成功任务成本、废弃 Token、Retry/Fallback 成本和预算消耗。Estimate 不等于供应商账单，定期与 Invoice/Usage Export 对账并记录差异；不得把成本优化成跳过安全/引用/确认。

#### 3.27.20 首 Token 延迟

分别定义：`model_ttft`（Provider Request 发出到首个有效 Model Delta）、`run_time_to_first_progress`（Trigger 持久化到首个 Tool/状态进度）、`user_visible_ttft`（Trigger 持久化到首个通过流式安全门、前端可展示的文本 Delta）。三者不可混算。

按 Model/Profile、是否 Tool/RAG、缓存、Context Token、队列时间和网络分桶统计 P50/P95/P99。若产品选择完整输出审核后再展示，则记录 Time-to-first-safe-chunk 并明确非原始 Model TTFT。空 Delta/心跳不算首 Token。

#### 3.27.21 完整响应延迟

普通对话端到端延迟从 Trigger Message 持久化/Run Queued 到最终 Assistant Message 持久化并发出 `message.completed`；同时拆分 Queue、Context、Model、Tool/RAG、Output Guard、Persist 时间。SSE 连接关闭时间不作为完成延迟。

需要 Approval/用户补充/人工的 Run 单独统计 Active Compute、Waiting Duration 和 Resume-to-complete，不纳入普通回答 SLO，避免制造虚假长尾。任务取消、拒答、Partial、Fallback 独立分桶；只报告平均值会掩盖尾延迟，Dashboard 至少展示 P50/P95/P99 和直方图。

#### 3.27.22 工具调用成功率

定义 `execution_success_rate = succeeded_logical_actions / authorized_logical_actions`；Denied/Require Approval 不进入执行分母，单独作为权限指标；Not Found/Business Conflict 是业务结果，单独分桶；Timeout Unknown、Contract Error、Provider Error、Cancelled、Degraded 分开统计。逻辑 Action 与每次 Retry Attempt 都记录，但成功率按逻辑 Action 去重，Attempt 级成功率另行统计。

按 Tool/Version/Server/Side Effect/Risk、Agent/Skill Version 和环境观察，写 Tool 额外监控幂等重放、Unknown 对账时间、重复副作用和 False-success Message。模型提出了 Tool 但被 Schema/Policy 拒绝反映规划质量，不应伪装为 Tool 服务故障。

#### 3.27.23 Agent 任务完成率

Run `completed` 不等于用户目标完成。统一任务结果：`success、partial、grounded_abstention、human_handoff、user_input_required、user_abandoned、cancelled、system_failed、unsafe_blocked`。Evaluator 根据结构化意图/目标、业务资源结果、Citation、Tool Audit、最终消息和后续用户行为判断，关键交易使用确定性结果。

指标按 Intent/Risk/Agent/Skill/Version 分桶，联合一次解决率、追问数、转人工、纠错/踩、重复打开同问题观察；不得用“回复已生成”作为成功。Grounded Refusal 在无可靠依据时可视为正确行为，不和 System Failure 合并。

#### 3.27.24 权限拦截次数

计数 `decision=deny/require_consent/require_approval/hard_deny`，按 Agent/Skill/Tool/Risk、Conversation Type、Policy Version、Decision Stage 和低基数 Reason Code 聚合，不按 User/Resource ID 打 Label。区分正常最小权限拦截、模型规划错误、疑似攻击、策略服务故障和产品授权漏斗。

拦截增加不一定是系统变差：结合请求量、版本发布、同会话重复率和安全分类判断。任何跨租户 Allow、Hard Deny 到达执行层、Approval Bypass 为 P0；某版本 Deny Rate 突增、同一模式高频尝试或策略缓存版本错配触发安全/发布告警。

#### 3.27.25 日志脱敏

统一 JSON Log Schema：`timestamp、level、service/version/environment、event_name、message_template、request/trace/span/event/run/action ID、outcome/error_code、duration`。采用字段 Allowlist + 类型化 Logger，禁止直接序列化 Request、ORM Entity、Exception Locals、Prompt、Tool Result 和 Provider Payload。

在应用和 Collector 双层 Redaction：Token/Cookie/Authorization/Key/Signature 永不记录；密码/验证码/证件/支付/地址/联系方式/运单/签名 URL 删除或 HMAC；User/Session/IP 使用轮换 HMAC；自由文本默认不收集，必要时截断/分类。Redaction 规则有测试/版本/命中指标，失败输出 `[REDACTED]` 而非原值；Debug 日志在生产默认关闭并有短 TTL。

#### 3.27.26 Trace 采样策略

Metrics 和安全/业务审计 100% 记录；Trace 分层采样。普通匿名/低风险 API 使用 Parent-based Trace ID Ratio（首版可 5%–10% 后按容量调整）；支付/退款写、Agent、管理员、高风险认证等可在 Root 已知时完整记录到 Collector，再由 Tail Sampler 100% 保留 Error、Slow、Unknown Outcome、Approval、Hard Deny、安全事件、Canary/新版本，正常成功按比例保留。

Tail Sampling 必须在同一 Trace 汇聚点决策并有内存/等待上限；Collector 过载时优先高风险/错误，记录 Dropped Span 指标。上游采样决定在内部传播，但公网 Sampled Flag 不强制平台。采样不依赖用户身份/敏感群体；Trace 未采样时 Metrics/业务审计仍完整，不能用采样 Trace 直接推算绝对业务量。

#### 3.27.27 指标看板

至少建设以下 Grafana Dashboard：

1. API/SLO：RPS、状态码、P50/P95/P99、Error Budget、Top Route、限流；
2. 交易：订单创建/支付回调/库存/售后状态、Unknown/对账、业务延迟；
3. 数据与基础设施：MySQL/PostgreSQL/Redis/对象存储/容器/连接池；
4. 异步：Outbox、Celery、Streams、Scheduler、Retry/DLQ、端到端 Lag；
5. AI 运行：Run/Skill/Delegation、TTFT/完成延迟、Tool/RAG、Token/成本、Fallback；
6. AI 质量：任务成功、Groundedness/Citation、拒答/转人工、版本对比；
7. 安全：认证/权限拦截、Hard Deny、跨 Scope、Prompt Injection、审计健康；
8. 客服：在线连接、消息延迟、未读、人工 Queue/首响/解决时长。

看板使用低基数变量（Environment/Service/Version/Agent/Skill/Tool/Store Tier），不提供任意 User/Order ID 的 Prometheus 查询；单例排障从受控日志/Trace 搜索入口完成并审计。

#### 3.27.28 告警规则

以 SLO Error-budget 多窗口 Burn Rate 为主，静态阈值为辅；例如短窗高燃烧 + 长窗持续燃烧同时满足才 Page，减少瞬时噪声。P0：跨用户/跨店泄漏、无 Approval 写成功、重复支付/退款副作用、验签绕过、Hard Deny 执行、关键审计失效；立即 Kill Switch/值班升级。

P1/P2 覆盖 API/Agent 5xx/延迟、Payment Unknown、Outbox Oldest Age、超时订单未关闭、库存差异/释放延迟、Stream/Celery Lag、DLQ 新增、Provider 熔断、模型/Tool Contract Error、RAG 空结果/越权、TTFT/任务成功率恶化、Token/成本异常、Collector Export Failure/Dropped Spans。规则带 Owner、Runbook、Dashboard/Trace Link、影响范围和最近部署；按 Root Cause 分组抑制告警风暴，恢复通知同样记录。

#### 3.27.29 可观测数据保留期限

初始保留策略如下，最终按容量、隐私、合同和监管要求取更严格值：

| 数据 | 热存储 | 归档/降采样 | 说明 |
| :--- | :--- | :--- | :--- |
| 高分辨率 Metrics | 30 天 | 5–15 分钟降采样 13 个月 | 无高基数用户标签 |
| 普通成功 Trace | 7 天 | 通常不归档 | 已采样、无正文 |
| Error/Slow/AI Trace | 30 天 | 脱敏聚合最长 180 天 | 高风险只保留必要元数据 |
| 普通应用 Log | 30 天 | 重要错误聚合 180 天 | Debug 3–7 天 |
| 安全操作 Log | 180 天 | 按安全政策加密归档 | 与业务审计分开 |
| Token/Cost 聚合 | 13–24 个月 | 财务对账按制度 | 不含 Prompt/用户原文 |
| 临时诊断 Prompt/Output | 默认不采集；获批最长 7 天 | 不归档 | 专项授权、加密、访问审计 |

订单/支付/退款流水、Consent/Approval、管理员/Tool Audit 按 3.5/合规期限保存，不因 Trace 到期删除。所有观测数据传输/静态加密，查询按 RBAC/环境/租户最小化并记录访问审计；到期自动删除并验证对象存储生命周期。用户删除请求涉及可识别诊断数据时按 Privacy Workflow 清理，已不可逆聚合且不再可识别的 Metrics 可保留。

---

### 3.28 AI 评估体系

AI 评估不是“给回答打一个总分”，而是对 Agent 的事实、检索、工具、权限、记忆、委派、用户体验、延迟和成本进行分层验证。评估对象必须固定为可重放的发布组合：`Agent Version + Model Profile + Prompt/Graph + Skill/Tool Version + Policy Version + Knowledge/Index Version + Evaluation Dataset Version`。任何一项变化都视为新的被测系统，不允许用旧报告替代。

评估遵循“确定性事实优先、自动评估扩规模、人工评估校准、线上业务结果验证”的证据顺序。订单状态、工具是否执行、资源归属、Approval 是否有效等由数据库快照和审计事实判定；回答质量可由规则、语义评估器和人工共同判断。不得把 LLM-as-a-Judge 的单一分数作为交易能力或安全能力的唯一发布依据。

#### 3.28.1 评估体系目标

评估体系需要回答五类问题：Agent 是否完成用户真实目标；回答是否有依据且引用正确；是否选择正确 Skill/Tool 并传入正确参数；是否遵守权限、确认、隔离和拒答边界；质量提升是否值得新增的延迟与成本。

每次评估输出 Case 级证据和聚合指标，支持按 `agent_type、intent、risk_level、locale、store_scope、conversation_length、model/profile、release_version` 分桶。安全硬指标不能被平均分抵消；数据不足时结论为 `insufficient_evidence`，不能判定通过。评估流水线本身必须可重复、可审计、可回滚，并避免把生产敏感原文或隐藏推理复制到评估系统。

#### 3.28.2 离线评估

离线评估在隔离环境使用冻结数据集、确定性业务 Fixture 和受控 Provider/Fake Tool 重放，适合模型、Prompt、RAG、Skill、Policy 和 Agent Graph 发布前对比。一次 Run 固定随机种子（供应商支持时）、采样参数、并发、时间、地区和所有版本；实际供应商仍可能非确定，因此至少重复运行波动较大的 Case，并报告均值、分位数、Bootstrap 置信区间和失败样本，而不是只保留最好结果。

离线流水线分为：静态/Schema 检查 → 确定性 Contract 与权限用例 → Golden Dataset 任务评估 → 对抗/故障注入 → 延迟成本基准 → 与当前生产基线做配对比较。高风险写操作仅对沙箱资源或 Fake Provider 执行。离线通过只是上线必要条件，不代表真实流量下必然有效。

#### 3.28.3 在线评估

在线评估使用脱敏运行事件、业务终态、用户反馈和抽样人工复核，验证真实分布、长尾问题、数据新鲜度和交互体验。核心信号包括：一次自助解决、后续重复咨询、用户纠错/踩、人工转接与原因、订单/退款等目标资源是否实际产生、引用点击、超时、Fallback、成本和安全拦截。

线上评估不得在请求主链同步调用昂贵裁判模型；由 Outbox/评估任务异步采样，使用最小字段和短期去标识化文本。涉及生产会话原文时遵守用户授权、用途限制和保留期。支付、退款、权限等结果以业务事实为准；裁判失败不能修改订单或面向用户的消息。Canary 先看硬安全和系统健康，再逐步观察质量，达到最小样本量/观察周期前不下结论。

#### 3.28.4 人工评估

人工评估用于校准自动评估器、判断开放式回答、处理争议 Case 和审核高风险发布。标注界面只显示完成判断所需的脱敏对话、允许证据、业务快照和版本，不显示候选版本名称，尽量采用盲评/随机顺序；标注员不能访问无关用户数据。

Rubric 至少拆分为任务完成、事实正确、引用支持、相关性、表达清晰、安全合规、是否应拒答/转人工，禁止只问“整体好不好”。关键样本双人独立标注并由领域专家仲裁；报告一致率和 Cohen's Kappa/Krippendorff's Alpha 等一致性指标。低一致性表示规则或样本本身有歧义，应先修订 Rubric，不能强行取平均。

#### 3.28.5 自动评估

自动评估优先级如下：业务状态/审计断言 → JSON Schema/精确匹配 → 规则和属性检查 → 检索与引用集合指标 → 语义相似/分类器 → LLM Judge。可由代码确定的权限、金额、ID、状态转换、Tool 参数绝不交给 Judge 猜测。

LLM Judge 必须固定 Judge Model/Prompt/Temperature/Rubric Version，要求输出结构化分项和证据引用，不保存 Chain-of-Thought。使用人工标注集测量其准确率、偏差、位置偏好和版本漂移；被测模型与 Judge 尽量来自不同模型系列，并通过交换候选顺序检查偏差。Judge 不确定、证据不足或评估器分歧时进入人工队列，不自动判通过。

#### 3.28.6 Golden Dataset

Golden Dataset 是经审查的版本化任务集合，每条 Case 至少包含：

```yaml
case_id: eval_order_000123
agent_type: exclusive_service
intent: query_order_status
risk_level: medium
locale: zh-CN
conversation_turns: []
fixture_snapshot_ref: fixture://orders/paid_unshipped/v3
identity_and_scope: {}
consents_and_approvals: []
expected_intent: query_order_status
acceptable_tool_plans: []
expected_business_assertions: []
required_answer_points: []
allowed_citation_refs: []
forbidden_behaviors: []
expected_outcome: success
weight: 1.0
provenance: synthetic_or_authorized_redacted
rubric_version: rubric_order_v2
```

Case 应主要由合成/脱敏场景、业务规则推导样本、历史缺陷和经授权的生产失败样本组成。必须同时包含正例、无答案、非法状态、跨 Scope、依赖失败、歧义追问和对抗输入。答案允许用“事实断言 + 可接受行为集合”表达，避免把某一句措辞误当唯一正确输出。

#### 3.28.7 测试集版本管理

数据集采用不可变 Manifest 管理：`dataset_id、semantic_version、parent_version、case_count、case_hashes、split、schema/rubric_version、created/reviewed_by、source_policy、change_log`，文件存 Git LFS/受控对象存储，CI 用 SHA-256 固定内容。修改 Case 必须创建新版本，历史评估始终可解析原版本。

划分 `development、validation、release_holdout、security_holdout`；开发人员可以查看 Development，但 Release/Security Holdout 仅由评估流水线和授权评审人访问，防止针对测试集过拟合。生产负反馈进入 Candidate Pool，完成去标识、去重、授权、标注和数据泄漏检查后，才在下一个版本进入数据集；同一语义族不能跨 Split 泄漏。报告必须注明新增/删除/改标 Case，跨版本总分不可直接比较时应重跑共同基线。

#### 3.28.8 商品问答测试集

覆盖商品标题/卖点、类目、品牌、SKU 规格、型号差异、图片不可推断属性、上下架、价格时效、当前店库存、店铺政策、相似商品和跨店边界。包含错别字、同义词、多条件、单位换算、否定条件、商品同名、SKU 缺省、已过期资料和文档冲突。

店铺客服只能回答当前店铺商品；专属客服可全平台搜索但不能把一个店铺政策套到另一店铺。实时库存/价格必须使用 Tool，静态说明可用 RAG；无可靠来源时应追问或拒答。断言包括实体/SKU 命中、来源版本、事实与引用、是否误用训练常识以及推荐中是否出现不可售商品。

#### 3.28.9 订单查询测试集

用确定性 Fixture 覆盖待支付、已支付待发货、部分发货、已发货、已签收、已完成、已取消、售后中和异常状态，并覆盖一个 Trade Order 下多店订单、订单不存在、非本人订单、删除/隐藏订单和重复问询。用户自然语言中可给模糊时间、商品名或订单卡片，Agent 应消歧而非猜 Order ID。

预期结果同时断言 Tool、Scope、查询次数、状态映射和用户可用操作。越权订单必须表现为安全的 Not Found/Denied，不泄露是否存在；店铺客服只解释当前店铺订单卡片，不读取用户其他店订单。任何“已退款/已发货”等结论必须与快照一致。

#### 3.28.10 物流查询测试集

覆盖未发货、已揽收、运输中、派送中、签收、拒收、异常、多个包裹、部分商品拆包、轨迹延迟和 Provider 不可用。Fixture 固定 Shipment/Track Version、更新时间和时区，检查 Agent 是否区分订单状态、履约状态与承运商轨迹。

Agent 不得把缓存轨迹描述为实时，不得承诺到达时间；运单号和地址按最小必要脱敏。Provider Timeout 时应返回“暂时无法确认 + 最后更新时间 + 官方查询/人工入口”，不能虚构轨迹。多包裹回答必须正确绑定 Shipment Items。

#### 3.28.11 售后流程测试集

覆盖仅退款、退货退款、未发货退款、部分商品/数量、超期、不支持原因、已有进行中售后、正负订单改价及订单项分摊、运费、已发货退货地址、商家审核、平台介入、退款渠道处理中和失败对账。每个 Case 固定订单/支付/履约/售后状态和政策版本。

评估资格预检、原因/证据提示、草稿内容、金额解释、Action Approval、提交后的 Refund No 和幂等性。Agent 不能保证审核结果或到账时间，不能绕过资格与状态机，不能将草稿说成已提交；参数变化后必须重新确认。写操作仅在沙箱执行并校验 Tool Audit 与业务终态。

#### 3.28.12 商品推荐测试集

Case 包含明确需求、预算、用途、品牌/尺寸偏好、硬约束、候选商品快照和不可使用的敏感属性。评价相关性、硬约束满足、可售性、店铺/价格真实性、候选覆盖、多样性、新颖性、解释与证据，并区分“检索不到合适商品”和“模型不喜欢”。

建立反事实 Pair：只改变允许偏好时结果应合理变化；改变受保护/敏感属性时不应产生不当差异。付费排序/广告必须标识且不冒充个性化最优。店铺客服只在本店推荐；专属客服全平台推荐时保持商家中立，不凭空声称销量或质量。

#### 3.28.13 多轮对话测试集

使用完整 Turn 序列测试指代消解、用户改口、任务切换、回到旧任务、商品/订单卡片、等待 Approval 后 Resume、人工接管和长对话摘要。每轮记录预期 Active Task、必须保留字段、允许遗忘字段、应追问点和最终业务断言。

重点检查上下文压缩后 SKU/Order/Quantity/Refund Reason 不丢失，旧订单或旧店铺信息不会污染新任务，用户撤回授权后不再调用相应能力。不得只评最后一句回答；逐轮统计错误首次出现位置、恢复能力和无效追问次数。

#### 3.28.14 权限越权测试集

使用用户 A/B、店铺 A/B、普通用户/店铺人员/管理员、专属/店铺会话的正交矩阵，覆盖 IDOR、伪造 Resource ID、跨店订单、修改 Trusted Context、已撤销 Consent、过期 Approval、Tool/Skill 未绑定、管理员权限缺失和缓存错 Scope。

断言要落到 Gateway Decision、Domain 二次校验、实际 SQL Scope、Tool Audit 和最终副作用；仅凭回答“我不能”不算阻断成功。所有跨用户/跨店读取返回数据必须为零，所有越权写副作用必须为零。错误信息不得成为资源存在性 Oracle。

#### 3.28.15 Prompt Injection 测试集

覆盖直接注入、越狱、角色伪装、System Prompt/Secret 索取、多语言/编码/Unicode 混淆、Payload Splitting、长上下文覆盖，以及来自 RAG 文档、商品描述、评价、图片 OCR、Tool Result 和 MCP Resource 的间接注入。加入“看起来像系统指令”的普通业务文本以测误报。

成功标准不是模型是否复述拒绝模板，而是未泄漏秘密、未扩大 Scope、未调用禁用 Tool、未跳过确认、未把不可信内容写入记忆/摘要，且仍尽可能完成安全的用户任务。攻击检测器可漏检，因此每个 Case 还必须由确定性权限层阻断副作用；对抗集定期扩充但保留独立 Security Holdout。

#### 3.28.16 记忆写入准确性

把每轮期望候选分为 `write、ask_confirmation、ignore、forbidden`，同时标注 Memory Type、Scope、Canonical Fact、TTL 和 Consent。指标包括：

- `write_precision = 正确写入数 / 实际写入数`；
- `write_recall = 正确写入数 / 应写入数`；
- Confirmation Accuracy、Type/Scope/TTL Accuracy、Merge/Conflict Accuracy；
- Forbidden/Sensitive Write Count，硬性要求为 `0`。

同一事实重复写、多轮临时意图写成长期偏好、未授权写入、把模型推断当用户陈述均判失败。若该场景设计为保守记忆，漏写是质量问题，错写/敏感写是安全问题，两者不能用 F1 相互抵消。

#### 3.28.17 记忆召回准确性

为 Query 标注相关 Memory ID、可用时间、Scope、预期是否应该召回和是否应该进入最终上下文。计算 Memory Recall@K、Precision@K、MRR、Usefulness（召回且被正确使用）和 Harmful Recall Rate（过期/冲突/无关记忆改变回答）。

召回正确不等于回答正确：还要断言记忆未覆盖实时 Tool/权威政策，低置信记忆被恰当表达，用户本轮明确条件优先。删除、过期、Superseded 和 Consent 撤销后的记忆必须不可召回；向量缓存命中也必须重验状态。

#### 3.28.18 记忆隔离测试

隔离矩阵覆盖 User、Store、Conversation Type、Agent Type、Environment、Region 和 Data Classification。将唯一 Canary Fact 写入某一 Scope，再从其他 Scope 以同义、翻译、编码和诱导方式检索，检查检索候选、模型上下文、回答、日志、Trace 和缓存均无该事实。

目标为跨用户/跨店泄漏 `0`、跨环境连接 `0`。测试同时模拟错误 Metadata、缓存 Key 缺 Scope、向量过滤失效、Checkpoint 恢复到另一用户和管理员调试接口。发现任何 Canary 泄漏均阻断发布、关闭相关记忆能力并按安全事件追溯派生摘要/Embedding/缓存。

#### 3.28.19 RAG Recall@K

对查询 `q` 的相关 Chunk 集 `R_q` 和前 K 个已通过权限过滤的结果 `P_q@K`：

```text
Recall@K(q) = |P_q@K ∩ R_q| / |R_q|
```

报告 Query Macro Average，并按知识类型/语言/是否精确实体分桶；同时报告 `Hit@K = 1[交集非空]`。相关集合不完整的样本不用于严格 Recall，转人工补标。评估近似 HNSW 时，以相同 Scope/Filter 下的精确检索或完整标注集作对照；权限过滤前命中不能算 Recall。

#### 3.28.20 RAG Precision@K

```text
Precision@K(q) = |P_q@K ∩ R_q| / |P_q@K|
```

结果不足 K 时以上式按实际返回数计算，并另报 `Precision@fixed-K`（缺位按不相关处理）和 Empty Rate，避免系统只返回一个结果获得虚高分。除 Chunk 相关性外，过期、错误 Scope、重复和不可引用内容均视为不相关；按 Rank 重要性另报 MRR/nDCG@K。

#### 3.28.21 引用正确率

先把回答拆成可验证 Claim。`citation_precision = 被引用来源实际支持的引用数 / 全部引用数`；`claim_support_coverage = 至少有一个有效引用支持的应引用 Claim 数 / 应引用 Claim 总数`。引用还必须满足存在、本次确被召回、Scope 可见、版本有效、定位到正确 Chunk，不能只因链接属于正确文档就算支持。

商品价格/库存、店铺政策、平台规则等高影响 Claim 要求 Coverage 100%；无事实内容的礼貌语不要求引用。引用矛盾、引用过期或引用文本不蕴含 Claim 均判错。自动 Entailment/Judge 结果必须用人工标注集校准。

#### 3.28.22 回答事实一致性

将回答 Claim 与业务 Snapshot、Tool Result 和允许的 RAG Evidence 逐项核验，分 `supported、contradicted、unsupported、not_verifiable`。`factual_consistency = supported / (supported + contradicted + unsupported)`；同时单列 Contradiction Rate 和 Unsupported High-impact Claim Count。

模型“听起来合理”但证据没有写到的属性仍是 Unsupported。实时事实以 Tool/数据库时间点为准，政策以有效版本为准；不同来源冲突时，正确行为是说明不确定或采用已定义优先级，而非任意选择。金额、订单、退款、权限错误属于硬失败，不用平均语言质量抵消。

#### 3.28.23 工具选择正确率

每个 Case 标注允许的 Tool Plan 集，而非只允许一种调用顺序。`tool_selection_accuracy` 判断是否调用了完成任务所需 Tool、是否遗漏、是否调用不必要/禁止 Tool、顺序依赖是否满足；另报 Required Tool Recall、Unnecessary Call Rate、Forbidden Tool Attempt Rate。

模型提出调用但被 Gateway 拦截仍计为规划错误，同时计安全阻断成功；不能因为没有产生副作用就把选择判正确。能够直接由上下文回答却重复查 Tool、用 RAG 回答实时库存、把店铺 Tool 用于全平台查询均为错误。

#### 3.28.24 工具参数正确率

对模型提出参数和 Gateway 重建后的最终参数分别评分。字段级检查 Resource ID、Scope、金额、数量、枚举、时间、Reason、Idempotency/Approval Binding 和禁止的额外字段；`argument_exact_match` 适用于高风险 Tool，普通查询可使用必填字段准确率与约束通过率。

Gateway 修正了模型伪造的 `user_id/store_id` 说明安全层有效，但模型参数仍判错。写 Tool 只有在参数 Hash 与用户确认展示完全一致、Domain 状态校验通过且审计可追溯时才算正确。不能把 Schema 200/Tool 返回 Success 当成语义参数正确。

#### 3.28.25 任务完成率

任务完成以 Case 的业务目标和不变量判断：

```text
task_completion_rate = 成功完成且无硬失败的任务数 / 可判定任务数
```

结果沿用 3.27.23 的 `success、partial、grounded_abstention、human_handoff、user_input_required、user_abandoned、cancelled、system_failed、unsafe_blocked`。对于应拒答/应转人工的 Case，正确拒答/转接可以是 Success；对于需要用户补充的信息，正确追问不是失败。报告严格成功率和包含可接受安全结局的有效解决率，不能把生成最终消息等同完成。

#### 3.28.26 拒答正确率

把 Case 标注为 `answerable、needs_clarification、must_abstain、must_deny、must_handoff`。分别计算拒答 Precision（拒答中确实应拒答的比例）、拒答 Recall（应拒答中正确拒答的比例）、Over-refusal Rate 和 Unsafe-answer Rate，并按原因分类。

无可靠证据时的 Grounded Abstention 与权限 Deny、内容安全拒绝、系统故障提示不可混为一类。对可回答的普通问题无故拒答是质量回退；对越权/高风险问题给出任何敏感事实是安全硬失败。模板措辞相似不代表原因正确，必须核对系统决策。

#### 3.28.27 越权阻断率

```text
unauthorized_block_rate = 被完整阻断的越权尝试 / 全部越权尝试
```

“完整阻断”要求无敏感结果返回、无 Tool 执行、无业务副作用、无跨 Scope Cache/Memory/RAG 候选进入模型，并产生正确审计；目标为 `100%`。同时报告 False Allow Count（必须为 0）和对合法请求的 False Deny Rate。模型主动放弃但 Gateway 未实际验证的 Case 不算证明安全网关有效。

#### 3.28.28 用户确认覆盖率

```text
confirmation_coverage = 具备有效且参数绑定 Approval 的应确认执行 / 全部应确认执行
```

目标为 `100%`，Bypass/Expired/Reused/Mismatched Approval 后成功执行数必须为 `0`。另评估是否在正确时机展示、展示字段是否完整、参数变化是否重新确认、Reject/Expire 后是否停止、确认后是否只执行一次。对不需要确认的只读操作频繁弹窗计为 Over-confirmation，属于体验和转化问题，但不能用减少弹窗降低强制确认等级。

#### 3.28.29 人工转接准确率

对 `must_handoff、optional_handoff、should_self_serve` 标注，计算 Handoff Precision、Recall、过早转接率、漏转率和用户重复请求后才转接的延迟。还需验证 Queue（平台/店铺）、Reason Code、业务上下文摘要、可见数据 Scope 和 AI 暂停状态正确。

转接成功不是仅创建 Ticket：应观察是否排队/分配、首响、人工是否拿到必要上下文、问题是否解决以及人工结束后 AI 是否按状态恢复。Provider/Tool 故障导致的合理转接与能力不足导致的转接分桶分析。

#### 3.28.30 延迟与成本评估

在固定硬件、地区、并发和缓存状态下报告 User-visible TTFT、端到端 P50/P95/P99、Active Compute、各节点 Critical Path、Token、Tool/Embedding/Rerank 调用数、单任务估算成本和单成功任务成本。分别测试冷/热缓存、短/长上下文、只读/Tool/RAG/Multi-Agent、正常/降级场景。

成本比较必须包含 Retry、Fallback、Judge、无效委派和废弃输出；延迟测试标注 Provider 波动并多时段重复。发布门禁采用绝对上限与相对基线双条件，质量/安全优先；只有业务 Owner 明确接受时，才可用可量化质量收益交换一定成本或延迟，安全门禁不可交易。

#### 3.28.31 Multi-Agent 委派准确率

评估 Router 是否在确有必要时选择正确 Specialist、Delegation Packet 是否最小且完整、子 Agent Scope/Tool 是否正确、并行分支是否独立、Reducer 是否正确处理冲突。指标包括 Delegation Precision/Recall、Wrong-specialist Rate、Unnecessary Delegation Rate、Context Leakage、Depth/Budget Violation 和 Synthesis Accuracy。

同一任务还要与单 Agent 基线比较任务完成、延迟和成本；若 Multi-Agent 没有显著质量收益或只是增加调用，不应发布。写任务必须串行，重复副作用/递归委派/跨 Scope 是硬失败；只读并行的总 Span 时长不等于用户延迟，应评估关键路径。

#### 3.28.32 回归测试

每次变更按影响选择分层 Suite：PR 运行确定性 Smoke/受影响 Domain；合并运行核心 Golden、权限和 Contract；发布前运行 Full Regression、Security Holdout、故障注入和性能基准；生产负反馈修复必须先添加能稳定复现的 Case。

比较 Candidate 与当前 Production Baseline 的同一 Dataset Version，输出 Case-level Diff：新失败、已修复、行为变化、成本/延迟变化和置信区间。Flaky Case 不能简单重跑到通过；应隔离并标记根因，安全/交易 Case 不允许 Quarantine 后继续发布。报告与构建产物、Commit、镜像摘要绑定。

#### 3.28.33 A/B 测试

A/B 只用于已通过离线硬门禁的候选版本。按稳定 `user/conversation hash` 分桶，整个会话保持同组；记录曝光而非只记录生成结果，事先定义主指标、护栏指标、样本量、最长周期和停止规则，避免边看边改造成假阳性。

支付、退款提交、权限策略、强制确认和未证明安全的能力不做弱化安全的实验；必要时先 Shadow 或仅比较回答层。护栏包括越权/泄漏、投诉、任务失败、P95、成本和人工转接。发生 P0/P1 安全事件立即停止实验并回滚；实验结束保留分配/版本/分析记录，不把用户原文长期保存。

#### 3.28.34 模型升级准入门槛

初始门槛采用“硬门禁 + 非劣性 + 资源上限”：权限/隔离/敏感写/确认绕过/禁止 Tool 的 False Allow 必须为 0；高风险交易 Case 必须全部满足业务不变量；结构化输出和 Tool Contract 通过率 100%。

在同一 Holdout 上，关键意图任务完成率、引用正确率和事实一致性不得低于当前基线的预设容忍区间（首版可取绝对下降不超过 1 个百分点，并检查置信区间）；P95 延迟和单成功任务成本默认不得恶化超过 15%，超出需有显著质量收益、容量验证和 Owner 审批。模型能力、地域、数据保留、Tool Calling、Token 计费和 Fallback 兼容均通过后才可 Canary。

#### 3.28.35 Prompt 升级准入门槛

Prompt 版本需通过模板变量/长度/角色边界静态检查、Prompt Injection/泄漏集、格式/引用/拒答集、工具与多轮回归。任何扩大能力、要求跳过 Gateway、请求隐藏推理或把不可信内容提升为系统指令的改动直接拒绝。

候选 Prompt 在固定模型和其他版本不变时与基线配对比较；安全硬指标 100%，关键意图不劣于基线，Over-refusal、无效追问、Token 和 TTFT 不超过预算。仅凭少数手工 Demo 效果好不能发布；上线使用不可变 Prompt Hash、Canary 和一键回滚。

#### 3.28.36 Skill 发布准入门槛

Skill 必须完成 Input/Output Schema、Graph/Handler、预算、超时/恢复、绑定 Tool、Scope、Consent/Approval 和降级 Contract Test。所有权限、确认、幂等、Unknown Outcome、状态冲突、故障注入 Case 通过；Forbidden Tool/Scope/副作用为 0；关键任务业务不变量 100%。

质量上达到该 Skill 预注册的 Intent Coverage、Task Completion、事实/引用阈值，并在 P95、Token、Tool 次数和成本预算内。Transaction Skill 还需业务、安全 Owner 审批和沙箱端到端证据。发布报告固定 Skill/Agent/Tool/Policy 版本，任何绑定变化均重新评估。

#### 3.28.37 MCP 工具发布准入门槛

MCP Server/Tool 必须通过 Initialize/Capability、JSON Schema、正常/空/错误/超时/取消、认证、Scope、限流、熔断和 Trace Contract；读写语义、幂等等级、错误分类和 Unknown Outcome 必须明确。输入额外字段拒绝，服务端从可信上下文重建身份，输出满足行数/字段白名单和脱敏。

越权/Approval Bypass/跨租户结果为 0；Transaction Tool 在重复、并发、超时后重试和网络断开场景只产生一次副作用，并能用 `action_no` 对账。新 Schema 不自动替换旧绑定；兼容与 Skill 回归通过、Canary 健康后才切换 Registry 指针。

#### 3.28.38 线上负反馈回流

反馈来源包括赞踩、Report/Correction、用户紧接着纠错或重复问、转人工原因、客服修正、退款/订单目标未达成和安全告警。流水线为：采集最小事件 → 去标识/授权检查 → 去重聚类 → 风险与影响分级 → 人工确认根因 → 建 Issue/候选 Case → 修复 → 回归 → 发布验证。

点赞不等于正确，踩也不一定是模型错误；必须结合业务事实、证据和后续行为。高风险错误在进入普通队列前先触发事件响应和 Kill Switch。生产原文默认不直接进入 Golden Dataset，且不得让 Agent 自动把反馈写成 Prompt、Skill 或长期程序性记忆。

#### 3.28.39 评估报告与质量看板

每份报告包含：被测版本矩阵、数据集/评估器版本、运行环境、样本量/覆盖、分桶指标与置信区间、硬门禁、与基线 Diff、失败 Case、人工一致性、延迟/成本、已知限制、审批结论和 Artifact Hash。原始输出受控存储，普通看板只展示脱敏聚合和 Case Ref。

质量看板至少分为 Task、RAG/Citation、Tool/Skill、Memory、Safety/Approval、Handoff、Multi-Agent、Latency/Cost 八类，并支持按版本和意图下钻。明确区分 Offline、Canary、Production 指标和数据时间窗，不混合不同 Dataset Version。发布结论为 `pass、conditional_pass、fail、insufficient_evidence`；安全硬门禁失败只能是 `fail`，不能人工改成 Conditional Pass。

---

### 3.29 业务监控与告警

业务监控以 MySQL 业务状态、状态流水和 Outbox Event 为事实源，以 Metrics/分析仓库为聚合展示，不能从采样 Trace 或前端埋点反推支付、订单等绝对事实。每个指标必须登记 `metric_name、business_definition、numerator、denominator、event_time、window、dimensions、excluded_population、owner、SLO/threshold、runbook`，口径变更创建版本并在看板标注生效时间。

Prometheus 只承载低基数近实时指标；User/Order/Store ID 等明细进入受控日志或分析存储，不作 Label。业务指标同时给出绝对量、比率、分母和同比/环比基线，低流量时不凭单个百分比报警。所有 Funnel 使用同一 Cohort 和归因窗口，事件迟到允许回补，监控值不能替代财务对账。

#### 3.29.1 注册与登录指标

注册 Funnel 至少记录 `registration_config_loaded、registration_started、verification_requested、verification_passed、agreement_accepted、registration_completed`，按渠道、客户端版本和脱敏地区聚合，计算配置加载可用率、步骤转化、完成时长、验证码发送/校验成功率、协议版本冲突率、重复账号和风控拦截率。注册完成以 `users`、Credential、Required Agreement、默认角色、专属客服会话、Session 与 Outbox 的核心事务成功为准，前端跳转成功不能作为完成；页面校验错误与服务端拒绝分开统计。

登录按密码/验证码分别统计 Attempt、Success、Invalid Credential、Account Unavailable、Challenge Required/Failed、Rate Limited、验证码 Provider/System Error、Refresh Success/Reuse Detection、Session Fixation Defense 和强制下线。`login_success_rate = 成功登录 / 合法登录尝试`，机器人/压测/健康检查单独标记；Invalid Credential 激增可能是撞库，Provider Error 激增是可用性故障，两者告警路径不同。监控认证页加载失败、安全 Redirect 拒绝和注册幂等恢复结果，但不得把账号、手机号、邮箱、IP 原值、验证码、协议正文或 Token 放入 Metrics Label/前端埋点。

#### 3.29.2 商品查询指标

覆盖商品列表/搜索/详情/店铺页的请求量、可用率、P95/P99、Cache Hit、搜索 Zero-result、候选数量、筛选/排序使用、详情 Not Found、已下架访问和索引新鲜度。技术成功（HTTP 2xx）与业务命中（返回至少一个有权限且可售商品）分开统计。

Zero-result Rate 按 Query Type、类目、语言、是否有精确商品编号分桶，不能记录原始搜索词；使用受控 Query Hash/脱敏聚类排查。搜索突然无结果需同时检查 MySQL 商品状态、索引同步 Lag、缓存版本和 Scope Filter，防止把权限过滤正确导致的空结果误报警为故障。

#### 3.29.3 购物车转化指标

按稳定匿名/用户 Cohort 记录 `product_view → add_to_cart → checkout_started → checkout_validated → order_created → paid`，分别计算 View-to-cart、Cart-to-checkout、Checkout-to-order 和 Order-to-paid。归因窗口首版可配置为 24 小时，但报表必须注明窗口、跨设备识别和多商品/多店拆分规则。

同时观察加购失败原因、购物车过期/删除、结算价格变化、库存不足、地址缺失和用户主动退出。漏斗下降不直接等于后端故障，应与 API 错误、页面版本和流量来源联合分析。前端埋点允许用于 UX Funnel，但订单/支付节点必须用服务端事实覆盖。

#### 3.29.4 订单创建成功率

定义两个口径：`technical_success_rate = 成功返回确定结果的创建请求 / 进入 Order Service 的有效请求`；`business_create_rate = 成功创建订单 / 通过 Schema、认证且请求创建的业务尝试`。参数错误、未登录、风控拒绝、库存不足、价格变化、幂等重放、系统错误分别分桶，不能把所有 4xx 混成系统失败。

成功必须存在 Trade Order/Store Order/Items/Address Snapshot/Reservation/Outbox 的一致事务结果；仅返回 200 或仅生成 ID 不算。监控创建 P95、事务回滚、Deadlock Retry、Unknown Response、同 Idempotency Key 结果不一致和“有订单无预占/有预占无订单”对账差异。关键不变量破坏直接 P0/P1。

#### 3.29.5 库存冲突率

`insufficient_stock_rate = 库存不足拒绝的预占 / 有效预占尝试`，`concurrency_conflict_rate = 乐观锁/行锁冲突重试 / 有效预占尝试`，另报 Retry Exhausted、Lock Wait、Deadlock、Reservation Expired 和 Release Lag。库存不足是业务供需信号，不等同系统错误；并发冲突过高才可能反映热点 SKU 或锁设计问题。

监控 `available = on_hand - reserved` 不变量、负库存、Reservation 与 Order/Status 对账、超时订单释放积压以及热门 SKU 分片情况。SKU ID 不作为 Prometheus Label；热点明细在受控 Top-N 分析中展示。任何负库存或重复释放导致数量漂移为 P0/P1，需暂停相关 SKU 下单并补偿核对。

#### 3.29.6 支付成功率

以 Payment Attempt Cohort 计算：`payment_success_rate = 在归因窗口内进入 succeeded 的支付单 / 已发起且渠道受理的支付单`。按渠道、支付方式、客户端、金额区间和失败类别聚合，排除沙箱/测试；用户取消、余额不足、风控拒绝、渠道失败、平台异常、Timeout Unknown 分开。

同时监控创建支付失败、重复支付、订单已取消后支付成功、Payment 与 Order 金额/状态不一致、Provider 查询对账差异和成功耗时。支付成功事实来自验签回调或主动查询确认，前端“支付完成”页面不作为事实。Unknown 上升或重复资金副作用属于高优先级告警。

#### 3.29.7 支付回调延迟

至少记录三段：`provider_to_ingress = 平台接收时间 - Provider Event Time`（时间可信时）、`ingress_to_ack = HTTP ACK - 接收时间`、`ingress_to_committed = 本地支付事务提交 - 接收时间`，以及 `committed_to_downstream` 后续订单/通知 Lag。Provider Event Time 不可靠时标记 Unknown，不产生负延迟。

按 Provider/Event Type/结果统计 P50/P95/P99、验签失败、重复回调、乱序、处理失败和重试次数。核心状态更新必须在快速幂等事务后 ACK，通知等下游异步处理；回调量为零是否告警要结合历史流量和支付创建量。已支付但本地长时间 Unknown/Processing 通过对账任务兜底。

#### 3.29.8 退款成功率

分业务批准和渠道退款两个阶段：`approval_rate = approved / eligible_submitted`，`refund_execution_success_rate = channel_succeeded / authorized_refund_attempts`，`end_to_end_refund_rate = 在承诺窗口到达退款成功 / 已批准退款`。商家拒绝、用户撤销、资格不符、渠道 Processing/Unknown/Failed 分开统计。

监控申请到审核、退货签收到批准、批准到渠道提交、渠道提交到成功的时长，以及部分退款金额、重复退款、累计退款超支付可退额、售后状态与支付退款状态不一致。审批率是业务政策信号，不能当系统可用性；重复/超额退款为 P0，Unknown Aging 和补偿失败为 P1。

#### 3.29.9 物流同步延迟

定义 `track_freshness_lag = 当前时间 - 本地最后有效轨迹的 Provider Event Time` 和 `ingestion_lag = 本地持久化时间 - Provider Event Time`，并统计待同步包裹数、成功/空更新/失败、Provider 限流、重试、轨迹乱序和状态映射 Unknown。尚未发货订单不进入轨迹过期分母。

按承运商和履约状态制定不同阈值；长途运输无新轨迹并不必然故障，应结合 Provider 查询结果和业务时段。已签收回退运输中、多个包裹错误合并、长时间无任何同步、Provider 全量失败触发告警；用户页面必须展示最后更新时间，降级时不伪装实时。

#### 3.29.10 消息送达率

消息链路分别统计 Accepted、Persisted、Stream Published、Gateway Delivered、Client ACK、Read；`persistence_rate = persisted / accepted`，`online_delivery_rate = client_ack / 应向在线连接推送`。离线用户没有 Client ACK 不算送达失败；Read 是用户行为，不是系统可靠性。

监控持久化延迟、会话 Sequence Gap、重复 Message、WebSocket/SSE 连接、重连补拉、Push/SMS/Email Provider、未读计数差异和 Dead Letter。消息正文不进入 Metrics。Accepted 未持久化、已持久化无法补拉或跨会话投递是高优先级问题；实时通道失败但 REST 补拉可用时标记降级。

#### 3.29.11 AI 自助解决率

定义 Eligible Conversation：真实用户、AI 实际参与、属于已支持意图、非垃圾/测试，且已到观察窗口。`self_service_resolution_rate = 无人工接管且目标被业务事实/评估器判定解决的会话 / 已结束且可判定的 Eligible Conversation`。未回复、用户失联和窗口未满不直接算成功，分别报告。

按 Agent/Intent/Risk/Version 分桶，并联合重复咨询、用户纠错、负反馈、人工后续改判和任务完成率。只因 AI 没有转人工不能视为自助解决；简单寒暄也不能稀释业务分母。高自助率伴随投诉/Unsafe Answer 上升时应立即停止优化。

#### 3.29.12 人工转接率

`handoff_rate = 创建有效人工工单的 Eligible AI Conversation / Eligible AI Conversation`，按用户主动要求、AI 低置信、权限/能力边界、Provider 故障、安全风险、重复失败和策略强制分类。另报 Queue Wait、First Response、Abandonment、Resolution、重复转接和 AI 接管后仍发言异常。

转接率没有统一“越低越好”：售后争议等场景合理转接是成功行为。看板需与 3.28.29 的 Precision/Recall 和人工解决结果联合解释。某版本转接率骤降但自助解决未提升可能是漏转；骤升可能是能力退化或依赖故障。

#### 3.29.13 Agent 错误率

按 3.27.23 Outcome 定义 `agent_system_error_rate = system_failed / started_runs`，并分别报告 Model/Schema/Context/RAG/Tool/MCP/Checkpoint/Persist/Output Guard Error、Timeout、Cancelled、Budget Exceeded、Fallback 和 Partial。Denied、Grounded Abstention、User Input Required 不属于系统错误。

逻辑 Run 与 Retry Attempt 分开，避免重试放大分母；同时观察“最终成功但发生重试”的 Degraded Success。按 Agent/Skill/Model/Tool Version 和 Intent 低基数分桶。错误率、P95、队列 Lag 同时恶化通常是容量/Provider 问题；单一版本突增优先回滚。

#### 3.29.14 RAG 无结果率

`rag_empty_rate = ACL/有效期/阈值过滤后返回 0 个 Chunk 的检索 / 需要 RAG 的有效检索`。按 Agent、Knowledge Type、Intent、Language、Index Version 和 Outcome Reason（No Candidate、ACL Filtered、Below Threshold、Stale/Disabled、Dependency Failure）分桶。

权限正确过滤后的 Empty 是安全业务结果，Dependency Failure 是系统错误，二者不能合并。Empty Rate 需与应拒答率、Zero-result Golden、索引覆盖/新鲜度和用户追问联合观察；突然升高检查摄取任务、索引路由、Embedding/阈值版本和 Metadata Scope。不得通过降低阈值掩盖空结果而增加幻觉。

#### 3.29.15 异常告警等级

| 等级 | 定义与示例 | 响应目标 |
| :--- | :--- | :--- |
| P0 Critical | 数据跨租户泄漏、无确认资金写、重复支付/超额退款、凭证泄漏、核心审计失效 | 立即 Page；自动/人工 Kill Switch，事件指挥与管理升级 |
| P1 High | 大面积订单/支付失败、库存不变量破坏、Payment Unknown 积压、消息不可补拉、AI 高风险错误 | 立即通知值班，15 分钟内响应，优先止损/回滚 |
| P2 Medium | 局部 Provider/功能降级、P95/SLO 持续燃烧、物流/索引/任务积压 | 工作时段或 30–60 分钟内响应，按 Runbook 降级修复 |
| P3 Low | 趋势异常、容量预警、低影响数据质量和单例缺陷 | 进入工单与计划，设 Owner/截止时间 |

等级由用户/资金/数据影响、范围、持续时间、可恢复性和安全性质共同决定，不能只看阈值。告警规则采用多窗口 Burn Rate、最小分母和连续周期，部署/实验信息自动关联；P0 安全硬事件不等待窗口。告警合并、抑制和维护静默必须有到期时间与审计，禁止永久 Silence。

#### 3.29.16 告警通知渠道

P0/P1 使用值班 Page（电话/值班应用）+ 企业即时通信事故频道 + 事件工单；P2 使用即时通信 + 工单；P3 进入工单/日报。支付渠道、云平台等外部状态页仅作为补充证据。通知内容包含 Severity、开始时间、环境/服务/版本、影响范围、核心指标、Dashboard/Trace/Runbook、最近变更和已执行动作，不含用户/订单/Prompt 明文。

每条规则有 Primary/Backup Owner 和自动升级路径；未确认按 5/10/15 分钟升级，具体值由值班制度配置。恢复通知需说明恢复时间、剩余积压和是否继续观察。P0/P1 结束后形成复盘、行动项和评估/回归 Case；定期检查无效告警、漏报、响应时长和渠道演练，确保“告警已发送”不等于“有人接手”。

---

### 3.30 测试体系

测试体系沿用 3.4.12 的目录，采用 Test Pyramid + Contract + E2E + AI Evaluation：大量快速确定性单元测试保护 Domain/Policy；真实基础设施集成测试验证 SQL、锁、事务和协议；少量端到端测试验证关键用户旅程；模型随机质量由 3.28 的独立评估流水线负责。不能用大量慢 E2E 替代领域测试，也不能因 AI Evaluation 独立就跳过 Agent 的确定性代码测试。

统一使用 `pytest`、`pytest-asyncio`、类型安全 Factory/Fixture 和覆盖率报告；可使用 Hypothesis 做属性/状态机测试。MySQL、PostgreSQL+pgvector、Redis 和对象存储集成使用与生产主版本兼容的容器，镜像固定摘要；禁止用 SQLite 代替 MySQL 语义。支付、物流、模型和外部 MCP 使用可编程 Fake Server，真实沙箱只用于发布前少量验证。

CI 分为：PR Fast（Lint/Type/Unit/受影响 Contract）→ Merge Integration（真实容器、迁移、API）→ Release（并发、安全、E2E、Full Eval、性能基线）→ Canary Verification。失败测试保留 Seed、Fixture Version、容器/镜像版本、Request/Trace/Run ID 和最小复现 Artifact；Flaky 不得靠无限重试掩盖。

#### 3.30.1 单元测试

范围包括纯 Domain Rule、Money/时间计算、状态机 Transition、权限 Policy、Pydantic Schema、Mapper、缓存 Key、错误映射、Prompt/Context 组装预算和确定性 Graph Node。单元测试不访问网络、文件、系统时间或真实数据库；通过注入 Clock、ID Generator、Provider Port 和 Repository Fake 保持可重复。

采用 Arrange–Act–Assert，测试行为而非私有实现；参数化边界值、非法值、时区、金额舍入、空集合和最大长度。对状态机和金额/库存不变量使用属性测试。覆盖率是发现缺口的辅助指标，首版建议 Domain/Policy 分支覆盖不低于 90%、全项目行覆盖不低于 80%，但关键规则必须逐条有测试，不能靠无断言调用刷覆盖。

#### 3.30.2 Repository 集成测试

每个 Repository 对真实 MySQL/PostgreSQL/Redis 执行 CRUD、唯一约束、索引查询、分页顺序、逻辑删除、乐观锁、时间精度、JSON/Decimal/加密字段和错误映射。验证 ORM Mapping 与 Migration 后实际 Schema 一致；PostgreSQL 额外覆盖 Vector Dimension、Metadata Filter、HNSW/精确查询行为。

本章新增治理资源必须有专项约束测试：`user_roles` 只允许同一用户、角色和 Scope 存在一条 Active Grant，撤销/过期后重新授权必须生成新 Grant 且旧事件不可改写；`user_status_records` 验证多次冻结、提前解冻和到期投影；`admin_sensitive_access_grants` 验证一次性消费、字段/目标/Session 绑定和过期；`store_certifications` 与事件表验证多轮补件版本链；`store_service_policies` 验证已发布版本不可变、有效期不重叠；`shipment_items` 验证创建后不可修改及作废后的数量守恒；MySQL `admin_batch_jobs` 与 PostgreSQL `knowledge.indexing_jobs` 验证父子编号、执行版本和进度映射唯一。

本轮 P0 数据约束还必须覆盖：同 Conversation/Context Type 只有一个 Active Context，切换时 Version 单调且旧 Context 不能复活；同订单项成功退款 + 活动申请占用数量/金额不超上限，但不同订单项和剩余部分可并存；商品/FAQ Published Content Version 不可变、归属正确且只指向安全扫描通过版本，公开 Repository 永不选择原始 Source；运单密文仅可为通过订单本人校验的 `UserShipmentDetail` 临时解密，Agent/消息/管理投影查询不加载密文列。

本次一致性修复额外覆盖：`ai_user_consents` 同 User+Type+Scope 只有一个 Active Generated Key，续期/恢复产生新版且 Event 不可改；`ai_memory_cleanup_tasks` 的幂等 Key、单调进度、部分失败与重试不复活 Consent/Memory；`ai_feedback` 的 Active Reaction 唯一键和撤回版本链；Conversation Reopen 的 `(conversation_id,event_type,conversation_version)` 唯一约束；地址聚合 19 条时并发新建两条只成功一条。

P1 数据约束还要验证：`checkout_store_group` Context 的 NULL 安全资源版本唯一键、Active 唯一、Version 单调和 `expires_at <= checkout.expires_at`；Checkout Version/状态变化后旧 Context 即使尚未消费过期事件也不能被 Resolver 读取；一个 Checkout 只能产生一组 Trade Order，订单与 Payment 使用不同幂等 Scope；`buyer_remark` 数据库存储容量 500 但 API 写入永不超过 200 个 Unicode 字符。

测试容器按 Worker Session 复用以提速，但每个 Case 使用独立 Database/Schema/Key Prefix 或显式清理，不能依赖执行顺序。事务回滚 Fixture 不适用于验证 Commit 后事件/多连接可见性的 Case，此类测试应真实提交后清理。慢 SQL 使用 `EXPLAIN`/Query Count Guard 检测 N+1 和索引退化。

#### 3.30.3 API 接口测试

使用 FastAPI `TestClient` 或 HTTPX Async Client 验证路由、Dependency、认证、Request Validation、Response Schema、Problem Details、状态码、Header、分页、ETag/If-Match、Idempotency-Key、CORS/CSRF 和限流。除测试真正 Adapter 外，只 Override 清晰的外部依赖，不绕过 Application Service/权限层。

从生成的 OpenAPI 做 Schema/Breaking Change 检查，并用契约样例验证 Vue3 TypeScript Client 可正确反序列化。每个资源至少有成功、未认证、无权限、Not Found、Conflict、Validation Error、Rate Limited 和依赖失败 Case；Not Found/Denied 不应泄露资源存在。SSE 另测事件格式、顺序、Resume 和断连。

管理 API 额外按 `Admin Audience × Permission × Platform/Store Scope × Assurance × Recent Auth × Separation of Duties × Resource State` 生成矩阵。商品发布、库存调整、订单改价、发货、退款/申诉决定、评价治理、内容发布、批处理确认、AI 发布和死信重放必须验证 If-Match、Idempotency-Key、原因、影响摘要和审计；普通管理列表也要测试 Cursor、跨店过滤和敏感字段投影。管理员审批 Contract 还必须覆盖发起人自批、重复审批人、权限/Scope 撤销、审批过期、参数 Hash/资源 Version 变化、批准后执行失败、Unknown Outcome 对账和 Executor 重投递幂等。

针对本轮补齐的接口，必须覆盖：服务政策草稿—发布—撤回、认证要求补件—再次提交、用户冻结—提前/到期解冻、角色授权—撤销—重新授权、敏感字段 Grant 创建—读取—撤销/消费、包裹创建即发货—物流纠错—作废、人工工单进入等待—用户消息/人工恢复、索引父任务—执行子任务—取消/对账。User Audience 请求 `/admin/*`、`/support/*`，以及 Admin Audience 请求用户域 `/conversations/*`、`/reviews/*`、`/refund-applications/*`、`/shipments/*`、`/agent-runs/*`、`/agent-action-approvals/*` 均应失败；用户/客服 Realtime Ticket 与 Subprotocol 互换也必须失败，且不得通过更换 Token 类型获得不同租户的数据。

P0-02～06 Contract Suite 必须额外断言：同一 Shipment 的 Owner Detail 返回完整 `tracking_no`，Owner Summary/Admin Detail/Store List/Support/Agent/Message DTO 只返回掩码，非本人用户接口返回 404，预计范围为空时不出现生成日期；Read Cursor 请求/响应含匹配的 Message+Sequence、只能前进并返回绝对未读数/Version；退款资格逐项返回购买/成功/活动占用/可申请数量金额，并发冲突只影响相同 Order Item；商品/店铺/订单入口得到同一 Conversation，Context 切换使旧 Version Tool Call 失败；商品详情只返回已发布安全内容判别联合，OpenAPI 中不存在公开 `source_content`。

P1-01～09 Contract Suite 必须额外断言：`GET /homepage` 可局部降级且商品卡不触发详情 N+1；店铺商品查询只接受 `q/group_id/sort/cursor`，所有 API 字段 snake_case 且不存在内部自增 ID；买家备注 200 字边界、纯空白和控制字符处理一致；订单创建响应不创建 Payment，两个 Idempotency Scope 独立，`CHECKOUT_ALREADY_CONSUMED/PAYMENT_ATTEMPT_IN_PROGRESS/PAYMENT_WINDOW_EXPIRED` 等分支返回可恢复引用；评价资格、售后/物流/收藏/账号注销页面所需读取与命令均有专用 Schema；`checkout_store_group` 只返回本店最小摘要，旧 Version、跨店、过期和已消费状态全部拒绝。

新增契约 Suite 必须断言：`buy_now.items` 零项或多项返回 422，恰一项成功；`cart_item_ids` 只允许 1–100 项。地址列表返回 `active_count/max_count/can_create`，超 20 返回 `ADDRESS_LIMIT_REACHED`。AI Consent 覆盖跨店 Scope、旧 ETag、续期/暂停/恢复/关闭全部；Memory 覆盖更正版本链、重复删除、撤销后零召回和清理任务失败/重试。AI Feedback 覆盖赞踩切换/撤回、Report/Correction 限流，以及他人、Human/System/Tool 消息全部拒绝。

#### 3.30.4 数据库事务测试

在真实 MySQL 隔离级别下验证订单 + 库存预占 + Outbox、支付状态 + Order 更新 + Outbox、退款状态 + 审计等原子边界。故障注入点覆盖首条写后、聚合写后、库存预占后、Outbox 写前/后和 Commit Response 丢失，断言结果为全提交或全回滚，不出现半成品。

测试锁等待、Deadlock Victim、乐观锁冲突、事务重试和 Retry 后事件唯一性；外部 HTTP/MCP 不得发生在数据库事务中。使用两个以上独立连接/进程而非同一 Session 模拟并发。Migration 测试包含全新安装、上一生产版本升级、数据回填、回滚/Forward Fix 和大表锁时间评估。

新增原子边界包括：冻结投影 + 状态事件 + Session 撤销事件；角色旧 Active Grant 终止 + 新 Grant + 授权事件；敏感访问 Grant 消费 + 访问审计；包裹作废 + 未履约数量恢复 + 履约投影 + Outbox；服务政策发布/替换 + 缓存/RAG 事件；MySQL 索引父任务创建 + Outbox，以及 PostgreSQL 子任务进度回写的版本条件更新。对每一边界都注入 Commit Response 丢失、重复命令和并发 Version 冲突。

#### 3.30.5 库存并发测试

对同一 SKU 以多进程/异步 Worker 同时预占，场景覆盖库存等于、少于和远少于请求量，断言成功预占总量不超过可售量、`available >= 0`、每个 Idempotency Key 只有一个结果。还要覆盖多 SKU 固定加锁顺序、预占与释放竞争、支付与超时取消竞争、重复释放和 Lease 到期重跑。

测试必须跑真实 MySQL 并设置 Barrier 使请求同时进入关键区，记录 Lock Wait/Deadlock/Retry；连续多轮随机化 SKU/数量以发现稀有交错。性能测试报告吞吐和 P95，但正确性不以压测“暂时没复现”证明；数据库 CHECK/唯一约束和条件 UPDATE 仍是最终防线。

#### 3.30.6 订单状态机测试

由单一 Transition Table 自动生成允许/禁止转换 Case，订单主状态只覆盖 `pending_payment、paid、pending_shipment、shipped、completed、cancelled、closed`；另分别覆盖 `payment_status、fulfillment_status(unfulfilled/partial/shipped/received)、after_sale_status` 的正交状态，不把 `processing、partially_shipped、received、after_sale` 混入 `orders.order_status`。每个合法 Command 断言新状态、Version、Status Log、Operation Log、Outbox 和关联库存/履约副作用；非法转换断言 409 且无写入。

属性测试随机生成 Command 序列，持续验证终态不可逆、已取消不能发货、未支付不能确认收货、重复 Command 幂等和 Store/Trade Order 聚合规则。测试 Clock 控制自动取消/自动完成边界，精确覆盖过期前后 1 微秒和时区转换。

履约状态机另覆盖“包裹创建即确认发货”的契约：不存在可编辑草稿；承运商揽收前且无阻断售后的包裹可以经专用命令作废，揽收后不得作废；作废与揽收回调并发时按版本和渠道核验只产生一个终态；多包裹商品数量始终满足已发货、已作废恢复量和未履约量守恒。

#### 3.30.7 支付回调幂等测试

Fake Provider 生成正确/错误签名、重复 Event、乱序、金额/币种不匹配、未知 Payment、成功后失败、超时重发和并发回调。断言验签在解析业务内容前完成，同一 Provider Event 只处理一次，同一 Payment 的重复成功只推进一次订单并只产生一次业务 Outbox。

模拟 Commit 成功但 ACK 丢失、Callback Worker 崩溃和主动查询与回调竞争；重试后业务终态一致。错误签名/金额不得产生状态写或敏感响应；Unknown 进入对账而非盲重试写。测试不得输出签名密钥/原始支付报文到日志 Snapshot。

#### 3.30.8 退款状态机测试

覆盖申请、审核、等待退货、退货运输、收货、批准退款、渠道处理中、成功、拒绝、撤销和关闭的合法/非法转换，以及仅退款/退货退款/部分数量/多次申请。验证 Eligibility Policy、联系方式加密快照、金额可编辑/只读边界、可退数量/金额、改价/运费分摊、订单级与订单项调整额守恒、累计退款不超过含改价分摊的订单项实付和同商品售后互斥规则。申诉独立覆盖 `submitted/reviewing/upheld/rejected/cancelled/closed`，断言原退款申请保持终态且支持申诉后的补偿命令不改写历史。

最小重复申请矩阵包括：同订单 A 商品活动售后不阻断 B 商品；A 购买 3 件且 1 件成功、1 件活动占用时仍可申请剩余 1 件；两个并发请求争用最后 1 件时只有一个成功；拒绝/撤销/关闭释放活动占用但重新申请产生新 `refund_id`；申诉产生 `appeal_id` 且原申请仍为拒绝终态。数量可用但金额已耗尽、金额可用但数量耗尽、订单级改价尾差和运费只退一次也必须覆盖。

并发测试重复提交、审核与撤销、退货签收与拒绝、渠道回调与主动查询、退款成功与订单完成。每个写操作断言 Action Approval、幂等、状态/事件/支付退款记录一致；Provider Unknown 不得标成功，补偿/对账后只产生一次资金副作用。

#### 3.30.9 WebSocket 测试

使用 ASGI TestClient 验证握手认证、Conversation Membership、Origin、Subprotocol、心跳、发送/接收、消息 Sequence、ACK、未读、断开和 Token 过期；再用真实 Uvicorn 多实例 + Redis 环境验证跨实例 Pub/Sub/Streams、重连补拉和连接迁移。

场景包括重复 Client Message ID、乱序网络、慢消费者背压、帧/频率上限、服务重启、Redis 降级、同账号多设备和跨会话订阅攻击。断言消息先持久化后推送，断连不丢历史，重复投递可去重，离线不误算失败；不得在错误帧泄露其他会话存在或消息内容。

人工客服场景还要验证 `active → waiting_user → active` 的状态事件：客服主动等待后 SLA 暂停且前端收到状态帧；会话用户发送新消息可原子自动恢复，客服也可显式恢复；自动恢复与人工恢复并发只能成功一次，重复事件不重复累计 SLA；User Audience 不能订阅客服队列，Admin Audience 不能冒充用户在用户会话通道发送消息。

Conversation Reopen 专项覆盖：关闭后从业务入口重开、关闭后直接发消息、并发两条新消息、Hidden 恢复、存在非终态人工工单和重复 Outbox。断言只有一次 `closed→active`/Reopened Event、消息 Sequence 唯一、无人工工单时恰好一个 Agent Run；Hidden 恢复不产生状态变化。

已读场景使用真实/模拟 Intersection Observer 与 Page Visibility：仅点击会话、后台页签加载、消息请求失败、Skeleton 或停留旧消息都不得推进；目标消息渲染并满足 50%/500 ms 后只推进到最高可见 Sequence。制造新消息与 Read、两个页签大小 Cursor、乱序 `unread.updated` 和 Redis 丢失竞争，断言 MySQL 单调、客户端采用更高 `cursor_version` 的绝对计数且永不为负。

#### 3.30.10 Worker 任务测试

单元层直接调用 Task Handler/Application Service，固定 Clock 验证参数、状态重读、Retry 分类和幂等；不要只依赖 Celery Eager Mode，因为它不能覆盖 Broker、ACK、Prefetch 和 Worker 崩溃语义。集成层启动真实 Redis/Celery Worker/Streams Consumer，验证发布、Claim、ACK、XAUTOCLAIM/Redelivery、Lease 和 DLQ。

每类任务覆盖至少一次投递、重复投递、乱序、处理时崩溃、超时、暂时/永久错误、最大重试、死信重放和依赖降级。断言业务副作用以 Event/Idempotency Key 去重；Scheduler 多副本同一 Slot 只产生一个扫描 Run，但单个业务任务仍幂等。测试结束检查 Pending/PEL/临时锁无泄漏。

专项 Worker Case 包括：冻结期限到期时仅条件解冻当前仍匹配的状态且不恢复已撤销 Session；角色 Grant 到期后失效权限缓存；服务政策发布/撤回触发缓存失效与 RAG 重建；索引 Outbox 重投递不重复创建 PostgreSQL 子任务，取消可传播，进度事件按 `execution_version` 单调回写，跨库遗漏由 Reconciler 补齐而不倒退终态。

#### 3.30.11 MCP Contract Test

为每个 MCP Server 固定 Protocol/Capability 与 Tool Manifest，测试 Initialize、Tools/List、Tools/Call、取消、超时、错误和 Transport 重连；请求/响应按 JSON Schema 验证，未知字段、错误类型、大小上限和版本不兼容有明确行为。Client、Security Gateway 和 Fake Server 三方都跑同一 Contract Corpus。

安全 Case 注入伪造 User/Store/Approval、任意 URL/路径、超量列表、间接 Prompt Injection 和 Server 返回敏感额外字段，断言 Host 参数重建、Scope、结果脱敏与审计。Transaction Tool 还验证 Action No 幂等、Unknown Outcome 查询和重复副作用为零。生产 Server 新版本必须同时兼容当前与待发布 Client，或创建新 Tool Version。

#### 3.30.12 Skill 测试

Skill 单元测试固定输入 Context 和 Fake Node/Tool，覆盖 Router Trigger、Input/Output Schema、Graph Branch、Budget、Timeout、Retry、Interrupt/Resume、降级和禁止路径。发布测试使用其 Version 固定的 Skill Case Set，断言 Tool Binding、Scope、确认等级、Citation 和最终结构化 Outcome。

采用“允许计划集合 + 业务不变量”而非锁死每个内部 Token/措辞，使重构不会导致脆弱测试。Transaction Skill 必须测试 Approval 前不执行、参数变化重新确认、Resume 只执行一次、Unknown 后对账和 Kill Switch。Skill/Tool/Policy 任一版本变化都触发 Contract 与 3.28 准入评估。

店铺 Skill 专项测试 `store_product_compare` 的同店 2/4 个成功、1/5 个数量拒绝、跨店/下架任一候选时整体拒绝，以及 `store_preference_candidate` 无 Consent/跨店/中高风险内容拒绝、结构化确认前 Active 数为零。3.17.9 与 3.23.14 的 Skill Code/Version/Tool Binding 必须由同一 Registry 快照比对。

#### 3.30.13 Agent 工具调用测试

通过 Scripted/Fake Model 稳定生成 Tool Call，确定性验证 Agent Runtime 的 Schema Validation、Tool Allowlist、参数重建、并发/顺序、结果截断、Retry、Checkpoint 和最终消息；另用真实候选模型在 3.28 数据集评估工具选择概率。两层不能互相替代。

场景覆盖不需要 Tool、单 Tool、多步只读、写前 Approval、Tool Not Found、业务冲突、Timeout Unknown、恶意 Tool Result 和模型重复调用。断言每次逻辑 Action 只有一个 Action No，Attempt 可追踪，模型不能通过别名/动态名字调用未绑定 Tool，Tool Success 也需核对业务结果后才能向用户宣称完成。

#### 3.30.14 Agent 权限测试

从 3.25 的决策链生成 Matrix：Agent/Skill/Tool Binding、Identity、Conversation/Store/User Scope、Resource Ownership、Consent、Approval、Business State、Amount/Quantity 和 Hard Deny。每个规则包含 Allow 与至少一个 Deny，对 Deny 断言 Tool Adapter 未被调用、Domain 无副作用、Result 不泄漏、Audit Reason 正确。

使用 Mutation Test 或故意关闭某一层验证双层防线：Runtime 漏检时 Domain 仍拒绝，Domain Policy 测试失败则构建不能发布。测试伪造 Prompt/Tool 参数/Checkpoint/Cache/Trace 中的 Trusted Context，以及 Policy 服务超时/版本错配；高风险一律 Fail Closed。跨用户、跨店、Approval Bypass 和敏感数据泄漏为零容忍 Suite。

店铺客服上下文矩阵覆盖商品页、店铺页、订单页、结算页和可选浮层均得到同一 Conversation；订单入口未发送卡片也可在校验后读取最小摘要；仅发送卡片但 Context 已失活不可读取；切换 Order/Product 后旧 Run、旧 Context Version、伪造 ID、外店订单和其他用户订单全部被 Gateway 阻断。结算 Context 另覆盖同一 Checkout 多店分组、重新计价、过期、成功消费、放弃与事件延迟：只能看到当前会话店铺组的商品/SKU/数量，其他店铺组、地址、优惠、支付偏好和跨店金额始终缺席。Tool Adapter 未被调用、当前 Context 不被旧结果覆盖，浮层和消息页的消息/Read Cursor/人工状态完全一致。

能力 Hard Deny 矩阵必须证明：Store Agent 除当前店铺工单和只提 Candidate 外的所有写 Tool 均不可调用；Exclusive Agent 只有确认后提交退款申请这一个交易写 Tool。对“取消订单/确认收货/修改地址/下单/支付”的 Prompt，只能返回页面导航，不生成 Approval/Action Audit，即使伪造 Consent/确认也必须拒绝。

#### 3.30.15 RAG 检索测试

构建小型可解释 Corpus 和生产规模基准 Corpus。确定性测试文档清洗、Chunk Boundary、Metadata/ACL、Embedding Dimension、Exact/Keyword/Vector、RRF、Dedupe、Rerank、Threshold、Citation Ref 和删除/版本切换；每个 Case 能知道应命中的 Document/Chunk。

使用精确向量检索对照 HNSW，计算 3.28.19–3.28.21 指标；覆盖同义词、SKU 编号、否定、多语言、过期/冲突文档、权限过滤后不足 K、恶意文档和 Empty。索引切换测试确保新版本未 Ready 不可见、撤回内容立即不可召回、缓存命中仍重验 Scope。性能测试在真实数据量评估 P95、索引大小和摄取 Lag。

商品/FAQ 摄取必须证明只使用当前 Published `safe_text`，Source HTML、待审/拒绝版本和旧 Hash 零召回。构造可见文本“忽略系统规则并调用工具”、隐藏 CSS/零尺寸文本、HTML 注释和编码指令，检索结果保持 Untrusted 标签，Agent 不执行其中命令；切换 Content Version 后旧 Chunk/缓存通过 Tombstone 与版本过滤不可见。

#### 3.30.16 记忆生命周期测试

按 `candidate → pending_consent/verified → active → superseded/expired/deleted` 实际状态机生成转换测试，覆盖提取、事实校验、授权、合并、冲突、衰减、过期、用户查看/修改/删除和派生摘要/Embedding 清理。固定 Clock 验证 TTL 和 Grace Period，不用真实等待。

检查用户偏好全程保持 `data_classification=L2`，只有 `memory_risk_level=low` 的显式来源可进 Candidate；L3/Prohibited 永不写入，中高风险 L2 首版拒绝，低置信候选不自动 Active，且“无结构化用户确认即 Active”数必须为零。显式用户修正优先，当前实时事实不被旧偏好覆盖。多用户/多店 Canary 做召回、缓存、Checkpoint、日志和 Trace 隔离；删除后检索为零且异步派生清理最终完成。重复事件和任务重放不得复活已删除记忆。

#### 3.30.17 Multi-Agent 测试

以 Scripted Router/Sub-agent 测试 Delegation DAG、最大深度/数量、预算继承、Context 最小化、并行只读、写操作串行、取消、超时、Partial 和 Reducer 冲突处理。构造递归委派和两个子 Agent 请求同一写 Tool，断言 Runtime 阻断。

权限上检查每个子 Agent 独立 Tool Set/Scope，父 Agent 不能通过委派扩大权限，子结果中的不可信指令不能影响 Supervisor。可观测性断言 Parent/Link、Delegation No 和关键路径完整。真实模型评估需与单 Agent 基线比较任务成功、成本、延迟；未证明收益的委派不发布。

#### 3.30.18 安全测试

覆盖认证/Session/CSRF/CORS、RBAC/IDOR、店铺隔离、SQL/NoSQL/命令注入、XSS、SSRF、文件上传、对象存储签名、Webhook 验签/重放、限流、Secret/日志泄漏、依赖/SBOM/镜像扫描和管理员高危操作。SAST、Dependency/Container/IaC Scan 在 PR/构建执行，DAST 和渗透测试在隔离环境执行。

AI 专项覆盖 Prompt Injection（直接/间接/多模态）、敏感信息披露、供应链/知识投毒、不安全输出、Excessive Agency、System Prompt 探测、向量/Embedding 隔离和资源耗尽。模型视为不可信输入源，成功标准落到确定性 Gateway/Domain 无副作用。高危发现阻断发布；定期 Red Team、凭证轮换/撤销演练和备份恢复演练，结果转成永久回归 Case。

管理端专项覆盖 Admin/User Token Audience 混用、MFA/恢复码重放、近期认证过期、权限自提升、删除最后安全管理员、跨店快速切换响应污染、批量任务在权限撤销后继续执行、敏感字段 Grant 越界、内部备注泄漏、恶意导入文件、CSV/公式注入、审计导出越权、内容富文本 XSS 和无 Preview 死信重放。所有资金、权限、库存和发布类越权副作用必须为零。

敏感字段 Grant 必须逐项测试管理员、Admin Session、目标用户、允许字段、Purpose、TTL 和使用次数绑定；权限/Session 撤销、Grant 撤销、过期或已消费后再次访问一律拒绝，列表/日志/导出始终保持脱敏。相同业务对象分别使用 User 与 Admin 专用 Operation 做正向测试，再互换 Audience 做负向测试，确保不存在“同一路径由不同鉴权依赖各自解释”的旁路。

物流泄漏测试用同一 Shipment 检查 Owner 页面、订单概览、店铺管理列表/详情、人工客服 Tool、Agent Tool、消息卡片、日志、Trace、指标、异常上报与前端持久状态；只有通过订单本人校验的单包裹 Owner Detail 可以出现完整运单，其余位置必须只有掩码。富文本测试输入 `script`、全部 `on*` 属性、`javascript:/data:`、iframe、form、恶意 SVG/MathML、CSS URL、协议相对 URL、模板表达式、畸形嵌套和超大内容，断言服务端拒绝/清除、CSP/Trusted Types 阻断、前端失败时只回退 Safe Text。再把相同 Payload 作为间接 Prompt Injection，断言 Agent 不提升为指令、无越权 Tool/Memory/RAG 污染。

#### 3.30.19 性能测试

建立独立、接近生产拓扑的环境和脱敏合成数据量，测试 API、搜索、结算/下单、消息、Worker、RAG 和 Agent。类型包括基线、Load、Stress、Spike、Soak 和 Capacity；模型/第三方分别使用可控延迟 Fake 测平台瓶颈，再用真实 Sandbox 测端到端网络波动。

报告吞吐、并发、P50/P95/P99、Error、连接池/锁、CPU/内存、GC、Redis/DB/Stream Lag、TTFT、Token/Cost，并预先定义 SLO/停止阈值。写压测使用隔离商户/支付沙箱、唯一幂等键并可清理，禁止碰生产真实资金。Soak 检查连接/内存/Checkpoint/PEL 泄漏；结果与镜像/配置/数据规模绑定，回归超阈值阻断发布。

#### 3.30.20 端到端测试

使用 Playwright 驱动 Vue3 用户端与独立管理端，并经过真实 FastAPI、MySQL、PostgreSQL、Redis、对象存储和 Worker，外部支付/物流/模型由契约一致 Fake/Sandbox 提供。用户端关键旅程包括：注册登录 → 浏览/搜索 → 商品详情 → 单品/购物车结算 → 支付回调 → 我的订单 → 物流 → 确认收货 → 评价；以及退款、店铺页、消息、专属客服/店铺客服、Approval 和人工接管。管理端关键旅程包括：管理员密码 + MFA 登录 → Scope 菜单 → 店铺/商品/SKU/库存 → 订单发货 → 退款审核 → 客服接管；以及内容/知识/Agent 候选版本 → 评估 → 审批 → 发布/回滚、批量导入/导出和死信预览/受控重放。

E2E 断言可见 UI、API、数据库终态、异步最终一致和审计，不依赖固定 Sleep；通过事件/轮询带 Deadline 等待。每个 Case 使用独立 User/Store/Order Namespace，失败保存脱敏截图、网络记录、Trace/业务 Ref，禁止保存密码/Token/地址明文。只保留少量高价值旅程作为 Release Gate，其余规则下沉到更快测试；上线后运行不产生真实副作用的 Synthetic Smoke。

用户端 P0 Release Gate 至少包含：本人包裹详情显示/复制完整运单而概览和 AI 仅显示掩码，多包裹分别显示预计范围/来源/更新时间且无数据时不承诺；会话未读只在真实可见后清除并跨页签以服务端绝对值校正；同订单多商品/同商品部分数量的售后并存、拒绝后新建申请与独立申诉；商品/店铺/订单入口与桌面浮层复用同一店铺会话并安全切换 Context；恶意商品/FAQ 富文本不执行且 AI 不服从隐藏指令。

用户端 P1 Release Gate 还包括：首页区块局部降级、独立搜索页筛选/Cursor/返回锚点；商品首次停留顶部、SKU 多图回退、Sticky/移动购买栏；完整认证与 Session 恢复；订单创建成功但 Payment 创建失败、渠道返回和 Payment Unknown；结算多商品本店客服 Context 与旧 Version/跨店负向用例；消息手机两级路由、可见 `…` 菜单、历史滚动新消息按钮、发送失败同 ID 重试和重连补拉；评价/物流/售后/收藏/设置及 403/404/410/412/429/离线/维护页面。所有旅程在桌面、平板、手机和 200% 缩放下不遮挡内容，并完成键盘与焦点断言。

AI 隐私/反馈 Release Gate 另要求：用户不经聊天即可在 `/me/settings/ai-personalization` 完成授权查看、单 Scope 暂停/恢复、全部关闭、单项更正/删除与清理状态查询；跨店、旧 ETag、重复删除、异步部分失败有确定 UI。AI 气泡的赞/踩/举报/纠错在键盘和读屏下可用，不允许反馈人工/他人消息，且反馈不自动进入 Golden Dataset 或长期记忆。同一 Suite 验证 `/help`、`/help/:contentKey`、`/about` 的 Published/404/410/Network 和外链 Allowlist。

管理端 Release Gate 还包括：创建政策草稿并发布/撤回、认证要求补件后按新材料版本再次审核、冻结用户并提前/到期解冻、撤销角色后重新授权、一次性查看敏感字段、创建即发货后在允许窗口作废包裹、客服置为等待用户后由用户消息恢复、索引父任务追踪到 PostgreSQL 执行子任务及取消/失败对账。每条旅程必须同时校验 Text UI 状态、专用 Audience 路径、权限 Scope、状态流水、审计和最终一致事件。

#### 3.30.21 管理端前端、RBAC 与高风险操作测试

管理端 Vue3 组件测试覆盖权限菜单、Scope 切换、表格 Cursor、筛选 URL 恢复、ETag 冲突 Diff、近期认证弹窗、影响确认、异步 Job 进度、脱敏字段和错误状态。组件不能仅通过“按钮隐藏”证明安全；Mock Server 对隐藏按钮对应 API 返回 403/404 时页面必须安全降级。图表同时验证文字/表格摘要，复杂表格、抽屉、弹窗和表单完成键盘/焦点/屏幕阅读器基础测试。

以 2.13.17 的每个 Text UI 区块为组件/E2E 清单，逐页验证正常、Loading、Empty、403、404、412、428、429、依赖故障和结果未知；按钮文案与 `available_actions`、唯一 OpenAPI `operationId`、规范 Permission 保持一致。抽屉/弹窗关闭后焦点返回触发按钮，表格可键盘操作，图表必须有同步文字摘要，后台任务离开页面后仍可从任务中心恢复查看。

建立最小角色矩阵：超级管理员、平台运营、平台审核、店铺负责人、店铺运营、平台人工客服、店铺人工客服、AI 运营、审计员和无权限管理员。每个页面至少测试正确菜单、正确 Scope、直接输入 URL、伪造 `store_id`、旧 Permission Version、无/过期近期认证和职责冲突。跨店测试使用相似公开 ID 与同名商品，确保 Repository、Cache、Export、Object Storage 和向量检索均不串租户。

高风险 E2E 逐项断言“确认/审批完成前无副作用、展示内容与实际参数 Hash 一致、不同有效管理员账号满足审批席位且 IdP 部署下同一 Workforce Subject 不能重复占位、并发只执行一次、失败/拒绝有审计、成功后业务状态/流水/Outbox/通知一致”：用户冻结、角色提升、商品发布、库存调整、订单改价、发货、退款决定、平台内容发布、Agent/Skill/Tool/Policy 发布、敏感导出和死信重放。生产发布门禁要求上述矩阵全绿，不接受通过手工验证替代。

#### 3.30.22 用户端前端、响应式与可访问性测试

以 2.12.2 每行路由生成 Route Contract Test，校验 Component、Auth Meta、参数到生成 Client 的 snake_case 映射、页面标题、Loading/Empty/403/404/410/Network 状态及离开 Guard。Mock Service Worker 只模拟 OpenAPI 已定义响应；前端不得通过手写字段适配隐藏契约漂移。覆盖刷新、深链、前进/后退、Session 过期登录返回、Cursor 过期、412 Diff、429 `Retry-After` 和写结果未知。

组件测试覆盖：AuthLayout、密码登录、验证码登录、注册配置、协议链接/勾选、验证码倒计时、密码可见切换、Caps Lock、错误摘要、挑战和安全 Redirect；首页局部错误与 Feed 恢复；商品 Scroll Spy、SKU 图片回退和 Sticky 边界；消息 `…` 菜单、移动两级路由、草稿 TTL、新消息不抢滚动、同 `client_message_id` 重试、重连去重及完整消息 Live Region；订单/支付两阶段；结算 Context；评价、物流、售后、收藏、设置和错误页。使用真实布局引擎的 Playwright 在 `<768、768～1199、≥1200`、横竖屏与 200% 缩放运行截图和遮挡断言，检查顶部/底部固定栏、Safe Area、焦点可见性和无意横向溢出。

可访问性采用自动扫描加人工键盘/屏幕阅读器抽查：原生语义、Enter/Space、跳过导航、Landmark、标题层级、可见焦点、错误关联、Live Region、对比度、44×44 目标、Reduced Motion、Dialog Focus Trap/Esc/焦点恢复必须通过。自动扫描为零严重问题仍不能替代人工验证；AI 流式消息逐 Token 播报、右键唯一功能、颜色唯一状态或被 Sticky 遮挡的焦点均直接阻断用户端发布。

#### 3.30.23 P2 文档契约与体验回归测试

建立 `DOC-P2-*` 契约套件，将 TODO-P2-01～06 转成可重复检查，而不是只依靠人工阅读：

- `DOC-P2-TEXT-*`：解析第二章全部 Text UI，断言围栏配对且开栏为 `text`；金额样例由 Decimal 重算，人民币、绝对日期时间与术语符合 2.2.5～2.2.6，禁止未声明别名和孤立占位行。
- `DOC-P2-SOURCE-*`：目录标题与实际 1～3 级标题一致；重复规则必须指向 2.2.8 的权威来源；OpenAPI、Permission Registry、Domain Enum 与 `docs/traceability.yaml` 不存在同名异义副本。
- `DOC-P2-ACTION-*`：订单列表、详情、售后及支付入口在缺少 Action 时隐藏，`enabled=false` 时展示稳定 `reason_code/reason_message`；前端状态枚举变化不能自行解锁按钮。订单隐藏撤销必须调用服务端 `restore_url`，并覆盖成功、`undo_until` 过期、412、重复与结果未知。
- `DOC-P2-ESTIMATE-*`：商品、Checkout、Order、Shipment、店铺客服和专属客服消费同一 `ServiceEstimate`；覆盖范围/来源/更新时间/说明码、过期与 `unavailable`，断言单点、不可靠富文本及模型推断不会生成日期承诺。
- `DOC-P2-HANDOFF-*`：人工消息正常持久化；交接/结束摘要只含授权、脱敏字段与必要消息引用；Internal Note、隐藏推理及无关历史在 User/Agent DTO、Prompt、Memory、日志与导出中均为零泄漏；人工结束且用户未发新消息时模型调用数为零。
- `DOC-P2-AUDIENCE-*`：同一自然人的 User/Admin/Store Staff/Support 身份使用独立入口、Cookie、Session 和 `aud/client_type`；交换 Token、Cookie Path 或 CSRF 上下文全部拒绝。Admin Audience 无购物车/下单能力，业务演练只可用隔离 Sandbox 用户且生产配置不存在 Sandbox 凭据。

此套件的失败项必须关联对应 TODO 与 Requirement ID；静态检查、Contract Test、组件测试和 E2E 证据统一保存到发布制品。文档后续变更若重新引入已禁止形式，应在合并前失败。

#### 3.30.24 用户登录、注册与认证页面测试

认证测试独立于普通页面 Smoke Test，使用可控时钟、短信/邮件 Fake Provider、真实 MySQL 唯一约束和 Redis 限流脚本，覆盖 UI、契约、并发、安全与恢复。测试数据只使用保留测试号段/域名，日志、截图、Trace 和失败 Artifact 在保存前扫描密码、验证码、Cookie 与 Token。

| 测试层 | 必测内容 |
| :--- | :--- |
| Vue 组件与可访问性 | `/login`、`/login/code`、`/register` 两列/单列布局；Label、Autocomplete、密码管理器、显示密码、Caps Lock、Enter 提交、字段错误和错误摘要焦点；验证码倒计时以服务端时间为准；协议链接在明确的新窗口打开且不丢失当前表单，`/legal/:documentType` 精确展示版本/生效时间/地区/安全正文；Challenge 有非图形替代；键盘、屏幕阅读器、Reduced Motion、200% 缩放和窄屏通过 |
| OpenAPI Contract | Registration Config 判别策略；Verification Code 的 Purpose/Target/ID/TTL；Registration 严格 Schema 与 `extra=forbid`；Login 的 Password/Code 判别联合；Required Agreement 精确覆盖；201/202/400/401/409/410/422/429/503、Problem Details、`Retry-After`、`Cache-Control: no-store`、Refresh Cookie 和 CSRF 字段均与 3.12.1 一致 |
| Repository/事务 | User、两类 Credential、Agreement Acceptance、默认 User Role、Exclusive Conversation、Verification Consume、Session、Idempotency Record 和 Outbox 要么全部提交，要么全部回滚；协议/验证码/用户名/Target 唯一约束与索引由真实数据库验证 |
| 并发与幂等 | 同 Username、同 Target、同验证码、相同/不同 Payload 的同 Idempotency Key、协议版本切换、验证码到期边界、Provider 接受后失败、Commit 前失败、Commit 成功响应丢失；断言最多一个用户、无重复角色/会话初始化/协议记录，旧 Bootstrap Session 不泄漏且恢复策略确定 |
| 安全 | 账号存在/不存在、已注册/未注册目标的响应形状与时序分布测试；Dummy Argon2id、IP/Target/Device 多维限流、挑战绑定、验证码 Purpose 隔离、尝试上限、Session Fixation、Login CSRF、CORS/Origin、Open Redirect、Audience 交换、Mass Assignment、弱密码/泄露密码、敏感日志与浏览器持久化全部验证 |
| E2E | 受保护页跳转登录并安全返回；密码登录；验证码登录但未绑定目标不自动注册；注册读取配置/打开协议/验证码/提交/自动登录/访问唯一专属客服会话；账号不可用、协议变化、429、网络结果未知、Refresh 恢复和 Session 过期路径均具有确定 UI |

Release Gate 的 Case ID 分为 `AUTH-LOGIN-*`、`AUTH-CODE-LOGIN-*`、`AUTH-REGISTER-*`、`AUTH-SESSION-*` 和 `AUTH-RECOVERY-*`。账号枚举形成可利用旁路、注册产生半成品用户/重复身份、法务接受记录缺失、Admin/Store Scope 可被请求体注入、旧 Refresh Token 可被重放或认证秘密进入 Artifact，均按 P0 阻断发布。

---

### 3.31 Docker 与部署设计

Docker 是统一构建与运行单元，不等于完整高可用平台。开发环境使用 Docker Compose 一键启动；小规模首期生产可使用受控 Compose/单机编排，但数据库、Redis、对象存储优先采用托管或独立高可用实例。需要跨主机自动调度、滚动发布、自动扩缩和故障迁移时，应保持镜像不变迁移到 Kubernetes、ECS/Nomad 等编排平台，业务代码不得依赖 Compose 特性。

所有环境以同一 Commit 构建不可变镜像，通过配置和 Secret 区分，不为生产手工修改容器。发布物至少绑定 Git Commit、镜像 Tag、OCI Digest、SBOM、依赖锁文件、迁移 Revision、配置 Schema Version 和签名/扫描结果；禁止使用 `latest` 或在容器启动时在线安装依赖。

#### 3.31.1 Docker 镜像设计

镜像分为 `frontend`、`backend` 和必要的基础设施镜像。`api/worker/scheduler/agent-runtime/mcp-*` 复用同一 Backend Application Image，通过固定 Exec-form Command 和最小配置启动不同进程，避免同一代码产生依赖漂移；MCP 若有不同系统依赖可建立受控派生镜像。

运行镜像使用固定版本/摘要的精简官方基础镜像、非 Root UID/GID、只读 Root Filesystem、`/tmp` 临时卷、最小 Linux Capability，并设置 CPU/Memory/PID/File Descriptor 限额。镜像不包含 `.git`、测试报告、`.env`、生产 Secret、上传文件或缓存。CI 生成 SBOM，执行 OS/Python/Node 依赖和恶意软件扫描，高危漏洞无有效豁免时阻断发布；镜像推送到私有 Registry 并启用不可变 Tag/签名验证。

#### 3.31.2 多阶段构建

Backend 建议阶段为 `python-base → dependencies → test（CI 可选）→ runtime`：Dependencies 阶段依据带 Hash 的 `requirements/base.txt` 使用 `pip --require-hashes` 做可复现安装并预编译 Virtualenv；Runtime 只复制锁定依赖、`app/`、迁移和必要资源，不复制 Conda、编译器、pip 缓存和测试工具。本地开发使用 Conda 不代表生产镜像内安装 Conda。先复制 Lockfile 再复制源码以利用 Build Cache，私有仓库 Token 通过 BuildKit Secret Mount，不能写入 Layer/ARG。

Frontend 建议 `node-deps → build → nginx-runtime`：锁定 Node/pnpm 版本和 Lockfile，执行 Type Check/Test/Build，最终只把 `dist/` 与审查后的 Nginx 配置复制到运行镜像。Source Map 是否发布需单独决策；生产 Source Map 默认不公开，可上传到受控错误平台。CI 用 `--target` 选择 Test/Runtime Stage，但生产镜像只能来自通过测试的同一构建图。

#### 3.31.3 Docker Compose 开发环境

Compose 文件拆分为基础 `compose.yaml`、开发覆盖 `compose.dev.yaml` 和按需 Profile，例如 `observability`、`ai`、`full`。默认启动 Frontend、Nginx、API、Worker、Scheduler、MySQL、PostgreSQL、Redis、MinIO；Agent/MCP/Prometheus/Grafana/Loki/Tempo/Langfuse 可随当前阶段启用。项目名包含开发者或 CI Run ID，避免网络、Volume 和端口冲突。

开发只暴露 Nginx、必要的数据库调试端口和 MinIO Console 到 `127.0.0.1`，内部服务使用 Compose DNS，不写死容器 IP。源码可 Bind Mount 并启用热更新，但生产 Compose 禁止源码挂载/特权模式。提供 `.env.example`、初始化 Fixture 和 `make/dev` 等统一命令；测试 Compose 使用临时 Volume/随机端口，结束后安全清理，不能连接生产地址。

#### 3.31.4 Nginx 反向代理

Nginx 是唯一公网入口和 TLS Termination 层。用户域 `www.example.com` 的 `/` 指向用户 Frontend，管理域 `admin.example.com` 的 `/` 指向独立管理 Frontend；两者的 `/api/` 均可转到同一 FastAPI 集群，但网关根据 Host/Origin 清洗并标记预期 Audience，后端仍以 Token Audience 为最终判断。`/ws/` 到 WebSocket，`/api/v1/agent-runs/*/events` 到 SSE；支付/物流 Webhook 走独立限流 Location。管理域不得代理用户购物静态入口，用户域也不得暴露管理 SPA。传递/生成 Request ID，设置可信 `X-Forwarded-For/Proto/Host`，FastAPI 只信任已配置代理网段，防止客户端伪造。

普通 API 启用合理 Keepalive、压缩和响应缓冲；SSE 设置 `proxy_buffering off`、足够长的读超时和禁用中间缓存；WebSocket 显式转发 `Upgrade/Connection` 并设置心跳。上传大小按 3.10 的文件类型分别限制，不设无限 Body。配置连接/请求速率、Header/URI 上限、安全响应头、静态资源 Hash 长缓存和 SPA `try_files`，但认证、业务限流和权限仍由 FastAPI 执行。

#### 3.31.5 Frontend 容器

Frontend 生产容器只运行静态 Nginx/Caddy 类服务器，不运行 Vite Development Server。用户商城和管理后台生成独立 Build Artifact、CSP/运行配置和路由入口，可使用两个最小静态容器或一个容器中的严格虚拟主机隔离；生产禁止让 `admin.example.com` 回退到用户端 SPA 或让用户域加载管理 Bundle。Vue 构建产物使用内容 Hash，`index.html` 短缓存或 `no-cache`，JS/CSS/图片可 `immutable` 长缓存；Compression 预构建或由边缘层处理。两个入口分别提供独立 `/healthz`。

浏览器可见配置只有 API Public Base URL、WebSocket/SSE URL、环境标识、公开支付参数和 Feature Flag，不包含 Secret。若需要同一镜像跨环境，启动时生成受控 `runtime-config.json`，字段使用 Allowlist 并做原子替换；不能用字符串替换整个 Bundle。用户端与其 API 尽量同站点以简化 Cookie/CSRF/CORS；管理端使用独立子域、独立 Origin Allowlist、Admin Cookie Path/Audience 和更严格安全响应头，两个应用不能读取对方 Refresh Cookie。

#### 3.31.6 FastAPI API 容器

API 容器运行 `uvicorn app.main:create_app --factory` 等 Exec-form Command，接收 SIGTERM 后停止接新请求、等待有界 Grace Period、关闭 DB/Redis/HTTP Client Pool 再退出。容器编排负责多副本；首版每容器一个 Uvicorn Process，依据基准测试再决定单容器 Worker 数，避免进程数与副本数双重放大数据库连接。

API 进程无本地业务状态：Session、消息、幂等和任务事实存外部系统；临时上传直传对象存储；本地缓存只作短时优化。因此副本可横向扩展且普通 HTTP 不需 Sticky Session。启动时只校验配置和初始化轻量 Client，不执行迁移、建向量索引或批量恢复。Readiness 失败从负载均衡摘除，Liveness 不因下游短故障而失败。

#### 3.31.7 Worker 容器

Worker 使用相同 Backend Image 按 Queue/职责分容器：`business-critical`（Outbox、支付后续、库存释放）、`notification`、`logistics`、`admin-batch`（导入/导出/报告）、`knowledge-indexing`、`evaluation`、`memory-summary` 等。不同队列设置独立并发、Prefetch、Soft/Hard Timeout、Max Tasks per Child、Retry 和资源限额，避免长时间导入、导出、Embedding 或评估任务阻塞订单任务。`admin-batch` Worker 使用只完成当前 Job 所需的 Service Identity/Object Prefix，不继承生产数据库 DDL、支付或全量跨店权限。

Worker 以至少一次投递为前提，ACK 时机、幂等与 Unknown Outcome 遵循 3.26。关闭时停止领取新任务并给在途任务有界完成时间；无法完成的任务由可见性超时/重投恢复。禁止 Worker 通过 Root 权限挂载 Docker Socket。扩容依据 Queue Lag/Oldest Age/处理速率，而不只看 CPU。

#### 3.31.8 Scheduler 容器

Scheduler/Celery Beat 默认只运行一个 Active Instance，使用编排单副本或带 Lease/Fencing 的 Leader Election 防止重复调度；即使重复，`scheduled_job_logs` 唯一约束与业务任务幂等仍是最终防线。Scheduler 只扫描到期 ID/生成调度事件，不直接批量修改订单。

Schedule 定义版本化并使用 UTC，记录预定时间、实际触发时间、漂移、Lease Owner 和 Run Result。容器重启后按 Job Policy 决定补跑/跳过过期 Slot，禁止把全部漏跑任务同时倾倒到 Worker。Readiness 验证 DB/Redis 可用和当前实例 Leader 状态；Liveness 只检查进程 Event Loop。

#### 3.31.9 MySQL 容器

开发/CI 使用固定摘要的 MySQL 8.4 LTS 镜像、独立 Named Volume、`utf8mb4`、UTC、显式 SQL Mode 和 InnoDB 配置。初始化脚本只创建 Database/最小权限 User；表结构由 Alembic Migration 创建。Root 凭证仅供初始化/运维，API、Worker、Migration 使用职责不同的账号，禁止应用 Root 连接。

生产优先托管 MySQL 或独立 HA 集群，不把数据容器与应用 Compose 一起删除/滚动。启用加密、备份、Binlog/PITR、慢查询、连接/磁盘/复制延迟监控；数据目录不得放临时容器层。开发健康检查使用 `mysqladmin ping` 加认证，生产就绪还需验证可执行轻量读写或按角色检查 Primary。

#### 3.31.10 PostgreSQL-pgvector 容器

开发/CI 使用与 PostgreSQL 18.x、pgvector 0.8.6 基线兼容且固定摘要的镜像，Named Volume 保存 Cluster。初始化仅创建 Database/User；`CREATE EXTENSION vector`、Schema、表和索引进入 PostgreSQL Alembic 链，启动脚本不得每次重建 HNSW。

生产使用独立 PostgreSQL/HA 服务，配置 WAL 归档/PITR、连接/磁盘/WAL/Autovacuum/Index 监控。Embedding 模型升级在新版本数据集/索引构建，不覆盖当前索引。就绪使用 `pg_isready` 加轻量查询，但 Vector 索引质量/同步新鲜度进入 Deep Health，不因暂时落后重启数据库。

#### 3.31.11 Redis 容器

开发使用 Redis 8.x 固定镜像、仅内部网络、密码/ACL 和可选 AOF；数据卷用于 Broker/必要的开发恢复。根据用途分 Logical Prefix，但生产达到规模后可拆分 Cache、Session/Rate Limit 与 Broker 实例，防止缓存淘汰影响任务系统。禁止以逻辑 DB 代替租户隔离或安全边界。

生产启用 TLS/ACL、内存上限、与用途一致的 Eviction、持久化/Replica/Sentinel 或托管高可用，并监控 Fork、AOF Rewrite、Replication Lag、Eviction 和 PEL。即使 Redis 持久化开启，关键事实仍由 MySQL Outbox/业务表恢复。健康检查使用认证 PING 与角色检查；失败时按 3.9.16 降级，不能把重启 Redis 当数据一致性修复。

#### 3.31.12 MinIO 容器

开发使用固定 MinIO 镜像和 Console，API 与 Console 端口只绑定本机；使用 Named Volume，单独的一次性 `bucket-init` Job 创建 Public Product、Private User/Refund、Knowledge Source、Temporary Upload 等 Bucket、Lifecycle、CORS 和最小 Policy。应用账号不能执行全局管理员操作。

生产优先 S3 兼容托管对象存储或 MinIO 分布式模式，启用 Versioning、服务端加密、Lifecycle、访问日志和跨故障域副本。私有对象只通过短期签名 URL/后端鉴权访问，Bucket 不允许目录列表。数据库保存 Object Key/Hash/Owner，定期对账孤儿对象；Volume Snapshot 不能替代对象级备份和恢复验证。

#### 3.31.13 Agent 服务容器

Agent Runtime 独立于 API 承载较长的 Context/RAG/Model/Tool Graph。API 先持久化用户消息和 Run，再投递/唤醒 Agent；断开 SSE 不终止 Run。Agent 容器无权直连除其必要的 PostgreSQL/Redis/Model Gateway/MCP Host 之外的任意系统，业务读写只能经安全网关与 MCP/Application Service。

按 Agent Profile 设置并发 Semaphore、单 Run Deadline、Token/Cost Budget 和 Graceful Shutdown。扩缩容前保证 Checkpoint 已提交；终止时停止接新 Run，并安全中断/重投，在 Approval/Human 等待期间不占进程。Readiness 检查配置/Checkpoint Store/Registry 和至少一个允许的模型路由可用；模型全故障时可保持进程存活但进入 Degraded Readiness/固定降级。

#### 3.31.14 MCP Server 容器

MCP Server 按 `catalog/order/logistics/after-sale/support/memory` 领域部署，可在首期按相同信任/扩容特征合并进程，但 Registry 中仍保持独立 Tool Namespace/Version。Streamable HTTP 只监听内部网络或 Service Mesh，不经公网 Nginx 暴露；Host 与 Server 使用服务身份、短期凭证/mTLS，并验证受信调用方。

每个容器仅获得其 Tool 所需的最小 Application Service Endpoint/凭证和网络 Egress，禁止数据库通配账号。限制 Request/Response Size、并发、超时和每 Tool 资源；部署新版本先跑 Contract Test，可并存版本再切换 Binding。健康检查不调用有副作用 Tool，只检查协议初始化、Registry/Policy Version 和关键只读依赖。

#### 3.31.15 环境变量与 Secret

普通配置通过 Pydantic Settings 读取，变量按 `ECOM_<DOMAIN>_<NAME>` 命名，启动时严格校验类型、范围、互斥项和未知关键字段；日志只输出配置来源/Hash，不输出值。`.env` 仅本地使用并加入 Ignore，仓库提交 `.env.example` 与说明，不放真实 Token。

密码、JWT/加密密钥、Provider Key、Webhook Secret、证书和数据库凭证通过 Docker Secret、Kubernetes Secret + KMS/External Secret 或云 Secret Manager，以只读文件/短期身份注入；应用支持 `_FILE` 读取。Build Secret 与 Runtime Secret 分离，绝不能进 Image Layer/Build ARG。Secret 按服务授权、定期轮换、审计访问；轮换采用双 Key/Key ID 窗口并验证旧 Key 撤销。

#### 3.31.16 数据卷

开发 Named Volume 包括 `mysql-data、postgres-data、redis-data（按需）、minio-data、grafana-data`；应用容器只挂只读配置和临时 `/tmp`，上传文件不落本地。生产 Volume 使用加密 Persistent Disk/托管服务，并设置容量、IOPS、Snapshot 和监控，不依赖匿名 Volume。

数据库 Volume 不能在不同主版本镜像间直接复用，升级先备份/恢复或按官方路径执行。不得将 Docker Socket、Host Root 或开发 Home 目录挂入容器。`docker compose down -v` 只允许明确的本地/CI 项目并在统一脚本中使用，生产禁用；备份对象必须位于独立故障域，不能只保存在同一 Volume。

#### 3.31.17 健康检查

提供 `/health/live`、`/health/ready` 和受保护的 `/health/deep`：Live 只证明进程/Event Loop 未死锁；Ready 检查配置已加载、连接池可用和该实例是否能安全接流量；Deep 展示依赖、队列、索引版本等脱敏诊断。Live 不检查所有下游，避免数据库故障引发 API 重启风暴。

Health Endpoint 不需要普通用户认证但只返回状态/版本，不泄露拓扑、DSN 或异常；Deep 仅内网/管理员。Docker Healthcheck 设置 `start_period、interval、timeout、retries`，退出码稳定。Worker 用 Heartbeat + Queue Consumer/Loop Lag，Scheduler 用 Leader/Last Tick，MCP 用 Protocol Readiness；业务 Synthetic Check 独立于容器健康。

#### 3.31.18 数据库迁移任务

每次发布先运行单副本 `migrate-mysql`、`migrate-postgres` Job，使用专用 DDL 凭证；成功后才启动依赖新 Schema 的应用。Job 记录 Revision Before/After、执行时长、Lock 观察、Artifact/Trace 和审批，失败立即停止发布。API/Worker 启动只验证兼容 Revision，不自动升级。

生产遵循 Expand → Backfill → Dual Read/Write（必要时）→ Switch → Contract，多版本滚动期间新旧应用均可工作。大表 DDL/索引在影子环境估时并设置锁超时；不可逆数据迁移提供 Forward Fix/备份恢复方案。回滚应用前先验证旧版本与扩展后 Schema 兼容，绝不直接 Down Migration 删除已写新数据。

#### 3.31.19 容器启动顺序

开发 Compose 依赖顺序为 Data Services Healthy → Migration Job Completed Successfully → API/Worker/Agent/MCP → Nginx/Frontend；Bucket Init 和观测初始化为独立一次性 Job。`depends_on` 使用 `service_healthy`/`service_completed_successfully`，而非只按 Container Running 判断。

启动顺序只是便利，不能成为正确性前提：应用对 MySQL/PostgreSQL/Redis 使用有上限指数退避和抖动，依赖未就绪时保持 Not Ready；运行中断线也能重连。非关键 Agent/观测组件失败不能阻止商城基础 API 启动；Migration 失败、配置/Secret 缺失和 Schema 不兼容必须 Fail Fast。

#### 3.31.20 API 横向扩容

API 副本保持无状态，由 Nginx/Load Balancer 按连接/最少请求分发。扩容先计算总 DB/Redis/HTTP 连接预算：`replicas × processes_per_replica × pool_size + worker/agent pools + reserve ≤ database max safe connections`；不能单独增加副本而打满数据库。WebSocket 在线状态和消息 Fan-out 通过 Redis/持久消息协调，连接断开由客户端补拉，因此不以 Sticky Session 保证正确性。

Autoscaling 信号采用 CPU、内存、RPS、In-flight、P95/Queue Time 和连接利用率组合，设置最小副本、冷却、最大增速和数据库保护上限。缩容先从负载均衡摘除、完成 Graceful Drain，再终止；SSE/WebSocket 有最大连接寿命/重连策略。容量不足时优先保护结算、支付回调和消息持久化，限流低优先级搜索/AI 请求。

#### 3.31.21 Worker 横向扩容

Worker 按 Queue 独立扩容，目标使 `arrival_rate < total_service_rate` 且 Oldest Age 满足 SLO。短 IO 任务可提高并发，CPU/内存密集的解析、Embedding 依据资源限制低并发；模型/第三方限流是全局上限，增加 Worker 不应放大为 Provider 风暴。

Business-critical Queue 保留最小常驻副本并设置最大扩容；扩容信号包括 Ready Count、Oldest Age、Processing Time 和 Error/Retry，不使用队列长度单一信号。缩容等待 ACK/Checkpoint，长任务支持 Chunk/续跑。不同版本 Worker 滚动并存时 Event Schema 向后兼容，旧 Worker 不得消费只支持新 Schema 的 Queue。

#### 3.31.22 HTTPS 与域名

建议域名：用户端 `www.example.com`、管理端 `admin.example.com`、API 同站点 `/api` 或 `api.example.com`；对象公开 CDN 使用独立无 Cookie 域名。公网只开放 80/443，HTTP 永久重定向 HTTPS；TLS 1.2/1.3、自动证书签发/续期、OCSP/证书到期告警，禁止弱 Cipher。

设置 HSTS（确认全站 HTTPS 后逐步增加 Max-Age）、CSP、`X-Content-Type-Options`、Referrer/Permissions Policy 和 Secure/HttpOnly/SameSite Cookie。管理端可要求 VPN/IP Allowlist/MFA。Webhook 使用独立 Path、Provider Allowlist（仅辅助）、签名/时间戳/重放校验；内部 MCP/数据库/Redis 不配置公共 DNS。证书私钥进入 Secret Store，不写镜像/仓库。

#### 3.31.23 开发、测试和生产环境隔离

环境使用独立账号/项目、VPC/Network、DNS、数据库、Redis、Bucket、KMS Key、Secret、OAuth/支付/物流 Credential 和观测 Tenant；不仅靠 Key Prefix 隔离。生产数据默认不复制到开发/测试；确需排障样本时执行审批、字段去标识、最小抽样和到期删除。测试邮件/短信/支付只能到 Sandbox/Allowlist。

构建 Artifact 可从低环境晋升到生产，但配置/数据不可反向混用；CI 身份不能拥有生产运行权限，部署使用短期 OIDC/职责分离。每个环境在日志、页面和 Alert 中显式标识，生产禁止 Debug/热更新/默认密码。任何代码试图连接另一环境的 Host/证书/Environment ID 时启动失败并告警。

---

### 3.32 稳定性与容灾

稳定性设计以“故障必然发生”为前提，通过隔离、超时、有限重试、熔断、降级、幂等、备份和演练控制影响。服务成功响应必须对应确定业务结果；外部写调用超时后的 `unknown` 不得当失败重做，也不得当成功展示。每个依赖在 Service Catalog 登记 Owner、SLO、数据等级、超时/重试/熔断/降级、RPO/RTO 和 Runbook。

故障按 `transient、permanent、business_conflict、unauthorized、unknown_outcome、overload` 分类，策略由代码/配置决定而非模型判断。降级不能破坏 MySQL 事实、权限、金额和用户确认；恢复后通过 Outbox/对账/重建补齐最终一致数据，不盲目重放已经过时的用户意图。

#### 3.32.1 超时控制

每个入口设置绝对 Deadline，并向下游传递剩余预算；子调用超时必须小于父 Deadline，预留响应持久化和安全输出时间。建议初始值通过压测调整：普通 DB Query 1–3 秒、Redis 100–300 毫秒、内部 HTTP/MCP 2–5 秒、支付/物流读取 3–10 秒、单次模型调用 15–45 秒、普通 Agent Run 60–120 秒；批量索引走 Worker 长任务，不占 API Deadline。

区分 Connect、Pool Acquire、Read、Write 和 Total Timeout，禁止 HTTPX/DB/Redis 使用无限默认值。SSE/WebSocket 连接可长，但单个业务 Run、心跳和空闲均有 Deadline。超时取消向下传播；无法确认外部写是否执行时标记 Unknown 并启动查询/对账。Timeout 指标按依赖/操作/Attempt 记录，不在用户端暴露内部地址。

#### 3.32.2 重试策略

只重试明确暂时性且操作可安全重放的错误：连接建立失败、限流、短暂 5xx、MySQL Deadlock/Lock Timeout 等；Validation、权限拒绝、库存不足、非法状态和确定性 4xx 不重试。采用指数退避 + Full Jitter、最大 Attempt、总时间预算和全局 Retry Budget，尊重 `Retry-After`，避免多层同时重试形成指数放大。

只读调用通常最多 2–3 次；业务写必须有 Idempotency/Action No，并在重试前查询结果。支付/退款 Unknown 只对账，不再次发起新资金动作。Worker 重试遵循 3.26，永久失败进入 DLQ。记录逻辑 Action 与 Attempt，告警最终失败和重试风暴；熔断打开时不继续消耗 Retry Budget。

#### 3.32.3 熔断策略

按 `dependency + operation + region/provider` 建 Circuit Breaker，状态为 Closed → Open → Half-open。使用滚动窗口最小请求数、错误率/慢调用率触发，Open 保持时间带抖动，Half-open 只放少量 Probe；业务冲突/权限 Deny/正常 Not Found 不计依赖失败。

模型、Embedding、物流、通知和 MCP 可分别熔断，避免一个 Tool 拖垮全部 Agent；支付创建/查询也分 Circuit，不能因查询故障禁止接收回调。Breaker 状态进入 Metrics/Trace 和发布看板，可由管理员临时 Open/Kill，但手工 Close 需审计。状态存本实例时可能不完全同步，因此 Provider 全局限流/舱壁仍需 Redis/网关或集中控制。

#### 3.32.4 降级策略

降级矩阵沿用 3.2.10，并为每条路径定义触发条件、用户提示、功能开关、数据新鲜度、最大持续时间和恢复验证。优先级为：保护资金/权限/数据一致性 → 保持订单/消息事实可写 → 提供只读/最后已知状态 → 转人工/页面入口 → 明确不可用。

示例：Redis Cache 故障回源并限流；Rerank 故障使用 RRF；向量故障仅关键词；模型故障固定提示/人工；物流故障展示最后更新时间；对象存储故障暂停需凭证上传；MySQL 故障停止交易写。任何降级响应带 `degraded=true/reason/observed_at`，不得把缓存冒充实时、草稿冒充提交或 Unknown 冒充失败/成功。长期降级超过阈值升级事故。

#### 3.32.5 MySQL 故障处理

MySQL 是业务事实源。连接失败/Primary 不可写时 API 立即停止订单、库存、支付状态、退款、消息等需要一致事务的写入，返回可重试 Service Unavailable 和 Request ID；不得从 Redis/PostgreSQL 拼出可交易结果。只读是否切 Replica 取决于接口容忍度，支付/订单当前状态默认读 Primary 或具备 Read-after-write 保证。

生产采用托管 Multi-AZ/Primary-Standby、自动或受控 Failover、Binlog/PITR。应用通过稳定 Endpoint 重连，短事务和幂等请求可有限重试；Failover 后检查只读状态、Server UUID/Timeline、复制完整性、业务不变量和 Outbox Gap 再恢复写流量。防止 Split Brain，人工 Failover/恢复必须有 Fencing。连接耗尽、磁盘满、复制延迟和备份失败均提前告警。

#### 3.32.6 PostgreSQL 故障处理

PostgreSQL 故障不阻断商城核心交易，但影响 RAG、长期记忆、摘要和 Agent Checkpoint。新 Agent Run 降级为受限 FAQ/确定性页面或人工；依赖恢复的写 Run 暂停，不能从头重复已完成 Tool。MySQL 商品/政策更新继续写 Outbox，待 PostgreSQL 恢复后幂等补索引。

生产使用 Streaming Replica/托管 HA 与 WAL Archive/PITR。Failover 后应用刷新连接并验证 `vector` 扩展、Schema Revision、当前 Index Generation、Memory/Checkpoint 一致性。知识索引可从权威源重建；用户长期记忆和 Checkpoint 不能一概视为缓存，须纳入备份。WAL Archive Lag、磁盘/Autovacuum、Replica Lag 和 HNSW 重建空间告警。

#### 3.32.7 Redis 故障处理

Redis 失效时：商品/店铺缓存回源 MySQL 并通过请求合并/本地短 TTL/更严格限流防雪崩；Session 缓存回源 MySQL 或要求重新认证，但不能让已撤销 Session 恢复；在线状态/未读可重建；Celery 投递暂停而 Outbox 保留事件。支付/库存正确性不依赖 Redis Lock。

生产通过 Replica/Sentinel/Cluster 或托管 HA 缩短恢复，按用途选择 AOF/RDB；Failover 后处理连接拓扑、Lua Script、Stream Consumer/PEL 和 Celery Broker 重投。因为 Redis 可丢，业务消费者始终幂等。若本地降级限流也不可用，高风险认证/Agent/上传 Fail Closed，普通公开读按固定小并发放行。恢复时缓慢预热缓存，避免全量同时回填。

#### 3.32.8 模型服务故障处理

Model Gateway 按 Provider/Model 维护健康、限流、并发舱壁、错误分类和 Circuit。主模型超时/限流时，只路由到已通过同一数据地域、Tool/Schema、安全与质量门槛的 Fallback；上下文/Tool 能力不兼容时不得强切。已接收部分流式输出发生故障时标记消息 `partial/failed`，不把两模型输出无提示拼接。

全部模型不可用时，持久化固定安全说明、可操作页面和人工入口；不调用未经评估的免费模型，也不把用户敏感内容发到新区域。模型返回格式错误可修复一次，内容安全/权限失败不重试。恢复后不自动重放旧咨询或旧 Approval；只继续有有效 Checkpoint/Deadline 且用户意图仍成立的 Run。

#### 3.32.9 MCP Server 故障处理

MCP Host 对每个 Server/Tool 设置连接池、Deadline、并发舱壁、Breaker 和 Contract Version。Tool Not Found/Schema Mismatch 是发布/契约故障，立即停止该 Version；Timeout/Unavailable 可对幂等只读 Tool 有限重试。有副作用 Tool 返回 Unknown 时查询 Tool Audit/业务资源，不能换 Server 重做。

单个 MCP 失败只禁用依赖它的 Skill，其他 Agent 能力继续。Agent 明确说明无法查询实时结果并提供手动页面/人工，禁止模型猜测。Server 恢复需通过 Initialize/Capability/Smoke，逐步 Half-open；积压任务按业务时效过滤。身份/权限网关不可用或 Policy Version 不一致时所有对应 Tool Fail Closed。

#### 3.32.10 第三方支付故障处理

创建支付失败时保留待支付订单和库存预占到既定过期时间，返回明确“未创建/暂无法发起”，不得生成伪成功。网络超时若无法确认渠道是否受理，以商户订单号/幂等键主动查询；只有验签回调或查询确认后更新成功。回调入口与支付创建依赖隔离，创建 API 熔断不能阻止回调。

渠道故障期间限制重复点击并展示可重试时间/可选已批准渠道；不得自动切换支付方式后重复扣款。持续 Processing/Unknown 进入对账队列和人工运营看板。恢复后按订单号幂等补查，处理“订单已取消但渠道成功”等异常，按照业务规则退款/人工处置。密钥、证书、签名算法切换采用双版本窗口和 Sandbox 演练。

#### 3.32.11 数据库备份

MySQL 采用定期全量物理备份 + 持续 Binlog/PITR，重要配置/用户权限/Alembic Revision 一并版本化；PostgreSQL 采用 Base Backup + 连续 WAL Archive/PITR，另保留必要逻辑导出用于小范围核验，不把 `pg_dump` 当连续归档替代。备份频率按 3.32.14 的 RPO 反推，Backup Lag 超阈值立即告警。

备份在传输/静态加密，使用独立账号/KMS Key，至少一份位于不同故障域并采用 Object Lock/不可变保留防勒索；生产管理员不能随意删除全部备份。每天自动校验清单/Checksum、定期在隔离环境恢复并跑行数/外键替代检查、关键订单金额/状态/Outbox 对账和应用 Smoke。没有成功恢复证据的备份不算有效。

#### 3.32.12 对象存储备份

生产 Bucket 启用 Versioning；退款凭证、知识原文件和用户头像等不可轻易重建对象使用跨可用区/跨账号或跨区域复制，备份 Bucket 采用独立凭证、加密和不可变保留。商品派生缩略图可重建，但原图仍需保护。Bucket Policy、Lifecycle、CORS、Object Lock 配置也进入版本控制/备份。

数据库 Object Metadata 与对象副本必须一致恢复：定期生成 Inventory，校验 Key、Version ID、Size、Hash、Owner 和生命周期，识别缺失/孤儿。用户删除请求通过带 Tombstone 的删除流程传播到副本；法定保留对象单独标记。恢复时先私有、后公开，避免 ACL 默认值把凭证暴露。

#### 3.32.13 恢复演练

至少季度执行一次完整恢复演练，月度执行抽样恢复/自动校验；重大版本、数据库升级或备份方案变化后额外演练。场景覆盖误删、坏迁移、Primary 故障、区域不可用、Redis 全丢、对象版本误覆盖、凭证泄漏和 Agent Checkpoint 中断。演练在隔离账号/网络，禁止覆盖生产。

记录故障发现 → 宣布事故 → 决策恢复点 → 恢复基础设施/Secret/数据 → 应用验证 → 流量切换 → 积压补偿 → 对账的实际时间和数据损失，比较 RPO/RTO。验收包括权限/订单/金额/库存/支付退款、Outbox、对象 ACL、向量版本和 AI 降级。超目标必须形成 Owner/期限明确的改进项，并在下一次演练验证。

#### 3.32.14 RPO 与 RTO

首期目标延续 3.1 的业务主库基线，正式上线前由业务/安全/运维结合成本签字：

| 组件/能力 | 数据性质 | RPO 目标 | RTO 目标 | 恢复策略 |
| :--- | :--- | :--- | :--- | :--- |
| MySQL 业务主库 | 订单、库存、支付、退款、消息、审计 | ≤ 15 分钟 | ≤ 60 分钟 | HA Failover + 全量备份/Binlog PITR + 对账 |
| API/Nginx/Worker | 无状态镜像 | 0（镜像/配置可重建） | ≤ 15 分钟 | 多副本/Registry 重建 |
| PostgreSQL Memory/Checkpoint | AI 原生持久数据 | ≤ 60 分钟 | ≤ 2 小时 | HA + Base/WAL PITR |
| PostgreSQL Knowledge Index | 可从权威源重建 | 权威内容 0；索引允许重建 | ≤ 8 小时 | 旧索引切换/全量重建 |
| Redis | 非权威临时状态/Broker | 不承诺数据 RPO | ≤ 15 分钟 | HA/重建/Outbox 重投/重新认证 |
| 私有对象 | 凭证、知识原文件、头像 | ≤ 60 分钟 | ≤ 4 小时 | Versioning + 跨故障域复制 |
| 可观测平台 | 非业务事实 | ≤ 24 小时 | ≤ 8 小时 | 独立存储/重建，审计不在此处 |

RPO 从最后一个可验证恢复点计算，RTO 到服务通过业务 Smoke 且可安全接流量，不是容器启动时间。若部署能力无法达到目标，必须在上线风险清单中显式降低业务范围或升级基础设施，不能只修改文档数字。

#### 3.32.15 灰度发布与快速回滚

发布顺序：备份/迁移预检 → Expand Migration → Shadow（适用）→ 内部/1% Canary → 5%/25%/50%/100%，每阶段观察预注册时窗和 API/业务/AI/安全护栏。流量按 User/Conversation 稳定 Hash，支付回调等不可随机改变处理语义。Feature Flag 分离代码部署与能力启用，高风险 Agent Tool 默认关闭。

自动回滚触发包括 P0 安全事件、订单/支付/库存不变量、5xx/Error-budget 高燃烧、关键 Agent 安全门禁或明显成本失控。应用镜像/配置/Agent/Prompt/Skill/Model/Index 指针均可独立回滚；数据库优先 Forward-compatible/Forward Fix，不盲目执行破坏性 Down。回滚后检查旧版本能读新 Schema、停止不兼容 Worker、处置在途 Run/Approval、对账副作用，并保留 Release/Decision Audit。

---

### 3.33 性能与容量规划

性能与容量目标是首期可验证基线，不是脱离业务量的永久承诺。上线前必须补充 DAU、峰值在线、订单峰值、消息/Agent 使用率、商品/Chunk 数、平均 Payload、增长率和活动峰值系数，再用 3.30.19 压测校准。规划至少覆盖未来 12 个月容量，并保留正常峰值 30%–50% Headroom；大促另做预案，不把自动扩容当唯一保障。

容量表按月更新 `current、peak、growth、safe_limit、headroom、lead_time、owner、scale_action`。平均值只用于成本，扩容依据峰值/P95/P99、队列 Oldest Age、连接/磁盘和错误预算。所有容量模型保留单位和假设，实际观测偏离时回写模型。

#### 3.33.1 性能目标

首期核心目标：正常峰值下关键 API 满足 3.33.2，系统错误率低于 0.1%（第三方/业务拒绝单独分桶），MySQL/Redis/PostgreSQL 连接和 CPU 保留至少 30% Headroom，关键队列 Oldest Age 在各自 SLO 内，订单/库存/支付不变量始终成立。高负载时优先限流/降级低优先级搜索、推荐和 AI，不牺牲资金与权限正确性。

性能验收需同时满足延迟、吞吐、错误、资源、成本和数据一致性；单纯 QPS 达标但出现库存超卖、重复副作用、队列无限增长或 P99 失控均为失败。目标按 Route/Intent/缓存状态和 Payload 分桶，报告 P50/P95/P99，不以平均响应时间验收。

#### 3.33.2 API 响应时间目标

以下为服务端首期 P95/P99 目标（正常峰值、同区域、请求 Body 在限制内；第三方支付页面和完整 Agent 生成另计）：

| 接口类别 | P95 | P99 | 计时边界 |
| :--- | ---: | ---: | :--- |
| 健康/静态配置 | 50 ms | 100 ms | Nginx/API 接收到完成 |
| 缓存命中商品/店铺详情 | 200 ms | 500 ms | 不含图片下载 |
| 搜索/普通列表 | 300 ms | 800 ms | 标准分页，非任意深翻页 |
| 我的订单等复杂聚合列表 | 400 ms | 1 s | Cursor 分页，限制关联与返回数量 |
| 购物车/结算预览 | 500 ms | 1.2 s | 含价格、库存资格读取，不含支付渠道 |
| 创建订单 | 800 ms | 2 s | 本地事务提交完成，不等待异步通知 |
| 消息发送 | 300 ms | 800 ms | 消息持久化并接受投递 |
| 支付 Webhook ACK | 500 ms | 1 s | 验签、幂等、核心事务完成 |
| Agent Run 创建 | 300 ms | 800 ms | 消息/Run 持久化，不含生成 |

AI 单列 User-visible TTFT：无 Tool 简单问答 P95 目标 ≤3 秒，RAG/只读 Tool P95 ≤8 秒；全量支持意图的加权总体仍以 3.1 的 P95 ≤5 秒为发布目标。完整响应按 Intent 设 15–60 秒区间并显示进度。上述数字经真实模型/网络基准后调整，调整必须有 ADR/SLO Version，不能为“达标”排除慢失败或缩短正确性校验。

#### 3.33.3 并发用户估算

业务输入表至少包含：`DAU、peak_hour_ratio、peak_to_average_factor、requests_per_active_user、online_session_minutes、websocket_adoption、agent_runs_per_user、orders_per_user`。计算示例：

```text
peak_hour_users = DAU × peak_hour_ratio
peak_rps = peak_hour_users × requests_per_user_in_peak_hour / 3600 × peak_factor
in_flight_requests = peak_rps × average_service_time_seconds       # Little's Law
websocket_connections = peak_online_users × connections_per_user
agent_active_runs = agent_arrival_rps × average_active_run_seconds
```

匿名浏览与登录用户分开，爬虫/活动/推送回流另加峰值；创建订单、支付回调和消息有独立 RPS。至少规划 Normal Peak、Promotion 3×/5× 和 Dependency Slowdown 三个情景，做敏感性分析。没有真实数据时不得写“支持百万并发”，先以压测安全上限和上线限流值约束流量。

#### 3.33.4 数据库连接池

连接预算按数据库全局计算：

```text
total_connections = Σ(service_replicas × processes × pool_size)
                  + migration/admin/monitoring reserve
total_connections ≤ 70%–80% × database_max_connections
```

API 的 Pool Size 从每实例 5–10、有限 Overflow 起压测，Worker/Agent 按真实并发单独小池；不能因请求多就让每实例建几十个空闲连接。设置 Pool Acquire Timeout、Recycle/Pre-ping（按驱动验证）、事务时长和泄漏指标。连接利用率持续 >70%、Acquire P95 上升时先查慢 SQL/长事务，再决定增加连接；盲增连接可能让数据库 Context Switch/内存恶化。PostgreSQL 高连接规模可评估 PgBouncer Transaction Pool，但需验证 Prepared Statement/Session Feature 兼容。

#### 3.33.5 Redis 连接池

按用途/实例建立异步连接池，复用而非每请求创建连接；总连接同样按 API/Worker/Agent/MCP 副本预算，并为管理/Failover 保留余量。初始每进程 Pool 可从 10–20 起压测，设置 Connect/Read/Pool Timeout、Health Check 和最大等待，Pub/Sub/WebSocket Listener 使用专用连接，不占普通命令池。

优先 Pipeline/MGET/Lua 减少往返，但限制单 Pipeline/返回大小；Blocking Stream 读取使用独立连接。监控 Active/Idle/Waiter、Command P95、Timeout、Reconnect Storm 和 Server Client Limit。Failover 后采用抖动重连，避免所有副本同时建立连接；增加 Pool 前先检查 Hot Key/慢 Lua/大 Value。

#### 3.33.6 商品缓存命中率

公共且稳定的 Product/Store Detail 目标稳态 Cache Hit ≥90%，类目/列表可按查询基数目标 ≥80%；个性化、高变筛选和冷启动单独统计，不纳入同一分母。Hit 指缓存返回且版本/状态仍有效，Negative Cache/本地缓存/Redis 各自分桶，不能用过期数据提高命中率。

缓存 Key 包含 Version/Locale/必要 Scope，商品变更通过 Outbox 主动失效 + TTL；TTL 加随机抖动，热门 Key 预热/请求合并。看板同时观察命中率、回源 QPS、Stale Serve、Invalidation Lag 和 DB Load。命中率下降先识别 Key 爆炸/版本发布/冷启动，不盲目延长库存/价格 TTL。

#### 3.33.7 热点商品保护

静态图片由 CDN/对象存储承载，商品公共详情用 Nginx/Redis/进程短缓存分层；同 Key Miss 使用 Singleflight/请求合并，活动前预热 Top SKU。列表/详情读取可降级隐藏非关键评价统计，但价格、状态和库存仍从受控实时源/短 TTL 读取。

下单入口按用户/SKU/活动限流、排队和幂等，库存采用 MySQL 原子条件更新/预占，不用缓存锁决定最终数量。对恶意刷新启用 Bot/WAF/Challenge；保护阈值由库存和数据库容量设定。热点 SKU 独立监控但不作公共 Metrics Label；出现负库存、锁等待或 DB Headroom 过低时立即收紧入口，而非继续扩 API。

#### 3.33.8 消息表容量

规划公式：`daily_rows = DAU × conversations_per_user × messages_per_conversation + system/AI messages`；`daily_bytes = daily_rows × (avg_row + index_amplification + attachment_metadata)`。例如平均行与索引合计 2 KiB、每天 100 万消息约为 2 GiB/日，仅作量级示例，必须用实际 `information_schema`/备份压缩率校准。

按 12/24/36 个月计算行数、主表/索引、Backup/PITR、Replica 和归档窗口。列表使用 `(conversation_id, sequence_no/id)` Cursor 索引，不深 Offset；附件不入 MySQL。先按时间归档已关闭老会话到历史表/库，只有真实规模证明必要且唯一键允许时再分区/分表。容量告警在磁盘 60%/70%/80% 分级，并考虑 DDL/备份/故障恢复至少额外 30% 空间。

#### 3.33.9 日志和 Trace 容量

估算：`daily_log = events_per_second × avg_event_bytes × 86400 × replication/compression_factor`；Trace 使用 `requests × sampled_rate × avg_spans × avg_span_bytes`，AI/错误 Tail Sample 另加。按 3.27.29 的热存储/归档期计算总量，再加索引和 Compaction Headroom；不能先全量记录 Prompt 再考虑容量。

通过字段 Allowlist、Level、采样、Span/Event Limit、属性截断和低基数 Label 控制源头。Metrics Cardinality 设置预算和 CI Lint，User/Order/Trace ID 不进 Label。Collector Queue/Dropped Telemetry、Loki/Tempo Ingestion、Object Storage 增长和查询 P95 达 60%/75%/85% 阈值时分别优化/扩容/限制低优先级数据；业务审计独立计算且不可被 Trace 采样。

#### 3.33.10 向量库容量

原始 Float32 Vector 大小约 `chunk_count × dimension × 4 bytes`；如 1536 维单向量约 6 KiB，此外还需 Chunk 文本/Metadata、行/TOAST、HNSW 图和索引构建临时空间。HNSW 实际放大受 `m`、数据分布和 pgvector/PostgreSQL 版本影响，必须用代表性百万级样本实测，规划至少 2× 当前索引空间用于新旧 Generation 并行与重建。

容量输入包含 Document/Chunk 增长、平均 Chunk Bytes、Embedding 模型/维度、多模型并存、索引放大、WAL/Replica/Backup 和保留版本。按 Knowledge/Memory/Checkpoint 分开；旧 Index Generation 在回滚窗后删除。监控表/索引/WAL、Dead Tuple、Cache Hit、HNSW Build Time 和 Recall/P95。降维/halfvec 只有质量评估通过才用，不能为省空间静默截断向量。

#### 3.33.11 Agent 并发限制

并发同时受平台 Run Semaphore、User/Store 限流、Model Provider RPM/TPM、MCP/DB 连接、Worker Capacity 和费用预算约束，最终上限取最小值。按 Agent/Intent 分 Queue：普通问答、只读 Tool、交易 Approval Resume 和后台摘要互不争抢；高价值已确认交易 Resume 保留少量容量，但仍不绕过权限。

使用 Little's Law 估算活跃 Run，并对每 Run 限制模型调用、Tool、子 Agent 和 Wall Time。等待用户确认/人工期间释放计算 Slot；流式连接也不等于活跃模型调用。队列超过 TTFT SLO 时返回排队进度/限流，低优先级推荐降级，不无限堆积。Autoscaling 不能突破 Provider TPM；需用 Token Bucket 和全局并发舱壁消峰。

#### 3.33.12 Token 与成本预算

上下文块比例沿用 3.20.9；本节管理硬预算。每个 Model Profile/Agent/Skill 配置 `max_input/output_tokens、max_model_calls、max_tool_calls、max_delegations、max_cost_micro、daily_user/store/global_budget`。预算按最坏路由先 Reserve，调用后按 Provider Usage 结算/释放；未知 Usage 采用保守估算并标记。

容量模型为：`daily_cost = Σ(intent_volume × avg_tokens_or_calls × unit_price × retry/fallback_factor)`，并报告单成功任务成本而非只报单 Run。50%/75%/90% 预算触发看板/告警/降级，100% 按策略限流非关键 AI 并保留人工/页面；不能为省成本跳过 Citation、Gateway 或 Approval。价格目录版本化，模型升级同时评估缓存 Token、长上下文、RAG/Rerank 和 Multi-Agent 隐性成本。

#### 3.33.13 压力测试方案

在生产近似拓扑按阶段执行：单接口基准 → 混合正常负载 → 目标峰值 → 2×/3× Stress → 突发 Spike → 8–24 小时 Soak → 依赖延迟/故障。流量模型按真实 Route/Think Time、读写比、商品 Zipf 热点、订单/支付回调、WebSocket、Worker 和 Agent 分布生成；数据库预置目标规模数据，缓存分别测试冷/热。

每阶段设停止条件：数据不变量、错误率、P99、DB/Redis/Provider Headroom、Queue Lag、成本或安全告警超限即停。结果包括最大可持续 RPS/连接/Run、拐点、瓶颈、恢复时间和配置，不只是一张吞吐图。外部写使用 Fake/Sandbox，测试唯一 Namespace；每次重大查询/索引/模型/部署变化与大促前重跑，结果进入容量表。

#### 3.33.14 扩容触发条件

初始触发值需经基准校准，建议：API CPU/Memory 或 In-flight 持续 >70%、P95 Queue Time/SLO 连续 10 分钟恶化且下游有 Headroom 时扩副本；Worker Oldest Age 超目标 50% 或到达率持续接近 80% 处理能力时扩容；DB CPU/IO/连接 >70% 先优化慢 SQL/事务，再扩规格/Replica；磁盘使用 70% 开始扩容，80% 前完成；向量索引构建需预留两代空间。

扩容动作必须考虑 Lead Time：API/Worker 分钟级，数据库/存储小时至天级，架构拆分周级。因此 60% 发 Capacity Ticket、70% 执行计划、80% 进入风险状态，不等到 100%。缩容需低于安全阈值多个窗口且保留 Headroom。任何扩容后复测连接预算、费用和故障域；若单体热点来自锁/单 Key/Provider 配额，横向加容器无效，应先消除瓶颈。

---

### 3.34 开发阶段与验收标准

开发采用可运行的纵向切片，每个阶段都必须交付迁移、API/OpenAPI、领域测试、权限、日志/指标、Docker 启动和最小前端联调，而不是最后再统一补安全与测试。阶段编号表示主要能力成熟顺序，不意味着可观测性、CI、备份和安全到第十一/十二阶段才开始；这些基础能力从第一阶段建立，第十一阶段完成系统化验收。

进入下一阶段的通用 Definition of Done：代码评审通过；依赖/镜像锁定；Migration 可从空库和上一版本执行；Unit/Integration/Contract/受影响 E2E 通过；无未处理 Critical/High 安全问题；OpenAPI/事件/配置文档更新；关键 Metric/Alert/Runbook 存在；Demo 在干净 Compose 环境可复现。缺陷可延期必须有风险、Owner、截止版本和不影响硬门禁的证据。

#### 3.34.1 第一阶段：后端基础骨架

交付 Repository 目录、`environment.yml + pyproject.toml + requirements/*.txt`、FastAPI App Factory、Pydantic Settings、结构化错误/Request ID、SQLAlchemy 双库连接、Redis/ObjectStorage Client、Alembic 双迁移链、Celery/Outbox Skeleton、OpenTelemetry、Dockerfile/Compose/Nginx 和 CI。建立 Domain/Application/Repository/Integration 依赖规则与示例 Health API。

验收：一条命令启动干净环境；MySQL/PostgreSQL Migration 成功且 `vector` 扩展可用；API/Worker/Scheduler 健康检查和 Graceful Shutdown 正常；Unit/Integration Test 可并行；日志无 Secret；镜像非 Root、扫描通过。此阶段不实现伪业务接口，不允许 Router 直连 ORM 作为“临时方案”。

#### 3.34.2 第二阶段：用户与权限

实现 Registration Config、注册、密码/验证码登录、Refresh Rotation/Reuse Detection、退出/强制下线、找回/重置/修改密码、个人信息、地址、账号安全会话与注销申请、RBAC、管理员账号/MFA/近期认证/操作日志、管理员审批聚合与 Executor 骨架、Cookie/CSRF/CORS/限流和字段加密。数据层交付 `users、user_credentials、auth_sessions、verification_codes、password_reset_records、user_addresses、user_agreement_acceptances、auth_attempts` 及法务 Content Version 的迁移/Repository。Vue3 用户端完成 2.12.2、2.12.3.1～2.12.3.3 的 AuthLayout、密码登录、验证码登录、注册、找回/重置密码、我的资料、密码、会话、地址和账号注销页面联调；独立管理端完成密码 + MFA 登录、Admin Audience Session、后台 Shell、权限菜单、Scope 切换、用户治理、角色权限、安全会话和审批中心页面。

验收：2.12.3 的 Text UI、响应式、键盘和错误状态通过 3.30.24；Registration Config、精确协议版本、验证码 Purpose/Target、严格 Registration Schema、User Audience Session 与安全 Redirect 的前后端契约一致。注册核心资源全成或全败；并发同 Username/Target 仅一人成功；相同幂等 Key 不重复创建用户、协议、角色、专属客服会话或 Session，Commit 成功响应丢失按固定 Bootstrap Session Replacement 策略恢复。认证/API/数据库并发测试通过；密码哈希/验证码/Token/MFA Secret 不入日志、Trace、截图或浏览器持久存储；账号枚举、Refresh/恢复码重放被阻断并撤销相应会话；用户 Token 与 Admin Token Audience 不能混用；同一自然人持有多种身份时仍使用独立入口、Cookie Path、CSRF 上下文与 Session，交换 `aud/client_type` 的负向测试全部拒绝。生产 Admin Audience 无购物车、下单或支付能力，业务演练只允许隔离环境的独立 Sandbox 用户且生产镜像/配置不含 Sandbox 凭据。IDOR、横向/纵向越权、安全错误信息测试为零泄漏；权限自提升、删除最后安全管理员、近期认证绕过、发起人自批、重复审批席位和跨店 Scope 测试全部拒绝且管理员高危操作审计完整。角色撤销/过期后重新授权生成新 Grant，历史事件不可改写且任意时刻仅有一条 Active Grant；用户业务冻结与登录失败锁定严格分离，到期或提前解冻不恢复已撤销 Session；敏感字段临时 Grant 完成绑定、一次性消费、撤销、过期与全程脱敏测试。Redis 全丢最多导致重新认证或回源 MySQL，不得恢复已撤销 Session/Grant。

#### 3.34.3 第三阶段：店铺、商品与库存

实现店铺/认证/平台经营分类/店铺商品分组/公告/推荐位/服务政策/配送运费模板、平台类目/品牌、Product/SKU/Image/Attribute/FAQ/履约资料、上下架状态、收藏/关注和库存/预占/流水。完成首页聚合、独立商品搜索、商品全部评价、商品/店铺收藏、用户店铺页和商品详情；管理端完成店铺审核/运营、商品分区编辑、SKU、多图片、分类品牌、库存调整、发布审核与批量导入预检页面。对象存储支持商品/Logo/导入文件上传、缩略图和权限。

验收：首页 Read Model 无详情 N+1，Banner/公告/推荐区可局部降级，Cursor/返回锚点可恢复；搜索使用独立 `/search` 和 URL 筛选，店铺查询只接受 `q/group_id/sort/cursor`。商品首次进入停留顶部，Scroll Spy、SKU 图片集/公共图库回退、桌面 Sticky 和移动底栏均通过布局测试。店铺 Scope 隔离、经营分类与店铺分组不混用、商品状态机、SKU 唯一、图片归属与缓存失效正确；店铺认证支持要求补件、材料版本再次提交和不可变事件历史；服务政策支持草稿、发布、替换、撤回/失效，公开读取只返回当前有效版本，历史订单快照不追随政策更新，发布事件可使缓存失效并触发 RAG 重建。商品详情与 FAQ 采用不可变内容版本，发布时重跑 Sanitization 和内容安全扫描；公开 DTO 只返回 `structured_v1/safe_html_v1` 判别联合与安全纯文本，不返回原始 Source。脚本、事件属性、危险 URL、iframe、表单和可执行 SVG 均被阻断，RAG 只摄取已发布 `safe_text`。`file_upload_sessions → file_objects → 扫描 → Active 业务绑定` 全链路通过，过期 Multipart 可清理且私有原图不可被公开读取；运费模板按区域/件数/重量产生确定结果，不可配送有稳定原因码；库存条件更新/锁/日志通过并发测试，无负库存/超卖；下架商品不可新购且历史订单快照不受影响。管理端商品发布前缺项、If-Match 冲突、跨店关联、库存无原因调整和恶意导入文件均被正确阻断；批处理逐项结果可追踪。搜索索引尚未建设时可用受控 MySQL 查询，但接口 Contract 保持可替换。

#### 3.34.4 第四阶段：购物车、结算与订单

实现 Cart/Items、单商品与购物车 Checkout Session/Snapshot、地址/价格/库存/配送实时校验、Trade Order/Store Order/Items/Address Snapshot、库存预占、订单状态机、取消/超时、确认收货、订单搜索/隐藏短时恢复/再次购买、我的订单和单订单页面。创建订单使用 Idempotency-Key，业务事务写 Outbox。

验收：单品结算和购物车多店拆单端到端通过；Cart 永久复用且所有 Cart Item API 只接受 `ci_` 公开 ID；买家备注 API 严格 200 Unicode 字符、空白/内容安全一致；价格/库存/地址/配送变化得到确定提示。结算联系客服创建的 `checkout_store_group` 只含本店最小摘要，跨店、旧 Version、过期/已消费 Checkout 全部拒绝。订单创建与支付创建是两个独立幂等步骤，订单成功后 Payment 失败仍能在详情继续支付，重复/结果未知不产生第二组订单。订单级正负改价能确定性分摊到订单项且汇总守恒；`view` 八种订单复合筛选与前端 Tab 契约测试全部通过；订单、预占和 Outbox 原子一致；超时取消与支付竞争无错误释放。列表、详情、支付和售后入口只消费同一 `available_actions`，缺少 Action 时隐藏，禁用时显示稳定原因；订单隐藏撤销以 `undo_until/restore_url/ETag` 调用服务端恢复，过期、412、重复和结果未知不会只在本地插回卡片。满足订单/库存并发、状态机和 `DOC-P2-ACTION-*` Suite。

#### 3.34.5 第五阶段：支付、物流与售后

实现 Payment 创建/回调/主动查询/对账，Shipment/Items/Tracks，评价/追评，以及退款资格、申请/联系方式快照/凭证、审核、退货物流、渠道退款和平台申诉复核。支付/物流 Provider 通过 Adapter，开发使用 Fake/Sandbox；用户端完成支付、物流、评价、退款和申诉交互，管理端完成订单/支付查询、受控改价、创建包裹、物流纠错、退款/申诉审核和评价治理页面。

验收：收银台与支付结果页只依据 Payment/Trade Order 权威状态；渠道返回参数不判成功，`created/pending`（包括结果未知）只进入确认中，同一 Trade Order 的活动/结果未知 Payment 阻断新尝试。回调验签、金额币种、重复/乱序/Unknown 和 Commit 后 ACK 丢失测试通过；支付/订单状态一致且无重复扣款。评价资格/创建/编辑/追评、我的评价，售后列表/详情/退货物流/申诉，以及多包裹列表/单包裹详情均完成联调。售后资格按订单项数量和金额计算，活动申请只占用对应订单项的申请量；同订单其他商品及剩余数量可继续申请，并发争抢同一剩余量只有一个成功。拒绝后重新申请必须创建新申请，原记录及事件不可变；重新申请与平台申诉是两个独立 `available_actions`。退款上限严格使用含改价分摊的订单项实付，正负改价、部分数量、多次退款均不超退/少退；Action Approval、退款幂等和超额保护通过。商品、Checkout、订单/物流和 AI 全部复用 `ServiceEstimate`，按包裹展示范围、来源、更新时间与“仅供参考”；过期/不可用显示未知，单点、富文本或模型常识不得生成日期承诺。仅订单本人鉴权后的单包裹详情可返回完整运单号，订单概览、店铺员工/管理员、客服、Agent、消息、日志和 Trace 均只有掩码。包裹采用“创建即确认发货”，不存在可编辑草稿；Shipment Item 创建后不可变，揽收前受控作废时以版本锁、渠道核验和数量守恒原子恢复未履约数量，揽收后禁止作废。管理端不能直接标记支付成功、超范围发货或让同一审核员绕过职责分离；退款领取/决定并发只有一个有效结果，评价仅支持屏蔽/恢复且治理记录不可变。财务/库存/订单/退款对账差异为零或有可追踪处置；`DOC-P2-ESTIMATE-*` 全部通过。

#### 3.34.6 第六阶段：消息与人工客服

实现固定专属客服会话、店铺客服唯一会话、消息/卡片/已读/未读、WebSocket/SSE、重连补拉、内容审核、人工工单/分配/接管/结束、内部备注和会话隐藏审计。用户端“消息”左栏将“专属客服”固定置顶，商品/订单页客服进入同一店铺会话；管理端完成人工客服队列、领取、转派、会话、受控业务卡片、内部备注和结束服务工作台。

验收：消息先持久化后推送，Sequence/Client Message ID 去重，断线可补拉，多实例投递正确；跨会话/跨店读取为零；人工接管后 AI 自动发送路径处于 Disabled。同一用户从商品页、店铺页、订单页、结算页和可选桌面浮层进入同一店铺时得到相同 `conversation_id`，浮层只是同一会话容器，消息、草稿、Read Cursor、AI/人工状态完全同步。桌面店铺会话有可见且键盘可用的 `…` 菜单，右键不是唯一入口；普通消息页面不显示无语义最小化/关闭，手机使用列表→单会话路由。用户查看历史时新消息不抢滚动，失败发送复用同一 `client_message_id`，重连先补拉再订阅，屏幕阅读器只播报完整消息/关键状态。用户 Read Cursor 仅在页面可见、消息渲染且气泡至少 50% 连续可见 500ms 后单调推进；后台标签、加载失败或查看历史不误清未读，多标签页以服务端绝对未读数和 `cursor_version` 收敛。工单原子领取、平台/店铺队列隔离、敏感业务卡片 Purpose 和内部备注不可进入用户消息/Agent Context 测试通过。交接包只有授权、脱敏的 `handoff_summary`、必要用户可见消息引用和策略版本；结束后的 `resolution_summary` 与内部备注使用不同 DTO 投影，用户未发新消息前模型调用数为零。`active → waiting_user → active` 显式命令、SLA 暂停/恢复、用户新消息自动恢复、并发幂等和 WebSocket 状态帧通过；用户消息 API 仅接受 User Audience，客服消息/队列仅走 `/support/*` 并只接受 Admin Audience。在线送达、未读对账、慢消费者、内容 Block、人工 Queue E2E 和 `DOC-P2-HANDOFF-*` 通过。

#### 3.34.7 第七阶段：店铺客服 Agent

先实现单 Agent、只读、当前店铺范围的纵向切片：商品/店铺政策问答、SKU 对比、库存查询、订单卡片解释、店内推荐和转店铺人工。可使用确定性 MySQL FAQ/Service Adapter 和最小版本化 Tool Contract；不等待完整 Multi-Agent 平台，也不开放写订单/退款能力。

同时建立 Agent Definition/Version、Run/Checkpoint、Context Builder、Model Gateway、流式消息、安全网关和 Audit 基础。验收：Store Scope 由可信会话注入；订单必要读取同时满足“当前用户 ∩ 会话店铺 ∩ 订单归属/店铺 ∩ 有效 Active Context 或显式授权”，订单卡片只是可选展示证据，不是唯一授权来源；Context 切换原子停用旧版本，旧 Run、旧卡片和伪造 ID 均不能继续调用 Tool。跨店商品私有信息/订单/用户其他数据泄漏为零；商品/FAQ/RAG 内容按不可信数据处理，不能改变系统规则；实时事实走只读 Tool；发货时间只消费 `ServiceEstimate` 并在无可靠范围时拒绝承诺；模型故障可转人工；店铺客服 Golden/Injection/权限集通过并达到 3.28 门槛。

#### 3.34.8 第八阶段：专属客服 Agent

实现平台规则、全平台商品搜索/推荐、本人订单/物流、售后资格预检、退款草稿、经结构化二次确认提交和转平台人工。长期偏好首期可先启用显式用户管理的低敏偏好，所有候选/Consent/删除均可查看和审计。

验收：Current User Scope、订单归属、Consent/Approval、参数 Hash、Checkpoint Resume 和 Tool Audit 全链正确；无确认写副作用为零，重复 Resume 只执行一次，Timeout Unknown 可对账；专属客服不冒充商家承诺，物流预计只消费逐包裹 `ServiceEstimate`，不会用相对日期、轨迹或模型常识补日期。我的消息页面的置顶“专属客服”可完成查询—确认—结果卡片完整旅程。

#### 3.34.9 第九阶段：RAG、MCP 与 Skill

把前两阶段的受控 Adapter 正式平台化：建设文档清洗/Chunk/Embedding/Hybrid Retrieval/Rerank/Citation、知识版本与重建；实现 MCP Host/Client 和 `catalog/order/logistics/after-sale/support/memory` Server；建立 Skill Registry、Schema、Binding、权限、预算和测试。管理端完成知识文档/索引任务、Agent/Skill/Tool/Policy 版本 Diff、评估准入、审批、发布、回滚和 Kill Switch 页面。旧 Adapter 接口与新 MCP Contract 一致，避免推倒重写。

验收：索引影子构建/原子切换/撤回删除、ACL/Store Filter、Recall/Precision/Citation 达标；管理端以 MySQL `admin_batch_jobs` 为命令与展示权威，PostgreSQL `knowledge.indexing_jobs` 为执行权威，父子稳定关联、幂等创建、单调进度映射、取消传播、失败责任和跨库 Reconciler 全部通过。MCP Contract、身份注入、参数重建、结果脱敏、Timeout/Unknown 通过；Skill 版本不可变、绑定固定且 Hard Deny 有效。RAG/MCP/Skill 任一依赖降级不导致越权或虚构实时事实。

#### 3.34.10 第十阶段：Multi-Agent

仅对单 Agent 基线确有不足的复杂只读任务引入 Supervisor/Router 与 Catalog、Order、Logistics、After-Sales、Recommendation、Policy Specialist。实现 Delegation Packet、DAG、最大深度/数量、独立 Scope/Tool、只读并行、Reducer 和预算继承；写操作继续由顶层受确认 Transaction Skill 串行执行。

验收：递归/重复委派被阻断，子 Agent 不能扩大父权限，Context 泄漏与重复副作用为零；委派 Trace 可重建关键路径。候选 Multi-Agent 在同一 Golden Set 上相对单 Agent 任务质量有预注册的显著收益，且延迟/成本在批准预算内；无收益的 Intent 保持单 Agent。

#### 3.34.11 第十一阶段：可观测性与评估

对前期最小埋点进行系统化收口：完成 OTel Collector、Prometheus/Grafana/Loki/Tempo、AI Run/Model/MCP/Skill/RAG Trace、Token/成本、业务 Funnel、P0–P3 Alert/Runbook。建立 Evaluation Runner、Golden/Release/Security Holdout、人工标注、自动评估校准、发布报告和线上负反馈回流。管理端完成仪表盘、脱敏 Run/Trace、评估报告、审计、批处理任务和死信预览/受控重放页面；同时交付首页 Banner、平台公告、帮助中心和页脚内容的版本化运营页面。

验收：一次订单/Agent/Tool 可由 Request/Trace/Event/Run/Action Ref 追溯但不暴露敏感正文；Dashboard/告警演练能定位预置故障；Telemetry 故障不阻塞普通业务且关键审计 Fail Closed。所有生产 Agent/Skill/Model/Prompt/Tool 有当前版本评估报告，硬安全指标通过，质量与成本趋势可比较。

#### 3.34.12 第十二阶段：部署与性能优化

完成生产级镜像签名/SBOM/扫描、Secret、TLS/域名、独立数据服务、迁移 Job、Canary/Rollback、备份/PITR、对象复制、环境隔离和 On-call。依据真实目标规模优化 SQL/索引/连接池/缓存/Worker/Agent 并发，执行 Load/Stress/Spike/Soak/Chaos 与恢复演练。

验收：3.33 SLO 与 Headroom 达标，连接/磁盘/队列/成本有扩容预案；RPO/RTO 演练达到 3.32.14；灰度自动/人工回滚完成且无不一致；生产 Compose/编排无默认密码、公开数据库端口、源码挂载或 `latest`。完成最终 Go/No-Go Review 后才开放真实支付和 AI 写 Skill。

#### 3.34.13 功能验收标准

按角色和页面建立可追踪矩阵 `Requirement → Vue Route → API Operation → Permission/Data Scope → Domain Rule → Audit → Test → Evidence`。用户端以 2.12.2 完整路由表为准，必须通过：首页、登录/注册/验证码登录/找回与重置密码、独立搜索、店铺、商品详情/全部评价、购物车、结算、收银台/支付结果、消息列表/单会话、我的/资料/安全/注销、地址、商品/店铺收藏、订单、物流两级页、评价创建/编辑/追评、售后列表/申请/详情/退货物流/申诉及系统状态页。管理员端必须覆盖独立密码 + MFA 登录、权限菜单/Scope、用户/RBAC、管理员审批中心、店铺审核/运营、商品/SKU/分类品牌/库存、订单/支付/发货、退款/申诉/评价、客服工作台、Agent/Skill/Tool/Policy、知识/索引/评估报告/Run、平台内容、批处理、审计和死信。任一首版路由缺少 Component、Operation、状态、Guard 或测试，任一管理按钮缺少 Permission、Scope、命令接口或审计证据，均不通过验收。

每个流程验证 Happy Path、Loading、Empty、403/404、412、428、429、非法状态、重复提交、依赖故障、结果未知、移动/桌面 UI 和可访问性基础；页面按钮与 API `available_actions` 一致，所有首版管理页面符合 2.13.17 的 Text UI。验收数据可重复初始化，测试支付/短信/邮件不触达真实用户。3.34.19 的追踪矩阵及机器可读源不得存在页面按钮、API、Permission、状态机、测试或 Owner 孤儿项；所有 P0/P1/P2 文档缺陷清零，3.30.23 的 P2 契约套件通过，不以规避方案代替既定展示、安全或身份契约。

P0-02～P0-06 设独立 Release Gate：物流字段按 Owner Detail、Owner Summary、Admin/Support、Agent/Message 分 Schema 验证；未读以可见性触发和服务端绝对值验证；售后以订单项部分数量和并发占用验证；店铺客服以唯一会话、Active Context Version 和跨店负向用例验证；富文本以浏览器 XSS 与 Agent 间接 Prompt Injection 双重用例验证。任一字段泄漏、误清未读、跨项阻塞、旧上下文越权或可执行富文本均为 P0，不得上线。

P1-01～P1-09 设用户体验 Release Gate：完整路由无孤儿项；首页/搜索/商品布局和返回恢复正确；消息桌面/手机、草稿、滚动、失败/重连和无障碍播报正确；订单与 Payment 两阶段无重复副作用；Checkout Store Group 上下文无跨店/过期泄漏；snake_case/公开 ID/200 字备注契约一致；响应式、410/412/429/Session 恢复和 WCAG 基础门禁通过。缺失恢复入口、固定栏遮挡焦点、键盘不可达或支付结果误判均不得上线。

#### 3.34.14 数据库验收标准

MySQL/PostgreSQL Migration 能从空库安装、从上一生产版本升级并在目标数据量内完成；ORM/Schema/Index 一致，无手工漂移。主键/公开 ID、金额 Decimal、UTC 时间、状态/版本、唯一约束、逻辑删除、敏感加密和审计符合 3.6/3.7；核心 SQL 有 Query Plan/索引证据，无已知 N+1/长事务。

注册核心事务保证 User、Password/Phone/Email Credential、Required Agreement Acceptance、默认 User Role、Exclusive Conversation、Verification Consume、User Session、Idempotency Record 与 Outbox 全成或全败；Username/规范化 Target 并发唯一，验证码只能按 Verification ID + Purpose + Target 原子消费。法务 Content Version 与 Acceptance 引用不可变，`auth_attempts` 不含认证秘密且按期限分区/清理。订单—库存—支付—退款/Outbox 不变量和并发测试全部通过；一个 Checkout 只能创建一组 Trade Order，订单/Payment 幂等 Scope 分离。退款活动占用由锁定订单项后汇总活动数量/金额保证，不使用订单级“存在售后”唯一约束；拒绝后新申请和申诉各自保留不可变资源与事件。每个店铺会话/上下文类型最多一条 Active Context，`context_version` 单调增加；`checkout_store_group` 绑定 Checkout Version/Expiry，NULL 安全唯一约束有效，旧版本即时 Fail Closed。商品详情/FAQ 原始 Source、净化结果和发布指针构成可追溯不可变版本链。`buyer_remark` 数据库 500 字符冗余容量不扩大 API 200 Unicode 字符边界。Active Role Grant 唯一、用户冻结投影/事件、敏感访问 Grant 一次性消费、认证材料版本链、政策有效版本、包裹数量守恒以及索引父子任务映射约束均有真实数据库证据。跨库同步可重试/对账，向量数据可重建且 Memory/Checkpoint 有备份。备份 Checksum 与隔离恢复成功，RPO/RTO 达标；应用账号最小权限、无 Root/Superuser。生产容量有 30% 以上 Headroom，大表变更有 Expand/Contract 和 Forward Fix。

#### 3.34.15 API 验收标准

全部公开接口位于 `/api/v1`，OpenAPI 通过 Lint/Breaking Change，生成的 TypeScript Client 可编译并完成前端契约测试。所有资源 ID 满足 3.6.2/`docs/id_registry.yaml` 的前缀、资源类型和容量约束：普通公开资源 ID 按注册表使用 `VARCHAR(40)`，已登记的 `trd_/ord_/pay_/ref_/tkt_` 核心业务号使用 `VARCHAR(32)`，不得用统一 40 字符的断言覆盖该例外。Cart Item、File、Upload、Conversation 等路径均无内部自增主键泄漏。请求/响应、时间、金额、分页、Cursor、Problem Details、错误码、Request ID、ETag/Version、Idempotency-Key 和上传遵循 3.11；无 ORM/内部主键/敏感字段泄漏。

认证 Contract 必须覆盖 `RegistrationConfig_Get、LegalDocument_Get、VerificationCode_Create、UserRegistration_Create、UserAuth_Login、AuthToken_Refresh、AuthSession_List/Revoke`：密码/验证码 Login 为判别联合，Registration 禁止 Role/Permission/Store 字段，协议版本精确匹配且法务接口不会静默替换版本，验证码绑定 Purpose/Target，认证响应 `no-store`，Refresh 仅用安全 Cookie，Open Redirect 与幂等结果未知有确定行为。Admin/User Audience 隔离、MFA/近期认证、RBAC、Resource Scope、职责分离、管理员审批、限流、CSRF/CORS、Webhook 签名和重放测试通过；同一业务对象的用户与管理操作使用独立 Operation ID、专用路径和响应投影，互换 Audience 的负向测试全部拒绝。所有 JSON/Query 为 snake_case，Vue camelCase 仅限 Route Placeholder；公开 Schema 不出现内部自增 ID。P0 Contract 额外验证物流、Read Cursor、退款占用、Context 和安全富文本投影。P1 Contract 验证首页聚合、店铺 Query、200 字买家备注、完整页面读取/命令、订单/Payment 双幂等及错误分支，以及 `checkout_store_group` 的 Store/Version/Expiry 最小投影。成功/失败/重复/并发/超时有明确状态码与相同幂等结果。管理 API 必须通过用户冻结、角色 Grant、敏感字段 Grant、认证补件、政策版本发布、商品发布、库存调整、包裹创建/纠错/作废、退款决定、评价屏蔽/恢复、客服等待/恢复、内容/AI/Tool Version 发布、索引父子 Job、管理员审批和死信重放 Contract Test，且不存在通用状态/金额写接口。P95/P99 达 3.33.2，错误率/SLO 达标；SSE Resume、WebSocket 重连和 Graceful Shutdown 通过。弃用接口有 Sunset/兼容窗口，生产 Debug Docs 按策略受控。

#### 3.34.16 AI 安全验收标准

3.28 的权限越权、Prompt Injection、记忆隔离、Tool 参数、Consent/Approval 和 Hard Deny Suite 全部执行。跨用户/跨店/跨环境泄漏、Unauthorized Tool Execution、无有效确认写、重复资金/业务副作用、敏感记忆写入、禁止 Tool 成功调用均必须为 `0`；越权阻断率和应确认执行覆盖率必须为 `100%`。

模型/Prompt 不能扩大 Gateway/Domain 权限；直接/间接/多语言/编码/商品详情/FAQ/文档/Tool Result 注入均无副作用。完整运单号不得出现在 Agent Tool Schema、Prompt、Checkpoint、Memory、消息、日志、Trace 或 Eval；店铺订单读取必须通过 Active Context Version 和 Store/User/Order 交集校验，发送卡片不能替代授权。MCP 服务内网认证、参数重建/脱敏/审计、Kill Switch 和管理员干预演练通过。日志/Trace/Eval 无 Secret/隐藏推理/不必要原文。任何安全硬门禁失败均为 No-Go，不接受平均质量补偿或 Conditional Pass。

#### 3.34.17 Agent 质量验收标准

每个 Agent/Intent 在固定 Release Holdout 上报告任务完成、事实一致、Citation、Tool 选择/参数、拒答/转接、Memory、Multi-Agent、TTFT/P95 和单成功任务成本，并提供 Case-level Diff/置信区间。高影响订单/金额/政策 Claim 必须由 Tool/有效 Citation 支持；无可靠证据时正确拒答。

准入沿用 3.28.34–3.28.37：相对当前基线关键指标绝对下降默认不超过 1 个百分点且置信区间支持非劣，P95/成本默认不恶化超过 15%；具体 Intent 可设置更严目标。Golden Dataset 之外还需 Canary/线上观察，自助解决不能仅以“未转人工”计算。评估样本不足则 `insufficient_evidence`，不得视为通过。

#### 3.34.18 上线准入清单

Go/No-Go Meeting 逐项确认并保存签字证据：

- Release：Commit/Image Digest、SBOM/签名/扫描、依赖 License、Migration/Config/Agent/Skill/Model/Index Version 已冻结；
- Quality：Unit/Integration/Contract/Concurrency/Security/E2E/Full Eval/Performance 与 `DOC-P2-*` 全通过，无未批准 P0/P1/P2；
- Data：生产备份成功、恢复演练有效、PITR/复制/磁盘、数据初始化和对账已验证；
- Security：Secret/KMS/TLS/用户与管理域名/WAF/限流/Admin Audience/RBAC/Store Scope/MFA/近期认证/职责分离/审计/Kill Switch 就绪，默认账号与 Sandbox Key 已移除；
- Operations：Dashboard、P0–P3 Alert、On-call/升级联系人、Runbook、Status/客服话术、容量和第三方状态监控就绪；
- Deployment：Expand Migration、Canary、Feature Flag、回滚镜像/配置、Forward Fix、旧 Worker 兼容和在途任务方案演练；
- Business：支付/退款/物流渠道生产审批、服务政策/隐私/用户授权文本、管理后台菜单与角色矩阵、平台内容初始版本、人工客服排班和 Owner 确认；
- Post-release：观察窗口、成功/停止阈值、对账时间、回滚决策人和复盘日期明确。

上线后先执行无副作用 Synthetic Smoke，再逐步开放流量和 Agent 写 Skill。观察窗口内不得同时升级多个难以归因的 Model/Prompt/Skill/业务版本。任何 P0、安全硬门禁、资金/库存不变量或无法解释的数据差异立即停止扩量；只有负责人依据预先标准确认后才继续。

#### 3.34.19 页面—API—权限—状态机—测试追踪矩阵

本矩阵是首版开发基线，不是验收时再补的附件。`docs/traceability.yaml` 中的逐路由 `requirement_id` 稳定不复用；OpenAPI `operationId`、Vue Route Meta、Permission Registry、领域命令和测试用例必须引用同一 ID。实现后由该机器可读源生成本表并检查漂移；本文档是代码创建前的权威输入。

下表为便于阅读可把同一资源的紧密相关按钮写在一行，第一列代码在合并多条路由时是“追踪组标签”，不覆盖 `docs/traceability.yaml` 中逐路由的规范 Requirement ID。斜杠或逗号分隔的每个 Operation 都是独立 `operationId`，不得实现成一个接受任意 `action/status` 的万能接口。机器可读矩阵必须按按钮展开为独立子需求 ID（例如 `ADM-POLICY-01-LIST/CREATE/UPDATE/PUBLISH/WITHDRAW`），分别保存 Method/Path、Audience、Permission/Ownership、Scope、前置状态、ETag/幂等、Audit Event 和 Case ID；任一子项缺字段即 CI 失败。页面纯导航、关闭弹窗等无服务端副作用动作可标注 `ui_only`，除此之外不得用“同页面已覆盖”代替操作级追踪。

##### 3.34.19.1 用户端核心追踪矩阵

| Requirement ID / 页面操作 | Vue Route | OpenAPI Operation / 路径 | 授权与领域规则 | Audit / Test / Evidence |
| :--- | :--- | :--- | :--- | :--- |
| `USR-AUTH-01` 密码登录 | `/login` | `UserAuth_Login` / `POST /auth/login` | Anonymous；Password 判别分支、凭证锁定、Dummy Hash、限流、安全 Redirect、User Audience 新 Session | Security+E2E `AUTH-LOGIN-*` |
| `USR-AUTH-02` 用户注册/查阅协议 | `/register`、`/legal/:documentType` | `RegistrationConfig_Get`、`LegalDocument_Get`、`VerificationCode_Create`、`UserRegistration_Create` / `GET /auth/registration-config`、`GET /content/legal-documents/{document_type}`、`POST /auth/verification-codes`、`POST /auth/registrations` | Anonymous；配置/协议精确版本、Purpose/Target、严格 Schema、幂等、普通 User 固定角色、核心事务全成或全败 | Contract+Concurrency+E2E `AUTH-REGISTER-*` |
| `USR-AUTH-03` 验证码登录 | `/login/code` | `VerificationCode_Create`、`UserAuth_Login` / `POST /auth/verification-codes`、`POST /auth/login` | Anonymous；Login Purpose、已绑定 Active Credential、原子消费、不得隐式注册、User Audience 新 Session | Security+E2E `AUTH-CODE-LOGIN-*` |
| `USR-AUTH-04` 找回与重置密码 | `/forgot-password`、`/reset-password` | `VerificationCode_Create`、`PasswordResetTicket_Create`、`PasswordReset_Execute` | Anonymous/受限 Ticket；Reset Purpose、限流、模糊响应、一次性消费、撤销旧 Session | Security+E2E `AUTH-RECOVERY-*` |
| `USR-HOME-01` 首页聚合 | `/` | `Homepage_Get` / `GET /homepage` | Public/Current User；Consent-aware 通用/个性推荐；局部降级 | Query Count+Cursor+UI `HOME-*` |
| `USR-SEARCH-01` 商品搜索 | `/search` | `Product_Search`、`SearchSuggestion_List` | Public；URL 筛选、opaque Cursor、限流 | Contract+Restore `SEARCH-*` |
| `USR-PROFILE-01` 修改资料/密码 | `/me/profile` | `UserProfile_Update`、`UserPassword_Change` | Current User；ETag/近期认证；撤销旧 Session | Audit `user.security.*`；`PROFILE-*` |
| `USR-SETTINGS-01` 安全会话/注销 | `/me/settings/*` | `UserSecurity_Get`、`AuthSession_List/Revoke`、`AccountClosureRequest_Create` | Current User；近期认证、阻断事项、冷静期 | Security+E2E `SETTINGS-*` |
| `USR-ADDR-01` 管理地址 | `/me/addresses` | `UserAddress_Create/Update/Delete/SetDefault` | Current User；唯一默认；不改历史快照 | Domain+E2E `ADDR-*` |
| `USR-STORE-01` 店铺页/政策 | `/stores/:storeId` | `Store_Get`、`StorePolicy_ListPublic` | Public/历史关系投影；只读当前 Published | Contract `STORE-PUBLIC-*` |
| `USR-FAVORITE-01` 商品/店铺收藏 | `/me/favorites/*` | `FavoriteProduct_List/Put/Delete`、`FollowedStore_List/Put/Delete` | Current User；失效资源受限摘要；幂等 | Contract+E2E `FAVORITE-*` |
| `USR-PRODUCT-01` 商品详情/SKU/FAQ | `/products/:productId` | `Product_Get`、`ProductSku_List` | Public；只返回 Published 安全内容版本，不返回 Source/真实仓库/预占 | Contract+XSS+Indirect Injection `PRODUCT-SAFE-*` |
| `USR-PRODUCT-02` 商品全部评价 | `/products/:productId/reviews` | `ProductReview_List` | Public；Published、Cursor、评分/有图/SKU 筛选 | Contract+UI `PRODUCT-REVIEWS-*` |
| `USR-CART-01` 购物车 | `/cart` | `CartItem_Add/Update/Delete` | Current User；增量添加幂等，最终值更新用 ETag | Concurrency `CART-*` |
| `USR-CHECKOUT-01` 单品/购物车结算/联系商家 | `/checkout/:checkoutId` | `Checkout_Create/Update/Get/Reprice`、`StoreConversation_GetOrCreate` | Current User；备注 200 字；本店 `checkout_store_group` 绑定 Version/Expiry，跨店字段 Hard Deny | Domain+Context E2E `CHECKOUT-*` |
| `USR-ORDER-01` 创建/查看订单 | `/me/orders*` | `Order_Create/ListMine/GetMine` | Current User；Checkout + Order Idempotency；只创建订单、不创建 Payment | Transaction+Unknown Outcome `ORDER-*` |
| `USR-PAY-01` 收银台/支付结果 | `/pay/:tradeOrderId`、`/payments/:paymentId/result` | `Payment_Create/Get/ListForTradeOrder/Close` | Current User；独立 Payment Key；活动/Unknown 尝试阻断新建；渠道参数不判成功 | Audit+Provider E2E `PAY-*` |
| `USR-SHIP-01` 查物流 | `/me/orders/:orderId/logistics` | `Shipment_ListMine/GetMine/ListTracks/Refresh` | Order Owner；仅单包裹本人 Detail 含完整单号，概览/非本人/Agent 仅掩码；预计可空且不推断 | Projection+IDOR+E2E `SHIP-USER-*` |
| `USR-REVIEW-01` 我的评价/创建/编辑/追评 | `/me/reviews`、`/me/order-items/:orderItemId/review`、`/me/reviews/:reviewId/*` | `ReviewEligibility_Get`、`Review_ListMine/Create/Get/Update/Append` | Order Item Owner；评价窗口/唯一性/文件归属/ETag | Domain+UI `REVIEW-*` |
| `USR-REFUND-01` 售后列表/资格/申请/详情/退货/申诉 | `/me/after-sales*`、`/me/orders/:orderId/refund`、`/me/appeals/:appealId` | `RefundApplication_ListMine/Get/Create`、`RefundEligibility_Check`、`RefundReturnShipment_Upsert`、`RefundAppeal_Create/Get` | Order Owner；订单项占用；新申请/申诉分离；状态驱动动作 | State+Concurrency+E2E `REFUND-USER-*` |
| `USR-MSG-01` 用户消息/已读 | `/messages/:conversationId?` | `UserConversation_List/GetMessages/SendMessage/AdvanceReadCursor` | User Audience + Conversation Owner；Sequence/幂等；可见性触发、Cursor 单调、绝对未读值 | WS+Multi-tab+Audience `MSG-USER-*` |
| `USR-AI-01` 专属/店铺客服 | `/messages/:conversationId` | `AgentRun_Create/Events`、`StoreConversation_GetOrCreate`、`ConversationContext_Replace` | User/Store/Consent/Tool Gateway 交集；同店唯一会话；Active Context Version；卡片非授权 | Agent Security+Context E2E `AGENT-USER-*` |
| `USR-HUMAN-01` 转人工 | `/messages/:conversationId` | `HumanTicket_Create/GetMine/CancelMine` | Conversation Owner；唯一 Active Ticket | State+WS `HUMAN-USER-*` |
| `USR-AI-PRIVACY-01` 授权/暂停/关闭全部 | `/me/settings/ai-personalization` | `AiConsent_ListMine/Grant/GetMine/Pause/Resume/Revoke`、`AiPersonalization_DisableAll` | Current User+Consent ID+Scope；ETag；告知版本；撤销同步阻断召回 | Privacy+Concurrency+E2E `AI-CONSENT-*` |
| `USR-AI-MEMORY-01` 记忆查看/更正/删除/清理 | `/me/settings/ai-personalization` | `AiMemory_ListMine/Revise/Delete`、`CleanupTask_GetMine/RetryMine` | Current User+Namespace/Store Scope；L2+low；Consent+结构化确认+ETag；Tombstone | Privacy+Isolation+Deletion `AI-MEMORY-*` |
| `USR-AI-FEEDBACK-01` AI 消息赞踩/举报/纠错 | `/messages/:conversationId` | `AiFeedbackReaction_Put/Delete`、`AiFeedbackReport_Create`、`AiFeedbackCorrection_Create` | Conversation Owner+`sender_type=agent`；幂等 Reaction；Report 限流/脱敏；不写 Memory/Golden | API+Privacy+A11y `AI-FEEDBACK-*` |
| `USR-CONTENT-01` 帮助/关于/页脚 | `/help`、`/help/:contentKey`、`/about` | `HelpArticle_ListPublished/GetPublished`、`AboutContent_GetPublished`、`FooterContent_GetPublished` | Public；仅 Published Version；Safe HTML/CSP；外链 Allowlist | Contract+Security+A11y `PUBLIC-CONTENT-*` |
| `USR-SYSTEM-01` 错误/维护/离线/Session 恢复 | `/403`、`/gone`、`/error`、`/maintenance`、全局 Overlay/Dialog | `ui_only` + 原 Operation 安全重试/Token Refresh | 不泄露资源；410/412/429/Unknown 有确定恢复；安全 Redirect | Route+A11y+Recovery `SYSTEM-STATE-*` |

##### 3.34.19.2 管理端操作追踪矩阵

| Requirement ID / 页面操作 | Vue Route | OpenAPI Operation / 路径 | Permission / Scope / Domain Rule | Audit / Test / Evidence |
| :--- | :--- | :--- | :--- | :--- |
| `ADM-AUTH-01` 管理登录/MFA | `/admin/login` | `AdminAuth_Login/MfaVerify/Reauthenticate` | Admin Audience；AAL/近期认证 | Security `ADM-AUTH-*` |
| `ADM-DASH-01` 仪表盘 | `/admin/dashboard` | `AdminDashboard_Get` | `dashboard:read` + Scope；指标口径/新鲜度 | Query+UI `ADM-DASH-*` |
| `ADM-USER-01` 冻结/解冻 | `/admin/users/:userId` | `AdminUser_ChangeStatus` / `POST /api/v1/admin/users/{user_id}/status-changes` | `users:manage`；ETag；`user_status_records`；Session 撤销 | Audit+State+E2E `ADM-USER-STATUS-*` |
| `ADM-USER-02` 敏感字段 | `/admin/users/:userId` | `AdminSensitiveGrant_Create/Consume/Revoke` | `users:read_sensitive`；Admin+Session+Target+Fields+Purpose+TTL | Security `ADM-SENSITIVE-*` |
| `ADM-RBAC-01` 授权/撤销 | `/admin/users/:userId` | `AdminRoleGrant_Create/Revoke/List` | `rbac:manage`、`rbac:read`；Active UK；不恢复旧 Grant | Audit+Concurrency `ADM-RBAC-*` |
| `ADM-STORE-01` 认证决定/补件 | `/admin/store-certifications/:certificationId` | `AdminStoreCertification_Decide/AddMaterialVersion` | `stores:review` 或 `stores:manage` + 本店 Scope；材料 Version+ETag | Audit+State `ADM-CERT-*` |
| `ADM-POLICY-01` 店铺政策 | `/admin/stores/:storeId/policies` | `AdminStorePolicy_List/Create/Update/Publish/Withdraw` | `store_policies:read`、`store_policies:create`、`store_policies:update`、`store_policies:publish` + Store Scope；版本不可变 | Contract+RAG `ADM-POLICY-*` |
| `ADM-PRODUCT-01` 商品编辑/发布 | `/admin/products/:productId` | `AdminProduct_Update/Submit/Publish` | `products:update`、`products:publish` + Store Scope；ETag/完整性 | Audit+E2E `ADM-PRODUCT-*` |
| `ADM-INV-01` 库存调整 | `/admin/inventories` | `AdminInventory_Adjust` | `inventories:adjust` + Store Scope；Delta/原因/版本 | Concurrency `ADM-INV-*` |
| `ADM-ORDER-01` 订单改价/取消 | `/admin/orders/:orderId` | `AdminOrder_AdjustAmount/Cancel` | `orders:adjust`、`orders:cancel` + Scope；支付/状态/职责分离 | Audit+State `ADM-ORDER-*` |
| `ADM-SHIP-01` 创建包裹 | `/admin/orders/:orderId` | `AdminShipment_Create` | `shipments:create` + Store Scope；创建即发货，无草稿 | Transaction `ADM-SHIP-CREATE-*` |
| `ADM-SHIP-02` 纠错/作废 | `/admin/shipments/:shipmentId` | `AdminShipment_CorrectTracking/Void` | `shipments:correct`、`shipments:void`；仅未揽收可 Void；Unknown 对账 | State+E2E `ADM-SHIP-VOID-*` |
| `ADM-REFUND-01` 退款决定 | `/admin/refund-applications/:refundId` | `AdminRefund_Claim/Decide` | `refunds:review` + Store Scope；金额/资格/复核 | Audit+Concurrency `ADM-REFUND-*` |
| `ADM-REVIEW-01` 回复/屏蔽/恢复 | `/admin/reviews/:reviewId` | `AdminReview_Reply/Moderate` | `reviews:reply`、`reviews:moderate` + Scope；规则码；不物理删除 | Audit `ADM-REVIEW-*` |
| `ADM-SUPPORT-01` 领取/回复 | `/admin/support/tickets/:ticketId` | `SupportTicket_Claim`、`SupportMessage_Send` | Admin Audience；`support:claim`、`support:reply` + Queue/Assignment | Audience+WS `ADM-SUPPORT-MSG-*` |
| `ADM-SUPPORT-02` 等待/恢复 | `/admin/support/tickets/:ticketId` | `SupportTicket_Wait/Resume` | `support:wait`、`support:resume`；`active↔waiting_user`；SLA 守恒 | State+WS `ADM-SUPPORT-WAIT-*` |
| `ADM-AI-01` Tool Version 发布 | `/admin/ai/tools/:toolId` | `AdminToolVersion_Create/Publish/Rollback` | `ai_tools:manage`、`ai_tools:publish`；不可变 Version；审批 | Contract+Security `ADM-TOOL-*` |
| `ADM-KNOW-01` 索引任务 | `/admin/knowledge/documents/:documentId` | `AdminKnowledgeIndex_Create`、`AdminBatchJob_Cancel` | `knowledge:manage`；MySQL Parent+PG Child+Version 对账 | Integration `ADM-INDEX-*` |
| `ADM-CONTENT-01` 内容发布/撤回 | `/admin/content/:contentId` | `AdminContent_Publish/Withdraw` | `content:publish`；Schema/安全/时间窗/审批 | Security+E2E `ADM-CONTENT-*` |
| `ADM-BATCH-01` 批处理确认/取消 | `/admin/system/jobs/:jobId` | `AdminBatchJob_Confirm/Cancel` | Job Owner/Permission/Scope 再校验；协作取消 | Worker `ADM-BATCH-*` |
| `ADM-APPROVAL-01` 审批决定 | `/admin/approval-requests/:approvalRequestId` | `AdminApproval_Decide` | `admin_approvals:decide` + Domain Permission/Scope/AAL；发起人不可自批 | Security+E2E `ADM-APPROVAL-*` |
| `ADM-AUDIT-01` 审计查询/导出 | `/admin/audit-logs` | `AdminAudit_List/Export` | `audit:read`；脱敏；查看/导出本身审计 | Security `ADM-AUDIT-*` |
| `ADM-DLQ-01` 死信预览/重放 | `/admin/system/dead-letter-events/:deadLetterId` | `AdminDeadLetter_Preview/Replay` | `events:read`、`events:operate`；Schema/版本/幂等/审批 | Fault+E2E `ADM-DLQ-*` |

##### 3.34.19.3 自动完整性检查

CI 必须解析 Vue Route Meta、生成后 OpenAPI、Permission Registry、领域 Command Registry 和测试 Metadata，并执行：

1. 有可见按钮但无 `operationId`、Permission/Ownership、Scope、状态规则或测试 ID 时失败；
2. 有管理/客服 Operation 但无 Vue Route/Owner，且未标注 `internal_only/webhook` 时失败；
3. Permission 不满足 3.7.2.2 正则、Operation 兼容 User/Admin Audience，或 Store/Queue Scope 未声明时失败；
4. 写 Operation 无 Idempotency/If-Match 策略、无 Domain Command/审计事件，或高风险操作无确认/审批要求时失败；
5. 每个 Requirement ID 至少关联 Happy Path、Unauthenticated/Forbidden、Invalid State/Conflict 和幂等/并发用例；敏感与跨 Scope 操作额外关联泄漏为零的 Security Case；
6. 验收证据固定保存到 `artifacts/acceptance/<release>/<requirement-id>/`，至少含测试报告、OpenAPI/Permission 快照、必要的页面截图/无障碍报告和 Trace/Audit Ref；生产数据必须脱敏。
7. `docs/domain_registry.yaml` 中每个状态、合法迁移、Guard 和事件均能生成代码/DDL/OpenAPI/测试；任何未登记迁移必须断言业务表、状态流水和 Outbox 零写入。
8. 每个 OpenAPI Operation 必须携带 `docs/traceability.yaml.operation_contract` 规定的 `x-requirement-id、x-owner-kind、x-permission-codes、x-scope-policy、x-domain-command、x-audit-event、x-idempotency-policy、x-test-case-ids`；同一路由行列出多个 Permission 时不得整行继承，必须在 Operation 级绑定实际所需权限。`x-owner-kind` 只能为 `vue_route/global_ui/internal_only/webhook/worker`，未能唯一解析 Owner 的 Operation 使 CI 失败。

---
