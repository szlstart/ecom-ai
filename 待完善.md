# 待完善

> 更新日期：2026-08-31
> 当前分支：`codex/production-ai-hardening`
> 本文只记录本轮真实账号、真实容器和隔离验收中得到的证据。不得用“有代码”“有页面”代替真实运行结论。

## 1. 验收规则

- 状态仅使用：`待验证`、`修复中`、`待回归`、`已关闭`、`外部上线条件`。
- `已关闭` 至少需要实现证据、自动化测试证据和真实运行复测证据。
- AI 右栏展示服务端依据实际意图、权限、工具、RAG、委派和结果生成的可核验分析；不保存或展示模型隐藏思维链。
- 真实微信/支付宝、真实快递、公网 TLS、云备份等外部资源不得用本地模拟结果冒充。
- 本轮测试不得破坏现有商品、订单、余额和会话；全量集成测试使用独立 MySQL、PostgreSQL、Redis 和对象存储命名空间。

## 2. 三端与 AI 审查矩阵

| 编号 | 场景 | 状态 | 当前证据 |
|---|---|---|---|
| AI-01 | 用户专属客服：平台规则、商品搜索、推荐 | 已关闭 | Kimi 可用；真实检索返回 2 个在售商品；商品、店铺、价格、SKU 和实时库存均来自结构化工具；个性化推荐不再编造商品。 |
| AI-02 | 用户专属客服：本人订单、金额、状态 | 已关闭 | 真实订单返回订单号、店铺、`¥6.00` 和中文状态；其他用户订单不泄露是否存在；6 元不再误报 600 元。 |
| AI-03 | 用户专属客服：模拟物流 | 已关闭 | 本人订单可查询 Ecom 速运、脱敏运单和 4 段轨迹；已签收订单在列表显示“已签收，待确认收货”。 |
| AI-04 | 用户专属客服：售后资格、退款草稿、确认 | 已关闭 | 资格检查保持只读；草稿金额为 `¥6.00`；只有页面确认卡可提交，聊天文字“确认”不能执行；拒绝后退款记录仍为 0。 |
| AI-05 | 店铺客服：商品、SKU、库存、政策 | 已关闭 | 绿杆 2B 铅笔准确返回 6/8/10 支款、`¥6/¥7/¥8`、库存 `98/99/0`；10 支明确缺货；店铺 RAG 返回 2B、适用场景和公开来源。 |
| AI-06 | 店铺专属客服：经营、商品、订单、库存 | 已关闭 | 只读返回本店 2 个在售商品、5 个 SKU、零库存风险、1 个履约中订单、待确认金额 `¥6.00`、已确认营业额 `¥0.00`；越权注入已阻断。 |
| AI-07 | 管理端 AI 管家与 Multi-Agent | 已关闭 | 4 个只读专家并行返回用户 3、店铺 1、商品 2、已发货订单 1、Outbox 0；给出三条建议；历史失败后 35 次成功，被正确判定为已恢复。 |
| MSG-01 | 用户与店铺消息、头像、未读、商品/订单卡片 | 已关闭 | 用户 `+` 可发送本店商品和本人订单；店铺实时收到卡片、图片、价格和库存；打开会话后未读清零。 |
| MSG-02 | 用户/店铺与平台专属客服互通 | 已关闭 | 用户、商家专属客服会话均出现在管理端分组中；管理员可接入并回复。 |
| MSG-03 | AI 转人工、人工回复、结束与 AI 恢复 | 已关闭 | 仅显式意图触发；出现居中接入提示；人工期间 AI 暂停；结束后系统消息、是否解决按钮和 AI 恢复均已真实复测。 |
| MSG-04 | 删除对话与记忆清理 | 已关闭 | 用户删除专属客服会话后固定会话重建为空；关联长期记忆停止召回；用户亦可在隐私中心单独删除记忆并查看清理任务。 |
| RAG-01 | 平台规则知识库 | 已关闭 | 4 份平台规则文档已发布并向量化；真实问答能引用平台知识；无证据时拒绝编造服务时间。 |
| RAG-02 | 店铺商品与政策知识库 | 已关闭 | 店铺公开资料已发布；PostgreSQL 存在有效向量；检索日志未降级；真实店铺问题返回 1 个 ACL 匹配来源，不读取库存、资金等实时事实。 |
| AGT-01 | Skill/MCP 注册、版本、权限和审计 | 已关闭 | 8 个 Skill 均为 active/published v1；34 个 Tool 为不可变已发布版本；管理页已补齐 9 类已注册 MCP Server；右栏显示真实工具调用。 |
| AGT-02 | Multi-Agent 委派、隔离和失败处理 | 已关闭 | 管理端 4 专家并行、商家商品/库存/订单专家均已运行；子任务只读、限制作用域；委派记录和右栏工作记录可追踪。 |
| AGT-03 | 上下文、摘要、长期记忆 | 已关闭 | 明确偏好先生成候选，用户确认后才生效；推荐能召回该偏好；删除后立即停止召回；订单、价格、库存不写长期记忆。 |
| COM-01 | 商品、购物车、结算、余额和店铺资金 | 已关闭 | 当前真实账本：模拟充值 1000 元、订单扣款 6 元、余额 994 元；订单实付 6 元；商家仅在确认收货后确认收入。 |
| COM-02 | 订单、确认收货、评价、售后投影 | 已关闭 | 用户订单操作与状态匹配；签收不等同确认收货；用户、商家、管理员读取同一订单权威状态。 |
| LOG-01 | 支付后模拟物流 | 已关闭 | 自动创建物流单，按 5 秒间隔生成待揽收、已揽收、派送、签收 4 段轨迹；发货地、区县和详细地址逐级呈现。 |
| SEC-01 | 身份隔离、越权和 Prompt Injection | 已关闭 | 用户、商家、管理员三个 Audience 分离；其他用户订单、跨店数据、密钥、系统提示词和危险写操作均被阻断。 |
| UX-01 | 三端独立消息页与三栏界面 | 已关闭 | 桌面真实页面布局、头像、气泡、商品/订单卡片、输入区和可展开分析栏已复测；旧分析步骤名称已清除；最终 Playwright 桌面与移动布局共 16 项通过，2 个移动端重复的真实账号流程按设计跳过。 |
| OPS-01 | Worker、Outbox、模型、Embedding 和存储 | 已关闭 | Kimi 模型发现、结构化输出、流式输出和用量统计均通过；5 个知识切片均有 768 维向量；Outbox 待处理为 0、无未处置死信；20 个服务已统一重建并通过版本、健康和错误日志巡检。 |
| REL-01 | 全量迁移、测试、构建、安全和发布证据 | 已关闭 | 最终隔离验收：后端 366 项通过、1 项按条件跳过、覆盖率 65.13%；前端单测通过；双库迁移和生产构建通过；浏览器 16 项通过、2 项按设计跳过；Agent 安全专项 18 项通过；生成 145 份需求证据，本地决策为 `pass`。公网生产依赖仍按第 5 节单独管理。 |

## 3. 本轮问题闭环

### P0-01 真实模型配对评估观测与发布门禁

- 状态：`已关闭`
- 修复：40 用例发布集已作为后端包内只读资源随镜像发布；评估 Worker 会采集与同一数据集哈希绑定的 baseline/candidate 真实模型观测，逐项记录通过状态、工具、引用、安全、延迟和成本。任务中断超过两小时会以 `EVALUATION_WORKER_INTERRUPTED` 失败关闭，绝不把残缺观测伪装为通过。
- 真实证据：管理端任务 `evr_01M1AC8X8CVVTMCWNDSAKG4WPS` 从排队、运行到完成；Kimi 共执行 80 次真实调用，无 429 或 5xx。40 个候选用例通过率 100%，工具正确率 100%，引用正确率 100%，安全违规 0；相对基线提升 7.5 个百分点；候选 P95 延迟 4089.121 ms，为基线的 86.66%；平均成本为基线的 111.64%；发布门禁最终为 `pass`。

### P1-01 当前打开会话的未读数仍有短暂延迟

- 状态：`已关闭`
- 修复：可见且位于底部的当前会话会在合并消息后主动提交最新非用户序号，不再依赖 50% IntersectionObserver；父组件同步更新会话列表。
- 真实证据：当前用户专属客服会话在 AI 回复落屏后立即显示“消息已读”，会话列表不再产生错误未读红点；自动化回归同时覆盖了当前会话和跨会话隔离。

### P1-02 知识索引历史失败记录缺少“已由新版本恢复”的聚合视图

- 状态：`已关闭`
- 修复：列表按文档/版本聚合最新有效状态；早期失败后已有成功任务时显示“历史失败已恢复”，历史任务仍保留并可展开追踪。
- 真实证据：管理端知识索引页面已显示 5 份已发布文档及“历史失败已恢复”，当前有效状态与 PostgreSQL 中的向量化结果一致。

### P1-03 AI 右栏旧步骤名称浏览器回归

- 状态：`已关闭`
- 修复：用户与店铺 Agent 的步骤名称改为“分析用户诉求”“读取最近会话”“核验依据并组织答复”；代码全局检索已无旧名称。
- 真实证据：最终隔离 Playwright 全部桌面用例通过，三端真实 Agent 链路均能展示与服务端执行事实一致的可展开分析摘要。

## 4. 本轮已关闭的关键缺陷

- 模拟物流 Worker 因后台 `trace_id` 为空持续失败；已生成后台 Request ID、增加失败心跳，并通过真实 4 段物流复测。
- 显式订单号查询曾被页面上下文阻断；现在显式资源优先并重新做归属校验。
- 全平台商品搜索曾把整句直接用于 `%整句%`，导致在售商品漏召回；现在进行候选词扩展和原问题回退。
- 店铺短句追问“6 支装能买吗”曾命中上一个商品；现在按商品名/SKU 名综合评分，并确定性输出实时库存。
- 商品与个性化推荐曾由模型补写不存在商品；现在事实段由实时工具结果确定性渲染。
- 订单金额曾将 600 分表述成 600 元；所有金额接口和 AI 工具统一携带 minor units、currency 和 display。
- 签收订单曾仍显示“运输中”；用户列表与详情现在读取物流摘要并显示“已签收，待确认收货”。
- 管理 AI 曾把已恢复历史失败描述为当前故障；现区分 24 小时失败、失败后成功次数和未恢复故障。
- MCP 管理页缺少 store-ops/governance/observability 三类 Server；现与运行注册表一致。
- 评估任务曾永久排队；新增评估 Worker 后可结束且保持 fail-closed。
- 全量隔离验收曾因生产 Compose 漏掉评估 Worker、库存测试仍要求英文状态失败；两项已修复。
- 规则类问答在模型依据校验失败时曾直接倾倒整份知识文档；现统一使用相关性筛选后的简明规则降级答复，短问最多一条、一般问题最多两条，并保留公开来源标题。
- AI 回复曾在规则答案前生成“刀本地”“科技本地”等虚构称呼；现将平台和店铺规则答案统一改为“模型理解与检索、服务端按可信证据确定性成文”，保留来源且不允许模型自由添加称呼。
- AI 分析栏曾因空的通用 `items` 列表遮蔽 `knowledge_sources`，把已有知识命中显示成“返回 0 项”；现按所有结构化结果集合的有效最大值计数，回答、引用与公开执行轨迹保持一致。
- Git 历史密钥扫描曾把固定 CI 加密测试键和密码哈希测试向量识别为泄漏；已按精确历史指纹建立最小忽略清单，其他路径与新增内容仍完整扫描，最终 Gitleaks 检查为 0 泄漏。
- 前端 Nginx/Alpine 基础镜像曾包含有修复版本的 OpenSSL、musl、libxml2 等高危漏洞；镜像构建现升级运行时系统包，Trivy 对仓库、API 镜像和前端镜像的高危/严重漏洞扫描均为 0。
- 公开商品读取负载基线已完成：25 个并发用户持续 2 分钟，共 14,393 次请求，成功率 100%、失败率 0，P95 为 24.21 ms、P99 为 66.86 ms，全部阈值通过。

## 5. 外部上线条件

- 用户在历史对话中发送过模型和 Embedding 密钥；正式上线前必须在供应商控制台轮换，仓库和日志中不得保存明文，只能通过 Secret 注入。
- 真实微信/支付宝商户渠道、真实清结算与退款沙箱需要商户资质和渠道审批；当前仅为明确标注的本地模拟充值/支付。
- 当前物流为产品要求的模拟物流；接真实快递前需完成 Webhook 验签、对账、幂等和 SLA 验收。
- 公网域名、TLS、WAF、云数据库 PITR、对象存储跨区域复制、生产告警送达、值班表和容量/浸泡压测必须在实际生产资源上留证。
- 上述外部项未完成前可以作为本地完整演示和测试环境使用，但不能宣称已满足公网生产上线条件。

## 6. 当前结论与后续动作

- 本地代码、数据迁移、三端业务、AI/RAG/MCP/Skill/Multi-Agent、消息实时性、权限安全、自动化测试和真实浏览器验收均已完成并有证据，本清单不再保留未关闭的内部产品缺陷。
- 本地完整演示环境可以直接使用；发布到公网前，只需按第 5 节完成供应商密钥轮换及真实基础设施/渠道验收，并把相应证据纳入 Go/No-Go 审批。
- 后续模型、Prompt、Skill、MCP、知识库或依赖版本发生变化时，必须重新运行同一套 40 用例真实评估、隔离验收、安全扫描和性能基线，不得沿用本次结论。
