# 待完善

> 建立日期：2026-08-31  
> 当前审查分支：`codex/production-ai-hardening`  
> 目标：以用户端、商铺端、超级管理端的真实账号和真实运行服务，循环完成“探索测试—定位—修复—自动化回归—真实界面复测”，直至达到可验证的上线准入标准。

## 1. 使用规则

- 本文是本轮上线加固的唯一问题台账；每项必须包含复现证据、根因、修复、自动化回归和真实页面复测结果。
- 状态只使用：`待验证`、`已复现`、`修复中`、`待回归`、`已关闭`、`外部上线条件`。
- “已关闭”必须同时具备代码或配置证据、自动化测试证据和真实运行验证证据。
- 不把本地模拟支付/物流描述为已接入真实微信、支付宝或物流公司；外部资源才能完成的事项单列为“外部上线条件”。
- 不展示或保存模型隐藏思维链；右栏展示服务端依据真实意图、上下文、权限、工具、RAG 与执行结果生成的可核验分析。

## 2. 审查矩阵

| 编号 | 场景 | 状态 | 验收重点 |
|---|---|---|---|
| AI-01 | 用户专属客服：平台规则、商品搜索、推荐 | 待验证 | Kimi 回复、RAG、引用、权限范围、无幻觉 |
| AI-02 | 用户专属客服：本人订单、金额、状态 | 待验证 | 分/元正确、只读本人订单、订单卡片不误转人工 |
| AI-03 | 用户专属客服：物流查询 | 待验证 | 模拟轨迹真实、地址脱敏、订单归属校验 |
| AI-04 | 用户专属客服：售后资格、退款草稿、确认 | 待验证 | 状态机、授权、二次确认、工具审计 |
| AI-05 | 店铺客服：商品、SKU、库存、政策 | 待验证 | 仅当前店铺、结构化数据优先、RAG 兜底 |
| AI-06 | 店铺专属客服：经营、商品、订单、库存 | 待验证 | 商家范围隔离、只读/写权限边界 |
| AI-07 | 管理端 AI 管家：用户、店铺、订单、运行状态 | 待验证 | 平台范围、只读治理、多 Agent 委派 |
| MSG-01 | 用户与店铺互发消息、未读、头像、商品/订单卡片 | 待验证 | 实时到达、顺序、去重、卡片正确 |
| MSG-02 | 用户/店铺与平台专属客服互通 | 待验证 | 管理端分组、未读、历史同步 |
| MSG-03 | AI 转人工、人工回复、结束人工、AI 恢复 | 待验证 | 居中系统消息、AI 暂停/恢复、解决确认 |
| MSG-04 | 三端删除对话与 AI 记忆清理 | 待验证 | 会话不可访问、摘要/记忆/检查点清理、重新建会话 |
| RAG-01 | 平台规则知识库检索 | 待验证 | 文档已发布、向量与关键词混合检索、引用准确 |
| RAG-02 | 店铺商品与政策知识检索 | 待验证 | 店铺/商品 ACL、版本更新、无跨店泄露 |
| AGT-01 | Skill 与 MCP 工具注册、绑定、权限和审计 | 待验证 | 输入输出 Schema、超时、降级、审计完整 |
| AGT-02 | Multi-Agent 路由、并行只读与失败处理 | 待验证 | 委派准确、范围隔离、成本/深度限制 |
| AGT-03 | 短期上下文、摘要、长期记忆生命周期 | 待验证 | 召回准确、用户授权、冲突/删除/隔离 |
| COM-01 | 商品浏览、收藏、购物车、立即购买与支付 | 待验证 | 三端同步、库存/余额/营业额、幂等 |
| COM-02 | 订单取消、确认收货、评价、售后 | 待验证 | 状态机与商家/管理员投影一致 |
| LOG-01 | 支付后模拟物流全轨迹 | 待验证 | 5 秒阶段推进、编号、发货地/目的地、签收 |
| SEC-01 | 三端身份隔离、越权、Prompt Injection | 待验证 | 不能跨用户/跨店/跨 Audience，危险工具阻断 |
| UX-01 | 三端消息独立页视觉、输入、滚动、响应式 | 待验证 | 无空白滚动、三栏可用、移动降级合理 |
| OPS-01 | Worker、Outbox、模型、Embedding、对象存储健康 | 待验证 | 无积压、失败可追踪、降级准确 |
| REL-01 | 全量测试、迁移、构建、安全、部署、恢复证据 | 待验证 | 所有门禁通过，运行版本与 Git SHA 一致 |

## 3. 已发现问题

### P0-01 模拟物流 Worker 持续失败且健康检查误报健康

- 状态：`待回归`
- 真实证据：已付款订单 `ord_01M19K9GS9ZG90TSGAFJ3DPMNY` 长时间停留在“待发货”，没有物流单；`logistics-sync-worker` 每约 0.5 秒记录一次 `logistics_sync_batch_failed`，但容器健康检查仍返回健康。
- 根因：后台任务没有 HTTP 请求上下文，创建 `shipment.automatic_created.v1` Outbox 事件时把空 `trace_id` 写入非空字段，触发 MySQL `IntegrityError`；现有 Worker 心跳只证明事件循环仍存活，不证明批次执行成功。
- 修复要求：为后台任务生成独立 Request ID；健康心跳记录连续失败、最近成功时间和错误码，连续失败达到阈值时健康检查必须失败；补充无请求上下文的物流集成测试与真实订单四阶段轨迹复测。

### P0-02 显式订单号物流查询被隐藏页面上下文阻断

- 状态：`已关闭`
- 真实证据：本人明确询问 `ord_01M19K9GS9ZG90TSGAFJ3DPMNY` 的物流状态时，AI 回复“当前选择的订单或售后上下文已变化”。
- 根因：`logistics_lookup` 强制要求页面中的 `active_context`，没有从当前消息或订单卡片解析资源编号。
- 修复要求：显式订单号优先，随后严格校验订单归属；无显式编号时才使用有效页面上下文；外部用户订单必须拒绝且不可泄露是否存在。同类规则同步覆盖订单详情、退款资格和售后进度。
- 修复与验证：当前消息可解析受限格式的订单/售后编号，工具层继续执行本人归属校验；单元测试覆盖合法、短编号和错误资源类型。真实用户页面再次查询同一订单后成功返回已签收、脱敏物流单号和详细位置。

### P0-03 全平台商品检索存在明显漏召回

- 状态：`已关闭`
- 真实证据：首页存在 2 个“文具专卖店”的在售商品，但询问“平台当前在售的文具商品”时工具返回 0，AI错误回答平台没有在售文具，并产生了无依据的称呼前缀。
- 根因：模型生成的整句搜索词直接进入 `%整句%` 名称匹配；检索仅匹配商品名和品牌名，不匹配店铺名，也没有关键词拆分、宽泛意图回退和空结果回答约束。
- 修复要求：搜索词规范化与候选词召回、店铺名范围检索、宽泛在售列表安全回退、具体不存在商品不误召回；空证据只能明确无结果，禁止模型补写昵称或事实。
- 修复与验证：新增自然语言清洗、候选词、店铺名匹配及受限宽泛回退；单元测试验证“文具”召回和不存在商品不回退全部。真实用户页面已正确列出 2 个在售商品、店铺和价格。

### P1-01 订单 AI 降级回复缺字段且暴露内部英文状态

- 状态：`已关闭`
- 真实证据：用户要求订单号、店铺、实付金额和当前状态，回复只包含订单号、店铺及 `pending_shipment`，没有金额。
- 根因：回答模型不可用或输出未通过校验后，确定性渲染模板未覆盖结构化金额，也没有中文状态映射；运行只记录笼统的 `answer_model_unavailable`，难以区分网络、结构和落地性校验失败。
- 修复要求：降级回答必须完整输出被询问的结构化字段，金额统一分转元，所有状态中文化；细分模型失败原因并保留不含敏感内容的可观测证据。
- 修复与验证：确定性回答已统一使用金额展示字段和中文状态映射；真实用户页面返回订单号、店铺、`¥6.00` 与“运输中”，未再暴露 `pending_shipment`。

### P1-02 店铺专属客服只能返回经营汇总，不能完成明细问答

- 状态：`已关闭`
- 真实证据：商家要求列出本店在售商品、每件价格与库存并给出经营建议，回复只有 `on_sale=2`、`deleted=5` 状态计数。
- 根因：经营 Multi-Agent 的 Catalog 子 Agent 只投影状态汇总，未提供授权店铺内的商品、SKU、价格和库存明细；降级模板也只会枚举工具返回键值。
- 修复要求：增加店铺范围内的结构化商品/SKU/库存读取，中文呈现，并基于真实数据给出可核验建议；保持跨店铺隔离与只读权限。
- 修复与验证：商家 Catalog 工具现按当前店铺读取在售商品、SKU、分单位价格、在手/预占/可售库存；真实商家页面已列出 2 个商品、5 个款式和低库存建议，且工具仍由可信店铺作用域注入。

### P1-03 AI 模型失败诊断和回答质量门禁不足

- 状态：`待回归`
- 真实证据：模型 HTTP 请求返回成功时仍可能进入统一降级，当前运行记录无法判断是请求失败、JSON 结构失败、引用失败还是事实落地性校验失败；空工具结果时曾出现无依据称呼。
- 根因：异常分类被合并为单一降级码，模型输出校验只决定“接受/拒绝”，缺少安全的失败阶段指标与对空证据回答的确定性约束。
- 修复要求：按请求、解析、Schema、落地性、超时分别计数；不记录 Prompt、密钥和私密正文；空结果与金额/状态等高风险字段优先使用确定性结构化渲染。
- 当前进展：已增加模型失败阶段码和虚构称呼服务端清洗；仍需部署后证明新回复不再出现虚构昵称，并检查三类 Agent 的失败原因分布。

### P1-04 当前打开的用户会话短暂显示自己的 AI 回复为未读

- 状态：`待回归`
- 真实证据：专属客服会话保持打开时，AI 回复已出现在视口中，但顶部和会话列表仍显示 `1 条未读`，最长要等待下一轮轮询才消失。
- 根因：子会话组件提交已读游标后只更新自己的会话对象，父级会话列表先被实时刷新为未读，未收到子级已读结果。
- 修复要求：已读游标成功后立即把结果回传父级列表；自动化测试与真实页面验证当前会话未读数及时归零。

### P1-05 管理员 Multi-Agent 发布版本未落到运行库

- 状态：`待回归`
- 真实证据：首次跨用户、店铺、订单和运行健康查询没有生成任何 `ai_agent_delegations`，右栏也显示普通处理；运行库只有 `admin_copilot` v1。
- 根因：代码已定义带准入报告的 v2 多智能体策略，但当前环境在该版本加入前完成过初始化，之后没有再次执行幂等 Bootstrap。
- 修复要求：已执行幂等 Bootstrap 发布 v2；真实页面已显示“多智能体协作”，数据库生成 4 个成功的只读专家委派。还需补充结构化数量、风险与建议的渲染回归，并把版本检查加入部署门禁。

### P0-04 RAG 运行库为空，知识检索只有代码没有可用语料

- 状态：`修复中`
- 真实证据：MySQL `knowledge_documents` 为 0；PostgreSQL `knowledge.document_chunks`、`knowledge.index_generations`、`knowledge.indexing_jobs` 和 `knowledge.retrieval_logs` 均为 0。虽然已配置通义多模态 Embedding，三端 Agent 实际无法召回任何平台或店铺知识。
- 根因：系统只有管理员手工创建知识文档的 API，没有随部署落库的基准知识、店铺公开资料投影和持续同步任务；此前初始化只发布 Agent、Skill 与 Tool 定义。
- 修复要求：建立代码随附的平台规则知识源；按店铺生成仅含公开介绍、已发布详情、规格、款式、FAQ 和服务政策的知识投影；价格、库存、订单、资金和物流状态仍以实时业务 Tool 为唯一权威；幂等发布、向量化、版本切换、撤回、失败重试与作用域隔离全部可验证。
- 当前进展：已新增 4 份平台知识源、内容寻址版本、店铺公开知识投影、幂等索引任务和 60 秒持续同步；正在部署并验证真实向量、混合检索、引用和店铺隔离。

## 4. 已关闭问题

> 每项关闭时记录提交、测试和真实复测证据。

## 5. 外部上线条件

- 真实微信/支付宝商户渠道、真实资金清算与退款沙箱不在本地模拟范围内，正式上线前必须由真实商户资源验收。
- 当前物流为产品要求中的模拟物流；正式接入快递公司时需补 Webhook 验签、对账和真实时效 SLA。
- 公网域名、TLS、云数据库 PITR、对象存储跨区域复制、告警值班与生产容量压测必须在实际生产资源上留证。
- 用户曾在对话中发送模型与 Embedding 密钥；正式上线前必须在供应商控制台轮换，并只通过 Secret 注入。
