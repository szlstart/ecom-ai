# AI Agent 五轮严苛审视与整改台账

> 审视角色：企业级 AI Agent 应用开发面试官 / 上线评审人  
> 审视范围：用户专属客服、店铺客服、商家 AI 管家、超级管理员 AI 管家，以及其模型网关、RAG、记忆、MCP/Skill、Multi-Agent、消息实时链路、评估、可观测性和故障恢复。  
> 执行规则：每轮必须完成“发现问题 → 修改实现 → 自动化验证 → 回填证据”；同一问题不得换名重复计入下一轮。没有证据的项目不能标记完成。

## 总体上线门槛

- 业务事实只能来自经过权限校验的实时工具结果或带 ACL 的知识库，模型不能直接决定权限或写数据库。
- 流式展示与最终持久化必须一致；校验失败时不得把未经验证的答案作为最终答案留在界面。
- 用户、店铺、管理员三种身份必须隔离；商品、订单、物流、售后、余额等高风险数据要再次校验归属。
- RAG、长期记忆和 Skill 必须真正进入运行链路，不能只存在数据表、配置或设计文档中。
- 必须能观察模型、工具、RAG、委派、首字延迟、总延迟和 Token 使用；未知成本必须标记“未知”，不能伪造为 0。
- 必须有正常场景、越权场景、注入场景、乱序/重复事件、供应商超时和依赖故障的测试证据。

## 第一轮：模型网关、流式协议与失败语义

| 编号 | 严重度 | 问题 | 整改验收标准 | 状态 |
|---|---|---|---|---|
| R1-01 | P0 | 证据 JSON 直接按字符截到 24000，可能生成语法不完整的 JSON，并丢失关键字段。 | 使用结构感知、确定性的证据预算器；输出始终是合法 JSON；记录是否发生裁剪。 | 已完成 |
| R1-02 | P0 | Responses API 的流式回答与结构化请求没有执行备用模型和熔断策略。 | 主模型发生超时、网络错误、429 或 5xx 时切换备用模型；非瞬态 4xx 不盲目重试。 | 已完成 |
| R1-03 | P0 | Responses 模式把全部可用来源当成“已引用来源”，形成虚假引用。 | 只记录真实使用或经过服务端确定性归因的来源；无法证明时引用集合为空。 | 已完成 |
| R1-04 | P0 | 流式答案事实校验失败时只降置信度，未经验证内容仍会持久化。 | 校验失败重试一次；仍失败时以安全降级答案替换已流式内容，最终消息不得保留失败答案。 | 已完成 |
| R1-05 | P1 | SSE 帧 JSON 损坏、流结束但没有完整响应时的失败语义不足。 | 损坏帧、`failed`、`incomplete`、空输出均产生可分类错误并覆盖测试。 | 已完成 |

## 第二轮：权限、Skill/MCP、RAG、记忆与上下文

| 编号 | 严重度 | 问题 | 整改验收标准 | 状态 |
|---|---|---|---|---|
| R2-01 | P0 | 向量检索没有最低相关度阈值，语义无关内容也可能进入 Top-K。 | 配置相似度阈值并在 SQL 层过滤；无可靠结果时返回空集合。 | 已完成 |
| R2-02 | P0 | 店铺/专属客服只使用 Skill 的工具集合，没有执行 Skill 调用预算、超时与确认策略。 | 每次 Run 建立确定性预算；工具调用超预算被拒绝，超时被分类，写操作仍需权限/确认网关。 | 已完成 |
| R2-03 | P0 | 长期记忆虽然有 pgvector 表，但运行时只做词法召回，向量表未形成闭环。 | 已授权且激活的记忆生成向量；召回按用户/命名空间/状态隔离并做混合排序；删除/撤回同步失效。 | 已完成 |
| R2-04 | P1 | 店铺/专属客服使用定制 Tool Gateway，MCP Host 的策略执行存在分叉。 | 形成统一的前置策略执行器，至少让预算、超时、权限、审计语义一致；补契约测试。 | 已完成（策略语义统一，传输适配保留） |
| R2-05 | P1 | 上下文与工具结果虽有限长，但缺少“哪些字段被裁剪”的可观测记录。 | Trace 只记录裁剪计数/字段名等非敏感元数据，禁止记录凭证和完整隐私内容。 | 已完成 |

## 第三轮：消息 UI、实时性、并发与人工接管

| 编号 | 严重度 | 问题 | 整改验收标准 | 状态 |
|---|---|---|---|---|
| R3-01 | P0 | 前端异步处理实时事件时并发执行，可能导致事件乱序和未捕获异常。 | 单连接事件串行排队；单个处理器失败不会阻断后续事件。 | 已完成 |
| R3-02 | P0 | 推理摘要没有记录/校验 `chunk_index`，乱序帧会让右栏内容倒退。 | 推理与答案都按 Run 和 chunk 去重，忽略旧帧；测试覆盖重复和乱序。 | 已完成 |
| R3-03 | P1 | completion 到 durable message 之间过早清空 live trace，界面可能闪空。 | 最终消息到达前保留已完成临时态；持久化消息到达后无闪烁切换。 | 已完成 |
| R3-04 | P0 | 页面和顶栏存在两个消息中心实例，未读数状态不统一。 | 未读总数进入共享 Pinia 状态；已读事件同时更新页面、顶栏和会话列表。 | 已完成 |
| R3-05 | P1 | 人工接管/结束后 AI 恢复依赖事件时序，缺少重复事件测试。 | 接管、结束、恢复均幂等；AI 不与人工同时回复；系统消息只出现一次。 | 已完成 |

## 第四轮：评估、安全、可观测性、成本与运维

| 编号 | 严重度 | 问题 | 整改验收标准 | 状态 |
|---|---|---|---|---|
| R4-01 | P0 | 当前评估偏重意图路由，不能证明最终客服回答的事实性和任务完成度。 | 增加执行级 Golden Case：商品、SKU、订单金额、物流、售后、拒答、转人工和越权。 | 已完成 |
| R4-02 | P0 | Run 未完整记录实际模型、输入/输出 Token、首 Token 延迟和模型总延迟。 | 从 Responses 完成帧读取 usage；Trace 持久化非敏感指标；缺失值显式为 null。 | 已完成 |
| R4-03 | P1 | 未知供应商价格下仍可能让看板误认为成本为零。 | 成本未知显示为 unknown/null；只有配置了版本化价格表才计算金额。 | 已完成 |
| R4-04 | P1 | 缺少面向三类 Agent 的统一质量回归门禁。 | 路由、事实、工具参数、权限、拒答、转人工和延迟至少形成一套可重复命令。 | 已完成 |
| R4-05 | P1 | 对 Prompt Injection 的测试偏规则匹配，缺少来自商品详情/知识库/工具结果的间接注入。 | 增加间接注入样例，确认不改变系统权限、不泄漏秘密、不执行写操作。 | 已完成 |

## 第五轮：真实场景、故障注入、回归与上线门槛

| 编号 | 严重度 | 问题 | 整改验收标准 | 状态 |
|---|---|---|---|---|
| R5-01 | P0 | 缺少供应商超时、畸形 SSE、Redis 发布失败、重复/乱序流帧的系统化故障测试。 | 每类故障都有自动化用例，且不会丢失最终持久消息或产生重复副作用。 | 已完成 |
| R5-02 | P0 | 三端真实账号消息场景尚未在本轮整改后重新验收。 | 用户、店铺、管理员至少各完成一次问答，并覆盖商品/订单/物流/运营之一。 | 已完成 |
| R5-03 | P1 | 隔离式 E2E 固定账号与当前业务库账号混用，会导致伪失败或伪通过。 | 测试明确区分隔离种子环境与现有开发库冒烟；报告中标明数据前置条件。 | 已完成 |
| R5-04 | P0 | 缺少一条汇总上线门禁证明后端、前端、类型、迁移头和运行健康。 | 汇总命令全部通过；失败项必须修复或明确列为外部阻塞，禁止“带病完成”。 | 已完成 |
| R5-05 | P1 | 真实支付、真实物流、生产 TLS、备份恢复演练属于外部能力，容易被误报完成。 | 最终报告单列外部未验收项；本地模拟不能等同生产验收。 | 已明确生产验收边界 |

## 五轮结论与证据

### 第一轮结论：模型输出链路已改为可验证、可降级

- 主要实现：`backend/app/modules/agent_runtime/provider_gateway.py`、`live_stream.py`、`store_agent.py`、`exclusive_agent.py`、`operations_agent.py`。
- 证据预算器按字段和集合元素裁剪，任何情况下都重新序列化为合法 JSON，并将 `evidence_truncated` 与 `truncated_evidence_fields` 写入公开 Trace。
- 主/备模型只对超时、网络错误、429 和 5xx 等瞬态故障切换，并设置失败冷却；非法请求等非瞬态错误直接失败。
- 来源改为“实际引用且能在服务端证据集中解析”的交集；事实校验失败会重试，仍失败时发送 `answer_replace` 并持久化确定性安全答案。
- 自动化证据：`test_agent_provider_gateway.py`、`test_agent_live_stream.py` 覆盖损坏 SSE、`failed`、`incomplete`、空流、备用模型和安全替换。

### 第二轮结论：RAG、记忆和工具权限已进入真实运行链路

- 主要实现：`knowledge/retrieval.py`、`knowledge/skill_registry.py`、`agent_runtime/memory_runtime.py`、`store_tools.py`、`exclusive_tools.py`。
- 向量召回在 SQL 层执行最低相似度过滤；长期记忆按用户、命名空间、授权、状态、过期时间和向量模型隔离，以向量与词法混合排序。
- 记忆激活/修订生成新向量，撤回/删除同步失效；没有可用 Embedding 时不伪装成语义召回。
- 多个 Skill 暴露同一 Tool 时执行最严格确认策略、最小调用预算和最短超时；工具网关统一执行范围检查、预算、超时、确认和审计。MCP 传输适配仍可独立，但安全策略语义不再分叉。
- 自动化证据：`test_agent_tool_runtime_policy.py`、`test_agent_runtime_contract.py` 以及隔离验收中的 RAG ACL、长期记忆生命周期、退款二次确认测试全部通过。

### 第三轮结论：三端消息实时链路已按有序状态机处理

- 主要实现：`frontend/src/api/realtime.ts`、`stores/message-center.ts`、三端 Message Center、`ConversationPage.vue` 和 `AgentTracePanel`。
- 每条连接的异步事件串行消费，单处理器异常隔离；推理与答案分别按 `run_id + chunk_index` 去重，旧帧不能覆盖新帧。
- `completed` 仅切换为“等待最终消息”的临时态，直到持久化 `message.created` 到达才清理流式内容，避免右栏和答案闪空。
- 顶栏与消息页共享 Pinia 未读状态；人工接管、结束、AI 恢复和重复事件由后端幂等约束及前端补拉共同保护。
- 自动化证据：前端共 40 个测试文件、118 项测试通过；三端实时消息、乱序/重复帧、未读同步与 Trace 切换均有回归用例。

### 第四轮结论：评估从“路由演示”升级为最终回答门禁

- 主要实现：`evaluation/collector.py`、`runner.py`、`processor.py`、`data/release-holdout-v2.json` 与公开 Trace 指标。
- 数据集共 45 个版本化 Case，覆盖商品、SKU、¥6 与 600 的金额边界、订单卡片澄清、物流、售后确认、RAG、记忆、越权、直接/间接 Prompt Injection 和 Multi-Agent。
- 对真实 GPT-5.4 候选观测的成对评估结果：候选通过率、答案准确率、工具准确率、引用准确率均为 `1.0`，安全违规 `0`；基线通过率 `0.6889`，绝对提升 `0.3111`，候选 P95 `7673.869 ms`，发布门禁 `pass`。
- 实际模型、Token、首 Token 延迟和模型总延迟来自供应商完成帧；供应商未提供价格时成本保持 `null/unknown`，不再显示伪造的零成本。
- 本轮证据文件生成在忽略版本控制的 `artifacts/acceptance/current/agent/` 下；数据集 SHA-256 为 `376c8d360932a83b272498f358530a72085a37e37a711b58b7742ec0969c27d6`。

### 第五轮结论：故障、三端和完整隔离验收已闭环

- 真实开发库冒烟使用现有用户、商家、管理员三种身份分别调用专属客服、店铺客服和运营管家；检查了商品/库存、平台搜索及运营概览，并验证实际模型指标或明确的安全降级原因。冒烟不创建测试业务数据，也不与隔离 E2E 账号混用。
- 隔离验收使用专用 `ecom_ai_test`、`ecom_ai_ai_test`、Redis DB 15 和 `test-acceptance-` 对象前缀，退出时精确清理；现有开发库不参与测试。
- 回归过程中额外发现并修复：可信审批号必须由服务端注入，客户端伪造不能提交退款；商品与订单上下文并存时，指代性商品问题必须优先校验消息时的商品快照；空会话主动问候不能被浏览器测试误计为持久消息。
- 最终 `make acceptance-test-isolated`：双数据库从空库迁移及 schema drift 检查通过；后端 `388 passed, 1 skipped`，覆盖率 `65.20%`；前端单元测试通过；Playwright 三端浏览器 `16 passed, 2 skipped`（移动端按设计跳过桌面三栏流程）；Agent 安全测试 `18 passed`；生成 `145` 份需求证据，决策 `pass`。
- 最终 `make lint`：Ruff 通过，Mypy `411` 个源文件无错误，Vue TypeScript 检查通过；`git diff --check` 通过。

## 本轮最终判断

五轮中登记的代码缺陷均已修复并获得本地自动化或真实模型评估证据；当前没有已知的本地发布门禁阻塞。这里的“完成”只表示代码、迁移、隔离集成、三端浏览器、Agent 安全和模型质量门禁已通过，不等同于生产环境已经上线。

以下事项必须由真实外部环境另行验收，不能用本地模拟替代：真实支付渠道签约与回调、真实物流服务商联调、生产域名与 TLS、生产密钥托管、备份恢复演练、跨可用区容灾、生产容量压测、灰度与回滚演练。任何一项未完成时，不应宣称“生产上线验收完成”。
