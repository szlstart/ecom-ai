# 第二轮多路径主动验收实施方案

> 目标：不再只按一条“注册—购买—聊天”的正常顺序验收，而是从状态、并发、故障、跨端同步和自然语言变化等不同方向交叉测试。每个关键能力至少取得两类证据，发现问题后先补回归用例，再修复，再重跑相关层与全量门禁。

## 1. 验收原则

1. **先隔离、后真实**：会创建订单、商品、消息或人工工单的测试优先在隔离验收库运行；真实 `8080` 环境只做可恢复、非破坏性的冒烟验证。
2. **至少双证据**：关键功能不能只看页面提示，必须同时验证页面结果、接口/数据库状态、跨端可见性、运行日志中的至少两项。
3. **失败即资产**：每个新发现的问题先固化为可重复失败的自动化用例，再修复实现，避免同类问题再次出现。
4. **不依赖固定顺序**：测试随机入口、刷新、返回、新标签页、重复点击、上下文切换，验证系统状态而不是脚本偶然通过。
5. **安全边界优先**：跨用户、跨店铺、跨角色、越权参数和高风险写操作必须由服务端拒绝，不能只依赖前端隐藏按钮。
6. **如实分层结论**：单元、集成、浏览器、外部模型、容灾各自给结论；一层通过不能替代另一层。

## 2. 测试方法矩阵

| 方法 | 主要发现的问题 | 实施方式 | 通过标准 |
|---|---|---|---|
| 状态模型测试 | 某状态下按钮错误、非法状态转换 | 从订单、商品、会话、人工工单的各状态反向进入 | 页面动作与后端状态机允许动作一致 |
| 变形测试 | 不同入口结果不一致 | 商品页立即购买与购物车结算、刷新前后、同账号多标签页对比 | 等价操作得到等价业务结果 |
| 组合测试 | 角色与资源组合漏测 | 用户/商家/管理员 × 本人/本店/他人资源 × 正常/冻结/暂停状态 | 允许项成功，越权项稳定拒绝 |
| 并发与幂等 | 重复订单、重复扣款、重复领取 | 双标签同时提交、重复 `client_message_id`、重复回调、双管理员领取 | 最多一次生效，冲突有明确提示 |
| 异常注入与恢复 | 网络异常后数据丢失、按钮卡死 | 文件不合法后重试、Worker 短暂不可用、模型超时/降级 | 已填内容保留，可恢复，错误定位到字段/服务 |
| 对抗式 Agent 测试 | 意图误判、上下文丢失、越权 | 同义句、否定句、代词、多轮跳题再回指、恶意指令 | 正确路由、连续理解、权限不扩大 |
| 跨端一致性测试 | 一端已改另一端不更新 | 用户—商家—管理员同时在线观察订单、工单、店铺状态 | 无需地址栏回车即可在约定时间内更新 |
| 视觉与可访问性 | 遮挡、空白滚动、按钮不可点 | 多视口、键盘、焦点、元素命中点、截图 | 核心控件可见可点、无横向溢出、焦点清晰 |
| 数据清理与重放 | 测试污染、只能成功一次 | 测试数据带唯一前缀并在结束时清理，连续执行两次 | 两次结果一致，无残留业务数据 |

## 3. 本轮执行用例

### 3.1 Agent 与人工接管

| ID | 场景 | 前置数据 | 操作与变化 | 核心断言 | 清理 |
|---|---|---|---|---|---|
| PATH-AI-01 | 转人工表达变体 | 用户专属客服、店铺经营助理 | 分别输入“转人工”“再次请求平台人工”“请问如何申请人工”等正反例 | 只有真实请求创建工单；咨询规则、历史提及、否定表达不得误转 | 结束新工单 |
| PATH-AI-02 | 订单连续追问 | 用户有一笔可见订单 | “我买过什么”→选择卡片→“它到哪了”→“帮我退款” | 始终绑定所选订单；金额按元展示；操作以卡片/确认卡呈现 | 清除隔离会话 |
| PATH-AI-03 | 商品上下文切换 | 两个在售商品 | 发送 A 卡片并追问，再发送 B，随后问“刚才那件” | 当前对象切到 B；歧义回指会澄清，不凭空使用 A | 清除隔离会话 |
| PATH-AI-04 | 店内与全平台边界 | 店铺客服、专属客服 | 相同商品搜索问题分别询问两种 Agent | 店铺客服仅本店；专属客服可跨店；结果为可点击商品卡 | 清除隔离会话 |
| PATH-AI-05 | 双管理员竞争领取 | 一个等待人工工单、两个管理员会话 | 几乎同时点击领取 | 仅一个管理员成为处理人，另一端收到明确冲突并刷新状态 | 获胜方结束工单 |
| PATH-AI-06 | 人工结束后恢复 | 已领取工单 | 人工回复并结束，用户继续发消息 | 居中系统消息出现；AI 恢复并保留必要业务上下文 | 清除隔离会话 |

### 3.2 商品、文件与店铺

| ID | 场景 | 前置数据 | 操作与变化 | 核心断言 | 清理 |
|---|---|---|---|---|---|
| PATH-CAT-01 | 非法图片后恢复 | 新商品已有 SKU | 先选错误类型文件，再上传合法图片 | 错误就在上传区说明；其他编辑内容不丢；合法文件可继续扫描绑定 | 删除商品 |
| PATH-CAT-02 | 刷新/返回保留草稿 | 填写参数、详情、FAQ、图片 | 刷新、离开再返回、直接提交审核 | 自动保存或明确暂存；提交使用最新字段，无 `detail_content/fulfillment` 漏保存 | 删除商品 |
| PATH-CAT-03 | 店铺状态跨端同步 | 商家与管理员同时在线 | 任一端暂停/恢复营业 | 另一端无需地址栏回车更新；用户端可售状态一致 | 恢复营业 |
| PATH-CAT-04 | 有交易商品删除 | 已产生交易商品 | 商家和管理员分别尝试删除 | 不能物理删除，只能选择下架或取消 | 恢复原状态 |

### 3.3 交易、订单与物流

| ID | 场景 | 操作与变化 | 核心断言 |
|---|---|---|---|
| PATH-ORD-01 | 两种购买入口 | 商品页立即购买、购物车结算各走一次 | 均为弹窗；数量和总额联动；地址、包邮一致 |
| PATH-ORD-02 | 双击/双标签提交 | 同一结算会话重复提交 | 只创建一笔交易；库存只预占一次；返回同一结果或明确幂等冲突 |
| PATH-ORD-03 | 支付跨端可见 | 用户支付后同时观察三端 | 用户余额减少、商家待结算变化、订单三端可见且金额一致 |
| PATH-ORD-04 | 模拟物流时间线 | 支付后按模拟时钟推进 | 待揽收—运输—派送—签收顺序正确，地址粒度逐步变化 |
| PATH-ORD-05 | 已取消用户投影 | 用户取消订单 | 仅用户历史入口显示；商家/管理员订单不出现“已取消”；用户删除后物理清理 |

### 3.4 认证、权限与多标签

| ID | 场景 | 操作与变化 | 核心断言 |
|---|---|---|---|
| PATH-AUTH-01 | 同账号多标签 | 连续打开两个标签并交替操作 | 不互相挤下线，刷新后仍登录 |
| PATH-AUTH-02 | 三端身份隔离 | 用用户、商家、管理员凭证交叉登录 | 只能进入对应端；服务端令牌受众与角色也拒绝交叉访问 |
| PATH-AUTH-03 | 跨资源 IDOR | 替换商品、订单、会话、文件的公开 ID | 跨用户/跨店铺读取和写入均拒绝且不泄漏存在性 |
| PATH-AUTH-04 | 注销与撤销 | 注销/强制下线后复用旧会话 | 所有旧会话失效，相关个人数据按规则清理或匿名化 |

### 3.5 UI 与可访问性

| ID | 场景 | 核心断言 |
|---|---|---|
| PATH-UI-01 | 三端消息页桌面视口 | 页面本身无空白滚动；三栏不遮挡；输入框、发送、领取按钮命中点正确 |
| PATH-UI-02 | 消息页窄屏降级 | 会话列表、聊天、思考过程可切换；无横向溢出；输入控件始终可见 |
| PATH-UI-03 | 键盘流程 | Tab 顺序合理，弹窗焦点锁定，Escape/取消后错误提示清除 |
| PATH-UI-04 | 跨端即时反馈 | 保存、删除、领取、状态切换均就地反馈，不要求滚到页面顶部或手工刷新 |

## 4. 实施顺序

1. **建立基线**：记录 Git 状态、容器健康、API readiness、当前自动化数量。
2. **补最小回归用例**：优先覆盖转人工语言变体、领取竞争、非法上传恢复、消息控件命中点和跨端刷新。
3. **隔离环境执行**：运行新增用例及相关后端/前端测试，保留失败截图、Trace 和服务日志。
4. **修复循环**：每个失败执行“复现—根因—小补丁—定向测试—相邻回归”。
5. **真实页面抽查**：在 `8080` 用真实页面做非破坏性消息、导航、跨标签和视觉检查。
6. **全量验收**：执行格式、类型、后端、前端、浏览器、Agent 安全与需求证据门禁。
7. **连续重放**：新增的高风险用例至少连续执行两次，验证无顺序依赖和数据残留。

## 5. 失败升级标准

- P0：越权读取/写入、重复扣款、跨店数据泄漏、管理员身份混用。
- P1：订单或库存不一致、已填商品资料丢失、人工工单错投/重复领取、支付后跨端不可见。
- P2：Agent 上下文丢失、错误转人工、卡片不可操作、页面必须手工刷新才更新。
- P3：布局、文案、焦点、轻微延迟等不阻断主流程的问题。

P0/P1 未关闭时停止上线结论；P2 必须有修复或明确降级；P3 进入可追踪清单但不得被描述为“完美无缺”。

## 6. 最终交付证据

- 自动化用例名称、通过数与执行命令；
- 新发现问题的复现条件、根因、修复文件与回归用例；
- 三端关键页面截图或可复查的浏览器断言；
- API/Worker 日志中的错误扫描结果；
- 全量门禁结果和仍未被真实外部依赖验证的边界；
- 测试数据清理结果、Git 提交与远端分支状态。

## 7. 执行记录

当前状态：**本轮完成**。本节记录实际执行结果，不以计划代替测试结论。

### 7.1 本轮发现并关闭的问题

| 问题 | 根因 | 修复 | 回归证据 |
|---|---|---|---|
| “转到人工客服”未被识别，否定句和规则咨询存在误转风险 | 转人工识别只覆盖部分动词组合，且缺少否定与信息询问保护 | 扩充“转到/转给”等明确请求；否定表达优先拒绝；“如何/为什么/流程”等只读询问不创建工单 | 7 个正例、9 个反例参数化测试，两轮全量后端测试通过 |
| 两名管理员同时领取同一工单缺少显式并发验收 | 旧测试只有串行状态转换，不能证明竞争条件下唯一归属 | 使用两个独立数据库会话并发领取；断言一个成功、一个以 `412` 版本冲突失败，获胜方完成工单 | 两轮隔离全量验收均通过 |
| 非法图片只显示“文件类型不符合策略” | 前端只校验大小，MIME/扩展名依赖后端通用错误 | 上传前在本地校验 MIME 与扩展名，并保留已填商品名、已完成 SKU 和后续重试能力 | 真实浏览器先传 `text/plain`、再传 PNG，完成扫描、草稿恢复、发布和清理 |
| 移动端消息用例偶发超时 | 同一个隔离验收场景被桌面和移动项目并发使用，会竞争同一会话；窄屏列表已折叠却仍尝试点击列表项 | 连接真实后端时 Playwright 全局单 Worker；窄屏按实际“自动选中专属客服”状态验收 | 连续两轮移动端三端消息布局通过 |
| 商品数量按钮的读屏名称不明确 | “数量”标签和相邻控件文本共同参与名称计算 | 为减少和增加按钮增加独立 `aria-label` | 商城支付浏览器链路连续两轮通过 |

### 7.2 已执行的多路径证据

- **状态与跨端路径**：用户完成购物车结算和支付后，商家端、超级管理端均能看到同一订单；金额与状态一致。
- **失败恢复路径**：商家商品编辑先遇到非法文件，再上传合法文件；商品名、SKU、价格、库存不丢失，详情图片、参数、购买须知、FAQ 在离开再进入后仍保留。
- **Agent 与上下文路径**：用户专属客服、店铺 AI 经营助理、管理员 AI 管家均通过真实 Worker 运行，并产生可审计公开轨迹。
- **人工接管路径**：用户和商家分别请求平台人工，管理员可领取、回复、结束；另一端实时收到消息，结束后出现系统提示并恢复 AI 状态。
- **并发路径**：两个管理员数据库会话竞争同一待领取工单，只允许一个归属成功。
- **自然语言变体路径**：转人工同义句、否定句、规则询问、历史提及分别验证，不再依靠单一关键词。
- **窄屏路径**：用户、商家、管理员三个消息页在 Pixel 7 视口均无横向溢出，输入框可见，用户附件按钮不压入输入框，桌面思考栏按设计隐藏。
- **真实页面非破坏抽查**：在 `8080` 检查首页、消息页、商品页和立即购买弹窗；页面无控制台错误，商品购买区与结算信息可见且未提交真实订单。

### 7.3 连续重放结果

完成两轮彼此独立的 `make acceptance-test-isolated`，每轮使用新建的随机 MySQL 数据库、PostgreSQL 数据库、Redis DB 和 MinIO 前缀，结束后自动清理：

| 验收层 | 第一轮 | 第二轮 |
|---|---:|---:|
| 后端测试 | 446 通过、1 跳过，覆盖率 65.88% | 446 通过、1 跳过，覆盖率 65.86% |
| 前端组件测试 | 127 通过 | 127 通过 |
| 真实浏览器测试 | 19 通过、5 按设备能力跳过 | 19 通过、5 按设备能力跳过 |
| Agent 安全专项 | 18 通过 | 18 通过 |
| 需求证据 | 145 份，`decision=pass` | 145 份，`decision=pass` |

浏览器中的 5 个跳过均为显式设备分工：4 条桌面专用连接测试在移动项目跳过，1 条移动专用布局测试在桌面项目跳过，并非失败或未执行。

### 7.4 本轮结论边界

本轮覆盖了计划中风险最高且本次改动直接相关的 Agent 意图、人工接管、并发领取、商品失败恢复、三端交易可见性、桌面与窄屏 UI。表中未逐条自动化的长期容灾、真实第三方支付和跨区域故障不据此宣称已验证；它们仍应在正式上线环境的专项演练中验收。

### 7.5 当前运行环境复核

- Docker 已用本轮 Git 提交重新构建，API、前端、MySQL、PostgreSQL、Redis、MinIO、ClamAV 及全部 Worker 均处于 `running/healthy`，运行镜像 revision 与 Git 提交一致。
- 前端首页返回 `200`；新构建中的商品数量增减按钮在真实 `8080` 页面具有独立可访问名称。
- 向量模型探针返回正常。Agent 模型的 `/models` 可访问且包含配置的 `gpt-5.5`，但供应商曾连续对最小 `/responses` 探针返回 `503 system_cpu_overloaded`，因此当时 `/health/ready` 如实为 `degraded`。这是外部供应商资源过载，不是密钥缺失或容器未启动。
- 在同一过载窗口从真实用户消息页发送商品问题，Agent 随后的实际调用取得 `200`，页面完成流式回复并附可点击商品卡片，未出现“网络异常”。这证明当前重试/降级链路可恢复，但不能把第三方上游的瞬时健康状态承诺为始终可用。
