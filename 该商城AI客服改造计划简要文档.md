# 该商城 AI 客服改造计划简要文档

> 版本：v2.0（四类 Agent 统一架构与三类改造实施版）
> 本次范围：第 0—4 节及统一实施目标
> 改造目标：将目前偏关键词、单意图的客服流程，升级为符合本商城实际业务的“Supervisor + 领域 Agent + Skill + MCP Tool + RAG + Memory + 人工接管”体系。

## 0. 右栏 AI 思考与执行视图

### 0.1 设计目标

当前右栏不能继续使用“理解问题、查询数据、整理结果”等预制步骤模拟思考。它应由 Agent Runtime 的真实事件驱动，让开发者能够回答：AI 理解了什么、为什么这样安排、调用了谁、使用了什么数据、结果是否通过复核，以及为什么改变计划。

右栏展示**模型在 Agent Runtime 中实际输出的全部中文思考说明 + 实际执行事件 + 工具证据**。模型输出给 Runtime 的思考内容应逐段原样保存和流式展示，后端与前端不得二次缩写、改写、截断或按角色隐藏。系统提示词、模型供应商未返回的隐藏内部状态以及从未进入 Agent Runtime 的内容无法展示，也不得用虚构内容补齐。

### 0.2 展示模式

三端右栏全部采用**开发者模式**，不再区分简洁模式、开发者模式和运维审计模式。

| 统一模式 | 使用范围 | 默认展示 |
| --- | --- | --- |
| 开发者模式 | 用户端、店铺端、超级管理员端的全部 AI 会话 | 完整思考输出、任务计划、Agent 委派、Skill、Tool、RAG、Memory、实际参数、实际结果、权限判定、确认、返工、错误码、耗时、Token、成本和 Trace |

三端显示结构、详细程度和事件字段完全一致，不做角色级简化、二次摘要或展示内容脱敏。右栏完整显示**本次 Agent Run 实际接收到和产生的内容**；已经完成的步骤可以在视觉上折叠，但点击后必须能看到全部原始记录。数据归属检查仍在 Tool 调用前完成，因此某个 Agent Run 本身不会取得其他用户、其他店铺的越权数据；密码、密钥、Token 和支付凭据等字段必须在进入模型与 Trace 前从数据源排除，它们不是可展示的思考内容。

### 0.3 每轮必须记录的真实事件

| 事件 | 右栏显示内容 | 数据来源 |
| --- | --- | --- |
| 收到请求 | 用户本轮目标、消息类型、关联商品/订单卡片 | 消息服务 |
| 读取上下文 | 当前焦点、未完成任务、待确认动作、召回的记忆摘要 | Context Builder |
| 初步理解 | Agent 对诉求、约束、指代和不确定点的中文说明 | 模型本轮显式决策记录 |
| 制定或修改计划 | 子任务、依赖关系、并行项、为什么修改计划 | Supervisor |
| 选择 Agent | 被委派 Agent、目标、允许的 Skill 和工具范围 | Delegation Runtime |
| 选择 Skill | Skill 名称、版本、用途和选择原因 | Skill Registry |
| 调用工具 | 工具名称、实际调用参数、开始时间和调用原因 | Tool Gateway |
| 工具结果 | 成功/拒绝/失败/未知、关键结果摘要、数据版本和耗时 | Tool Gateway |
| RAG 检索 | 查询词、过滤范围、命中文档、版本、相似度和引用 | RAG Runtime |
| Memory 使用 | 本轮实际召回或准备写入的记忆内容、类型和授权状态 | Memory Runtime |
| Supervisor 复核 | 通过项、失败项、冲突、遗漏和事实优先级 | Verifier + Supervisor |
| 定向返工 | 退回哪个 Agent、原因、要求补充什么 | Supervisor |
| 等待确认 | 操作对象、影响、确认有效期和确认状态 | Approval Service |
| 最终回复 | 采用哪些事实和卡片、未完成事项 | Response Composer |

没有真实发生的步骤不得显示；不能为了让界面“看起来像在思考”而生成虚假的 Tool、RAG、返工或等待动画。

### 0.4 单步决策说明

每次 Supervisor 或领域 Agent 准备采取下一步行动时，应额外输出一份可保存、可评估的结构化决策说明：

| 字段 | 含义 | 示例 |
| --- | --- | --- |
| 当前目标 | 这一轮正在解决什么 | 清空当前用户购物车，并返回本人地址 |
| 已知事实 | 来自上下文或工具的可靠事实摘要 | 当前购物车有 4 件商品；用户有 2 个地址 |
| 不确定点 | 仍缺少什么，是否影响执行 | 清空属于删除操作，需要用户确认 |
| 下一步动作 | `respond`、`ask`、`delegate`、`tool_call`、`request_confirmation`、`replan` 或 `handoff` | `request_confirmation` |
| 选择理由 | 为什么此时选择该动作 | 已完成读取，继续执行会删除真实数据 |
| 预期结果 | 下一步成功后应获得什么 | 获得绑定当前购物车版本的确认凭证 |
| 置信度 | 当前理解和选择的可信程度 | 0.96 |

该说明必须来自本轮模型或执行器的实际决策，使用中文展示，不能由前端根据意图名称拼接固定句子。

### 0.5 右栏交互

| 交互 | 要求 |
| --- | --- |
| 实时更新 | 通过 SSE 推送实际事件；当前步骤展开，完成后可自动折叠 |
| 查看层级 | 按“Supervisor → 领域 Agent → Skill → Tool/RAG”树形展开 |
| 查看证据 | 可查看本轮实际工具参数、完整结构化结果、引用文档和业务数据版本 |
| 查看计划变化 | 保留旧计划并标记为什么修订，不能只显示最后计划 |
| 查看失败 | 显示稳定错误码、失败位置、是否重试以及最终如何降级 |
| 调试导出 | 所有人均可复制本次会话的完整 Trace JSON，用于复现测试；禁止进入 Trace 的系统机密自然不会出现在导出中 |
| 页面样式 | 白色背景、黑色正文、状态色区分运行/等待/成功/失败；不使用大面积黑色终端样式 |

每个事件必须携带 `trace_id`、`run_id`、`step_id`、父步骤、Agent/Skill/Tool 版本、时间和状态。消息、Agent、MCP、RAG、数据库与异步任务使用同一 Trace 链路。

### 0.6 “AI 思考中”的实时内容

右栏不能等最终答案生成后再一次性补写“思考过程”。Agent 运行期间通过 SSE 按真实发生顺序推送以下事件：

| SSE 事件 | 思考中显示的内容 |
| --- | --- |
| `agent.reasoning_summary.delta` | 当前 Agent 正在形成的中文分析与下一步选择，逐段流式显示 |
| `agent.plan.created` | 当前任务计划、依赖和预计并行项 |
| `agent.plan.revised` | 旧计划、修改后的计划，以及触发修改的新观察 |
| `agent.delegation.started/completed` | 委派对象、目标、允许能力和返回状态 |
| `agent.skill.selected` | 实际选择的 Skill、版本和选择原因 |
| `agent.tool.started/completed` | 实际工具、完整调用参数、完整结构化结果、调用原因和耗时 |
| `agent.rag.started/completed` | 检索范围、命中文档和引用；未调用 RAG 时不显示 |
| `agent.review.completed` | Supervisor 检查了什么、哪些通过、哪些需要补充或返工 |
| `agent.confirmation.required/resolved` | 为什么需要确认、用户确认或取消了什么 |
| `agent.run.completed/failed` | 完成依据、未完成项或具体失败位置 |

正在运行的当前步骤保持展开并显示流式光标；完成的步骤折叠为一行摘要，点击后查看详情。前端只渲染后端真实事件，不自行生成“正在理解、正在查询、正在整理”等假进度。

确认卡片持久化后，当前流式阶段必须立即结束，Run 状态转为 `waiting_confirmation`，右栏显示“等待确认”及已执行的实际工具轨迹，不得因未发出普通完成消息而无限显示“思考中”。

### 0.7 完整 Trace 的性能与保存

完整展示不等于一次性把全部历史事件加载到浏览器。系统不得删除或改写实际内容，但可以使用分页、虚拟列表和按需加载保证界面流畅。

| 项目 | 首期设计 |
| --- | --- |
| 当前 Run | SSE 实时推送，当前步骤自动展开 |
| 历史 Run | 先加载步骤索引，展开时读取完整思考、参数和结果 |
| 长 Tool/RAG 结果 | 分页显示完整内容，单页建议 100 条；不截断底层记录 |
| 前端渲染 | 使用虚拟列表，只渲染当前可见事件；右栏独立滚动 |
| 实时事件缓冲 | Redis 保存 24 小时，支持断线续传和 `Last-Event-ID` |
| 热查询索引 | MySQL 保存 Run、步骤、状态、版本、耗时和对象存储引用，默认 90 天 |
| 完整 Trace 归档 | 对象存储保存不可变 JSON，默认 180 天 |
| L2/L3 写操作审计 | 默认保存 365 天，包括确认、执行和回读证据 |
| 清除聊天记录 | 不再将旧 Trace 用作上下文；仍按审计期限保存，不能恢复成聊天记忆 |
| 容量保护 | 按用户和 Run 限制并发、事件数量和单事件大小；超限结果转对象存储分页读取 |

保存期限必须可通过环境配置调整。归档清理任务写入审计日志，不能因清理失败影响正常对话。

## 1. 角色

### 1.1 人类角色

| 角色 | 现有系统角色码 | 使用端 | 可以管理的数据 |
| --- | --- | --- | --- |
| 商城用户 | `user` | 用户端 | 本人资料、地址、余额、收藏、购物车、订单、物流、售后和消息 |
| 店铺运营人员 | `store_operator` | 店铺端 | 被授权店铺的资料、商品、库存、订单、评价、顾客消息和经营数据 |
| 平台超级管理员 | `platform_super_admin` | 超级管理端 | 平台授权范围内的用户、店铺、商品、交易、售后、AI 和系统数据 |
| 店铺人工客服 | 由店铺运营人员承担 | 店铺端“消息” | 当前店铺顾客会话及处理该会话所需的本店业务数据 |
| 平台人工客服 | 现阶段由平台管理员承担 | 超级管理端“消息” | 用户专属客服和商家平台服务产生的工单数据 |

### 1.2 三端 AI 角色

| 页面显示名 | 完整名称 | 新 Agent 代码 | 服务对象 | 核心范围 |
| --- | --- | --- | --- | --- |
| 专属客服 | 专属客服 Supervisor Agent（用户服务总管） | `user_exclusive_support_supervisor` | 商城用户 | 全平台选购及当前用户本人的购物车、订单、物流、售后、账户和平台规则 |
| 店铺 AI 客服 | 店铺 AI 客服 Supervisor Agent（店铺顾客服务总管） | `store_customer_support_supervisor` | 咨询某店铺的用户 | 当前店铺商品、库存、政策及用户在该店铺的订单与物流 |
| AI 经营助理 | AI 经营助理 Supervisor Agent（店铺经营总管） | `merchant_operations_supervisor` | 店铺运营人员 | 当前店铺的商品、库存、订单、评价、顾客服务和经营分析 |
| AI 管家 | AI 管家 Supervisor Agent（平台治理总管） | `platform_governance_supervisor` | 平台超级管理员 | 用户、店铺、交易、售后、风险、AI 和系统运行治理 |

现有代码中的 `exclusive_support`、`store_support`、`merchant_copilot`、`admin_copilot` 是旧代码名。改造时应建立版本映射并迁移，页面和新代码统一使用上表名称，避免长期存在两套命名。

### 1.3 系统组件角色

| 组件 | 主要职责 | 是否直接执行业务写操作 |
| --- | --- | --- |
| Supervisor Agent | 理解请求、联系上下文、拆分任务、选择领域 Agent、安排并行、复核结果、要求返工、组织最终回复 | 否 |
| 领域 Agent | 在一个业务领域内分析问题、调用 Skill 和获准工具、返回结构化结果 | 否，必须经 Tool Gateway |
| Skill | 提供可测试、可版本化的领域说明、输入输出要求和可用工具集合，由 Agent 按任务动态选择 | 否 |
| MCP Tool / 内部 Tool | 调用 FastAPI 后端业务 Service，查询或修改真实业务数据 | 可以，但必须经过权限与确认检查 |
| Agent 权限与安全网关 | 注入登录身份，检查资源归属、工具白名单、状态、金额、版本、确认凭证和幂等 | 负责拦截，不负责业务决策 |
| 人工服务协调器 | 创建工单、分配人工、暂停 AI、结束人工服务后恢复 AI | 按工单状态机执行 |

统一调用关系：

```text
用户消息/卡片 → Supervisor → 领域 Agent → Skill → Tool Gateway → FastAPI Service
                              ↘ RAG / Memory
Supervisor ← 结构化结果 ← 复核与回读验证 ←──────────────┘
```

### 1.4 三端权限边界

| AI | 商品范围 | 订单范围 | 主要人工接管对象 | 禁止范围 |
| --- | --- | --- | --- | --- |
| 专属客服 | 全平台公开在售商品 | 当前用户本人全部订单 | 平台人工客服 | 他人数据、店铺内部数据、平台管理数据 |
| 店铺 AI 客服 | 当前店铺公开商品 | 当前用户在当前店铺的订单 | 当前店铺人工客服 | 跨店数据、其他顾客数据 |
| AI 经营助理 | 当前运营人员获授权的店铺商品 | 当前店铺订单 | 平台人工客服 | 其他店铺内部数据、平台治理操作 |
| AI 管家 | 平台授权范围 | 平台授权范围 | 对应用户或店铺的人工服务人员 | 密码、密钥、Token；绕过审批和业务状态机 |

所有 Agent 均不得直接连接数据库，也不得采用模型从聊天文字中猜测的用户 ID、店铺 ID 或订单归属。身份和范围只能由后端认证上下文注入。

### 1.5 四类 Supervisor 必须复用的运行基线

本节是专属客服、店铺 AI 客服、AI 经营助理和 AI 管家的共同约束。后续设计 2.2、3.1、4.1 时必须直接复用，避免再次出现“对话越长越容易丢上下文”的问题。

| 基线 | 统一要求 |
| --- | --- |
| 会话状态 | 每个会话独立保存版本化 `ConversationState`；当前焦点、最近卡片集合、待回答问题、待确认操作、任务和纠正记录必须分字段保存，不能只依赖聊天原文 |
| 上下文构建 | 每轮只装配系统边界、身份权限、当前状态、滚动摘要、少量相关原文和本轮证据；禁止把整段历史重复塞给模型 |
| 事实边界 | 商品库存、金额、购物车、订单、物流和售后来自业务主库 Tool；平台规则来自已发布 RAG；长期记忆只保存用户确认的稳定偏好 |
| 模型与规则分工 | 模型负责理解、拆分、选择 Agent 和重新规划；确定性代码只负责权限、归属、状态机、确认、幂等，以及“不要执行、只试算”等不可越过的安全约束 |
| 目标账本 | 模型 Supervisor 必须把本轮每个独立诉求写入 `goal_ledger`，为每项目标绑定实际子任务，并在返回前声明 `coverage_complete=true`；账本缺失、映射无效或覆盖不完整时整份模型计划作废并降级，后端不得用关键词静默补任务后伪称模型规划成功 |
| 渐进式披露 | Supervisor 先看到紧凑能力目录，只向被选中的领域 Agent 注入该任务需要的 Skill 和最小 Tool 白名单；不得每轮暴露全部工具 Schema |
| RAG | 先按租户、店铺、商品、状态和文档类型过滤，再混合检索；命中小切片后可在同一语义父段内扩展，禁止跨标题、跨商品拼接 |
| 并发与写入 | 相互独立的只读任务可并行；写操作按资源串行，写前重新读取版本，写后回读验证；所有消息写入使用会话序号锁 |
| 清除记录 | 清除聊天时同时清除该会话的短期状态、摘要引用、待确认操作和焦点；长期偏好只有在用户明确删除时才删除 |
| 右栏轨迹 | 只显示模型服务商实际提供的推理摘要和系统真实产生的计划、委派、Tool、RAG、Memory、确认及验证事件；不伪造不可取得的隐藏思维链 |

四类 Supervisor 可以拥有不同领域 Agent 和权限，但不得各自重新发明状态结构、压缩算法、消息顺序、审计协议或 RAG 安全边界。

## 2. 用户端

| 用户端 AI | 会话形式 | 适合处理的问题 |
| --- | --- | --- |
| 专属客服 | 每个用户固定一个置顶会话 | “帮我找商品”“购物车里有什么”“我的订单到哪了”“帮我申请退款”“把我的地址发给我” |
| 店铺 AI 客服 | 每个用户与每个店铺最多一个有效会话 | “这个商品有什么款式”“还有库存吗”“本店用什么快递”“我在你店买过什么” |

两种客服共享消息、流式输出和卡片组件，但数据范围、工具权限、RAG 过滤条件及人工接管对象必须隔离。

### 2.1 专属客服

#### 2.1.1 定位

| 项目 | 设计 |
| --- | --- |
| 页面显示名 | 专属客服 |
| 主 Agent 全称 | 专属客服 Supervisor Agent（用户服务总管） |
| Agent 代码 | `user_exclusive_support_supervisor` |
| 服务对象 | 当前已登录商城用户 |
| 会话规则 | 每个用户一个固定置顶会话，多用户之间完全隔离 |
| 核心能力 | 理解连续对话和复合请求，调用领域 Agent，使用真实业务工具，以卡片形式给出结果和操作 |
| 人工入口 | 转平台人工客服；人工接管期间 AI 停止向用户自动回复 |

专属客服不能只是“问一句、匹配一个关键词、回复固定文字”。正常业务问题必须由模型结合当前消息、最近对话和已绑定的商品/订单卡片进行理解；关键词规则只用于安全拦截、明确控制命令和模型故障降级。

#### 2.1.2 Supervisor Agent 职责

| 阶段 | 职责 | 本商城中的要求 |
| --- | --- | --- |
| 理解 | 找出一条消息里的全部诉求、条件和指代 | “删购物车并发地址”必须识别为两个任务；“它、这个、好”必须承接上一轮 |
| 澄清 | 只补问真正缺失的信息 | 多个订单都可能匹配时展示订单卡片让用户选，不反复让用户重述 |
| 规划 | 生成有依赖关系的任务计划 | 独立查询并行；同一资源的写操作串行 |
| 确认 | 在产生副作用前展示操作对象和影响 | 清空购物车、删地址、取消订单、确认收货、提交退款必须确认 |
| 派发 | 选择最少数量的领域 Agent | 一个 Agent 能完成时不使用 Multi-Agent；不为单个工具创建 Agent |
| 复核 | 检查完整性、归属、金额、版本和执行状态 | 防止漏掉第二个诉求、跨用户、¥6 变 ¥600、旧数据覆盖新数据 |
| 返工 | 将具体缺失项退回原领域 Agent | 最多返工 2 次，仍失败则返回可靠的部分结果 |
| 回复 | 使用简短文字和可操作卡片 | 不把内部订单号、数据库字段或大段原始数据直接扔给用户 |

#### 2.1.3 Agent 化运行方式

本系统采用**有边界的动态 Agent 循环**，而不是为每种问题预先画一条固定 Workflow。Supervisor 每取得一项新观察，都可以重新判断下一步；任务计划是可修改的工作假设，不是必须照走的脚本。

| Agent 循环阶段 | Agent 自主完成的事情 | 可能选择的下一步 |
| --- | --- | --- |
| Observe 观察 | 读取用户消息、卡片、短期上下文、当前任务和最新工具结果 | 进入分析 |
| Reason 分析 | 判断目标、全部诉求、指代、不确定点、风险和完成条件，并生成右栏决策说明 | 直接回答或继续行动 |
| Decide 决策 | 动态选择最有价值的下一步，不依赖固定意图分支 | 回答、追问、委派、调用工具、请求确认、改计划、转人工 |
| Act 行动 | 调用一个或多个领域 Agent、Skill、Tool、RAG 或 Memory | 获得新的观察 |
| Critique 复核 | 检查任务是否完整、证据是否可靠、是否越权、是否真正完成 | 结束、补查、返工或降级 |

| Agent 化要求 | 具体设计 |
| --- | --- |
| 动态规划 | Supervisor 可以增加、删除、合并、暂停或重排子任务，右栏保留修改原因 |
| 动态选工具 | 领域 Agent 根据目标和观察选择获准工具，可以连续调用多个工具，不由关键词固定绑定一个工具 |
| 动态结束 | 达到用户目标且通过复核时结束；证据不足时追问或拒答，不强制走完预设步骤 |
| 最少委派 | 简单问候或单一问题可由 Supervisor 直接回答；只有专业业务事实才委派相应 Agent |
| 结果驱动 | 工具返回的新事实可以改变原计划，例如发现多个订单后改为请用户选择 |
| 受控自主 | 模型负责理解和决策；权限、状态机、确认、幂等、金额校验和资源归属由后端强制执行 |

实施时采用自研 Agent Runtime：Supervisor 每轮根据新观察动态决策，由 `MultiAgentOrchestrator` 执行有范围、有预算、可追踪的委派，由检查点与确认服务处理中断恢复。不强制依赖 LangGraph，也不为“查订单、清购物车、问物流”等意图分别写死流程。写操作的预览和确认是安全协议，不是整个客服的固定 Workflow。

#### 2.1.4 领域 Agent 设计

专属客服保留 7 个领域 Agent。推荐归入商品选购，收藏归入账户资产，记忆作为公共受控能力，不再分别建立 Agent，从而减少无效委派和模型调用。

| 完整名称 | Agent 代码 | 负责范围 | 主要 Skill | 主要结果组件 |
| --- | --- | --- | --- | --- |
| 用户商品选购 Agent | `user_catalog_shopping_agent` | 全平台商品搜索、需求筛选、推荐、商品详情、SKU 比较、库存 | 商品发现、商品比较、选购建议 | 商品列表、商品卡片、规格对比卡片 |
| 用户购物车与结算 Agent | `user_cart_checkout_agent` | 购物车查询、改数量、移除、清空、选择商品、结算预检 | 购物车管理、结算准备 | 购物车卡片、变更确认卡片、结算预览 |
| 用户订单与评价 Agent | `user_order_review_agent` | 本人订单列表、详情、状态、取消、确认收货、再次购买、评价和追评 | 订单查询、订单操作、评价协助 | 订单列表、订单卡片、评价草稿和操作卡片 |
| 用户物流履约 Agent | `user_logistics_fulfillment_agent` | 本人包裹、物流轨迹、当前位置、预计送达和签收 | 物流跟踪 | 物流卡片、物流时间线 |
| 用户售后退款 Agent | `user_after_sales_refund_agent` | 售后资格、退款草稿、提交申请、进度和撤销申请 | 售后预检、退款申请、售后查询 | 售后资格卡片、退款确认卡片、进度卡片 |
| 用户账户资料与资产 Agent | `user_account_asset_agent` | 本人资料、收货地址、余额、余额流水、商品收藏、店铺收藏 | 账户查询、地址管理、资产与收藏管理 | 地址卡片、余额卡片、收藏列表 |
| 平台规则与知识 Agent | `user_platform_policy_knowledge_agent` | 平台购物、支付、余额、物流、签收、售后和客服规则 | 平台规则问答、来源引用 | 简短规则回答、来源卡片 |

`platform_human_service_coordinator` 是人工服务协调器，不是 LLM Agent。它只负责平台工单创建、排队状态、暂停 AI 和人工结束后恢复 AI。

#### 2.1.5 工具种类

| 类型 | 含义 | 是否需要确认 |
| --- | --- | --- |
| `query` | 查询当前业务主库中的真实数据 | 否 |
| `retrieval` | 检索已发布且符合权限范围的 RAG 知识 | 否 |
| `preview` | 计算操作资格、对象和影响，不修改数据 | 否，但必须显示“尚未执行” |
| `mutation` | 修改真实业务数据 | 按 L1—L3 操作等级决定 |
| `memory` | 读取或管理长期偏好 | 读取不需要，写入和删除需要授权 |
| `handoff` | 创建、取消或结束人工服务工单 | 用户明确要求立即转人工时可视为确认 |

UI Action 不是 Tool。它只负责打开页面、弹窗或选择器，不进入 MCP Tool 白名单，也不能修改业务数据。

#### 2.1.6 工具清单

“已有”表示当前专属客服已有执行能力；“需接入”表示商城已有相关注册项或 FastAPI 业务接口，但尚未成为专属客服可可靠调用的工具；“需新增”表示需要补充 Tool Gateway 包装或业务能力。

| 领域 | 工具 | 类型 | 作用 | 状态 |
| --- | --- | --- | --- | --- |
| 商品 | `catalog.search_products` | `query` | 搜索全平台公开在售商品 | 已有，需改为 Supervisor 生成语义检索条件 |
| 商品 | `catalog.get_product` | `query` | 读取商品、款式、参数、FAQ、详情文字和图片 OCR 说明 | 需接入 |
| 商品 | `catalog.compare_products` | `query` | 对比多个商品的价格和公开参数 | 已有 |
| 商品 | `catalog.compare_skus` | `query` | 对比同一商品的款式、价格和库存 | 需接入 |
| 商品 | `catalog.get_inventory_availability` | `query` | 查询 SKU 实时可售库存 | 需接入 |
| 商品 | `store.search_public` | `query` | 按店铺名称搜索营业中的公开店铺 | 需新增 Agent Tool，复用现有店铺接口 |
| 商品 | `store.get_public` | `query` | 查询店铺资料、营业状态和公开服务政策 | 需新增 Agent Tool，复用现有店铺接口 |
| 商品 | `catalog.search_store_products` | `query` | 查询指定店铺内的公开在售商品 | 已注册，需接入专属客服 |
| 商品 | `review.get_product_summary` | `query` | 查询商品评分、评价数量和公开评价摘要 | 需新增 Agent Tool，复用现有评价接口 |
| 购物车 | `cart.get_mine` | `query` | 查询本人购物车、选中状态和实时有效性 | 已有 |
| 购物车 | `cart.add_item` | `mutation` | 校验公开在售商品、款式、数量、库存和版本后加入本人购物车 | 已有；用户本轮明确要求即可执行并回读 |
| 购物车 | `cart.update_quantity` | `mutation` | 校验购物车项归属、库存和版本后修改数量 | 已有；用户本轮明确要求即可执行并回读 |
| 购物车 | `cart.remove_item` | `mutation` | 校验购物车项归属和版本后移除指定项 | 已有；用户本轮明确要求即可执行并回读 |
| 购物车 | `cart.clear.preview` | `preview` | 读取全部购物车项并生成清空影响 | 已有；展示影响范围、数量与有效期 |
| 购物车 | `cart.clear.commit` | `mutation` | 按有效确认清空全部购物车；不是只清失效项 | 已有；必须点击确认卡片并校验版本与幂等 |
| 购物车 | `cart.replace_selection` | `mutation` | 修改购物车商品选中状态 | 需接入 |
| 结算 | `checkout.create_session` | `mutation` | 根据直接购买或购物车选择创建临时结算会话 | 已有；只创建临时预览，不创建订单、不付款 |
| 结算 | `checkout.refresh_preview` | `preview` | 重新校验库存、地址、包邮和应付金额 | 需接入现有结算接口 |
| 结算 | `order.create.preview` | `preview` | 根据结算会话展示商品、地址、分店铺订单和应付总额 | 需新增 Agent Tool |
| 结算 | `order.create.commit` | `mutation` | 按有效确认创建订单并执行库存预占 | 需新增 Agent Tool，复用现有订单接口 |
| 订单 | `order.list_user_orders` | `query` | 查询本人订单并按状态、店铺和时间筛选 | 已有 |
| 订单 | `order.get_user_order_detail` | `query` | 查询本人指定订单详情及实时可用操作 | 已有 |
| 订单 | `order.cancel.preview` | `preview` | 检查本人订单取消资格及影响 | 需新增 Agent Tool |
| 订单 | `order.cancel.commit` | `mutation` | 按有效确认取消本人订单 | 需新增 Agent Tool，复用现有订单接口 |
| 订单 | `order.confirm_receipt.preview` | `preview` | 检查包裹状态并生成确认收货预览 | 需新增 Agent Tool |
| 订单 | `order.confirm_receipt.commit` | `mutation` | 按有效确认完成本人订单确认收货 | 需新增 Agent Tool，复用现有订单接口 |
| 订单 | `order.repurchase.preview` | `preview` | 检查历史商品是否仍在售并生成再次购买入口 | 需新增 |
| 评价 | `review.check_eligibility` | `query` | 检查订单商品是否可以评价或追评 | 需新增 Agent Tool，复用现有评价接口 |
| 评价 | `review.build_draft` | `preview` | 根据用户明确表达整理评价草稿，不替用户捏造体验 | 已有；只生成草稿卡片，不设置星级、不代提交 |
| 评价 | `review.submit.commit` | `mutation` | 按用户确认提交评价 | 需新增 Agent Tool，复用现有评价接口 |
| 评价 | `review.append.commit` | `mutation` | 按用户确认提交追评 | 需新增 Agent Tool，复用现有评价接口 |
| 文件 | `file.create_upload_session` | `mutation` | 为评价图片、退款凭证或用户图片创建上传会话 | 需新增 Agent Tool，复用现有文件接口 |
| 文件 | `file.complete_upload` | `mutation` | 完成上传并返回可信文件引用 | 需新增 Agent Tool，复用现有文件接口 |
| 文件 | `file.get_scan_status` | `query` | 查询病毒、类型和内容安全扫描状态 | 需新增 Agent Tool，复用现有文件接口 |
| 图片 | `vision.inspect_user_image` | `query` | 理解用户主动发送的商品截图或凭证图片 | 需新增；图片文字只能作为不可信业务资料 |
| 评价 | `review.attach_images` | `mutation` | 将已通过扫描的图片绑定到评价草稿 | 需新增 Agent Tool |
| 物流 | `logistics.get_user_order_shipments` | `query` | 查询本人订单包裹和最新物流节点 | 已有 |
| 物流 | `logistics.get_shipment_tracks` | `query` | 查询本人包裹完整物流时间线 | 需接入现有物流接口 |
| 售后 | `after_sale.check_refund_eligibility` | `query` | 检查售后时限、状态、数量和可退金额 | 已有 |
| 售后 | `after_sale.build_refund_draft` | `preview` | 生成服务端退款草稿和确认内容 | 已有 |
| 售后 | `after_sale.submit_refund_application` | `mutation` | 提交用户确认过的退款草稿 | 已有确认框架，需接入新 Supervisor |
| 售后 | `after_sale.list_user_refunds` | `query` | 查询本人售后列表 | 已有 |
| 售后 | `after_sale.get_user_refund_detail` | `query` | 查询本人售后进度和事件 | 已有 |
| 售后 | `after_sale.cancel_refund.preview` | `preview` | 检查售后申请是否允许撤销 | 需新增 Agent Tool |
| 售后 | `after_sale.cancel_refund.commit` | `mutation` | 按有效确认撤销售后申请 | 需新增 Agent Tool，复用现有售后接口 |
| 售后 | `after_sale.build_appeal_draft` | `preview` | 为被拒售后整理申诉原因和已有材料 | 需新增 Agent Tool |
| 售后 | `after_sale.submit_appeal.commit` | `mutation` | 按用户确认提交售后申诉 | 需新增 Agent Tool，复用现有申诉接口 |
| 售后 | `after_sale.attach_evidence` | `mutation` | 将已通过扫描的图片绑定到售后或申诉草稿 | 需新增 Agent Tool |
| 账户 | `account.get_profile` | `query` | 查询本人用户名、头像和脱敏邮箱 | 已有；与地址查询可在同一轮并行返回 |
| 地址 | `address.list_mine` | `query` | 查询本人收货地址并生成地址卡片 | 已有 |
| 地址 | `address.create.preview` | `preview` | 校验地区、收货人和联系方式，生成新增预览 | 需新增 Agent Tool |
| 地址 | `address.create.commit` | `mutation` | 按有效确认新增本人收货地址 | 需新增 Agent Tool，复用现有地址接口 |
| 地址 | `address.update.preview` | `preview` | 展示本人地址修改前后差异 | 需新增 Agent Tool |
| 地址 | `address.update.commit` | `mutation` | 按有效确认修改本人地址 | 需新增 Agent Tool，复用现有地址接口 |
| 地址 | `address.delete.preview` | `preview` | 生成本人地址删除预览及默认地址影响 | 需新增 Agent Tool |
| 地址 | `address.delete.commit` | `mutation` | 按有效确认删除本人地址 | 需新增 Agent Tool，复用现有地址接口 |
| 地址 | `address.set_default` | `mutation` | 设置本人默认收货地址 | 需接入现有地址接口 |
| 资产 | `account.wallet.get_mine` | `query` | 查询本人余额和币种 | 已有 |
| 资产 | `wallet.list_transactions` | `query` | 查询本人脱敏余额流水 | 已有；余额与最近流水使用实时账户数据 |
| 收藏 | `account.favorites.list_mine` | `query` | 一次查询本人收藏商品和关注店铺 | 已有 |
| 收藏 | `favorite.add_product` | `mutation` | 收藏指定公开商品 | 已有；校验商品公开状态与当前用户 |
| 收藏 | `favorite.remove_product` | `mutation` | 取消收藏指定商品 | 已有；执行后回读收藏结果 |
| 收藏 | `favorite.add_store` | `mutation` | 收藏指定店铺 | 已有；校验公开店铺 ID，禁止模型覆盖数据范围 |
| 收藏 | `favorite.remove_store` | `mutation` | 取消收藏指定店铺 | 已有；执行后回读收藏结果 |
| 规则 | `rag.policy.search` | `retrieval` | 检索已发布的平台规则并返回来源 | 已有 |
| 记忆 | `memory.list_mine` | `memory` | 读取与当前任务相关的已授权偏好 | 已有 |
| 记忆 | `memory.remember_preference` | `memory` | 保存用户确认后的非敏感稳定偏好 | 已有确认策略，需完善执行器 |
| 记忆 | `memory.delete_mine` | `memory` | 删除本人指定偏好记忆 | 已有确认策略，需完善执行器 |
| 人工 | `support.create_platform_ticket` | `handoff` | 创建平台人工工单并暂停 AI | 已有 |
| 人工 | `support.get_ticket_status` | `query` | 查询排队、接管或已结束状态 | 已有 |

不向专属客服提供支付、直接扣款、密码修改、邮箱换绑、充值、账户注销工具；它只能提供对应安全页面入口。

#### 2.1.7 Skill 设计

| Skill 代码 | 使用 Agent | 处理内容 | 主要工具 |
| --- | --- | --- | --- |
| `user_product_discovery` | 商品选购 Agent | 搜索、推荐、详情和连续追问 | `catalog.search_products`、`catalog.get_product`、`catalog.get_inventory_availability` |
| `user_product_comparison` | 商品选购 Agent | 商品或 SKU 对比 | `catalog.compare_products`、`catalog.compare_skus` |
| `user_cart_management` | 购物车与结算 Agent | 查询、增加、改数量、移除和清空 | `cart.get_mine` 及对应完整 `preview`、`commit` 工具 |
| `user_checkout_preparation` | 购物车与结算 Agent | 创建结算会话、检查库存地址金额、创建订单并返回支付入口 | `checkout.create_session`、`checkout.refresh_preview`、`order.create.preview`、`order.create.commit`；完成后返回 `ui.payment_panel.open` |
| `user_order_assistance` | 订单与评价 Agent | 订单列表、详情、取消和确认收货 | `order.list_user_orders`、`order.get_user_order_detail` 及订单操作工具 |
| `user_review_assistance` | 订单与评价 Agent | 评价资格、评价草稿、图片、提交和追评 | `review.check_eligibility`、`review.build_draft`、`review.attach_images`、`review.submit.commit`、`review.append.commit` |
| `user_logistics_tracking` | 物流履约 Agent | 包裹和物流时间线 | `logistics.get_user_order_shipments`、`logistics.get_shipment_tracks` |
| `user_after_sale_assistance` | 售后退款 Agent | 资格、草稿、凭证、提交、进度、撤销和申诉 | `after_sale.check_refund_eligibility`、`after_sale.build_refund_draft`、`after_sale.attach_evidence` 及售后写工具 |
| `user_attachment_understanding` | 商品、评价或售后 Agent | 上传、扫描并理解用户主动发送的图片 | `file.create_upload_session`、`file.complete_upload`、`file.get_scan_status`、`vision.inspect_user_image` |
| `user_account_assistance` | 账户资料与资产 Agent | 地址、余额、收藏和资料查询 | `account.get_profile`、`address.*`、`wallet.*`、`favorite.*` |
| `user_platform_policy_qa` | 平台规则与知识 Agent | 平台规则问答和来源引用 | `rag.policy.search` |
| `user_platform_handoff` | Supervisor + 协调器 | 转平台人工、查询排队状态 | 工单工具 |

#### 2.1.8 确认与执行规则

| 等级 | 操作性质 | 本商城操作 | 用户交互 |
| --- | --- | --- | --- |
| L0 | 只读 | 查商品、店铺、评价、购物车、订单、物流、售后、地址和余额 | 不确认，Agent 直接查询 |
| L1 | 轻量且容易恢复 | 收藏/取消收藏、购物车选择、创建临时结算会话 | 用户本轮明确文字或点击按钮即构成本次授权，不再弹确认 |
| L2 | 重要修改 | 加入购物车、修改数量、移除单件商品、新增/修改地址、提交评价 | 展示简短确认卡片，说明对象和变化 |
| L3 | 高影响操作 | 清空购物车、删除地址、取消订单、确认收货、创建订单、提交退款、撤销售后、提交申诉 | 展示完整影响、金额或数量及有效期，必须明确确认 |
| L4 | 不向 Agent 开放 | 代替付款、修改密码、邮箱换绑、充值、注销账户 | 只返回安全页面或弹窗入口 |

确认等级写入 Tool Contract，由后端执行，模型不能自行降低等级。同一复合请求中的不同操作分别判断，例如“收藏商品并清空购物车”中收藏按 L1，清空按 L3。

| 用户请求 | 派发方式 | 用户看到的结果 |
| --- | --- | --- |
| “我的订单有哪些？” | 直接派发用户订单与评价 Agent | 可点击订单卡片，不以内部订单号作为主体 |
| “购物车有什么？地址也发我” | 购物车 Agent 与账户资料 Agent 并行查询 | 购物车卡片 + 脱敏地址卡片 |
| “把购物车清空” | 先查购物车并生成预览，用户确认后执行，最后回读验证 | 清空确认卡片 → 完成结果卡片 |
| “帮我退款”且只有一个符合条件的订单 | 用户订单与评价 Agent 定位订单，用户售后退款 Agent 生成草稿，用户确认后提交 | 订单卡片 + 退款草稿 + 确认按钮 |
| “帮我退款”但有多个候选订单 | 查询后暂停，让用户从订单卡片中选择 | 候选订单卡片 |
| “它到哪里了？” | 从上一轮唯一订单或订单卡片续接，再派发物流 Agent | 物流状态和时间线卡片 |
| “我要找人工” | 创建平台人工工单并暂停 AI | 居中系统消息 + 排队状态 |

当 Agent 自主决定需要执行 L2 或 L3 写操作时，必须遵守以下安全协议：

```text
读取最新数据 → 生成操作预览 → 用户确认 → 执行写工具 → 回读验证 → 告知真实结果
```

确认凭证必须绑定当前用户、会话、业务对象、资源版本和操作指纹；用户修改操作范围或数据版本变化后，原确认自动失效。

这只是写操作边界，不限制 Agent 在执行前进行多少次查询、比较、澄清、重新规划或跨领域协作。

#### 2.1.9 复合请求执行示例

用户说：“帮我把购物车里面的内容都删了，并且把我的收货地址发给我。”

| 顺序 | 执行者 | 动作 | 是否产生修改 |
| --- | --- | --- | --- |
| 1 | Supervisor | 识别“清空购物车”和“查询地址”两个任务 | 否 |
| 2 | 购物车 Agent + 账户资料与资产 Agent | 并行读取购物车和本人地址 | 否 |
| 3 | Supervisor | 复核归属和数量，展示地址卡片及购物车清空确认卡片 | 否 |
| 4 | 用户 | 点击“确认清空” | 形成确认凭证 |
| 5 | 购物车 Agent | 调用 `cart.clear.commit` | 是 |
| 6 | 购物车 Agent | 再次调用 `cart.get_mine` 验证购物车为空 | 否 |
| 7 | Supervisor | 返回“已清空”结果卡片；若失败则说明具体失败原因 | 否 |

#### 2.1.10 上下文与记忆

| 内容 | 保存位置与期限 | 使用规则 |
| --- | --- | --- |
| 最近对话和滚动摘要 | 短期会话上下文 | 保留上一轮问题、用户回答、当前唯一商品/订单和未完成任务 |
| 商品、订单、包裹等引用 | 当前会话资源上下文 | 每次使用时重新检查用户归属和最新业务状态 |
| 待确认操作 | Agent Run 检查点 | 绑定确认凭证；过期、修改范围或版本变化后失效 |
| 选购偏好 | 用户授权的长期记忆 | 只保存稳定、非敏感偏好，例如预算和风格 |
| 订单、库存、物流和余额 | 不写入长期记忆 | 每次通过业务工具实时查询 |
| 密码、完整邮箱、完整地址、支付凭据 | 禁止记忆 | 只在获准的业务接口中按需使用，不进入 Prompt 和长期记忆 |

上下文按模型实际窗口动态分配，以下百分比是首期默认预算，未使用额度可由 Supervisor 重新分配：

| 上下文组成 | 默认预算 | 压缩规则 |
| --- | ---: | --- |
| 系统规则和安全边界 | 10% | 不压缩、不截断 |
| 可信身份、权限和工具声明 | 5% | 不接受模型改写 |
| 当前目标、任务、焦点和待确认状态 | 10% | 不压缩关键字段 |
| 最近有效消息 | 15% | 保留与当前目标直接相关的原文 |
| 滚动会话摘要 | 10% | 按事实、决定、未完成项结构化更新 |
| 当前商品、订单和卡片资源快照 | 10% | 保留资源 ID、版本和必要展示字段 |
| 本轮 Tool 结果 | 20% | 大结果保留必要字段并引用完整 Trace |
| RAG 与授权长期记忆 | 10% | 只保留当前任务相关且满足权限的内容 |
| 最终回复预留 | 10% | 不被输入上下文占用 |

上下文不足时依次压缩旧工具详情、旧 RAG 片段、较早消息和滚动摘要；系统规则、权限、当前任务、资源归属、金额、待确认操作及其版本不得被压缩掉。

用户点击“清除记录”后，专属客服开启新的会话记忆世代；旧消息、旧摘要和未完成确认不能再次进入新对话上下文。

当前实现采用有界的分层规划输入：单轮规划输入上限为约 12,000 字符，其中当前消息、结构化会话状态、滚动摘要、会话档案和最近相关原文分别设独立上限。摘要按上下文压力自适应更新；长会话进入高压区后，每新增约 4 条有效消息即可滚动更新，而不是等待固定的大批量消息。完整消息与 Trace 仍持久化，但不直接进入每次模型调用。

#### 2.1.11 连续聊天状态

连续聊天不能只依赖最近几条原始消息。Supervisor 必须维护结构化会话状态，并在每一轮先判断用户是在继续、补充、纠正、取消、切换话题还是发起新任务。

| 状态 | 保存内容 | 解决的问题 |
| --- | --- | --- |
| `active_goal` | 当前主要目标和完成条件 | 避免每轮重新介绍自己，不知道正在做什么 |
| `active_resources` + `topic_frames` | 当前焦点及有界历史话题中的商品、SKU、订单、包裹、退款和地址 | 支持“这个、刚才那个”，也支持切换后按明确名称返回原话题 |
| `pending_assistant_question` | Agent 上一轮向用户提出的问题 | 用户回答“好、是、第二个”时能正确承接 |
| `card_sets` | 最近多组商品、订单、地址等卡片集合；集合版本和集合内焦点分开保存 | “三件里的第二件”后仍可回到原三件，不会被单张焦点卡覆盖 |
| `pending_approval` | 待确认操作、影响范围和有效期 | “确认、算了、改成删除两件”能作用于正确操作 |
| `open_tasks` + `resolved_tasks` | 进行中、暂停和已经完成的任务 | 回答插入问题后可以恢复旧任务，也不会重复执行已经完成的写操作 |
| `last_successful_actions` | 最近实际成功的 Agent、Skill、Tool、参数摘要和业务结果 | 复核复合请求是否漏项，并支持失败后的定向补做 |
| `explicit_corrections` | 用户明确纠正的对象、条件和选择 | “不是第三件，是第一件”直接改焦点，不重新做一次无关搜索 |
| `facts_requiring_refresh` | 下轮使用前必须重查的动态事实类型 | 防止把旧库存、金额、订单或物流摘要当成实时真相 |
| `active_run` | 当前运行、流式状态和取消标记 | 用户打断生成后停止未执行动作 |
| `conversation_version` | 会话版本和最新消息序号 | 防止多标签页消息乱序、旧回复覆盖新回复 |
| `service_mode` | `ai_active`、`waiting_human`、`human_active` | 人工接管期间阻止 AI 抢答 |

| 用户连续表达 | Supervisor 应如何理解 |
| --- | --- |
| “好”“可以”“就这样” | 优先匹配有效的待确认或待回答问题；没有待办时才追问含义 |
| “不是这个，是第二个” | 修改当前选择并重新校验，不从头开始对话 |
| “先别退款，帮我看物流” | 暂停退款任务，查询物流；不得把“先别”理解为确认退款 |
| “物流看完了，继续刚才的” | 恢复暂停的退款任务和已选订单 |
| “价格改成 20 元以内” | 修改正在执行的商品搜索约束，并重新规划搜索 |
| “算了，都不要了” | 取消仍未执行的待确认动作；已执行动作必须如实说明无法假装撤回 |
| 连续发送两条补充消息 | 尚未行动时合并约束；已经行动时由 Supervisor 判断补充、打断或新任务 |
| 发送新的商品/订单卡片 | 将新卡片设为当前焦点，旧焦点进入历史栈，不直接删除 |
| 插入无关查询后明确说出旧商品或订单名称 | 在有界历史卡片中按名称恢复该资源；“它、这个、第一个”等弱指代仍只绑定最新卡片集合 |
| “不要铅笔，预算和排序不变” | 增加明确排除条件，同时继承预算、排序和其他未被否定的约束 |
| “这些订单里金额最高的是哪笔” | 只在最近一次可见订单卡片集合中按最小货币单位比较，唯一命中后展示该订单；并列时请求选择 |
| “我那笔 19.10 元的订单，快递到哪了” | 金额是订单限定条件，整句只委派物流 Agent；不拆成“全部订单 + 最新订单物流” |
| “余额能提现吗，退款退到哪里” | 识别为纯平台规则问题，只调用规则 RAG，不额外查询余额或订单 |

#### 2.1.12 商城业务场景覆盖

| 领域 | 必须理解的正常场景 | 必须处理的异常或边界场景 |
| --- | --- | --- |
| 商品选购 | 搜索、推荐、详情、款式、价格、库存、比较 | 无结果、商品下架、店铺暂停、款式缺货、OCR 与结构化参数冲突、推荐替代商品 |
| 购物车 | 查看、选择、改数量、移除、清空 | 多店铺、部分失效、库存不足、价格变化、版本冲突、只有部分操作成功 |
| 结算 | 商品与地址确认、包邮、应付金额预检 | 无地址、地址失效、下单前库存或价格变化、某店铺暂不可结算 |
| 订单与评价 | 全部订单、指定订单、取消、确认收货、再次购买、评价和追评 | 多个候选、待付款、支付失败、待发货、运输中、已完成、历史商品下架、重复评价 |
| 物流 | 包裹列表、当前节点、完整轨迹、签收 | 尚未发货、一个订单多个包裹、轨迹延迟、派送异常、模拟物流与真实物流区分 |
| 售后 | 资格检查、仅退款、退货退款、提交、进度、撤销 | 多商品、部分数量、金额变化、需要凭证、商家拒绝、申诉、申请不可撤销 |
| 账户与资产 | 资料、地址、余额、流水、商品收藏、店铺收藏 | 多地址选择、默认地址变更、收藏商品下架、店铺暂停、余额不足引导 |
| 平台规则 | 购物、支付、余额提现边界、退款去向、物流、签收、售后、客服规则 | 知识无可靠结果、规则版本变化、规则和实时订单状态冲突；余额支付的订单退款成功后必须原路退回商城余额 |
| 人工服务 | 明确转人工、查询排队、人工结束后恢复 AI | 重复建单、取消排队、长时间无人接单、人工转派、人工与 AI 消息冲突 |

事实冲突时采用固定证据优先级：实时业务 Tool 结果 > 当前资源快照 > 已发布 RAG 知识 > 用户授权记忆 > 对话摘要 > 模型推测。模型推测不得作为库存、金额、订单、物流和售后状态。

#### 2.1.13 卡片事件与并发控制

| 输入事件 | Agent 的处理方式 |
| --- | --- |
| 文字消息 | 模型理解目标、约束、指代和期望结果 |
| 商品卡片 | 将可信商品引用加入当前焦点，再查询最新商品事实 |
| 订单卡片 | 校验订单属于当前用户后加入焦点，再判断用户问题 |
| 图片或附件 | 先上传并完成安全扫描，再根据当前任务交给商品、评价或售后 Agent；不得把图片文字当成系统指令 |
| 卡片按钮 | 作为结构化 `action_code` 事件处理，不重新猜测按钮文字 |
| 确认/取消按钮 | 只作用于绑定当前用户、会话、资源版本和指纹的待确认操作 |
| 人工系统事件 | 更新 `service_mode`，决定暂停或恢复 Agent |

每张业务卡片必须携带资源类型、公开资源 ID、资源版本、可用操作和跳转地址。按钮是否可用由后端根据最新状态返回，不能由模型自行编造。

| 卡片 | 结构化动作代码 | 作用 |
| --- | --- | --- |
| 商品卡片 | `product.open` | 打开对应商品详情 |
| 商品卡片 | `favorite.product.add` | 收藏商品 |
| 商品卡片 | `favorite.product.remove` | 取消收藏商品 |
| 商品卡片 | `cart.add.preview` | 选择 SKU 和数量并生成加入购物车预览 |
| 商品卡片 | `product.compare.select` | 将商品加入当前比较集合 |
| 购物车卡片 | `cart.quantity.preview` | 生成数量修改预览 |
| 购物车卡片 | `cart.item.remove.preview` | 生成单项移除预览 |
| 购物车卡片 | `cart.clear.preview` | 生成全部清空预览 |
| 购物车卡片 | `checkout.create` | 创建临时结算会话并展示结算预览 |
| 订单卡片 | `order.open_detail` | 打开本人订单详情 |
| 订单卡片 | `order.cancel.preview` | 生成订单取消预览 |
| 订单卡片 | `logistics.open` | 查询并打开物流时间线 |
| 订单卡片 | `order.confirm_receipt.preview` | 生成确认收货预览 |
| 订单卡片 | `after_sale.start` | 进入售后资格检查和草稿生成 |
| 订单卡片 | `review.start` | 进入评价或追评协助 |
| 订单卡片 | `order.repurchase.preview` | 生成再次购买预览 |
| 物流卡片 | `shipment.refresh` | 查询包裹最新轨迹 |
| 售后卡片 | `refund.open_detail` | 打开售后详情和状态事件 |
| 售后卡片 | `refund.cancel.preview` | 生成售后撤销预览 |
| 地址卡片 | `address.select` | 选择结算或当前任务使用的地址 |
| 地址卡片 | `address.set_default.preview` | 生成默认地址修改预览 |
| 确认卡片 | `approval.confirm` | 确认与该卡片指纹绑定的动作 |
| 确认卡片 | `approval.cancel` | 取消与该卡片指纹绑定的动作 |
| 支付卡片 | `ui.payment_panel.open` | 打开支付弹窗，后续支付由用户亲自完成；这是 UI Action，不是 Tool |

卡片动作事件至少包含 `client_event_id`、`action_code`、资源引用、资源版本、会话版本和确认引用。前端不能把按钮文字拼成一条普通聊天消息。

| 并发情况 | 处理规则 |
| --- | --- |
| 同一用户打开多个浏览器标签页 | 共享服务端会话版本和消息序号，不使另一标签页登录失效 |
| 同一会话同时发送多条消息 | 每条消息有唯一客户端 ID；Supervisor 决定合并、排队或中断 |
| 同一 Run 包含多个子任务 | `subtask_key` 在 Run 内必须唯一且稳定；任务合并、补齐或重排后统一重新编号 |
| 旧 Agent Run 晚于新 Run 返回 | 旧结果可以进入审计，但不得覆盖新会话焦点或作为最新回复 |
| 重复点击确认 | 使用同一幂等键只执行一次，并返回第一次执行的真实结果 |
| 流式连接断开 | Agent Run 可继续到安全检查点；重连后按事件序号补发，不能重复写操作 |
| 写操作正在执行时收到取消 | 未提交则取消；结果未知则先查幂等记录，不能直接宣布已取消或已成功 |

#### 2.1.14 领域 Agent Contract

Supervisor 通过统一 Contract 调用领域 Agent，不能只发送一段自然语言，也不能接受无法校验的自由文本结果。

| 输入字段 | 说明 |
| --- | --- |
| `task_id`、`objective` | 子任务唯一标识、目标和完成条件 |
| `trusted_scope` | 后端注入的用户、会话和允许的数据范围 |
| `resource_refs` | 当前商品、SKU、订单、包裹、退款或地址引用及版本 |
| `conversation_state` | 当前焦点、上一轮问题、待选择和暂停任务 |
| `user_constraints` | 预算、数量、时间、店铺、款式等明确条件 |
| `allowed_skills/tools` | 本次子任务实际允许使用的 Skill 和工具白名单 |
| `expected_outputs` | 期望事实、卡片类型和必须回答的问题 |
| `budget` | 模型、Token、工具次数、超时和返工预算 |

| 输出字段 | 说明 |
| --- | --- |
| `status` | `succeeded`、`partial`、`failed`、`denied` 或 `unknown` |
| `facts` | 来自 Tool/RAG 的结构化事实，不夹带模型猜测 |
| `cards` | 前端可直接校验和渲染的业务卡片 |
| `evidence_refs` | Tool 调用、RAG 文档、数据版本和 Trace 引用 |
| `missing_information` | 尚缺少的用户信息或业务数据 |
| `next_action_candidates` | 可供 Supervisor 选择的后续动作，不表示已经执行 |
| `decision_record` | 本 Agent 实际输出的完整中文思考与选择说明 |
| `usage` | 模型、Token、工具次数、耗时和成本 |

| 领域 Agent | 完成标准 |
| --- | --- |
| 用户商品选购 Agent | 结果满足用户硬条件；商品在售；价格、SKU、库存有实时依据；返回商品卡片 |
| 用户购物车与结算 Agent | 每项变更对象和版本明确；结算金额来自实时预检；写操作有确认和回读 |
| 用户订单与评价 Agent | 订单属于当前用户；状态和可用按钮最新；评价资格经过查询 |
| 用户物流履约 Agent | 包裹属于当前用户订单；轨迹来源明确；模拟物流明确标记 |
| 用户售后退款 Agent | 资格、数量、金额、类型和材料完整；提交结果可回读 |
| 用户账户资料与资产 Agent | 只处理本人资料；地址、余额、流水和收藏来源明确 |
| 平台规则与知识 Agent | 使用已发布、生效且权限匹配的知识；回答带来源和版本 |

#### 2.1.15 结果验证器

Supervisor 负责语义复核，但确定性事实由独立验证器校验，不能让同一个模型既生成结果又自行宣布正确。

| 验证项 | 验证方式 | 失败处理 |
| --- | --- | --- |
| 输出 Schema | JSON Schema 和卡片协议校验 | 退回原 Agent 修正格式 |
| 用户与资源归属 | 后端身份和资源关系查询 | 立即拒绝并记录越权事件 |
| 商品与店铺状态 | 业务主库实时状态 | 移除不可用操作并要求重新规划 |
| 金额、数量和币种 | Decimal、币种和汇总公式校验 | 禁止回复，重新查询或计算 |
| 订单、物流和售后状态 | 领域状态机校验 | 重新生成允许动作 |
| 资源版本 | 乐观锁和确认指纹校验 | 旧确认失效，重新预览 |
| 写操作结果 | 幂等记录和执行后回读 | 标记 `unknown`，先查结果，不重复执行 |
| 卡片跳转与按钮 | 路由、资源存在性和后端动作列表校验 | 删除失效按钮并返工 |
| 子任务完整性 | Supervisor 对照用户全部诉求 | 将遗漏项定向返工 |
| 回答相关性和可读性 | 语义质量评估器 | 重写回复，不重复业务写操作 |

验证结果只有 `passed`、`needs_rework`、`blocked` 三种。只有 `passed` 可以进入最终回复；`needs_rework` 返回具体失败项；`blocked` 返回可靠的部分结果和人工或页面入口。

#### 2.1.16 回复与界面规范

| 场景 | 主要展示 | 必要操作 |
| --- | --- | --- |
| 商品搜索/推荐 | 图片、名称、价格、店铺、简短推荐理由 | 查看商品、收藏、加入购物车 |
| 购物车 | 按店铺分组的商品、款式、数量、金额 | 改数量、移除、结算 |
| 订单 | 商品图片、店铺、款式、金额、中文状态 | 查看详情、物流、评价、售后等实时可用按钮 |
| 物流 | 当前节点、时间、位置和完整时间线 | 查看订单；模拟物流标明“商城模拟物流” |
| 售后 | 可退商品、数量、金额、原因和当前进度 | 确认提交、查看进度、按状态撤销 |
| 地址 | 默认标识、收货人和脱敏地址 | 用户主动展开后查看完整地址 |
| 失败 | 出错对象、具体原因和可执行下一步 | 重试、打开对应页面或转人工 |

回复顺序统一为“简短结论 → 业务卡片 → 下一步”。不输出字段墙，不用“请求字段校验失败”“网络异常”覆盖所有错误，也不把执行计划说成已经完成。

聊天右侧按照第 0 节展示真实 Agent 决策说明和执行事件，不再显示前端预制的固定思考步骤。

#### 2.1.17 运行限制、性能与成本

| 项目 | 首期规则 |
| --- | --- |
| 委派深度 | 仅 Supervisor 可以委派，领域 Agent 不再创建子 Agent；最大深度 1 |
| Agent 数量 | 单次最多 4 个领域 Agent，并行最多 3 个只读任务 |
| 写操作 | 同一资源严格串行；超时且结果未知时先查幂等结果，不盲目重试 |
| 返工 | 同一子任务最多 2 次，并给出明确的缺失项 |
| 部分成功 | 保留可靠结果，明确标注“已完成、待确认、失败”，不全部报错 |
| 人工接管 | 人工接管期间 AI 不向用户自动发消息；人工结束后再恢复 |
| RAG 边界 | 只回答已发布规则；订单、库存、金额和物流必须查询业务主库 |
| RAG 发布一致性 | API、Agent Worker 和 Knowledge Indexer 必须使用同一版本知识源；新文档完成向量化并切换 active generation 后才算发布完成，验收时校验命中的 `content_version` |
| 模型不可用 | 保留确定性查询和页面/人工入口，不用固定话术伪装成正常智能回答 |

动态 Agent 根据任务复杂度选择运行规模，不能为了体现 Multi-Agent 而强制启动多个模型：

| 请求类型 | 推荐运行方式 | 首期调用预算 |
| --- | --- | --- |
| 问候、感谢、能力询问 | Supervisor 直接回答 | 1 次模型调用，不调用领域 Agent |
| 单领域事实问题 | Supervisor 选择 1 个领域 Agent，由其自主选择工具 | 最多 2 次模型、3 次 Tool |
| 多个独立只读问题 | Supervisor 派发 2—4 个领域 Agent，并行后综合 | `1 + N + 1` 次模型，`N` 为实际 Agent 数量 |
| 需要写操作 | 先动态收集事实并生成确认；确认后的新一轮执行和回读 | 每个阶段独立计费，不因重试重复写入 |
| 纯卡片按钮操作 | 校验结构化动作后直接进入对应 Agent 或 Tool | 不重新调用模型猜测按钮含义 |
| 明确转人工 | 人工协调器直接建单 | 无需调用领域 Agent |

| 模型职责 | 首期模型策略 | 配置方式 |
| --- | --- | --- |
| Supervisor 理解、规划和综合 | 目标默认使用 `gpt-5.5`，优先保证多意图和连续对话质量 | `ECOM_AGENT_MODEL_NAME`，禁止写死在代码中 |
| 7 个领域 Agent | 首期与 Supervisor 使用同一批准模型，稳定后再按领域评估是否使用不同模型 | 继承全局配置，可预留 Agent Version 覆盖项 |
| Supervisor 语义复核 | 复用主模型；确定性校验不调用模型 | Agent Runtime 配置 |
| 离线质量评估 | 与在线回答解耦，异步运行，不增加用户等待时间 | 独立 Evaluation Worker 配置 |
| RAG Embedding | 使用当前项目配置的 `tongyi-embedding-vision-flash` | `ECOM_EMBEDDING_MODEL` |
| 模型降级 | 只使用已验证支持当前 Tool/流式协议的候选模型 | `ECOM_AGENT_MODEL_FALLBACK_NAMES` |

模型地址、密钥、Wire API、温度和超时分别通过现有 `ECOM_AGENT_MODEL_API_URL`、`ECOM_AGENT_MODEL_API_KEY`、`ECOM_AGENT_MODEL_WIRE_API`、`ECOM_AGENT_MODEL_TEMPERATURE`、`ECOM_AGENT_MODEL_TIMEOUT_SECONDS` 配置。API Key 只能由 Secret 注入，不能出现在文档、Prompt、消息或右栏 Trace 中。

| 性能指标 | 首期目标 |
| --- | --- |
| 首个真实进度事件 | P95 不超过 500 ms |
| 首个模型思考或回复增量 | P95 不超过 3 秒 |
| 单领域只读任务完成 | P95 不超过 8 秒 |
| 三个领域以内的并行任务完成 | P95 不超过 15 秒 |
| Tool 调用 | 每个 Tool 单独设置超时；写 Tool 超时不自动盲重试 |
| Token 与成本 | 每个 Run、Agent、Skill 分别记录预算和实际消耗；达到预算后返回部分结果或请求继续 |

商品公开资料和已发布 RAG 可短时缓存；用户订单、购物车、余额、库存、物流、售后和待确认数据不得依赖旧缓存作为最终结论。

#### 2.1.18 中断、恢复与结果未知

| 情况 | Agent Runtime 行为 |
| --- | --- |
| 用户点击“停止生成” | 设置 Run 取消标记，不再启动新任务；已完成只读结果可以保留 |
| 只读 Tool 正在执行 | 尽力取消；无法取消时允许结果进入 Trace，但不自动发送过期回复 |
| 写 Tool 尚未提交 | 取消操作并使确认凭证失效 |
| 写 Tool 已提交但响应丢失 | 标记 `unknown`，根据幂等键查询真实结果，禁止直接重试 |
| 用户关闭或刷新页面 | Run 继续到最近安全检查点；重连后使用最后事件序号补发 |
| SSE 暂时断开 | 事件保存在服务端，重连后补发缺失事件，不重新运行 Agent |
| 待确认操作长期未处理 | 默认 10 分钟过期；再次确认前重新读取业务状态 |
| 新消息补充当前任务 | 更新当前目标或约束并触发 `replan`，保留计划修改历史 |
| 新消息切换到无关任务 | 暂停原任务并压入 `paused_tasks`，新任务完成后允许恢复 |
| 人工接管 | 当前 AI Run 停在检查点；人工消息进入会话，AI 不自动发送回复 |
| 人工结束服务 | 生成交接摘要，恢复 `ai_active`；未完成写操作必须重新确认 |
| 服务重启 | 从最近持久化检查点恢复；已成功的幂等任务不得重复执行 |

#### 2.1.19 关键连续对话验收集

| 对话或操作 | Agent 应有行为 | 禁止结果 |
| --- | --- | --- |
| “找 20 元以内适合考试的文具” | 动态搜索并返回可点击商品卡片，说明简短推荐理由 | 只输出商品名称文字列表 |
| “第二个有什么款式？” | 以上一轮第二张商品卡为焦点，查询最新 SKU | 重新问用户是哪件商品 |
| 两件商品比较后问“第二件有哪些款式和库存” | 绑定比较结果中的第二张商品卡，读取该商品全部在售 SKU 与实时库存 | 把序号当搜索词、跳回更旧商品集或返回空结果 |
| 商品卡默认 6 支，用户说“把 10 支款加入购物车”且 10 支缺货 | 加购前重新读取全部 SKU；明确显示 10 支库存为 0，购物车不变 | 将默认 6 支款加入购物车或只依据旧卡库存 |
| 加入 8 支款后问“核对刚加入的是不是 8 支” | 只读回查购物车，明确回答款式和数量 | 因句中出现“加入购物车”而再次加购 |
| “要两件，放进购物车” | 生成对应 SKU、数量和价格预览，确认后加入并回读 | 加错商品、只回复“已加入”但未执行 |
| “就买这两件，用默认地址” | 创建结算会话，校验库存金额，展示订单预览；确认后创建订单并打开支付弹窗 | Agent 代替用户付款或跳过订单确认 |
| “购物车清空，地址也发我” | 拆成两个任务并行读取；地址直接展示，清空等待确认 | 只完成其中一个任务 |
| “地址邮编是多少？购物车这件改成 3 件只试算” | 地址 Agent 明确说明是否保存邮编，购物车 Agent 返回修改前后金额且不落库 | 把“改成 3 件”误判成修改地址，或漏掉任一结果 |
| 搜索结果后说“把第二件加入购物车 2 件，再告诉我余额” | 复用上一轮第二张商品卡；串行加购并回读购物车，同时返回实时余额卡 | 重新发起商品搜索、丢失余额、加错序号或展示虚假空购物车 |
| “我买过什么？” | 返回跨店铺的本人订单卡片 | 回复一串 `ord_...` 编号 |
| “可见订单累计实付、已退款和净支出是多少” | 查询完整可见订单集合，按分计算并展示汇总与订单明细卡 | 只统计默认前 5 笔、把“退款”误判成创建退款申请 |
| “最近已完成订单用什么方式支付、实付多少” | 过滤已完成订单，读取真实支付记录并回答支付渠道与金额 | 用订单金额猜支付方式、只展示卡片不回答问题 |
| “按待付款、待发货、运输中、待评价、售后中整理” | 一次展示全部指定状态、准确统计缺失状态和对应卡片 | 被“售后中”单词劫持为单笔售后进度查询 |
| 两笔订单金额相同，问“分别什么状态，哪笔还能售后” | 同时展示两张订单卡，并用商品与款式指出具备售后入口的一笔 | 依据金额擅自选一笔、混入已有售后进度或漏掉状态对比 |
| “第二单到哪了？” | 选中第二张订单卡并查询包裹时间线 | 使用第一单物流或编造轨迹 |
| “帮我退款” | 沿用当前订单，查询资格并生成退款草稿 | 丢失订单上下文或直接声称退款成功 |
| “先别退款，看看物流”→“继续刚才的” | 暂停退款、处理物流，再恢复原退款任务 | 将“先别”当成确认退款 |
| “不是红色，换黑色，预算还是 100” | 修改当前选购约束并重新规划 | 清空全部约束或重新介绍客服能力 |
| 确认清空前另一标签页修改购物车 | 检测版本变化，让旧确认失效并生成新预览 | 用旧确认删除后来新增的商品 |
| 流式回答中用户发送“停止” | 中断尚未开始的行动，保留已完成查询，报告真实状态 | 继续执行待确认写操作 |
| 用户甲要求查看用户乙订单 | 权限网关拒绝，右栏显示范围拦截 | 依赖 Prompt 拒绝但工具实际可越权 |
| “查他人订单；不行就只告诉我自己的余额” | 拒绝他人数据子任务，只将用户明确指定的本人子句交给规划器并返回余额卡 | 整轮拒绝，或把他人地址误解为本人地址 |
| “余额流水中哪笔是消费、哪笔是充值” | 读取余额流水并按真实交易类型展示 | 把“充值”当成新的充值指令并插入无关拒绝文案 |
| “余额可以提现吗，余额支付退款退到哪里” | 使用已发布支付规则 RAG 回答不提现、退款原路回余额 | 把“余额支付的订单”误判成其他用户的数据 |
| 收藏店铺后查看列表，再说“取消刚才这家店铺” | 结合最近成功收藏动作和当前实时收藏列表取消同一家店 | 依据较新的无关商品卡取消另一家店或显示虚假成功 |
| 取消未收藏的具名店铺 | 明确说明当前收藏列表没有该店，展示未变化的实时收藏卡 | 误取消商品收藏或在澄清卡中显示错误计数 |
| “把默认地址直接改成……” | 明确说明聊天内未修改，展示当前地址和地址管理入口 | 只展示旧地址却不说明操作没有执行 |
| “我的快递到哪了”且有多笔物流订单 | 只展示待发货/运输中候选，提示列出每个实际可选序号；用户回复序号后延续查询 | 混入已完成/售后订单，或展示 5 张卡却只提示前两张 |
| 退款/清空确认卡已生成 | 流式思考停止，右栏显示“等待确认”和本轮实际工具证据 | 卡片可操作但右栏永久“思考中” |
| 点击“清除记录”后问“继续刚才的” | 作为新会话处理，不召回旧任务和确认 | 从已清除摘要恢复旧订单 |
| 转人工后继续发消息 | 消息进入人工队列，AI 不抢答 | AI 和人工同时回复 |
| 某个并行工具失败 | 展示其他成功卡片并指出失败项和重试入口 | 整轮统一回答“网络异常” |
| “物流不再每 5 秒更新，现在由谁更新节点” | 只查询已发布物流规则，说明由有权限的店铺人员、平台管理员或经确认的 Agent 显式记录 | 混入某一笔无关订单或把固定计时推进说成仍有效 |

#### 2.1.20 首次进入与主动交互

| 进入场景 | 专属客服行为 | 用户看到的内容 |
| --- | --- | --- |
| 用户首次打开专属客服 | 主动发送一次自然问候，不读取无关隐私数据 | AI 头像、简短问候、4 个快捷入口 |
| 新注册用户 | 用新用户语气介绍能做什么 | “找商品、了解购物流程、管理地址、转人工” |
| 已有正常会话 | 恢复最后阅读位置，不重复问候 | 最近消息和当前焦点 |
| 存在未完成选择 | 恢复候选卡片 | “上次等待你选择以下订单/商品” |
| 存在有效待确认操作 | 恢复确认卡片并显示剩余有效时间 | 操作对象、影响、确认和取消按钮 |
| 存在人工工单 | 优先显示人工排队或接管状态 | 居中系统消息和工单状态 |
| 用户清除记录后进入 | 创建新记忆世代并重新问候 | 不提及被清除的历史内容 |
| 跨天重新进入 | 简短欢迎，不主动输出订单或余额 | 快捷入口；用户询问后再实时查询 |

首次问候建议提供“帮我找商品”“查看我的订单”“查询物流”“申请售后”四个动态快捷按钮。点击按钮发送结构化意图事件，不把按钮标题拼成普通消息。用户进入后 500 ms 内应看到会话骨架和真实连接状态，问候消息只发送一次。

#### 2.1.21 图片、附件与凭证交互

| 环节 | 界面与 Agent 行为 |
| --- | --- |
| 添加入口 | 输入框左侧“+”菜单提供商品、订单、图片和文件；支持本地选择、拖拽、粘贴图片 |
| 上传前 | 显示缩略图、文件名、大小、删除按钮；用户发送前可以移除 |
| 上传中 | 展示真实上传进度和取消按钮，不用无限转圈代替状态 |
| 安全扫描 | 明确显示“扫描中、可使用、被拒绝、扫描失败”；失败说明具体原因 |
| 图片理解 | Agent 说明识别的是图片中的可见内容，OCR 与视觉结论均作为待核验资料 |
| 商品截图 | 可用于搜索相似商品、理解款式或补充用户问题，最终商品事实仍查商城商品数据 |
| 评价图片 | 先绑定评价草稿，用户确认整份评价后提交 |
| 退款凭证 | 先绑定售后草稿，展示图片数量和用途，用户确认后提交申请 |
| 发送后 | 气泡中显示可点击缩略图；右栏显示上传、扫描、视觉工具和绑定结果 |

附件限制直接复用商城现有文件上传策略，前端在选择文件时提前显示允许类型、单文件大小和数量限制。上传失败不得清空用户已经输入的文字和其他已成功附件。

#### 2.1.22 Tool Contract

每个 Tool 必须在注册中心提供完整、可版本化的 Contract：

| Contract 字段 | 作用 |
| --- | --- |
| `tool_code`、`version` | 稳定工具名和版本；旧版本在会话恢复期内保持可用 |
| `description` | 明确说明工具做什么、不做什么、何时使用 |
| `tool_type`、`side_effect` | 标识查询、检索、预览、写入、记忆或人工接管；UI Action 不属于 Tool |
| `allowed_agents` | 可调用该工具的 Agent 版本白名单 |
| `input_schema` | 参数类型、必填项、范围、枚举、金额和数量格式 |
| `output_schema` | 安全结果、数据版本、卡片映射和证据引用 |
| `confirmation_policy` | 无需确认、明确指令视为确认或必须确认卡片 |
| `idempotency_scope` | 幂等键按用户、会话、资源和动作的组合范围 |
| `timeout_retry_policy` | 超时、只读重试、写操作结果未知时的查询方式 |
| `error_codes` | 稳定错误码及面向用户的具体中文解释 |
| `audit_policy` | Trace、参数、结果、权限判断和成本的记录要求 |

所有 Tool 返回统一外层结果：

| 字段 | 允许值或说明 |
| --- | --- |
| `status` | `succeeded`、`denied`、`failed`、`unknown` |
| `data` | 符合工具输出 Schema 的完整结构化数据 |
| `resource_versions` | 本次读取或修改的业务资源版本 |
| `evidence_refs` | 业务查询、RAG 或异步任务证据 |
| `error_code`、`error_detail` | 机器可处理错误码和具体中文原因 |
| `idempotency_result` | 首次执行、复用已有结果或状态未知 |
| `duration_ms` | 实际工具耗时 |

以 `cart.clear.commit` 为例，必须接收 `approval_id`、`cart_version`、`operation_fingerprint` 和 `idempotency_key`；不能只接收模型生成的“用户已经同意”字符串。

#### 2.1.23 Agent Tool—API—权限映射

Agent Tool 可以通过内部适配器复用 FastAPI Service，不要求在后端内部再次发送 HTTP 请求；但行为、Schema、权限和状态机必须与对应 OpenAPI operation 保持一致。

| Agent Tool | 对应现有 OpenAPI operation / Service | 权限范围 | 实施方式 |
| --- | --- | --- | --- |
| `catalog.search_products` | `Product_Search` | 公开在售商品 | 复用 Catalog Service |
| `catalog.get_product` | `Product_Get`、`ProductSku_List`、`ProductFaq_List` | 公开商品 | 聚合现有接口及详情 OCR |
| `catalog.compare_products` | `Product_Get` | 公开商品 | Tool 聚合多个商品并比较 |
| `catalog.compare_skus` | `ProductSku_List` | 公开在售 SKU | Tool 结构化比较 |
| `catalog.get_inventory_availability` | Inventory Service | 公开 SKU 的可售状态 | 新增只读适配器 |
| `store.search_public` | 暂无对应 operation | 公开营业店铺 | 新增公开店铺搜索 Service |
| `store.get_public` | `Store_Get`、`StorePolicy_ListPublic` | 公开店铺 | 聚合现有接口 |
| `catalog.search_store_products` | `StoreProduct_List` | 指定公开店铺 | 复用 Store Catalog Service |
| `review.get_product_summary` | `ProductReview_List` | 公开评价 | Tool 聚合评分与评价摘要 |
| `cart.get_mine` | `Cart_GetMine` | 当前用户 | 已有 Agent Tool |
| `cart.add_item.preview` | Catalog + Inventory + Cart Service | 当前用户 | 新增只读预检适配器 |
| `cart.add_item.commit` | `CartItem_Create` | 当前用户 | 复用购物车写服务 |
| `cart.update_quantity.preview` | Cart + Inventory Service | 当前用户 | 新增只读预检适配器 |
| `cart.update_quantity.commit` | `CartItem_Patch` | 当前用户 | 复用购物车写服务 |
| `cart.remove_item.preview` | `Cart_GetMine` | 当前用户 | 新增影响预览 |
| `cart.remove_item.commit` | `CartItem_Delete` | 当前用户 | 复用购物车写服务 |
| `cart.clear.preview` | `Cart_GetMine` | 当前用户 | 新增全部清空预览 |
| `cart.clear.commit` | 暂无“清空全部”operation | 当前用户 | 新增原子清空 Service 和 API |
| `cart.replace_selection` | `CartSelection_Replace` | 当前用户 | 复用购物车选择服务 |
| `checkout.create_session` | `CheckoutSession_Create` | 当前用户 | 复用结算服务 |
| `checkout.refresh_preview` | `CheckoutSession_Get`、`CheckoutSession_Patch`、`CheckoutRepricing_Create` | 当前用户 | 聚合结算刷新能力 |
| `order.create.preview` | Checkout Service | 当前用户 | 复用结算快照生成确认内容 |
| `order.create.commit` | `Order_Create` | 当前用户 | 复用订单创建和库存预占 |
| `order.list_user_orders` | `Order_ListMine` | 当前用户 | 已有 Agent Tool |
| `order.get_user_order_detail` | `Order_GetMine`、`OrderEvent_ListMine` | 当前用户 | 已有 Tool，补充事件聚合 |
| `order.cancel.preview` | Order Domain | 当前用户订单 | 新增状态与影响预检 |
| `order.cancel.commit` | `Order_CancelMine` | 当前用户订单 | 复用订单取消状态机 |
| `order.confirm_receipt.preview` | Order + Logistics Domain | 当前用户订单 | 新增签收状态预检 |
| `order.confirm_receipt.commit` | `Order_ConfirmReceiptMine` | 当前用户订单 | 复用确认收货状态机 |
| `order.repurchase.preview` | `Order_RepurchaseMine` + Catalog Service | 当前用户历史订单 | 先检查当前商品与 SKU 可用性 |
| `review.check_eligibility` | `ReviewEligibility_Get` | 当前用户订单商品 | 复用评价资格服务 |
| `review.build_draft` | 暂无对应 operation | 当前用户 | Agent 生成草稿，不写业务库 |
| `review.attach_images` | File + Review Service | 当前用户评价草稿 | 新增草稿图片绑定 |
| `review.submit.commit` | `Review_Create` | 当前用户订单商品 | 复用评价提交服务 |
| `review.append.commit` | `Review_Append` | 当前用户已有评价 | 复用追评服务 |
| `logistics.get_user_order_shipments` | `Shipment_ListMine`、`Shipment_GetMine` | 当前用户订单 | 已有 Agent Tool |
| `logistics.get_shipment_tracks` | `ShipmentTrack_ListMine` | 当前用户包裹 | 复用物流轨迹服务 |
| `after_sale.check_refund_eligibility` | `RefundEligibility_Check` | 当前用户订单 | 已有 Agent Tool |
| `after_sale.build_refund_draft` | AfterSale Domain | 当前用户订单 | 已有服务端草稿能力 |
| `after_sale.attach_evidence` | File + AfterSale Service | 当前用户售后草稿 | 新增凭证绑定 |
| `after_sale.submit_refund_application` | `RefundApplication_Create` | 当前用户订单 | 已有确认框架 |
| `after_sale.list_user_refunds` | `RefundApplication_ListMine` | 当前用户 | 已有 Agent Tool |
| `after_sale.get_user_refund_detail` | `RefundApplication_GetMine`、`RefundEvent_ListMine` | 当前用户售后 | 已有 Agent Tool，补充事件聚合 |
| `after_sale.cancel_refund.preview` | AfterSale Domain | 当前用户售后 | 新增撤销资格预检 |
| `after_sale.cancel_refund.commit` | `RefundApplication_Cancel` | 当前用户售后 | 复用售后撤销服务 |
| `after_sale.build_appeal_draft` | AfterSale Domain | 当前用户被拒售后 | 新增申诉草稿 |
| `after_sale.submit_appeal.commit` | `RefundAppeal_Create` | 当前用户被拒售后 | 复用申诉创建服务 |
| `account.get_profile` | `UserProfile_Get` | 当前用户 | 复用用户资料服务 |
| `address.list_mine` | `Address_ListMine`、`Address_GetMine` | 当前用户 | 复用地址查询服务 |
| `address.create.preview` | Identity Domain | 当前用户 | 新增地区和字段预检 |
| `address.create.commit` | `Address_Create` | 当前用户 | 复用地址创建服务 |
| `address.update.preview` | Identity Domain | 当前用户地址 | 新增差异预览 |
| `address.update.commit` | `Address_Patch` | 当前用户地址 | 复用地址修改服务 |
| `address.delete.preview` | Identity Domain | 当前用户地址 | 新增默认地址影响预览 |
| `address.delete.commit` | `Address_Delete` | 当前用户地址 | 复用地址删除服务 |
| `address.set_default` | `Address_SetDefault` | 当前用户地址 | 复用默认地址服务 |
| `wallet.get_balance` | `UserWallet_GetMine` | 当前用户 | 复用钱包查询服务 |
| `wallet.list_transactions` | `UserWalletTransaction_ListMine` | 当前用户 | 复用余额流水服务 |
| `favorite.list_products` | `FavoriteProduct_ListMine` | 当前用户 | 复用商品收藏服务 |
| `favorite.add_product` | `FavoriteProduct_Put` | 当前用户 | 复用商品收藏服务 |
| `favorite.remove_product` | `FavoriteProduct_Delete` | 当前用户 | 复用商品收藏服务 |
| `favorite.list_stores` | `FollowedStore_ListMine` | 当前用户 | 复用店铺收藏服务 |
| `favorite.add_store` | `FollowedStore_Put` | 当前用户 | 复用店铺收藏服务 |
| `favorite.remove_store` | `FollowedStore_Delete` | 当前用户 | 复用店铺收藏服务 |
| `file.create_upload_session` | `FileUploadSession_Create` | 当前用户 | 复用文件服务 |
| `file.complete_upload` | `FileUploadSession_Complete` | 当前用户上传会话 | 复用文件服务 |
| `file.get_scan_status` | `FileUploadSession_Get` | 当前用户上传会话 | 复用文件状态查询 |
| `vision.inspect_user_image` | Vision/OCR Adapter | 当前用户已通过扫描的文件 | 新增多模态工具 |
| `rag.policy.search` | Knowledge Retrieval Service | 已发布平台知识 | 统一现有 RAG 入口 |
| `memory.list_mine` | Memory Service | 当前用户授权记忆 | 接入新 Supervisor |
| `memory.remember_preference` | Memory Service | 当前用户授权记忆 | 复用确认策略并完善执行器 |
| `memory.delete_mine` | Memory Service | 当前用户授权记忆 | 复用确认策略并完善执行器 |
| `support.create_platform_ticket` | Messaging/Human Service | 当前用户会话 | 已有 Agent Tool |
| `support.get_ticket_status` | Messaging/Human Service | 当前用户会话 | 已有 Agent Tool |

UI Action 单独使用 `ui.*` 命名，例如 `ui.product.open`、`ui.order.open`、`ui.payment_panel.open`、`ui.refund.open`；它们由前端路由白名单执行，不注册为 MCP Tool。

#### 2.1.24 回复流与卡片一致性

以下是消息传输协议，不是固定业务 Workflow。Agent 仍然自主决定调用什么能力，但回复必须保证文字、卡片和实际执行结果一致。

| 状态 | 行为 |
| --- | --- |
| `run_started` | 建立 Agent Run 和右栏事件流，不创建虚假的 AI 消息 |
| `reasoning_streaming` | 右栏流式展示完整思考输出；聊天区可以显示真实的“AI 正在处理”状态 |
| `answer_drafting` | 回复文字只作为草稿，不提前写成已完成消息 |
| `card_assembling` | 根据已经验证的 Tool 结果生成卡片和动作 |
| `response_validating` | 验证文字结论、卡片数据、按钮状态和证据一致 |
| `message_committed` | 一次性提交最终文本块、卡片块、动作和证据引用 |
| `run_failed` | 保留右栏真实失败记录，聊天区只提交具体、可恢复的错误说明 |

| 异常 | 处理方式 |
| --- | --- |
| 文字提到卡片但卡片生成失败 | 重写最终文字，不得出现“请看下方卡片”却没有卡片 |
| Tool 成功但模型最终回复失败 | 使用确定性结果卡片和简短系统说明，不重复 Tool |
| 用户停止生成 | 停止未完成文字和卡片；已经执行的业务操作按真实结果显示 |
| SSE 重连 | 使用 `Last-Event-ID` 补发右栏和聊天增量，不重新运行任务 |
| 最终消息重复提交 | 按 `run_id + response_version` 幂等，只保留一个最终消息 |

#### 2.1.25 用户交互与界面体验

专属客服继续使用独立消息页面和三栏布局，不能退回小弹窗。三栏分别独立滚动，整页不应因聊天消息增长产生大面积空白滚动。

| 区域 | 桌面端设计 |
| --- | --- |
| 左栏会话列表 | 建议 260—300 px；专属客服固定置顶；显示头像、最后消息、时间和未读数 |
| 中栏聊天区 | 自适应剩余宽度，建议不小于 640 px；顶部、消息区、输入区结构稳定 |
| 右栏开发者视图 | 建议 360—440 px，可拖动调整；完整展示第 0 章内容并独立滚动 |
| 较窄屏幕 | 左栏可收起，右栏使用全高抽屉，但内容和详细程度不得减少 |

| 交互细节 | 设计要求 |
| --- | --- |
| 头像和气泡 | 用户在右、AI 在左；AI 使用统一 AI 图片头像；系统消息居中小字显示 |
| 消息宽度 | 普通文字不超过中栏约 72%；业务卡片可更宽，但不贴满两侧 |
| 输入框 | 固定在中栏底部，支持多行、自适应高度、Enter 发送、Shift+Enter 换行 |
| “+”菜单 | 支持发送商品、本人订单、图片和文件；选择器使用商城卡片而不是纯文字列表 |
| 发送状态 | 发送中按钮不可重复点击；失败消息保留原内容并提供“重试” |
| 停止生成 | AI 运行时发送按钮切换为“停止”；停止后清楚显示哪些操作已完成 |
| 快捷回复 | 只在有明确上下文时显示，例如“查看物流、申请售后、继续、取消” |
| 卡片点击 | 卡片主体可进入详情；按钮区域只执行对应动作，避免点击冲突 |
| 确认卡片 | 用不同视觉强调操作对象、影响和有效时间；确认与取消按钮位置稳定 |
| 当前咨询 | 顶部商品或订单上下文可点击进入详情，也可明确移除或切换 |
| 清除记录 | 只清空当前会话消息、短期记忆和待确认动作，不删除左栏专属客服入口 |
| 删除会话 | 专属客服是固定会话，不允许彻底删除入口；仅允许清除记录 |
| 自动滚动 | 用户位于底部时跟随新消息；用户正在看历史时不强拉到底部，显示“有新消息”按钮 |
| 加载状态 | 使用卡片骨架和真实运行事件；不闪屏、不刷新整页、不丢失滚动位置 |
| 错误提示 | 在发生问题的位置说明具体原因，保留用户输入；禁止统一提示“网络异常” |
| 中文与金额 | 状态全部中文；金额统一 `¥0.00`；日期使用本地时区和易读格式 |
| 无障碍 | 键盘可操作、焦点可见、按钮有语义标签、颜色不是唯一状态提示 |

| AI 回复体验 | 要求 |
| --- | --- |
| 首句 | 先直接回答用户当前问题，不重复介绍“我是专属客服” |
| 长度 | 默认给结论和 1—3 条关键说明，详细数据放卡片或可展开区域 |
| 推荐 | 先展示 3—6 个最相关商品，说明与需求对应的简短理由 |
| 多任务 | 按用户原始诉求分块展示，并标记已完成、待确认或失败 |
| 澄清 | 一次只问最关键的问题；能用商品或订单卡片选择时不用用户输入编号 |
| 主动性 | 完成当前任务后只给最相关的 1—2 个下一步，不连续营销或反复追问 |
| 真实性 | 不编造库存、价格、物流、规则和操作结果；不知道时直接说明缺少什么 |

#### 2.1.26 自动化评估体系

| 测试集 | 核心检查 | 主要指标 |
| --- | --- | --- |
| 商品选购 | 条件理解、推荐相关性、款式库存和商品卡片 | 硬条件满足率、卡片正确率 |
| 购物车与结算 | 增删改、价格库存变化、下单和支付入口 | 工具参数正确率、任务完成率 |
| 订单与物流 | 多订单、多包裹、状态和连续指代 | 指代正确率、事实一致性 |
| 售后与评价 | 部分退款、凭证、撤销、申诉、评价和追评 | 资格判断率、确认覆盖率 |
| 复合请求 | 跨领域拆分、并行、综合和漏项 | 意图覆盖率、漏项率 |
| 长对话 | 话题切换、暂停恢复、纠正、否定、清除记录 | 20 轮连续成功率 |
| 权限与并发 | 多用户、多店铺、多标签页、重复确认 | 越权阻断率、重复写入率 |
| 故障恢复 | 模型、Tool、RAG、SSE 和服务重启 | 恢复率、伪成功率 |
| 界面交互 | 卡片、按钮、附件、滚动、重试和响应式布局 | 操作成功率、前端错误率 |
| 右栏追踪 | 思考、计划、委派、工具与 Trace 一致 | 事件完整率、虚假事件数 |
| 回复体验 | 简洁、相关、可操作、中文状态和错误说明 | 人工评分、用户负反馈率 |

Golden Dataset 每条用例至少保存：初始数据快照、多轮消息、卡片事件、预期 Agent、允许和禁止的工具、预期业务变化、预期卡片、右栏关键事件和禁止回复。模型、Prompt、Agent、Skill、Tool 或 RAG 版本升级都必须运行回归。

#### 2.1.27 旧 Agent 与历史会话迁移

本次改造不能通过删除旧会话、重新创建“专属客服”来上线。用户左栏置顶会话、消息历史、人工工单和业务卡片必须连续保留；迁移对象是 Agent 定义及运行状态，不是用户会话本身。

| 迁移对象 | 迁移规则 |
| --- | --- |
| Agent 标识 | 旧 `exclusive_support` 映射到新 `user_exclusive_support_supervisor`；保留别名解析，历史 Trace 仍显示当时真实 Agent 与版本 |
| 固定会话 | 保留原 `conversation_id`、参与者、置顶位置、未读数和全部消息，不新建第二个“专属客服”会话 |
| 新消息 | 灰度命中的用户从下一条新消息开始使用新 Agent Version；不得让同一消息同时产生新旧两份回复 |
| 正在运行的旧 Run | 纯只读 Run 可以按旧版本结束；尚未调用写 Tool 的 Run 取消并由新版本重建计划 |
| 待确认操作 | 旧版本生成但尚未确认的 L2/L3 操作全部失效；新版本依据最新业务数据重新生成确认卡片和 `confirmation_token` |
| 人工服务 | 已创建或正在处理的人工工单不迁移、不重复建单；人工接管期间新 Agent 继续保持暂停 |
| 短期状态 | 只迁移已验证的当前焦点、商品/订单资源引用、未完成问题和人工接管状态；不继承旧的自由文本计划、猜测和未执行动作 |
| 长期记忆 | 保留用户已授权且仍有效的事实与偏好；升级记忆 Schema 时按版本转换，失败条目隔离重建而不是静默丢弃 |
| 对话摘要 | 原消息永远是事实来源；旧摘要可只读保留，新版本首次使用时按需重新生成带版本号的摘要 |
| RAG 引用 | 历史回复保留当时引用快照；新 Run 只检索当前已发布知识版本，不能复用已经撤回的旧检索结果 |
| 右栏 Trace | 旧 Trace 只读保存；新 Trace 使用第 0 节事件 Schema，并通过 `agent_version`、`prompt_version`、`tool_version` 区分 |
| 回滚 | 保留上一个已发布 Agent Version 和路由开关；回滚只影响新 Run，不反向重放消息或撤销已完成业务操作 |

采用按用户稳定哈希的 `5% → 25% → 50% → 100%` 灰度发布，同一用户在灰度期间稳定落到同一版本，避免连续对话中途无故切换。上线前可以使用 Shadow Run 比较新旧规划和回复质量，但 Shadow Run 只能读取主 Run 已获授权的同一输入，不得扩大数据范围、调用写 Tool、发送第二条消息、创建工单或改变未读数。

迁移必须满足以下验收条件：专属客服会话数量不增加；历史消息和卡片无丢失；同一用户消息只出现一个 AI 回复；待确认写操作不会跨版本继续执行；人工接管不中断；灰度和回滚均不重复执行 Tool；新旧版本 Trace 均可追溯。

#### 2.1.28 实施优先级与验收

| 优先级 | 实施内容 | 验收重点 |
| --- | --- | --- |
| P0 | 动态 Agent 循环、多意图计划、计划修订和领域委派 | 不依赖固定意图 Workflow；新观察能够改变下一步 |
| P0 | 连续聊天状态、焦点栈、暂停恢复、用户/会话隔离 | 不漏任务、不串用户，“好/它/这个/继续刚才的”正确续接 |
| P0 | L0—L4 操作分级、UI Action 与 Tool 分离、写操作预览—确认—执行—回读 | L2/L3 确认覆盖率 100%；L4 只给页面入口；不把前端跳转伪装成后端 Tool |
| P0 | 商品、购物车、订单、物流、退款、地址卡片统一 | 不以内部编号或长文字代替业务卡片 |
| P0 | 右栏真实 Agent 事件和中文决策说明 | 不显示预制步骤；Agent、Skill、Tool、RAG、返工与证据可核验 |
| P0 | 领域 Agent Contract 与确定性结果验证器 | 输入输出可校验；越权、金额、状态、版本和伪成功被拦截 |
| P0 | Agent Tool—API—权限映射与 Tool Contract | 每个 Tool 能映射到现有 FastAPI Service 或明确的内部服务，不绕过业务权限和状态机 |
| P0 | 回复流、卡片和业务结果原子提交 | 不出现文字与卡片矛盾、重复消息或成功假象 |
| P0 | 三栏消息页面与核心交互 | 输入、附件、停止、滚动、重试、确认和跳转稳定可用 |
| P1 | 7 个领域 Agent、Skill 白名单、并行和返工 | Agent 只访问自己的领域和当前用户数据 |
| P1 | 首次问候、快捷入口、图片和售后/评价附件 | 不重复问候；上传失败不丢文字和已成功附件 |
| P1 | RAG 来源、短期记忆、授权长期偏好 | 动态事实不走 RAG，敏感数据不进记忆 |
| P1 | 多标签页、消息乱序、断线恢复和重复确认 | 不丢消息、不重复写入、旧 Run 不覆盖新结果 |
| P1 | Token、模型、Tool、延迟和成本预算 | 简单请求不滥用 Multi-Agent，超预算诚实返回部分结果 |
| P1 | 模型路由、降级候选和实际请求追踪 | 配置、运行请求和 Trace 中的模型版本一致；降级后仍遵守相同权限边界 |
| P1 | Agent/Skill/Tool/确认/人工接管 Trace | 每轮能还原真实执行过程、计划变化和失败位置 |
| P1 | 完整 Trace 的分页、冷热分层与保留策略 | 长会话不卡顿；实时、索引、完整审计数据均可按期限检索和清理 |
| P1 | 旧 Agent、历史会话、确认卡片和人工工单迁移 | 不新建重复会话、不重复回复、不跨版本执行旧确认，能够安全灰度和回滚 |
| P2 | Golden Dataset、连续 20 轮、多意图、附件、UI、越权和故障回归 | 金额数量正确、交互友好、跨用户阻断、部分失败可恢复 |

专属客服只有同时通过后端单元与集成测试、Tool Contract Test、权限隔离测试、Multi-Agent 测试和真实浏览器端到端测试，才视为完成。

#### 2.1.29 现行实现基线与回归结论（2026-09-14）

| 项目 | 已落地实现 | 本轮实测结论 |
| --- | --- | --- |
| 版本化短期状态 | MySQL `ai_conversation_states`，状态协议 `conversation_state_v2` | 1600+ 消息的长会话仍能保存当前领域、卡片集合、历史话题、纠正记录和最近成功动作 |
| 分层上下文 | 约 12,000 字符的有界规划输入；状态、摘要、会话档案和最近原文分别限额 | 规划不再回放全部历史；滚动摘要维持在有界规模 |
| Supervisor | 正常业务由模型 Supervisor 动态规划；复合请求按领域拆分并并行执行独立只读任务 | “推荐三件商品 + 查余额 + 默认地址”正确拆为三个任务并返回三类卡片 |
| 确定性安全边界 | 只保留身份、权限、状态机、确认、幂等、否定和只读试算等不可被模型覆盖的规则 | “不要删除，只查询”未误删；“改成 3 件只试算”返回 ¥112 → ¥336，购物车数量和版本均未改变 |
| 卡片集合与焦点 | 多卡集合、集合版本和当前焦点分开保存 | 先查询三件商品、切到订单话题后，仍可回到原集合查询第三件；纠正为第一件时不再重新搜索 |
| RAG 切片 | `semantic-markdown-v3`，按标题和语义边界切片，包含来源类型、权限范围、商品和图片顺序等元数据 | 平台物流规则正确命中、引用和展示；动态物流状态仍只允许业务 Tool 查询 |
| 父子检索 | 小切片负责匹配，只在同一语义父段内扩展并去重 | 不跨标题、店铺或商品拼接上下文 |
| 真实执行轨迹 | 记录模型、规划来源、委派、工具、参数、结果、RAG、记忆和完成状态 | 浏览器右栏可区分 `provider_model_supervisor` 与 `deterministic_read_only_guard`，未发生的步骤不显示为已执行 |

上述结论只代表本轮覆盖的专属客服场景通过，不等于 2.1 工具清单中所有“需接入/需新增”能力已经完成。后续每增加一个 Tool、Skill 或写操作，仍必须补充 Contract Test、权限测试和真实浏览器回归。

### 2.2 店铺 AI 客服

#### 2.2.1 定位与目标

| 项目 | 设计 |
| --- | --- |
| 页面显示名 | 当前店铺名称 + AI 客服标识 |
| 主 Agent 全称 | 店铺 AI 客服 Supervisor Agent（店铺顾客服务总管） |
| Agent 代码 | `store_customer_support_supervisor`，兼容旧代码 `store_support` |
| 服务对象 | 正在咨询该店铺的当前登录用户 |
| 数据范围 | 当前用户 + 当前店铺；公开商品可直接查询，本店订单必须再次校验用户归属 |
| 会话规则 | 每个用户与每个店铺最多一个有效会话；换商品不新建会话，只切换当前咨询卡片 |
| 核心目标 | 用最少追问解决商品、款式、库存、店铺政策、本店订单和物流问题，并在需要时转本店人工客服 |
| 禁止范围 | 跨店商品和订单、其他顾客数据、平台账户资产、代付款、代提交退款、商家内部经营数据 |

店铺客服必须首先理解用户正在咨询的商品、订单或店铺，而不是每轮重复“我是本店智能客服”。商品页面进入会话时，前端应传递可信商品卡片引用；用户只说“最大码、几天发货、用什么快递”时，Agent 应先结合当前商品及详情 OCR，再决定是否需要工具或追问。

#### 2.2.2 Supervisor 职责与动态运行

| 阶段 | 职责 | 店铺场景要求 |
| --- | --- | --- |
| 理解 | 识别商品、SKU、库存、店铺规则、订单、物流、售后咨询和人工诉求 | “这件最大码还有货吗，多久发”至少包含 SKU、库存和发货三项 |
| 定位 | 解析当前商品卡片、本店订单卡片和上下文指代 | “这个、红色那款、刚才第二件、我的那单”必须绑定正确资源 |
| 规划 | 动态选择最少领域 Agent，并根据新事实调整计划 | 商品资料与库存可并行；订单物流必须先确认订单归属 |
| 复核 | 检查回答是否覆盖全部问题、是否使用实时数据、是否越店 | 不得用商品详情中的旧库存代替实时库存 Tool |
| 澄清 | 多个商品、订单或 SKU 均可能匹配时给出卡片供选择 | 不要求用户复制内部商品号或订单号 |
| 转人工 | 用户明确要求或问题需要店铺承诺、人工判断时创建本店工单 | AI 停止抢答；人工结束后恢复上下文并询问是否解决 |
| 回复 | 文字简短，事实用商品、SKU、订单、物流、规则来源卡片表达 | 不把所有商品参数、所有 SKU 和全部订单状态一次性倾倒给用户 |

运行方式复用 2.1.3 的 Observe—Reason—Decide—Act—Critique 动态循环。关键词只用于越权、明确人工转接、否定写操作等安全边界；正常商品理解、连续追问和多意图拆分由模型 Supervisor 完成。

#### 2.2.3 领域 Agent

| 完整名称 | Agent 代码 | 负责范围 | 主要结果组件 |
| --- | --- | --- | --- |
| 店内商品顾问 Agent | `storefront_catalog_advisor_agent` | 当前店铺商品搜索、商品介绍、用途匹配、同店推荐、商品和 SKU 比较 | 商品列表、商品卡片、对比卡片 |
| 商品规格与库存 Agent | `storefront_variant_inventory_agent` | 当前商品款式、颜色、尺码、规格、价格和实时可售库存 | SKU 卡片、规格表、库存状态 |
| 本店订单履约 Agent | `storefront_order_fulfillment_agent` | 当前用户在本店的订单列表、订单详情、支付/履约状态和物流轨迹 | 本店订单卡片、包裹卡片、物流时间线 |
| 店铺服务知识 Agent | `storefront_service_knowledge_agent` | 店铺发货地、承运方式、发货时效、包邮、退换、FAQ 和商品详情 OCR | 简短回答、规则来源卡片、商品详情片段 |
| 本店售后引导 Agent | `storefront_after_sale_guidance_agent` | 解释本店订单是否有售后入口、需要准备的材料及当前售后状态 | 售后入口卡片、材料清单、状态卡片 |

`store_human_service_coordinator` 是确定性的人工工单协调器，不是 LLM Agent。一个问题能由同一领域 Agent 完成时不得拆成多个 Agent；例如“介绍商品并推荐款式”由店内商品顾问处理，只有必须核验实时库存时才并行委派规格库存 Agent。

#### 2.2.4 Skill 设计

| Skill 代码 | 使用 Agent | 处理内容 | 主要工具 |
| --- | --- | --- | --- |
| `storefront_product_advice` | 店内商品顾问 | 商品介绍、场景匹配、同店搜索和推荐 | `catalog.search_store_products`、`catalog.get_product` |
| `storefront_variant_comparison` | 商品顾问 + 规格库存 Agent | SKU 比较、最大/最小规格、价格和库存 | `catalog.compare_store_skus`、`catalog.get_inventory_availability` |
| `storefront_product_detail_qa` | 商品顾问 + 服务知识 Agent | 参数、FAQ、详情文字和图片 OCR 问答 | `catalog.get_product_content`、`rag.store_product.search` |
| `storefront_service_policy_qa` | 服务知识 Agent | 发货、快递、包邮、退换和服务政策 | `catalog.get_store_policy`、`rag.store_policy.search` |
| `storefront_order_assistance` | 本店订单履约 Agent | 本店订单列表、详情和状态解释 | `order.list_user_store_orders`、`order.get_store_order_summary` |
| `storefront_logistics_tracking` | 本店订单履约 Agent | 本店订单包裹和物流时间线 | `logistics.get_store_order_shipments`、`logistics.get_shipment_tracks` |
| `storefront_after_sale_guidance` | 本店售后引导 Agent | 售后入口、资格说明、材料与进度 | `after_sale.get_store_order_eligibility`、`after_sale.list_user_store_refunds` |
| `storefront_cart_assistance` | 商品顾问 | 将用户明确选择的本店 SKU 加入本人购物车 | `cart.add_store_item.preview`、`cart.add_item` |
| `storefront_handoff` | Supervisor + 协调器 | 转本店人工、查询排队状态和恢复 AI | `support.create_store_ticket`、`support.get_ticket_status` |

#### 2.2.5 Tool 清单与权限

| 领域 | Tool | 类型 | 数据与行为边界 | 状态 |
| --- | --- | --- | --- | --- |
| 商品 | `catalog.search_store_products` | query | 只返回当前店铺公开在售商品 | 已有 |
| 商品 | `catalog.get_product` | query | 只读当前店铺商品、公开属性、SKU 和 FAQ | 已有 |
| 商品 | `catalog.get_product_content` | query | 读取商品详情块、图片顺序和已确认 OCR 说明 | 已合并到 `catalog.get_product` 的可信商品资料投影 |
| 商品 | `catalog.compare_store_skus` | query | 比较当前店铺内指定 SKU，不允许模型传入其他店铺范围 | 已有，运行时代码为 `catalog.compare_skus` |
| 库存 | `catalog.get_inventory_availability` | query | 实时查询当前店铺 SKU 可售库存 | 已有 |
| 规则 | `catalog.get_store_policy` | query | 当前店铺结构化发货与售后政策 | 已有 |
| RAG | `rag.store_product.search` | retrieval | 强制过滤当前 `store_no`、商品状态及可选 `product_no` | 已通过店铺范围知识检索接入；执行轨迹记录真实来源 |
| RAG | `rag.store_policy.search` | retrieval | 只检索当前店铺已发布政策和 FAQ | 已有；无可靠命中时回到结构化政策或明确拒答 |
| 评价 | `review.get_product_summary` | query | 查询当前商品公开评分、数量和评价摘要 | 已合并到 `catalog.get_product` 的公开评价投影；仅返回已发布评价与有限摘要 |
| 订单 | `order.list_user_store_orders` | query | 只返回当前用户在当前店铺可见订单 | 已有 |
| 订单 | `order.get_store_order_summary` | query | 同时校验用户、店铺和订单归属 | 已有 |
| 物流 | `logistics.get_store_order_shipments` | query | 只返回上述本店订单的包裹 | 已有 |
| 物流 | `logistics.get_shipment_tracks` | query | 返回当前用户本店包裹的完整时间线 | 已由 `logistics.get_store_order_shipments` 返回最近可信轨迹；完整页面由订单入口打开 |
| 售后 | `after_sale.get_store_order_eligibility` | query | 只解释资格和入口，不提交退款 | 已由本店订单实时状态和可操作项只读投影实现 |
| 售后 | `after_sale.list_user_store_refunds` | query | 查询当前用户在本店的售后进度 | 已实现：独立 `after_sale_progress` 意图委派本店售后引导 Agent，按 `user_id + store_id` 双重过滤最近售后，返回商品、金额、原因、状态、时间和售后入口卡片；不创建或提交新申请 |
| 购物车 | `cart.add_store_item.preview` | preview | 校验 SKU、库存、价格和当前店铺，展示将加入内容 | 已内置在加购前校验；多款式不明确时只展示商品选择卡，不写入 |
| 购物车 | `cart.add_item` | mutation | 只允许加入当前店铺公开 SKU，执行后回读购物车 | 已实现；服务端强制校验会话店铺、在售商品、有效 SKU、实时库存和当前用户，成功后返回购物车卡片 |
| 人工 | `support.create_store_ticket` | handoff | 为当前店铺当前用户创建人工工单并暂停 AI | 已有 |
| 人工 | `support.get_ticket_status` | query | 查询当前会话工单状态 | 已有 |

店铺客服不获得订单取消、确认收货、评价提交、退款提交、地址管理、余额、充值、支付、跨店收藏及店铺后台写工具。需要这些能力时返回相应用户端弹窗或引导使用专属客服，而不是伪装执行成功。

截至 2026-09-14 的真实回归还确认：最大包装、价格与库存由一次 SKU 查询回答；条件式请求会先读取实时库存，再决定是否继续委派推荐 Agent；本店订单列表可展示至 8 张可点击订单卡；金额指代会重新读取业务主库；“推荐后把第一件加入购物车”会完成本店 SKU 范围校验并回传完整购物车卡片；商品评价问答使用已发布评价摘要；已提交售后使用独立售后意图、工具和进度卡，且同时校验当前用户与当前店铺。

#### 2.2.6 上下文、短期记忆与连续聊天

| 状态内容 | 保存规则 | 典型用途 |
| --- | --- | --- |
| 当前店铺 | 会话创建时由后端绑定，全生命周期不可由 Prompt 改写 | 防止跨店读取 |
| 当前商品与 SKU | 保存可信卡片引用、版本和当前焦点 | “这个、红色款、最大码” |
| 商品卡片集合 | 集合顺序、查询条件和当前焦点分开保存 | “刚才推荐的第三件”“不是第三件，是第一件” |
| 本店订单与包裹 | 保存引用和显示集合，不保存易变状态结论 | “刚才那单到哪了”时重新调用订单/物流 Tool |
| 当前任务 | 未回答子问题、等待选择、等待人工和暂停任务 | 插入问题后继续原任务 |
| 用户纠正 | 商品、SKU、预算、用途、尺码和订单纠正 | 避免继续沿用错误对象 |
| 店铺长期偏好 | 默认不建立 | 店铺客服不得把一次咨询写成跨店用户画像 |

店铺客服短期状态按 `user_id + store_id + conversation_id` 隔离，复用 `conversation_state_v2`。进入新商品只新增话题帧；明确“换个问题”才关闭旧焦点。清除记录后删除该会话状态、摘要、待确认和人工前上下文；平台依法保留的审计记录不再参与回答。

#### 2.2.7 商品资料、OCR 与 RAG

| 信息 | 首选来源 | 处理规则 |
| --- | --- | --- |
| 名称、SKU、价格、库存 | 业务主库 Tool | 每次实时读取，不能从 RAG 得出当前库存和价格 |
| 商品参数、FAQ、详情块 | 商品结构化内容 Tool | 按用户问题只取相关字段 |
| 详情图片文字 | 已完成 OCR 且由商家/管理员确认的图片说明 | 作为商品事实补充，保留图片顺序和来源图片 |
| 店铺发货与售后政策 | 结构化店铺政策 + 已发布店铺 RAG | 冲突时以当前结构化政策为准并记录冲突 |
| 用户订单和物流 | 订单/物流 Tool | 禁止进入知识库或长期记忆 |

RAG 必须先按当前店铺 ACL 过滤，再按可选商品、文档类型、发布状态和版本过滤。切片不得跨商品、SKU、FAQ 或图片；命中 OCR 时，回答卡片可提供“查看商品详情”而不向用户展示后台 OCR 字段名。

#### 2.2.8 复合请求与连续提问示例

| 用户表达 | 动态处理 | 结果形式 |
| --- | --- | --- |
| “这个最大码还有货吗，多久发？” | 商品顾问定位当前商品；规格库存与服务知识并行查询 | 目标 SKU 卡片 + 库存 + 发货说明 |
| “你们店有什么适合考试的文具？” | 店内商品顾问按用途搜索当前店铺 | 3—5 张可点击商品卡，不输出长列表文字 |
| “第二件有哪些款式？” | 从最近店内商品集合定位第二件，再查 SKU | 当前商品卡 + SKU 卡片 |
| “不是第二件，是第一件” | 写入纠正并切换焦点，不重新进行模糊搜索 | 更新后的第一件商品卡 |
| “我在你店买过什么？” | 本店订单履约 Agent 实时查询 | 本店订单卡片列表 |
| “它到哪里了？” | 沿用上一轮唯一订单；有多单则展示选择卡 | 物流时间线卡片 |
| “付款后几天发，用什么快递？” | 优先读取商品详情、OCR 和店铺政策；不是订单查询 | 简短回答 + 商品/政策来源卡 |
| “帮我退款” | 定位本店订单并查询售后入口；提示由专属客服或订单售后弹窗继续 | 订单卡 + 售后入口，不直接提交 |
| “比较你们店这两件并把便宜的加购物车” | 先比较，唯一结果后生成加入预览；必要时让用户选 SKU | 对比卡 + 购物车操作卡 |
| “看看别家有没有更便宜的” | 说明本会话只能服务当前店铺，给出打开专属客服入口 | 不调用跨店 Tool |

#### 2.2.9 回复、卡片与人工接管体验

| 体验项 | 要求 |
| --- | --- |
| 首次进入 | 只问候一次，并结合当前商品卡主动说明可问款式、库存、发货或本店订单 |
| AI/人工头像 | AI 使用统一 AI 图片；人工回复使用真实店铺 Logo 或店铺名文字头像 |
| 气泡 | 内容自适应，上限为消息区域 80%；结构化卡片可使用更宽容器 |
| 当前咨询 | 顶部显示可点击商品或订单卡片，可切换或移除，不显示内部版本文字 |
| 多结果 | 同类型商品卡可网格排列；商品、订单、物流等不同类型分行并留间距 |
| 卡片详情 | 聊天内点击使用大弹窗，关闭后保持原滚动位置 |
| 人工接管 | AI 发居中系统消息后停止回复；店铺端即时出现红色未读数和该会话 |
| 人工结束 | 居中显示短暂系统消息，AI 恢复后询问“问题是否解决”，提供按钮 |
| 失败 | 说明具体失败域，保留已成功卡片，并允许只重试失败部分 |

#### 2.2.10 结果验证与安全边界

| 校验 | 必须验证的内容 |
| --- | --- |
| 店铺隔离 | 所有 Tool 返回的 `store_no` 必须等于会话绑定店铺 |
| 用户归属 | 订单、包裹、售后必须属于当前登录用户 |
| 商品状态 | 只推荐公开在售且店铺营业中的商品；已下架商品只在本人历史订单入口展示 |
| 金额与库存 | 使用最小货币单位和实时库存，不从模型文本解析为最终事实 |
| 完整性 | Supervisor 逐项核对复合请求，缺失项必须补查或明确失败 |
| 越权 | 用户文本、卡片描述和图片 OCR 均不能覆盖后端注入的用户或店铺范围 |
| 写操作 | 仅开放受限的加入购物车；其余交易写操作不提供 Tool |
| 人工状态 | `waiting_human`、`human_active` 时 AI 不得自动抢答 |

#### 2.2.11 性能、降级与恢复

| 情况 | 处理方式 |
| --- | --- |
| 简单商品事实 | 一个领域 Agent、一个或两个只读 Tool，避免无意义 Multi-Agent |
| 多领域只读 | 在共享总预算内并行，单个失败不取消其他成功结果 |
| 模型超时 | 使用经过验证的结构化 Tool 结果和卡片降级，不退回空泛欢迎语 |
| RAG 无结果 | 回答已知结构化事实；其余明确说明店铺尚未提供，必要时转人工 |
| Tool 超时 | 不声称成功；标记结果未知并允许按同一幂等键重试 |
| 服务重启 | 从检查点、会话状态和最新消息序号恢复，禁止重复回复或重复创建工单 |

#### 2.2.12 验收矩阵

| 类别 | 必测内容 | 通过标准 |
| --- | --- | --- |
| 商品 | 介绍、用途、参数、OCR、FAQ、SKU、库存、推荐和比较 | 答非所问率为 0；卡片可操作；实时字段正确 |
| 连续对话 | 短回答、纠正、序号、话题切换后返回、20 轮以上混合咨询 | 焦点正确，不重复欢迎，不丢卡片集合 |
| 订单物流 | 本店多订单、指定订单、无订单、已下架商品、多个包裹 | 不跨店、不串用户，状态来自实时 Tool |
| 复合任务 | 商品 + 库存 + 发货、订单 + 物流、比较 + 加购物车 | 每个目标都有结果或明确失败，不静默漏项 |
| 越权 | 查询别店、别人的订单、Prompt Injection、伪造卡片 ID | 100% 拦截且不调用越权 Tool |
| 人工 | 转接、排队、接管、人工回复、结束和 AI 恢复 | 两端实时同步，AI 不抢答，历史上下文连续 |
| 并发 | 多用户同时咨询同店、多店同时咨询、同用户多标签页快速发送 | 会话、状态、未读数和流式输出不串流 |
| 故障 | 模型、RAG、Tool、SSE、Worker 重启 | 不伪成功、不重复写入，可重试失败部分 |
| UI | 头像、气泡、卡片、弹窗、滚动、移动端和右栏轨迹 | 无遮挡、无跳页、关闭弹窗后保持位置 |

#### 2.2.13 实施顺序

| 顺序 | 实施内容 | 完成证据 |
| ---: | --- | --- |
| 1 | 将 `conversation_state_v2`、摘要 v2、序号锁和清除逻辑接入店铺会话 | 状态与清除测试 |
| 2 | 建立模型优先的店铺 Supervisor 与领域能力目录 | 规划、复合请求和降级测试 |
| 3 | 收紧现有 Store Tool Gateway，并补商品内容、RAG、售后只读和购物车工具 | Tool Contract 与越权测试 |
| 4 | 统一商品、SKU、订单、物流、规则和人工卡片 | 组件与浏览器测试 |
| 5 | 接入真实 Trace、检查点、人工接管和失败恢复 | Trace、并发和恢复测试 |
| 6 | 使用多个用户、多个店铺完成真实浏览器长对话回归 | 截图、运行记录和数据库不变量 |

#### 2.2.14 第一轮优化：业务闭环与证据优先级

第一轮审校不增加 Agent 数量，而是补齐容易在真实咨询中出错的边界：

| 风险场景 | 优化后的规则 |
| --- | --- |
| 商品集合被单品覆盖 | `store_product_card_set` 与 `active_product/active_sku` 分开保存；查看第二件详情后仍能回到原商品集合 |
| 商品咨询误判成订单查询 | “几天发、用什么快递、材质、最大码”等默认属于当前商品；只有用户明确说“我的订单、买过、这单、物流到哪”才查询本人订单 |
| 订单问题被商品焦点覆盖 | 一旦用户明确切换到本人订单，订单任务使用独立 `store_order_card_set`；商品焦点仅作为订单商品展示信息，不改变订单目标 |
| 多个 SKU 都符合 | 不擅自选择库存最多或价格最低款，展示紧凑 SKU 卡让用户选择；用户明确条件能唯一命中时直接回答 |
| 店铺暂停营业 | 不再推荐或加入购物车；仍允许用户查询本人历史订单、物流、售后入口和人工服务 |
| 商品下架 | 不作为新购买候选；本人历史订单中保留商品快照和“已下架”提示，不跳到不存在页面 |
| OCR 与结构化字段冲突 | 实时业务字段 > 商家确认的结构化内容 > 已确认 OCR > 未确认 OCR；发现冲突时不自行选一个答案，提示店铺核实 |
| 店铺政策与商品详情冲突 | 当前结构化店铺政策优先；商品特定承诺只有在已发布且仍有效时覆盖通用政策，并展示依据 |
| 推荐理由空泛 | 每个推荐必须对应用户的用途、预算、规格或库存条件；没有证据时只展示候选，不编造“最适合” |
| 售后越界 | 店铺客服可以解释本店规则和提供入口，但不能以店铺立场承诺一定退款、一定赔付或代替用户提交 |

店铺客服采用以下固定事实证据顺序：实时业务 Tool > 当前有效商品/订单快照 > 商家确认的结构化内容 > 当前店铺已发布 RAG > 已确认 OCR > 会话摘要 > 模型推测。模型推测不得成为价格、库存、发货承诺、订单、物流或售后结论。

#### 2.2.15 第二轮优化：安全、交互与失败可恢复性

| 审校项 | 第二轮优化结果 |
| --- | --- |
| 渐进式披露 | Supervisor 只看到店内商品、规格库存、订单履约、服务知识、售后引导五类能力摘要；选中领域后才注入该 Skill 与最小 Tool Schema |
| 参数安全 | Tool Gateway 丢弃模型提供的 `user_id/store_id`，由认证会话和店铺会话重新构建；资源号只作为候选，再做归属校验 |
| 不可信内容 | 商品文案、FAQ、OCR、用户消息和 RAG 文本全部视为数据，不能指示 Agent 忽略权限、调用工具或泄露系统内容 |
| 否定与假设 | “别加购物车、只看看、如果买三件多少钱”强制进入只读查询或试算，模型规划不能覆盖这一约束 |
| 多任务部分失败 | 已成功的商品/订单卡正常展示；失败任务在原位置显示原因和“只重试此项”，不得整轮退化为网络异常 |
| 响应速度 | 首屏先流式显示真实分析摘要；可验证的结构化卡片完成后立即发送，模型润色超时不阻塞卡片 |
| 长结果 | 默认展示最相关 3—5 项；“查看更多”在聊天弹窗中分页，不能一次塞入全部 SKU、评价或订单 |
| 人工转接 | 只有用户明确要求、需要店铺承诺或连续两次无法解决时建议转人工；不得因普通订单卡片或“好”自动转人工 |
| 重复消息 | 每个用户消息只允许一个有效 AI Run；消息写入前锁定并刷新序号，重启和重试复用 `run_id` 与幂等键 |
| 可访问性 | 卡片支持键盘操作和清晰焦点，颜色状态同时有文字，弹窗关闭后焦点与聊天滚动位置恢复 |

两轮优化后的 2.2 设计以“回答正确且可继续操作”为完成标准，而不是以调用了模型或返回 HTTP 200 为完成标准。

## 3. 店铺端

### 3.1 AI 经营助理

#### 3.1.1 定位与工作台关系

| 项目 | 设计 |
| --- | --- |
| 页面显示名 | AI 经营助理 |
| 主 Agent 全称 | AI 经营助理 Supervisor Agent（店铺经营总管） |
| Agent 代码 | `merchant_operations_supervisor`，兼容旧代码 `merchant_copilot` |
| 服务对象 | 已登录且具有当前店铺权限的运营人员 |
| 会话规则 | 每个店铺一个固定置顶经营助理会话；切换店铺必须切换独立会话与状态 |
| 数据范围 | 当前运营人员被授权的当前店铺，包含店铺资料、商品、库存、订单、售后、评价、顾客服务和经营指标 |
| 核心目标 | 把经营问题转为可验证的数据、建议、草稿、确认卡和业务结果，减少运营人员在多个后台页面之间切换 |
| 人工入口 | 对平台规则、账号和平台审核问题转平台人工客服；不是转本店顾客客服 |

店铺端左侧会话列表分为“AI 经营助理”和可折叠的“顾客会话”。顾客会话用于运营人员作为人工客服直接服务顾客；AI 经营助理是店铺私有工作空间，两者共享商品、订单等卡片组件，但不得混用聊天对象、未读数、短期记忆或消息权限。

#### 3.1.2 运营人员在消息中的直接能力

| 操作 | 交互形式 | 约束 |
| --- | --- | --- |
| 发送本店商品 | 输入框 `+` 打开商品选择器，卡片发送给当前顾客 | 只允许当前店铺公开商品或明确标注的下架历史商品 |
| 发送该顾客的本店订单 | 输入框 `+` 打开本店订单选择器 | 必须同时匹配当前顾客和当前店铺 |
| 发送物流卡 | 从订单卡选择包裹并发送 | 使用实时物流数据，号码按顾客可见规则展示 |
| 发送售后处理卡 | 展示处理结论、补充材料或退货地址 | 只能作用于当前顾客的本店售后单 |
| 快捷回复 | 选择店铺审核过的话术，发送前可编辑 | 不允许模板绕过内容安全审核 |
| AI 辅助拟稿 | 让经营助理根据当前会话和业务事实生成回复草稿 | 默认仅填入输入框，运营人员确认后发送 |
| 结束人工服务 | 点击后生成居中系统消息并恢复店铺 AI 客服 | 必须同步用户端状态和未读数 |

#### 3.1.3 Supervisor 职责与动态运行

| 阶段 | 职责 | 经营场景要求 |
| --- | --- | --- |
| 理解 | 识别经营目标、时间范围、商品/订单/顾客对象、指标口径和期望动作 | “把低库存商品列出来并给销量最高的补到 100 件”包含查询、排序、选择和库存写入 |
| 规划 | 按依赖关系拆为经营分析、业务查询、草稿、确认和执行 | 指标查询可并行；同一 SKU 写操作串行 |
| 建议 | 将数据转成有证据的可执行建议 | 说明依据和风险，不把相关性写成因果关系 |
| 执行 | 只在权限、状态、版本和确认均有效时调用写 Tool | 模型不能直接更新数据库 |
| 复核 | 核对店铺范围、金额、库存、订单状态、执行数量和写后结果 | 所有写操作回读；部分失败逐项展示 |
| 协作 | 可为顾客会话生成草稿，或转平台人工处理平台侧问题 | 不以 AI 身份冒充运营人员发送未经确认的承诺 |

运行方式复用统一动态 Agent 循环。经营助理可以根据工具结果调整计划，例如发现目标商品正在审核中时，停止“立即上架”并改为展示缺失项；发现订单已经发货时，不再执行重复发货。

#### 3.1.4 领域 Agent

| 完整名称 | Agent 代码 | 负责范围 | 主要结果组件 |
| --- | --- | --- | --- |
| 店铺经营分析 Agent | `merchant_business_insight_agent` | 营业额、销量、商品、订单、转化和时间对比 | KPI 卡、趋势卡、经营建议卡 |
| 店铺与商品运营 Agent | `merchant_store_catalog_agent` | 店铺资料、Logo、营业状态、服务政策，以及商品草稿、SKU、详情、OCR、审核和上下架 | 店铺资料卡、商品编辑卡、缺失项卡、发布确认卡 |
| 价格库存 Agent | `merchant_price_inventory_agent` | SKU 价格、可售库存、低库存、库存流水和批量调整 | SKU 表、库存风险卡、变更确认卡 |
| 订单履约 Agent | `merchant_order_fulfillment_agent` | 店铺订单、待发货、包裹、发货和物流节点 | 订单列表、发货卡、物流时间线 |
| 售后评价 Agent | `merchant_after_sale_review_agent` | 售后待处理、审核建议、退款结果、评价和商家回复 | 售后卡、评价卡、回复草稿 |
| 顾客服务 Agent | `merchant_customer_service_agent` | 顾客会话汇总、未读、人工接管、会话事实和回复草稿 | 会话卡、顾客上下文卡、回复草稿 |
| 店铺规则与平台支持 Agent | `merchant_policy_support_agent` | 商家规则、商品审核、平台流程和转平台人工 | 规则来源卡、整改清单、平台工单卡 |

运营分析、商品、库存、订单、售后和顾客服务属于不同证据域，必要时可以并行；商品发布、库存修改、发货和售后处理属于写操作，必须由 Supervisor 统一排序和确认。

#### 3.1.5 Skill 设计

| Skill 代码 | 使用 Agent | 能力 | 关键 Tool |
| --- | --- | --- | --- |
| `merchant_business_review` | 经营分析 | 今日、昨日、近 30 日和自定义区间经营分析 | `store_ops.overview`、`store_ops.revenue_metrics` |
| `merchant_store_profile_management` | 店铺与商品运营 | 店铺资料、Logo、营业状态和服务政策 | `store_ops.profile.*` |
| `merchant_catalog_management` | 店铺与商品运营 | 商品查询、草稿编辑、完整性检查、审核、上下架和删除资格 | `store_ops.catalog.*` |
| `merchant_product_content_ocr` | 店铺与商品运营 | 读取图片扫描、OCR 说明并生成可编辑草稿 | `file.get_scan_status`、`catalog.detail_ocr.*` |
| `merchant_price_inventory_management` | 价格库存 | 价格、库存、低库存和批量调整 | `store_ops.inventory.*`、`store_ops.price.*` |
| `merchant_order_fulfillment` | 订单履约 | 待发货定位、创建包裹、发货与物流节点 | `store_ops.orders.*`、`store_ops.shipments.*` |
| `merchant_after_sale_handling` | 售后评价 | 售后查询、处理预览、审核和退款跟踪 | `store_ops.after_sale.*` |
| `merchant_review_engagement` | 售后评价 | 待回复评价、回复草稿和提交 | `store_ops.reviews.*` |
| `merchant_customer_service_assistance` | 顾客服务 | 未读会话、人工接管、业务事实和回复草稿 | `store_ops.conversations.*`、`support.*` |
| `merchant_platform_policy_qa` | 规则与支持 | 平台商家规则和审核整改 | `rag.merchant_policy.search` |
| `merchant_platform_handoff` | Supervisor + 协调器 | 创建平台商家支持工单 | `support.create_merchant_platform_ticket` |

#### 3.1.6 Tool 清单

| 领域 | Tool | 类型 | 作用与边界 | 状态 |
| --- | --- | --- | --- | --- |
| 总览 | `store_ops.overview` | query | 当前店铺经营、商品、订单与待办概览 | 已有 |
| 总览 | `store_ops.revenue_metrics` | query | 按统一口径查询已确认营业额、今日/昨日/30 日收入 | 已实现：按完成时间和实付减退款口径实时计算，附订单数与待确认金额 |
| 店铺 | `store_ops.profile.get` | query | 读取当前店铺资料、Logo、营业状态、服务政策和版本 | 已实现：独立领域工具、店铺范围校验、资料/政策卡片和真实执行轨迹 |
| 店铺资料 | `store_ops.profile.update.preview/commit` | preview/mutation | 展示并确认店铺名称或简介修改 | 已实现：名称与简介具备显式确认、唯一性/版本校验、事件和主库回读 |
| 店铺 Logo | `store_ops.profile.logo.update.commit` | mutation | 将经过安全扫描的暂存图片绑定为本店 Logo | 已实现：经营助理附件面板支持上传与粘贴；服务端重新校验文件归属、用途、扫描状态与店铺版本，确认后替换 Logo、写事件并回读新图片 |
| 店铺账号 | `store_ops.account.email.update.commit` | mutation | 修改当前店铺唯一店主账号的恢复邮箱 | 已实现：不接收店铺 ID 或店主 ID，由认证上下文锁定当前店铺及其唯一店主；确认卡与结果仅展示掩码邮箱，确认时复核店铺和凭证版本，写入后撤销旧邮箱验证状态并生成审计事件 |
| 店铺政策 | `store_ops.policy.manage.commit` | mutation | 新建、修改、发布或撤回版本化店铺服务政策 | 已实现：按售后、发货、客服等类型解析明确标题与正文；新建只生成草稿，修改仅作用于草稿，发布/撤回遵守状态机；每步均有确认卡、店铺/政策版本复核、幂等、审计与主库回读。真实浏览器已覆盖“新建草稿→确认→再次定位→发布→回读”，审批卡标题与政策业务标题使用独立字段，避免元数据覆盖业务正文 |
| 店铺 | `store_ops.status.update.preview` | preview | 说明暂停/恢复营业对商品展示和新订单的影响 | 已实现：服务端重建目标并生成确认卡 |
| 店铺 | `store_ops.status.update.commit` | mutation | 按店铺状态机暂停或恢复营业 | 已实现：显式确认、版本校验、事件和回读 |
| 商品 | `store_ops.catalog_summary` | query | 当前店铺商品状态汇总和列表 | 已有 |
| 商品 | `store_ops.catalog.get_product` | query | 获取当前店铺商品完整可编辑内容和版本 | 已实现：按名称或公开 ID 定位本店全部状态商品；返回商品版本、全部 SKU、价格、实时库存、图片扫描状态、结构化参数、当前详情块、OCR 结果、FAQ 与履约资料；多候选时先给选择卡，不猜目标 |
| 商品 | `store_ops.catalog.save_draft` | mutation | 保存商品、SKU、详情块、FAQ 和图片引用草稿 | 首批已实现：经营助理从自然语言提取商品名称、首个款式、售价、库存、发货地和发货时效；字段不全时继续追问，完整后生成确认卡；确认时重新校验当前店铺、平台分类和生效配送模板版本，并在同一事务中创建商品、首个 SKU、库存、发货配置、状态流水与 Outbox。结果保持草稿并提供可弹窗预览的商品编辑入口；图片、详情块和 FAQ 仍在编辑页补充 |
| 商品 | `store_ops.catalog.skus.create.commit` | mutation | 为当前店铺的唯一商品新增款式、售价和初始库存 | 已实现：用户必须明确给出款式名称、价格和库存；确认时重新加锁并复核店铺、商品版本、重名款式和可编辑状态，同一事务创建 SKU、库存和库存流水，重算商品价格区间并写 Outbox；结果卡说明还需在商品编辑弹窗为新款式补充安全图片 |
| 商品 | `store_ops.catalog.skus.disable.commit` | mutation | 停用现有款式，保留历史订单、库存流水和审计 | 已实现：按款式名称唯一定位活动 SKU，在售商品不允许停用最后一个可售款式；确认时重新校验范围、版本和状态，复用商品领域服务更新默认 SKU 与价格区间，并明确告知只从顾客选择中移除、不删除历史事实 |
| 商品 | `store_ops.catalog.skus.update.commit` | mutation | 原子修改现有款式名称、价格或库存 | 已实现：支持只改名称、同时改价格与库存或三项组合；确认前后锁定商品、SKU 与库存，校验重名、预占库存下限和资源版本，同一事务更新 SKU、库存流水、商品价格区间与 Outbox，并回读实际结果 |
| 商品 | `store_ops.catalog.faqs.upsert.commit` | mutation | 新增常见问题，或按完整问题修改回答 | 已实现：必须明确提供问题和回答；确认时锁定商品和 FAQ，复用内容清洗、版本发布、审计和主库回读，其他问答保持不变；发布后同时供顾客详情页和店铺 AI 使用 |
| 商品 | `store_ops.catalog.faqs.delete.commit` | mutation | 从公开商品知识中移除指定常见问题 | 已实现：按完整问题唯一定位，确认瞬间复核商品、FAQ 和内容版本；删除后不再向顾客或店铺 AI 公开，但旧版本保留审计。真实浏览器已验证“新增→发布→删除→回读” |
| 商品 | `store_ops.catalog.detail_sections.upsert.commit` | mutation | 新增或按标题修改结构化商品详情段落 | 已实现：只编辑唯一标题对应的标题与正文块，不重排图片或其他章节；确认前后复核店铺、商品和当前内容版本，在售商品生成并立即发布新版本，历史版本保留审计 |
| 商品 | `store_ops.catalog.detail_sections.delete.commit` | mutation | 删除指定结构化详情段落 | 已实现：仅删除目标标题及其相邻正文/列表，不删除图片和其他章节；禁止把在售商品最后一个可读详情删空。真实浏览器已验证“保留基线章节→新增目标段落→发布→删除目标段落→回读” |
| 商品 | `store_ops.catalog.validate_submission` | preview | 返回缺失项、敏感词、图片扫描和状态问题 | 已合并到 `store_ops.catalog.get_product`：每次读取完整商品快照时按基础资料、有效 SKU、每个 SKU 的安全图片、履约资料和已通过扫描的详情内容生成提交准备度卡；最终提交时仍重新校验 |
| 商品 | `store_ops.catalog.submit_review` | mutation | 提交自动审核；绑定当前商品版本 | 已实现：按本店商品与版本生成确认卡，确认后复用商品完整性校验和毒品、杀人违禁词自动审核；通过立即上架，不通过保留整改状态 |
| 商品 | `store_ops.catalog.publish` | mutation | 发布审核通过的商品 | 已由 `store_ops.catalog.status.commit` 覆盖恢复上架 |
| 商品 | `store_ops.catalog.unpublish` | mutation | 暂时下架商品，不删除历史交易 | 已由 `store_ops.catalog.status.commit` 覆盖下架 |
| 商品 | `store_ops.catalog.delete_eligibility` | preview | 检查是否产生交易；有交易只能下架 | 已实现：先查订单商品交易引用；有交易时把原“删除”请求转换为明确的“下架/取消”确认卡 |
| 商品 | `store_ops.catalog.delete` | mutation | 仅删除无交易商品 | 已实现：确认瞬间再次加锁复核交易与资源版本，无交易才软删除商品并停用全部 SKU，写状态流水和 Outbox |
| OCR | `store_ops.catalog.list_ocr_results` | query | 查询详情图片 OCR 和扫描状态 | 已合并到 `store_ops.catalog.get_product`：按详情图片顺序返回扫描状态、OCR 文字、引擎和处理时间；独立分页工具待大数据量阶段拆分 |
| OCR | `store_ops.catalog.update_image_description` | mutation | 修改单张详情图片说明，管理员也可编辑 | 已实现：商家按商品与图片顺序定位安全详情图，展示原说明和新说明，确认后以新内容版本保存并回读；管理员侧使用同一结构化版本边界、平台权限和平台审计，均保留图片顺序和历史版本 |
| 库存 | `store_ops.inventory_risks` | query | 查询缺货、低库存、异常预占 | 已有 |
| 库存 | `store_ops.inventory.get_skus` | query | 查询 SKU 实时库存、版本和流水摘要 | 已实现：实时库存、预占、可售、安全库存线和版本已接入，并按库存项返回最近 3 条变更流水；大数据量历史分页留在库存管理页 |
| 库存 | `store_ops.inventory.adjust.preview` | preview | 展示调整前后数量、原因和超卖风险 | 已实现：展示现有、目标、预占和可售量 |
| 库存 | `store_ops.inventory.adjust.commit` | mutation | 按 SKU 版本串行修改并写库存流水 | 已实现：行锁、资源版本、库存流水、Outbox 和主库回读 |
| 价格 | `store_ops.price.update.preview` | preview | 展示价格差异和在途结算影响 | 已实现：明确说明不改历史订单成交价 |
| 价格 | `store_ops.price.update.commit` | mutation | 更新 SKU 价格并使新结算读取新价格 | 已实现：确认后更新 SKU 与商品版本并回读 |
| 订单 | `store_ops.order_summary` | query | 店铺订单、状态和已确认营业额汇总 | 已有 |
| 订单 | `store_ops.orders.list` | query | 按状态、时间和顾客筛选店铺订单 | 已实现：支持订单状态、今天/昨天、近 7/30 日、顾客名和订单号组合筛选，返回可点击富订单卡片并排除用户专属取消记录 |
| 订单 | `store_ops.orders.get` | query | 获取店铺订单、商品、地址快照和版本 | 已实现：店铺范围和订单号校验后返回全部商品、金额、售后/履约状态、版本及该订单授权地址快照 |
| 发货 | `store_ops.shipments.create.preview` | preview | 校验待发货状态、商品和物流信息 | 已实现：解析本店待发货订单并生成承运商、运单号、发货地确认卡 |
| 发货 | `store_ops.shipments.create.commit` | mutation | 创建包裹并推进履约状态 | 已实现：确认后创建包裹、写首条轨迹、推进履约状态并回读 |
| 物流 | `store_ops.shipments.append_track` | mutation | 显式记录揽收、运输、派送、签收或异常节点 | 已实现：店铺明确操作并确认后推进合法物流节点；不再按固定时间自动模拟 |
| 售后 | `store_ops.after_sale.list` | query | 查询本店售后待办和进度 | 已实现：独立售后领域 Agent、待处理优先排序、金额/状态卡片和店铺隔离 |
| 售后 | `store_ops.after_sale.review.preview` | preview | 校验申请、金额、数量和当前状态 | 已实现首批：按售后 ID、订单 ID、唯一顾客或连续指代锁定本店待审核申请，展示金额、决定、原因与资源版本；拒绝时必须提供明确原因 |
| 售后裁决 | `store_ops.after_sale.review.commit` | mutation | 批准或拒绝售后，写入状态流水 | 已实现：确认后调用既有售后状态机；同意进入退款/退货流程，拒绝释放占用并写状态流水，高金额仍进入平台双人复核 |
| 补充售后材料 | `store_ops.after_sale.request_more_info.commit` | mutation | 指定售后单与具体材料要求，保持审核中并通知顾客 | 已实现：确认前展示顾客、店铺、订单、售后单和具体材料；确认时重新校验店铺范围、状态与版本，加锁写不可变事件，在真实顾客—店铺会话发送居中系统通知；本操作不会批准、拒绝或退款 |
| 评价 | `store_ops.reviews.list` | query | 查询本店待回复和历史评价 | 已实现：待回复优先并同时返回已回复评价、顾客原评价和商家回复；每批 5 条，以绑定当前店铺、经营助理会话和 Tool 的防篡改 Cursor 继续读取；用户回复“下一页/下一批/继续/更多”时从版本化会话状态恢复评价领域，最后一批明确结束且不回到第一页 |
| 评价 | `store_ops.reviews.reply_draft` | preview | 基于评价内容生成可编辑回复草稿 | 已实现：锁定本店未回复评价并生成可编辑确认内容 |
| 评价 | `store_ops.reviews.reply_commit` | mutation | 运营人员确认后发布商家回复 | 已实现：唯一回复约束、状态复核、审计与主库回读 |
| 消息 | `store_ops.conversations.list` | query | 查询本店顾客会话、未读和人工状态 | 已实现：每批返回 8 个顾客会话及其未读数、AI/人工接待状态、最后消息和入口；以绑定当前店铺、经营助理会话和 Tool 的防篡改 Cursor 继续读取，裸“下一页”由版本化会话状态保持在顾客服务领域，跨店铺、跨会话或过期 Cursor 不可复用 |
| 消息 | `store_ops.conversations.get_context` | query | 读取选定顾客会话及所需本店业务事实 | 已合并到 `store_ops.conversations.list` 的唯一命中模式：按顾客名或会话 ID 定位后读取最近 20 条有序消息和有效商品/订单业务上下文，生成顾客会话上下文卡；回复或执行前仍重新读取业务主库；当运营人员要求“拟回复/回复草稿”时，模型基于该证据生成标明“尚未发送”的可编辑草稿卡，真实发送继续走独立确认动作 |
| 消息 | `store_ops.conversations.send_draft` | preview | 生成顾客回复草稿，不直接发送 | 已实现：选定唯一顾客会话后读取最近消息与有效业务上下文，由模型生成标明“尚未发送”的草稿卡；真实发送仍走独立确认动作 |
| 消息 | `store_ops.conversations.send_message` | mutation | 运营人员确认后发送文字或卡片 | 已实现：只允许当前店铺已由本人接管的人工工单；文字要求明确“回复内容”，商品必须属于本店且仍在售，订单必须同时属于当前顾客和本店；确认时复核工单、会话及商品/订单版本，发送后回读真实消息类型与序号 |
| 消息 | `store_ops.support.claim/resolve` | mutation | 领取或结束本店人工服务并同步 AI 接管状态 | 已实现：确认后复用 Support Service；结束服务会写居中系统消息、恢复店铺 AI，并保留问题是否解决的交互 |
| 规则 | `rag.merchant_policy.search` | retrieval | 只检索已发布平台商家规则 | 已实现首批：通过统一 Knowledge Service 对 `platform:platform` 发布索引执行关键词+向量混合检索，失败时明确标记降级；后续再拆独立 Tool 注册码 |
| 人工 | `support.create_merchant_platform_ticket` | handoff | 转平台人工客服处理商家平台问题 | 已实现，当前兼容 Tool 代码为 `support.create_platform_ticket`，会创建平台人工工单并暂停 AI |

所有 Tool 都忽略模型提交的店铺 ID，使用认证上下文中的 `active_store_id` 重建范围。经营助理不获得查看其他店铺内部数据、顾客密码、完整支付凭据或绕过商品/订单/售后状态机的工具。

上述“已实现”写操作均沿用显式确认、幂等、资源版本、范围复核、状态机和主库回读边界。商品删除、提交审核与售后裁决新增了确认瞬间的二次事实校验；其中高金额退款仍进入平台双人复核。商品级发货地、发货时效和购买须知修改已实现省级名称/行政区代码解析、履约版本复核及确认后主库回读；店铺服务政策已实现草稿、发布和撤回生命周期。经营助理现可创建包含首个 SKU、价格、库存和履约资料的结构化商品草稿，也可经确认新增、深度修改或停用非最后一个活动款式，修改店主恢复邮箱，新增、修改和删除版本化 FAQ，以及按标题新增、修改、删除结构化详情段落；详情段落操作不重排图片或其他章节，并禁止把最后一个可读详情删空。店铺 Logo 与指定 SKU 款式图片现也已接入“业务对象选择 → 安全上传/粘贴 → Agent 受控消息 → 服务端重新解析对象 → 确认卡 → 加锁复核 → 执行与回读”，替换 SKU 图片不会影响同商品其他款式、详情图片或历史订单快照。售后补材料已接入独立的受控 Tool：必须明确售后单和具体所需材料，确认后只保持/进入商家审核态、写不可变事件并通知顾客，不得借此隐式批准、拒绝或退款。

#### 3.1.7 操作分级与确认

| 等级 | 操作 | 交互规则 |
| --- | --- | --- |
| M0 | 查询经营、商品、库存、订单、评价、会话和平台规则 | 直接执行并展示来源与口径 |
| M1 | 生成商品、评价、客服回复或售后处理草稿 | 不改变业务数据，草稿卡可继续编辑 |
| M2 | 保存商品草稿、修改图片说明、发送客服消息、轻量库存调整 | 展示对象与差异，运营人员明确点击确认 |
| M3 | 发布/下架/删除商品、批量改价库存、发货、记录签收、处理售后、发布评价回复 | 完整影响卡 + 资源版本 + 明确确认；执行后回读 |
| M4 | 店铺注销、绕过交易状态、直接改营业额、查看其他店铺或顾客机密 | 不向 Agent 开放，转安全页面或平台人工 |

“超级管理员权限最大”不改变店铺端边界；店铺运营人员的授权仍由当前账号与店铺成员关系决定。模型不能自行降低确认等级。

#### 3.1.8 上下文、记忆与经营口径

| 内容 | 状态/记忆策略 |
| --- | --- |
| 当前店铺、运营人员和权限 | 后端注入，不可由对话修改 |
| 当前经营目标 | 保存时间范围、指标口径、目标商品/订单和完成条件 |
| 商品/订单/顾客卡片集合 | 分类型、分版本保存集合与当前焦点，避免序号串用 |
| 未完成任务和确认 | 保存预览指纹、对象版本、影响范围和过期时间 |
| 最近成功操作 | 保存 Tool、对象、版本和回读结果，防止重复执行 |
| 长期经营偏好 | 经运营人员确认后保存补货阈值、常用发货设置、客服语气和经营目标 |
| 禁止长期记忆 | 实时营业额、库存、订单状态、顾客地址、顾客隐私和临时促销数字 |

营业额只统计用户确认收货或系统合法完成且资金已结算给店铺的金额；待付款、已取消、仅支付未完成和退款金额不得混入。每张指标卡必须显示口径、时间范围、币种和数据截止时间。

#### 3.1.9 典型复合任务

| 运营人员表达 | Supervisor 计划 | 结果 |
| --- | --- | --- |
| “今天先处理什么？” | 并行查询订单、库存、售后、评价和未读顾客会话，再按紧急度复核 | 可点击待办卡，不只给文字建议 |
| “列出低库存商品，把销量最高的补到100” | 查库存与销量 → 唯一定位 SKU → 生成调整预览 → 确认后串行写入 → 回读 | 库存风险卡 + 调整确认卡 |
| “这个商品价格改成99并上架” | 读取商品版本 → 改价预览 → 完整性/审核检查 → 依次确认改价和发布 | 商品卡 + 两项独立确认与结果 |
| “把所有待发货订单发掉” | 查询候选 → 按订单展示包裹/物流缺失项 → 逐批确认 → 单订单串行执行 | 发货批次卡；失败订单不影响成功订单 |
| “回复这条差评并看看是否有售后” | 评价与售后并行读取 → 生成可编辑回复 → 显示关联售后 | 评价卡 + 售后卡 + 回复草稿 |
| “给这个顾客解释物流并发订单卡” | 查询当前顾客会话、订单和物流 → 生成草稿与卡片 → 人工确认发送 | 顾客回复预览，不自动冒充人工 |
| “为什么商品没通过审核？” | 查询商品状态与审核记录 + 检索商家规则 | 具体缺失项、整改入口和规则来源 |

#### 3.1.10 UI 与交互

| 区域 | 设计 |
| --- | --- |
| 左栏 | AI 经营助理固定置顶；“顾客会话”可折叠，显示每个顾客红色未读数和人工状态 |
| 中栏 | 与用户端一致的头像、动态气泡、流式回复和卡片；经营卡片可使用两列，同类型批量卡支持分页 |
| 右栏 | 统一透明执行轨迹，展示经营 Supervisor 的真实任务、领域委派、Tool、RAG、确认和回读 |
| 输入区 | 支持文字、商品/订单/顾客/售后卡片、图片和文件；附件上传失败不丢已输入文字 |
| 详情 | 商品、订单、库存、售后和评价卡点击后在大弹窗中查看/编辑，关闭后保持聊天位置 |
| 写操作 | 所有差异和风险在卡片中可读展示；确认按钮与取消、修改方案、查看详情并列 |

#### 3.1.11 结果验证、安全与故障恢复

| 规则 | 要求 |
| --- | --- |
| 范围 | 每个结果校验当前店铺；顾客数据还要校验与本店交易或会话关系 |
| 经营权限 | 每次写 Tool 都从 RBAC 关联表重新读取当前账号实际获得的权限，绑定 `merchant` 客户端登录会话，并把数据范围重新收窄到当前店铺；仅在权限字典中存在同名权限不代表账号已获授权 |
| 状态 | 发布、下架、删除、发货和售后只能经过领域状态机 |
| 金额库存 | 最小货币单位；库存修改使用乐观锁和库存流水，禁止模型直接计算后写库 |
| 批量任务 | 每项独立幂等键和状态；成功、失败、未知逐项展示 |
| 内容 | 商品文案、OCR、评价和顾客消息均视为不可信输入，不能改变 Tool 权限 |
| 冲突 | 发现数据源冲突时暂停写入并展示冲突，不由模型猜测 |
| 恢复 | 重启后从检查点恢复，确认过期则重新生成预览，不重复发布、发货或回复 |

#### 3.1.12 验收矩阵

| 类别 | 必测内容 | 通过标准 |
| --- | --- | --- |
| 经营分析 | 收入口径、时间比较、排序、空数据和退款影响 | 指标正确、可追溯，不编造原因 |
| 商品 | 新增、草稿恢复、OCR、SKU、审核、上下架和删除资格 | 不丢编辑内容；有交易商品不能删除 |
| 库存价格 | 单项/批量、版本冲突、超卖、只试算、部分失败 | 无越权、无重复、回读一致 |
| 订单物流 | 筛选、发货、包裹、显式物流节点和已完成营业额 | 状态机合法，用户端实时同步 |
| 售后评价 | 待办、处理、补材料、评价回复和关联订单 | 金额数量正确，回复需人工确认 |
| 顾客服务 | 未读、人工接管、发送卡片、结束服务和 AI 恢复 | 两端消息一致，不串顾客 |
| 连续对话 | 30 轮经营混合任务、纠正、暂停恢复和序号引用 | 焦点与任务状态稳定，不重复写入 |
| 隔离并发 | 多运营人员、多店铺、多标签页、快速发送和批量操作 | 店铺严格隔离，序号和版本正确 |
| 故障 | 模型、Tool、SSE、Worker、RAG 和对象存储异常 | 已成功结果保留，未知结果可核验恢复 |

#### 3.1.13 实施顺序

| 顺序 | 实施内容 | 完成证据 |
| ---: | --- | --- |
| 1 | 将统一状态、摘要、序号锁、Trace 和清除逻辑接入经营助理 | 状态、长对话和并发测试 |
| 2 | 重构旧 `merchant_copilot` 为模型优先 Supervisor + 7 个领域能力 | 规划和多任务测试 |
| 3 | 将现有四个汇总 Tool 扩展为查询、预览、提交的原子 Tool | Contract、权限和状态机测试 |
| 4 | 统一经营、商品、库存、订单、售后、评价和顾客卡片 | 组件和浏览器测试 |
| 5 | 完成写操作确认、幂等、回读、部分失败和恢复 | 集成与故障注入测试 |
| 6 | 使用多个店铺账号完成全部经营业务浏览器回归 | 页面证据、Trace 和数据库不变量 |

#### 3.1.14 第一轮优化：完整经营闭环

| 审校发现 | 优化后的设计 |
| --- | --- |
| 店铺资料缺席 | 将店铺资料、Logo、服务政策、发货地和营业状态并入店铺与商品运营 Agent，支持查询、差异预览和确认修改 |
| 收入口径混乱 | 区分“已支付待结算、可结算、已结算营业额、退款扣减”；首页的今日/昨日/30 日收益只按统一结算事件统计 |
| 商品编辑可能丢失 | 每个编辑块独立保存草稿版本；图片上传、OCR 和文字编辑异步完成也不会覆盖较新的草稿 |
| 批量上架风险 | 批量任务先逐商品验证，审核失败商品停留在草稿/需修改状态，不能因其他商品成功而误标成功 |
| 库存补货目标含糊 | “补到100”解释为目标库存，“增加100”解释为增量；确认卡必须展示调整前、调整方式和调整后 |
| 发货信息不足 | 缺承运商、运单号或包裹商品时只生成待补充卡，不自动伪造物流编号 |
| 售后角色冲突 | 经营助理给出平台规则和可执行选项，但批准/拒绝使用售后状态机；不能通过普通消息承诺退款结果 |
| 顾客回复责任不清 | AI 默认生成草稿；只有运营人员明确确认或开启经过审计的低风险自动回复策略后才发送 |
| 多运营人员冲突 | 商品、库存、订单和售后写入均带资源版本；冲突时展示他人已更新后的最新值并要求重新确认 |

经营指标证据优先级为：结算/退款流水 > 订单与履约状态 > 商品和库存主库 > 统计物化结果 > 会话摘要。经营助理不得为了给出“增长原因”而编造未采集的曝光、转化或渠道数据。

#### 3.1.15 第二轮优化：智能程度、交互效率与治理

| 优化项 | 最终要求 |
| --- | --- |
| 主动但不打扰 | 进入经营助理时只展示今日最重要的 3—5 个真实待办；没有异常时不制造风险提示 |
| 可执行建议 | 每条建议绑定来源数据、影响对象和下一步按钮；纯推测只能标为“假设”，不能提供直接执行按钮 |
| 渐进式能力 | Supervisor 先选择经营、店铺商品、库存价格、订单履约、售后评价、顾客服务或平台规则领域，再加载对应 Skill/Tool |
| 计划复核 | 复合任务完成前逐项检查 `requested/completed/failed/waiting_confirmation`；预算耗尽时明确列出未完成项 |
| 批量体验 | 同类对象使用可选择表格或卡片集合，支持全选、取消和逐项修改；不同业务类型必须分区展示 |
| 写入透明度 | 右栏显示实际预览、确认、工具参数、幂等键、状态机结果和回读，不用“正在优化经营”等模糊步骤 |
| 失败恢复 | 写结果未知时先按幂等键查询，不直接重试；只重试失败项，已成功项不可重复执行 |
| 长期记忆 | 只保存经确认的经营偏好；每次使用时显示“本次采用的偏好”，并允许运营人员修改或删除 |
| 顾客隐私 | 经营分析优先使用聚合数据；读取单个顾客资料必须与本店订单、售后或会话任务直接相关 |
| 上线门槛 | 除自动化测试外，必须由真实店铺账号完成新增商品、改价库存、发货、售后、评价、顾客消息和长对话回归 |

两轮优化后，AI 经营助理的衡量标准是“帮助运营人员完成经营闭环且不产生错误写入”，而不是展示更多统计或生成更长的建议。

## 4. 超级管理员端

### 4.1 AI 管家

#### 4.1.1 定位与消息中心关系

| 项目 | 设计 |
| --- | --- |
| 页面显示名 | AI 管家 |
| 主 Agent 全称 | AI 管家 Supervisor Agent（平台治理总管） |
| Agent 代码 | `platform_governance_supervisor`，兼容旧代码 `admin_copilot` |
| 服务对象 | 已登录平台超级管理员及后续被授权的平台运营角色 |
| 会话规则 | AI 管家固定置顶；平台用户消息、店铺消息分别可折叠，人工工单保持独立会话 |
| 数据范围 | 当前管理员权限覆盖的平台用户、店铺、商品、交易、售后、客服、AI 治理和系统运行数据 |
| 核心目标 | 从平台问题定位到可信证据、风险判断、治理方案、确认执行和结果复核，形成完整治理闭环 |
| 绝对禁止 | 明文密码、密钥、Token、支付凭据、任意 SQL、任意 Shell、任意文件系统、绕过审批或业务状态机 |

超级管理员权限最大，表示它能在平台授权范围内管理更多业务资源，不表示模型可以跳过权限网关、确认、幂等、审计或状态机。AI 管家不得直接连接数据库；所有查询和操作必须通过按领域定义的 Tool。

#### 4.1.2 管理员在人工消息中的能力

| 服务对象 | 管理员可以处理 | 消息内组件 |
| --- | --- | --- |
| 平台用户 | 平台规则、账户状态、本人订单/物流/售后、余额异常、投诉和平台人工服务 | 用户卡、订单卡、物流卡、售后卡、处理结果卡 |
| 店铺运营人员 | 商家账号、店铺状态、商品审核、平台规则、交易售后争议、AI 经营助理无法解决的问题 | 店铺卡、商品卡、订单/售后卡、整改卡、平台工单卡 |
| AI 管家 | 平台经营、风险、用户店铺治理、客服队列、Agent/RAG/Skill/MCP 和运行状况 | KPI、风险、治理确认、评估和运行卡 |

人工会话中的 `+` 支持发送与当前对象有权限关系的用户、店铺、商品、订单、物流、售后、规则和处理结果卡。管理员代表平台发送消息，头像使用管理员头像；AI 管家回复使用 AI 图片，不能混淆身份。

#### 4.1.3 Supervisor 职责与动态运行

| 阶段 | 职责 | 平台治理要求 |
| --- | --- | --- |
| 理解 | 识别对象、范围、指标口径、风险、期望修改和完成条件 | “冻结异常用户并暂停关联店铺”必须拆成两个资源和依赖关系 |
| 调查 | 动态调用多个只读领域 Agent 收集证据 | 相互独立查询可并行，跨资源关联必须保留证据链 |
| 评估 | 区分事实、规则、异常信号和模型推测 | 单一异常指标不能自动等同违规 |
| 规划 | 生成查询、预览、审批、执行和回读计划 | 高风险动作可以拆分确认或要求双人审批 |
| 执行 | 在管理员权限、审批、版本、状态和幂等均通过后调用写 Tool | 不能根据聊天中的伪造管理员身份扩大权限 |
| 复核 | 核对每个目标是否完成、是否出现副作用和跨对象影响 | 批量治理逐项返回成功、失败或未知 |
| 解释 | 用业务卡片和证据说明原因、影响、回滚入口和未完成项 | 不用内部表名、SQL 或长日志代替结论 |

AI 管家采用动态 Agent 循环，允许在调查过程中修改计划。例如发现店铺暂停是运营人员主动操作而非风控处置时，应撤销“违规关店”计划，改为解释状态；发现支付回调异常来自系统故障时，应转运行治理而不是处罚用户。

#### 4.1.4 领域 Agent

| 完整名称 | Agent 代码 | 负责范围 | 主要结果组件 |
| --- | --- | --- | --- |
| 平台经营洞察 Agent | `platform_business_insight_agent` | 用户、店铺、商品、订单、支付、退款和客服总体指标 | 平台 KPI、趋势、异常概览 |
| 用户治理 Agent | `platform_user_governance_agent` | 用户查询、创建、资料、状态、会话、地址、余额、收藏、购物车和本人订单管理 | 用户档案、状态卡、资产与订单卡 |
| 店铺治理 Agent | `platform_store_governance_agent` | 店铺创建、资料、状态、经营信息、成员及当前店铺商品/订单入口 | 店铺档案、经营卡、状态确认卡 |
| 商品与内容治理 Agent | `platform_catalog_content_governance_agent` | 全平台商品查询、编辑、审核、上下架、删除资格、图片 OCR 和内容风险 | 商品卡、审核/整改卡、内容证据卡 |
| 交易售后治理 Agent | `platform_trade_after_sale_governance_agent` | 订单、支付、履约、物流、退款、售后争议和资金对账 | 交易时间线、售后卡、对账异常卡 |
| 平台客服协调 Agent | `platform_support_coordination_agent` | 用户/店铺工单、排队、分配、人工回复、升级和服务质量 | 工单队列、会话卡、处理草稿 |
| AI 治理 Agent | `platform_ai_governance_agent` | Agent、Prompt、Skill、MCP Tool、RAG、记忆策略、评估、灰度和回滚 | 版本卡、评估报告、发布确认卡 |
| 系统运行治理 Agent | `platform_runtime_operations_agent` | API、Worker、队列、数据库依赖、模型调用、成本、Trace、告警和死信 | 健康卡、积压卡、告警与恢复卡 |

领域 Agent 只负责调查和提出结构化动作，不直接突破 Tool Gateway。涉及多领域时由 Supervisor 保留原始目标清单并对每项给出 `completed/failed/waiting_confirmation/deferred` 状态。

#### 4.1.5 Skill 设计

| Skill 代码 | 使用 Agent | 能力 | 关键 Tool |
| --- | --- | --- | --- |
| `platform_business_review` | 经营洞察 | 平台指标、时间对比、异常趋势和影响范围 | `governance.platform_overview`、`governance.metrics.*` |
| `platform_user_management` | 用户治理 | 用户增查改、状态、强制下线、重置凭证、地址与资产 | `governance.users.*` |
| `platform_store_management` | 店铺治理 | 店铺创建、资料、状态、成员和经营档案 | `governance.stores.*` |
| `platform_catalog_moderation` | 商品内容治理 | 商品编辑、审核、上下架、删除资格、OCR 与内容风险 | `governance.catalog.*` |
| `platform_trade_investigation` | 交易售后治理 | 订单、支付、物流、退款和资金链路调查 | `governance.trade.*` |
| `platform_after_sale_resolution` | 交易售后治理 | 售后争议、申诉和平台裁决 | `governance.after_sale.*` |
| `platform_support_operations` | 客服协调 | 用户/店铺工单、分配、消息、升级和结束 | `governance.support.*` |
| `platform_agent_governance` | AI 治理 | Agent/Prompt/Skill/Tool 版本、权限、评估和发布 | `governance.ai.*` |
| `platform_knowledge_governance` | AI 治理 | 知识文档、索引代次、检索质量和回滚 | `governance.knowledge.*` |
| `platform_runtime_diagnostics` | 系统运行治理 | 健康、队列、Trace、成本、死信和安全告警 | `observability.*`、`governance.jobs.*` |

#### 4.1.6 Tool 清单：平台与用户治理

| Tool | 类型 | 作用与边界 | 状态 |
| --- | --- | --- | --- |
| `governance.platform_overview` | query | 平台用户、店铺、商品、订单、售后和运行概览 | 已有 |
| `governance.metrics.query` | query | 按时间、口径和维度查询平台指标 | 已实现首批：支持今日、近 N 天（上限 90 天）与默认 7 天窗口，返回订单量、已支付成交额、已完成净额、退款额及新增用户、店铺和上架商品；每项附主库口径和时间范围 |
| `governance.user_summary` | query | 用户状态汇总 | 已有 |
| `governance.users.search` | query | 按用户名搜索，不暴露密码和密钥 | 已实现：明确用户名时返回深度用户视图，包含状态、在线会话数、余额、有效订单、地址数量、购物车、收藏、关注、版本和近期订单；列表模式保持有界 |
| `governance.users.get` | query | 用户资料、状态、在线会话和关联业务概览 | 首期能力已合并到 `governance.users.search` 的唯一命中模式；资产明细使用下述窄工具独立留痕，后续仅需补 Cursor 分页 |
| `governance.users.create.preview/commit` | preview/mutation | 创建普通用户，不在对话中接收或返回明文密码 | 已实现：管理员只需提供唯一用户名和恢复邮箱；系统生成不可见随机初始凭证，创建默认用户角色与零余额钱包，确认后回读账号、钱包和凭证状态；首次使用必须通过恢复邮箱重置密码，浏览器已覆盖创建与注销清理 |
| `governance.users.update_profile.preview/commit` | preview/mutation | 修改用户名、邮箱、头像等允许字段 | 已实现：用户名与邮箱接入目标定位、格式/唯一性校验、确认卡、版本复核、管理审计和主库回读；用户头像通过独立 `governance.users.avatar.update.commit` 接入消息页受控图片面板，管理员先选择唯一用户，再上传或粘贴图片并完成安全扫描，Agent 确认时复核用户、文件归属和版本，提交后由用户服务更新公开头像。跨用户文件不能绑定，结果会同步到用户导航、个人中心与消息头像 |
| `governance.users.force_logout.preview/commit` | preview/mutation | 撤销该用户有效会话，不删除账号 | 已实现：服务端解析目标、生成确认卡、按版本执行并回读撤销数量 |
| `governance.users.status.preview/commit` | preview/mutation | 冻结、解冻用户并说明影响 | 已实现：冻结/解冻均需确认，禁止 AI 操作当前管理员账号 |
| `governance.users.reset_password.preview/commit` | preview/mutation | 将密码凭证标记为必须重置并撤销旧会话；永不读取或在聊天中设置明文密码 | 已实现：唯一定位普通用户和活动密码凭证，确认时复核用户与凭证版本，复用 RBAC 服务设置必须重置并撤销全部活动会话；用户随后凭恢复邮箱走既有找回密码流程 |
| `governance.users.addresses.*` | query/preview/mutation | 管理指定用户地址，地区使用结构化省市县 | 查询、新增、字段编辑、删除和设为默认均已实现：按明确用户读取有效地址；新增/编辑使用与前端一致的标准中国省市区父子数据校验，所有写操作经确认后复核用户、地址数量或地址版本，调用真实地址服务加密写入并回读结果 |
| `governance.users.wallet.get` | query | 查询用户余额和流水 | 已实现：按明确用户读取实时余额及最近 12 条不可变资金流水，展示方向、金额、余额变化、说明和发生时间；没有流水时不伪造记录 |
| `governance.users.wallet.adjust.preview/commit` | preview/mutation | 管理员调整余额并生成不可变资金流水 | 已实现：金额与方向校验、确认卡、钱包行锁与版本检查、不可变资金流水、审计、Outbox 和回读 |
| `governance.users.favorites.*` | query/mutation | 查询或取消指定用户收藏 | 查询与单项取消已实现：`governance.users.favorites.list` 返回商品、店铺收藏卡；取消时必须唯一定位对象，经确认后复核收藏关系与商品/店铺版本，支持移除已下架商品或暂停店铺的残留收藏并回读剩余数量 |
| `governance.users.cart.*` | query/preview/mutation | 管理指定用户购物车项和数量 | 查询、改单项数量、移除单项和整车清空均已实现：单项目标须由完整商品名、款式名或条目编号唯一定位；整车清空展示商品种数与总数量并明确不影响订单、收藏和商品；确认时复核用户、购物车、条目或总条目数，并复用购物车服务执行和回读 |
| `governance.users.orders.list/get` | query | 查询指定用户非取消订单和详情 | 列表首批已实现：`governance.users.orders.list` 排除取消/关闭订单并返回店铺、金额及支付/履约/售后状态卡；商品行、物流和售后完整时间线随独立详情 Tool 补齐 |
| `governance.users.delete.preview/commit` | preview/mutation | 展示关联数据影响，按账户删除编排器注销 | 已实现首批：禁止删除当前管理员和管理身份；有历史交易时阻断，符合条件时生成影响确认卡并调用账户删除编排器，不直接级联硬删 |

#### 4.1.7 Tool 清单：店铺、商品与交易治理

| Tool | 类型 | 作用与边界 | 状态 |
| --- | --- | --- | --- |
| `governance.store_summary` | query | 店铺和商品状态汇总 | 已有 |
| `governance.stores.search` | query | 按店铺名称或公开 ID 查询经营状态、商品数、销量、评分和已确认营业额 | 已实现：唯一命中时追加店主、简介、商品/订单状态、进行中售后、近期商品与订单和资源版本，提供深度治理卡片 |
| `governance.stores.get` | query | 店铺完整资料、商品和订单关联视图 | 首期能力已合并到 `governance.stores.search` 的唯一命中模式；后续随成员、政策和资金流水分页拆分 |
| `governance.stores.create.preview/commit` | preview/mutation | 创建独立商家身份和店铺，不复用用户登录凭据 | 已实现：要求唯一店铺名、唯一商家用户名和恢复邮箱；系统生成不可见随机初始凭证，原子创建独立商家、店铺和店铺范围角色，确认后回读店铺与账号；首次登录前必须通过恢复邮箱重置密码，真实浏览器已覆盖创建与注销清理 |
| `governance.stores.update.preview/commit` | preview/mutation | 修改名称、简介、邮箱和允许的服务资料 | 部分实现：名称、简介和唯一店主的恢复邮箱已接入唯一性、范围、版本、权限、确认、审计和回读；其余服务资料仍需接入 |
| `governance.stores.logo.update.commit` | mutation | 将经过安全扫描的暂存图片绑定为指定店铺 Logo | 已实现：管理员附件面板先选店铺再上传或粘贴，服务端重新校验店铺、文件归属/用途/扫描状态与版本，确认后替换、审计并回读；跨店错绑被拒绝 |
| `governance.stores.status.preview/commit` | preview/mutation | 暂停或恢复营业，说明商品和新订单影响 | 已实现：确认前后校验店铺版本并记录管理审计 |
| `governance.stores.delete.preview/commit` | preview/mutation | 检查交易、售后和资金后进入店铺注销编排 | 已实现首批：有历史交易时阻断，符合条件时展示影响并调用商家账户删除编排器；不删除交易、支付或审计历史 |
| `governance.catalog.search` | query | 按完整名称、明确简称或公开 ID 查询商品状态、店铺、销量和最低售价 | 已实现：明确商品时只返回匹配商品 |
| `governance.catalog.get` | query | 获取商品完整编辑内容、SKU、详情块、OCR 与版本 | 已合并到 `governance.catalog.search` 唯一命中模式：返回所属店铺、全部 SKU/价格/实时库存、图片扫描、参数、详情块、OCR、FAQ、发货资料和资源版本 |
| `governance.catalog.update.preview/commit` | preview/mutation | 以管理员身份修改商品、SKU、详情、图片和 OCR 说明 | 已实现：商品名称与描述、图片说明、结构化详情段落，以及 SKU 新增、名称/价格/库存深度修改、停用和指定 SKU 图片替换均已接入权限、版本、确认、审计和回读；新增 SKU 原子创建库存与流水，现有 SKU 深度修改原子校验预占下限并同步价格区间，停用保留历史交易且禁止移除在售商品最后一个有效款式；图片替换使用安全暂存文件，确认时重新校验店铺、商品、SKU、文件归属和扫描状态，只替换目标款式图片，不改动其他款式、详情图片或历史快照 |
| `governance.catalog.skus.create/disable.commit` | mutation | 跨店新增款式或停用现有款式 | 已实现：唯一定位店铺、商品和款式，确认后按店铺/商品/SKU 版本执行；新增时创建库存与事件，停用时复用商品状态服务维护默认 SKU 与价格区间；真实浏览器覆盖“新增平台验收款式→回读价格库存→停用→历史事实保留” |
| `governance.catalog.skus.update.commit` | mutation | 跨店原子修改现有款式名称、价格或库存 | 已实现：平台权限下唯一定位店铺、商品和款式，确认时锁定 SKU/库存并复核版本、重名与预占下限；变更写入库存流水、管理员审计和事件，真实浏览器覆盖“新增→改名/改价/改库存→回读→按新名称停用” |
| `governance.catalog.detail_sections.upsert/delete.commit` | mutation | 跨店新增、修改或删除指定商品的结构化详情段落 | 已实现：先唯一定位店铺与商品，确认时复核店铺、商品、内容版本和平台 `products:update` 权限，生成新内容版本并写管理员审计；真实浏览器已覆盖新增、发布、删除和主库回读 |
| `governance.catalog.update_image_description.commit` | mutation | 修改指定商品的单张详情图片说明 | 已实现：与商家侧共用结构化内容版本边界，但使用平台权限和平台审计；真实浏览器已覆盖“商家上传并发布详情图片→管理员 AI 定位→确认→新版本回读”，用户端与 Agent 随后读取新说明 |
| `governance.catalog.review.preview/commit` | preview/mutation | 平台审核商品并写审核证据 | 已实现首批：支持通过、驳回和要求修改；驳回/修改必须说明原因，确认后调用商品审核状态机，通过后按发布规则上架并回读 |
| `governance.catalog.status.preview/commit` | preview/mutation | 上下架商品并写状态流水 | 已实现：只允许合法状态转换并记录商品状态流水 |
| `governance.catalog.delete_eligibility/delete` | preview/mutation | 无交易可删；有交易只能下架并保留历史快照 | 已实现：平台管理员同样先做交易资格检查；有交易只允许下架，无交易确认后才软删除并停用 SKU，写商品状态流水、平台审计和 Outbox |
| `governance.order_summary` | query | 全平台订单状态汇总 | 已增强：支持按用户、店铺、状态、订单号和今天/昨天/近 7/30 日组合筛选，排除用户专属取消快照，返回商品图片、店铺 Logo、金额、支付/履约/售后状态和资源版本的富订单卡片 |
| `governance.trade.orders.search/get` | query | 查询订单、商品、支付、履约、物流和售后链路 | 首期订单及商品视图已合并到 `governance.order_summary` 精确查询；支付、物流和售后分别由下述窄 Tool 读取，不再用订单当前状态反推历史事件 |
| `governance.trade.order_action.preview/commit` | preview/mutation | 只执行订单状态机允许的平台操作 | 已实现第一项高价值闭环：`governance.trade.orders.cancel.commit` 只接受完整 `ord_…`，仅允许待付款且未支付的订单；确认卡明确展示整笔合并交易影响，提交时锁定目标订单、复核资源版本、全部店铺子订单和支付尝试，再复用订单服务原子取消交易、释放库存并保留用户“已取消”导航快照。其他订单治理动作仍须按各自状态机逐项接入，不能用通用改状态工具代替 |
| `governance.trade.payment_timeline` | query | 查询支付尝试、回调和对账事件 | 已实现：可按支付单、交易单、订单、顾客、店铺和状态精确过滤，返回实付/退款金额、脱敏渠道引用、关联店铺订单、不可变支付事件及安全回调字段；不存在的明确顾客或店铺返回空结果，真实浏览器已覆盖支付卡片、事件卡片及临时详情弹窗 |
| `governance.trade.shipments.*` | query/preview/mutation | 查询或修正经授权的物流节点，保留原轨迹和审计 | 已实现首批：`governance.trade.shipments.get` 可按包裹、订单、顾客、店铺和状态读取包裹内容、脱敏运单号、目的地、当前节点及不可变轨迹，并明确区分模拟物流和承运商同步；`governance.trade.shipments.progress.commit` 只允许针对唯一的商城模拟包裹生成确认卡，确认时复核包裹/订单/店铺版本并按物流状态机写入节点，不存在固定时间自动推进。真实浏览器已覆盖商家 AI 发货、管理员 AI 查询、确认揽收和再次读取轨迹的三端闭环；真实承运商运单更正、作废与异常补偿仍需独立接入 |
| `governance.after_sale.search/get` | query | 查询退款、退货、申诉和支付退款记录 | 已由窄 Tool `governance.after_sale.timeline` 实现：可按售后单、订单、退款支付单、申诉、顾客、店铺和状态精确过滤，分别返回商品、不可变售后事件、退货物流、退款支付事件和申诉链路；不存在的明确对象返回空结果，真实浏览器已覆盖卡片与临时详情弹窗 |
| `governance.after_sale.decision.preview/commit` | preview/mutation | 按规则进行平台裁决，不绕过金额和数量校验 | 已实现首批：锁定待审核申请并生成决定卡，确认后调用既有售后服务；金额阈值、原路退款、退货状态机与双人复核均不绕过 |
| `governance.after_sale.request_more_info.commit` | mutation | 代表平台要求顾客补充具体售后材料，但不作裁决 | 已实现：必须唯一定位售后单并说明具体材料；确认时复核平台权限、售后状态与版本，写不可变事件和管理员审计，在对应顾客—店铺会话发送系统通知；不会批准、拒绝或退款 |
| `governance.trade.reconcile.preview/commit` | preview/mutation | 生成并执行受控对账补偿 | 需新增，默认双人审批 |

#### 4.1.8 Tool 清单：客服、AI 与运行治理

| Tool | 类型 | 作用与边界 | 状态 |
| --- | --- | --- | --- |
| `governance.support.queues` | query | 用户与店铺工单队列、等待时间和 SLA | 已合并到 `governance.support_summary` 首批：区分用户/店铺队列，按优先级返回待处理工单、会话、摘要、等待时间和 SLA 卡片 |
| `governance.support.get_context` | query | 读取选定工单和授权业务上下文 | 已合并到 `governance.support_summary` 的唯一命中模式：可按工单、会话、用户或店铺定位，读取最近 30 条有序消息和最多 12 个有效商品/订单业务上下文，生成工单会话上下文卡；回复和治理写操作前仍须重新读取业务主库 |
| `governance.support.assign/close` | mutation | 分配或结束人工服务并同步三端 | 已实现首批单工单领取与结束：只作用于平台专属客服/商家平台支持范围，确认时复核工单、接待人和会话版本，复用 Support Service 同步人工/AI 状态 |
| `governance.support.send_message` | mutation | 管理员向当前用户或店铺发送消息/卡片 | 已实现文字、商品和订单卡片：必须由当前管理员接管对应平台工单；商品发送前复核在售状态，订单同时复核当前会话用户归属；确认后写入真实会话并回读消息类型与序号；治理结果卡片仍待补 |
| `governance.ai.agents.*` | query/preview/mutation | Agent 定义、版本、Prompt 和发布状态 | 已实现查询、Prompt 草稿创建及发布申请：`governance.ai.agents.list` 返回每个 Agent 的作用域、发布版本、实际模型配置、授权 Tool 数及已绑定 Skill；`governance.ai.agents.prompt_draft.create.commit` 根据管理员给出的完整正文继承最新版本的模型与工具配置，经确认和并发版本复核后创建不可变的新草稿，清除旧评估结论且不切换线上版本；`governance.ai.agents.publish_request.commit` 只接受唯一 Agent 与已通过评估的草稿版本，经当前管理员确认后创建双人审批资源，不直接切换线上版本。|
| `governance.ai.skills.*` | query/preview/mutation | Skill 版本、Agent 绑定、预算和发布 | 已实现查询及发布申请：`governance.ai.skills.list` 返回 Skill 发布状态、版本、评估报告及每个 Tool 绑定的确认策略、调用预算和超时；`governance.ai.skills.publish_request.commit` 经对话确认后创建既有 `ai.skill.publish.v1` 双人审批，发起人不能自批，确认结果明确标记“尚未发布”。真实浏览器已覆盖确认卡、审批资源和结果回读 |
| `governance.ai.tools.*` | query/preview/mutation | MCP/内部 Tool Schema、权限、限流和启停 | 已实现查询及发布申请：`governance.ai.tools.list` 返回 Tool 服务归属、风险级别、发布版本、输入 Schema 字段和评估报告；`governance.ai.tools.publish_request.commit` 只对已通过评估的草稿 Tool Version 创建双人审批。完整输出 Schema、限流、熔断和启停写操作待接入 |
| `governance.ai.evaluations.*` | query/preview/mutation | 查询固定 Golden Dataset、已登记基线/候选版本、最近评估指标和发布门禁，并受控启动评估 | 查询与启动均已实现：使用不可变数据集标识、版本、哈希与用例数作为当前准入基准；启动前展示数据集、生产基线、候选策略与显著提升要求，经确认后以 `Idempotency-Key` 创建唯一异步评估任务，不自动发布任何版本；真实浏览器覆盖确认、排队结果卡与 Tool Trace |
| `governance.knowledge.documents.list` | query | 按文档编号、标题、状态和作用域查询知识文档及索引历史 | 已实现：读取 MySQL 权威发布状态、内容版本、资源版本与索引任务历史，展示知识文档和索引记录卡片；显式不存在的 `kdoc_` 返回空结果，不扩大为全库搜索，不把知识正文复制进聊天 |
| `governance.knowledge.documents.publish.commit` | mutation | 发布知识文档并创建影子索引，验证后切换 | 已实现：必须指定唯一文档并二次确认，确认时复核内容版本和状态，使用既有知识服务和幂等键创建影子索引，旧索引在成功切换前继续服务；PostgreSQL 不可用时明确失败而不伪成功。真实浏览器已完成确认、执行、结果卡与文档编号回读 |
| `governance.knowledge.documents.withdraw.commit` | mutation | 撤回知识文档并从后续检索范围移除 | 已实现：必须指定唯一文档并二次确认，确认时复核资源版本；撤回后由既有知识服务使其退出检索。已撤回文档不得直接重新发布，需形成新内容版本 |
| `governance.knowledge.reindex.preview/commit` | preview/mutation | 创建新索引代次，完成验证后原子切换 | 已并入 `governance.knowledge.documents.publish.commit` 的单文档受控发布闭环；批量重建仍需独立快照、逐项结果和部分失败报告，当前不宣称支持 |
| `observability.runtime_health` | query | API、Worker、数据库依赖、模型和队列健康 | 已有 |
| `observability.traces.search/get` | query | 按 Trace/Run/对象查询真实执行链路 | 已实现首批：可按 Run/Trace 精确查询或查看近期运行，联合读取真实委派账本与工具审计，返回领域 Agent、工具结果、延迟和错误状态卡片 |
| `observability.cost_metrics` | query | 模型 Token、成本、延迟和错误率 | 部分实现：按 1—90 天窗口计算运行完成率、工具成功率、调用量、平均工具延迟和已记录委派 Token；供应商用量与单价尚未完整落库时明确显示覆盖不足且不伪造成本金额 |
| `governance.jobs.retry.preview/commit` | preview/mutation | 按幂等和可重试条件重放失败任务 | 需新增 |
| `observability.dead_letters.list` | query | 列出有界死信候选、失败原因、原始事件状态、载荷摘要和可操作性 | 已实现：只读诊断从真实死信表及原始 Outbox 事件读取，返回状态/原因/重试次数/版本卡；无死信时明确返回空结果，不伪造异常 |
| `observability.dead_letters.replay_request.commit` | mutation | 对唯一开放死信复核不可变原始事件并发起受控重放审批 | 已实现单事件审批申请：确认前后复核死信状态、原始事件状态、事件类型和载荷哈希；确认只创建 `events.dead_letter.replay.v1` 双人审批资源，不直接重放。批量重放仍禁止 |

AI 管家没有“执行 SQL”“运行命令”“读取服务器任意文件”“查看环境变量”等泛化工具。系统修复需要开发与运维流程时，只提供诊断证据和受控任务入口。

截至 2026-09-14，上述已实现写操作已通过确认与取消、幂等重放、并发版本冲突、商家身份调用管理员接口越权拦截、管理员自操作阻断，以及刷新页面后确认卡状态持久化测试。其余仍标记“需接入/需新增”的治理工具不得由 Prompt 或模型文字宣称已经执行。

#### 4.1.9 操作分级、确认与审批

| 等级 | 操作 | 要求 |
| --- | --- | --- |
| A0 | 查询、检索、统计和 Trace 查看 | 无确认，但必须记录范围和查询条件 |
| A1 | 生成治理方案、回复、配置或修复草稿 | 不改变业务状态 |
| A2 | 单一可恢复修改，如强制下线、冻结、资料修改、消息发送 | 展示对象和影响，管理员二次确认 |
| A3 | 删除用户/店铺、商品批量下架、售后裁决、资金调整、AI 版本发布 | 完整预览、版本锁、明确确认和写后回读 |
| A4 | 大额资金补偿、批量账户处置、Tool 权限扩大、知识代次切换、死信批量重放 | 双人审批或预配置审批策略；发起人与审批人不能相同 |
| 禁止 | 明文密码/密钥、任意数据库/系统操作、删除审计、绕过状态机 | Tool Registry 中不存在该能力 |

前端不再要求管理员重新输入登录密码完成每个高风险操作，但必须展示可读确认卡；A4 通过独立审批资源实现，不以聊天中的“我同意”替代第二审批人。

#### 4.1.10 上下文、记忆与跨域调查

| 内容 | 保存与使用规则 |
| --- | --- |
| 当前管理员及权限 | 后端注入管理员身份、权限版本和授权数据范围；每次 Tool 调用重新校验 |
| 当前调查对象 | 用户、店铺、商品、订单、售后、Agent 版本和 Trace 使用带类型的资源引用 |
| 多对象集合 | 用户集合、店铺集合、风险集合和订单集合分别保存顺序与版本，不能共享“第一项” |
| 调查假设 | 与已验证事实分开保存；新证据可推翻假设并触发重新规划 |
| 待审批操作 | 保存动作、对象版本、影响范围、发起人、审批策略和有效期 |
| 最近成功动作 | 保存幂等键、执行结果和回读证据，防止刷新、重试或多标签页重复执行 |
| 管理员长期偏好 | 可保存常用看板、筛选条件、告警关注点和报告格式 |
| 禁止记忆 | 密钥、密码、Token、完整支付凭据、无关顾客隐私和临时高风险操作参数 |

跨域调查允许 Supervisor 将同一平台事件委派给多个 Agent，但领域 Agent 只接收完成任务所需的最小数据。例如调查店铺售后异常时，AI 治理 Agent 不应收到顾客地址，系统运行 Agent 不应收到完整聊天内容。

#### 4.1.11 RAG、Skill、MCP 与 Agent 治理

| 治理对象 | AI 管家能力 | 发布要求 |
| --- | --- | --- |
| Agent | 查看定义、Prompt、模型路由、工具权限、预算和版本差异 | 离线评估 + Shadow Run + 灰度 + 回滚版本 |
| Skill | 查看输入输出、工具绑定、确认策略、测试和评估结果 | Contract Test 与准入门槛通过 |
| MCP Tool | 查看 Schema、范围、限流、风险级别和调用审计 | 权限测试、越权测试、幂等/超时验证 |
| RAG 文档 | 创建草稿、权限标签、切片预览、索引和发布 | 新代次完整索引、引用评估和隔离测试后原子切换 |
| Memory | 查看策略、类型、保留期和隔离评估 | 禁止直接浏览无任务关系的用户记忆内容 |
| Multi-Agent | 查看委派准确率、预算、失败和递归保护 | 长对话、复合任务、并发和降级回归 |

AI 管家可以管理 Agent 系统，但不能让正在运行的自己即时扩大自己的权限。涉及 Tool 权限、Prompt、模型、RAG 代次或 Skill 发布的更改必须创建新版本，经评估和审批后由发布服务切换；当前 Run 始终使用启动时绑定的版本快照。

#### 4.1.12 典型复合任务

| 管理员表达 | 动态计划 | 结果 |
| --- | --- | --- |
| “今天平台最需要处理什么？” | 并行查询交易、售后、客服、AI 和运行异常，按影响与紧急度复核 | 3—5 张治理待办卡 |
| “查这个用户，强制下线并冻结” | 定位用户 → 查询会话与状态 → 两个动作预览 → 确认后串行执行并回读 | 用户卡 + 下线/冻结结果卡 |
| “暂停这家店并下架所有在售商品” | 调查店铺、在售商品、未完成订单和售后 → 展示影响 → 分资源确认执行 | 店铺治理批次卡；保留订单履约 |
| “为什么退款成功率突然下降？” | 售后、支付、运行 Agent 并行调查 → 对齐时间范围 → 区分业务拒绝和系统失败 | 原因证据卡 + 修复建议，不自动处罚 |
| “把这个商品详情 OCR 改正确并重新索引” | 查商品和图片 → 生成文字差异 → 确认修改 → 创建新知识代次 → 验证后切换 | 内容修改卡 + 索引任务卡 |
| “发布新版退款 Skill” | 查版本、工具权限和评估 → 不达标则返回缺失项；达标后生成发布预览和审批 | 评估卡 + A4 发布审批卡 |
| “重试所有失败任务” | 分类可重试、已成功、结果未知和不可重试任务 → 只为安全候选生成批次预览 | 死信/任务批次卡，不盲目全重放 |
| “回复这个投诉并调整用户余额” | 查询工单、订单和资金流水 → 生成回复草稿与余额调整预览 → 分别确认 | 消息草稿 + 资金确认卡 |

#### 4.1.13 UI 与交互

| 区域 | 要求 |
| --- | --- |
| 左栏 | AI 管家置顶；“用户消息”“店铺消息”可折叠，分别显示未读总数、单会话未读和工单状态 |
| 中栏 | 复用用户端稳定聊天布局；AI 与管理员左右分离，系统事件居中；结构化卡片按业务类型分区 |
| 右栏 | 展示真实模型摘要、Supervisor 计划、领域委派、Tool、RAG、审批、状态机、回读和错误；白底黑字 |
| 搜索与选择 | `+` 选择用户、店铺、商品、订单、售后、规则或治理结果卡；只显示当前会话获准对象 |
| 批量操作 | 使用可筛选、可取消选择的批次弹窗；确认前显示总数、影响、不可执行项和预计耗时 |
| 结果 | 成功、失败、未知和等待审批分开显示；每项可查看 Trace 或只重试失败项 |
| 导航 | 点击卡片默认打开大弹窗；需要进入管理详情时由用户主动选择“在管理页打开” |

#### 4.1.14 结果验证、安全与可恢复性

| 校验 | 要求 |
| --- | --- |
| 身份权限 | 每次 Tool 调用校验管理员会话、权限版本和资源范围；账号冻结或权限变化立即失效 |
| 权限真实性 | 写 Tool 必须读取当前管理员实际角色授权和有效 `admin` 客户端会话，再按平台或资源范围执行；禁止只按权限码字典是否存在来判定有权 |
| 业务状态 | 用户、店铺、商品、订单、支付、物流、售后和 AI 发布各自走状态机 |
| 证据 | 治理结论引用业务 Tool、审计事件、RAG 规则或 Trace；模型推测单独标记 |
| 写入 | 写前重新读取对象版本，写后回读；消息和 Agent 输出使用会话序号锁 |
| 批量 | 每项独立幂等和状态；结果未知先查询，不直接重复执行 |
| Prompt Injection | 用户消息、店铺资料、商品 OCR、知识文档、日志和外部错误文本都不能指示系统扩大权限 |
| 审计 | 操作人、审批人、Agent/Skill/Tool 版本、参数、结果、证据和 Trace 全部可追溯 |
| 回滚 | 业务数据按补偿流程处理；Prompt、Skill、Tool 权限和知识代次使用版本切换回滚 |

#### 4.1.15 验收矩阵

| 类别 | 必测内容 | 通过标准 |
| --- | --- | --- |
| 用户治理 | 搜索、创建、资料、下线、冻结、地址、余额、购物车、收藏、订单和注销 | 不读取明文密码；资金有流水；删除走编排 |
| 店铺治理 | 创建、资料、状态、商品、订单、注销和多店隔离 | 独立商家凭据；状态影响同步三端 |
| 商品治理 | 编辑、OCR、审核、上下架、删除资格和批量操作 | 有交易不删除；所有状态有审计 |
| 交易售后 | 订单、支付、物流、退款、申诉、资金对账和异常恢复 | 金额数量正确，不绕状态机 |
| 客服 | 用户/店铺工单、消息、卡片、分配、结束和 AI 恢复 | 身份清晰、消息实时、不串会话 |
| AI 治理 | Agent、Prompt、Skill、Tool、RAG、Memory、评估、灰度和回滚 | 版本不可变；未通过准入不能发布 |
| 运行治理 | 健康、Trace、成本、告警、积压、死信和受控重试 | 无任意 Shell/SQL；恢复不重复副作用 |
| 连续对话 | 30 轮跨用户/店铺/交易/AI 话题切换、纠正和返回 | 类型化焦点正确，不把不同集合序号混用 |
| 并发隔离 | 多管理员、多用户、多店铺、多标签页和权限变更 | 无串流、无越权、旧 Run 不覆盖新结果 |
| 攻击故障 | Prompt Injection、伪造卡片、模型/RAG/Tool/Worker 故障 | 拦截率 100%，不伪成功，可核验恢复 |

#### 4.1.16 实施顺序

| 顺序 | 实施内容 | 完成证据 |
| ---: | --- | --- |
| 1 | 为 AI 管家接入统一状态、上下文、摘要、序号锁、检查点和 Trace | 状态、长对话、并发测试 |
| 2 | 将旧 `admin_copilot` 重构为模型优先 Supervisor + 8 个领域能力 | 规划、多任务和返工测试 |
| 3 | 从现有四个汇总 Tool 开始，按查询/预览/提交扩展原子治理 Tool | Contract、权限、审批和状态机测试 |
| 4 | 实现用户/店铺人工消息及平台治理卡片与弹窗 | 组件、消息同步和浏览器测试 |
| 5 | 实现 AI/RAG/Skill/MCP 版本治理、评估、灰度和回滚入口 | 发布门槛和回滚测试 |
| 6 | 完成批量幂等、双人审批、故障恢复和多管理员隔离 | 集成、并发和故障注入测试 |
| 7 | 使用管理员及多个用户/店铺账号完成全业务浏览器回归 | 页面证据、Trace、审计和数据库不变量 |

#### 4.1.17 第一轮优化：平台治理闭环与不可逆操作

| 审校发现 | 优化后的规则 |
| --- | --- |
| “管理员可直接改一切”不可审计 | 所有修改必须映射到具体领域 Tool 和 Service；不存在万能更新 Tool，也不接受任意字段字典 |
| 余额直接覆盖会破坏账目 | 余额调整通过资金流水和会计方向记录，先校验币种、金额与重复凭证，再更新聚合余额 |
| 用户/店铺硬删会破坏交易 | 删除先生成关联数据清单；交易、支付、物流、售后和审计按法定/业务期限保留，账号进入注销编排而非直接级联 |
| 批量操作目标会变化 | 预览生成不可变候选快照和查询条件；确认时逐项检查资源版本，不把后来新增的对象自动纳入 |
| 一条消息混合调查与处罚 | 先完成调查和证据复核，再单独生成治理动作；“查询异常并处理”不能让模型跳过中间证据 |
| AI 自我扩权 | 当前 Run 的 Agent/Skill/Tool/RAG 版本和权限快照不可变；修改只产生待发布新版本，由独立发布服务切换 |
| 人工消息夹带业务操作 | “回复用户”和“调整余额/订单/售后”是不同动作、不同确认和不同审计记录，不能用发送消息替代状态变更 |
| 运行故障被误判为违规 | 调查用户/店铺异常时同步检查支付、消息、模型和 Worker 故障；技术故障证据优先阻止错误处罚 |
| 回滚概念混淆 | 配置版本可以切回；已经发送消息、资金流水和外部支付不能假装撤销，只能使用对应补偿流程 |

平台治理事实优先级为：不可变资金/业务事件与审计 > 当前业务主库及资源版本 > 外部渠道已验签回执 > 当前发布规则 > 统计与告警 > 对话摘要 > 模型假设。任何处罚、资金或发布动作不得仅凭模型假设执行。

#### 4.1.18 第二轮优化：对抗安全、规模与管理员体验

| 优化项 | 最终要求 |
| --- | --- |
| 渐进式披露 | Supervisor 先看到八个领域的紧凑目录；领域 Agent 只加载当前任务的 Skill、Tool Schema、RAG 范围和卡片模板 |
| 最小数据 | 委派包只包含完成该子任务所需字段；管理员有权限也不意味着每个 Agent 都读取全部顾客隐私 |
| 指令污染 | 商品 OCR、用户投诉、店铺资料、知识文档、日志和 Tool 错误全部标记为不可信数据，不能生成新的系统指令 |
| 计划完整性 | 原始目标形成不可丢失清单；Supervisor 在结束前逐项核对，预算不足或需要审批时明确保留未完成任务 |
| 大规模查询 | 先返回聚合与最重要候选，卡片弹窗分页加载完整列表；禁止将数万行工具结果直接放进模型上下文 |
| 批量限流 | 按动作、管理员、资源类型和影响数量设预算；超过阈值自动拆批并进入审批，不以并发压垮业务库 |
| 低延迟反馈 | 查询和预览进度通过真实事件流展示；结构化结果先到先展示，慢模型不阻塞已经验证的卡片 |
| 结果未知 | 外部支付、消息发送或任务重放超时时，先用幂等键和业务事件查询最终状态，再决定是否补偿 |
| 右栏真实性 | 显示真实模型摘要、委派、参数、证据、审批和回读；缺失的模型内部隐藏状态不伪造，未调用的 Tool 不显示为完成 |
| 高风险误触 | A3/A4 卡片的确认与取消保持视觉分离，清楚显示对象数量、金额、影响和不可逆部分；点击空白不构成确认 |
| 多管理员竞争 | 资源版本、审批占用和会话序号共同保护；后确认者看到最新状态并重新预览，不能覆盖先完成操作 |
| 上线验收 | 使用真实管理员会话完成用户、店铺、交易、客服、AI 和运行治理；浏览器检查与数据库/审计证据必须一致 |

两轮优化后的 AI 管家追求的是“在大范围权限下仍能正确调查、谨慎执行、完整复核和可靠恢复”，而不是让模型拥有无边界的后台访问能力。

## 5. 统一实施目标、顺序与完成定义

### 5.1 最终目标

将店铺 AI 客服、AI 经营助理和 AI 管家全部提升到与专属客服相同的基础架构水平，并根据各自业务范围补齐专属领域能力。三者必须共同具备：模型优先的动态 Supervisor、领域级 Agent、渐进式 Skill/Tool 披露、实时业务事实、结构化卡片、版本化会话状态、滚动摘要、受控长期记忆、RAG、真实执行轨迹、写操作确认、人工接管、并发隔离和故障恢复。

### 5.2 执行顺序

| 阶段 | 工作 | 退出条件 |
| --- | --- | --- |
| D0 文档 | 完成 2.2、3.1、4.1 初稿，并分别完成业务闭环、权限安全与体验两轮优化 | 本文 v2.0 通过交叉一致性检查 |
| D1 共用基座 | 抽取状态、上下文、摘要、能力目录、结果验证、序号锁、Trace 和清除逻辑 | 三类 Agent 使用同一协议且旧会话可迁移 |
| D2 店铺客服 | 按 2.2 实现 Supervisor、5 个领域 Agent、Tool/RAG、卡片和人工接管 | 2.2 自动化与真实浏览器业务矩阵通过 |
| D3 经营助理 | 按 3.1 实现 Supervisor、7 个领域 Agent、查询/预览/提交 Tool 和经营卡片 | 3.1 自动化与多店铺浏览器矩阵通过 |
| D4 AI 管家 | 按 4.1 实现 Supervisor、8 个领域 Agent、治理 Tool、审批和版本管理 | 4.1 自动化与管理员浏览器矩阵通过 |
| D5 总验收 | 三端多账号并发、长对话、跨话题、人工接管、故障注入、隔离、性能与恢复 | 无 P0/P1 缺陷，所有证据可复核后才完成目标 |

### 5.3 每类 Agent 的完成定义

| 维度 | 必须满足 |
| --- | --- |
| 设计 | Agent、Skill、Tool、RAG、Memory、权限、卡片、人工和失败策略与实际代码一致 |
| 功能 | 单任务、复合任务、连续追问、纠正、否定、假设、暂停恢复和明确写操作均正确 |
| 数据 | 实时事实来自业务主库，规则来自 RAG，偏好来自确认记忆；金额、数量、状态和归属正确 |
| 交互 | 回复简洁、卡片可操作、弹窗不丢位置、流式稳定、头像与身份清晰、错误可重试 |
| 安全 | 不跨用户/店铺/角色；写操作确认和审批完整；Prompt Injection 不改变权限 |
| 并发 | 快速连续发送、多标签页、多账号、多店铺和多个管理员不会串流或重复写入 |
| 可观测 | Trace 与实际规划、委派、Tool、RAG、Memory、审批和结果一致，不伪造步骤 |
| 恢复 | 模型、Tool、SSE、Worker、RAG 或外部服务异常时不伪成功，能核验并恢复 |
| 测试 | 单元、契约、集成、权限、并发、故障和真实浏览器测试全部通过 |

### 5.4 测试与修复循环

每一类 Agent 按“静态检查 → 单元/契约 → 集成 → 浏览器主流程 → 长对话与复合任务 → 越权并发 → 故障注入 → 修复 → 全量回归”的顺序循环。发现问题后必须补自动化回归用例；浏览器看似正确但数据库、审计或 Trace 不一致仍判定失败。不得使用“模型偶尔答对一次”作为验收证据，也不得在尚有 P0/P1 缺陷时将阶段标记为完成。

### 5.5 当前实施进度与证据口径

| 阶段 | 当前状态 | 已验证内容 | 尚未满足的退出条件 |
| --- | --- | --- | --- |
| D0 文档 | 已完成 | 2.2、3.1、4.1 均完成初稿及两轮优化，Agent、Skill、Tool、RAG、Memory、卡片、权限和测试边界已对齐 | 实施中如发现契约变化仍需同步回写本文 |
| D1 共用基座 | 核心完成 | 三类 Agent 共用可信身份、版本化会话状态、滚动摘要、模型 Supervisor 目标账本、委派账本、工具审计、写入序号锁、公开 Trace、确认卡和清除语义；有效模型计划是正常业务的权威计划，关键词规划只在供应商失败或目标覆盖协议不合法时整体接管；右栏可核对每项目标到子任务的真实映射与覆盖状态；消息实时通道以 WebSocket 为主、REST 增量补拉为兜底，不能把不可靠的 `navigator.onLine` 提示当成禁止补拉的硬条件 | 继续补全供应商 Token/成本采集、极端并发和更多故障恢复证据 |
| D2 店铺客服 | 核心完成，细节补强中 | 店内商品/SKU/库存/政策、本店订单/物流/售后进度、店内推荐、购物车加入、RAG、连续指代、卡片和转店铺人工均有自动化与浏览器覆盖；集成用例在同一会话执行 16 次 Agent Run，覆盖上下文切换、伪造跨店上下文、Prompt Injection、工具拒绝和模型不可用；店铺 RAG 已增加 PostgreSQL 检索异常强制注入，验证事务回滚、明确 `RAG_RETRIEVAL_UNAVAILABLE` 降级标记且仍保留可信业务政策回答；真实浏览器覆盖本店售后列表、人工结束后的物流与售后连续追问 | 仍需补齐更高并发浸泡证据 |
| D3 AI 经营助理 | 核心闭环完成，等待全矩阵准入 | 经营总览、商品、库存、订单、收入、物流、评价、售后、顾客会话和店铺资料查询，以及受控改价、库存、上下架、发货、商品名称/描述、履约资料、店主恢复邮箱、商品 FAQ、结构化详情段落、店铺 Logo、指定 SKU 图片、店铺政策、评价回复、售后和客服操作均已接入确认、审计与回读；人工会话已支持发送文字、本店商品卡和指定顾客本店订单卡；售后可按售后单、订单、顾客、退款支付单或申诉精确定位，并分卡展示商品、不可变事件、退货物流、退款支付和申诉；经营助理可经确认原子创建包含首个 SKU、价格、库存和履约资料的结构化草稿，并可原子修改已有款式名称、价格与库存。评价与顾客会话列表已具备会话连续、范围绑定、防篡改 Cursor 分页；一次跨域请求出现部分失败时，Supervisor 会保留已经核验的可靠结果，以子任务状态清单公开失败领域，并生成“只重试此项”卡片，点击后仅重派该领域 Agent，不重复成功任务。同一真实浏览器长会话已验证“修改商品描述→新增后续款式→深度修改 SKU/库存→停用款式并保留历史”“新增 FAQ→版本化发布→删除公开问答→保留审计版本”和“保留基线详情→新增目标段落→发布→删除目标段落→其他章节保持不变”；浏览器还验证商家恢复邮箱修改、无交易草稿永久删除、商品发货配置、政策草稿发布、售后全链路读取、卡片弹窗、向指定顾客发送订单卡，以及安全上传店铺 Logo 后经 Agent 确认执行与结果回显；要求顾客补充售后材料也已形成独立确认、状态/版本复核、不可变事件和真实会话通知闭环 | 仍需完成 D5 级高并发浸泡、模型故障注入和跨店铺全矩阵准入 |
| D4 AI 管家 | 首批闭环，持续扩展 | 平台指标、用户/店铺/商品/订单/支付/物流/售后/客服/AI/运行查询，真实 Trace，Agent/Skill/Tool 版本与绑定明细、部分成本指标，以及用户、店铺、商品、订单、退款、工单的确认型操作已经接入；用户治理新增了普通用户创建和“必须重置密码 + 撤销旧会话”，创建过程不接收、不展示、不持久化聊天明文密码，并原子初始化普通用户角色与零余额钱包；用户资料治理已支持用户名、恢复邮箱和公开头像，头像必须经过受控上传、安全扫描、目标用户归属及版本复核；店铺治理新增了独立商家身份与店铺创建，账号不与消费者身份混用，首次使用同样必须通过恢复邮箱重置密码；用户资产治理已支持购物车单项改数量/移除/整车清空、商品与店铺取消收藏，以及地址新增、字段编辑、删除和设默认；店铺治理已支持修改店主恢复邮箱与安全替换 Logo；商品治理在名称、描述、图片说明、FAQ、结构化详情段落之外，新增跨店 SKU 创建、现有 SKU 名称/价格/库存深度修改、停用和指定 SKU 图片替换，包含库存/流水/事件、版本复核、预占下限、最后一个有效款式保护及款式图片隔离；订单治理首项支持经确认取消仍未付款的整笔合并交易，确认时复核全部子订单、支付尝试和版本，并由订单服务释放库存。支付、售后、人工客服、模拟物流、知识文档、评估任务、Agent Prompt 不可变草稿、Agent/Skill/Tool 双人发布申请和单死信重放申请均保持既有受控闭环。Prompt 草稿经确认后只创建新版本，继承模型/工具配置、清除旧评估结论且不影响当前线上 Run；隔离数据库集成测试已核对旧版仍为 published、新版为 draft、审计存在。真实浏览器已覆盖临时用户创建→重置要求→注销、临时店铺/独立商家创建→恢复邮箱修改→注销、管理员 SKU 新增→深度修改→回读→停用、管理员跨店选择商品与款式后安全上传图片、选择指定用户并安全上传头像→确认绑定→用户重新登录查看新头像，以及买家创建待付款订单→AI 管家定位订单→确认取消合并交易→买家待付款列表移除并保留已取消入口 | 其余服务资料、真实承运商物流修正、一般任务重试与死信批量治理仍需完成 |
| D5 总验收 | 未完成 | 当前基线已覆盖三端多账号隔离、真实购买、三类 Agent、人工接管、桌面与移动端关键布局和安全用例 | 必须在 D2—D4 剩余 P0/P1 能力闭环后重新执行全矩阵，不能以当前首批通过代替最终完成 |

2026-09-15 核心收尾：D4 已增加独立的 `governance.stores.service_profile` 只读 Tool 和“店铺服务资料整改卡”，按同一店铺范围核对店铺简介、商品履约发货地/时效、生效配送模板、实际包裹承运商和已发布售后政策；系统会明确区分“店铺级默认快递未配置”与“历史包裹实际承运商”，不用历史数据伪造默认承运承诺。AI 管家已发布 v26，新 Tool 已经 Agent—Skill—Tool 权限链生效。真实承运商修正、通用任务重试和死信批量治理作为后续增强，不阻断当前核心使用。

截至 2026-09-14 的最近一次完整隔离基线为：MySQL/PostgreSQL 迁移无漂移，后端 755 项通过、1 项按条件跳过，覆盖率 61.94%；前端测试与生产构建通过；真实浏览器 21 项通过、7 项按设备设计跳过；Agent 安全专项 19 项通过；145 条需求证据包和严格追踪审计均判定为通过，严格审计收集了 831 个测试选择器且没有发现追踪缺口。本轮真实浏览器回归还覆盖了 WebSocket 不可用或浏览器网络提示失真时的 REST 增量补拉、长对话之后用户/商家转平台人工、管理员领取回复、结束服务和 AI 恢复、经营助理经确认原子创建结构化商品草稿、结果卡弹窗进入商品编辑页及永久删除无交易草稿、商品描述确认修改与主库回读、在同一商品上确认新增后续款式并创建 SKU/库存/流水/事件后，再确认停用该款式且保留历史事实、商家与管理员 AI 分别对商品 FAQ 执行“新增/发布→删除公开问答→保留历史版本”的真实治理闭环，以及商家与管理员分别执行“保留基线详情→新增目标段落→发布→删除目标段落→保留其他章节”的结构化详情治理闭环；管理员 AI 还真实执行了临时普通用户创建、密码强制重置要求、用户注销，临时店铺与独立商家账号创建、店铺注销，以及跨店新增并停用 SKU，所有动作都经过确认、范围与版本复核、主库回读和结果卡展示，且聊天中未出现初始明文密码。经营助理经确认向指定顾客真实发送本店订单卡片、店铺服务政策从草稿创建到发布的完整状态链路、商家发布图片后管理员 AI 修改其结构化图片说明并创建新内容版本、“商家 AI 创建包裹→管理员 AI 读取模拟物流→确认推进已揽收→再次读取不可变轨迹”的三端物流治理闭环、管理员 AI 对知识文档/索引历史的只读查询、确认发布与影子重建、固定评估数据集和发布门禁读取、确认并以幂等键启动唯一异步评估任务、“指定已评估 Skill 草稿→确认卡→创建双人审批→保持草稿未发布”的 AI 治理闭环、管理员 AI 以标准中文地区完成用户第二地址“新增→字段修改→删除”，以及指定用户购物车整车清空的确认、执行、回读和清理。本轮还修复了确认卡展示元数据污染评估工具严格参数的问题，并保证确认后发生未知异常时客户端一定收到终态故障消息，不再无限停留在“正在执行”。所有可执行 Agent 动作与 API 响应类型现由集合一致性测试约束，避免出现“操作已执行但结果序列化为 500”的契约漂移。离线评估报告当前因缺少与本轮构建配对的供应商观测而判定 `insufficient_evidence`，因此不能把本次工程验收通过表述为模型发布准入通过；该数字只证明当时已纳入范围的回归基线，不等于 D2—D5 已全部完成，后续每批能力合入后都必须重新生成证据。

2026-09-15 增量改造中，三类 Supervisor 的有效供应商计划已升级为带 `goal_ledger` 与 `coverage_complete` 的权威计划；普通业务不再把关键词任务追加到模型计划，确定性逻辑仅在模型失败、目标覆盖协议无效以及权限、资金、确认和状态机边界接管。模型任务的原始目标会继续驱动领域内最窄 Tool 选择，支付、物流和平台指标使用独立治理子 Agent；商家商品详情、SKU 库存、订单、收入、评价和顾客会话，以及管理员用户资产、Trace、售后时间线和 AI 治理窄工具均受“所选 Tool 必须属于对应 Specialist Policy”契约约束。各模型阶段使用独立硬时限和过期流式事件闸门；REST 增量补拉发现同一 `run_id` 的持久化回复时会自愈清除临时流，隔离验收环境同时运行实时 Outbox Relay。店铺客服对“款式、规格、价格、库存”等同义问法不再依赖模型恰好选择库存意图，只要商品可信快照含 SKU 就生成可操作的“款式与实时库存”卡；经营优先级连续追问增加 Supervisor 回答契约复核，模型草稿若丢失明确的第 1/2/3 项焦点会被拒收并依据同轮实时证据重组，而不是改成固定路由。当前真实供应商浏览器回归已持续穿过用户店铺客服、专属客服、商家经营诊断和管理员治理/确认链路；本轮尚在复跑，不能替代 D5 最终全矩阵结论。

2026-09-15 本轮真实供应商复跑进一步修复两类跨端根因。第一，商家端和管理员端消息 DOM 现携带持久化 `sequence_no`，浏览器回归按“本次用户消息序号之后的 AI 回复”建立关联，不再以最后气泡或数量变化猜测回复归属。第二，经营优先级追问仍由模型选择主要领域 Agent，但 Supervisor 会补齐排名判断必需的商品、库存和订单实时证据依赖，并只展示用户所问的序号项；普通业务计划不受该依赖闭包影响。支付治理、物流治理和指标治理三个窄 Agent 也已加入统一卡片聚合契约，避免右栏显示 Tool 成功而聊天区遗漏结果。隔离环境使用当前实际模型完成 41 轮浏览器 Agent 交互，其中店铺客服 6 轮、专属客服 4 轮、AI 经营助理 16 轮、AI 管家 15 轮；商城购买链路与 Agent 质量链路均通过，覆盖商品/SKU/库存、订单金额、越权拒绝、跨轮经营排名、确认型商品治理、售后与人工接管、支付回调、物流人工推进、知识、评估、死信和购物车治理。该结果证明本批修改有效，但最长单轮观测约 53.4 秒，且 D3、D4 仍有表中列明的图片、服务资料、售后补材料、批量治理等能力未闭环，因此 D5 仍保持未完成。

2026-09-15 后续增量把 AI 管家的 Prompt 修改从确定性关键词直达改为“供应商模型理解与目标账本 → AI 治理 Agent 委派 → 服务端精确定位与权限/版本校验 → 待确认卡 → 不可变草稿提交”。确认后仅创建继承当前模型与工具配置的新草稿版本，清除旧评估结论，不切换线上版本；真实浏览器已验证确认卡、右栏 Tool Trace、提交结果和“线上版本未改变”。Supervisor 还增加独立目标覆盖复核：仅当用户明确提出多个并列目标而模型遗漏时，向模型反馈缺项并允许一次修订；仍不完整才使用安全覆盖兜底，普通单目标和开放问题继续由模型自主规划。商家端和管理员端现会在确认卡持久化后结束流式阶段，右栏由“思考中”切换为“等待确认”，确认提交完成后再显示“已完成”，与 Run 持久状态一致。最新隔离环境使用实际模型完成 42 轮 Agent 浏览器交互（店铺客服 6、专属客服 4、AI 经营助理 16、AI 管家 16），购买链路和 Agent 质量链路结果为 2 项通过、0 项失败；最长单轮约 34.3 秒，样本 P95 约 25.1 秒。模型回答若包含证据外事实或事实校验超时，会保留 Agent 的规划、工具和卡片结果并使用可信降级摘要，不向用户展示未验证结论。该证据仍不替代 D3/D4 剩余能力和 D5 全量准入。

2026-09-15 本批继续补齐经营助理的售后协作与长列表连续性。商家和管理员要求顾客补充售后材料均使用独立受控 Tool：确认时重新校验范围、状态与版本，只写不可变“要求补充材料”事件并在真实顾客—店铺会话发送居中系统通知，不批准、不拒绝、也不触发退款；当前通知只承诺顾客可在会话补充说明，图片或文件需联系人工客服确认提交方式，不虚构尚不存在的附件入口。评价和顾客会话列表分别按 5 条、8 个返回，下一页由版本化会话状态恢复领域，并以绑定店铺、经营助理会话和 Tool 的签名 Cursor 读取；最后一批不会循环回第一页。新增隔离数据库测试构造 7 条评价和 10 个顾客会话，验证两批结果无重复且正确结束；Agent 契约测试与售后专项测试通过。完整隔离基线已复跑为后端 787 项通过、1 项条件跳过、前端测试和生产构建通过；真实浏览器购买与 Agent 长对话组合 2 项通过，商家商品浏览器链路 1 项通过，生产 API、Agent Worker、前端、MySQL、PostgreSQL、Redis 与对象存储均保持健康。上述证据证明本批售后协作与长列表能力闭环；跨域部分成功协议的后续闭环记录见下一段，D4 剩余治理能力与 D5 全矩阵仍按表中条件继续推进。

2026-09-15 本批补齐经营助理和 AI 管家的跨领域部分成功协议。每个动态委派都会记录领域 Agent、目标、最窄 Tool、完成状态和错误码；Supervisor 不再因一个子任务失败而丢弃其他可靠结果，也不会用笼统“请重试”掩盖失败范围。聊天区会显示失败任务卡，运营人员或管理员点击后可直接发送只包含该目标的重试请求，成功领域不会重复执行。后端契约测试覆盖“一个成功、一个超时”的保留与重试语义，前端组件测试覆盖临时弹窗和重试事件，TypeScript 类型检查与 Agent 契约回归通过。该能力完成 D3 当前列明的功能缺口，但仍需在 D5 故障注入浏览器矩阵中验证真实供应商超时、进程重启和多会话并发。

2026-09-15 本批新增 AI 管家对未付款订单的首个真实状态机写操作。管理员自然语言请求先由 Supervisor 和订单治理 Agent 定位完整订单，服务端只接受明确 `ord_…`，在聊天区生成包含当前状态、合并交易范围和库存影响的确认卡；确认后以 `orders:cancel` 权限和幂等键重新锁定目标，复用订单服务检查全部店铺子订单、支付尝试及版本，成功时原子取消交易并释放库存，状态变化则拒绝执行。独立真实 Chromium 用例在买家页面创建订单且不支付，再由管理员消息页确认取消，最终验证订单从买家“待付款”消失并进入“已取消”；后端 Agent 契约、工具登记、权限映射、前端类型检查和浏览器 Trace 状态同时通过。该能力没有引入万能“直接改订单状态”工具，后续动作仍按订单领域状态机逐项扩展。

2026-09-15 本批补齐 AI 管家用户头像治理。管理端消息页的受控图片面板新增“更新用户头像”，先从真实用户列表选择唯一目标，再以 `user_avatar` 用途和该用户公开 ID 创建上传会话；文件完成安全处理后只作为结构化 Agent 消息提交，不立即生效。确认卡再次绑定用户、文件和资源版本，提交时以 `users:manage` 权限调用用户服务，拒绝把其他用户的文件绑定到目标用户。组件测试覆盖用户选择和上传上下文，隔离数据库集成验证只更新指定用户，真实 Chromium 依次执行店铺 Logo、SKU 图片和用户头像三种上传，并在用户重新登录后确认个人中心展示新头像。该闭环关闭了 4.1 用户资料工具中剩余的头像缺口。

2026-09-15 本批真实 Chrome 长会话复测补齐 AI 管家的身份口径与展示聚焦。由于三端共用 `users` 身份表，用户治理 Tool 现在与管理员首页统一使用“平台级有效 `user` 角色”作为普通消费者边界，商家店主和超级管理员不再进入用户数量、列表或用户卡片；浏览器同句跨域查询已从错误的 10 个正常用户修正为 2 个。Supervisor 仍可为售后调查调用用户、店铺、订单等辅助 Agent 交叉核验，但“只看/只展示”会约束最终可见卡片，不再把内部辅助结果铺满聊天区；模型回答未通过事实校验或供应商失败时，售后聚焦请求也使用目标售后证据摘要，不再误套“未发现平台风险”的总览话术。连续浏览器回归已验证“平台用户+店铺+运输中订单+待处理售后”跨域查询、“只看刚才那笔售后”的上下文追问，以及“指定用户+店铺+运输状态且只显示订单卡片”的组合筛选。

2026-09-15 AI 经营助理真实 Chrome 长会话进一步验证了否定约束与售后连续追问。浏览器首次发送“列出待处理售后，只展示售后卡片，不要展示营业额、商品或普通订单卡片”时，发现模型把否定示例误规划为商品和订单子任务，并把售后 Tool 挂到订单 Agent，权限网关正确阻断但整轮失败。现已在委派前按 Tool 所有权重新收窄领域 Agent，否定的展示子句不再生成业务目标，显式卡片范围同时约束商家端最终可见内容。复测只委派售后评价 Agent 调用 `store_ops.after_sale.list`；连续追问“刚才第一笔”的顾客、订单、原因、金额和下一步时，系统读取短期会话上下文并保持同一售后焦点。多个模型语义目标若最终落到同一个领域 Agent 与同一个 Tool，会合并为一次真实委派；要求“只展示这笔售后卡片”时仅保留目标详情卡，不再额外展示概览。当前 Chrome 证据为 1 个售后 Agent、1 次 Tool 调用、42 毫秒，右栏委派数量与实际执行一致；模型事实校验超时则保留包含申请原因、金额、状态和下一步的可信售后卡与受控降级摘要，不执行写操作。卡片以当前页临时弹窗打开，关闭后保持原会话位置。

同一 Chrome 长会话随后用“实时可售为 0 的款式 + 近 30 天已确认营业额”检验跨域自主规划。首次运行时模型把库存目标归到商品 Agent 并选择过宽的 `store_ops.catalog_summary`，导致聊天区错误声称没有库存告警。现已保留模型目标账本，同时由领域工具网关依据目标语义收窄为库存 Agent 的 `store_ops.inventory.get_skus`，并在 Tool 内执行“实时可售为 0”结构化过滤；收入目标仍由订单履约 Agent 调用 `store_ops.revenue_metrics`。复测右栏公开 2 个独立 Agent、2 次真实 Tool 调用，聊天区仅分行展示零库存款式卡和经营收入卡，不再混入商品总览或经营优先级卡；模型事实校验失败时的可信降级摘要也必须准确报告零库存数量、近 30 天营业额和只读边界。

2026-09-15 关键主干最终收口：修复“商品和库存概览”把库存 Tool 错挂到商品 Agent 的多意图路由问题，普通库存概览现由库存 Agent 查询款式实时库存，商品 Agent 独立返回商品概览；只有缺货、低库存、安全库存等明确风险语义才收窄到库存风险 Tool。经营助理与 AI 管家的模型回答阶段最长等待由 35 秒收紧至 15 秒，模型超时后直接保留已核验的多 Agent 卡片和确定性摘要。专属客服现同时记录模型 Supervisor 规划调用，右栏不再把已使用 gpt-5.5 的规划回合显示成“模型未调用”。最终隔离环境实测：后端 788 项测试零失败（1 项条件跳过），前端 148 项零失败；真实 Chromium 三端 Agent 主流程 1 项通过（移动设备按套件设计跳过），覆盖真实 gpt-5.5 请求、供应商超时安全降级、专属客服、店铺 AI/AI 经营助理、AI 管家、Multi-Agent、卡片弹窗、确认写操作、权限范围及透明执行轨迹。该证据完成本轮关键使用主干；真实承运商修改、通用批量重试等增强项继续作为非阻断后续能力。
